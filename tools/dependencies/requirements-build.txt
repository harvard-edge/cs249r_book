# MLSysBook Python Dependencies - Container/Build Only
# Minimal dependencies for container builds and production deployments
# Excludes heavy ML libraries (PyTorch, etc.) to save ~3-4GB

-r requirements/production.txt