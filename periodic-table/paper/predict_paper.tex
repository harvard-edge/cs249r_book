
\begin{table*}[t!]
\centering
\caption{\textbf{The Periodic Table as a Generative Framework.} A summary of how the Constraint-Driven Structural Search mechanically derives state-of-the-art ML systems papers. Given a binding physical constraint, the framework filters the 90 primitives down to a few candidates, from which the architectural intervention deterministically follows.}
\label{tab:predict-the-paper}
\small
\begin{tabularx}{\textwidth}{@{} >{\bfseries}l l l X @{}}
\toprule
\textbf{System / Paper} & \textbf{Binding Constraint} & \textbf{Filtered Candidates} & \textbf{Chosen Intervention (Element)} \\
\midrule
FlashAttention & HBM Capacity ($n^2$ interm.) & Tiling (Ti), Fusion (Fs), Recompute & \texttt{Tiling (Ti)} + \texttt{Fusion (Fs)} \\
ZeRO-1 / ZeRO-2 & HBM Capacity (Opt. States) & Sharding (Sd), Checkpointing & \texttt{Shard (Sd)} across Data Parallel ranks \\
ZeRO-3 / FSDP & HBM Capacity (Weights) & Sharding (Sd), Offloading & \texttt{Shard (Sd)} weights + \texttt{Prefetch (Pf)} \\
Megatron-LM (TP) & HBM Capacity (Weights) & Sharding (Sd), Pipeline (Pl) & \texttt{Shard (Sd)} activations/weights locally \\
PagedAttention & HBM Fragmentation (KV) & Caching (Cc), Virtualization (Vr) & \texttt{Virtualization (Vr)} (OS page tables) \\
GPipe / PipeDream & HBM Capacity (Monolithic) & Pipeline (Pl), Sharding (Sd) & \texttt{Pipeline (Pl)} across pipeline ranks \\
Continuous Batching & GPU Utilization (idle compute) & Scheduler (Sc), Batching & \texttt{Scheduler (Sc)} (iteration-level batching) \\
Quantization (PTQ/QAT) & HBM Bandwidth / Capacity & Quantize (Qz), Sparsify (Sp) & \texttt{Quantize (Qz)} (reduced precision) \\
\bottomrule
\end{tabularx}
\end{table*}
