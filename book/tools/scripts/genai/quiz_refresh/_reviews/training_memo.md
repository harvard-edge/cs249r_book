# training quiz-refresh memo

- **Chapter**: vol1/training (position 8 / 33 in reading order)
- **Anchors processed**: 9 sections + 22 subsections = 31 entries in the JSON (the anchor map listed 10 sections total; the Summary section is included as the 10th and marked `quiz_needed: false`).
- **Quizzes written**: 8 section-level quizzes (+ 1 section marked `quiz_needed: false`) and 21 subsection-level quizzes (+ 1 subsection marked `quiz_needed: false`).
- **Question mix**:
    - Section-level (8 quizzed sections, 35 total questions): 22 MCQ, 8 Short, 5 TF. Per-section: every section sits in the 4–6 window.
    - Subsection-level (21 quizzed subsections, 53 total questions): 29 MCQ, 18 Short, 6 TF. Per-subsection: every quizzed subsection sits in the 2–3 window.
    - Grand total: **88 questions** across 29 quizzed entries, within the expected 75–100 band.
- **Sections/subsections marked `quiz_needed: false`**:
    - `#sec-model-training-optimization-technique-comparison-a89a` ("Optimization technique comparison") — pure comparison table recapping the four preceding technique subsections with no new content; grounded questions on every concept already appear in the four technique-specific subsections and the parent section.
    - `#sec-model-training-summary-2d06` ("Summary") — administrative chapter-close recap. Every takeaway bullet corresponds to a concept already tested in the preceding 9 sections; quizzing here would duplicate or reward rote reading.
- **Prior-vocab reuse**: Prior-chapter terms are used freely in both questions and answers without redefinition, including: Adam, SGD, gradient descent, momentum, mini-batch, batch size, forward/backward pass, softmax, ReLU, sigmoid, activation, chain rule, backpropagation, GEMM, attention, transformer, GPT-2, ResNet-50, Tensor Core (as cross-chapter concept), GPU, TPU, HBM, NVLink, InfiniBand, PCIe, FP16, FP32, BF16, iron law of ML systems, memory wall. No question tests the *definition* of any of these terms — only their application to training-systems reasoning.
- **Novel to this chapter (tested)**: iron law of training performance, MFU (Model FLOPs Utilization), arithmetic intensity applied to training kernels, ridge point, FlashAttention, gradient checkpointing (sqrt(L) strategy), gradient accumulation equivalence, wave quantization, loss scaling, Linear Scaling Rule, master weights, D-A-M taxonomy applied to training, communication tax formula, pipeline bubble.
- **Open issues / flags for human review**:
    1. **Numeric drift risk**: Several questions cite specific numbers that flow from Python cells (e.g., the 84 GB Adam state for 7B, the 2.2-second AllReduce at 100 Gbps, the 208 FLOP/byte A100 ridge point, the 85/45 ms A100 vs. V100 tokenization crossover). I cross-checked each against the prose I read, but a spot-check is worth doing before merge because the cells are re-executed at render time. The A100 ridge-point quoted in Question 1 of the Arithmetic Intensity subsection (208 FLOPs/byte FP16) should in particular be cross-checked against `AcceleratorSpecs.a100_ridge_str` in the rendered output — the text above the cell discusses both FP16 and FP32 ridge points and I used the FP16 value.
    2. **Tokenization-time arithmetic**: The GPT-2 Data Pipeline question in `#sec-model-training-data-pipeline-8e71` uses the 66 ms single-core vs. 80 ms V100 forward comparison. The prose variable for tokenization time is computed from `32 * 1024 / 500_000 * 1000 = 65.536` ms; I rounded to "about 66" in the answer. If the rendered output displays a different rounding, tweak the answer phrasing to match.
    3. **D-A-M taxonomy section reference**: The "Identifying Bottlenecks" question set references `@sec-dam-taxonomy` via the table. I chose to test the D-A-M axes directly rather than rely on cross-chapter content. Fine as-is, but worth noting the D-A-M taxonomy is defined in a sibling chapter; these questions stay self-contained.
    4. **Energy/carbon numbers in the Optimization Impact Summary**: The 275,000 kWh, 115,000 kWh, and 58 percent reduction figures come from `GPT2SummaryCalc` constants. The TF question I wrote reasons about the difference between the 1.7x time speedup and the 2.4x energy reduction — a human verifier should confirm these specific numbers still appear in the rendered table, since the quiz's correctness hinges on them.
- **Paths**:
    - JSON: `/Users/VJ/GitHub/MLSysBook-quiz-refresh/book/quarto/contents/vol1/training/training_quizzes.json.new`
    - Memo: `/Users/VJ/GitHub/MLSysBook-quiz-refresh/book/tools/scripts/genai/quiz_refresh/_reviews/training_memo.md`
- **Validation**: `validate_quiz_json.py` reports `OK: training_quizzes.json.new passes schema + anchor validation` — 0 errors, 0 warnings.
- **Self-assessment**: I think my questions meet the quality bar because each is grounded in a specific numerical claim, table entry, or mechanism from the section (not generic jargon recall); every MCQ's distractors encode a real mental-model failure (confusing data-bound with memory-bound symptoms, mistaking hardware specs for achievable utilization, mixing gradient-accumulation conventions, expecting linear speedup under Amdahl's law); every answer explains *why* the right choice is right and names the specific mistake in the wrong choice; and every learning objective starts with a Bloom's verb. The hardest category to pitch was the Mathematical Foundations parent section, which is largely a framing passage rather than a content-dense section — I leaned on the four-cost-dimension taxonomy there and kept questions conceptual. The Pipeline Architecture section's throughput arithmetic and the Iron Law section's MFU-style calculations are the most quantitatively demanding and should discriminate well among students.
