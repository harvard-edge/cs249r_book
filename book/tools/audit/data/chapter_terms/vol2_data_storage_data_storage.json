{
  "volume": "vol2",
  "section": "data_storage_data_storage",
  "path": "book/quarto/contents/vol2/data_storage/data_storage.qmd",
  "total_terms": 143,
  "terms": [
    {
      "term": "RAID (Redundant Array of Independent Disks)",
      "index_head": "RAID!ML storage",
      "type": "bold_indexed_concept",
      "first_lineno": 165,
      "context": "[^fn-raid-array-redundancy]: **RAID (Redundant Array of Independent Disks)**\\index{RAID!ML storage}: A 1988 Berkeley taxonomy of drive-combining strategies, each trading redundancy against bandwidth [@patterson1988raid]. ML training inverts the database-era default: RAID 0 (striping, no parity) maximizes sequential read throughput at the cost of zero fault tolerance, a safe trade-off because training data is immutable and durably backed in object storage. Choosing RAID 5 or 6 instead wastes bandwidth on parity calculations that protect data already protected elsewhere."
    },
    {
      "term": "I/O wall",
      "index_head": "I/O Wall!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 167,
      "context": "ML workloads systematically invert these assumptions. Training data access is predominantly sequential, streaming through datasets that span hundreds of terabytes. Individual accesses are large (megabytes rather than kilobytes) because models consume batches of images or text sequences. Consistency requirements are relaxed, since slightly stale features rarely affect model quality. Bandwidth demands, however, are extreme: hundreds of gigabytes per second, sustained for days or weeks. The mismatch between what storage was optimized for and what ML actually needs creates the **I/O wall**\\index{I/O Wall!definition} (principle \\ref{pri-io-wall}): when storage throughput cannot deliver training data as fast as accelerators consume it, GPUs idle regardless of their computational power. A storage system that was adequate for 8 GPUs becomes the bottleneck at 64, making the *data pipeline*, not the *model*, the limiting factor. @Sec-appdx-c3-taxonomy-bottleneck-table classifies this wall as a constraint that lives at the intersection of Communication and Compute in the fleet-scale diagnostic framework, so a storage engineer can confirm that the cure is more bandwidth rather than faster accelerators."
    },
    {
      "term": "access pattern",
      "index_head": "Access Pattern",
      "type": "bold_indexed_concept",
      "first_lineno": 350,
      "context": "The first inversion is **access pattern**\\index{Access Pattern}. Where database workloads exhibit *random access patterns* that benefit from seek-time optimization, ML training performs massive *sequential scans*. A training epoch reads every sample once, in whatever order the shuffling algorithm produces. This pattern resembles video streaming more than database queries. Storage systems optimized for random IOPS[^fn-iops-throughput-tradeoff] waste their capabilities on ML workloads, while systems optimized for sequential throughput excel. The distinction is quantitatively dramatic: a Gen4/Gen5 NVMe drive delivers roughly 7--14 GB/s for sequential reads, while small 4 KB random reads can fall near 0.5 GB/s, an order-of-magnitude to 30$\\times$ penalty for the wrong access pattern. The penalty is even more severe on hard drives, where mechanical seek times impose a 100$\\times$ throughput reduction for random access compared to sequential."
    },
    {
      "term": "IOPS (Input/Output Operations Per Second)",
      "index_head": "IOPS!ML irrelevance",
      "type": "bold_indexed_concept",
      "first_lineno": 352,
      "context": "[^fn-iops-throughput-tradeoff]: **IOPS (Input/Output Operations Per Second)**\\index{IOPS!ML irrelevance}: The metric that dominated storage procurement for decades because database workloads issue millions of small random reads. ML training inverts this priority: a pipeline streaming 256 MB shards sequentially needs sustained GB/s throughput, not per-operation speed. Provisioning storage by IOPS rating for an ML workload over-spends on random-access capability the pipeline never exercises."
    },
    {
      "term": "working set size",
      "index_head": "Working Set Size",
      "type": "bold_indexed_concept",
      "first_lineno": 386,
      "context": "The second inversion is **working set size**\\index{Working Set Size}. Traditional applications exhibit temporal locality\\index{Locality!temporal}: a web server repeatedly accesses popular pages, and a cache holding the top 10 percent of content serves 90 percent of requests. ML training datasets are accessed uniformly. Our `{python} StorageAccessPatternFacts.tokenized_text_tb_str` serialized corpus has no \"popular\" tokens; each is consumed once per epoch. A cache of any practical size holds only a tiny fraction of the dataset, and most samples are effectively \"cold\" when accessed. This lack of temporal locality makes traditional caching strategies much less effective. An LRU[^fn-lru-eviction-failure] (Least Recently Used) cache can approach a 0 percent hit rate for a cyclic scan larger than the cache. With independently reshuffled epochs, cross-epoch hits are low but nonzero and depend on the cache fraction and sample order."
    },
    {
      "term": "LRU (Least Recently Used)",
      "index_head": "LRU!ML failure mode",
      "type": "bold_indexed_concept",
      "first_lineno": 388,
      "context": "[^fn-lru-eviction-failure]: **LRU (Least Recently Used)**\\index{LRU!ML failure mode}: LRU is effective when recently accessed data is likely to be accessed again soon; a cyclic full-dataset scan larger than the cache can evict each sample before its next access and drive the hit rate toward zero. Independent reshuffling changes the boundary behavior because some samples retained from the end of one epoch may be reused before eviction in the next, so the hit rate depends on the cache-to-dataset ratio and ordering rather than being universally zero. Teams should measure this reuse before allocating DRAM that could otherwise serve as prefetch buffers."
    },
    {
      "term": "write pattern",
      "index_head": "Write Pattern",
      "type": "bold_indexed_concept",
      "first_lineno": 392,
      "context": "The third inversion is **write pattern**\\index{Write Pattern}. Transactional systems generate continuous streams of small writes, each immediately durable. ML systems generate occasional massive writes when saving checkpoints. A `{python} StorageAccessPatternFacts.gpt3_params_b_str` parameter model checkpoint, including optimizer state, occupies roughly `{python} StorageAccessPatternFacts.ckpt_total_gb_str`. Saving it every `{python} StorageAccessPatternFacts.checkpoint_interval_min_str` generates concentrated bursts that saturate bandwidth for seconds, followed by long idle periods. Together, these bursts form a **Checkpoint Storm**\\index{Checkpoint Storm!definition}: a synchronized checkpoint-write event that parallel file systems must absorb without disrupting ongoing training reads. The bursty write pattern is particularly challenging because all nodes in the cluster write their checkpoint shards simultaneously. If `{python} StorageAccessPatternFacts.ckpt_node_count_str` each write `{python} StorageAccessPatternFacts.ckpt_shard_gb_str`, the parallel file system receives `{python} StorageAccessPatternFacts.ckpt_burst_tb_str` of writes in a single burst, which must complete before the training pipeline can resume. @Tbl-storage-assumptions consolidates these inversions into the procurement rule: optimize for streaming and bursts, not database-style random IOPS."
    },
    {
      "term": "Checkpoint Storm",
      "index_head": "Checkpoint Storm!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 392,
      "context": "The third inversion is **write pattern**\\index{Write Pattern}. Transactional systems generate continuous streams of small writes, each immediately durable. ML systems generate occasional massive writes when saving checkpoints. A `{python} StorageAccessPatternFacts.gpt3_params_b_str` parameter model checkpoint, including optimizer state, occupies roughly `{python} StorageAccessPatternFacts.ckpt_total_gb_str`. Saving it every `{python} StorageAccessPatternFacts.checkpoint_interval_min_str` generates concentrated bursts that saturate bandwidth for seconds, followed by long idle periods. Together, these bursts form a **Checkpoint Storm**\\index{Checkpoint Storm!definition}: a synchronized checkpoint-write event that parallel file systems must absorb without disrupting ongoing training reads. The bursty write pattern is particularly challenging because all nodes in the cluster write their checkpoint shards simultaneously. If `{python} StorageAccessPatternFacts.ckpt_node_count_str` each write `{python} StorageAccessPatternFacts.ckpt_shard_gb_str`, the parallel file system receives `{python} StorageAccessPatternFacts.ckpt_burst_tb_str` of writes in a single burst, which must complete before the training pipeline can resume. @Tbl-storage-assumptions consolidates these inversions into the procurement rule: optimize for streaming and bursts, not database-style random IOPS."
    },
    {
      "term": "model rollout",
      "index_head": "Model Rollout!storage bandwidth",
      "type": "bold_indexed_concept",
      "first_lineno": 408,
      "context": "When scaling from a single user to thousands of concurrent inference requests, the storage challenge shifts from a single-stream throughput problem to a massive fan-out distribution problem. A serving cluster with 100 replicas of our 175B parameter model requires 35 TB of model weights distributed across the cluster. When a new model version is deployed (a **model rollout**\\index{Model Rollout!storage bandwidth}), all 100 replicas must be updated, triggering a 35 TB data distribution event that must complete within minutes to minimize serving disruption. This is analogous to the checkpoint storm in training but in reverse. Instead of many nodes *writing* to a central location simultaneously, many nodes are *reading* the same data simultaneously. The storage system must sustain this burst read bandwidth for model distribution while continuing to serve inference requests from the existing model version without degradation."
    },
    {
      "abbrev": "GB/s",
      "canonical_expansion": "O throughput",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 425,
      "context": "::: {#fig-access-patterns-vol2 fig-env=\"figure\" fig-pos=\"htb\" fig-cap=\"**The Storage Bottleneck Zone**: Required I/O throughput (GB/s) for full GPU utilization vs. model size (parameters, 100M to 175B). The red curve shows storage demand growing with model size; horizontal dashed lines mark the ceilings for common storage tiers (NVMe at 7 GB/s, Object at 50 GB/s, Lustre at 100 GB/s). Between the Compute-Bound and Storage-Bottleneck regions lies the transition where storage, not compute, limits training throughput.\" fig-alt=\"Log-scale line chart: Model Size 100M to 175B parameters on the x-axis, Data Throughput (GB/s) on the y-axis. Red curve shows required I/O rising to about 500 GB/s at 175B. Dashed lines mark NVMe (7), Object (50), and Lustre (100 GB/s) ceilings.\"}"
    },
    {
      "abbrev": "HBM",
      "canonical_expansion": "fast enough that the top tier",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 515,
      "context": "The engineering challenge is to ensure that data flows upward through the pyramid fast enough that the top tier (HBM) is never empty when the accelerator needs it. Return to the running example: the durable corpus lives in object storage (Tier 4) as a `{python} StoragePyramidContext.compressed_text_tb_str` compressed source copy plus `{python} StoragePyramidContext.tokenized_text_tb_str` of tokenized training shards, but the accelerator needs each batch in HBM (Tier 0) within 200 ms. The data must be promoted through intermediate tiers, staged in progressively faster storage, so that by the time the accelerator requests a batch, it is already waiting in host DRAM, one PCIe transfer away from HBM."
    },
    {
      "abbrev": "PFS",
      "canonical_expansion": "s aggregate parallel file system",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 519,
      "context": "The bandwidth ratios between adjacent tiers reveal the severity of each transition in the hierarchy. Between HBM and host DRAM, the ratio is roughly `{python} StoragePyramidContext.hbm_host_dram_ratio_mult_str` (`{python} StoragePyramidContext.h100_bw_tb_per_s_str` vs. ~`{python} StoragePyramidContext.host_dram_bw_gb_per_s_str`). Between host DRAM and NVMe, the ratio is roughly `{python} StoragePyramidContext.dram_nvme_ratio_mult_str` (`{python} StoragePyramidContext.host_dram_bw_gb_per_s_str` vs. ~`{python} StoragePyramidContext.raid0_bw_gb_per_s_str` from a 4-drive RAID-0). Between NVMe and a parallel file system, the ratio depends on the per-node allocation: if a 1 TB/s aggregate parallel file system (PFS) serves 256 nodes, each node receives roughly 4 GB/s, a 7$\\times$ reduction from local NVMe. Between the parallel file system and object storage, the ratio is typically 10$\\times$ or more, depending on the number of concurrent clients and network bandwidth."
    },
    {
      "abbrev": "NCCL",
      "canonical_expansion": "Communication Buffers",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 764,
      "context": "| Communication Buffers (NCCL)              |      `{python} HbmMemoryBudget.comm_range_str`      |"
    },
    {
      "term": "pinned memory",
      "index_head": "Pinned Memory!DMA transfer",
      "type": "bold_indexed_concept",
      "first_lineno": 816,
      "context": "4. **Collate**: The collation stage assembles individual samples into batches and places them in **pinned memory**\\index{Pinned Memory!DMA transfer} for efficient direct memory access (DMA) transfer to the accelerator."
    },
    {
      "abbrev": "DMA",
      "canonical_expansion": "for efficient direct memory access",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 816,
      "context": "4. **Collate**: The collation stage assembles individual samples into batches and places them in **pinned memory**\\index{Pinned Memory!DMA transfer} for efficient direct memory access (DMA) transfer to the accelerator."
    },
    {
      "term": "prefetch buffer",
      "index_head": "Prefetch Buffer",
      "type": "bold_indexed_concept",
      "first_lineno": 820,
      "context": "Host DRAM's most critical function is serving as a **prefetch buffer**\\index{Prefetch Buffer}. The CPU data loader reads data from lower tiers (NVMe or network storage), decodes compressed formats (JPEG, gzip), applies augmentations (random crops, flips, color jitter), and assembles tensors in host DRAM. When the accelerator finishes processing batch $i$, batch $i+1$ should already be assembled in host memory, ready for transfer to HBM. The depth of this prefetch buffer determines how much I/O variance the pipeline can absorb without stalling."
    },
    {
      "term": "embedding tables",
      "index_head": "Embedding Table",
      "type": "bold_indexed_concept",
      "first_lineno": 822,
      "context": "Recommendation workloads place a different demand on host DRAM: hosting **embedding tables**\\index{Embedding Table} that can exceed 100 GB, far too large for HBM. These tables reside in host DRAM and are accessed through lookups that fetch only the rows needed for the current batch. The bandwidth between host DRAM and HBM becomes the critical bottleneck for these workloads, which is *why* some systems use CPU-side DRAM with RDMA to serve embedding lookups across the network."
    },
    {
      "term": "augmentation workspace",
      "index_head": "Data Augmentation!workspace memory",
      "type": "bold_indexed_concept",
      "first_lineno": 824,
      "context": "Host DRAM also provides the **augmentation workspace**\\index{Data Augmentation!workspace memory} that the CPU pipeline requires. Data augmentation operations (resizing images, tokenizing text, applying noise) execute on the CPU and require temporary memory for intermediate results. A training pipeline that applies five augmentations to a 256-image batch at 150 KB per image needs tens of megabytes of working space for each augmentation stage. Although modest per-batch, this memory accumulates when multiple data loader workers run in parallel."
    },
    {
      "abbrev": "DALI",
      "canonical_expansion": "libraries like NVIDIA Data Loading Library",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 826,
      "context": "Some augmentation pipelines have moved from CPU to GPU execution, using libraries like NVIDIA Data Loading Library (DALI)\\index{DALI} to perform image decoding and augmentation on the accelerator itself. This approach eliminates the CPU augmentation bottleneck and reduces the host DRAM bandwidth demand, because compressed data (smaller) is transferred to the GPU instead of decoded data (larger). The trade-off is that augmentation on the GPU consumes HBM capacity and compute cycles that would otherwise be available for training. For *compute-bound* workloads (where the GPU is already saturated with matrix multiplications), GPU-based augmentation is counterproductive. For *I/O-bound* workloads (where the GPU waits for data), it can improve overall throughput by shifting work from the bottleneck (CPU) to the resource with spare capacity (GPU)."
    },
    {
      "abbrev": "CPU",
      "canonical_expansion": "by shifting work from the bottleneck",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 826,
      "context": "Some augmentation pipelines have moved from CPU to GPU execution, using libraries like NVIDIA Data Loading Library (DALI)\\index{DALI} to perform image decoding and augmentation on the accelerator itself. This approach eliminates the CPU augmentation bottleneck and reduces the host DRAM bandwidth demand, because compressed data (smaller) is transferred to the GPU instead of decoded data (larger). The trade-off is that augmentation on the GPU consumes HBM capacity and compute cycles that would otherwise be available for training. For *compute-bound* workloads (where the GPU is already saturated with matrix multiplications), GPU-based augmentation is counterproductive. For *I/O-bound* workloads (where the GPU waits for data), it can improve overall throughput by shifting work from the bottleneck (CPU) to the resource with spare capacity (GPU)."
    },
    {
      "abbrev": "GPU",
      "canonical_expansion": "to the resource with spare capacity",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 826,
      "context": "Some augmentation pipelines have moved from CPU to GPU execution, using libraries like NVIDIA Data Loading Library (DALI)\\index{DALI} to perform image decoding and augmentation on the accelerator itself. This approach eliminates the CPU augmentation bottleneck and reduces the host DRAM bandwidth demand, because compressed data (smaller) is transferred to the GPU instead of decoded data (larger). The trade-off is that augmentation on the GPU consumes HBM capacity and compute cycles that would otherwise be available for training. For *compute-bound* workloads (where the GPU is already saturated with matrix multiplications), GPU-based augmentation is counterproductive. For *I/O-bound* workloads (where the GPU waits for data), it can improve overall throughput by shifting work from the bottleneck (CPU) to the resource with spare capacity (GPU)."
    },
    {
      "term": "NUMA-aware allocation",
      "index_head": "NUMA!aware allocation",
      "type": "bold_indexed_concept",
      "first_lineno": 830,
      "context": "The physical layout of DRAM has performance implications that are invisible in single-socket benchmarks but critical at production scale. Multi-socket servers exhibit non-uniform memory access (NUMA) topology where each CPU socket has \"local\" DRAM that it can access at full bandwidth and \"remote\" DRAM attached to the other socket at roughly half bandwidth. In a dual-socket DGX node with eight GPUs split four per socket, a data loader thread running on socket 0 that allocates its prefetch buffer in socket 1's DRAM pays a roughly 2$\\times$ bandwidth penalty on every buffer access. The fix is **NUMA-aware allocation**\\index{NUMA!aware allocation}: pin each data loader worker to the same CPU socket (the same NUMA domain) as the GPUs it serves, and use `numactl` or `libnuma` to ensure memory allocation stays local. Proper NUMA pinning can improve data loading throughput by 30--50 percent on dual-socket systems, a gain that is invisible in development environments but essential when every percentage point of utilization translates to thousands of dollars per day."
    },
    {
      "abbrev": "NUMA",
      "canonical_expansion": "Multi-socket servers exhibit non-uniform memory access",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 830,
      "context": "The physical layout of DRAM has performance implications that are invisible in single-socket benchmarks but critical at production scale. Multi-socket servers exhibit non-uniform memory access (NUMA) topology where each CPU socket has \"local\" DRAM that it can access at full bandwidth and \"remote\" DRAM attached to the other socket at roughly half bandwidth. In a dual-socket DGX node with eight GPUs split four per socket, a data loader thread running on socket 0 that allocates its prefetch buffer in socket 1's DRAM pays a roughly 2$\\times$ bandwidth penalty on every buffer access. The fix is **NUMA-aware allocation**\\index{NUMA!aware allocation}: pin each data loader worker to the same CPU socket (the same NUMA domain) as the GPUs it serves, and use `numactl` or `libnuma` to ensure memory allocation stays local. Proper NUMA pinning can improve data loading throughput by 30--50 percent on dual-socket systems, a gain that is invisible in development environments but essential when every percentage point of utilization translates to thousands of dollars per day."
    },
    {
      "term": "NVMe (Non-Volatile Memory Express)",
      "index_head": "NVMe!queue parallelism",
      "type": "bold_indexed_concept",
      "first_lineno": 929,
      "context": "[^fn-nvme-storage-protocol]: **NVMe (Non-Volatile Memory Express)**\\index{NVMe!queue parallelism}: A storage protocol designed for SSDs, connecting directly to the CPU via PCIe lanes. NVMe replaced Advanced Host Controller Interface (AHCI)'s single queue of 32 commands with 64K queues of 64K commands each, a 130-million-fold increase in maximum outstanding I/O. This deep queue parallelism is what allows a multi-worker ML data loader to saturate the drive's bandwidth; with AHCI, 32 workers would serialize on the single command queue regardless of flash speed."
    },
    {
      "abbrev": "AHCI",
      "canonical_expansion": "NVMe replaced Advanced Host Controller Interface",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 929,
      "context": "[^fn-nvme-storage-protocol]: **NVMe (Non-Volatile Memory Express)**\\index{NVMe!queue parallelism}: A storage protocol designed for SSDs, connecting directly to the CPU via PCIe lanes. NVMe replaced Advanced Host Controller Interface (AHCI)'s single queue of 32 commands with 64K queues of 64K commands each, a 130-million-fold increase in maximum outstanding I/O. This deep queue parallelism is what allows a multi-worker ML data loader to saturate the drive's bandwidth; with AHCI, 32 workers would serialize on the single command queue regardless of flash speed."
    },
    {
      "term": "warm cache",
      "index_head": "Warm Cache",
      "type": "bold_indexed_concept",
      "first_lineno": 931,
      "context": "In ML training, local NVMe acts as a **warm cache**\\index{Warm Cache}, storing data shards fetched from distributed storage. This design allows workers to re-read samples across multiple epochs without re-traversing the network. For multi-epoch training on petabyte-scale datasets, the network egress cost of re-fetching from object storage each epoch would be prohibitive (@sec-storage-economics). Populating local NVMe from shared storage at job start and reading locally thereafter eliminates both cost and latency."
    },
    {
      "abbrev": "VFS",
      "canonical_expansion": "s Virtual File System",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 941,
      "context": "The first approach stores each image as a separate JPEG file in a directory hierarchy. This format is natural for data collection (download one image, save one file) but adversarial for training I/O. Each `open()` system call has a fixed overhead of roughly 10--50 $\\mu$s in the kernel's Virtual File System (VFS) layer. At 8,000 images per second, the overhead alone consumes 80--400 ms of CPU time per second. Worse, the directory structure forces the file system to maintain an inode for each file, consuming metadata resources that would otherwise be available for data reads."
    },
    {
      "abbrev": "HTTP",
      "canonical_expansion": "are also valid HyperText Transfer Protocol",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 945,
      "context": "The third approach uses the tar-based archive format popularized by WebDataset. Each sample is stored as a group of related files (image, label, metadata) within a standard POSIX tar archive. The tar format supports sequential iteration without a separate index, because each file's header contains its size, allowing the reader to skip forward to the next sample. This format combines the simplicity of individual files (each sample is self-describing) with the sequential I/O efficiency of large binary files. The tar archives are also valid HyperText Transfer Protocol (HTTP) byte-range targets, making them directly streamable from object storage without local staging."
    },
    {
      "abbrev": "HDD",
      "canonical_expansion": "A 7200 RPM hard disk drive",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 976,
      "context": "2.  **HDD reality**: A 7200 RPM hard disk drive (HDD) delivers `{python} NVMeTierCalcs.hdd_iops_str` at random reads\\index{HDD!random IOPS bottleneck}. Sustaining `{python} NVMeTierCalcs.images_per_s_str` requires `{python} NVMeTierCalcs.hdd_slowdown_factor_mult_str` more IOPS than the disk provides."
    },
    {
      "term": "local checkpoint staging",
      "index_head": "Checkpointing!local checkpoint staging",
      "type": "bold_indexed_concept",
      "first_lineno": 985,
      "context": "Local NVMe is also the primary tier for **local checkpoint staging**\\index{Checkpointing!local checkpoint staging}. When saving a model checkpoint, the fastest strategy is to write to local NVMe at full bandwidth (minimizing the time the training pipeline is paused), then asynchronously replicate to shared storage for durability, since a per-node shard written directly to a contended parallel file system takes several times longer than the same shard written to local drives. The optimal checkpoint frequency depends on cluster failure rates and checkpoint write time, but the storage-side design goal is already clear: minimize $T_{\\text{write}}$ through tiered staging by writing to local NVMe at full bandwidth, then background-copying to shared storage. @Sec-storage-checkpointing works the timings through in full."
    },
    {
      "abbrev": "ROI",
      "canonical_expansion": "Return on investment",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1036,
      "context": "::: {#nbk-data-storage-roi-local-nvme-caching .callout-notebook title=\"Return on investment (ROI) of local NVMe caching\"}"
    },
    {
      "term": "Remote Only",
      "index_head": "Warm Cache!remote-read baseline",
      "type": "bold_indexed_concept",
      "first_lineno": 1043,
      "context": "1. **Remote Only**\\index{Warm Cache!remote-read baseline}: `{python} LocalCacheRoi.lc_compute_ms_str` / (`{python} LocalCacheRoi.lc_compute_ms_str` + `{python} LocalCacheRoi.lc_remote_io_ms_str`) $\\approx$ `{python} LocalCacheRoi.lc_util_remote_str`."
    },
    {
      "term": "Local Cache",
      "index_head": "Warm Cache!local NVMe cache",
      "type": "bold_indexed_concept",
      "first_lineno": 1044,
      "context": "2. **Local Cache**\\index{Warm Cache!local NVMe cache}: Using prefetching into local NVMe reduces the exposed I/O wait to near zero."
    },
    {
      "term": "SSD endurance",
      "index_head": "SSD Endurance",
      "type": "bold_indexed_concept",
      "first_lineno": 1051,
      "context": "A practical concern at this tier is **SSD endurance**\\index{SSD Endurance}. NAND flash memory can sustain a limited number of write-erase cycles before the cells degrade. Enterprise NVMe drives are rated for 1 to 3 Drive Writes Per Day (DWPD) over a 5-year lifespan. For a 7.68 TB drive at 1 DWPD, this means the drive can absorb 7.68 TB of writes per day, or roughly 14 PB total over its lifetime. ML training workloads are predominantly read-heavy (the dataset is written once and read many times), which is favorable for SSD endurance. However, checkpoint writes can be intensive: if each node saves a 4 GB checkpoint shard every 10 minutes, that is 576 GB per day of checkpoint writes, well within the 1 DWPD budget. The risk emerges when local NVMe is used as a staging buffer for both checkpoint writes and dataset caching: the combined write volume from initial dataset staging plus repeated checkpoint saves must remain within the drive's endurance rating."
    },
    {
      "abbrev": "DWPD",
      "canonical_expansion": "to 3 Drive Writes Per Day",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1051,
      "context": "A practical concern at this tier is **SSD endurance**\\index{SSD Endurance}. NAND flash memory can sustain a limited number of write-erase cycles before the cells degrade. Enterprise NVMe drives are rated for 1 to 3 Drive Writes Per Day (DWPD) over a 5-year lifespan. For a 7.68 TB drive at 1 DWPD, this means the drive can absorb 7.68 TB of writes per day, or roughly 14 PB total over its lifetime. ML training workloads are predominantly read-heavy (the dataset is written once and read many times), which is favorable for SSD endurance. However, checkpoint writes can be intensive: if each node saves a 4 GB checkpoint shard every 10 minutes, that is 576 GB per day of checkpoint writes, well within the 1 DWPD budget. The risk emerges when local NVMe is used as a staging buffer for both checkpoint writes and dataset caching: the combined write volume from initial dataset staging plus repeated checkpoint saves must remain within the drive's endurance rating."
    },
    {
      "term": "PFS (Parallel File System)",
      "index_head": "Parallel File System!bandwidth aggregation",
      "type": "bold_indexed_concept",
      "first_lineno": 1059,
      "context": "[^fn-pfs-storage-architecture]: **PFS (Parallel File System)**\\index{Parallel File System!bandwidth aggregation}: A family of distributed file systems (Lustre, GPFS/Spectrum Scale, BeeGFS, WekaFS) whose defining property is that a single client reads from multiple storage servers simultaneously, aggregating their bandwidth into one logical stream. For ML training, this means a single data shard striped across 100 servers can deliver 100$\\times$ the bandwidth of any individual server, the architectural feature that makes petabyte-scale dataset access feasible within training-iteration time budgets."
    },
    {
      "term": "Parallel file system",
      "type": "callout_definition",
      "first_lineno": 1061,
      "context": "::: {#dfn-data-storage-parallel-file-system .callout-definition title=\"Parallel file system\"}"
    },
    {
      "term": "Parallel File System (PFS)",
      "index_head": "Parallel File System!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1063,
      "context": "***Parallel File System (PFS)***\\index{Parallel File System!definition} is a distributed storage architecture for ML training clusters that stripes data across many storage servers to provide aggregate throughput exceeding the capacity of any single device."
    },
    {
      "abbrev": "OSS",
      "canonical_expansion": "object storage servers",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1065,
      "context": "1.  **Significance**: A PFS aggregates $\\text{BW}_{\\text{io}}$ linearly with the number of storage servers (object storage servers [OSS]). A Lustre cluster with 20 OSS nodes each delivering 10 GB/s provides 200 GB/s aggregate, vs. a single network-attached storage (NAS) server capped at 10 GB/s, enabling a training job to load a 10 GB striped shard in about 50 ms rather than 1 second. This aggregate bandwidth directly reduces the $D_{\\text{vol}}/\\text{BW}$ term in the iron law."
    },
    {
      "abbrev": "NAS",
      "canonical_expansion": "a single network-attached storage",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1065,
      "context": "1.  **Significance**: A PFS aggregates $\\text{BW}_{\\text{io}}$ linearly with the number of storage servers (object storage servers [OSS]). A Lustre cluster with 20 OSS nodes each delivering 10 GB/s provides 200 GB/s aggregate, vs. a single network-attached storage (NAS) server capped at 10 GB/s, enabling a training job to load a 10 GB striped shard in about 50 ms rather than 1 second. This aggregate bandwidth directly reduces the $D_{\\text{vol}}/\\text{BW}$ term in the iron law."
    },
    {
      "abbrev": "MDS",
      "canonical_expansion": "metadata from a dedicated Metadata Server",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1066,
      "context": "2.  **Distinction**: Unlike network-attached storage (NAS), where every I/O request routes through a single server, a PFS client receives stripe location metadata from a dedicated Metadata Server (MDS) and then reads data directly from multiple OSS nodes in parallel: the MDS and OSS paths are architecturally separated, so data bandwidth scales with OSS count while metadata operations scale with MDS count."
    },
    {
      "term": "Metadata Servers (MDS)",
      "index_head": "Metadata Server",
      "type": "bold_indexed_concept",
      "first_lineno": 1071,
      "context": "The architecture of a Lustre-style parallel file system separates two concerns that traditional file systems handle together [@schwan2003lustre]. **Metadata Servers (MDS)**\\index{Metadata Server}\\index{MDS} manage the namespace: file creation, directory listings, permission checks, and lock management. **Object Storage Servers (OSS)**\\index{Object Storage Server}\\index{OSS}[^fn-oss-pfs-disambiguation] manage the actual data blocks, each serving a stripe of every large file. When a client opens a 10 GB training shard, the MDS tells the client which OSS nodes hold which stripes, and the client reads from all of them in parallel. A Lustre[^fn-lustre-parallel-origin] deployment with 100 OSS nodes, each providing 10 GB/s, delivers an aggregate 1 TB/s."
    },
    {
      "term": "Object Storage Servers (OSS)",
      "index_head": "Object Storage Server",
      "type": "bold_indexed_concept",
      "first_lineno": 1071,
      "context": "The architecture of a Lustre-style parallel file system separates two concerns that traditional file systems handle together [@schwan2003lustre]. **Metadata Servers (MDS)**\\index{Metadata Server}\\index{MDS} manage the namespace: file creation, directory listings, permission checks, and lock management. **Object Storage Servers (OSS)**\\index{Object Storage Server}\\index{OSS}[^fn-oss-pfs-disambiguation] manage the actual data blocks, each serving a stripe of every large file. When a client opens a 10 GB training shard, the MDS tells the client which OSS nodes hold which stripes, and the client reads from all of them in parallel. A Lustre[^fn-lustre-parallel-origin] deployment with 100 OSS nodes, each providing 10 GB/s, delivers an aggregate 1 TB/s."
    },
    {
      "term": "stripe count",
      "index_head": "Stripe Count",
      "type": "bold_indexed_concept",
      "first_lineno": 1073,
      "context": "This abstract architecture has concrete implications for performance tuning. When a training job creates a new dataset directory on Lustre, the administrator configures the **stripe count**\\index{Stripe Count} (the number of OSS nodes a file is spread across) and **stripe size**\\index{Stripe Size} (the chunk size written to each OSS) for that directory [@lustreSetstripe]. For large sequential-read training shards, administrators may choose wider striping and multi-megabyte stripe sizes so a single file can draw bandwidth from multiple OSS nodes; Lustre's striping guide treats wide striping as useful for very large files or files accessed by many clients, while smaller files often use fewer stripes to reduce overhead [@lustreFileStripingGuide]. A 4 GB data shard striped across 100 OSS nodes with 4 MB stripes places roughly 10 stripes on each OSS. When a data loader reads large contiguous ranges, the Lustre client can issue parallel read requests to the OSS nodes holding those stripes, aggregating their bandwidth. The client also maintains its own read-ahead buffer, prefetching the next several stripes while the application processes the current ones. This file-system-level read-ahead is distinct from the data loader's application-level prefetch buffer; the two layers of prefetching compound to provide deep latency hiding, making the physical distance to the OSS nodes nearly transparent to the training process."
    },
    {
      "term": "stripe size",
      "index_head": "Stripe Size",
      "type": "bold_indexed_concept",
      "first_lineno": 1073,
      "context": "This abstract architecture has concrete implications for performance tuning. When a training job creates a new dataset directory on Lustre, the administrator configures the **stripe count**\\index{Stripe Count} (the number of OSS nodes a file is spread across) and **stripe size**\\index{Stripe Size} (the chunk size written to each OSS) for that directory [@lustreSetstripe]. For large sequential-read training shards, administrators may choose wider striping and multi-megabyte stripe sizes so a single file can draw bandwidth from multiple OSS nodes; Lustre's striping guide treats wide striping as useful for very large files or files accessed by many clients, while smaller files often use fewer stripes to reduce overhead [@lustreFileStripingGuide]. A 4 GB data shard striped across 100 OSS nodes with 4 MB stripes places roughly 10 stripes on each OSS. When a data loader reads large contiguous ranges, the Lustre client can issue parallel read requests to the OSS nodes holding those stripes, aggregating their bandwidth. The client also maintains its own read-ahead buffer, prefetching the next several stripes while the application processes the current ones. This file-system-level read-ahead is distinct from the data loader's application-level prefetch buffer; the two layers of prefetching compound to provide deep latency hiding, making the physical distance to the OSS nodes nearly transparent to the training process."
    },
    {
      "term": "OSS (Object Storage Server)",
      "index_head": "OSS!PFS disambiguation",
      "type": "bold_indexed_concept",
      "first_lineno": 1075,
      "context": "[^fn-oss-pfs-disambiguation]: **OSS (Object Storage Server)**\\index{OSS!PFS disambiguation}: In parallel file system terminology, \"object\" means a chunk of striped file data managed by an Object Storage Target (OST), a usage that predates cloud object storage (S3, GCS) by over a decade. Confusing the two is a common source of miscommunication: when a Lustre administrator says \"add more OSS nodes,\" they mean adding data-serving capacity to the parallel file system, not provisioning cloud buckets."
    },
    {
      "abbrev": "OST",
      "canonical_expansion": "managed by an Object Storage Target",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1075,
      "context": "[^fn-oss-pfs-disambiguation]: **OSS (Object Storage Server)**\\index{OSS!PFS disambiguation}: In parallel file system terminology, \"object\" means a chunk of striped file data managed by an Object Storage Target (OST), a usage that predates cloud object storage (S3, GCS) by over a decade. Confusing the two is a common source of miscommunication: when a Lustre administrator says \"add more OSS nodes,\" they mean adding data-serving capacity to the parallel file system, not provisioning cloud buckets."
    },
    {
      "term": "Lustre",
      "index_head": "Lustre!etymology",
      "type": "bold_indexed_concept",
      "first_lineno": 1077,
      "context": "[^fn-lustre-parallel-origin]: **Lustre**\\index{Lustre!etymology}: A portmanteau of \"Linux\" and \"cluster,\" developed at Carnegie Mellon University and first deployed in production in 2003. Lustre is common in HPC and ML infrastructure because its architecture scales aggregate bandwidth linearly with the number of OSS nodes, the same property that makes it a natural choice for training clusters where a single job may demand hundreds of GB/s of sustained read throughput."
    },
    {
      "term": "GPFS (General Parallel File System)",
      "index_head": "GPFS!distributed metadata",
      "type": "bold_indexed_concept",
      "first_lineno": 1081,
      "context": "[^fn-gpfs-parallel-filesystem]: **GPFS (General Parallel File System)**\\index{GPFS!distributed metadata}: Developed by IBM Research starting in 1998, now marketed as IBM Spectrum Scale. Unlike Lustre's dedicated metadata-server/object-server organization, GPFS is a shared-disk parallel file system that can stripe data and metadata and uses token-based locking to coordinate concurrent client access. For ML checkpoint writes, this distributed coordination model can reduce reliance on a single dedicated metadata path, but it also makes lock behavior part of the performance envelope."
    },
    {
      "term": "Small file problem",
      "type": "callout_definition",
      "first_lineno": 1085,
      "context": "::: {#dfn-data-storage-small-file-problem .callout-definition title=\"Small file problem\"}"
    },
    {
      "term": "striping",
      "index_head": "Striping",
      "type": "bold_indexed_concept",
      "first_lineno": 1107,
      "context": "The design response to the small file problem is **striping**\\index{Striping} and **aggregation**\\index{Aggregation!small-file mitigation}. Striping distributes a single large file across multiple OSS nodes so that a client can read from all of them in parallel. The stripe size (typically 1 to 4 MB per OSS) determines the granularity: a 4 GB file striped at 1 MB across 100 OSS nodes places 40 MB on each node, and a sequential read saturates all 100 data paths simultaneously. The stripe count (how many OSS nodes participate) can be configured per-file or per-directory, allowing administrators to tune bandwidth for different access patterns. Training data shards benefit from maximum striping; small configuration files benefit from minimal striping to avoid the overhead of coordinating across many nodes."
    },
    {
      "term": "aggregation",
      "index_head": "Aggregation!small-file mitigation",
      "type": "bold_indexed_concept",
      "first_lineno": 1107,
      "context": "The design response to the small file problem is **striping**\\index{Striping} and **aggregation**\\index{Aggregation!small-file mitigation}. Striping distributes a single large file across multiple OSS nodes so that a client can read from all of them in parallel. The stripe size (typically 1 to 4 MB per OSS) determines the granularity: a 4 GB file striped at 1 MB across 100 OSS nodes places 40 MB on each node, and a sequential read saturates all 100 data paths simultaneously. The stripe count (how many OSS nodes participate) can be configured per-file or per-directory, allowing administrators to tune bandwidth for different access patterns. Training data shards benefit from maximum striping; small configuration files benefit from minimal striping to avoid the overhead of coordinating across many nodes."
    },
    {
      "term": "WebDataset",
      "index_head": "WebDataset!streaming format",
      "type": "bold_indexed_concept",
      "first_lineno": 1113,
      "context": "[^fn-webdataset-tar-streaming]: **WebDataset**\\index{WebDataset!streaming format}: A data format that repurposes standard POSIX tar archives for ML training. The design insight is that tar's sequential header-then-data layout, invented in 1979 for tape backup, maps perfectly onto the streaming access pattern that ML data loaders need. Because tar archives are valid HTTP byte-range targets, a data loader can stream training samples directly from object storage without local staging, reducing the I/O path from five tiers to two."
    },
    {
      "term": "hedged requests",
      "index_head": "Hedged Requests",
      "type": "bold_indexed_concept",
      "first_lineno": 1123,
      "context": "Systems mitigate tail latency through **hedged requests**\\index{Hedged Requests}: after waiting for a configurable timeout (typically the median response time), the client issues a redundant read to a different replica of the same data stripe. The first response to arrive is used; the second is discarded. If the storage system replicates each stripe across two OSS nodes, the probability that both replicas are slow is $0.01^2 = 0.0001$, reducing the fraction of slow steps from 63 percent to less than 1 percent. The cost of the redundant request, one additional network read, is negligible compared to the cost of an idle accelerator consuming hundreds of watts while waiting."
    },
    {
      "term": "failure-domain-aware placement",
      "index_head": "Failure-Domain-Aware Placement",
      "type": "bold_indexed_concept",
      "first_lineno": 1125,
      "context": "The effectiveness of hedged requests depends on the replicated data layout. If both replicas reside on OSS nodes that share the same network switch, a switch failure makes both copies simultaneously unavailable. Effective hedging requires **failure-domain-aware placement**\\index{Failure-Domain-Aware Placement}: replicas should reside in different racks, connected through different top-of-rack switches, so that the failure of any single component affects at most one replica."
    },
    {
      "term": "noisy neighbor problem",
      "index_head": "Noisy Neighbor Problem",
      "type": "bold_indexed_concept",
      "first_lineno": 1127,
      "context": "In production clusters, the parallel file system is a shared utility, simultaneously servicing dozens of concurrent training jobs with different I/O patterns. At any given moment, a large language model job might be streaming sequentially through text shards, a computer vision job might be reading random image shards, and a checkpoint storm from a third job could be saturating write bandwidth. This concurrency gives rise to the **noisy neighbor problem**\\index{Noisy Neighbor Problem}, where one job's I/O pattern severely degrades performance for all other jobs. A checkpoint save that consumes a disproportionate share of the PFS's internal network bandwidth causes read latency for other jobs to spike, potentially stalling their training pipelines. PFS administrators mitigate this through I/O scheduling policies (quality-of-service tiers, bandwidth quotas per job), but these policies add administrative complexity and can reduce peak single-job throughput."
    },
    {
      "term": "namespace isolation",
      "index_head": "Namespace Isolation",
      "type": "bold_indexed_concept",
      "first_lineno": 1129,
      "context": "A related challenge is **namespace isolation**\\index{Namespace Isolation}. Different teams and workloads require different storage configurations. A team training on millions of 100 KB images needs a directory with high stripe count and small stripe size to maximize metadata performance. A team working with large video files needs fewer, larger stripes to optimize for sequential bandwidth. Misconfigured striping for one team's workload can create hotspots that degrade performance for the entire shared file system. The impact of sharing is easy to quantify: a PFS with 1 TB/s aggregate bandwidth, shared across 10 concurrent training jobs, provides only 100 GB/s per job on average. If one job's checkpoint storm consumes 400 GB/s for 10 seconds, the remaining nine jobs share 600 GB/s, a 33 percent reduction that can trigger data stalls in their accelerator pipelines."
    },
    {
      "term": "Erasure Coding",
      "index_head": "Erasure Coding!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1149,
      "context": "[^fn-reedsolomon-erasure-coding]: **Erasure Coding**\\index{Erasure Coding!definition}: Built on Reed-Solomon codes\\index{Erasure Coding!Reed-Solomon} [@reed1960], which use maximum-distance-separable coding to recover the original data from any sufficient subset of encoded fragments. In storage systems, Reed-Solomon-style codes and later local-reconstruction variants trade storage overhead against repair bandwidth and degraded-read cost [@plank1997; @huang2012erasure]. For ML storage, the trade-off is concrete: a 4+2 code tolerates two missing fragments at 1.5$\\times$ storage overhead, while triple replication costs 3$\\times$; degraded reads then pay extra network I/O and reconstruction work when fragments are unavailable."
    },
    {
      "term": "Eleven Nines (99.999999999 Percent Durability)",
      "index_head": "Durability!eleven nines",
      "type": "bold_indexed_concept",
      "first_lineno": 1151,
      "context": "[^fn-durability-nines-availability]: **Eleven Nines (99.999999999 Percent Durability)**\\index{Durability!eleven nines}: A design target of 11 nines annual durability corresponds to an expected loss probability of about $10^{-11}$ per object-year; for one million objects, that is about one expected object loss per 100,000 years. This extreme durability is why object storage is the only tier where training data and checkpoint archives can be treated as permanent. The same service indirection, erasure coding, replication, and metadata lookup that support durability also make object storage a higher-latency tier than local NVMe or a warm parallel file system, so real-time data pipelines need prefetch buffering."
    },
    {
      "term": "silent data corruption",
      "index_head": "Silent Data Corruption",
      "type": "bold_indexed_concept",
      "first_lineno": 1233,
      "context": "The immense scale of ML datasets makes them vulnerable to **silent data corruption**\\index{Silent Data Corruption}: subtle bit flips in the storage medium or during network transfer that can introduce training artifacts nearly impossible to diagnose. Object storage services provide per-object checksums that verify integrity on upload, copy, or download; Amazon S3, for example, documents checksum options including CRC-family and cryptographic hashes [@awsS3ObjectIntegrity]. Parallel file systems often rely on underlying RAID or erasure coding for physical integrity but may not provide end-to-end checksums visible to the application. A robust large-scale ML pipeline computes and stores a checksum for each data shard during preprocessing and verifies it when the shard is first loaded by a training worker. The verification overhead is negligible compared to the cost of training on corrupted data. For model checkpoints, integrity verification is even more critical: a single bit flip in a weight or optimizer state can corrupt the model, causing training to diverge silently after recovery. Frameworks and checkpoint libraries can include integrity verification, but the pipeline should not assume it without testing the checkpoint format."
    },
    {
      "abbrev": "IAM",
      "canonical_expansion": "policies through Identity and Access Management",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1235,
      "context": "Storage security is a first-class concern in production ML infrastructure. Training datasets often contain sensitive information (personally identifiable data, proprietary text, licensed images) that requires access control. Object storage provides fine-grained security policies through Identity and Access Management (IAM) roles, per-bucket policies, and integrated encryption for data at rest and in transit. Parallel file systems provide POSIX permissions and access-control lists (ACLs) but often lack the audit logging that compliance requires. For organizations subject to data protection regulations such as the General Data Protection Regulation (GDPR) and the California Consumer Privacy Act (CCPA), the storage architecture must ensure that data access is logged, that deletion requests can be honored (a task complicated by immutable preprocessed shards), and that model checkpoints do not inadvertently memorize protected information. Storage design therefore has to preserve both throughput and governance metadata."
    },
    {
      "abbrev": "ACLs",
      "canonical_expansion": "provide POSIX permissions and access-control lists",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1235,
      "context": "Storage security is a first-class concern in production ML infrastructure. Training datasets often contain sensitive information (personally identifiable data, proprietary text, licensed images) that requires access control. Object storage provides fine-grained security policies through Identity and Access Management (IAM) roles, per-bucket policies, and integrated encryption for data at rest and in transit. Parallel file systems provide POSIX permissions and access-control lists (ACLs) but often lack the audit logging that compliance requires. For organizations subject to data protection regulations such as the General Data Protection Regulation (GDPR) and the California Consumer Privacy Act (CCPA), the storage architecture must ensure that data access is logged, that deletion requests can be honored (a task complicated by immutable preprocessed shards), and that model checkpoints do not inadvertently memorize protected information. Storage design therefore has to preserve both throughput and governance metadata."
    },
    {
      "abbrev": "GDPR",
      "canonical_expansion": "as the General Data Protection Regulation",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1235,
      "context": "Storage security is a first-class concern in production ML infrastructure. Training datasets often contain sensitive information (personally identifiable data, proprietary text, licensed images) that requires access control. Object storage provides fine-grained security policies through Identity and Access Management (IAM) roles, per-bucket policies, and integrated encryption for data at rest and in transit. Parallel file systems provide POSIX permissions and access-control lists (ACLs) but often lack the audit logging that compliance requires. For organizations subject to data protection regulations such as the General Data Protection Regulation (GDPR) and the California Consumer Privacy Act (CCPA), the storage architecture must ensure that data access is logged, that deletion requests can be honored (a task complicated by immutable preprocessed shards), and that model checkpoints do not inadvertently memorize protected information. Storage design therefore has to preserve both throughput and governance metadata."
    },
    {
      "abbrev": "CCPA",
      "canonical_expansion": "and the California Consumer Privacy Act",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1235,
      "context": "Storage security is a first-class concern in production ML infrastructure. Training datasets often contain sensitive information (personally identifiable data, proprietary text, licensed images) that requires access control. Object storage provides fine-grained security policies through Identity and Access Management (IAM) roles, per-bucket policies, and integrated encryption for data at rest and in transit. Parallel file systems provide POSIX permissions and access-control lists (ACLs) but often lack the audit logging that compliance requires. For organizations subject to data protection regulations such as the General Data Protection Regulation (GDPR) and the California Consumer Privacy Act (CCPA), the storage architecture must ensure that data access is logged, that deletion requests can be honored (a task complicated by immutable preprocessed shards), and that model checkpoints do not inadvertently memorize protected information. Storage design therefore has to preserve both throughput and governance metadata."
    },
    {
      "term": "lifecycle policies",
      "index_head": "Lifecycle Policy",
      "type": "bold_indexed_concept",
      "first_lineno": 1277,
      "context": "Automated **lifecycle policies**\\index{Lifecycle Policy} govern the transition of data between tiers based on access recency. As one illustration of how such automation is expressed, a time-based rule for ML infrastructure might move checkpoints as follows: the most recent three checkpoints remain on the parallel file system for fast recovery; checkpoints older than 72 hours are transitioned to object storage; checkpoints older than 90 days are transitioned to archive storage; and checkpoints older than two years are deleted unless flagged for regulatory retention. S3 Lifecycle rules provide the same general transition/archive/delete mechanism for object lifetimes [@awsS3LifecycleManagement]. The running example later adopts a count-based variant of the same idea (@sec-storage-checkpointing), where the transition rule keys on checkpoint count rather than age. Without automation, teams either hoard data on expensive tiers (wasting budget) or delete data too aggressively (losing reproducibility). The lifecycle policy encodes the organization's cost-recovery trade-off in a declarative rule that requires no manual intervention."
    },
    {
      "term": "Row-oriented formats",
      "index_head": "Row-Oriented Format",
      "type": "bold_indexed_concept",
      "first_lineno": 1304,
      "context": "The useful design question is which access cost the workload must pay: per-file metadata, unnecessary feature reads, or sequential shard traversal. **Row-oriented formats**\\index{Row-Oriented Format}, such as CSV, JavaScript Object Notation (JSON) lines, or a directory of individual image files (JPEG, PNG), are the most intuitive and human-readable. They are easy to inspect and debug, making them suitable for initial data exploration or for datasets small enough to fit entirely in memory (typically under 10 GB). For training at scale, they are catastrophic for I/O performance. Each sample is a separate file, requiring a distinct file system operation (`open`, `stat`, `read`, `close`) that incurs significant per-sample overhead, often dominating the actual time spent reading data."
    },
    {
      "abbrev": "JSON",
      "canonical_expansion": "JavaScript Object Notation",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1304,
      "context": "The useful design question is which access cost the workload must pay: per-file metadata, unnecessary feature reads, or sequential shard traversal. **Row-oriented formats**\\index{Row-Oriented Format}, such as CSV, JavaScript Object Notation (JSON) lines, or a directory of individual image files (JPEG, PNG), are the most intuitive and human-readable. They are easy to inspect and debug, making them suitable for initial data exploration or for datasets small enough to fit entirely in memory (typically under 10 GB). For training at scale, they are catastrophic for I/O performance. Each sample is a separate file, requiring a distinct file system operation (`open`, `stat`, `read`, `close`) that incurs significant per-sample overhead, often dominating the actual time spent reading data."
    },
    {
      "term": "columnar formats",
      "index_head": "Columnar Format",
      "type": "bold_indexed_concept",
      "first_lineno": 1306,
      "context": "A second choice solves a different storage problem: **columnar formats**\\index{Columnar Format} like Apache Parquet\\index{Parquet} and Apache Arrow avoid unnecessary reads when tabular features can be selected before transfer. They organize data by column rather than by row, which allows for excellent compression as data within a column is typically of the same type and has lower entropy. This structure also enables **predicate pushdown**\\index{Predicate Pushdown}, where the storage engine reads only the specific columns (features) required by a query, avoiding unnecessary I/O. These formats are well-suited for tabular ML tasks and feature engineering but are less natural for unstructured data like images or audio, which are treated as atomic blobs."
    },
    {
      "term": "predicate pushdown",
      "index_head": "Predicate Pushdown",
      "type": "bold_indexed_concept",
      "first_lineno": 1306,
      "context": "A second choice solves a different storage problem: **columnar formats**\\index{Columnar Format} like Apache Parquet\\index{Parquet} and Apache Arrow avoid unnecessary reads when tabular features can be selected before transfer. They organize data by column rather than by row, which allows for excellent compression as data within a column is typically of the same type and has lower entropy. This structure also enables **predicate pushdown**\\index{Predicate Pushdown}, where the storage engine reads only the specific columns (features) required by a query, avoiding unnecessary I/O. These formats are well-suited for tabular ML tasks and feature engineering but are less natural for unstructured data like images or audio, which are treated as atomic blobs."
    },
    {
      "term": "sequential streaming formats",
      "index_head": "Streaming Format!sequential",
      "type": "bold_indexed_concept",
      "first_lineno": 1308,
      "context": "A third choice addresses the training access pattern directly: **sequential streaming formats**\\index{Streaming Format!sequential} reduce per-sample metadata to the cost of opening a shard. TFRecord, Mosaic's StreamingDataset format [@mosaicStreamingMainConcepts], and WebDataset (based on standard `tar` archives) group many samples into large, contiguous binary files called shards. This amortizes the cost of opening a file over thousands of samples. A data loader opens a single shard and streams its contents sequentially, maximizing bandwidth. The `tar`-based formats have the added advantage of being valid targets for HTTP byte-range requests, making them ideal for streaming data directly from object storage without downloading the entire file first."
    },
    {
      "term": "index",
      "index_head": "Data Format!index",
      "type": "bold_indexed_concept",
      "first_lineno": 1310,
      "context": "A key trade-off within streaming formats is the presence of an **index**\\index{Data Format!index}. TFRecord and HDF5 often use a separate index file to record the byte offset of each sample within a shard. This enables efficient random access within a shard but introduces a metadata dependency: the index must be read and held in memory. WebDataset is indexless: each record in the `tar` archive contains its own header specifying its size, allowing for purely sequential streaming without any external metadata. The trade-off is that indexless formats cannot efficiently seek to an arbitrary sample within a shard, a capability that matters for curriculum learning or active learning workloads that require nonsequential data access."
    },
    {
      "term": "data pipeline throughput equation",
      "index_head": "Data Pipeline!throughput equation",
      "type": "bold_indexed_concept",
      "first_lineno": 1392,
      "context": "Every training job eventually raises a concrete sizing question: how much storage bandwidth must the pipeline deliver before accelerators begin to stall? The required rate depends on the training configuration, and getting it wrong in either direction is expensive. Under-provisioning storage bandwidth starves accelerators. Over-provisioning wastes budget on storage capacity that sits partially idle. The **data pipeline throughput equation**\\index{Data Pipeline!throughput equation} in @eq-pipeline-throughput quantifies the bandwidth the pipeline must sustain so that accelerators never wait for data:"
    },
    {
      "term": "Data Stall Ratio",
      "index_head": "Data Stall Ratio!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1409,
      "context": "The consequences of falling short are captured by the **Data Stall Ratio**\\index{Data Stall Ratio!definition}, defined in @eq-data-stall as the fraction of each training step where the accelerator waits for data:"
    },
    {
      "term": "pipeline stress test",
      "index_head": "Pipeline Stress Test",
      "type": "bold_indexed_concept",
      "first_lineno": 1439,
      "context": "A common and costly failure mode is to develop and test a data pipeline on a small local dataset (1 GB) and only discover at full production scale (1 TB+) that the pipeline cannot sustain the required throughput. The root cause is often a bottleneck invisible at small scale but crippling under load: a Python Global Interpreter Lock (GIL) contention in a data augmentation function, a file descriptor leak that accumulates over millions of samples, or a memory fragmentation issue that causes the prefetch buffer to slow down after hours of continuous operation. Robust teams benchmark the data pipeline in isolation, measuring sustained throughput over at least 10 to 20 minutes at the target data volume, before connecting it to the training loop. This **pipeline stress test**\\index{Pipeline Stress Test} catches system-level bottlenecks that would otherwise manifest as mysterious training slowdowns days into a production run."
    },
    {
      "abbrev": "GIL",
      "canonical_expansion": "a Python Global Interpreter Lock",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1439,
      "context": "A common and costly failure mode is to develop and test a data pipeline on a small local dataset (1 GB) and only discover at full production scale (1 TB+) that the pipeline cannot sustain the required throughput. The root cause is often a bottleneck invisible at small scale but crippling under load: a Python Global Interpreter Lock (GIL) contention in a data augmentation function, a file descriptor leak that accumulates over millions of samples, or a memory fragmentation issue that causes the prefetch buffer to slow down after hours of continuous operation. Robust teams benchmark the data pipeline in isolation, measuring sustained throughput over at least 10 to 20 minutes at the target data volume, before connecting it to the training loop. This **pipeline stress test**\\index{Pipeline Stress Test} catches system-level bottlenecks that would otherwise manifest as mysterious training slowdowns days into a production run."
    },
    {
      "abbrev": "MFU",
      "canonical_expansion": "cluster achieves low Model FLOPs Utilization",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1595,
      "context": "**Scenario**: A large GPU cluster achieves low Model FLOPs Utilization (MFU) despite storage metrics reporting ample network and byte bandwidth."
    },
    {
      "term": "pipelining",
      "index_head": "Pipelining!data loading",
      "type": "bold_indexed_concept",
      "first_lineno": 1605,
      "context": "The primary weapon against data stalls is **pipelining**\\index{Pipelining!data loading}: the CPU prepares batch $i+1$ while the GPU processes batch $i$, overlapping storage transfer with accelerator execution (@fig-data-pipeline-vol2). When I/O time is less than compute time, pipelining hides the storage latency entirely, and the accelerator never stalls."
    },
    {
      "term": "I/O jitter",
      "index_head": "I/O Jitter",
      "type": "bold_indexed_concept",
      "first_lineno": 1613,
      "context": "The key insight from @fig-data-pipeline-vol2 is that pipelining converts a sequential bottleneck into a parallel overlap, but only when the prefetch buffer is deep enough to absorb variance in I/O latency. Pipelining works perfectly when I/O times are consistent. In practice, they are not. Storage systems exhibit **I/O jitter**\\index{I/O Jitter}: variation in read latency caused by a multitude of factors. NVMe drives experience occasional latency spikes during internal garbage collection (when the controller reorganizes NAND flash blocks) or wear-leveling operations. Parallel file systems exhibit latency spikes when multiple training jobs contend for the same OSS nodes, when the MDS processes a burst of metadata operations, or when background scrubbing detects and repairs bit errors. Object storage latency can spike during cross-region replication, garbage collection of versioned objects, or load-balancer rebalancing."
    },
    {
      "abbrev": "DCT",
      "canonical_expansion": "inverse discrete cosine transform",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1693,
      "context": "A single CPU core cannot keep a high-throughput accelerator fed, even when the storage hardware is fast enough, because of the CPU work required between the storage read and the GPU transfer. Consider the throughput required for image training: decoding a 150 KB JPEG image into a $224{\\times}224{\\times}3$ raw tensor requires entropy decoding of Huffman-coded coefficients, inverse discrete cosine transform (DCT), chroma upsampling, and pixel-format conversion. This process takes roughly 1 to 5 ms per image on a server CPU core. After decoding, augmentation adds further CPU work: a random crop requires computing crop coordinates and copying the sub-region; horizontal flip requires copying with reversed column order; color jitter requires per-pixel multiplication and addition. Each augmentation adds 0.5 to 2 ms per image."
    },
    {
      "term": "multi-worker data loading",
      "index_head": "Multi-Worker Data Loading",
      "type": "bold_indexed_concept",
      "first_lineno": 1695,
      "context": "At 1,000 images per second per GPU and 8 GPUs per node, a single core would need to decode and augment 8,000 images per second, a throughput 10$\\times$ to 40$\\times$ beyond what a single core can sustain. The solution is **multi-worker data loading**\\index{Multi-Worker Data Loading}, where $W$ worker processes each read from storage, decode, augment, and enqueue batches independently. The effective I/O throughput scales approximately linearly with $W$ until one of three bottlenecks is reached: the storage device's bandwidth saturates, the PCIe bus between host and device saturates, or the workers exhaust host CPU cycles."
    },
    {
      "term": "shuffle quality vs. I/O efficiency",
      "index_head": "Data Shuffling!quality vs. I/O efficiency",
      "type": "bold_indexed_concept",
      "first_lineno": 1725,
      "context": "A subtle pitfall in multi-worker loading is **shuffle quality vs. I/O efficiency**\\index{Data Shuffling!quality vs. I/O efficiency}. Perfect shuffling requires that each batch contain samples drawn uniformly from the entire dataset, which implies random access across all shards. Random access, however, defeats the sequential I/O patterns that storage systems optimize for. The practical compromise is *shard-level shuffling*\\index{Data Shuffling!shard-level} combined with *within-shard shuffling*: the loader shuffles the list of shards at epoch start, assigns contiguous groups of shards to each worker, and then shuffles samples within each shard's local buffer. This approach provides sufficient randomness for training convergence while maintaining sequential I/O at the storage level. Empirical studies confirm that shard-level shuffling produces training loss curves indistinguishable from full random shuffling for most workloads, as long as the shard size is large enough (typically 256 MB or more) to provide adequate within-shard diversity."
    },
    {
      "term": "static placement",
      "index_head": "Static Placement",
      "type": "bold_indexed_concept",
      "first_lineno": 1735,
      "context": "The simplest strategy is **static placement**\\index{Static Placement}. At the beginning of a training job, the orchestrator assigns a fixed subset of data shards to each node. The node stages this data by copying its shards from the shared PFS to local NVMe drives. For the remainder of the training run, all data reads are local, maximizing I/O bandwidth. This approach is highly effective for single-dataset training runs but introduces inflexibility: if the dataset changes or the job requires a different subset of data, the shards must be re-staged."
    },
    {
      "term": "dynamic placement",
      "index_head": "Dynamic Placement",
      "type": "bold_indexed_concept",
      "first_lineno": 1737,
      "context": "The alternative is **dynamic placement**\\index{Dynamic Placement}, where shards are fetched on demand from shared storage as needed. This provides maximum flexibility, as any node can access any shard at any time. Dynamic placement is essential when the total dataset size exceeds the aggregate local NVMe storage of the cluster, or when the job involves data sampling strategies that change the active subset over time. The cost is performance: every read incurs network latency and consumes shared storage bandwidth."
    },
    {
      "term": "locality-aware scheduling",
      "index_head": "Locality-Aware Scheduling",
      "type": "bold_indexed_concept",
      "first_lineno": 1739,
      "context": "A more advanced approach is **locality-aware scheduling**\\index{Locality-Aware Scheduling}. The scheduler keeps track of which data shards are cached on which nodes' local NVMe drives. When it starts a new job or replaces a failed node, it prefers a node that already has the required data cached, rather than treating all nodes as interchangeable. This uses the principle of data gravity,[^fn-datagravity-storage-placement] co-locating compute with data to minimize transfer times. The storage point is simple: cached data only saves time if the placement system knows where the cache is. For teams running repeated experiments on the same dataset, locality-aware scheduling reduces staging time from minutes to zero."
    },
    {
      "term": "Data Gravity",
      "index_head": "Data Gravity!placement economics",
      "type": "bold_indexed_concept",
      "first_lineno": 1741,
      "context": "[^fn-datagravity-storage-placement]: **Data Gravity**\\index{Data Gravity!placement economics}: A metaphor treating large datasets as massive objects that attract compute. The gravitational analogy is apt: moving a 100 TB dataset across cloud regions costs over \\$9,000 in egress fees and takes hours, while launching compute next to the data is near-instantaneous. In ML fleet design, data gravity means the storage location of the training dataset often dictates the physical placement of the entire training cluster."
    },
    {
      "abbrev": "GPT-4",
      "canonical_expansion": "While Archetype A",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1773,
      "context": "While Archetype A (GPT-4) deals with static, versioned datasets of trillions of tokens, Archetype B (Deep Learning Recommendation Model, or DLRM, at scale), the DLRM workload, deals with dynamic, high-velocity feature streams. For a recommendation system, the \"ground truth\" changes every second as users click and interact. This forces a move from simple file-based storage to a feature store\\index{Feature Store} architecture that must solve the point-in-time correctness problem: ensuring that the features retrieved for training exactly match what the model would have seen at the moment of inference, without \"leaking\" future information."
    },
    {
      "term": "Point-in-Time Correctness",
      "index_head": "Point-in-Time Correctness!feature stores",
      "type": "bold_indexed_concept",
      "first_lineno": 1779,
      "context": "[^fn-point-in-time]: **Point-in-Time Correctness**\\index{Point-in-Time Correctness!feature stores}: In recommendation systems, features (for example, user click history) must be retrieved exactly as they existed at the moment of the historical interaction. Retrieving current features for a past event---a \"temporal leakage\"---inflates training accuracy but causes the model to fail in production. Solving this at petabyte scale requires an immutable feature ledger or a \"time-travel\" query engine, significantly increasing storage complexity."
    },
    {
      "term": "Hash Cost (SHA-256)",
      "index_head": "Cryptographic Hash!validation cost",
      "type": "bold_indexed_concept",
      "first_lineno": 1815,
      "context": "[^fn-hash-computation-overhead]: **Hash Cost (SHA-256)**\\index{Cryptographic Hash!validation cost}: Verifying the integrity of a `{python} HashValidationCost.dataset_tb_str` dataset using SHA-256 consumes approximately `{python} HashValidationCost.cpu_core_hours_str` under a pipeline-level throughput assumption of `{python} HashValidationCost.throughput_mb_per_s_str` per core. For a 30-day foundation model training run, this metadata validation step must be pipelined or performed during ingestion to avoid stalling the fleet's startup phase."
    },
    {
      "abbrev": "SHA-256",
      "canonical_expansion": "Hash Cost",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1815,
      "context": "[^fn-hash-computation-overhead]: **Hash Cost (SHA-256)**\\index{Cryptographic Hash!validation cost}: Verifying the integrity of a `{python} HashValidationCost.dataset_tb_str` dataset using SHA-256 consumes approximately `{python} HashValidationCost.cpu_core_hours_str` under a pipeline-level throughput assumption of `{python} HashValidationCost.throughput_mb_per_s_str` per core. For a 30-day foundation model training run, this metadata validation step must be pipelined or performed during ingestion to avoid stalling the fleet's startup phase."
    },
    {
      "abbrev": "DAG",
      "canonical_expansion": "pipeline forms a directed acyclic graph",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1817,
      "context": "At petabyte scale, the data pipeline has to make every transformation reproducible. Raw documents are cleaned, tokenized, packed into training examples, and sharded across storage tiers. Each stage writes an immutable artifact, and each artifact records the input version, code version, schema, and statistical checks it satisfied. Once those dependencies are explicit, the pipeline forms a directed acyclic graph (DAG): later artifacts depend on earlier artifacts, and no stage is allowed to rewrite its own history. When a training job starts, the cluster manager verifies that each worker's local NVMe shards match the data snapshot declared for the run. Treating the data pipeline as a graph of versioned artifacts rather than a simple file transfer eliminates the silent data drift that is a primary source of failure in foundation model training."
    },
    {
      "abbrev": "GDS",
      "canonical_expansion": "GPUDirect Storage",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1874,
      "context": "The storage hierarchy can provide enough aggregate bandwidth and still leave GPUs waiting when the CPU copies data for thousands of small transfers. In the traditional path, every shard read bounces through host DRAM before reaching GPU HBM; GPUDirect Storage (GDS) removes that host-memory bounce copy when local or RDMA-attached NVMe is the bottleneck. The CPU still submits and coordinates I/O through the software stack. Understanding the traditional path first makes the GDS optimization clear."
    },
    {
      "term": "GDS (GPUDirect Storage)",
      "index_head": "GPUDirect Storage!GPUDirect family",
      "type": "bold_indexed_concept",
      "first_lineno": 1878,
      "context": "[^fn-gds-gpudirect-storage]: **GDS (GPUDirect Storage)**\\index{GPUDirect Storage!GPUDirect family}: Part of NVIDIA's GPUDirect family, which also includes GPUDirect RDMA (network adapters writing directly to GPU memory) and GPUDirect Peer-to-Peer (inter-GPU memory access); GDS extends this data-copy-bypass approach to NVMe storage via the cuFile API. With RDMA and GDS, payload data can traverse the path remote storage $\\to$ network $\\to$ GPU memory without a CPU-mediated bounce copy. The CPU still issues and coordinates cuFile operations, but it spends less time copying payloads and can devote more capacity to augmentation and pipeline orchestration."
    },
    {
      "term": "GPUDirect Storage",
      "type": "callout_definition",
      "first_lineno": 1880,
      "context": "::: {#dfn-data-storage-gpu-direct-storage .callout-definition title=\"GPUDirect Storage\"}"
    },
    {
      "term": "GPUDirect Storage (GDS)",
      "index_head": "GPUDirect Storage!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1882,
      "context": "***GPUDirect Storage (GDS)***\\index{GPUDirect Storage!definition} is a technology that enables a direct DMA path between NVMe storage devices and GPU memory, bypassing the system-DRAM bounce buffer and CPU payload copies while retaining CPU submission and control."
    },
    {
      "term": "Traditional I/O",
      "index_head": "Traditional I/O",
      "type": "bold_indexed_concept",
      "first_lineno": 1885,
      "context": "2.  **Distinction**: Unlike **Traditional I/O**\\index{Traditional I/O}, where payload data is staged through host buffers and copied between them, GDS provides Direct Memory Access between the storage controller and the accelerator. CPU-issued cuFile calls and kernel storage drivers remain in the control path."
    },
    {
      "term": "Traditional path",
      "index_head": "Traditional I/O!data path",
      "type": "bold_indexed_concept",
      "first_lineno": 1908,
      "context": "**Traditional path**\\index{Traditional I/O!data path}: Each image requires a DMA from NVMe to DRAM, a host copy from kernel to user space, and a PCIe transfer to GPU. At `{python} GDSLatency.gds_total_images_per_s_str` with `{python} GDSLatency.gds_trad_us_str` of CPU time per image, the CPU spends `{python} GDSLatency.gds_trad_cpu_seconds_str` of CPU time per wall-clock second, consuming roughly `{python} GDSLatency.gds_trad_cpu_cores_str` cores worth of processing just for data movement."
    },
    {
      "term": "GDS path",
      "index_head": "GPUDirect Storage!data path",
      "type": "bold_indexed_concept",
      "first_lineno": 1910,
      "context": "**GDS path**\\index{GPUDirect Storage!data path}: Each image is transferred directly via DMA from NVMe to GPU. At `{python} GDSLatency.gds_bypass_us_str` of CPU time per image (for initiating the DMA), the CPU spends `{python} GDSLatency.gds_bypass_cpu_seconds_str` of CPU time per wall-clock second, freeing roughly `{python} GDSLatency.gds_freed_cpu_cores_str` cores for data augmentation."
    },
    {
      "term": "total cost of data delivery",
      "index_head": "Total Cost of Data!delivery",
      "type": "bold_indexed_concept",
      "first_lineno": 1962,
      "context": "A 100 TB training dataset can reside on any tier of the hierarchy, but the cost differs by orders of magnitude depending on which tier is chosen and how the data is accessed. ML storage economics\\index{Storage!economics} is not simply about choosing the cheapest tier; it is about minimizing **total cost of data delivery**\\index{Total Cost of Data!delivery}, which includes storage costs, transfer costs, and the opportunity cost of idle accelerators."
    },
    {
      "term": "Data transfer costs",
      "index_head": "Data Transfer Cost",
      "type": "bold_indexed_concept",
      "first_lineno": 2124,
      "context": "Storage cost, however, is only part of the equation. **Data transfer costs**\\index{Data Transfer Cost} can dominate, especially in cloud environments. Under the representative cross-boundary egress assumption used here, reading the 100 TB dataset from object storage to compute instances incurs a transfer charge of `{python} EconRatios.egress_100tb_usd_str`. For multi-epoch training that reads the dataset `{python} StorageIcebergCosts.epochs_str` times, the egress cost alone exceeds `{python} StorageIcebergCosts.egress_10epoch_usd_str`, more than the annual storage cost. This inversion, where *reading* data costs more than *storing* it, drives the architecture decision to cache data on local NVMe rather than streaming from object storage each epoch."
    },
    {
      "term": "Egress fees",
      "index_head": "Egress Fee",
      "type": "bold_indexed_concept",
      "first_lineno": 2132,
      "context": "- **Egress fees**\\index{Egress Fee}: A representative cross-boundary egress charge of `{python} StorageIcebergCosts.egress_rate_gb_str` makes a `{python} StorageIcebergCosts.dataset_tb_str` dataset read once per epoch across `{python} StorageIcebergCosts.epochs_str` epochs cost `{python} StorageIcebergCosts.egress_10epoch_usd_str` in egress alone."
    },
    {
      "term": "IOPS charges",
      "index_head": "IOPS!charges",
      "type": "bold_indexed_concept",
      "first_lineno": 2133,
      "context": "- **IOPS charges**\\index{IOPS!charges}: Object storage charges per-request fees. S3 Standard GET requests cost about `{python} StorageIcebergCosts.get_rate_per_1k_usd_str` per `{python} StorageIcebergCosts.get_request_unit_str`, so a dataset of `{python} StorageIcebergCosts.file_count_str` individual files, read once, costs about `{python} StorageIcebergCosts.get_request_usd_str` in GET request fees before data transfer or retrieval charges, even though the storage cost is `{python} StorageIcebergCosts.storage_monthly_usd_str`/month."
    },
    {
      "term": "Idle accelerator cost",
      "index_head": "Idle Accelerator Cost",
      "type": "bold_indexed_concept",
      "first_lineno": 2134,
      "context": "- **Idle accelerator cost**\\index{Idle Accelerator Cost}: If storage stalls reduce GPU utilization from `{python} StorageCostExtremes.util_high_pct_str` to `{python} StorageCostExtremes.util_low_pct_str` on a cluster of `{python} StorageCostExtremes.cluster_size_str` at `{python} StorageCostExtremes.gpu_hour_rate_str`, the lost compute costs `{python} StorageCostExtremes.idle_loss_daily_usd_str`/day (`{python} StorageCostExtremes.util_delta_pct_str` of `{python} StorageCostExtremes.idle_daily_usd_str` daily spend), dwarfing any storage savings."
    },
    {
      "term": "Retrieval fees",
      "index_head": "Retrieval Fee",
      "type": "bold_indexed_concept",
      "first_lineno": 2135,
      "context": "- **Retrieval fees**\\index{Retrieval Fee}: Archive storage charges retrieval fees (`{python} StorageIcebergCosts.glacier_retrieval_rate_gb_str` for Glacier) in addition to egress. Restoring a `{python} StorageIcebergCosts.dataset_tb_str` dataset from Glacier costs `{python} StorageIcebergCosts.glacier_retrieval_usd_str` in retrieval fees plus `{python} StorageIcebergCosts.glacier_egress_usd_str` in egress."
    },
    {
      "term": "build vs. buy",
      "index_head": "Build vs. Buy!storage",
      "type": "bold_indexed_concept",
      "first_lineno": 2312,
      "context": "The decision to **build vs. buy**\\index{Build vs. Buy!storage} storage infrastructure hinges on the trade-off between the high capital expenditure of an on-premises parallel file system and the recurring operational expenditure of cloud object storage. A 1-petabyte Lustre deployment costs roughly \\$3--5 million in hardware, plus an additional `{python} BuildVsBuyStorageEconomics.annual_ops_usd_str` per year in operational overhead for power, cooling, and engineering support. Storing 1 PB in a cloud object store at `{python} BuildVsBuyStorageEconomics.cloud_cost_gb_month_str` costs `{python} BuildVsBuyStorageEconomics.cloud_storage_annual_usd_str` per year for storage alone. However, egress fees for accessing the data dominate the total cost: reading that petabyte at `{python} BuildVsBuyStorageEconomics.reads_per_year_str` adds `{python} BuildVsBuyStorageEconomics.cloud_egress_annual_usd_str` in data transfer fees. Under these assumptions, annual cloud delivery costs `{python} BuildVsBuyStorageEconomics.cloud_total_annual_usd_str`; after subtracting `{python} BuildVsBuyStorageEconomics.annual_ops_usd_str` of on-premises operations, the net savings are `{python} BuildVsBuyStorageEconomics.net_annual_savings_str` per year. The resulting hardware break-even is roughly `{python} BuildVsBuyStorageEconomics.breakeven_range_str`, not 18 to 24 months. Reaching an 18- to 24-month payback requires a hotter workload with many more full-dataset reads per year, lower capital expense, or non-egress drivers such as checkpoint latency and shared-namespace requirements. For the running example, the `{python} StorageEconomicsCorpus.tokenized_text_tb_str` serialized corpus is much smaller than 1 PB, so the local-storage case is driven more by latency, repeated experiments, and checkpoint staging than by one-year egress savings alone."
    },
    {
      "term": "storage refresh cycle",
      "index_head": "Storage Refresh Cycle",
      "type": "bold_indexed_concept",
      "first_lineno": 2314,
      "context": "Hardware degradation introduces the need for a **storage refresh cycle**\\index{Storage Refresh Cycle}. NVMe drives are rated for a specific write endurance, measured in Drive Writes Per Day (DWPD) over a typical five-year warranty period. Many large-scale ML training workloads are predominantly read-heavy once the initial dataset is ingested, so drives in a training cluster can last beyond their rated write lifespan. The primary driver for a refresh cycle may therefore be density rather than wear: newer drives often provide more capacity at a similar price point. Refreshing a cluster from 7.68 TB to 15.36 TB NVMe drives doubles the local cache capacity, enabling larger datasets to be staged directly on compute nodes and reducing dependence on the parallel file system for steady-state reads."
    },
    {
      "term": "tiering strategy",
      "index_head": "Tiering Strategy",
      "type": "bold_indexed_concept",
      "first_lineno": 2333,
      "context": "The cost structure described in the preceding section turns tiering into a placement policy: keep data only as close to the accelerator as its access frequency and latency requirement justify. A **tiering strategy**\\index{Tiering Strategy} encodes that policy across the hierarchy."
    },
    {
      "term": "hot tier",
      "index_head": "Hot Tier",
      "type": "bold_indexed_concept",
      "first_lineno": 2335,
      "context": "The fastest and most expensive layer is the **hot tier**\\index{Hot Tier} (local NVMe + host DRAM), which holds data being actively consumed by the running training job. Datasets are staged from shared storage to local NVMe at job start. This tier is provisioned per-node and is not shared across the cluster, so data that multiple jobs or teams need simultaneously cannot live here alone."
    },
    {
      "term": "warm tier",
      "index_head": "Warm Tier",
      "type": "bold_indexed_concept",
      "first_lineno": 2337,
      "context": "That shared-access requirement is what justifies the **warm tier**\\index{Warm Tier} (parallel file system), which holds datasets accessed by multiple jobs or teams, shared checkpoints for fault recovery, and model artifacts under active development. The parallel file system provides the shared namespace necessary for multi-tenant access with strong consistency."
    },
    {
      "term": "cold tier",
      "index_head": "Cold Tier",
      "type": "bold_indexed_concept",
      "first_lineno": 2339,
      "context": "Below the warm tier sits the **cold tier**\\index{Cold Tier} (object storage), which serves as the canonical repository for all organizational training data. Datasets are authored and versioned in object storage, then promoted to warmer tiers when needed for training. Object storage also serves as the durable backup for checkpoints after they are staged from local NVMe through the parallel file system."
    },
    {
      "term": "archive tier",
      "index_head": "Archive Tier",
      "type": "bold_indexed_concept",
      "first_lineno": 2341,
      "context": "Data that must be retained for compliance or reproducibility but is not expected to be accessed during normal operations drops to the **archive tier**\\index{Archive Tier} (Glacier or equivalent). Lifecycle policies automatically transition data from cold to archive after a configurable period, typically 90 to 365 days since last access."
    },
    {
      "term": "Prestaging",
      "index_head": "Prestaging",
      "type": "bold_indexed_concept",
      "first_lineno": 2377,
      "context": "- **Prestaging**\\index{Prestaging}: Copy the entire dataset from object storage to local NVMe before the first training iteration begins. This gives the best steady-state performance because all reads are local, but it creates the worst job start time; staging a 10 TB dataset across 256 nodes at 500 MB/s per node takes roughly 80 seconds per node, and shared parallel file system bandwidth can extend that delay to several minutes."
    },
    {
      "term": "On-demand staging",
      "index_head": "On-Demand Staging",
      "type": "bold_indexed_concept",
      "first_lineno": 2378,
      "context": "- **On-demand staging**\\index{On-Demand Staging}: Copy shards to local NVMe only when the data loader first accesses them. This removes the startup delay but makes the first epoch pay the full network-read cost, which works best for multi-epoch training where later local reads amortize the initial staging cost."
    },
    {
      "term": "Streaming",
      "index_head": "Streaming!data staging",
      "type": "bold_indexed_concept",
      "first_lineno": 2379,
      "context": "- **Streaming**\\index{Streaming!data staging}: Keep data off local storage and read from the parallel file system or object storage every epoch. This has zero startup delay and zero local storage requirement, but every epoch pays the full network-read cost, making it appropriate for single-epoch pretraining or datasets too large for local NVMe."
    },
    {
      "abbrev": "CXL",
      "canonical_expansion": "Compute Express Link",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2385,
      "context": "While the six-tier hierarchy represents a common production baseline, memory and storage technologies can fill the bandwidth gaps between existing tiers, reshaping effective storage architecture for large-scale ML. The widest unfilled gap in the hierarchy sits between host DRAM and local NVMe. Compute Express Link (CXL)\\index{Compute Express Link}, an open standard interconnect that allows CPUs, accelerators, and memory devices to share memory with cache-coherent semantics, targets exactly this gap. CXL-attached memory provides bandwidth between that of local DRAM and NVMe (roughly 30--60 GB/s) at latencies that are also intermediate (200--500 ns). For ML workloads, CXL memory could serve as a \"Tier 1.5\" between host DRAM and local NVMe, creating a massive capacity pool for embedding tables and prefetch buffers without the latency penalty of NVMe. For the running example, CXL-attached memory could expand the effective host memory tier from 512 GB to over 4 TB per node, large enough to hold the compressed source corpus or a large local shard cache, though still short of the full `{python} StorageEconomicsCorpus.tokenized_text_tb_str` serialized training copy on one node."
    },
    {
      "term": "Computational Storage Drive (CSD)",
      "index_head": "Computational Storage Drive",
      "type": "bold_indexed_concept",
      "first_lineno": 2387,
      "context": "A different bottleneck appears when CPU-side decoding, not storage bandwidth, throttles the pipeline. The **Computational Storage Drive (CSD)**\\index{Computational Storage Drive} closes that gap by embedding processing elements (field-programmable gate arrays, or FPGAs, or simple CPU cores) directly on the SSD controller, enabling decompression, filtering, or format conversion to happen at the storage device rather than consuming host CPU cycles. For ML pipelines where CPU-side decoding is the bottleneck, computational storage could eliminate the CPU from the data path entirely, complementing GDS by offloading work that direct DMA cannot address."
    },
    {
      "abbrev": "CSD",
      "canonical_expansion": "Computational Storage Drive",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2387,
      "context": "A different bottleneck appears when CPU-side decoding, not storage bandwidth, throttles the pipeline. The **Computational Storage Drive (CSD)**\\index{Computational Storage Drive} closes that gap by embedding processing elements (field-programmable gate arrays, or FPGAs, or simple CPU cores) directly on the SSD controller, enabling decompression, filtering, or format conversion to happen at the storage device rather than consuming host CPU cycles. For ML pipelines where CPU-side decoding is the bottleneck, computational storage could eliminate the CPU from the data path entirely, complementing GDS by offloading work that direct DMA cannot address."
    },
    {
      "term": "Persistent memory",
      "index_head": "Memory!persistent",
      "type": "bold_indexed_concept",
      "first_lineno": 2389,
      "context": "Checkpoint writes remain throttled by SSD write latency, the $T_{\\text{write}}$ term the Young-Daly formula penalizes. **Persistent memory**\\index{Memory!persistent}, despite market shifts such as the discontinuation of Intel Optane, remains the architectural idea that attacks that term directly. Byte-addressable non-volatile memory that sits between DRAM and NVMe in both latency and capacity could transform checkpoint storage: the durability of an SSD with write latencies approaching DRAM would reduce $T_{\\text{write}}$ from milliseconds to microseconds, making frequent, low-overhead checkpointing more practical."
    },
    {
      "term": "model loading latency",
      "index_head": "Model Loading!latency",
      "type": "bold_indexed_concept",
      "first_lineno": 2397,
      "context": "The most critical metric for inference storage is **model loading latency**\\index{Model Loading!latency}, often called cold-start time: the duration required to load a model from storage into the accelerator's HBM. For the 175B parameter language model, the weights alone occupy 350 GB in FP16 format. Loading this sequentially from a single high-performance NVMe drive at 14 GB/s takes 25 seconds, an unacceptable delay for a user-facing application. Loading from a shared PFS at a per-node rate of 4 GB/s takes nearly 88 seconds. From object storage at 1 GB/s, the delay approaches six minutes."
    },
    {
      "term": "warm replicas",
      "index_head": "Warm Replica",
      "type": "bold_indexed_concept",
      "first_lineno": 2399,
      "context": "To reduce cold-start time, the model is sharded and loaded in parallel. If the 350 GB model is striped across 8 NVMe drives, each loading a 43.75 GB shard at 7 GB/s, the total load time drops to roughly 6 seconds. When using tensor parallelism across 8 GPUs, each GPU loads only its 43.75 GB shard, which can be accomplished in under 4 seconds by reading from host DRAM. For the most latency-sensitive applications, organizations maintain **warm replicas**\\index{Warm Replica}: one or more copies of the model weights kept preloaded in host DRAM, ready to be transferred to HBM in under two seconds. This trades DRAM capacity for near-instantaneous cold-start times."
    },
    {
      "term": "key-value cache (KV cache)",
      "index_head": "KV Cache",
      "type": "bold_indexed_concept",
      "first_lineno": 2401,
      "context": "During autoregressive generation, the server keeps a per-request record of the attention keys and values produced by earlier tokens so it can generate the next token without recomputing the entire prefix. This record, called the **key-value cache (KV cache)**\\index{KV Cache}, grows with sequence length and can consume significant HBM. For long sequences (128K tokens or more), KV cache offloading to host DRAM or NVMe extends the effective context window at the cost of increased latency per generated token, because cache blocks must be moved back into HBM before attention can use them. The storage lesson is that inference has its own working set, not just a model-loading problem."
    },
    {
      "term": "Async copy to PFS",
      "index_head": "Checkpointing!async PFS copy",
      "type": "bold_indexed_concept",
      "first_lineno": 2582,
      "context": "2. **Async copy to PFS**\\index{Checkpointing!async PFS copy}: `{python} CheckpointStormCalc.nodes_str` $\\times$ `{python} CheckpointStormCalc.per_node_shard_gb_str` $\\approx$ `{python} CheckpointStormCalc.total_pfs_tb_str` total. If the PFS provides `{python} CheckpointStormCalc.pfs_aggregate_bw_tb_per_s_str` aggregate, the storm completes in roughly `{python} CheckpointStormCalc.pfs_copy_s_str`."
    },
    {
      "term": "Per-node PFS bandwidth",
      "index_head": "Bandwidth!per-node PFS",
      "type": "bold_indexed_concept",
      "first_lineno": 2583,
      "context": "3. **Per-node PFS bandwidth**\\index{Bandwidth!per-node PFS}: If all `{python} CheckpointStormCalc.nodes_str` write simultaneously, each gets `{python} CheckpointStormCalc.pfs_aggregate_bw_gb_per_s_str` $\\div$ `{python} CheckpointStormCalc.nodes_str` = `{python} CheckpointStormCalc.per_node_pfs_bw_gb_per_s_str`."
    },
    {
      "term": "Training pause",
      "index_head": "Checkpointing!training pause",
      "type": "bold_indexed_concept",
      "first_lineno": 2584,
      "context": "4. **Training pause**\\index{Checkpointing!training pause}: Only about `{python} CheckpointStormCalc.local_write_s_str` (the local NVMe write). The PFS copy overlaps with the next training iteration."
    },
    {
      "term": "Overhead",
      "index_head": "Checkpointing!overhead",
      "type": "bold_indexed_concept",
      "first_lineno": 2585,
      "context": "5. **Overhead**\\index{Checkpointing!overhead}: `{python} CheckpointStormCalc.local_write_s_str` pause every `{python} CheckpointStormCalc.checkpoint_interval_s_str` $\\approx$ `{python} CheckpointStormCalc.overhead_pct_str` training time lost to checkpointing."
    },
    {
      "term": "tiered staging",
      "index_head": "Tiered Staging!checkpoint storage",
      "type": "bold_indexed_concept",
      "first_lineno": 2591,
      "context": "The **tiered staging**\\index{Tiered Staging!checkpoint storage} strategy minimizes $T_{\\text{write}}$ by writing in two phases. In the first phase, each node writes its checkpoint shard to local NVMe at full bandwidth. With `{python} CheckpointStormCalc.local_drive_count_str` NVMe drives providing `{python} CheckpointStormCalc.local_bw_gb_per_s_str` aggregate, a per-node shard of roughly `{python} CheckpointStormCalc.per_node_shard_gb_str` (a `{python} CheckpointStormCalc.nodes_str`-node ZeRO-3 split of the `{python} CheckpointTotalGbRecap.ckpt_total_gb_str` total checkpoint) completes in roughly `{python} CheckpointStormCalc.local_write_s_str`. The training pipeline can resume immediately after the local write completes."
    },
    {
      "term": "checkpoint format optimization",
      "index_head": "Checkpoint Format Optimization",
      "type": "bold_indexed_concept",
      "first_lineno": 2682,
      "context": "Beyond the choice of synchronous or asynchronous methods, **checkpoint format optimization**\\index{Checkpoint Format Optimization} reduces the per-node I/O volume. In the simplest data-parallel setup, every node holds an identical copy of the model weights, so a naive checkpoint would redundantly save the same data from every node. Sharded checkpoint protocols avoid that redundancy by saving shards instead of full replicas. DeepSpeed's **zero-redundancy checkpointing**\\index{Checkpointing!zero-redundancy} uses its ZeRO optimizer-state partitioning: each of the $H$ nodes saves only its $1/H$ shard of the optimizer state, and the system saves one consolidated copy of the weights. PyTorch's **Distributed Checkpoint (DCP)**\\index{Distributed Checkpoint} protocol follows the same storage principle by writing each rank's sharded data to a distinct file. For the 175B model using ZeRO-3 across `{python} CheckpointStormCalc.nodes_str` nodes, the result is concrete: each node writes only its roughly `{python} CheckpointStormCalc.per_node_shard_gb_str` shard of the total `{python} CheckpointTotalGbRecap.ckpt_total_gb_str` state, reducing the local write from minutes to around a second on one NVMe drive, or well below a second on a local stripe. The tiered-staging derivation earlier in this section already threaded these per-node shard sizes and write times through the running example, and the same arithmetic governs distributed coordination: the exposed pipeline pause is the local NVMe write, while the durable copy to the parallel file system overlaps with subsequent training."
    },
    {
      "term": "zero-redundancy checkpointing",
      "index_head": "Checkpointing!zero-redundancy",
      "type": "bold_indexed_concept",
      "first_lineno": 2682,
      "context": "Beyond the choice of synchronous or asynchronous methods, **checkpoint format optimization**\\index{Checkpoint Format Optimization} reduces the per-node I/O volume. In the simplest data-parallel setup, every node holds an identical copy of the model weights, so a naive checkpoint would redundantly save the same data from every node. Sharded checkpoint protocols avoid that redundancy by saving shards instead of full replicas. DeepSpeed's **zero-redundancy checkpointing**\\index{Checkpointing!zero-redundancy} uses its ZeRO optimizer-state partitioning: each of the $H$ nodes saves only its $1/H$ shard of the optimizer state, and the system saves one consolidated copy of the weights. PyTorch's **Distributed Checkpoint (DCP)**\\index{Distributed Checkpoint} protocol follows the same storage principle by writing each rank's sharded data to a distinct file. For the 175B model using ZeRO-3 across `{python} CheckpointStormCalc.nodes_str` nodes, the result is concrete: each node writes only its roughly `{python} CheckpointStormCalc.per_node_shard_gb_str` shard of the total `{python} CheckpointTotalGbRecap.ckpt_total_gb_str` state, reducing the local write from minutes to around a second on one NVMe drive, or well below a second on a local stripe. The tiered-staging derivation earlier in this section already threaded these per-node shard sizes and write times through the running example, and the same arithmetic governs distributed coordination: the exposed pipeline pause is the local NVMe write, while the durable copy to the parallel file system overlaps with subsequent training."
    },
    {
      "term": "Distributed Checkpoint (DCP)",
      "index_head": "Distributed Checkpoint",
      "type": "bold_indexed_concept",
      "first_lineno": 2682,
      "context": "Beyond the choice of synchronous or asynchronous methods, **checkpoint format optimization**\\index{Checkpoint Format Optimization} reduces the per-node I/O volume. In the simplest data-parallel setup, every node holds an identical copy of the model weights, so a naive checkpoint would redundantly save the same data from every node. Sharded checkpoint protocols avoid that redundancy by saving shards instead of full replicas. DeepSpeed's **zero-redundancy checkpointing**\\index{Checkpointing!zero-redundancy} uses its ZeRO optimizer-state partitioning: each of the $H$ nodes saves only its $1/H$ shard of the optimizer state, and the system saves one consolidated copy of the weights. PyTorch's **Distributed Checkpoint (DCP)**\\index{Distributed Checkpoint} protocol follows the same storage principle by writing each rank's sharded data to a distinct file. For the 175B model using ZeRO-3 across `{python} CheckpointStormCalc.nodes_str` nodes, the result is concrete: each node writes only its roughly `{python} CheckpointStormCalc.per_node_shard_gb_str` shard of the total `{python} CheckpointTotalGbRecap.ckpt_total_gb_str` state, reducing the local write from minutes to around a second on one NVMe drive, or well below a second on a local stripe. The tiered-staging derivation earlier in this section already threaded these per-node shard sizes and write times through the running example, and the same arithmetic governs distributed coordination: the exposed pipeline pause is the local NVMe write, while the durable copy to the parallel file system overlaps with subsequent training."
    },
    {
      "abbrev": "DCP",
      "canonical_expansion": "Distributed Checkpoint",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2682,
      "context": "Beyond the choice of synchronous or asynchronous methods, **checkpoint format optimization**\\index{Checkpoint Format Optimization} reduces the per-node I/O volume. In the simplest data-parallel setup, every node holds an identical copy of the model weights, so a naive checkpoint would redundantly save the same data from every node. Sharded checkpoint protocols avoid that redundancy by saving shards instead of full replicas. DeepSpeed's **zero-redundancy checkpointing**\\index{Checkpointing!zero-redundancy} uses its ZeRO optimizer-state partitioning: each of the $H$ nodes saves only its $1/H$ shard of the optimizer state, and the system saves one consolidated copy of the weights. PyTorch's **Distributed Checkpoint (DCP)**\\index{Distributed Checkpoint} protocol follows the same storage principle by writing each rank's sharded data to a distinct file. For the 175B model using ZeRO-3 across `{python} CheckpointStormCalc.nodes_str` nodes, the result is concrete: each node writes only its roughly `{python} CheckpointStormCalc.per_node_shard_gb_str` shard of the total `{python} CheckpointTotalGbRecap.ckpt_total_gb_str` state, reducing the local write from minutes to around a second on one NVMe drive, or well below a second on a local stripe. The tiered-staging derivation earlier in this section already threaded these per-node shard sizes and write times through the running example, and the same arithmetic governs distributed coordination: the exposed pipeline pause is the local NVMe write, while the durable copy to the parallel file system overlaps with subsequent training."
    },
    {
      "abbrev": "FP16",
      "canonical_expansion": "Model weights",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2729,
      "context": "| Model weights (FP16)                       |                          350 GB                          | GPU HBM (distributed)                                      |"
    },
    {
      "abbrev": "FP32",
      "canonical_expansion": "Optimizer state",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2730,
      "context": "| Optimizer state (FP32)                     |                         1,400 GB                         | GPU HBM (ZeRO-partitioned)                                 |"
    },
    {
      "abbrev": "ZeRO-partitioned",
      "canonical_expansion": "GPU HBM",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2730,
      "context": "| Optimizer state (FP32)                     |                         1,400 GB                         | GPU HBM (ZeRO-partitioned)                                 |"
    },
    {
      "term": "sequential read bandwidth",
      "index_head": "Sequential Read Bandwidth",
      "type": "bold_indexed_concept",
      "first_lineno": 2786,
      "context": "This architectural pattern shifts the primary storage bottleneck from **sequential read bandwidth**\\index{Sequential Read Bandwidth} to **random-access IOPS**\\index{Random-Access IOPS}. Vector databases must traverse high-dimensional graph indexes to find approximate nearest neighbors across billions of embeddings. The performance is governed by a strict physical constraint: the **Recall vs. Latency vs. Capacity**\\index{Recall vs. Latency vs. Capacity} trade-off. Maximizing recall requires traversing a larger portion of the index, driving up IOPS and latency. The index is large because each embedded document chunk occupies substantial storage: a `{python} VectorIndexFootprint.embedding_dim_str`-dimension embedding in FP32 (`{python} VectorIndexFootprint.bytes_per_element_str` bytes per value) consumes approximately `{python} VectorIndexFootprint.per_vector_kb_str` per vector, so a billion-document corpus requires roughly `{python} VectorIndexFootprint.raw_vector_tb_str` of raw vector storage before the graph adjacency lists that enable fast navigation. If those adjacency lists add `{python} VectorIndexFootprint.adjacency_bytes_range_str` per node, they add roughly `{python} VectorIndexFootprint.adjacency_overhead_range_str`, making raw vectors plus minimal graph metadata about `{python} VectorIndexFootprint.total_high_str` at the high end for a billion-entry corpus. Full production indexes can be larger once identifiers, graph levels, allocator overhead, deleted-entry slack, and replication are included, but those are implementation overheads rather than the raw vector-plus-adjacency footprint. At that scale, the index no longer fits in DRAM\\index{HNSW!DRAM footprint}, which is the design pressure that motivates disk-backed alternatives. In-memory hierarchical navigable small world (HNSW) graph indexes\\index{HNSW} make the same graph-navigation idea fast when the index fits in DRAM [@malkov2020hnsw], while hybrid search\\index{Hybrid Search} combines vector similarity with lexical or metadata filters and therefore stresses both random vector traversal and document fetches. Disk-backed graph indexes such as DiskANN show how billion-scale search can trade DRAM footprint against SSD-backed graph traversal while targeting high recall and low query latency [@subramanya2019diskann]."
    },
    {
      "term": "random-access IOPS",
      "index_head": "Random-Access IOPS",
      "type": "bold_indexed_concept",
      "first_lineno": 2786,
      "context": "This architectural pattern shifts the primary storage bottleneck from **sequential read bandwidth**\\index{Sequential Read Bandwidth} to **random-access IOPS**\\index{Random-Access IOPS}. Vector databases must traverse high-dimensional graph indexes to find approximate nearest neighbors across billions of embeddings. The performance is governed by a strict physical constraint: the **Recall vs. Latency vs. Capacity**\\index{Recall vs. Latency vs. Capacity} trade-off. Maximizing recall requires traversing a larger portion of the index, driving up IOPS and latency. The index is large because each embedded document chunk occupies substantial storage: a `{python} VectorIndexFootprint.embedding_dim_str`-dimension embedding in FP32 (`{python} VectorIndexFootprint.bytes_per_element_str` bytes per value) consumes approximately `{python} VectorIndexFootprint.per_vector_kb_str` per vector, so a billion-document corpus requires roughly `{python} VectorIndexFootprint.raw_vector_tb_str` of raw vector storage before the graph adjacency lists that enable fast navigation. If those adjacency lists add `{python} VectorIndexFootprint.adjacency_bytes_range_str` per node, they add roughly `{python} VectorIndexFootprint.adjacency_overhead_range_str`, making raw vectors plus minimal graph metadata about `{python} VectorIndexFootprint.total_high_str` at the high end for a billion-entry corpus. Full production indexes can be larger once identifiers, graph levels, allocator overhead, deleted-entry slack, and replication are included, but those are implementation overheads rather than the raw vector-plus-adjacency footprint. At that scale, the index no longer fits in DRAM\\index{HNSW!DRAM footprint}, which is the design pressure that motivates disk-backed alternatives. In-memory hierarchical navigable small world (HNSW) graph indexes\\index{HNSW} make the same graph-navigation idea fast when the index fits in DRAM [@malkov2020hnsw], while hybrid search\\index{Hybrid Search} combines vector similarity with lexical or metadata filters and therefore stresses both random vector traversal and document fetches. Disk-backed graph indexes such as DiskANN show how billion-scale search can trade DRAM footprint against SSD-backed graph traversal while targeting high recall and low query latency [@subramanya2019diskann]."
    },
    {
      "term": "Recall vs. Latency vs. Capacity",
      "index_head": "Recall vs. Latency vs. Capacity",
      "type": "bold_indexed_concept",
      "first_lineno": 2786,
      "context": "This architectural pattern shifts the primary storage bottleneck from **sequential read bandwidth**\\index{Sequential Read Bandwidth} to **random-access IOPS**\\index{Random-Access IOPS}. Vector databases must traverse high-dimensional graph indexes to find approximate nearest neighbors across billions of embeddings. The performance is governed by a strict physical constraint: the **Recall vs. Latency vs. Capacity**\\index{Recall vs. Latency vs. Capacity} trade-off. Maximizing recall requires traversing a larger portion of the index, driving up IOPS and latency. The index is large because each embedded document chunk occupies substantial storage: a `{python} VectorIndexFootprint.embedding_dim_str`-dimension embedding in FP32 (`{python} VectorIndexFootprint.bytes_per_element_str` bytes per value) consumes approximately `{python} VectorIndexFootprint.per_vector_kb_str` per vector, so a billion-document corpus requires roughly `{python} VectorIndexFootprint.raw_vector_tb_str` of raw vector storage before the graph adjacency lists that enable fast navigation. If those adjacency lists add `{python} VectorIndexFootprint.adjacency_bytes_range_str` per node, they add roughly `{python} VectorIndexFootprint.adjacency_overhead_range_str`, making raw vectors plus minimal graph metadata about `{python} VectorIndexFootprint.total_high_str` at the high end for a billion-entry corpus. Full production indexes can be larger once identifiers, graph levels, allocator overhead, deleted-entry slack, and replication are included, but those are implementation overheads rather than the raw vector-plus-adjacency footprint. At that scale, the index no longer fits in DRAM\\index{HNSW!DRAM footprint}, which is the design pressure that motivates disk-backed alternatives. In-memory hierarchical navigable small world (HNSW) graph indexes\\index{HNSW} make the same graph-navigation idea fast when the index fits in DRAM [@malkov2020hnsw], while hybrid search\\index{Hybrid Search} combines vector similarity with lexical or metadata filters and therefore stresses both random vector traversal and document fetches. Disk-backed graph indexes such as DiskANN show how billion-scale search can trade DRAM footprint against SSD-backed graph traversal while targeting high recall and low query latency [@subramanya2019diskann]."
    },
    {
      "abbrev": "HNSW",
      "canonical_expansion": "In-memory hierarchical navigable small world",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2786,
      "context": "This architectural pattern shifts the primary storage bottleneck from **sequential read bandwidth**\\index{Sequential Read Bandwidth} to **random-access IOPS**\\index{Random-Access IOPS}. Vector databases must traverse high-dimensional graph indexes to find approximate nearest neighbors across billions of embeddings. The performance is governed by a strict physical constraint: the **Recall vs. Latency vs. Capacity**\\index{Recall vs. Latency vs. Capacity} trade-off. Maximizing recall requires traversing a larger portion of the index, driving up IOPS and latency. The index is large because each embedded document chunk occupies substantial storage: a `{python} VectorIndexFootprint.embedding_dim_str`-dimension embedding in FP32 (`{python} VectorIndexFootprint.bytes_per_element_str` bytes per value) consumes approximately `{python} VectorIndexFootprint.per_vector_kb_str` per vector, so a billion-document corpus requires roughly `{python} VectorIndexFootprint.raw_vector_tb_str` of raw vector storage before the graph adjacency lists that enable fast navigation. If those adjacency lists add `{python} VectorIndexFootprint.adjacency_bytes_range_str` per node, they add roughly `{python} VectorIndexFootprint.adjacency_overhead_range_str`, making raw vectors plus minimal graph metadata about `{python} VectorIndexFootprint.total_high_str` at the high end for a billion-entry corpus. Full production indexes can be larger once identifiers, graph levels, allocator overhead, deleted-entry slack, and replication are included, but those are implementation overheads rather than the raw vector-plus-adjacency footprint. At that scale, the index no longer fits in DRAM\\index{HNSW!DRAM footprint}, which is the design pressure that motivates disk-backed alternatives. In-memory hierarchical navigable small world (HNSW) graph indexes\\index{HNSW} make the same graph-navigation idea fast when the index fits in DRAM [@malkov2020hnsw], while hybrid search\\index{Hybrid Search} combines vector similarity with lexical or metadata filters and therefore stresses both random vector traversal and document fetches. Disk-backed graph indexes such as DiskANN show how billion-scale search can trade DRAM footprint against SSD-backed graph traversal while targeting high recall and low query latency [@subramanya2019diskann]."
    },
    {
      "term": "Data Wall",
      "index_head": "Data Wall!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 2794,
      "context": "The storage architecture changes again when the fleet exhausts its supply of human-authored training data, because synthesized data must carry its own provenance. As high-quality human-generated text becomes harder to expand, the fleet encounters the **Data Wall**\\index{Data Wall!definition}: the point where additional training samples must increasingly come from synthesis, simulation, curation, or expensive human collection [@villalobos2022will]. This shift from simple collection to managed data generation sends more than payload bytes through the hierarchy. A \"synthetic fuel line\" must also store the **Provenance Chain**\\index{Provenance Chain!definition} of every sample: which model generated it, which filters accepted or rejected it, which judge or verifier models scored it, and which downstream run consumed it. Without that lineage, the system risks **Model Collapse**\\index{Model Collapse!definition}, where a model degrades by training on its own unverified errors [@shumailov2024]."
    },
    {
      "term": "Provenance Chain",
      "index_head": "Provenance Chain!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 2794,
      "context": "The storage architecture changes again when the fleet exhausts its supply of human-authored training data, because synthesized data must carry its own provenance. As high-quality human-generated text becomes harder to expand, the fleet encounters the **Data Wall**\\index{Data Wall!definition}: the point where additional training samples must increasingly come from synthesis, simulation, curation, or expensive human collection [@villalobos2022will]. This shift from simple collection to managed data generation sends more than payload bytes through the hierarchy. A \"synthetic fuel line\" must also store the **Provenance Chain**\\index{Provenance Chain!definition} of every sample: which model generated it, which filters accepted or rejected it, which judge or verifier models scored it, and which downstream run consumed it. Without that lineage, the system risks **Model Collapse**\\index{Model Collapse!definition}, where a model degrades by training on its own unverified errors [@shumailov2024]."
    },
    {
      "term": "Model Collapse",
      "index_head": "Model Collapse!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 2794,
      "context": "The storage architecture changes again when the fleet exhausts its supply of human-authored training data, because synthesized data must carry its own provenance. As high-quality human-generated text becomes harder to expand, the fleet encounters the **Data Wall**\\index{Data Wall!definition}: the point where additional training samples must increasingly come from synthesis, simulation, curation, or expensive human collection [@villalobos2022will]. This shift from simple collection to managed data generation sends more than payload bytes through the hierarchy. A \"synthetic fuel line\" must also store the **Provenance Chain**\\index{Provenance Chain!definition} of every sample: which model generated it, which filters accepted or rejected it, which judge or verifier models scored it, and which downstream run consumed it. Without that lineage, the system risks **Model Collapse**\\index{Model Collapse!definition}, where a model degrades by training on its own unverified errors [@shumailov2024]."
    },
    {
      "term": "Raw Payload",
      "index_head": "Synthetic Data!raw payload",
      "type": "bold_indexed_concept",
      "first_lineno": 2844,
      "context": "1. **Raw Payload**\\index{Synthetic Data!raw payload}: `{python} SyntheticStorage.data_size_tb_str`."
    },
    {
      "term": "Provenance Overhead",
      "index_head": "Synthetic Data!provenance overhead",
      "type": "bold_indexed_concept",
      "first_lineno": 2845,
      "context": "2. **Provenance Overhead**\\index{Synthetic Data!provenance overhead}: `{python} SyntheticStorage.provenance_overhead_pct_str` extra for lineage hashes, generation logs, and reward-model scores."
    },
    {
      "term": "Verification Factor",
      "index_head": "Synthetic Data!verification factor",
      "type": "bold_indexed_concept",
      "first_lineno": 2846,
      "context": "3. **Verification Factor**\\index{Synthetic Data!verification factor}: To avoid \"self-poisoning,\" each sample is verified by `{python} SyntheticStorage.verification_passes_str` independent \"judge\" models."
    },
    {
      "abbrev": "LZ4",
      "canonical_expansion": "use uncompressed or lightly compressed formats",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2945,
      "context": "This is only true if the pipeline is I/O-bound. Compression reduces I/O volume but increases CPU load due to decompression. For a CPU-bound pipeline where complex data augmentation already saturates the host processor, adding decompression work makes the bottleneck *worse*, not *better*, leading to a net decrease in throughput to the accelerator. The correct approach is to profile the pipeline first: if the host CPU is the bottleneck, use uncompressed or lightly compressed formats (LZ4); if storage or network I/O is the bottleneck, use aggressive compression (zstd, gzip). The optimal compression level is a property of the pipeline's bottleneck, not a universal constant."
    }
  ]
}