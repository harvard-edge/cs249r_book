# Verified findings — model_compression.qmd (vol1)
Prior findings: 4 | Survived: 0 | Refuted: 4

## SURVIVING findings (genuinely under-explained — keep rewrite)

None.

## REFUTED findings (explanation found in neighborhood — drop)

- `fig-kd-overview` — REFUTED: explanation is in payoff ¶ L2099–2113 (the "Distillation mathematics" subsection) and in the caption. The first pass objected that the ref sentence ends in a colon and never explains why the two-path structure matters. In fact the payoff paragraphs immediately after the figure spell out the temperature-scaled softmax in the teacher path, the total loss formula with mixing coefficient $\gamma_{\text{KD}}$, and the explicit statement that balancing the two terms controls how much dark knowledge the student absorbs: "The total loss $\mathcal{L}_{\text{distill}}$ balances standard cross-entropy with the KL divergence... Here $\gamma_{\text{KD}} \in [0,1]$ weights the hard-label and distillation terms." The caption also names both paths. The announcer-colon shape of the ref sentence is real, but the payoff carries the full explanation the first pass said was absent.

- `fig-quantization-roadmap` — REFUTED: explanation is in prev ¶ L3795. The paragraph immediately before the ref sentence states the three tiers and their decision logic directly: "Post-training quantization (PTQ) reduces precision after training, requiring no retraining and minimal engineering effort. Quantization-aware training (QAT) incorporates quantization effects into the training loop, enabling models to adapt to lower precision and retain higher accuracy. Mixed-precision training assigns different precision levels to different operations, matching precision to each layer's sensitivity." That maps each tier to its effort level and purpose, which is the decision-ladder content the first pass said was missing. The ref sentence restates the caption, which is a prose-quality concern rather than a float-explanation failure; the neighborhood passes the refutation bar.

- `fig-color-mapping` — REFUTED: explanation is in caption and in the figure's own annotations. The caption explicitly states "analyzing weight distributions helps diagnose issues like dead or saturated filters." The TikZ figure labels the bottom row "Dead filters / Uniform = no learning" with the annotation "dead filters indicate pruning candidates" (L7706–7710). The first pass argued the diagnostic conclusion (dead filter after pruning signals over-aggressive threshold) is absent; in fact the figure visually encodes and labels that conclusion, and the caption names the diagnostic action. The neighborhood meets the refutation bar.

- `fig-sparse-heat-map` — REFUTED: explanation is in the caption and in the figure's own annotations. The caption says "The heatmap reveals how pruning affects different layers nonuniformly, with later layers typically exhibiting higher sparsity than early feature-extraction layers" — directly communicating the norm (late layers sparse, early layers preserved) against which a practitioner reads the visual. The figure annotations (L7984–7995) reinforce this with explicit labels: "Low sparsity (early layers preserved)" and "High sparsity / FC layers: most parameters pruned." The first pass argued the implication (high early-layer sparsity = over-pruning) is never stated; the caption establishes the directional norm from which that implication follows immediately. The neighborhood passes the refutation bar.
