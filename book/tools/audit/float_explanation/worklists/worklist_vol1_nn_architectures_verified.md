# Verified findings — nn_architectures.qmd (vol1)
Prior findings: 3 | Survived: 0 | Refuted: 3

## SURVIVING findings (genuinely under-explained — keep rewrite)

None.

## REFUTED findings (explanation found in neighborhood — drop)

- `fig-mlp` — REFUTED: explanation is in caption (L847) and prev ¶ (L843): caption reads "illustrating the dense $\mathcal{O}(N \times M)$ connectivity pattern implemented through matrix multiplications. For MNIST classification, a 784-dimensional input connects to 100 hidden neurons through a $784{\times}100$ weight matrix, requiring 78,400 multiply-accumulate operations per sample." The prev ¶ at L843 also explains dense all-to-all connectivity; the ref sentence at L845 is an announcer shape, but the announcer-shape rule is not a survival criterion when caption and neighborhood carry the takeaway. The first pass was correct that the ref sentence is weak, but incorrect that the float is under-explained — the caption does the explanatory work the ref sentence omits.

- `fig-cnn-spatial-processing` — REFUTED: explanation is in caption (L1237) and the first-reference paragraph (L1455). The caption reads "Convolutional neural networks identify patterns independent of their location in an image by applying learnable filters across the input, enabling robust object recognition. These filters detect local features, and their repeated application across the image creates translation equivariance, preserving spatial relationships between detected patterns regardless of position." A reader encountering the figure at its printed position (L1237) has the caption immediately available; that caption fully states what the figure shows and why it matters. The finding's diagnosis is positional (figure appears 218 lines before its prose pointer), but the bar requires that explanation be absent from the neighborhood of EITHER the figure OR its reference — and the caption is in the figure's neighborhood. The L1455 reference paragraph additionally provides a thorough explanation. Positional mismatch alone does not constitute under-explanation when the caption is rich.

- `fig-transformer-attention-visualized` — REFUTED: explanation is in caption (L2348) and the reference paragraph (L2398). The caption reads "An attention head from a transformer (layer 4, head 2) resolving the pronoun 'they.' Line thickness indicates attention weight magnitude: the model attends most strongly to 'student' and 'finish,' with weaker contextual links to nearby function words. This dynamic routing replaces RNN sequential processing with $\mathcal{O}(1)$ information flow depth, enabling parallel computation across all positions simultaneously." The preceding paragraph at L2346 sets up the conceptual need (dynamic relationships based on content) without pointing to the figure, and the reference paragraph at L2398 provides a full walkthrough of what the figure shows. As with `fig-cnn-spatial-processing`, the finding's only live complaint is positional: the first prose pointer arrives 50 lines after the figure definition. Because the caption at placement position already explains the figure's content and the reference paragraph when it arrives is detailed, no genuine gap in explanation exists.
