chapter: vol1/appendix_algorithm
source_file: book/quarto/contents/vol1/backmatter/appendix_algorithm.qmd
status: migrated
python_cells:
- label: appendix-algorithm-setup
  start_line: 26
  end_line: 120
  lego_class: AlgorithmFoundations
  purpose: Compute sparse-vs-dense storage, FP16 GEMM arithmetic intensity, A100 ridge
    point, and generic training-memory byte summaries for the appendix.
  constants:
  - name: precision_byte_widths
    value: BYTES_FP32, BYTES_FP16, BYTES_INT32
    kind: unit_conversion
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: Sparse-format and training-memory prose already render byte widths
      from MLSysIM precision constants.
  - name: A100 roofline constants
    value: A100_FLOPS_FP16_TENSOR, A100_MEM_BW
    kind: hardware_system_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.Hardware.Cloud.A100.compute.peak_flops, mlsysim.Hardware.Cloud.A100.memory.bandwidth
    preserves_rendered_value: true
    narrative_check: The small-GEMM efficiency comparison remains anchored to the
      A100 ridge point.
  - name: embedding_matrix_example
    value: 100K rows, 10K columns, 1 percent density
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The prose explicitly introduces a concrete vocabulary embedding
      example rather than a named model constant.
  - name: n_small
    value: '64'
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The small GEMM dimension is intentionally illustrative and does
      not claim a hardware tile constant.
  - name: sparse_metadata_widths
    value: CSR = FP32 value + INT32 index; COO = FP32 value + 2 INT32 indices
    kind: derived_quantity
    current_source: derived
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: Break-even percentages are derived from MLSysIM byte widths and
      sparse-format definitions.
  - name: gemm_intensity_formula
    value: 2n^3 / (3n^2 * BYTES_FP16) = n/3 FLOP/byte
    kind: derived_quantity
    current_source: derived
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The formula is local mathematical exposition, with precision
      width sourced from MLSysIM.
  - name: optimizer_overhead_str
    value: 8-12 bytes per parameter
    kind: unit_conversion
    current_source: local_literal
    should_change: false
    target_source: BYTES_ADAM_STATE for momentum/variance plus BYTES_FP32 for the
      optional FP32 master weight, or a new full Adam-state helper
    preserves_rendered_value: true
    narrative_check: The prose saying Adam optimizer state is 8-12 bytes and nonactivation
      overhead is 12-16 bytes remains true if the range is derived from MLSysIM byte
      constants.
- label: gpt2-training-memory
  start_line: 330
  end_line: 413
  lego_class: GPT2TrainingMem
  purpose: Compute GPT-2 XL training memory for BF16 weights/gradients, full Adam
    optimizer state, retained transformer activations, and A100 memory headroom.
  constants:
  - name: GPT-2 XL architecture
    value: GPT2_PARAMS, GPT2_LAYERS, GPT2_HIDDEN_DIM
    kind: model_architecture_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.Models.Language.GPT2.parameters, mlsysim.Models.Language.GPT2.layers,
      mlsysim.Models.Language.GPT2.hidden_dim
    preserves_rendered_value: true
    narrative_check: The worked example explicitly names GPT-2 XL and already uses
      MLSysIM model constants.
  - name: precision_byte_widths
    value: BYTES_FP16, BYTES_FP32
    kind: unit_conversion
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: Weight and gradient byte widths are source-of-truth.
  - name: A100_MEM_CAPACITY
    value: 80 GiB converted to decimal GB
    kind: hardware_system_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: The prose says 80 GB-class A100/H100 and then displays the decimal-unit
      capacity, so the existing conversion is narratively valid.
  - name: batch_small, batch_large, seq_len
    value: 8, 64, 1024
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: These define the worked training-memory scenario and should remain
      local unless the same scenario becomes a reusable benchmark profile.
  - name: optimizer_bytes_per_param
    value: 12 bytes per parameter
    kind: unit_conversion
    current_source: local_literal
    should_change: false
    target_source: 3 * BYTES_FP32, or a named BYTES_ADAM_FP32_FULL_STATE/full Adam
      training-state helper
    preserves_rendered_value: true
    narrative_check: The optimizer line remains FP32 master + momentum + variance,
      with the same 18.0 GB rendered value for GPT-2 XL.
  - name: act_factor
    value: 12 retained BF16 elements per token per hidden dimension per layer
    kind: formula_coefficient
    current_source: local_literal
    should_change: false
    target_source: new mlsysim.physics transformer_training_activation_bytes(...)
      helper or named TRANSFORMER_TRAINING_ACTIVATION_FACTOR
    preserves_rendered_value: true
    narrative_check: The prose already explains the 12x decomposition; a helper seeded
      with the same convention preserves the activation totals and checkpointing narrative.
  - name: activation_memory_quantities
    value: layers * act_factor * batch * seq_len * hidden_dim * BYTES_FP16
    kind: derived_quantity
    current_source: derived
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: Activation totals are derived from GPT-2 constants, scenario
      batch/sequence settings, and the retained-activation convention.
findings:
- id: appendix-algorithm-adam-state-bytes-local
  severity: medium
  lines: 117,371,423,480
  issue: 'The appendix encodes Adam optimizer byte overhead as local text/literals:
    8-12 bytes in the setup cell and 12 bytes in the GPT-2 worked example, even though
    MLSysIM already exposes BYTES_ADAM_STATE for the two FP32 moment tensors and BYTES_FP32
    for the FP32 master weight.'
  recommended_change: Derive the displayed 8-12 byte range from BYTES_ADAM_STATE and
    BYTES_FP32, and derive the GPT-2 full Adam value as 3 * BYTES_FP32 or through
    a named full-Adam training-state helper.
  mlsysim_target: mlsysim.core.units.BYTES_ADAM_STATE; mlsysim.core.units.BYTES_FP32;
    proposed BYTES_ADAM_FP32_FULL_STATE or training-state formula helper
  render_impact: No rendered change expected; 8-12 bytes, 12-16 total nonactivation
    bytes, and GPT-2 optimizer memory should remain the same.
  prose_validation: The adjacent prose remains valid because it describes momentum,
    variance, and optional FP32 master weights rather than a scenario-specific assumption.
- id: appendix-algorithm-transformer-activation-factor-local
  severity: low
  lines: 380-383,428-431
  issue: The GPT-2 training-memory cell uses a local retained-activation factor of
    12 for transformer training memory. The prose explains the decomposition, but
    the convention is reusable and should not drift across chapters.
  recommended_change: Add or reuse an MLSysIM formula helper for transformer training
    activation memory, with an explicit default retained-activation factor and documented
    components.
  mlsysim_target: mlsysim.physics.transformer_training_activation_bytes(...)
    or proposed mlsysim.core.units.TRANSFORMER_TRAINING_ACTIVATION_FACTOR
  render_impact: No rendered change expected if the helper preserves the current 12x
    convention.
  prose_validation: The narrative remains true because it already spells out input
    activations, QKV projections, attention output, FFN intermediate, output, and
    norms/masks as the source of the factor.
notes:
- No code or prose files were edited for this audit; only this YAML packet was added.
- Scenario literals such as the 100K x 10K embedding example, 1 percent density, 64
  x 64 GEMM, batch sizes, and sequence length are intentionally local worked-example
  assumptions.
- A100 hardware constants, GPT-2 architecture constants, and precision byte widths
  are already sourced from MLSysIM in the audited cells.
