% before-body-includes-vol2.tex
%
% This file is loaded via Quarto's `include-before-body` for vol2 builds. It
% runs AFTER the Quarto titlepage extension has already rendered the title
% page, so it is NOT a useful place to inject a half-title page.
%
% The half-title page for vol2 lives in tex/halftitle-vol2.tex and is loaded
% via `include-in-header` so it can register a \halftitle macro that fires
% from inside the redefined \frontmatter (see header-includes.tex), which
% runs BEFORE the titlepage extension's title page block.
%
% Currently this file is intentionally empty.
