# `@staffml/vault-types`

Shared TypeScript types for the StaffML vault (`index.ts`). Consumed by StaffML and workers via relative/workspace imports.

There is **no** `package-lock.json` here: this package has **no npm dependencies**—only type definitions. Add a lockfile if you later introduce dependencies or a publish flow that requires reproducible installs.
