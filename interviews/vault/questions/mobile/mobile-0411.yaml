schema_version: '1.0'
id: mobile-0411
track: mobile
level: L3
zone: implement
topic: real-time-deadlines
competency_area: precision
bloom_level: apply
phase: inference
title: The Janky AR Filter
scenario: You're trying to optimize an AR style transfer filter for a mobile app.
  The goal is a smooth 30 FPS (a 33ms per-frame budget). Currently, the FP16 model
  runs on the mobile NPU, but inference takes ~40ms, causing noticeable 'jank'. A
  profiler analysis reveals that one specific layer—a large depth-wise separable convolution—has
  a very wide and unpredictable activation distribution, making it prone to visual
  artifacts if quantized. The other 49 layers are standard, well-behaved convolutions.
  Your task is to solve the performance problem. Which approach should you apply?
question: Which mixed-precision plan gets an AR style transfer filter under 33 ms
  while protecting one unstable depthwise layer?
details:
  realistic_solution: The best solution is to use **mixed precision**. By quantizing
    the 49 well-behaved layers to INT8, you can capture most of the potential performance
    gain. You would then selectively keep the one problematic depth-wise convolution
    layer in its original FP16 precision. This hybrid approach balances the need for
    speed with the need to preserve visual quality by protecting the most sensitive
    part of the model from aggressive quantization. Modern mobile inference frameworks
    (like Core ML or TensorFlow Lite) explicitly support this by allowing you to specify
    the precision for individual layers.
  common_mistake: Engineers often take an all-or-nothing approach. They either try
    to quantize the entire model to INT8, ignoring the risk of quality degradation,
    or they give up on quantization entirely and start a complex, time-consuming model
    re-architecture or pruning project. The optimal solution is often a surgical,
    hybrid approach.
  napkin_math: 'A full INT8 quantization would roughly halve the latency: `40ms /
    2 = 20ms`. This meets the 33ms budget but risks visual artifacts. Let''s analyze
    a mixed-precision approach. Assume latency is proportional to layer count (a simplification).
    The 49 INT8 layers contribute `(49/50) * 20ms = 19.6ms`. The single FP16 layer
    contributes `(1/50) * 40ms = 0.8ms`. The total estimated latency is `19.6ms +
    0.8ms = 20.4ms`. This still comfortably meets the 30 FPS target while safeguarding
    the quality of the most sensitive layer.'
  options:
  - Quantize the entire model to INT8 to achieve the maximum possible speedup.
  - Keep the sensitive layer in FP16 but quantize the other 49 layers to INT8.
  - The NPU is the bottleneck. Offload the model to run on the mobile GPU instead.
  - The model is too large. Prune 25% of the channels from all layers and retrain
    the model.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
