schema_version: '1.0'
id: mobile-0416
track: mobile
level: L3
zone: diagnosis
topic: kernel-fusion
competency_area: optimization
bloom_level: apply
phase: inference
title: The Mobile GPU's Memory-Go-Round
scenario: You are optimizing a custom keyword-spotting model on an iPhone with an Apple A17 Pro chip. The profiler shows that a sequence of three operations (`Conv2D`, `BatchNorm`, `ReLU`) is taking up a significant portion of your 1.5ms latency budget, even though the model is small. The intermediate activation tensor between each operation is 1MB.
question: "Why is an unfused Conv2D-BatchNorm-ReLU sequence with 1 MB intermediates slow, and what optimization fixes it?"
details:
  realistic_solution: 'The bottleneck is memory bandwidth. Each separate operator writes its 1MB result to LPDDR5 memory, and the next operator reads it back. The solution is Operator Fusion, keeping intermediates in fast on-chip memory.'
  common_mistake: |
    **The Pitfall:** Focusing purely on computational complexity (FLOPs).
    **The Rationale:** Engineers assume small models must be fast, neglecting the massive overhead of memory round-trips between sequential ops.
    **The Consequence:** Suggesting drastic structural changes (like pruning) when a simple compiler optimization (fusion) solves the issue.
  napkin_math: |
    - **Memory Speed:** A17 Pro LPDDR5 bandwidth is ~51.2 GB/s = 51.2 MB/ms.
    - **Transfer Cost:** Reading or writing a 1MB tensor takes 1MB / 51.2 MB/ms = ~0.0195 ms.
    - **Unfused Overhead:** The sequence requires ~5 memory transfers (Conv write, BN read/write, ReLU read/write).
    - **Final Result:** 5 * 0.0195 ms = **~0.1 ms in pure memory overhead**, burning a massive chunk of the 1.5ms budget.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
chains:
- id: mobile-chain-099
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
