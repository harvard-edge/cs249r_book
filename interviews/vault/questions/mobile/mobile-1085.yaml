schema_version: '1.0'
id: mobile-1085
track: mobile
level: L3
zone: analyze
topic: data-quality-validation
competency_area: data
bloom_level: analyze
phase: training
title: On-Device Data Quality Bandwidth Contention
scenario: You are deploying a data quality gate to validate incoming streaming data before feeding it to an on-device LLM. You observe that enabling this data validation pipeline causes the LLM's token generation rate to drop by 11%, even though the TPU is only running at 10% of its capacity.
question: Why does enabling a Tensor G3 anomaly-detection gate cut Gemini Nano token generation despite only 10% TPU use?
details:
  realistic_solution: 'The performance drop is caused by memory bandwidth contention, not compute limits. LLM token generation (like Gemini Nano) is heavily memory-bound, requiring continuous loading of weights and KV cache from the 12 GB LPDDR5X pool. The continuous schema validation and anomaly detection process constantly reads streaming data and writes validation states to memory. This consumes shared memory bandwidth, reducing available bandwidth from 30 GB/s to ~20 GB/s for the LLM, causing a token generation drop from 15 to ~13.3 tokens/sec (11% reduction) despite ample remaining TPU TOPS.'
  common_mistake: |
    **The Pitfall:** Assuming the bottleneck is compute-related and that the TPU is saturated or under-clocked.
    **The Rationale:** Failing to recognize that mobile SoCs use a unified memory architecture.
    **The Consequence:** Attempting to optimize the model's compute graph when the actual solution requires reducing the memory bandwidth footprint of the data pipeline.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Total LPDDR5X Memory Bandwidth: ~60 GB/s.
       - LLM Model: 3B parameters (4-bit), needs ~1.5 GB/s reads per token.
       - Target LLM Throughput: 15 tokens/sec = 22.5 GB/s required.
    2. **Calculations:**
       - Assume OS/Display consumes 30 GB/s.
       - Assume Data Validation Pipeline consumes 10 GB/s.
       - Remaining Bandwidth for LLM = 60 GB/s - 30 GB/s - 10 GB/s = 20 GB/s.
       - Constrained Token Rate = 20 GB/s / 1.5 GB/token = 13.3 tokens/sec.
       - Throughput Drop = (15 - 13.3) / 15 = ~11% reduction.
    3. **Conclusion & Interpretation:**
       **Result: Memory-Bandwidth Bound Contention. The LLM generation rate drops mathematically perfectly with the bandwidth stolen by the data validation I/O, despite ample remaining TPU TOPS.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
