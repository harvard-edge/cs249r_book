schema_version: '1.0'
id: mobile-0730
track: mobile
level: L5
zone: design
topic: data-pipeline-engineering
competency_area: data
bloom_level: create
phase: training
title: The ISP/NPU Hardware Synchronization
scenario: Your AR headset uses a dedicated Image Signal Processor (ISP) and an NPU.
  You want zero-copy memory between them. You allocate a hardware buffer. The ISP
  writes a frame to it, then the NPU reads it. It works, but occasionally the ML model
  outputs complete garbage. The image visually looks fine if you save it to disk right
  after the ISP finishes. What hardware synchronization mechanism did you forget?
question: What sync primitive prevents an NPU from reading an ISP hardware buffer
  before DMA writes are visible?
details:
  realistic_solution: 'You forgot a hardware sync fence between the ISP''s DMA write
    and the NPU''s DMA read. The ISP signals ''frame complete'' to the CPU before
    its final DMA flush finishes (0.5ms remaining). The CPU immediately signals the
    NPU, which reads partially-written memory — the bottom portion of the image contains
    stale data from the previous frame, corrupting the ML input tensor. Fix: insert
    an Android SyncFence or iOS Metal Event between ISP output and NPU input. The
    fence guarantees all DMA writes are visible in DRAM before the NPU begins reading.
    Cost: <10us latency, zero CPU involvement.'
  common_mistake: '"You didn''t flush the CPU cache." The CPU isn''t touching this
    memory—it''s a direct ISP to NPU transfer. The issue is between the hardware blocks.'
  napkin_math: An ISP might process a frame in 10ms. The final DMA flush to RAM takes
    0.5ms. If the CPU signals the NPU in 0.1ms, the NPU reads memory that is 400 microseconds
    out of date, destroying the spatial consistency of the image tensor.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
chains:
- id: mobile-chain-027
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
