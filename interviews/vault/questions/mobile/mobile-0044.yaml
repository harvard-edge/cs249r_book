schema_version: '1.0'
id: mobile-0044
track: mobile
level: L5
zone: diagnosis
topic: differential-privacy
competency_area: cross-cutting
bloom_level: evaluate
phase: inference
title: The Keyboard Prediction Privacy Leak
scenario: Your on-device keyboard prediction model (LSTM, 20M params) learns from user typing to improve suggestions. A security researcher demonstrates that by observing the model's top-5 prediction probabilities over 1000 queries, they can reconstruct fragments of the user's private text. The model never leaves the device.
question: How can top-5 keyboard predictions over 1000 queries leak private text even when the model stays on-device?
details:
  realistic_solution: 'This is a memorization attack. The LSTM''s 20M params can store ~160K-800K words verbatim. Fix with DP during on-device training (epsilon=8): per-query leakage drops from ~2-3 bits to <0.3 bits.'
  common_mistake: |
    **The Pitfall:** Believing on-device processing guarantees data privacy.
    **The Rationale:** Engineers assume that since the model never leaves the device, user data cannot leak, forgetting that the model's outputs (predictions) encode training data.
    **The Consequence:** The model memorizes and subsequently leaks sensitive user data like passwords and emails to other apps.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model capacity: 20M parameters (FP32).
       - User typing history: 10K words/day * 90 days = 900K words.
       - Attack configuration: 1000 queries, top-5 predictions.
    2. **Calculations:**
       - Total capacity: 20M * 32 bits = 640M bits.
       - Word size constraint: ~40 bits/word.
       - Max theoretical memorization: 640M / 40 = 16M words.
       - Practical memorization (3%): 16M * 0.03 = ~500K words.
       - History exposure: 500K / 900K = ~55% of history memorized.
       - Attack leakage without DP: 1000 queries * 5 * 2.5 bits = ~12,500 bits (~300 words leaked).
       - Attack leakage with DP ($\epsilon=8$): drops to <0.3 bits/query (~7-8 words leaked).
    3. **Conclusion & Interpretation:**
       - **Result: Memorization Vulnerability Exploited**. The model's large capacity enables verbatim memorization, exposing sensitive sequences through API queries.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
chains:
- id: mobile-chain-026
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed