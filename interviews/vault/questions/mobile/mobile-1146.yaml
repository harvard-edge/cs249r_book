schema_version: '1.0'
id: mobile-1146
track: mobile
level: L5
zone: design
topic: responsible-ai
competency_area: cross-cutting
bloom_level: evaluate
phase: inference
title: Architecting On-Device Guardrails for Generative Text
scenario: You are designing an on-device text generation feature with strict content moderation requirements running on a Samsung Exynos 2400 NPU. The system must run a primary 3B parameter generative model and a secondary safety classifier concurrently to prevent toxic output generation. The dual-core NPU relies on shared 12 GB LPDDR5X memory.
question: How should the system concurrently run a 3B generator and safety classifier without breaking latency or safety?
details:
  realistic_solution: A robust architecture pipelines the generation and safety classification. The primary model streams tokens, and the safety model (e.g., a lightweight 100M parameter classifier) evaluates chunks of text asynchronously on the second NPU core. If the shared memory pressure from the ISP/GPU spikes (leaving limited RAM), the system dynamically quantizes the safety model or falls back to rule-based heuristics to maintain the safety SLA.
  common_mistake: |
    **The Pitfall:** Designing sequential pipelines (generate then classify).
    **The Rationale:** Candidates overlook stream-based moderation and the shared nature of mobile RAM, assuming all memory is available for the NPU.
    **The Consequence:** Unacceptable latency and potential OOM errors when system memory pressure spikes.
  napkin_math: |
    *   **Primary Model Memory:** 3B parameters at INT4 = ~1.5 GB.
    *   **Safety Model Memory:** 100M parameters at INT8 = ~100 MB.
    *   **Total ML Memory:** 1.5 GB + 0.1 GB = 1.6 GB.
    *   **Available Memory:** 12 GB total - ~9 GB (OS/GPU/ISP) = ~3 GB.
    *   **Conclusion:** The 1.6 GB ML footprint fits comfortably within the **3 GB** budget.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
