schema_version: '1.0'
id: mobile-1152
track: mobile
level: L4
zone: diagnosis
topic: adversarial-robustness
competency_area: reliability
bloom_level: analyze
phase: inference
title: Diagnosing High Latency in On-Device Gemini Nano Sanitization
scenario: You are deploying Gemini Nano on the Google Tensor G3. To defend against prompt injection, inputs route sequentially through a smaller 'sanitizer' model before hitting the main LLM. Telemetry shows latency spiked to over 1.5 seconds.
question: How would you analyze the interaction between the models and the hardware to identify the root cause of the latency?
details:
  realistic_solution: The root cause is memory bandwidth contention and context switching overhead. While the Tensor G3 has a capable TPU, running a sequential pipeline forces the OS to swap activations and weights entirely in and out of the TPU's local SRAM over the LPDDR5X bus.
  common_mistake: |
    **The Pitfall:** Assuming the TPU compute is simply too slow for two models.
    **The Rationale:** Candidates assume the compute capacity is lacking or that prompt attacks add compute, completely missing the sequential memory swapping overhead.
    **The Consequence:** Unnecessary model compression that fails to address the root I/O cause.
  napkin_math: |
    **Assumptions & Constraints:**
    - Sanitizer: 100M params (100MB INT8).
    - Main LLM: 1.8B params (1.8GB INT8).
    - LPDDR5X Bandwidth: 50 GB/s.

    **Calculations:**
    - Total weights per full turn: 100MB + 1.8GB = 1.9GB.
    - Memory swap time (read/write overhead): (1.9 GB / 50 GB/s) * 2 = 76ms.
    - Actual TPU compute time: ~50ms.

    **Conclusion & Interpretation:**
    - **Result: 76ms I/O Overhead (Memory-Bandwidth-Bound)**. The 76ms pure I/O overhead heavily dominates the ~50ms compute time, proving the bottleneck is sequential memory swapping over the bus.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
