schema_version: '1.0'
id: mobile-0634
track: mobile
level: L5
zone: evaluation
topic: model-serving-infrastructure
competency_area: deployment
bloom_level: evaluate
phase: inference
title: The Real-Time Batching Paradox
scenario: Your team develops a real-time translation feature for a flagship mobile messaging app. It runs an on-device model on the phone's NPU. The product requirement is a 'near-instant' user experience, which engineering defines as a P99 translation latency of under 250ms from the moment the user's audio input is captured.
question: Why does a throughput-oriented batching window violate the real-time translation latency budget?
details:
  realistic_solution: This is a classic case of violating a real-time system's latency budget by applying a throughput-oriented optimization. The core issue is **Head-of-Line Blocking** introduced by the batching queue. The first audio chunk that enters the queue for a new batch must wait for the entire batching window to fill before it can even begin processing. This waiting time is directly added to its end-to-end latency.
  common_mistake: |
    **The Pitfall:** Assuming that any technique that increases hardware utilization (TOPS) is a universal good.
    **The Rationale:** Engineers accustomed to high-throughput cloud batching apply the same logic to real-time streams.
    **The Consequence:** They miss the queuing theory trade-off. In a real-time system, predictable latency is more important than high average throughput, and batching causes fatal head-of-line blocking.
  napkin_math: |
    - **Baseline (Batch 1):** Inference time = **80 ms** (Well under 250ms budget).
    - **With Batch Window (200ms):** First audio chunk waits 200ms for window to fill + 80ms inference = **280 ms**.
    - **Conclusion:** 280 ms > 250 ms P99 budget. Head-of-line blocking ruins real-time latency. For real-time, keep batch size to 1 or set max batch window to < 170 ms.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
