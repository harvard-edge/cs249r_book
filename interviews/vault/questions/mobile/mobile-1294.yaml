schema_version: '1.0'
id: mobile-1294
track: mobile
level: L5
zone: evaluation
topic: profiling-bottleneck-analysis
competency_area: latency
bloom_level: evaluate
phase: inference
title: On-Device LLM Profiling Alternatives for Tensor G3
scenario: You are deploying Gemini Nano on the Google Tensor G3. Initial profiling during the autoregressive generation phase shows an unacceptable latency of 150ms per token.
question: Is Tensor G3 LLM generation at 150 ms/token limited by 7.5 TOPS TPU compute or 12 GB LPDDR5X bandwidth?
details:
  realistic_solution: Hypothesis B is correct. During autoregressive decoding at batch size 1, the entire model must be loaded from memory into the TPU for every single token generated. The TPU's 7.5 TOPS are vastly underutilized because the system is starved waiting for weights. Apply weight-only quantization (e.g., INT4) to fix this.
  common_mistake: |
    **The Pitfall:** Assuming LLMs are always compute-bound on edge hardware.
    **The Rationale:** Candidates fail to distinguish between the compute-heavy prefill phase and the memory-bandwidth-heavy autoregressive decoding phase (batch size 1) typical in mobile deployments.
    **The Consequence:** Choosing the wrong optimization path, such as aggressive network pruning instead of quantization.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - TPU Compute: 7.5 TOPS.
       - Memory: 12 GB LPDDR5X (~50 GB/s bandwidth).
       - Model: 3B parameters FP16 (6 GB memory footprint).
       - Batch Size: 1 (autoregressive decoding).
    2. **Calculations:**
       - Required Memory IO Time: 6 GB / 50 GB/s = 120ms.
       - Required Compute Time: 6 GOPs / 7.5 TOPS = 0.8ms.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bandwidth-Bound**. Memory I/O (120ms) entirely dominates compute (0.8ms). The TPU is starved for weights, directly causing the ~150ms latency.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
