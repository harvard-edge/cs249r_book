schema_version: '1.0'
id: mobile-1289
track: mobile
level: L5
zone: evaluation
topic: memory-pressure-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: LLM KV Cache Allocation Strategy on Tensor G3
scenario: 'You are deploying a 1.8B parameter on-device LLM (similar to Gemini Nano)
  on the Google Tensor G3, which features an on-device TPU and 12 GB of LPDDR5X shared
  memory. The system restricts the LLM process to a strict 2.5 GB memory budget to
  prevent OS-level eviction of essential background apps. You must choose between
  two KV cache management strategies for a multi-turn chat feature: Alternative A
  uses static pre-allocation of a maximum-length KV buffer at initialization, while
  Alternative B uses dynamic block-based allocation (like PagedAttention) to minimize
  memory usage for short prompts. Which design is better for managing memory pressure
  on this device?'
question: Should a 1.8B LLM on Tensor G3 use static or PagedAttention-style KV allocation
  under a 2.5 GB budget?
details:
  realistic_solution: Alternative A (Static pre-allocation) is the superior choice
    for mobile deployment. Because mobile inference typically operates at a batch
    size of 1, the theoretical memory savings from dynamic allocation do not outweigh
    the risks of memory fragmentation. Pre-allocating the maximum required KV cache
    ensures a fixed, predictable memory footprint, completely avoiding runtime fragmentation
    and guaranteeing that the app will not suddenly trigger the Android low-memory
    killer (LMK) during prolonged usage.
  common_mistake: Candidates often blindly recommend dynamic block allocation (Alternative
    B) because it is the state-of-the-art for server-side LLM serving with high concurrency.
    They fail to realize that on a mobile SoC with a shared memory space, dynamic
    allocation introduces severe memory fragmentation over time, which can trigger
    unpredictable OS-level OOM kills even if total used memory is under the limit.
  napkin_math: 'Tensor G3 Total Memory: 12 GB LPDDR5X. App Budget: 2.5 GB. Model Weights:
    1.8B params @ INT8 = 1.8 GB. Remaining memory for KV cache/activations: 2.5 GB
    - 1.8 GB = 700 MB. KV cache size per token (assuming 16 layers, 2048 hidden dim,
    FP16): 2 (K&V) * 2 bytes * 16 * 2048 = 131,072 bytes (128 KiB). Max static KV cache
    for a 2048-token context window: 128 KiB * 2048 = 262,144 KiB = 256 MiB (~268 MB).
    Since 256 MiB comfortably fits within the 700 MB remaining budget, static pre-allocation
    is safe and leaves ~432 MB for intermediate activations without risking fragmentation.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
