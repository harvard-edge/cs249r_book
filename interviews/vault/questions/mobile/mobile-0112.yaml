schema_version: '1.0'
id: mobile-0112
track: mobile
level: L4
zone: optimization
topic: power-budgeting
competency_area: power
bloom_level: analyze
phase: inference
title: The Background ML Battery Drain
scenario: Your photo app has a background ML feature that auto-tags photos using an on-device classification model (MobileNetV3, 5 MB, INT8). Users report 15% battery drain overnight. The model inference takes 3ms per photo. With 200 photos, that's 600ms of total inference compute.
question: Why does a tiny MobileNetV3 photo tagger with only 600 ms of total inference compute drain 15% of the battery overnight?
details:
  realistic_solution: |
    The massive battery drain has virtually nothing to do with the ML inference and everything to do with OS-level Wake Lock patterns. By processing the photos incrementally, the app triggers the device's CPU to wake up from deep sleep repeatedly throughout the night. The correct architectural fix is to utilize OS batch APIs (e.g., `BGProcessingTask` in iOS) that coalesce the work into a single batched operation. Furthermore, using constraints like `requiresExternalPower = true` guarantees the heavy lifting only happens when the device is actively charging on nightstands.
  common_mistake: |
    **The Pitfall:** Assuming the neural network compute is intrinsically the primary cause of the battery drain.
    **The Rationale:** ML engineers tunnel-vision on the network topology, assuming that optimizing the model size or parameter count will structurally lower battery use.
    **The Consequence:** Spending weeks pruning and quantizing a model that represents less than 0.02% of the actual energy consumed, entirely ignoring the catastrophic system wake patterns.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Workload: 200 photos, 3 ms inference/photo.
       - Component Power: NPU (~2W), CPU Wake Overhead (~2W), ISP/Storage Decode (~3W).
       - Background Pattern: Device un-batches the work, waking 10 separate times overnight.
    2. **Calculations:**
       - ML Inference Energy: 200 photos * 0.003s * 2W = 1.2 Joules.
       - Image Decode Energy: 200 photos * 0.050s * 3W = 30 Joules.
       - System Wake Energy (Radio, OS Sync, Networking): ~10,000 Joules total overhead.
       - Model Attribution: 1.2 Joules / ~10,000 Joules = 0.012%.
    3. **Conclusion & Interpretation:**
       **Result: ML is 0.012% of the power cost (System-Overhead-Bound).** Uncoalesced background wakes obliterate the power budget; optimizing the model yields literally zero measurable battery savings.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: mobile-chain-042
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
