schema_version: '1.0'
id: mobile-1017
track: mobile
level: L3
zone: realization
topic: network-bandwidth-bottlenecks
competency_area: networking
bloom_level: apply
phase: inference
title: Realize Peak Bandwidth Utilization During On-Device LLM Response
scenario: Concretely compute peak and average LPDDR5X bandwidth utilization during a 50-token on-device response from a 3B INT4 LLM on Snapdragon 8 Gen 3. The response takes 975ms.
question: What peak and average LPDDR5X bandwidth does a 50-token 3B INT4 LLM response require on Snapdragon 8 Gen 3?
details:
  realistic_solution: 'Peak BW demands include weight reads and KV cache operations. Peak BW: 1.5GB/19.5ms = 76.9GB/s. Including system processes (3GB/s) pushes total demand to 80.5GB/s, exceeding hardware limits. Effective TPOT slows to 22ms at mid-response due to stalls.'
  common_mistake: |
    **The Pitfall:** Computing only static weight bandwidth.
    **The Rationale:** Candidates frequently fail to include dynamic KV-cache reads (which scale linearly with context) and writes (new KV entries).
    **The Consequence:** Underestimating the total memory bandwidth requirements, leading to unexpected NPU stalls deep into generation.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 3B INT4 (1.5GB weights).
       - Hardware Target TPOT: 19.5ms.
       - KV entry size: 524KB per token.
       - Background System BW: 3 GB/s.
    2. **Calculations:**
       - Weight BW per token: 1.5 GB / 19.5ms = 76.9 GB/s.
       - KV BW at mid-response (token 25): (25 * 524KB) / 19.5ms = 0.67 GB/s.
       - Peak BW demand: 76.9 + 0.67 + 3.0 = 80.57 GB/s.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bandwidth-Bound**. 80.57 GB/s clearly exceeds the hardware's 77 GB/s physical limit. The system is entirely saturated, forcing the NPU to stall and pushing effective TPOT to ~22ms.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
