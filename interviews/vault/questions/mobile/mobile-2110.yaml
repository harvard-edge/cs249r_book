schema_version: '1.0'
id: mobile-2110
track: mobile
level: L5
zone: recall
topic: model-serving-infrastructure
competency_area: architecture
bloom_level: understand
phase: both
title: Mobile NPU Phase Optimization
scenario: A mobile SoC serves an LLM. The context prefill uses large dense GEMM operations, while token decoding uses vector-matrix multiplications.
question: Why does dynamically switching from a compute-optimized kernel for prefill to a bandwidth-optimized kernel for decoding improve token throughput?
details:
  realistic_solution: Prefill is compute-bound, maximizing arithmetic intensity. Decoding is memory-bandwidth bound, requiring entire model weight loading per token. Switching kernels isolates these two distinct limits.
  common_mistake: |
    **The Pitfall:** Confusing the arithmetic intensity of prefill and decoding.
    **The Rationale:** Candidates often assume all LLM phases are bound by the same hardware constraint.
    **The Consequence:** This leads to the assumption that decoding throughput is limited by NPU clock speed rather than memory bandwidth, resulting in flawed optimization efforts.
  napkin_math: |
    1. Analyze Prefill Arithmetic Intensity: Matrix-matrix multiplication has high intensity -> $O(\text{Batch} \times \text{SeqLen})$.
    2. Analyze Decode Arithmetic Intensity: Matrix-vector multiplication requires loading all weights for one token -> $O(1)$.
    3. Conclusion: Prefill is compute-bound, Decode is memory-bandwidth bound. Switching kernels isolates these two distinct hardware limiters.
status: draft
provenance: llm-draft
expected_time_minutes: 6
requires_explanation: true
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
tags:
- gemini-generated
- target-recall-L5
created_at: '2026-04-25'