schema_version: '1.0'
id: mobile-0655
track: mobile
level: L6+
zone: design
topic: latency-decomposition
competency_area: latency
bloom_level: create
phase: inference
title: The Guardian Copilot's Dilemma
scenario: 'You are the Lead ML Systems Architect for a new flagship EV''s ''Guardian'' copilot. The system uses a 4B-parameter multi-modal model. It monitors driver state (video) and road context (navigation) to provide proactive assistance.'
question: 'What preemptible scheduling architecture prevents low-priority Guardian copilot work from blocking safety-critical tasks?'
details:
  realistic_solution: 'The problem is not one of raw performance, but of scheduling. A long-running, low-priority task is occupying the GPU, causing any subsequent high-priority task to miss its deadline. The architecture must be able to interrupt the low-priority work.'
  common_mistake: |
    **The Pitfall:** Proposing a smaller model or dedicated safety MCU instead of addressing the scheduling architecture.
    **The Rationale:** Engineers sidestep the core challenge of safely multiplexing tasks on a powerful SoC by throwing more hardware at the problem or sacrificing model quality.
    **The Consequence:** The system remains vulnerable to blocking tasks; a slow task still blocks, it's just slightly less slow.
  napkin_math: |
    **Assumptions & Constraints:**
    - Safety Task Deadline: 33ms limit (arrives at ms 1).
    - Background Task: 100ms processing on NPU.
    - Preemption Overhead: ~2ms for context switch.

    **Calculations:**
    - Non-preemptible Queue: Safety task waits 99ms -> 99ms > 33ms (Deadline missed).
    - Preemptible System: Interrupt at 1ms + 2ms (overhead) + 20ms (safety task execution) = 23ms total.

    **Conclusion & Interpretation:**
    - **Result: Preemption-Bound Scheduling**. Hardware-level preemption allows the safety-critical task to finish in **23ms**, easily meeting the 33ms strict deadline by interrupting low-priority workloads.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
