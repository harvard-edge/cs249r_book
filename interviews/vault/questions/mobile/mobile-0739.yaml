schema_version: '1.0'
id: mobile-0739
track: mobile
level: L1
zone: recall
topic: graph-compilation
competency_area: optimization
bloom_level: remember
phase: inference
title: CoreML vs PyTorch
scenario: You are tasked with deploying a trained PyTorch model (.pth file) into a native iOS Swift application.
question: Can you deploy a raw `.pt` or `.pth` PyTorch model directly into a native iOS Swift application?
details:
  realistic_solution: No. The raw PyTorch model must be converted into Apple's proprietary CoreML format (`.mlpackage` or `.mlmodel`) so that the iOS operating system can automatically route the operations to the Neural Engine, GPU, or CPU.
  common_mistake: |
    **The Pitfall:** Thinking iOS natively supports PyTorch weight files.
    
    **The Rationale:** Developers coming from cloud environments assume they can simply `pip install torch` inside the iOS app.
    
    **The Consequence:** They fail to realize the model must be converted to an Apple-proprietary format to utilize hardware acceleration, stalling deployment.
  napkin_math: |
    1. **Analyze Raw PyTorch Weight Size:** MobileNetV2 (3.4M params) in FP32 = 3.4M × 4 bytes = 13.6 MB.
    2. **Analyze CoreML Conversion:** CoreML automatically downcasts to FP16 during export = 3.4M × 2 bytes = 6.8 MB.
    3. **Result:** The CoreML package is **2x smaller (6.8 MB)** and fundamentally restructured to route operations directly to the A17 Pro's 35 TOPS Neural Engine.
  options:
  - Yes, iOS natively executes raw PyTorch files using the Swift Torch API.
  - Yes, but only if you use an iPhone 14 or newer.
  - No, the model must be converted to the CoreML format for hardware acceleration on iOS.
  - No, iOS only supports models trained in TensorFlow.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 1
chains:
- id: mobile-chain-000
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
