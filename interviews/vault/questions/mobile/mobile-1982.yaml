schema_version: '1.0'
id: mobile-1982
track: mobile
level: L6+
zone: realization
topic: queueing-theory
competency_area: latency
bloom_level: create
phase: both
title: A17 Pro AR Burst Processing
scenario: A mobile NPU processes AR frames. Mean service time is 20 ms (range 18-28 ms, CV ~0.15). A naive analysis computes burst drain as 15 frames * 20 ms = 300 ms, treating the NPU as drain-only. The realistic system has three complications - (a) the L1 caches are evicted by the pan, costing an extra +5 ms on the first 3 frames after the burst; (b) service-time variance pushes worst-case per-frame to 28 ms; (c) the camera continues to deliver new frames at 30 fps during drainage, refilling the queue.
question: Starting from the figure's 300 ms naive estimate, specify the realistic worst-case time for the queue to return to its pre-burst depth, including the cold-cache tax, worst-case variance, and the open-loop arrival stream during drainage.
visual:
  kind: svg
  path: mobile-1982.svg
  alt: Queue length graph starting at 15 and linearly draining to 0 over 300ms.
  caption: AR Burst queue drain over time.
details:
  realistic_solution: |
    During drainage, the camera keeps delivering at 30 fps (1 frame every 33.3 ms) while service per frame is 28 ms worst-case, giving rho = 28/33.3 = 0.84 - so 0.84 new frames arrive for every 1 frame drained. The queue still transitions because rho < 1 (the system is stable), but only at a net rate of (1 - 0.84) = 0.16 frames cleared per service slot. Effective drain time is massively longer than the naive estimate, blowing through SLAs.
  common_mistake: |
    **The Pitfall:** Multiplying queue depth by average service time to find drain time.
    **The Rationale:** Candidates ignore the cold-cache penalty, worst-case service variance, and the continued 30 fps open-loop arrival stream during drainage.
    **The Consequence:** Calculating a 300ms recovery time when the true recovery takes seconds, leading to catastrophic UI stutter in AR applications.
  napkin_math: |
    **Assumptions & Constraints:**
    - Queue depth: 15 frames.
    - Cold cache penalty: +5 ms on first 3 frames.
    - Worst-case service time: 28 ms.
    - Arrival rate: 30 FPS (33.3 ms/frame).

    **Calculations:**
    - Cold drain (isolation): 3 * (28 + 5) + 12 * 28 = 99 + 336 = 435 ms.
    - Rho during drainage: 28 ms / 33.3 ms = 0.84.
    - Net clear rate: 1 - 0.84 = 0.16 frames per service slot.
    - Effective drain time: 435 ms / 0.16 = 2718.75 ms.

    **Conclusion & Interpretation:**
    - **Result: ~2720 ms (Queueing Delay)**. Effective drain time is ~9x the naive 300 ms estimate.
status: published
provenance: llm-draft
expected_time_minutes: 5
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-realization-L6+
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
