schema_version: '1.0'
id: mobile-0780
track: mobile
level: L3
zone: fluency
topic: latency-decomposition
competency_area: latency
bloom_level: apply
phase: inference
title: TFLite Delegate Subgraph Partitioning Overhead
scenario: You are deploying a custom object detection model using TFLite on an Android phone. The model runs entirely on the CPU in 20ms. You enable the GPU delegate, which accelerates 90% of the model's operations (by original CPU time) by a factor of 6x. However, 4 interspersed custom operations are not supported by the GPU delegate, causing TFLite to partition the graph into 9 subgraphs (5 GPU, 4 CPU).
question: If each CPU-GPU context switch (tensor copy and synchronization) takes 2ms, what is the expected new end-to-end latency?
details:
  realistic_solution: |
    The new end-to-end latency is 21ms, which is actually slower than the CPU-only baseline. To fix this, you must either replace the unsupported custom ops with standard GPU-supported ops, or move them to the very end of the model to reduce the number of subgraphs to exactly 2.
  common_mistake: |
    **The Pitfall:** Failing to account for memory synchronization overhead.
    **The Rationale:** Candidates correctly calculate the raw compute time reduction but forget context switching at each subgraph boundary.
    **The Consequence:** The calculated latency is significantly lower than the actual observed latency, leading to incorrect deployment decisions.
  napkin_math: |
    - **Original CPU compute:** 20ms
    - **GPU compute portion:** 18ms / 6x speedup = 3ms
    - **CPU fallback compute portion:** 2ms
    - **Transitions between 9 subgraphs:** 8 context switches
    - **Context switch overhead:** 8 * 2ms = 16ms
    - **Total Latency:** 3ms (GPU) + 2ms (CPU) + 16ms (Overhead)
    - **Result:** **21ms**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: mobile-chain-030
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T01:47:56.958694'
