schema_version: '1.0'
id: mobile-1252
track: mobile
level: L5
zone: design
topic: roofline-analysis
competency_area: compute
bloom_level: evaluate
phase: inference
title: Architecting an AR Segmentation Pipeline on Snapdragon 8 Gen 3
scenario: You are designing a real-time on-device 4K super-resolution pipeline targeting an NPU with a shared memory pool.
question: How should a 4K super-resolution pipeline use roofline analysis to guarantee it stays compute-bound?
details:
  realistic_solution: Architect a tiled dataflow that strictly confines intermediate activations to the NPU's SRAM, fusing layers to maximize operational intensity above the ridge point.
  common_mistake: |
    **The Pitfall:** Assuming peak TOPS are always achievable regardless of memory design.
    **The Rationale:** Candidates design un-fused pipelines with massive intermediate activations, failing to account for the shared nature of the memory bus.
    **The Consequence:** The workload hits the memory wall, evicting activations to DRAM and permanently stalling the pipeline well below its 30 FPS target.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - NPU peak compute: 45 TOPS.
       - Available bandwidth: ~15 GB/s (accounting for contention).
       - Compute requirement: 1 TOPS/frame at 30 FPS (30 TOPS total).
    2. **Calculations:**
       - Hardware Ridge Point: 45e12 Ops / 15e9 Bytes = 3000 Ops/Byte.
       - Max memory traffic to stay compute-bound: 30e12 Ops / 3000 Ops/Byte = 10 GB/s.
       - Max memory per frame: 10 GB/s / 30 FPS = ~333 MB/frame.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bound Without Fusion**. The pipeline must aggressively fuse layers to keep activations under 333 MB/frame and stay compute-bound.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed