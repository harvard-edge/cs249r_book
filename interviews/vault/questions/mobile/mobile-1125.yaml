schema_version: '1.0'
id: mobile-1125
track: mobile
level: L5
zone: design
topic: data-quality-validation
competency_area: data
bloom_level: evaluate
phase: training
title: 'Real-time Video Quality Gating on Exynos 2400'
scenario: A real-time segmentation pipeline processes 1080p camera feeds on a Samsung Exynos 2400 NPU (34.7 TOPS, 12 GB shared LPDDR5X). A data quality gate is needed to reject blurred or anomalous frames before executing the heavy segmentation model.
question: Where should the quality validation run, and how does early frame rejection balance power against accuracy?
details:
  realistic_solution: 'The optimal design leverages zero-copy memory from the ISP and uses a lightweight, heuristic-based image quality assessment (IQA) algorithm on the CPU, or a highly quantized micro-model on the NPU. By evaluating the tradeoffs, the engineer should recognize that spending a fraction of compute on early rejection saves massive power downstream. If a frame fails the quality gate, the system simply drops it or reuses the previous frame''s mask, ensuring the 34.7 TOPS NPU budget is reserved strictly for high-confidence data.'
  common_mistake: |
    **The Pitfall:** Running a complex validation model on the NPU alongside the main workload.
    **The Rationale:** Overlooking the power overhead of heavy NPU context switching and ignoring zero-copy memory access from the ISP.
    **The Consequence:** Burning more power evaluating frame quality than is saved by rejecting bad frames.
  napkin_math: |
    **Assumptions & Constraints:**
    - Main segmentation model: 0.5 TOPS/frame.
    - Lightweight validation model: 0.01 TOPS/frame.
    - Target: 30 FPS.
    - Rejection rate: 20% of frames.

    **Calculations:**
    - Main model cost: 0.5 TOPS * 30 FPS = 15 TOPS.
    - Validation model cost: 0.01 TOPS * 30 FPS = 0.3 TOPS.
    - Computation saved (20% rejected): 0.5 TOPS * 6 frames = 3.0 TOPS.
    - Net saving: 3.0 TOPS saved - 0.3 TOPS cost = 2.7 TOPS.

    **Conclusion & Interpretation:**
    - **Result: 2.7 TOPS (Compute-Bound)**. Significant thermal and battery savings by skipping heavy inference on garbage data.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
