schema_version: '1.0'
id: mobile-balance-0003
track: mobile
level: L5
zone: realization
topic: communication-computation-overlap
competency_area: parallelism
bloom_level: evaluate
phase: both
title: Mobile NPU CPU Overlap
scenario: A mobile pipeline preprocesses on CPU for 12 ms and runs NPU inference for 28 ms per frame. With double buffering, CPU preprocessing for frame N+1 overlaps NPU inference for frame N. Estimate steady-state frame time.
question: What steady-state frame time is possible when 12 ms CPU preprocessing overlaps 28 ms NPU inference?
details:
  realistic_solution: The steady-state throughput interval is the slower stage, 28 ms/frame, once the pipeline is full. The first frame can still pay CPU plus NPU fill latency. Without overlap, steady-state work would be 40 ms/frame. Double buffering can meet a 33 ms sustained frame budget if scheduling jitter is controlled.
  common_mistake: Adding CPU and NPU times even when they can run in parallel on separate engines.
  napkin_math: Overlapped steady state = max(12,28)=28 ms. Sequential = 12+28=40 ms.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 12
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- iteration-gap-pack
