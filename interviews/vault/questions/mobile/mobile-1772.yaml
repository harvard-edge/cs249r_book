schema_version: '1.0'
id: mobile-1772
track: mobile
level: L3
zone: analyze
topic: queueing-theory
competency_area: latency
bloom_level: analyze
phase: inference
title: On-Device Inference Queue Latency on A17 Pro
scenario: An iOS app uses the A17 Pro Neural Engine (35 TOPS, 8GB) for real-time photo enhancement. Users submit requests at 5/s during burst editing sessions. Each enhancement takes 150ms.
question: What is the mean end-to-end latency and p99, and should the UX team expect snappy responses?
details:
  realistic_solution: 'λ=5/s, μ=6.67/s, ρ=0.75. M/M/1 mean wait Wq = ρ/(μ(1-ρ)) = 0.75/(6.67×0.25) = 450ms. Mean total W = 150+450=600ms. p99 ≈ 4.6/μ(1-ρ) = 4.6/1.67 = 2.75s! Recommendation: queue UX feedback immediately (show preview), cap λ to 3/s with debouncing (ρ=0.45 → Wq=125ms, W=275ms total).'
  common_mistake: |
    **The Pitfall:** Quoting only service time (150ms) as the user-perceived latency.
    **The Rationale:** Ignoring queuing delay entirely when utilization is high (ρ=0.75).
    **The Consequence:** Promising a "snappy" 150ms experience when users will actually encounter p99 latencies of almost 3 seconds during bursts.
  napkin_math: |
    - Arrival rate (λ) = 5/s
    - Service time = 150ms -> Service rate (μ) = 1 / 0.15s = 6.67/s
    - Utilization (ρ) = λ / μ = 5 / 6.67 = 0.75
    - Mean queue wait (Wq) = ρ / (μ(1-ρ)) = 0.75 / (6.67 * 0.25) = 450ms
    - Mean total wait (W) = Wq + 150ms = 600ms
    - **Result: p99 wait ≈ 4.6 / (μ(1-ρ)) ≈ 2.75 seconds.**
    - (If limited to λ=3/s: ρ=0.45, Wq=123ms, p99 ≈ 820ms)
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 5
deletion_reason: 'Physics violation: queueing-theory inapplicable on mobile'
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
