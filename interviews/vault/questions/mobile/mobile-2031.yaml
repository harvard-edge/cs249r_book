schema_version: '1.0'
id: mobile-2031
track: mobile
level: L5
zone: evaluation
topic: quantization-fundamentals
competency_area: precision
bloom_level: evaluate
phase: both
title: Evaluating Memory Bandwidth in Mobile INT8 Quantization
scenario: A team wants to deploy a 2B parameter language model on a Snapdragon 8 Gen 3. They are choosing between FP16 and INT8 weights. The GPU concurrently runs display composition that consumes ~6 GB/s of LPDDR5 bandwidth, contending with the LLM's weight-streaming reads. Total LPDDR5 bandwidth on this SoC is ~50 GB/s.
question: Evaluate the per-token decode latency under FP16 vs INT8 weights, accounting for the 6 GB/s GPU contention on the shared LPDDR5 bus. Identify whether INT8 fully recovers the 2x throughput it provides on a contention-free bus.
details:
  realistic_solution: 'Footprints: FP16 = 4 GB, INT8 = 2 GB. Effective LPDDR5 BW after 6 GB/s GPU contention = 44 GB/s. FP16 per-token: 4 GB / 44 GB/s = 91 ms/token -> 11 tok/s. INT8 per-token: 2 / 44 = 45 ms/token -> 22 tok/s. INT8 still doubles throughput because the bandwidth bound is linear in bytes-per-token. However, if GPU contention rises to 25 GB/s (heavy gaming), effective LLM BW drops to 25 GB/s; FP16 = 6.25 tok/s, INT8 = 12.5 tok/s - both halved, INT8 still 2x but now both barely usable.'
  common_mistake: |
    **The Pitfall:** Reporting only the contention-free 2x speedup.
    **The Rationale:** Candidates miss that the absolute throughput depends on the available bandwidth after contention with the display path.
    **The Consequence:** Inaccurate latency estimates that fail to reflect real-world performance under heavy contention.
  napkin_math: |
    - Effective bandwidth = 50 GB/s - 6 GB/s = 44 GB/s.
    - FP16 model size = 2B * 2 bytes = 4 GB. FP16 throughput = 44 GB/s / 4 GB/token = **11 tok/s**.
    - INT8 model size = 2B * 1 byte = 2 GB. INT8 throughput = 44 GB/s / 2 GB/token = **22 tok/s**.
    - Under heavy contention (25 GB/s available to LLM): FP16 = 25 / 4 = **6.25 tok/s**, INT8 = 25 / 2 = **12.5 tok/s**.
status: published
provenance: llm-draft
expected_time_minutes: 8
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-fluency-L5
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
