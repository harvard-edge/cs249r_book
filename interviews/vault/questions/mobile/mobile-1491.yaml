schema_version: '1.0'
id: mobile-1491
track: mobile
level: L4
zone: specification
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: inference
title: Shadow Deployment Design for Exynos 2400
scenario: You are tasked with designing a shadow deployment strategy for a new image enhancement model targeting the Samsung Exynos 2400 NPU. The new model requires 1.5 GB of RAM and executes in 15ms per frame, while the existing CPU/GPU pipeline is currently using 8 GB of the shared 12 GB LPDDR5X memory.
question: "How would you specify an Exynos 2400 shadow deployment that protects latency and memory headroom without causing memory starvation or thermal throttling?"
details:
  realistic_solution: Specify a telemetry-gated shadow rollout where the NPU model executes asynchronously on a sampled subset of frames rather than the full 30 fps video stream. Set hard memory thresholds to abort the shadow inference if total system memory usage exceeds 10.5 GB to protect the primary pipeline. Utilize the dual-core nature of the Exynos 2400 NPU by pinning the shadow model to a single core, ensuring predictable thermal output while monitoring LPDDR5X bandwidth contention.
  common_mistake: |
    **The Pitfall:** Designing the shadow deployment to run synchronously on every frame and assuming the NPU has isolated memory.
    **The Rationale:** Candidates treat the NPU as a separate system and ignore the unified LPDDR5X architecture where NPU memory allocations compete with the primary CPU/GPU pipeline.
    **The Consequence:** The 1.5 GB shadow model allocation starves the primary pipeline of bandwidth, leading to dropped frames in the user-facing application.
  napkin_math: |
    * Total unified memory: 12 GB.
    * Existing pipeline usage: 8 GB.
    * OS/System overhead: ~1.5 GB.
    * Available memory: 12 - 8 - 1.5 = 2.5 GB.
    * Shadow model requirement: 1.5 GB.
    * Buffer remaining: 1.0 GB.
    * Compute requirement: 15 ms/frame at 30 FPS = 450 ms of compute per second (45% utilization per core).
    * **The 1.5 GB fits mathematically, but running synchronously at 30 FPS consumes ~45% of a core and substantial memory bandwidth; an asynchronous, sampled shadow deployment is required.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
