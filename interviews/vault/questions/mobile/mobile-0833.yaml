schema_version: '1.0'
id: mobile-0833
track: mobile
level: L6+
zone: specification
topic: memory-hierarchy-design
competency_area: memory
bloom_level: create
phase: both
title: 'NPU Memory Tiering Specification'
scenario: 'An LLM inference engine runs on a mobile NPU. The 7B parameter INT4 model exceeds the fast NPU SRAM but fits entirely in the device''s main memory.'
question: 'Create a layer execution and weight-tiering specification to maximize the auto-regressive token generation rate, detailing how to utilize the fast SRAM.'
details:
  realistic_solution: 'Since token generation (batch size 1) is entirely memory-bandwidth bound, reading the full 3.5GB model from memory takes about 45ms per token. The candidate should specify pinning the most frequently accessed data (the KV cache, managed via PagedAttention, to prevent repetitive R/W operations) and the most critical embedding/LM-head weights into the high-bandwidth NPU SRAM. The remaining layers must be streamed sequentially from main memory.'
  common_mistake: |
    **The Pitfall:** Trying to partition layers such that compute and memory overlap perfectly.
    **The Rationale:** Candidates ignore that auto-regressive decoding is overwhelmingly memory-bandwidth bound rather than compute-bound.
    **The Consequence:** Complex scheduling logic is created that provides zero performance benefit over simple sequential streaming.
  napkin_math: |
    **Assumptions & Constraints:**
    - 7B INT4 model size: ~3.5 GB.
    - Memory bandwidth: ~77 GB/s.

    **Calculations:**
    - Reading the full model from memory takes 3.5 GB / 77 GB/s = ~45ms per token.

    **Conclusion & Interpretation:**
    - **Result: 22 tokens/sec (Memory-Bound)**. This establishes a hard baseline maximum generation rate of 1 / 0.045s ≈ 22 tokens/second.
status: published
provenance: llm-draft
expected_time_minutes: 12
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
