schema_version: '1.0'
id: mobile-1256
track: mobile
level: L4
zone: diagnosis
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: inference
title: Diagnosing OOM in On-Device LLM Shadow Rollout
scenario: You are running a shadow deployment of an upgraded on-device LLM on Google Tensor G3 devices. The new model silently processes user inputs in the background to log output differences against the active control model. However, telemetry from the shadow cohort reveals a massive spike in system-level OOM app terminations and severe UI stuttering.
question: Why does a Tensor G3 shadow rollout of an LLM cause OOM and UI stutter, and what rollout architecture avoids it?
details:
  realistic_solution: The root cause is RAM capacity exhaustion. Holding both the control and shadow models in memory concurrently starves the system, triggering the Android Low Memory Killer Daemon (LMKD) when the user opens other apps. The correct architectural fix is to abandon concurrent shadow rollouts for large models and instead use a true A/B canary rollout where only one model is loaded per device, or to deploy the new model as a LoRA adapter so both versions share the same base weights in memory.
  common_mistake: |
    **The Pitfall:** Misdiagnosing OOM spikes as a compute bottleneck on the TPU.
    
    **The Rationale:** Candidates assume 7.5 TOPS isn't enough for concurrent inference, overlooking the static memory capacity footprint of hosting two large LLMs.
    
    **The Consequence:** They try to tweak CPU thread priorities or quantize models slightly, which fails to resolve the physical 12GB memory limit exhaustion.
  napkin_math: |
    1. **Calculate Model Footprint:** An INT8 quantized 3.2B parameter LLM requires ~3.2 GB of RAM.
    2. **Calculate Shadow Overhead:** Loading both control and treatment models simultaneously consumes 6.4 GB.
    3. **Calculate System Headroom:** The OS reserves ~4 GB. Total used = 10.4 GB. This leaves only 1.6 GB of the 12 GB available.
    4. **Result:** Launching a memory-intensive app like the Camera (~2 GB) immediately **exceeds the 12 GB limit**, causing LMKD to forcefully kill processes. A/B canary rollouts must be used instead.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
