schema_version: '1.0'
id: mobile-0911
track: mobile
level: L4
zone: design
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: create
phase: training
title: 'Fault Tolerance: Design Model Update Safety Protocol for Mobile LLM'
scenario: 'You are designing a safe model update protocol for a 1B LLM deployed on an NPU as part of a keyboard app with 50M users. An update last month caused crashes on 0.5% of devices (250K users affected).'
question: 'How would you design a fault-tolerant update protocol that limits the blast radius of a bad update to < 0.01% of total users?'
details:
  realistic_solution: 'A/B model partitions. Staged rollout: 0.01% (5K) -> validation 48h -> 0.1% -> 1% -> 10% -> 100%. Device-side: download to B partition, validate 100 inference calls, commit. Kill switch: server-side flag to rollback all devices in < 1 hour.'
  common_mistake: |
    **The Pitfall:** Not implementing a device-side validation step before committing an update.
    **The Rationale:** Engineers assume that if a model passes cloud CI/CD, it will run correctly on all heterogenous client devices.
    **The Consequence:** Unforeseen device-specific hardware or OS issues cause crashes in production, leading to a massive blast radius.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Target Users: 50M.
       - Blast Radius Safety Limit: < 0.01% of total users (5,000 devices).
       - Bad Update Crash Rate: 0.5%.
       - Kill Switch Latency: 5 minutes (polling) + 50ms (partition switch).
       - Validation Overhead: 100 inference calls × 50ms = 5 seconds/device.
    2. **Calculations:**
       - Stage 1 Rollout Size: 0.01% of 50M = 5,000 devices.
       - Expected Crashes (Stage 1): 5,000 × 0.005 = 25 crashes.
       - Blast Radius: 25 affected users = 0.00005% of 50M users.
    3. **Conclusion & Interpretation:**
       **Result: Fault-Tolerant Rollout.** The combination of 5-second on-device validation and a 0.01% staged rollout ensures the blast radius is strictly contained at **0.00005%**, vastly outperforming the 0.01% constraint.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
