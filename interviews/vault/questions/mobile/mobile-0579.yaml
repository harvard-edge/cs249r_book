schema_version: '1.0'
id: mobile-0579
track: mobile
level: L4
zone: analyze
topic: transformer-systems-cost
competency_area: architecture
bloom_level: analyze
phase: inference
title: The Transformer vs CNN on Mobile
scenario: ViT-Small (22M params, 4.6 GFLOPs) and MobileNetV3-Large (5.4M params, 0.22 GFLOPs) both achieve ~75% top-1 on ImageNet. On a Snapdragon 8 Gen 3, MobileNetV3 runs in 3ms while ViT-Small takes 35ms. Despite ViT having only 21x more FLOPs, it's 12x slower.
question: Why is ViT-Small disproportionately slower than MobileNetV3 on Snapdragon 8 Gen 3 despite having only 21x more FLOPs?
details:
  realistic_solution: Memory access patterns, not arithmetic. MobileNetV3's depthwise convolutions have ~50 FLOPs/byte arithmetic intensity — data stays in NPU SRAM. ViT's attention operators have extremely low arithmetic intensity and often require reshaping and transposes that map poorly to the NPU's fixed-function matrix units. While a single layer's attention matrix (~461 KB) fits in SRAM, the complete forward pass forces excessive DRAM traffic. Furthermore, unsupported ops force costly delegation to the GPU. MobileNetV3 is compute-bound (good for NPU); ViT is memory-bound (bad for mobile).
  common_mistake: |
    **The Pitfall:** Assuming latency scales linearly with FLOPs or parameter count.
    **The Rationale:** Candidates see a 21x FLOP difference and assume ViT's slowdown is purely due to computational complexity, missing the memory-bound nature of attention mechanisms.
    **The Consequence:** Trying to optimize ViT by pruning FLOPs instead of addressing the memory access patterns or switching to a CNN-based architecture that better utilizes the NPU's SRAM.
  napkin_math: |
    1. **MobileNetV3 Compute:** 0.22 GFLOPs / 45 TOPS = 0.005ms arithmetic time. Actual: 3ms (compute-bound with ~50 FLOPs/byte).
    2. **ViT Attention Memory:** 196 $\times$ 196 $\times$ 6 heads $\times$ 2 bytes = 461 KB per layer.
    3. **ViT DRAM Traffic:** Low arithmetic intensity generates ~50 MB of DRAM reads per forward pass.
    4. **ViT Memory Stalls:** 50 MB / 51.2 GB/s = ~1ms best-case, but cache misses and irregular access add 5-10x (~10ms).
    5. **ViT Fallback:** Unsupported ops delegate to GPU (~20ms).
    6. **Conclusion:** Compute (~0.1ms) + Memory (~10ms) + Delegation (~20ms) = **~35ms** total.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
