schema_version: '1.0'
id: mobile-1767
track: mobile
level: L3
zone: analyze
topic: adversarial-robustness
competency_area: reliability
bloom_level: apply
phase: inference
title: Token-Level Safety Classifier Bottleneck
scenario: To defend against prompt injection and model extraction, we deploy a continuous safety classifier alongside an on-device LLM (Gemini Nano, 3.2B parameters, 4-bit) on the Google Tensor G3. The G3 features a TPU with 7.5 TOPS and 12 GB of LPDDR5X memory. The safety classifier (200M parameters, 8-bit) evaluates every single generated token. When activated, the generation speed drops drastically from 20 tokens/sec to 5 tokens/sec, despite the TPU compute utilization remaining under 15%.
question: Why does a 200M INT8 safety classifier cut Gemini Nano generation from 20 to 5 tokens/sec on Tensor G3?
details:
  realistic_solution: |
    The severe drop in throughput is caused by memory cache thrashing and bandwidth contention. Autoregressive generation requires streaming the entire model's weights from RAM for every token. By forcing the system to also load the safety classifier's weights for every token, the TPU constantly context-switches, destroying memory locality and drastically reducing the effective bandwidth of the LPDDR5X memory.
  common_mistake: |
    **The Pitfall:** Blaming the TPU compute capacity (TOPS).
    **The Rationale:** Candidates assume that running two models simultaneously exhausts the 7.5 TOPS.
    **The Consequence:** They fail to recognize that autoregressive text generation and token-by-token classification are heavily memory-bandwidth bound, not compute bound.
  napkin_math: |
    - LLM size: 3.2B params * 0.5 bytes = 1.6 GB.
    - Classifier size: 200M params * 1 byte = 0.2 GB.
    - Total weights loaded per token: 1.8 GB.
    - At the original 20 tokens/sec, the LLM alone required 1.6 GB * 20 = 32 GB/s of bandwidth.
    - To maintain 20 tokens/sec with the defense, the system would need 1.8 GB * 20 = 36 GB/s.
    - While within theoretical LPDDR5X limits (~60 GB/s), the constant context switching drops effective bandwidth.
    - At the observed 5 tokens/sec, the system is only achieving an effective bandwidth of 1.8 GB * 5 = **9.0 GB/s** due to memory thrashing overhead.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
