schema_version: 1
id: mobile-cell-10153
title: Recall depthwise separable convolution parameter count for MobileNetV2 first block
topic: cnn-efficient-design
status: published
provenance: imported
scenario: You are reviewing a MobileNetV2 implementation on Snapdragon 8 Gen 3. The first bottleneck block
  has input 112×112×32, expansion t=1 (no expand for first block), 3×3 depthwise at 32 channels, and project
  32→16. Calculate the exact parameter count and FLOPs for this block and explain why t=1 is used only
  in the first block.
details:
  realistic_solution: '3×3 conv 32→16 at 112×112: 2×32×16×9×112² = 115.6M FLOPs (not 102M).'
  common_mistake: Including bias parameters in the count when MobileNetV2 uses no bias (bias=False) in
    conv layers — all normalization is done by BatchNorm which has its own γ,β parameters; counting BN
    parameters as conv parameters.
  napkin_math: 'Standard 3×3 conv 32→16 at 112×112: FLOPs = 2 × 32 × 16 × 9 × 112 × 112 = 2 × 32 × 16
    × 9 × 12,544 = 2 × 57,802,752 = 115,605,504 ≈ 115.6M FLOPs (not 102M — 102M would require ~88 for
    the kernel×spatial product instead of 9×12544=112,896).'
last_modified: '2026-04-01'
