schema_version: '1.0'
id: mobile-0896
track: mobile
level: L4
zone: design
topic: dataset-curation
competency_area: data
bloom_level: create
phase: training
title: 'Dataset Curation: Design On-Device Data Collection for Mobile Model Training'
scenario: You are building a personalized keyboard model that trains on-device on
  A17 Pro (35 TOPS Neural Engine, 8GB RAM). The model learns from user typing patterns.
  Design the data collection, quality filtering, and privacy-preserving local storage
  pipeline. The device has 500MB available for training data.
question: How would you design the data collection, quality filtering, and privacy-preserving
  local storage pipeline?
details:
  realistic_solution: 'Collect: n-gram statistics not raw keystrokes. Filter: exclude
    password fields (type=password), URLs, and short sessions (< 20 keystrokes). Store:
    DP-noised n-gram counts with ε=2.0. Convert to training examples only at training
    time. Delete after training.'
  common_mistake: Storing raw keystrokes including passwords and sensitive data; not
    applying differential privacy before on-device storage.
  napkin_math: '500MB budget. User types 200 words/day = 1,200 keystrokes/day. Store
    4-gram counts: English has ~500K frequent 4-grams. Per gram count: 4 chars + 4
    byte count = 8 bytes. 500K × 8 = 4MB for n-gram model. Can store 30 days of n-gram
    counts: 30 × 4MB = 120MB. Training data conversion: n-gram to sequence pairs,
    1M pairs × 16 bytes = 16MB. Total: 136MB well within 500MB. DP noise: add Laplace(0,
    1/ε) = Laplace(0, 0.5) to each count, ensuring privacy at ε=2.0.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
