schema_version: '1.0'
id: mobile-1388
track: mobile
level: L6+
zone: mastery
topic: memory-mapped-inference
competency_area: memory
bloom_level: create
phase: inference
title: Zero-Copy LLM Architecture on Google Tensor G3
scenario: You are leading the deployment of a 2B parameter LLM on the Google Tensor G3 (12 GB LPDDR5X). Multiple system apps need concurrent access to this model for instant text processing.
question: How would you design a memory-mapped inference architecture to achieve zero-copy weight sharing across processes?
details:
  realistic_solution: Implement a centralized ML service daemon that maps the model weights into memory using a read-only mmap backed by hardware-friendly buffers (like dmabuf). Client applications communicate with this daemon via IPC. The daemon locks the memory (mlock) to prevent paging, allowing the TPU to perform zero-copy DMA reads straight from the LPDDR5X RAM.
  common_mistake: |
    **The Pitfall:** Suggesting standard per-process weight loading or failing to align mmap buffers.
    **The Rationale:** Treating the mobile OS like a standard server environment without zero-copy DMA considerations.
    **The Consequence:** Exhausting RAM (multiple 1GB copies) and incurring massive 250ms+ cold start latencies from UFS reads.
  napkin_math: |
    - 2B INT4 Model: ~1 GB.
    - 3 concurrent apps: 3 GB (without sharing).
    - Savings with mmap: 2 GB.
    - Memory read (1 GB at 50 GB/s): ~20 ms.
    - Compute (4 GOPs at 7.5 TOPS): ~0.53 ms.
    - **Eliminating CPU-RAM copies guarantees sub-50ms Time-To-First-Token.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
