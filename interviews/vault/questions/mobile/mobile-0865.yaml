schema_version: '1.0'
id: mobile-0865
track: mobile
level: L2
zone: fluency
topic: cnn-efficient-design
competency_area: architecture
bloom_level: remember
phase: inference
title: Recall depthwise separable convolution parameter count for MobileNetV2 first
  block
scenario: You are reviewing a MobileNetV2 implementation on Snapdragon 8 Gen 3. The
  first bottleneck block has input 112×112×32, expansion t=1 (no expand for first
  block), 3×3 depthwise at 32 channels, and project 32→16. Calculate the exact parameter
  count and FLOPs for this block and explain why t=1 is used only in the first block.
question: How would you calculate the exact parameter count and FLOPs for this block
  and explain why t=1 is used only in the first block?
details:
  realistic_solution: '3×3 conv 32→16 at 112×112: 2×32×16×9×112² = 115.6M FLOPs (not
    102M).'
  common_mistake: Including bias parameters in the count when MobileNetV2 uses no
    bias (bias=False) in conv layers — all normalization is done by BatchNorm which
    has its own γ,β parameters; counting BN parameters as conv parameters.
  napkin_math: 'Standard 3×3 conv 32→16 at 112×112: FLOPs = 2 × 32 × 16 × 9 × 112
    × 112 = 2 × 32 × 16 × 9 × 12,544 = 2 × 57,802,752 = 115,605,504 ≈ 115.6M FLOPs
    (not 102M — 102M would require ~88 for the kernel×spatial product instead of 9×12544=112,896).'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Math `32×16×9×112²×2 = 102M FLOPs` is incorrect; it equals ~115.6M FLOPs.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
