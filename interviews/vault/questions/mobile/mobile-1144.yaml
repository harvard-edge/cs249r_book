schema_version: '1.0'
id: mobile-1144
track: mobile
level: L5
zone: design
topic: profiling-bottleneck-analysis
competency_area: latency
bloom_level: evaluate
phase: inference
title: Architecting Heterogeneous Profiling for NPU Latency
scenario: You are designing the execution and profiling architecture for a real-time
  AR pipeline running on the Snapdragon 8 Gen 3. The ML workload targets the Qualcomm
  Hexagon NPU (45 TOPS INT8), while graphics render on the GPU, with both sharing
  the 12-16 GB LPDDR5X memory pool. You must design a system to trace and isolate
  the root cause of intermittent latency spikes, determining whether they stem from
  NPU compute limits, CPU-NPU synchronization overhead, or memory bandwidth contention.
question: How should a Snapdragon 8 Gen 3 profiling system distinguish NPU compute
  limits, sync overhead, and memory contention?
details:
  realistic_solution: Design a telemetry system that uses hardware-specific tracing
    tools (like Snapdragon Profiler) to capture NPU hardware performance counters
    alongside GPU and CPU traces. The architecture should correlate asynchronous NPU
    queue execution times with memory bus utilization metrics to differentiate compute-bound
    operations from bandwidth-starved NPU stalls.
  common_mistake: Candidates often propose running standard CPU/GPU profilers or inserting
    simple CPU timestamps, failing to account for the asynchronous nature of the Hexagon
    NPU DSP queues and the hidden latency of memory contention across the heterogeneous
    system.
  napkin_math: For a 30 FPS pipeline, the latency budget is 33.3ms per frame. A 1.5
    Tera-operation model requires exactly 33.3ms of theoretical compute on the 45
    TOPS INT8 NPU (1.5 / 45 = 0.0333s). If the model processes 250MB of activations
    and weights per frame, it demands 7.5 GB/s of memory bandwidth (250MB * 30 FPS).
    If the GPU saturates the shared 12-16 GB LPDDR5X bus, the NPU will stall, and
    actual latency will significantly exceed the 33.3ms compute bound.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
