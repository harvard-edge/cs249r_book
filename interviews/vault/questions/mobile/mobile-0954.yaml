schema_version: '1.0'
id: mobile-0954
track: mobile
level: L5
zone: mastery
topic: kv-cache-management
competency_area: memory
bloom_level: create
phase: inference
title: 'KV-Cache: Mastery — Long-Context KV Cache Strategy for Mobile Code Assistant'
scenario: Design the KV cache strategy for a 3B LLM code assistant on an edge device with 8GB RAM that must handle 32K-token codebases. The current 4096-token limit is insufficient.
question: How would you design a memory-efficient KV cache, eviction policy, and quality preservation strategy for a 32K context?
details:
  realistic_solution: |
    Deploying a 32K full KV cache in BF16 is mathematically impossible on an 8GB device due to the immense scale of the Key/Value matrices. The solution requires compounding structural memory optimizations:
    1. **INT4 KV Cache Quantization:** Downcasting KV caches to 4-bit shrinks the memory requirement by 4x with minimal perplexity degradation.
    2. **Local Sliding Window Attention:** Expire/evict standard tokens outside a sliding 4096-token local window.
    3. **Attention Sinks (Landmarks):** Preserve the first ~1024 tokens (the system prompt and crucial context) permanently in cache to anchor the attention distribution, preventing the model's logic from decaying over long sequences.
  common_mistake: |
    **The Pitfall:** Proposing architectural changes (like Ring Attention) without actively calculating the hard hardware memory limits.
    **The Rationale:** Candidates lean on trendy academic papers but fail to quantify the impossibility of holding a 4.2 GB cache in a constrained 8GB device already running an OS and model weights.
    **The Consequence:** Designing an elegant software system that immediately triggers a hard OS-level OOM crash upon processing its first large codebase.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Target Context: 32,768 tokens.
       - Architecture: 3B model (32 layers, GQA-8, 128 head_dim). Precision: BF16 (2 bytes/param).
       - System Overhead: 8GB total - 2GB OS - 0.75GB Weights = ~5.25 GB available RAM.
    2. **Calculations:**
       - Full BF16 KV Cache: 2 (K+V) * 32,768 tokens * 32 layers * 8 heads * 128 dim * 2 bytes = 4.19 GB.
       - Capacity Check: 4.19 GB cache in a 5.25 GB budget guarantees catastrophic eviction.
       - Optimized Strategy (INT4 + Window + Sinks): 
         - Downcast to 4-bit: 4.19 GB / 4 = ~1.05 GB (for full 32k).
         - Apply Windowing (4096 local + 1024 sink = 5120 active tokens): 5120 * 131 KB = ~671 MB.
    3. **Conclusion & Interpretation:**
       **Result: ~671 MB optimized KV cache (Memory-Capacity-Bound solved).** Compressing bit-depths and truncating attention topologies safely fits 32K functionality entirely within the 8GB edge envelope.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 18
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
