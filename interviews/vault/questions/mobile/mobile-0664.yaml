schema_version: '1.0'
id: mobile-0664
track: mobile
level: L3
zone: fluency
topic: memory-hierarchy-design
competency_area: memory
bloom_level: apply
phase: inference
title: The Mobile LLM KV-Cache Squeeze
scenario: You're running a 3B parameter LLM (Phi-3-mini, INT4) on an iPhone 15 Pro
  with 8 GB RAM. The model weights take 1.5 GB. Short conversations work fine, but
  after 10+ back-and-forth turns, the app gets killed by iOS. The model weights haven't
  changed. What's growing?
question: What grows until iOS jetsams a 3B INT4 Phi-3-mini chat app after 10 or more
  turns?
details:
  realistic_solution: 'The KV-cache. During autoregressive generation, the model stores
    key and value tensors for every token in the conversation history. For Phi-3-mini
    (32 layers, 32 heads, head_dim=96, FP16 KV): KV-cache per token = 2 × 32 × 32
    × 96 × 2 bytes = 393 KB. After 10 turns (~2000 tokens): 2000 × 393 KB = **786
    MB**. Add model weights (1.5 GB), iOS overhead (~3 GB), and app runtime (~200
    MB): total = 5.5 GB. iOS jetsam threshold is ~6 GB for foreground apps. By turn
    15 (~3000 tokens): KV-cache = 1.15 GB, total = 5.85 GB — jetsam kills the app.'
  common_mistake: '"There''s a memory leak in the inference engine." The memory growth
    is by design, not a bug.'
  napkin_math: 'Phi-3-mini KV cache: 32 layers, 32 heads, dim 96. Each token = 2 *
    32 * 32 * 96 * 2B = 393KB. At 3000 tokens, KV cache = 1.15GB! iPhone 15 Pro (8GB
    RAM): iOS takes 3GB, model weights 1.5GB, app 0.5GB. With 1.15GB KV cache, total
    is 6.15GB, crossing the ~6GB iOS Jetsam limit. It OOMs strictly from KV growth.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: mobile-chain-067
  position: 1
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Napkin math calculation for KV-cache per token is slightly inaccurate (393KB should
  be 384KB).
validation_status_pro: WARN
validation_issues_pro:
- The napkin math for GQA uses 8 KV heads for Phi-3-mini, but the actual model has
  4 KV heads.
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- The KV cache size calculated is 3000 tokens * 393.2 KB/token = 1.18 GB, but the
  text loosely rounds this down to 1.15 GB.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
