schema_version: '1.0'
id: mobile-0907
track: mobile
level: L4
zone: design
topic: dataset-curation
competency_area: data
bloom_level: apply
phase: training
title: 'Dataset Curation: Realize Cross-Device Dataset Consistency for Mobile Fleet'
scenario: You are rolling out a federated learning update to 50M mobile devices split between iOS and Android architectures.
question: How should cross-device federated learning enforce consistent tokenizers, data formats, and label schemas?
details:
  realistic_solution: |
    Package the tokenizer and model as a single immutable artifact. Enforce strict schema versioning via Protobuf for all labels and utilize SHA256 content-addressed storage to guarantee data artifact consistency before clients participate in training rounds.
  common_mistake: |
    **The Pitfall:** Decoupling tokenizer deployments from model weight updates.
    **The Rationale:** Treating tokenizers as static preprocessing utilities rather than tightly coupled model parameters.
    **The Consequence:** Version skew across the fleet causes devices to submit garbage gradients, silently poisoning the global federated model.
  napkin_math: |
    1. **Assumptions & Constraints:** 50M devices. SentencePiece tokenizer = 2MB. 1M examples per device. Gradient upload = 200KB per round.
    2. **Calculations:** 2% version skew = 1M devices excluded. Protobuf schema overhead = 1 byte/example = 1MB overhead. Bandwidth = 50M * 200KB = 10TB = $100/round.
    3. **Conclusion & Interpretation:** **Result: Network-Bound**. Bundling tokenizers eliminates the 2% silent corruption rate with negligible bandwidth impact.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
