schema_version: '1.0'
id: mobile-1000
track: mobile
level: L3
zone: analyze
topic: network-bandwidth-bottlenecks
competency_area: networking
bloom_level: analyze
phase: inference
title: 'Network Bandwidth Bottlenecks: Analyze LPDDR5X Shared Bus for Mobile SoC Memory Bottleneck'
scenario: A mobile SoC uses a unified LPDDR5X memory bus shared between CPU, GPU, and DSP. Analyze how memory bandwidth contention between a 3B INT4 LLM and a concurrent video encode task (H.265 encoder reading/writing 4K frames) creates a bottleneck.
question: How much of a 77 GB/s Snapdragon memory bus does a 3B INT4 LLM consume alongside 4K H.265 encoding, and how does it degrade TPOT?
details:
  realistic_solution: 'LLM decode BW: 3B INT4 = 1.5GB weight reads per token. At 19.5ms/token: 1.5GB/0.0195s = 76.9GB/s — nearly saturates the entire bus! Video encoder: 4K @ 30fps YUV420: 4096x2160x1.5x30=0.4GB/s (negligible). But LLM alone saturates 77GB/s. In practice, INT4 dequantization creates additional writes to scratchpad: add 20% overhead -> LLM needs 92GB/s > 77GB/s. Result: TPOT degrades from 19.5ms to ~25ms due to BW contention and saturation. Fix: reduce LLM to batch=1 without concurrent tasks, or use INT8 model (3GB, same BW but different contention pattern).'
  common_mistake: |
    **The Pitfall:** Analyzing the LLM and video encoder in isolation.
    **The Rationale:** Candidates assume each gets the full 77 GB/s, forgetting the shared LPDDR5X bus means their bandwidths sum together.
    **The Consequence:** Failing to predict severe TPOT degradation from memory contention.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - LLM Size: 3B INT4 (1.5 GB weights).
       - Video Encode: 4096x2160, YUV420, 30 FPS.
       - Bus Capacity: 77 GB/s.
       - ISP/Display Bandwidth Baseline: 1.0 GB/s.
    2. **Calculations:**
       - LLM Bandwidth (isolated): 1.5GB / 0.0195s = 76.9 GB/s.
       - Video Encode Bandwidth: 4096 * 2160 * 1.5 * 30 FPS = ~0.4 GB/s.
       - Total Demand: 76.9 + 0.4 + 1.0 = 78.3 GB/s (> 77 GB/s Limit).
       - Actual LLM Available Bandwidth: 77 - 1.4 = 75.6 GB/s.
       - Calculate Actual TPOT: 1.5 GB / 75.6 GB/s = 19.8 ms.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bound.** The LLM nearly saturates the bus, and contention degrades TPOT.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
