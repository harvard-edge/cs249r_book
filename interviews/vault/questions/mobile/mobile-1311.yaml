schema_version: '1.0'
id: mobile-1311
track: mobile
level: L3
zone: fluency
topic: distribution-drift-detection
competency_area: reliability
bloom_level: apply
phase: inference
title: On-Device KL Divergence Calculation
scenario: You are running a background drift detection task on a Samsung Exynos 2400 NPU. To avoid waking up the CPU, the dual-core NPU computes the Kullback-Leibler (KL) divergence of a categorical feature using base-2 logarithms. The actual observed distribution (P) across three categories is [0.5, 0.25, 0.25], while the expected baseline distribution (Q) stored in the 12 GB LPDDR5X memory is [0.25, 0.5, 0.25].
question: How would you calculate the KL divergence KL(P||Q) from memory, and determine if the 34.7 TOPS NPU is bottlenecked by compute or the LPDDR5X memory bandwidth for this operation?
details:
  realistic_solution: The candidate should correctly recall and apply the KL divergence formula by multiplying each probability in P by the base-2 log of the ratio of P to Q. For the hardware aspect, they should recognize that calculating this over small probability arrays requires virtually zero compute, meaning the operation is entirely bottlenecked by the latency and bandwidth of pulling the baseline Q distribution from the shared LPDDR5X memory.
  common_mistake: |
    **The Pitfall:** Confusing the direction of KL divergence or misjudging the arithmetic intensity.
    **The Rationale:** Candidates often calculate KL(Q||P) instead of KL(P||Q) and assume mathematical operations must be compute-bound on an NPU.
    **The Consequence:** Incorrect drift calculations and failing to realize the operation is entirely bottlenecked by memory latency, not the 34.7 TOPS NPU compute.
  napkin_math: |
    * Formula: KL(P||Q) = sum(P(i) * log2(P(i) / Q(i))).
    * Bin 1: 0.5 * log2(0.5 / 0.25) = 0.5 * log2(2) = 0.5.
    * Bin 2: 0.25 * log2(0.25 / 0.5) = 0.25 * log2(0.5) = -0.25.
    * Bin 3: 0.25 * log2(0.25 / 0.25) = 0.25 * log2(1) = 0.
    * Total KL Divergence = 0.5 - 0.25 + 0 = 0.25.
    * Memory: Fetching 6 floats (24 bytes) from LPDDR5X (~60 GB/s). Compute: ~15 ops. Arithmetic intensity = 0.625 Ops/Byte.
    * **Max throughput = 60 GB/s × 0.625 = 37.5 GOPS, massively below the 34.7 TOPS limit, proving it is memory-bound.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
