schema_version: '1.0'
id: mobile-0972
track: mobile
level: L6+
zone: mastery
topic: mlops-lifecycle
competency_area: deployment
bloom_level: create
phase: inference
title: 'MLOps Lifecycle: End-to-End Mobile MLOps with Personalization Pipeline'
scenario: A mobile health app uses on-device ML on a Tensor G3 for personalized activity recognition. The system fine-tunes a base model (10M params INT8, 10MB) using 30 days of on-device sensor data, then uploads adapter weights for fleet aggregation.
question: How would you design the full lifecycle including on-device training, privacy-preserving aggregation, global model update, and OTA distribution to 5M devices?
details:
  realistic_solution: 'On-device fine-tuning: last 2 layers only, 200K params × 30 days data × 1000 samples/day. Training: 30K gradient steps on Tensor G3 at 7.5 TOPS. Forward pass dominated; backprop through 2 layers only: 0.4M FLOPs/step × 30K steps = 12G FLOPs total over 30 days = 400M FLOPs/day — trivial (seconds/day). Adapter upload: 200K × 4 bytes = 0.8MB. 5M devices × 0.8MB = 4TB/aggregation round. Federated avg on H100: 5M gradient tensors, 200K dim each = 5M × 200K × 4B = 400TB to sum (streaming, never stored all at once). OTA: 10MB × 5M = 50TB.'
  common_mistake: |
    **The Pitfall:** Attempting to fine-tune the full 10M parameter model on-device.
    **The Rationale:** Not realizing that full backprop is rarely supported natively on mobile TPUs and consumes excessive memory/compute.
    **The Consequence:** Fails on-device execution or drains battery, whereas fine-tuning just the last 2 layers (200K params) is trivial and effective.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Device count: 5M devices.
       - Adapter size: 200K params (FP32).
       - Global model OTA: 10MB per device.
       - Tensor G3 capacity: 7.5 TOPS.
       - Steps: 30K gradient steps.
    2. **Calculations:**
       - On-device training FLOPs: 200K params * 2 (forward+backward) * 30K steps = 12B FLOPs.
       - Time on 7.5 TOPS TPU: 12B / 7.5T = 1.6ms total.
       - Per day: 1.6ms / 30 = 0.053ms/day (runs trivially in background).
       - Adapter upload: 200K * 4 bytes = 0.8MB per device.
       - Total upload for 5M devices: 5M * 0.8MB = 4TB.
       - Global model OTA: 10MB * 5M = 50TB.
    3. **Conclusion & Interpretation:**
       - **Result: Bandwidth-Bound. CDN OTA Cost is 50TB * $0.09/GB = $4,500/round.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
