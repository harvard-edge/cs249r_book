schema_version: '1.0'
id: mobile-0886
track: mobile
level: L6+
zone: mastery
topic: compound-ai-systems
competency_area: deployment
bloom_level: create
phase: inference
title: Mastering On-Device Knowledge Base Update Strategy for Compound AI on Tensor G3
scenario: A Tensor G3 (7.5 TOPS, 12GB) enterprise mobile app serves 10,000 users with personalized on-device RAG. Each user has a local corpus of 2,000 documents. The enterprise syncs 50 new/updated documents per user per day via MDM push. The embedding model (256-dim, INT8, 22MB) runs at 150 vecs/s on Tensor G3 NPU.
question: How would you design a knowledge base update strategy that maintains retrieval quality, minimizes battery impact, and handles the case where updates arrive while the user is actively using the app?
details:
  realistic_solution: 'Copy-on-write index with shadow update: (1) Maintain active index (read-only, served from mmap). (2) Background thread builds a shadow index by adding new vectors to a cloned or delta index. (3) When shadow is complete, perform an atomic pointer swap (< 1ms). (4) During active use, queue incoming updates in a tiny flat index (max 50 entries = 50 * 256 * 4 = 51KB) and query active_index + flat_pending_index, then merge results. (5) Schedule shadow builds when screen-off or charging, using a 10% NPU duty cycle. Embedding 50 docs takes 50/150 = 0.33s of active NPU compute; at 10% duty, that spans about 3.3s of wall-clock time. Energy is active power times active time, or equivalently average duty-cycled power times wall-clock time: 5W * 0.33s = 0.5W * 3.3s = 1.65J. Active-use overhead from the flat index is about 0.5ms.'
  common_mistake: |
    **The Pitfall:** Updating the FAISS index atomically by taking a write lock during active user querying.
    **The Rationale:** Developers often use simple thread locks for data structure updates without calculating the blocking time caused by the underlying NPU embedding latency.
    **The Consequence:** The retrieval stage blocks for up to 333ms, causing users to experience random, noticeable pauses mid-conversation.
  napkin_math: |
    **Assumptions & Constraints:**
    - Update Workload: 50 docs/day at 150 vecs/s.
    - Index insertion: FAISS HNSW adding 50 vectors takes ~5ms.
    - Active-use Overhead (Flat Index): 50 vectors * 256 dims * 4 bytes = 51 KB. Search time = 0.5ms.

    **Calculations:**
    - Active NPU time: 50 / 150 = 0.33s.
    - Wall-clock time at 10% duty cycle: 0.33s / 0.10 = 3.3s elapsed.
    - Battery impact: 5W active * 0.33s active = 1.65 J = ~0.00046 Wh per day.
    - Equivalent duty-cycle view: 0.5W average NPU power * 3.3s elapsed = 1.65 J.
    - Combined Query Latency: HNSW(2000 docs) at 5ms + flat(50 docs) at 0.5ms = 5.5ms.

    **Conclusion & Interpretation:**
    - **Result: 0.33s active compute, 3.3s elapsed background time, 5.5ms query latency.** The dual-index strategy keeps user-facing retrieval responsive while making the daily update energy small under the stated duty-cycle assumptions.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
