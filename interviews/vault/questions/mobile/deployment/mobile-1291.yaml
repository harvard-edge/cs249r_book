schema_version: '1.0'
id: mobile-1291
track: mobile
level: L5
zone: evaluation
topic: model-format-conversion
competency_area: deployment
bloom_level: evaluate
phase: inference
title: LLM TPU Delegation vs CPU Fallback on Tensor G3
scenario: You are deploying a 2B parameter INT8 mobile LLM. You must choose between Option A mapping all operators to standard TFLite ops for full TPU delegation with a 2.2B-parameter graph, and Option B retaining a smaller 2.0B-parameter custom op graph that forces 30% of token compute onto the CPU.
question: 'Which TFLite strategy is faster: 100% TPU delegation with 2.2B params or 30% CPU fallback at 2.0B?'
details:
  realistic_solution: |
    Option A is the correct choice. Avoiding CPU fallback is critical because alternating execution between the CPU and TPU for every token completely bottlenecks generation latency due to synchronization limits and the TPU's massive compute advantage.
  common_mistake: |
    **The Pitfall:** Choosing partial CPU fallback to save memory.
    **The Rationale:** Candidates assume that 70% TPU delegation is "good enough" or that memory savings justify the fallback, ignoring the massive latency penalty of alternating execution.
    **The Consequence:** Auto-regressive decoding speed is completely bottlenecked by synchronization overhead and CPU slowness.
  napkin_math: |
    **Assumptions & Constraints:**
    - TPU compute: 7.5 TOPS = 7500 GOPS.
    - CPU compute for unsupported ops: ~100 GOPS.
    - Decoder cost estimate: about 2 operations per parameter per generated token.
    - CPU fallback also adds TPU/CPU synchronization overhead.

    **Calculations:**
    - Option A token work: 2.2B params * 2 ops/param = 4.4 GOP/token.
    - Option A ideal TPU time: 4.4 GOP / 7500 GOPS = 0.00059 s = 0.59 ms/token before memory and runtime overheads.
    - Option B token work: 2.0B params * 2 ops/param = 4.0 GOP/token.
    - Option B TPU portion: 70% * 4.0 GOP / 7500 GOPS = 0.37 ms/token.
    - Option B CPU portion: 30% * 4.0 GOP / 100 GOPS = 12 ms/token, plus synchronization at each boundary.

    **Conclusion & Interpretation:**
    - **Result: CPU fallback dominates latency.** Even with fewer parameters, Option B spends far more time in CPU execution and synchronization than Option A spends on the slightly larger fully delegated graph.
status: published
provenance: imported
