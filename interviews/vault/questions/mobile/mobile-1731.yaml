schema_version: '1.0'
id: mobile-1731
track: mobile
level: L5
zone: specification
topic: streaming-ingestion
competency_area: data
bloom_level: analyze
phase: inference
title: Real-time Edge ML System Design for Mobile Sensor Data
scenario: Design an edge ML system for real-time processing of high-frequency sensor data (e.g., IMU, audio, camera metadata) on a mobile device equipped with a Snapdragon 8 Gen 3 Hexagon NPU. The NPU provides 45 TOPS INT8 performance and has access to 12-16 GB LPDDR5X memory. The system needs to ingest data from multiple sensors, perform lightweight ML inference for anomaly detection or feature extraction with sub-10ms latency, and push aggregated insights to a cloud backend while minimizing power consumption and maintaining privacy.
question: How would you design an edge ML system for real-time processing of high-frequency sensor data (e.g., IMU, audio, camera metadata) on a mobile device equipped with a Snapdragon 8 Gen 3 Hexagon NPU?
details:
  realistic_solution: Implement a stream processing pipeline using a lightweight, quantized ML model (e.g., MobileNetV3, EfficientNet Edge TPU variant, or a custom tiny transformer) optimized for INT8 inference on the Hexagon NPU. Utilize the NPU's specialized DSP cores for pre-processing and post-processing tasks. Data ingestion should leverage hardware-accelerated sensor interfaces. Employ a circular buffer for incoming sensor data to manage memory efficiently. For aggregation, use a time-windowing approach (e.g., 100ms windows) before pushing to the cloud, potentially using MQTT or a similar lightweight protocol. Implement dynamic power management, scaling NPU clock speeds based on workload or available battery.
  common_mistake: |
    **The Pitfall:** Overlooking the strict memory bandwidth and power constraints of continuous high-frequency data ingestion.
    **The Rationale:** Candidates propose large models or continuous cloud uploads, forgetting that constant polling and memory bus saturation drain the battery and miss latency targets.
    **The Consequence:** The system is too power-hungry for continuous operation and fails to synchronize multiple high-frequency sensors accurately.
  napkin_math: |
    1. **Compute Budget:** To achieve sub-10ms latency (100 Hz), the model must compute in <10ms.
    2. **Compute Capacity:** 45 TOPS (45,000 GOPs/sec) × 0.010 sec = 450 GOPs per inference maximum.
    3. **Bandwidth Load:** Streaming 4 high-res sensors at 100 MB/s each = 400 MB/s continuous memory bandwidth usage.
    4. **Memory Constraint:** While 400 MB/s is well within the LPDDR5X peak bandwidth (64-85 GB/s), it requires continuous active states and prevents the SoC from entering deep sleep.
    **Result:** The model must be kept strictly under **450 GOPs**, and hardware-accelerated DSP buffers must be used to aggregate the **400 MB/s** sensor data to conserve power.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
