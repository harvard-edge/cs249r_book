schema_version: '1.0'
id: mobile-1270
track: mobile
level: L4
zone: diagnosis
topic: model-format-conversion
competency_area: deployment
bloom_level: analyze
phase: inference
title: NPU Memory Bandwidth Contention
scenario: A vision model converted to TFLite is deployed on the Samsung Exynos 2400 NPU. The device features a dual-core NPU with 34.7 TOPS and shares 12 GB of LPDDR5X memory across the CPU, GPU, and ISP. During field testing, engineers observe that while median inference latency is excellent, 99th percentile latency spikes by 5x specifically when the camera ISP is simultaneously processing high-resolution video frames.
question: "Why does Exynos 2400 NPU P99 latency spike 5x when the ISP processes high-resolution video?"
details:
  realistic_solution: The root cause is memory bandwidth contention. Because the 12 GB LPDDR5X memory is shared, the ISP consuming massive bandwidth for high-resolution video starves the NPU of data. To resolve this, the deployment engineer must optimize the model's memory footprint (e.g., ensuring fully quantized INT8 operators to minimize weight fetching) or implement a scheduler that throttles NPU inferences during peak ISP buffer flushes.
  common_mistake: |
    **The Pitfall:** Misdiagnosing the latency spikes as NPU thermal throttling or missing operator coverage forcing CPU fallbacks.
    **The Rationale:** Candidates fail to identify the architectural bottleneck of shared unified memory subsystems in mobile SoCs.
    **The Consequence:** Time is wasted on model-level optimizations (like removing specific layers) when the root cause is external resource contention.
  napkin_math: |
    - **Bandwidth Limits:** LPDDR5X provides peak ~68 GB/s.
    - **Contention:** 4K 60FPS ISP workload uses ~40 GB/s. OS/CPU uses ~15 GB/s. Remaining for NPU = **~13 GB/s**.
    - **Model Demands:** 300 MB traffic/inference * 30 FPS = **9 GB/s required**.
    - **Result:** During ISP burst buffers, available bandwidth drops below 9 GB/s. The 34.7 TOPS NPU starves for data, causing P99 latency to spike 5x.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
