schema_version: '1.0'
id: mobile-1499
track: mobile
level: L4
zone: specification
topic: dataset-curation
competency_area: data
bloom_level: analyze
phase: training
title: Data Curation for INT8 Hexagon NPU Calibration
scenario: Your team is deploying a real-time object detection model to the Qualcomm Hexagon NPU (Snapdragon 8 Gen 3) on a new flagship phone. The NPU requires INT8 quantization to achieve its 45 TOPS peak performance, but post-training quantization is causing a significant drop in mean Average Precision (mAP) due to outlier activations.
question: How would you curate an INT8 calibration dataset that captures long-tail edge cases considering the shared 12-16 GB LPDDR5X memory?
details:
  realistic_solution: Design an active learning pipeline that runs the FP32 model over a large unlabelled pool, logging maximum activation values across all layers. Curate the calibration dataset by stratifying based on these activation magnitudes, ensuring the long-tail 'outlier' images are heavily sampled. This provides the quantization algorithm with the true dynamic range needed to set clipping thresholds optimized for the Hexagon NPU's INT8 execution.
  common_mistake: |
    **The Pitfall:** Proposing uniform random sampling from the training set.
    **The Rationale:** Candidates fail to realize that random sampling under-represents rare lighting and poses that cause the activation spikes necessary for accurate scale/zero-point calibration.
    **The Consequence:** The quantized model clips outlier activations incorrectly, leading to a massive drop in mAP.
  napkin_math: |
    1. **Dataset Size:** 500 images at 1080p (FP32) = 1920 * 1080 * 3 channels * 4 bytes = 24.8 MB per image.
    2. **Total Memory:** 500 * 24.8 MB = 12.4 GB raw data.
    3. **Constraint:** Shared 16GB RAM leaves only a few GB free due to OS/GPU overhead.
    4. **Solution:** Must process calibration in batches (e.g., 16 images = **~400 MB footprint**) rather than loading the whole 12.4 GB set at once.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
