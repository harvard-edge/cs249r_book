schema_version: '1.0'
id: mobile-0049
track: mobile
level: L5
zone: evaluation
topic: federated-learning
competency_area: cross-cutting
bloom_level: evaluate
phase: inference
title: The Privacy-Utility Squeeze
scenario: You're implementing federated learning for a next-word prediction model across 100 million Android
  devices. Each device fine-tunes locally and uploads gradient updates. The privacy team mandates differential
  privacy with ε = 2. After deployment, the model's perplexity is 40% worse than centralized training.
  The PM asks you to 'just increase epsilon.' What do you tell them?
question: "What should you tell the PM when epsilon 2 differential privacy makes federated next-word prediction 40% worse?"
details:
  realistic_solution: 'The problem isn''t just ε — it''s the interaction between DP noise, client heterogeneity,
    and the communication budget. With ε = 2, the per-update Gaussian noise scale is large relative to
    the gradient signal from a single device''s small local dataset (maybe a few hundred sentences). But
    simply increasing ε is a one-way door: once you ship a weaker privacy guarantee, you can''t retroactively
    strengthen it for data already collected.'
  common_mistake: '"ε = 2 is too strict, we should relax to ε = 8 or 10." This treats ε as a tuning knob
    without understanding the system-level trade-offs.'
  napkin_math: '**DP-FedAvg Noise & Composition:** To achieve a global ε=2 over T rounds, the noise multiplier must account for composition. Typical σ_sum can be ~50-100. Let σ_sum = 100. | **Aggregation over n clients:** Noise on the average gradient = σ_sum / n. At n=1,000: σ_agg = 100 / 1000 = 0.1. At n=10,000: σ_agg = 100 / 10000 = 0.01. (10x more clients => 10x less noise, i.e., 10x better SNR). | **LoRA dimension reduction:** Noise is added to *every* parameter. The L2 norm of the noise vector scales with √d. Full model: 85M params. LoRA: ~600K params. Ratio: 85M / 600K ≈ 142x fewer params. The noise vector L2 norm is reduced by √142 ≈ 11.9x, improving SNR by 11.9x without touching ε. | **Combined fix:** 10x clients + LoRA = 10 x 11.9 = **119x better effective SNR** at the same ε=2.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: OK
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
