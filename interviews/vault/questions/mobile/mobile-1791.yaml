schema_version: '1.0'
id: mobile-1791
track: mobile
level: L5
zone: specification
topic: model-serving-infrastructure
competency_area: architecture
bloom_level: create
phase: inference
title: Multi-Task Model Specification for On-Device AI Assistant on A17 Pro
scenario: 'Specify the architecture of an on-device AI assistant for iPhone 15 Pro
  that must handle: speech recognition (streaming, <200ms latency), intent classification
  (50ms), and response generation (LLM, 10-15 tok/s). Total memory budget: 1.5GB.
  Specify model sizes, quantization, and execution units.'
question: Which model sizes, quantization levels, and execution units fit ASR, intent,
  and LLM generation in 1.5 GB on iPhone 15 Pro?
details:
  realistic_solution: 'Speech recognition: Whisper-small (244M params) → INT4 = 122MB,
    runs on ANE. Streaming: chunk-based (1s windows), ANE throughput = 35 TOPS, Whisper-small
    2.3 GFLOP/s = 0.066ms chunk → well within 200ms real-time. Intent classification:
    lightweight BERT-mini (11M params) INT8 = 11MB, runs on ANE concurrently. Response
    LLM: Phi-2 2.7B INT4 = 1.35GB — large but fits budget. Total: 122+11+1350 = 1483MB
    ≈ 1.45GB < 1.5GB. Execution: Whisper on ANE (streaming background), intent on
    ANE (post-ASR, 50ms), LLM on CPU/ANE hybrid (decode). Pipeline: ASR feeds intent
    feeds LLM. No model is running simultaneously at full capacity.'
  common_mistake: Designing three separate full-size models that each compete for
    memory; a shared encoder with task-specific heads dramatically reduces memory
    footprint.
  napkin_math: 'Whisper-small INT4: 122MB. BERT-mini INT8: 11MB. Phi-2 INT4: 1350MB.
    Total: 1483MB < 1500MB budget. Execution: ANE handles ASR+intent (sequential,
    <250ms). LLM: CPU+Metal at 10-15 tok/s. Memory budget met. Latency: ASR 200ms
    + intent 50ms + first token 300ms = 550ms response start.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
