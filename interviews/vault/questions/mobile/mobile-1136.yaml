schema_version: '1.0'
id: mobile-1136
track: mobile
level: L5
zone: design
topic: memory-pressure-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: Architecting Memory Management for On-Device LLMs
scenario: You are designing an on-device GenAI feature targeting the Qualcomm Hexagon
  NPU on the Snapdragon 8 Gen 3. The platform features 45 TOPS INT8 performance and
  a heterogeneous CPU+GPU+NPU architecture with a shared 12-16 GB LPDDR5X memory pool.
  However, the OS actively manages memory pressure, and your application will be evicted
  if it consumes too much of the shared RAM or causes system-wide memory fragmentation
  during generation. Architect a memory management strategy to handle the KV cache,
  model weights, and intermediate activations without triggering OS-level OOM eviction.
question: How would you architect a memory management strategy to handle the KV cache,
  model weights, and intermediate activations without triggering OS-level OOM eviction?
details:
  realistic_solution: A robust design involves implementing a paged KV cache system
    to eliminate memory fragmentation and allow dynamic allocation. Model weights
    should be quantized and memory-mapped (mmap) to allow the OS to page them out
    under extreme pressure, while memory pressure listeners dynamically shrink the
    context window or halt generation before eviction occurs.
  common_mistake: Candidates often assume exclusive access to the full 12-16 GB of
    RAM and design static allocations that ignore OS-level memory pressure. They might
    propose keeping the entire KV cache in contiguous memory, failing to account for
    memory fragmentation which leads to sudden OOM kills.
  napkin_math: On a Snapdragon 8 Gen 3 with 12 GB shared LPDDR5X, the OS reserves
    ~6 GB, leaving a 6 GB budget. Assume a 7B parameter model quantized to INT4 takes
    3.5 GB for weights. Total Budget (6 GB) - Weights (3.5 GB) - NPU INT8 Activations
    (0.5 GB) = 2.0 GB remaining for the KV cache. At 128 KB per token, the maximum
    context size is 2.0 GB / 128 KB = 16,384 tokens. Paging this cache into 2,048
    pages of 1 MB each prevents fragmentation OOMs.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
