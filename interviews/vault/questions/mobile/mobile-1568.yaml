schema_version: '1.0'
id: mobile-1568
track: mobile
level: L6+
zone: realization
topic: profiling-bottleneck-analysis
competency_area: latency
bloom_level: analyze
phase: inference
title: Optimizing On-Device Object Detection Latency on Tensor G3
scenario: Your team has deployed a new real-time object detection model on Google
  Pixel phones powered by the Google Tensor G3 chip. The model is designed for a critical
  AR application that processes high-resolution video frames (e.g., 4K input). Initial
  user reports indicate intermittent, but significant, latency spikes during inference,
  making the AR experience janky. The Tensor G3 features a 7.5 TOPS on-device TPU
  and 12 GB LPDDR5X RAM. You are tasked with identifying and resolving the primary
  bottleneck causing these latency issues.
question: How would you optimize Tensor G3 on-device object detection latency using
  profiling and model changes?
details:
  realistic_solution: The first step involves utilizing on-device profiling tools
    specific to the Tensor G3 (e.g., Android GPU Inspector, Perfetto for system-wide
    tracing, or custom instrumentation within the TensorFlow Lite runtime). The goal
    is to generate detailed traces and flame graphs to visualize where time is spent.
    Pay close attention to data movement between CPU and TPU, memory access patterns,
    and the execution times of individual model layers or pre/post-processing steps.
    If data transfer is a bottleneck, consider zero-copy techniques or optimizing
    data layout. If CPU pre-processing is the issue, offload to dedicated DSPs/NPUs
    if available, or optimize using SIMD instructions. If it's TPU bound, examine
    layer-wise execution and potentially explore mixed-precision inference or further
    model optimization. Thermal throttling should also be monitored via system metrics.
  common_mistake: A common mistake is to immediately jump to optimizing the model
    architecture (e.g., pruning, quantization) or switching to a lighter model without
    thoroughly profiling the entire inference pipeline. The bottleneck might not be
    the model's raw compute on the TPU but rather data transfer (e.g., from RAM to
    TPU), I/O (e.g., reading video frames), pre-processing overhead on the CPU, or
    even system-level issues like thermal throttling impacting sustained performance.
  napkin_math: Assuming a target latency of 30 ms per frame for real-time 30 FPS video,
    the model must process frames within this budget. If a 4K frame (3840x2160 pixels)
    needs to be processed, and each pixel requires a certain number of operations,
    we can estimate theoretical TOPS needed. For example, if the model requires 100
    GMACs (Giga Multiply-Accumulate Operations) per inference, and a MAC typically
    translates to 2 operations (multiply and add), that's 200 Giga Operations. With
    the Tensor G3's 7.5 TOPS (7500 Giga Operations per second), the theoretical inference
    time would be 200 / 7500 = ~26.7 ms, which *seems* acceptable on paper. However,
    this calculation assumes 100% TPU utilization and ignores overhead. The discrepancy
    between this theoretical value and observed spikes (e.g., 100ms+) points towards
    non-compute bottlenecks like memory bandwidth. If 4K frames (e.g., 24-bit RGB)
    are 3840*2160*3 bytes = ~24.8 MB, moving this data to/from memory at 100 MB/s
    would take 24.8/100 = 248 ms, clearly exceeding the budget. LPDDR5X typically
    offers much higher bandwidth (e.g., 50-70 GB/s), so the bottleneck might be related
    to *inefficient* access patterns or contention rather than raw bandwidth, or even
    CPU-side copying before being handed to the TPU driver.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The 'Napkin Math' section is incomplete, with the last sentence cut off.
- Reasoning Competency, Knowledge Area, and Mode are missing for an L6+ question.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
