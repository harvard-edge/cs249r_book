schema_version: '1.0'
id: mobile-1564
track: mobile
level: L3
zone: implement
topic: profiling-bottleneck-analysis
competency_area: latency
bloom_level: analyze
phase: inference
title: Optimizing a Transformer Model on Snapdragon Hexagon NPU
scenario: A large language model based on a transformer architecture is deployed on a mobile device equipped with a Hexagon NPU (45 TOPS INT8, 64 GB/s LPDDR5X bandwidth). Despite quantization to INT8, inference latency is consistently higher than the target 100ms per query. You suspect a bottleneck and must pinpoint its origin within the NPU's compute, memory, or I/O subsystem using available profiling tools.
question: How would you diagnose and optimize a transformer model bottleneck on a mobile NPU?
details:
  realistic_solution: Utilize Qualcomm's Hexagon SDK profiling tools (e.g., Hexagon NN Direct Profiler, System Trace, HVX profiler) to analyze execution times of individual layers, memory access patterns, and data transfers between host CPU and NPU. Prioritize identifying if the bottleneck is compute-bound (e.g., inefficient matrix multiplications, underutilization of vector extensions like HVX), memory-bound (e.g., excessive data transfers, poor cache utilization), or I/O-bound (e.g., slow data loading from main memory). Subsequently, explore kernel optimization (e.g., custom HVX kernels), data layout changes, or model architecture adjustments (e.g., pruning, distillation) based on the identified bottleneck. For memory, investigate if LPDDR5X bandwidth is saturated during peak data transfers and optimize data locality.
  common_mistake: |
    **The Pitfall:** Focusing solely on FLOPs/MACs and overlooking memory bandwidth or NPU-specific architectural quirks.
    **The Rationale:** Engineers are used to server-side optimization where compute is the primary bottleneck, and fail to realize mobile performance is overwhelmingly dictated by data movement.
    **The Consequence:** Optimization efforts are wasted on compute kernels while the system remains heavily memory-bound, failing to reach the 100ms latency target.
  napkin_math: |
    - **Compute Bound Test:** Given 45 TOPS INT8, a 100ms target implies a budget of ~4.5 trillion operations. Exceeding this makes it compute-bound.
    - **Memory Bound Test:** With LPDDR5X bandwidth of ~64 GB/s, a 100ms budget allows moving at most 6.4 GB of data.
    - **Analysis:** Moving 10GB of intermediate activations and weights back and forth multiple times (e.g., 20GB total movement) takes 20GB / 64GB/s = 312.5ms.
    - **Result:** **If data movement exceeds 6.4 GB per inference, the model is strictly memory-bound and will easily miss the 100ms budget.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
