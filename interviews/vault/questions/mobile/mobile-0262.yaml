schema_version: '1.0'
id: mobile-0262
track: mobile
level: L2
zone: recall
topic: model-serving-infrastructure
competency_area: deployment
bloom_level: understand
phase: inference
title: The OTA Update Budget (mobile-0262)
scenario: You are an ML systems engineer on a mobile team. You need to ship a new on-device model with 50 million parameters via an Over-the-Air (OTA) update. To manage user data consumption, your product manager has set a strict 100 MB budget for the model download size.
question: 'How much storage will the model require if deployed using FP16 precision, and does it meet the 100 MB budget?'
details:
  realistic_solution: Using FP16 (half-precision floating-point) means each parameter requires 16 bits, or 2 bytes, of storage. With 50 million parameters, the total size is 100 million bytes. Since 1 megabyte (MB) is 1,000,000 bytes, the model size is exactly 100 MB. This meets the product manager's budget, but leaves no room for overhead like metadata or container formats.
  common_mistake: |
    **The Pitfall:** Confusing bits with bytes or incorrectly recalling the byte requirements for FP16 precision.
    **The Rationale:** Engineers misremember if FP16 uses 1, 2, or 4 bytes, or forget to divide the 16 bits by 8.
    **The Consequence:** Calculating an off-by-2x or off-by-4x error, leading to fundamentally flawed OTA budget estimations.
  napkin_math: |
    **Assumptions & Constraints:**
    - Parameter Count: 50,000,000.
    - Precision: FP16.
    - Target Budget: 100 MB.

    **Calculations:**
    - Bytes per Parameter: 16 bits / 8 bits/byte = 2 bytes.
    - Total Size (Bytes): 50,000,000 params * 2 bytes = 100,000,000 bytes.
    - Total Size (MB): 100,000,000 bytes / 1,000,000 bytes/MB = 100 MB.

    **Conclusion & Interpretation:**
    - **Result: 100 MB (Storage-Bound)**. The payload exacts meets the budget, leaving zero room for packaging overhead.
  options:
  - '50 MB. It is well under budget.'
  - '200 MB. It is over budget.'
  - '100 MB. It exactly meets the budget.'
  - '800 MB. It is far too large.'
  correct_index: 2
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 2
deletion_reason: Duplicate of mobile-ota-update-size-the-ota-update-budget-3 (same title + track)
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
