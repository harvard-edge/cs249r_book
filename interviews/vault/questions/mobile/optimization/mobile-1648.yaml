schema_version: '1.0'
id: mobile-1648
track: mobile
level: L3
zone: analyze
topic: operator-scheduling
competency_area: optimization
bloom_level: analyze
phase: inference
title: Transformer Decode Block Scheduling on Hexagon NPU
scenario: You are optimizing a transformer decode block on a Snapdragon-class Hexagon NPU. The block runs RMSNorm, QKV projections, attention with KV-cache reads, output projection, MLP matmuls, GELU/SwiGLU, residual adds, and requantization; the matmuls are compute-heavy while norms, residuals, and cache reads are memory-heavy.
question: How would you schedule an LLM block on the NPU to reduce latency, reuse memory, exploit parallelism, and apply layer fusion to meet a 100ms latency target?
details:
  realistic_solution: |
    Represent the decode block as a DAG and schedule it around the longest live tensors: hidden state, QKV projections, attention output, MLP intermediate, and KV-cache slices. Fuse RMSNorm with the following linear input preparation where supported, fuse residual/add/requantization epilogues into matmul kernels, and use a FlashAttention-style or tiled attention kernel so QK, softmax, and AV do not materialize large intermediate tensors. Reuse a preallocated activation arena across the attention and MLP phases, keep KV-cache blocks contiguous or paged for predictable gathers, and avoid CPU/GPU fallback operators in the hot path. Use Qualcomm AI Engine Direct or Snapdragon Profiler to compare per-operator latency, bytes moved, and NPU occupancy, then revise tile sizes, fusion choices, and parallel head/MLP scheduling until the block fits the 100 ms budget.
  common_mistake: |
    **The Pitfall:** Prioritizing compute utilization without adequately considering memory access patterns and bandwidth limitations.
    **The Rationale:** Candidates often focus purely on parallelizing matrix multiplications and maximizing TOPS utilization.
    **The Consequence:** Naive scheduling leads to excessive data movement between DRAM and NPU caches, significantly bottlenecking performance even if the NPU's raw compute is underutilized.
  napkin_math: |
    **Assumptions & Constraints:**
    - Latency budget: 100 ms on a 45 TOPS NPU.
    - Example compiled block requirement: 100 GOP of INT8-equivalent work per token/block.
    - LPDDR5X bandwidth: ~50 GB/s.
    - Example unfused block traffic: 1 GB of total reads/writes across weights, activations, and KV-cache slices.

    **Calculations:**
    - Compute capacity in 100 ms: 45e12 ops/s * 0.1 s = 4.5e12 ops = 4.5 TOP.
    - Theoretical compute time for the example block: 100e9 ops / 45e12 ops/s = 0.0022 s = 2.2 ms.
    - Ideal transfer time for 1 GB traffic: 1 GB / 50 GB/s = 0.020 s = 20 ms.
    - Combined lower bound before scheduling overhead is about 22.2 ms, leaving roughly 77.8 ms for launch, synchronization, and non-ideal bandwidth effects.

    **Conclusion & Interpretation:**
    - **Result: The example block is more sensitive to bytes moved than raw TOPS.** Fusion and memory reuse should target the 1 GB traffic term because 20 ms of ideal transfer is much larger than the 2.2 ms compute floor.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
