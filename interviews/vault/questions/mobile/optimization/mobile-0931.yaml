schema_version: '1.0'
id: mobile-0931
track: mobile
level: L4
zone: implement
topic: kernel-fusion
competency_area: optimization
bloom_level: apply
phase: inference
title: Kernel Fusion Implement Fused Attention for On-Device LLM
scenario: Implement Flash Attention for a 1B LLM on a mobile NPU. The model has 16 heads, head dim 64, and must handle sequences up to 2048 tokens.
question: How would you specify tile sizes for flash attention given available SRAM, and compute memory savings vs standard attention?
details:
  realistic_solution: 'Q,K,V activation size = seq × hidden × dtype_bytes. For seq=2048, hidden=1024, fp16: 2048 × 1024 × 2B = 4.19MB each.'
  common_mistake: |
    **The Pitfall:** Using generic tile sizes that spill to LPDDR5 or fail to maximize arithmetic intensity.
    **The Rationale:** Candidates do not verify the specific SRAM capacity of the hardware, or they fail to account for the unique tiling strategy required by Grouped-Query Attention (GQA).
    **The Consequence:** The model defaults to costly off-chip DRAM accesses, destroying the performance benefits of Flash Attention.
  napkin_math: |
    1. **Assumptions & Constraints:** Sequence=2048, heads=16, head_dim=64, FP16 precision (2 bytes per element).
    2. **Calculations:** MHA Q, K, V size = seq × heads × head_dim × 2 bytes = 2048 × 16 × 64 × 2 = 4,194,304 bytes.
    3. **Conclusion & Interpretation:** **Result: Memory Capacity-Bound.** Each Q, K, V tensor is exactly ~4.19 MB, requiring tile sizes carefully tuned to fit within local SRAM block without spilling to DRAM.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
