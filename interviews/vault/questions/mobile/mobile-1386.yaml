schema_version: '1.0'
id: mobile-1386
track: mobile
level: L6+
zone: mastery
topic: latency-decomposition
competency_area: latency
bloom_level: create
phase: inference
title: Real-time Multimodal Assistant Latency on Hexagon NPU
scenario: "You are architecting an on-device real-time visual assistant running entirely on a smartphone powered by the Qualcomm Hexagon NPU. The application streams 30fps camera frames and user audio, processes them through a local 7B parameter multimodal LLM, and outputs synthesized speech. The system must guarantee a Time-to-First-Token (TTFT) under 600ms and a Time-Per-Output-Token (TPOT) under 80ms."
question: "How would you schedule a real-time multimodal assistant on Hexagon NPU while avoiding shared memory bottlenecks?"
details:
  realistic_solution: |
    Strict subsystem isolation is required to mitigate LPDDR5X bus contention. Offload all audio/video pre-processing to the DSP and ISP. Quantize the 7B LLM to W4A8 (INT4 weights, INT8 activations) to shrink memory bandwidth requirements during the autoregressive decode phase. Chunk the prompt prefill phase to prevent the high-throughput NPU compute from starving the ISP's continuous DMA streams.
  common_mistake: |
    **The Pitfall:** Treating the NPU as a standalone black box.
    **The Rationale:** Candidates ignore memory bandwidth contention on the shared LPDDR5X bus from the camera ISP and DSP audio processing.
    **The Consequence:** Severe degradation of TPOT in real-world scenarios, leading to audio stutter and frame drops.
  napkin_math: |
    **Assumptions & Constraints:**
    - Peak LPDDR5X bandwidth: 68 GB/s.
    - ISP/DSP/OS background usage: 20 GB/s. Available for NPU: 48 GB/s.
    - 7B Model (INT4): ~3.5 GB footprint.
    - NPU Effective Compute: 18 TOPS (40% utilization of 45 TOPS).
    - Prefill compute (500 tokens): 500 * 7B * 2 = 7 TFLOPs.

    **Calculations:**
    - TPOT Memory Time: 3.5 GB / 48 GB/s = 73ms (Under 80ms target).
    - Prefill Time: 7 TFLOPs / 18 TOPS = 388ms.
    - Total TTFT: 388ms (Prefill) + 50ms (Pre) + 50ms (Post) = 488ms.

    **Conclusion & Interpretation:**
    - **Result: 73ms TPOT, 488ms TTFT (Memory-Bandwidth Bottleneck)**. W4A8 quantization allows the system to satisfy strict latency deadlines, overcoming the severe memory-bandwidth bottleneck.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
