schema_version: '1.0'
id: mobile-2131
track: mobile
level: L4
zone: design
topic: collective-communication
competency_area: networking
bloom_level: analyze
phase: both
title: Automotive Ethernet AllGather
scenario: A vehicle cluster with 3 Snapdragon 8 Gen 3 modules shares 8MB bounding box tensors via an automotive Gigabit Ethernet switch (1000 Mbps).
question: Analyze the ring AllGather latency for an 8MB tensor per node, accounting for a 50us switch processing delay per hop and a 1ms synchronization barrier.
details:
  realistic_solution: In AllGather, each node receives (N-1) chunks. Each node must transmit its 8MB to the others, meaning 2 chunks of 8MB (16MB) are received per node. At 1000 Mbps, 128Mb takes 128ms. The switch delays and 1ms barrier add negligible overhead compared to the payload.
  common_mistake: |
    **The Pitfall:** Confusing the AllGather payload scaling factor with AllReduce.
    **The Rationale:** Engineers mistakenly use the AllReduce factor of $2*(N-1)/N$ instead of the AllGather factor of $(N-1)*M$.
    **The Consequence:** This leads to incorrect bandwidth bounds and underestimating the network latency for the aggregation step.
  napkin_math: |
    - **Data Received per Node:** $(N-1) * M = (3-1) * 8\text{MB} = 16\text{MB}$.
    - **Conversion to Bits:** 16MB = 128 Megabits (Mb).
    - **Transmission Time:** 128 Mb / 1000 Mbps = 128 ms.
    - **Switch Delay:** 2 hops * 50 us = 100 us = 0.1 ms.
    - **Total Latency:** 128 ms + 0.1 ms + 1.0 ms (barrier) = **129.1 ms**.
status: published
provenance: llm-draft
expected_time_minutes: 8
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-design-L4
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
