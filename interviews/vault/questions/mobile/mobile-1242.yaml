schema_version: '1.0'
id: mobile-1242
track: mobile
level: L5
zone: design
topic: memory-pressure-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: Architecting OOM-Resilient On-Device LLM Inference
scenario: You are designing an on-device LLM assistant for a flagship smartphone. The OS strictly enforces a dynamic 4 GB memory budget for background AI tasks to prevent eviction of user-facing apps.
question: "How should an LLM assistant manage a growing KV cache under a dynamic 4 GB AI memory budget without triggering an OOM kill?"
details:
  realistic_solution: A robust design uses a paged KV cache system (PagedAttention) to eliminate memory fragmentation and allow dynamic, non-contiguous allocation of context blocks. The architecture should tightly integrate with OS memory pressure callbacks to preemptively evict older KV blocks or dynamically quantize the KV cache to INT8 on the fly.
  common_mistake: |
    **The Pitfall:** Proposing static KV cache allocation and ignoring the shared nature of mobile memory.
    **The Rationale:** Candidates assume the AI budget is a hard, isolated partition, failing to account for dynamic OS eviction when foreground apps spike in memory usage.
    **The Consequence:** The LLM either wastes available capacity or crashes due to fragmentation and OS-level memory pressure.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - AI Budget = 4.0 GB.
       - Model weights (3B INT4) = 1.5 GB.
       - Token KV size = 3072 (dim) * 32 (layers) * 2 (K&V) * 2 (bytes) = 393 KB.
    2. **Calculations:**
       - Available for KV + Activations = 4.0 - 1.5 = 2.5 GB.
       - Max theoretical context = 2.5 GB / 393 KB = ~6,360 tokens.
       - Standard contiguous allocation fragmentation loss = 20-30% (~750 MB wasted).
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bound**. Paged memory is required to eliminate the 750 MB fragmentation waste and dynamically adjust to OS memory pressure signals.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
