schema_version: '1.0'
id: mobile-1310
track: mobile
level: L3
zone: fluency
topic: dataset-curation
competency_area: data
bloom_level: apply
phase: training
title: On-Device Active Learning Compute for Curation
scenario: You are designing an on-device active learning pipeline to curate training
  data on a smartphone powered by the Google Tensor G3. To preserve privacy, you use
  the device's 12 GB LPDDR5X RAM and 7.5 TOPS TPU to run a 1.5-billion-parameter on-device
  LLM (Gemini Nano) that calculates uncertainty scores for user messages, selecting
  the top percentile to upload for server-side human annotation. If a user receives
  50,000 messages per day, with an average length of 50 tokens each, calculate the
  total daily compute time in seconds required on the TPU to score and curate this
  dataset.
question: How much daily Tensor G3 compute time is needed to score 50,000 messages
  for on-device active learning?
details:
  realistic_solution: The correct approach calculates the total computational operations
    for the dataset and divides it by the TPU's compute throughput. Given the task
    runs offline in the background, 1,000 seconds (approx 16.6 minutes) of compute
    distributed over 24 hours is a highly practical workflow for on-device data selection
    without degrading user experience.
  common_mistake: Candidates often fail to recall the standard transformer forward
    pass arithmetic (2 * parameters * tokens) or erroneously throttle their calculation
    based on the 12 GB LPDDR5X memory capacity rather than the 7.5 TOPS compute throughput
    limit for active learning batch processing.
  napkin_math: 1. Total tokens per day = 50,000 messages * 50 tokens/message = 2,500,000
    tokens. 2. Compute per token for a forward pass = 2 * 1.5 billion parameters =
    3 billion operations (3 GigaOPs). 3. Total daily compute = 2,500,000 tokens *
    3 GigaOPs = 7.5 * 10^15 operations = 7,500 TeraOPs. 4. Compute time = 7,500 TeraOPs
    / 7.5 TOPS (from the Google Tensor G3 TPU) = 1,000 seconds.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
