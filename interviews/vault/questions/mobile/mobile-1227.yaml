schema_version: '1.0'
id: mobile-1227
track: mobile
level: L5
zone: design
topic: data-efficiency-selection
competency_area: data
bloom_level: evaluate
phase: training
title: On-Device Coreset Selection Architecture for Apple A17 Pro
scenario: You are designing an on-device data pruning and coreset selection pipeline for personalized language model fine-tuning on the Apple A17 Pro Neural Engine. The system continuously collects user text data, but due to the shared 8 GB unified memory and the need to keep power consumption under a ~5W thermal envelope during overnight charging, you cannot store or train on all collected data.
question: "How would you architect a system that evaluates the Information-Compute Ratio (ICR) of incoming data and selects a high-value coreset for on-device training without starving OS resources?"
details:
  realistic_solution: The system should use a lightweight, quantized proxy model running efficiently on the 16-core Neural Engine to compute perplexity scores as a proxy for information gain. High-scoring, rare user interactions are buffered into a prioritized coreset. To prevent model collapse and maximize data efficiency, the architecture includes an overnight batch process that evaluates the coreset using the ICR framework, generating synthetic variations of the highest-value local intents using the base model while strictly bounding the working memory.
  common_mistake: |
    **The Pitfall:** Proposing complex neural-based data valuation methods, like using the full LLM to compute gradients for every sample.
    **The Rationale:** Candidates rely on standard server-side active learning literature without translating the constraints to an edge device.
    **The Consequence:** The continuous background grading exceeds both the 8GB shared memory limit and the ~5W thermal budget, starving the OS and crashing the device.
  napkin_math: |
    - **Memory Budget:** Total 8 GB - OS (3 GB) - 8-bit 3B model (3 GB) = **2 GB remaining** for activations and data.
    - **Coreset Size:** 10,000 samples * 128 seq length * 2 bytes = **2.56 MB** (easily fits).
    - **Compute Required:** Scoring 100,000 daily interactions at 10 MegaOPS/sequence = 1 TeraOP total.
    - **Execution Time:** On the 35 TOPS Neural Engine, 1 TeraOP takes roughly **0.03 seconds**, keeping background power usage well under the 5W budget.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
