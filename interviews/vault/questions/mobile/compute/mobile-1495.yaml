schema_version: '1.0'
id: mobile-1495
track: mobile
level: L4
zone: specification
topic: compute-cost-estimation
competency_area: compute
bloom_level: analyze
phase: inference
title: On-Device Inference Cost on Snapdragon 8 Gen 3
scenario: You are tasked with deploying a 1 billion parameter generative model on a premium smartphone. The NPU is capable of 45 TOPS in INT8, utilizing shared LPDDR5X memory.
question: How would you estimate on-device inference cost on Snapdragon 8 Gen 3 from compute and memory limits to meet a 20 tokens/sec requirement?
details:
  realistic_solution: |
    Estimate both lower bounds: compute work per generated token and the time to stream the INT8 weights from shared LPDDR5X. For a 1B-parameter INT8 model, the weight stream is about 1 GB/token, so 64 GB/s memory bandwidth implies roughly 15.6 ms/token, or about 64 tokens/sec before runtime overhead. Since that exceeds the 20 tokens/sec target, the design is plausible, but the release spec should still reserve headroom for KV-cache traffic, sampling, and thermal throttling.
  common_mistake: |
    **The Pitfall:** Calculating latency strictly using NPU TOPS.
    **The Rationale:** Candidates blindly divide operations by 45 TOPS, failing to realize that autoregressive token generation is bottlenecked by the speed at which weights can stream from LPDDR5X.
    **The Consequence:** Promising theoretical frame rates that are physically impossible due to memory bandwidth starvation.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model: 1B params (INT8 = 1 GB).
    - Target: 20 tok/sec.
    - Bandwidth: ~64 GB/s LPDDR5X.
    - NPU: 45 TOPS INT8 peak.
    - Approximate dense decode work: one multiply-add per parameter per token, counted as ~2 integer operations.

    **Calculations:**
    - Memory Time = 1 GB / 64 GB/s = 15.6 ms.
    - Bandwidth-limited rate = 1 / 0.0156s ≈ 64 tok/sec.
    - Compute work ≈ 1B params * 2 ops = 2 GOP/token.
    - Compute lower bound = 2 GOP / 45,000 GOP/s ≈ 0.044 ms/token.
    - The larger lower bound is the memory stream time.

    **Conclusion & Interpretation:**
    - **Result: Bandwidth-Bound but target-feasible**. Weight streaming alone supports ~64 tok/sec, leaving about 3x headroom over the 20 tok/sec target before overheads.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
