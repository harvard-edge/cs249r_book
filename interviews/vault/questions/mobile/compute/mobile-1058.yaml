schema_version: '1.0'
id: mobile-1058
track: mobile
level: L5
zone: optimization
topic: transformer-systems-cost
competency_area: compute
bloom_level: evaluate
phase: inference
title: 'Mobile Transformer Cost Optimization: On-Device vs Streaming Hybrid'
scenario: A mobile LLM app uses Tensor G3 for on-device inference. A Phi-3-Mini INT4 model generates a constant P99 latency of 2.79s for 100 tokens. The strict product SLO is <3s. Alternatively, when network RTT is <100ms, a cloud H100 API ($0.001/1K tokens) generates a P99 latency of 1.5s.
question: How would you design an optimal hybrid policy that minimizes cost while maintaining P99<3s across network conditions, and what is the cost per 1000 daily queries?
details:
  realistic_solution: 'Deploy a predictive thresholding architecture. Probe the network. If RTT is <150ms, route execution to the cloud cluster for lower latency. If the network degrades, immediately fall back to the 2.79s on-device model. By routing to the cloud 70% of the time, the app guarantees a composite P99 latency bounded by the local model (2.79s) while shaving API costs.'
  common_mistake: |
    **The Pitfall:** Blindly routing to the cloud 100% of the time purely for its 1.5s latency profile.
    **The Rationale:** Engineering teams rigidly prioritizing speed without computing the accumulating fiscal cost or acknowledging the tail-latency disaster of 30% network failure rates.
    **The Consequence:** The deployment violates P99 SLA guarantees during bad network conditions while simultaneously maximizing cloud API burn rates.
  napkin_math: |
    **Assumptions & Constraints:**
    - Target P99 SLA: < 3.0s. Output Payload: 100 tokens per query.
    - Cloud: P99 = 1.5s, Cost = $0.0001 per query.
    - Local Model: P99 = 2.79s (Constant), Cost = $0.00 per query.
    - Network distribution: 70% good state, 30% poor state.

    **Calculations:**
    - Fallback strategy: Cloud (if Good) else Local.
    - Blended P99 latency is mathematically bounded by the maximum of both active paths: max(1.5s, 2.79s) = 2.79s.
    - Blended Cost per query: (0.70 * $0.0001) + (0.30 * $0.00) = $0.00007.
    - Daily Run Rate (1000 queries): $0.00007 * 1000 = $0.07/day.

    **Conclusion & Interpretation:**
    - **Result: $0.07/day (System-Bound)**. The predictive thresholding framework ensures a strict 2.79s P99 latency while permanently slashing daily API operational expenditures by 30%.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
