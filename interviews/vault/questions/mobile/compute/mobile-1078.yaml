schema_version: '1.0'
id: mobile-1078
track: mobile
level: L3
zone: analyze
topic: accelerator-comparison
competency_area: compute
bloom_level: analyze
phase: inference
title: Tensor G3 CPU vs TPU Decoding Bottlenecks
scenario: You are migrating the Gemini Nano model (3 billion parameters, 8-bit quantization) from the CPU to the on-device TPU (7.5 TOPS, 12 GB LPDDR5X) on a Google Tensor G3. Moving to the TPU accelerated the prompt prefill phase, but autoregressive token generation speed only improved marginally.
question: Why does the highly efficient TPU not provide a proportional speedup for decoding, and what is its theoretical compute limit?
details:
  realistic_solution: |
    Autoregressive decoding is a memory-bandwidth bound matrix-vector operation. For a 3B 8-bit model, a decode step needs roughly 6 GOPs of compute and streams about 3 GB of weights from shared memory. The 7.5 TOPS TPU has a theoretical compute limit of about 7,500 GOP/s / 6 GOP/token = 1,250 tokens/sec, so compute is not the limiting resource. Actual generation is far lower because the TPU spends most of the decode step waiting on LPDDR5X weight reads and KV-cache traffic.
  common_mistake: |
    **The Pitfall:** Assuming a custom ASIC with higher TOPS will uniformly accelerate all phases of execution.
    **The Rationale:** Candidates fail to distinguish between the compute-bound prefill phase and the memory-bandwidth bound decoding phase.
    **The Consequence:** They mistakenly attribute the lack of speedup to driver overhead or thermal throttling.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model memory footprint: 3B parameters * 1 byte (8-bit) = 3 GB.
    - Compute required per token: 3B params * 2 ops (MAC) = 6 Giga-operations (GOPs).

    **Calculations:**
    - TPU theoretical compute limit: 7.5 TOPS (7500 GOPs) / 6 GOPs per token = 1250 tokens/sec.
    - Memory traffic per token: about 3 GB of weights, plus KV-cache reads.
    - If effective shared-memory bandwidth during decode is 30-60 GB/s, the bandwidth limit is roughly 10-20 tokens/sec before other runtime overhead.

    **Conclusion & Interpretation:**
    - **Result: 1250 tok/s theoretical compute limit, ~10-20 tok/s memory-bound practical limit**. The large gap is why the TPU accelerates compute-heavy prefill but does not provide a proportional speedup for single-token decoding.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
