schema_version: '1.0'
id: mobile-2144
track: mobile
level: L6+
zone: specification
topic: kv-cache-management
competency_area: architecture
bloom_level: create
phase: both
title: 5G Remote KV Prefetching
scenario: A transformer's KV cache is split between local LPDDR5 and an edge server's NVMe SSD over a 5G mmWave connection (1 Gbps, 15ms ping).
question: Propose a speculative prefetching mechanism for the remote KV blocks that hides the 15ms 5G synchronization latency.
details:
  realistic_solution: The 15ms ping represents a hard stall if fetched reactively. At an assumed 50ms/token generation speed, the local NPU must predict and issue the 5G fetch request at least 1 token in advance. Fetching a 1MB chunk takes 8ms plus the 15ms sync, totally 23ms, safely hiding behind the 50ms compute.
  common_mistake: |
    **The Pitfall:** Using a purely reactive paging strategy.
    **The Rationale:** Candidates often design systems that wait for a cache miss before requesting the remote block, ignoring network latency.
    **The Consequence:** A reactive fetch blocks the local NPU generation loop for at least 15ms on every remote cache miss, destroying token generation throughput.
  napkin_math: |
    1. Network Ping Sync = 15ms.
    2. Bandwidth: 1 Gbps = 125 MB/s.
    3. Fetch time for a 1MB chunk = 1MB / 125MB/s = 8ms.
    4. Total fetch time = 15ms (ping) + 8ms (transfer) = 23ms.
    5. Conclusion: **Since the 23ms total fetch time is less than a typical 50ms token generation time, fetching 1 token in advance perfectly hides the network latency.**
status: published
provenance: llm-draft
expected_time_minutes: 14
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-specification-L6+
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
