schema_version: '1.0'
id: mobile-1998
track: mobile
level: L4
zone: design
topic: queueing-theory
competency_area: latency
bloom_level: create
phase: both
title: Mobile Neural Processing Unit Adaptive Batching Queues
scenario: A mobile live-translation app queues incoming audio chunks of 200ms to the NPU for feature extraction.
question: Model the processing pipeline as an M/D/1 queue and formulate an adaptive batching strategy to keep 99th percentile latency under 500ms when user speech rate unexpectedly doubles?
details:
  realistic_solution: |
    Model doubled speech as 10 chunks/sec because each chunk covers 200 ms. Use an adaptive batcher with two triggers: dispatch immediately when 4 chunks are queued, or dispatch earlier when the oldest chunk has waited about 300 ms. At the burst rate, filling a 4-chunk batch takes at most 3 interarrival gaps, or 300 ms, and the batch service time is 40 ms. The resulting batch arrival rate is 10 / 4 = 2.5 batches/sec with utilization 2.5 * 40 ms = 0.10, so queueing delay remains small. This keeps the oldest chunk's P99 latency below 500 ms while avoiding large batches during normal low-load speech.
  common_mistake: |
    **The Pitfall:** Assuming exponential service times (M/M/1 queue model).
    **The Rationale:** Candidates default to generic queuing theory without realizing that fixed-size audio chunks passing through an NPU yield highly deterministic execution times.
    **The Consequence:** Grossly inaccurate latency modeling and deploying overly conservative adaptive batching logic that harms baseline responsiveness.
  napkin_math: |
    **Assumptions & Constraints:**
    - Input chunk size: 200 ms, so normal speech produces about 5 chunks/sec.
    - Doubled speech rate: 10 chunks/sec.
    - Target P99 latency: < 500 ms.
    - Batch size limit: 4 chunks.
    - Batch processing time: 40 ms total.
    - Adaptive timeout for oldest chunk: 300 ms.

    **Calculations:**
    - Full-batch service capacity = 4 chunks / 0.04 sec = 100 chunks/sec.
    - At doubled speech rate, utilization = 10 / 100 = 0.10.
    - Batch arrival rate at size 4 = 10 / 4 = 2.5 batches/sec.
    - M/D/1 mean queue wait for batches: Wq = λ_b * S^2 / (2 * (1 - ρ)) = 2.5 * 0.04^2 / (2 * 0.90) = 0.0022 sec.
    - Conservative oldest-chunk latency budget = batching wait 300 ms + service 40 ms + queueing/jitter allowance about 50 ms = about 390 ms.

    **Conclusion & Interpretation:**
    - **Result: adaptive batch-4 with a 300 ms oldest-chunk timeout (Queue-Bound)**. At the doubled 10 chunks/sec arrival rate, utilization is only 0.10 and the latency budget stays below the 500 ms P99 target with margin.
status: published
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 15
validated: true
validation_status: passed
validation_date: '2026-04-25'
validation_model: pydantic-1.0
math_verified: true
math_status: CORRECT
math_date: '2026-04-25'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
  - gemini-generated
  - target-analyze-L6+
created_at: '2026-04-25'
