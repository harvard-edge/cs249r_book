schema_version: '1.0'
id: mobile-1072
track: mobile
level: L4
zone: analyze
topic: kv-cache-management
competency_area: memory
bloom_level: analyze
phase: inference
title: On-Device KV-Cache Budget for Mobile LLM
scenario: You are deploying a 3B-parameter LLM (32 layers, 4 GQA KV heads, head_dim=64) on an A17 Pro. The OS and other apps consume 4 GB, leaving 4 GB for the model. After loading INT4 weights (1.5 GB) and runtime overhead (0.5 GB), only 2 GB remains for KV-cache.
question: What KV-cache context length fits a 3B INT4 LLM on A17 Pro when only 2GB remains for KV storage?
details:
  realistic_solution: 'KV per token per layer: 2 (K+V) x 4 (KV heads) x 64 (head_dim) x 2B (FP16) = 1,024 bytes = 1 KB. Per token across 32 layers: 1 KB x 32 = 32 KB. Maximum context: 2 GB / 32 KB = 65,536 tokens. This is generous — the model supports ~64K context from a memory perspective. However, practical constraints: (1) Memory pressure from OS can reclaim memory at any time — reserve 25% buffer -> effective budget 1.5 GB -> 48K tokens. (2) Memory bandwidth for attention scales O(n) per token. (3) Practical sweet spot: 4K-8K context (128-256 MB KV-cache).'
  common_mistake: |
    **The Pitfall:** Computing KV-cache size using server-scale dimensions.
    **The Rationale:** Engineers mistakenly use standard cloud configurations (e.g., many more KV heads and larger head dimensions) instead of mobile-optimized GQA configurations.
    **The Consequence:** This leads to heavily underestimating the possible context length on mobile hardware.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - KV-Cache Budget: 2 GB.
       - Model: 32 layers, 4 GQA KV heads, head_dim=64.
       - Precision: FP16 (2 bytes).
    2. **Calculations:**
       - KV per token per layer: 2 (K+V) x 4 (KV heads) x 64 (head_dim) x 2 bytes = 1,024 bytes = 1 KB.
       - Total KV per token: 1 KB x 32 layers = 32 KB.
       - Max theoretical context: 2 GB / 32 KB = 65,536 tokens (64K).
       - Practical context: Reserving a 25% OS memory buffer leaves 1.5 GB -> 48K tokens.
    3. **Conclusion & Interpretation:**
       - **Result: 4K-8K Practical Context** (Memory-Bandwidth-Bound).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
