schema_version: '1.0'
id: mobile-1747
track: mobile
level: L5
zone: specification
topic: differential-privacy
competency_area: cross-cutting
bloom_level: analyze
phase: inference
title: On-Device DP-SGD for Federated Learning on Snapdragon 8 Gen 3 Hexagon NPU
scenario: You are designing an on-device federated learning system using DP-SGD. The system requires an overall privacy budget epsilon of 2 for each training round over 100 communication rounds using advanced composition.
question: How would you design the noise calibration mechanism and manage the privacy-utility tradeoff given NPU constraints?
details:
  realistic_solution: Implement an optimized DP-SGD pipeline fine-tuning the gradient clipping threshold and using efficient noise generation.
  common_mistake: |
    **The Pitfall:** Ignoring the compute overhead of noise generation and clipping.
    **The Rationale:** Candidates focus entirely on theoretical epsilon budgets without realizing that generating high-quality cryptographic noise securely on a constrained NPU is incredibly expensive.
    **The Consequence:** The DP-SGD implementation crushes the device's battery and fails to meet real-time background processing windows.
  napkin_math: |
    1. Calculate per-round epsilon ($\epsilon_s$): Using advanced composition, $\epsilon_s \approx \epsilon / \sqrt{T} = 2 / 10 =$ **0.2**.
    2. Calculate noise standard deviation ($\sigma$): Given sensitivity $S=1.0$ and $\delta=10^{-5}$, $\sigma \approx S \cdot \sqrt{2 \cdot \ln(1.25/\delta)} / \epsilon_s$.
    3. Compute $\sigma$: $\approx 1.0 \cdot \sqrt{2 \cdot 11.7} / 0.2 \approx 4.8 / 0.2 \approx$ **24**.
    4. Evaluate compute load: The NPU easily handles gradients, but the massive $\sigma=24$ noise generation requires specialized INT8 random number generation.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed