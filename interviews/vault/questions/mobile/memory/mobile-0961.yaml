schema_version: '1.0'
id: mobile-0961
track: mobile
level: L4
zone: specification
topic: kv-cache-management
competency_area: memory
bloom_level: create
phase: inference
title: 'KV-Cache: Specification — KV Cache Budget for Multi-Turn Code Completion'
scenario: Specify the KV cache budget for a code completion assistant using a 3B LLM. Requirements include a 16K-token codebase context, real-time completion (< 1s to first token), and < 50ms per subsequent token.
question: How would you specify the KV cache budget, quantization, layout, and context management for this code completion assistant?
details:
  realistic_solution: 'To meet the <50ms TPOT SLA at 77 GB/s bandwidth, total memory reads must be <3.85GB. With 1.5GB INT4 weights, the KV cache must be <2.35GB. A standard FP16 MHA KV cache for 16K tokens is 7.5GB, which would cause 116ms TPOT. Specify GQA (e.g., 8 KV heads) and INT8 quantization, reducing KV size to 0.94GB and TPOT to ~32ms. For the <1s TTFT SLA: computing 16K tokens requires ~160 TFLOPs, taking 3.5s on a 45 TOPS NPU. Therefore, specify persistent prompt caching (background prefill) for the codebase so only new user tokens are prefilled.'
  common_mistake: |
    **The Pitfall:** Focusing only on total memory capacity and ignoring bandwidth limits.
    **The Rationale:** Engineers assume that if a KV cache fits in RAM, it will perform well.
    **The Consequence:** Reading a massive FP16 KV cache for every generated token severely violates the decode latency SLA due to memory bandwidth bottlenecks.
  napkin_math: |
    **Assumptions & Constraints:**
    - Decode SLA limit: 50ms. Bandwidth: 77 GB/s.
    - INT4 3B model weights: 1.5 GB.
    - 45 TOPS NPU.

    **Calculations:**
    - Read budget per token: 50ms * 77 GB/s = 3.85 GB.
    - Max KV allowed: 3.85 GB - 1.5 GB = 2.35 GB.
    - FP16 MHA KV (16K tokens): 7.5 GB -> TPOT = 9.0 GB / 77 GB/s = 116ms (Fails).
    - INT8 GQA KV (8 heads, 16K tokens): 0.94 GB -> TPOT = 2.44 GB / 77 GB/s = 31.7ms (Passes).

    **Conclusion & Interpretation:**
    - **Result: INT8 GQA meets the 50ms SLA, but prefill takes 3.5s, requiring persistent prompt caching (Bandwidth-Bound).**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
