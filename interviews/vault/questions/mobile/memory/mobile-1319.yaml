schema_version: '1.0'
id: mobile-1319
track: mobile
level: L3
zone: fluency
topic: kv-cache-management
competency_area: memory
bloom_level: apply
phase: inference
title: KV-Cache Sizing for A17 Pro
scenario: You are deploying a 7B parameter LLM quantized to 4-bit on an iPhone 15 Pro. The OS reserves 2.5 GB and the model weights consume 3.5 GB.
question: How much FP16 KV-cache memory does a 4096-token 7B LLM need (32 layers, 32 heads, 128 head dimension), and does it fit in the remaining RAM?
details:
  realistic_solution: The KV-cache calculation yields exactly 2 GB, which consumes all remaining unified memory and leaves zero room for intermediate activations or peak memory spikes. A realistic solution involves reducing the KV-cache footprint by implementing Grouped-Query Attention (GQA), applying 8-bit KV-cache quantization, or restricting the maximum context window.
  common_mistake: |
    **The Pitfall:** Forgetting the Key/Value multiplier or ignoring the OS baseline.
    **The Rationale:** Candidates often forget to multiply by 2 for both Key and Value tensors, or fail to subtract the OS footprint from the shared unified memory pool.
    **The Consequence:** The system immediately OOM crashes upon deployment because the cache exceeds the actual usable RAM limits.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 32 layers, 32 heads, 128 head dimension.
       - KV Cache format: FP16 (2 bytes per element).
       - Total Unified Memory: 8 GB. OS: 2.5 GB, Weights: 3.5 GB.
    2. **Calculations:**
       - Bytes Per Token: 2 (Key + Value) * 32 (heads) * 128 (dim) * 2 bytes (FP16) = 16,384 bytes.
       - Total KV Size: 16,384 bytes * 32 (layers) * 4096 (tokens) = 2,147,483,648 bytes (2 GB).
       - Remaining Memory: 8 GB - 2.5 GB - 3.5 GB = 2.0 GB.
    3. **Conclusion & Interpretation:**
       - **Result: 2 GB KV-cache required. The cache perfectly consumes all remaining RAM, leaving zero overhead for intermediate activations.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
