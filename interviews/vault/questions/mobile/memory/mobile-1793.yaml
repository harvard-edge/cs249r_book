schema_version: '1.0'
id: mobile-1793
track: mobile
level: L4
zone: optimization
topic: attention-scaling
competency_area: memory
bloom_level: apply
phase: inference
title: Grouped Query Attention Memory Impact for Long Context on Snapdragon 8 Gen 3
scenario: A mobile LLM (Mistral-7B-Instruct) with GQA (32 Q heads, 8 KV heads, head_dim=128) runs on an NPU. The model has 32 layers, deployed at INT4.
question: What KV cache size does GQA use for a 16K-context Mistral-7B, and how does it compare with MHA?
details:
  realistic_solution: GQA significantly reduces long-context KV-cache memory by storing keys and values for 8 KV heads instead of 32 attention heads. With 32 layers, head_dim=128, and FP16 KV cache, a 16K context uses about 2.0 GiB for GQA. A standard MHA layout with 32 KV heads under the same assumptions would use about 8.0 GiB, so GQA gives a 4x KV-cache reduction. The INT4 deployment of model weights does not automatically imply INT4 KV cache unless the question explicitly asks for quantized KV storage.
  common_mistake: |
    **The Pitfall:** Applying standard MHA FP16 formulas and failing to account for the multiplicative reduction of GQA paired with integer quantization.
    **The Rationale:** Engineers mechanically apply default LLM formulas without tracing the architectural specifics of Grouped Query Attention or bit-width scaling.
    **The Consequence:** Drastically overestimating memory requirements, falsely classifying a feasible 16K context deployment as impossible and rejecting a viable product feature.
  napkin_math: |
    **Assumptions & Constraints:**
    - GQA Config: 8 KV heads, 128 dim, 32 layers.
    - Context window: 16,384 tokens.
    - KV cache precision: FP16 unless separately quantized.

    **Calculations:**
    - Elements per token: 2 (K+V) * 8 heads * 128 dim = 2048 elements.
    - FP16 bytes per token per layer: 2048 * 2 bytes = 4096 bytes.
    - Total GQA FP16 KV size per token: 4096 bytes * 32 layers = 131,072 bytes.
    - Total GQA FP16 KV size (16K context): 131,072 * 16,384 = 2,147,483,648 bytes = 2.0 GiB.
    - MHA uses 32 KV heads instead of 8: 4x larger = 8.0 GiB.

    **Conclusion & Interpretation:**
    - **Result: GQA KV cache is about 2.0 GiB vs 8.0 GiB for MHA**. GQA cuts the FP16 KV cache by 4x for the same 16K context and layer count.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
