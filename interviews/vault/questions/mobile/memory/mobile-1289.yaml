schema_version: '1.0'
id: mobile-1289
track: mobile
level: L5
zone: evaluation
topic: memory-pressure-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: LLM KV Cache Allocation Strategy on Tensor G3
scenario: 'You are deploying a 1.8B parameter on-device LLM on the Google Tensor G3. The system restricts the LLM process to a strict 2.5 GB memory budget to prevent OS-level eviction of essential background apps. You must choose between two KV cache management strategies for a multi-turn chat feature: Alternative A uses static pre-allocation of a maximum-length KV buffer at initialization, while Alternative B uses dynamic block-based allocation (like PagedAttention) to minimize memory usage for short prompts.'
question: Should a 1.8B LLM on Tensor G3 use static or PagedAttention-style KV allocation under a 2.5 GB budget?
details:
  realistic_solution: Alternative A (Static pre-allocation) is the superior choice for mobile deployment. Because mobile inference typically operates at a batch size of 1, the theoretical memory savings from dynamic allocation do not outweigh the risks of memory fragmentation. Pre-allocating the maximum required KV cache ensures a fixed, predictable memory footprint, completely avoiding runtime fragmentation and guaranteeing that the app will not suddenly trigger the Android low-memory killer (LMK) during prolonged usage.
  common_mistake: |
    **The Pitfall:** Blindly recommending dynamic block allocation (PagedAttention).
    **The Rationale:** Candidates know this is state-of-the-art for server-side LLMs and incorrectly apply it to batch=1 mobile environments.
    **The Consequence:** Dynamic allocation introduces severe memory fragmentation over time, triggering unpredictable Android Low Memory Killer (LMK) app crashes.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Total Budget: 2.5 GB.
       - Weights: 1.8B parameters at INT8 = 1.8 GB.
       - Remaining Budget: 700 MB.
       - KV Configuration: 16 layers, 2048 hidden dim, FP16 (2 bytes).
    2. **Calculations:**
       - KV Cache Per Token: 2 (Key, Value) * 16 layers * 2048 dim * 2 bytes = 131,072 bytes (128 KB).
       - Static Maximum Cache (2048 context): 128 KB * 2048 = 262.14 MB.
       - Total Memory: 1.8 GB + 262.14 MB = ~2.06 GB.
    3. **Conclusion & Interpretation:**
       - **Result: Static Pre-allocation. The 262 MB statically allocated cache comfortably fits inside the 700 MB remaining limit, completely eliminating fragmentation risks.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
