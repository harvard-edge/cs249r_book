schema_version: '1.0'
id: mobile-1512
track: mobile
level: L4
zone: specification
topic: model-size-estimation
competency_area: architecture
bloom_level: analyze
phase: inference
title: On-Device LLM Sizing for Apple A17 Pro
scenario: You are tasked with deploying a 7-billion parameter language model for real-time translation on a device utilizing the Apple A17 Pro Neural Engine.
question: How should a 7B LLM be sized and partitioned to fit within the 8 GB unified memory while efficiently leveraging the Neural Engine?
details:
  realistic_solution: A realistic approach quantizes the 7B model to 4-bit (INT4) to reduce the weight footprint to about 3.5 GB. The design must reserve roughly 1.5 GB for the KV cache and inference activations, leaving 3 GB for the OS out of the 8 GB shared pool. The strategy maps compute-heavy matrix multiplications to the 16-core Neural Engine to leverage its 35 TOPS within the tight ~5W power budget.
  common_mistake: |
    **The Pitfall:** Assuming the entire 8 GB of unified memory is available for the model.
    **The Rationale:** Forgetting to account for the OS footprint and the dynamic KV cache required during text generation.
    **The Consequence:** The OS forcefully evicts the application or crashes due to memory exhaustion.
  napkin_math: |
    1. **Assumptions & Constraints:** 7B parameter LLM. Apple A17 Pro (8 GB unified memory, 35 TOPS). Must reserve memory for OS and dynamic KV cache.
    2. **Calculations:**
       - 7B FP16 footprint: 7B * 2 bytes = 14 GB (Exceeds capacity).
       - 7B INT4 footprint: 7B * 0.5 bytes = 3.5 GB.
       - KV cache overhead: ~1.5 GB.
       - Total remaining: 8.0 GB - 3.5 GB - 1.5 GB = 3.0 GB.
       - Compute check: 20 tokens/s * 14 GOPS = 280 GOPS (< 35 TOPS).
    3. **Conclusion & Interpretation:** **Quantizing to INT4 leaves 3.0 GB safely remaining for the OS. Result: Memory-Capacity Bound.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
