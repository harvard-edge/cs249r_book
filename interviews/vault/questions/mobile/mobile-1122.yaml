schema_version: '1.0'
id: mobile-1122
track: mobile
level: L5
zone: design
topic: cnn-efficient-design
competency_area: architecture
bloom_level: evaluate
phase: inference
title: Architecting a Real-Time Video Segmentation Model for Exynos NPU
scenario: You are tasked with designing a real-time background blur feature for 1080p video calls running at 30 FPS on a mobile NPU (34.7 TOPS). The device shares its 12 GB LPDDR5X memory across the CPU, GPU, ISP, and NPU. You must design the CNN architecture to maximize accuracy while strictly adhering to a 15ms per-frame latency budget.
question: How would you evaluate the trade-offs of using larger feature maps early in the network versus aggressive downsampling given the shared memory bandwidth constraints?
details:
  realistic_solution: 'A strong L5 design uses a hybrid approach: standard convolutions or grouped convolutions for early layers where channel counts are low and feature maps are large to maximize NPU compute utilization, switching to inverted bottleneck blocks deeper in the network. The architecture should aggressively downsample the spatial resolution early using stride-2 convolutions to reduce memory footprint and bandwidth pressure on the shared LPDDR5X bus, and rely on an efficient decoder to recover resolution.'
  common_mistake: |
    **The Pitfall:** Optimizing solely for FLOPs by aggressively using depthwise separable convolutions everywhere.
    **The Rationale:** Candidates rely on standard mobile CNN heuristics, ignoring that depthwise layers possess low arithmetic intensity and easily become memory bandwidth bound.
    **The Consequence:** The model hits the shared memory bandwidth wall, starving the ISP and GPU during the live video call and exceeding the 15ms latency budget.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Mobile NPU with 34.7 TOPS. Latency budget: 15ms per frame (for 30 FPS).
       - LPDDR5X bandwidth: 60 GB/s. Contention limits NPU to 30% (~18 GB/s).
    2. **Calculations:**
       - Compute budget per frame: 34.7 TOPS * 0.015s = 520.5 GOPS.
       - Effective NPU bandwidth per frame: 18 GB/s * 0.015s = 270 MB.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bound.** Aggressive early spatial downsampling prevents the large early feature maps from hitting the strict 270 MB bandwidth wall, avoiding starvation of concurrent ISP processes.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null