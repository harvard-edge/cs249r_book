schema_version: '1.0'
id: mobile-1753
track: mobile
level: L5
zone: specification
topic: fairness-evaluation
competency_area: cross-cutting
bloom_level: analyze
phase: inference
title: Designing a Fair and Efficient On-Device ML System for Content Moderation on Google Tensor G3
scenario: You are designing an on-device machine learning system for real-time content moderation. The platform features 7.5 TOPS and 12 GB RAM. The system must achieve demographic parity within a 50ms latency budget and a footprint under 200MB.
question: "How would you specify a fair, efficient Tensor G3 content moderation model within 50ms latency and 200MB footprint?"
details:
  realistic_solution: Employ fairness-aware Quantization-Aware Training (QAT) to maintain subgroup accuracy under INT8 constraints. Deploy a lightweight MobileNetV3 backbone to satisfy the 200MB footprint. Track demographic parity using local sliding-window counters to avoid heavy post-hoc fairness overhead.
  common_mistake: |
    **The Pitfall:** Assuming globally high accuracy equals fairness, or applying heavy post-hoc fairness algorithms on-device.
    **The Rationale:** Candidates often port server-side fairness methods directly, ignoring that these add massive computational overhead.
    **The Consequence:** The model either amplifies existing biases against subgroups, or fails the strict 50ms latency constraint.
  napkin_math: |
    **Assumptions & Constraints:**
    - NPU: 7.5 TOPS.
    - Budget: 50ms latency, 200MB footprint.
    - Bandwidth: 68.26 GB/s LPDDR5X.
    
    **Calculations:**
    - Compute limit = 7.5 * 10^12 * 0.05s = 3.75 * 10^11 ops.
    - Memory transfer = 200 MB / 68.26 GB/s = ~2.9 ms.
    
    **Conclusion & Interpretation:**
    - **Result: Compute-Bound**. The 2.9ms memory transfer leaves 47.1ms of pure compute time, easily absorbing the minor overhead of local sliding-window fairness tracking.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
