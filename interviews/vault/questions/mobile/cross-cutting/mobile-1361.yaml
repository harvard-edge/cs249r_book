schema_version: '1.0'
id: mobile-1361
track: mobile
level: L3
zone: implement
topic: responsible-ai
competency_area: cross-cutting
bloom_level: apply
phase: inference
title: On-Device Guardrail Compute and Memory Sizing
scenario: You are deploying an on-device content safety guardrail on an iPhone 15 Pro to prevent toxic outputs, utilizing the Apple A17 Pro Neural Engine. The guardrail is a 500M parameter model in INT8 quantization that evaluates 128-token chunks from a main generation model.
question: How would you calculate the static memory footprint of the guardrail weights and the theoretical minimum compute latency for one 128-token chunk, assuming 100% Neural Engine utilization?
details:
  realistic_solution: The correct approach requires applying the standard transformer inference formula (Ops ≈ 2 * Params * Tokens) and mapping it to the provided hardware constraints. INT8 quantization simplifies the static memory footprint to exactly 1 byte per parameter. To find the minimum theoretical latency, we divide the total operations by the Neural Engine's maximum throughput (35 TOPS).
  common_mistake: |
    **The Pitfall:** Forgetting the multiplier when converting parameters to operations, or miscalculating TOPS vs GOPs.
    **The Rationale:** Candidates often use P * T instead of 2 * P * T for MACs, and confuse Tera (10¹²) with Giga (10⁹).
    **The Consequence:** The resulting latency calculation is off by orders of magnitude, completely invalidating the feasibility assessment of the guardrail.
  napkin_math: |
    **Assumptions & Constraints:**
    - Target Hardware: 35 TOPS NPU.
    - Model: 500M parameters (INT8).
    - Chunk Size: 128 tokens.

    **Calculations:**
    - Memory footprint: 500M parameters * 1 byte/param = 500 MB.
    - Compute (MACs): 500M params * 128 tokens = 64 Giga-MACs.
    - Compute (Operations): 64 Giga-MACs * 2 Ops/MAC = 128 GOPs.
    - Theoretical Latency: 128 GOPs / 35,000 GOPs/sec = 0.00365 seconds (3.66ms).

    **Conclusion & Interpretation:**
    - **Result: 3.66ms Minimum Latency**. The guardrail safely consumes 500 MB and easily completes evaluation within a few milliseconds assuming perfect 100% NPU utilization.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The use of '2 * P * T' as a general 'Compute Ops' formula should be clarified, as it is a common approximation for multiply-accumulate operations (MACs) and typically 1 MAC = 2 OPs, so it should be '2 * 2 * P * T' or '2 * P * T' refers to MACs.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
