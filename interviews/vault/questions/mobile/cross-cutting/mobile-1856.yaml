schema_version: '1.0'
id: mobile-1856
track: mobile
level: L5
zone: specification
topic: responsible-ai
competency_area: cross-cutting
bloom_level: create
phase: both
title: Algorithmic Accountability for On-Device Health Decisions
scenario: A mobile health app makes automated medication dosage recommendations based on an on-device 50M-parameter model using sensor data. A patient advocacy group raises concerns about auditability, fairness, and contestability.
question: How would you design a responsible AI governance framework that operates within mobile compute constraints and does not transmit raw health data off-device?
details:
  realistic_solution: |
    Implement Integrated Gradients for top 3 features. Audit fairness via encrypted logs using local differential privacy (epsilon=1). Provide a one-tap contestation mechanism. Display uncertainty-gated disclaimers.
  common_mistake: |
    **The Pitfall:** Proposing cloud-side explainability (e.g. SHAP values computed server-side).
    **The Rationale:** Candidates rely on standard ML explainability tools requiring centralized data.
    **The Consequence:** Violating data minimization principles by leaking sensitive health data off-device.
  napkin_math: |
    **Assumptions & Constraints:**
    - Params: 50M.
    - IG steps: 50 require repeated forward/backward-style attribution passes.
    - NPU: 15 TOPS.
    - Effective latency also depends on operator support, memory bandwidth, feature preprocessing, and whether attribution runs synchronously with the recommendation.

    **Calculations:**
    - A lower-bound arithmetic estimate is 50 steps * 50M params * 2 operations ≈ 5B operations.
    - 5B / 15 TOPS = 0.33 ms only at ideal peak utilization, with no memory traffic or backward-pass overhead.
    - Real mobile execution will be materially higher, so IG should run on a small feature head, cache reusable activations, or run asynchronously for audit display rather than blocking the dosage decision.

    **Conclusion & Interpretation:**
    - **Result: Compute- and Memory-Constrained**. On-device explainability is feasible only with bounded attribution scope and nonblocking presentation; the governance design should not rely on an idealized sub-millisecond IG latency.
status: published
provenance: imported
validated: true
