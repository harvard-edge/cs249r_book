schema_version: '1.0'
id: mobile-1235
track: mobile
level: L5
zone: design
topic: fairness-evaluation
competency_area: cross-cutting
bloom_level: evaluate
phase: inference
title: On-Device Fairness Evaluation Architecture
scenario: You are tasked with designing an on-device fairness evaluation pipeline for a computational photography feature running on the Apple A17 Pro Neural Engine. The system must ensure equalized odds across different skin tones and lighting conditions without uploading raw images, strictly preserving user privacy.
question: "How should an A17 Pro photography feature compute local fairness metrics under strict 100MB memory and 10ms latency constraints?"
details:
  realistic_solution: The optimal design extracts a shared latent embedding from the main model's early layers to feed a lightweight, quantized (INT8) auxiliary classifier for subgroup identification. The device computes true/false positive rates for the enhancement locally and aggregates these into a differentially private sketch. Only these aggregated, anonymized metrics are synced to the cloud, allowing the engineering team to monitor demographic parity and equalized odds without compromising privacy.
  common_mistake: |
    **The Pitfall:** Proposing a large, independent fairness classifier alongside the main model.
    **The Rationale:** Candidates treat fairness evaluation as an entirely separate ML task, failing to reuse the feature extraction work already done by the primary model.
    **The Consequence:** The system unnecessarily duplicates work, blowing past the strict memory and latency constraints.
  napkin_math: |
    **Assumptions & Constraints:**
    - Small MLP architecture: 512 -> 256 -> 15.
    - A17 Pro Neural Engine = 35 TOPS = 35,000,000 MOPs/sec.

    **Calculations:**
    - Parameters = (512 * 256) + (256 * 15) = 131,072 + 3,840 ≈ 135,000 params.
    - Memory in INT8 = 135 KB (Easily fits 100 MB budget).
    - Computations = 135,000 MACs = 270,000 Operations (0.27 MOPs).
    - Inference time = 0.27 MOPs / 35,000,000 MOPs/sec = 0.0077 μs.

    **Conclusion & Interpretation:**
    - **Result: 0.0077 μs (Compute-Bound)**. 0.0077 μs is astronomically below the 10 ms latency budget, leaving ample resources for the main workload.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
