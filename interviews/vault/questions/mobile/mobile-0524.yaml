schema_version: '1.0'
id: mobile-0524
track: mobile
level: L3
zone: optimization
topic: real-time-deadlines
competency_area: compute
bloom_level: apply
phase: inference
title: The Real-Time Filter Lag
scenario: 'You are a mobile ML engineer at a social media company, tasked with optimizing a new real-time
  video filter. The filter uses a custom neural network layer that is causing the app to drop frames,
  missing its 16ms per-frame budget. Your profiler isolates the problematic layer and provides the following
  data for a single inference pass on an Apple A17 Pro chip: 7 Giga-Ops compute and 50 MB data movement.'
question: "Is the custom real-time filter layer memory-bound or compute-bound, and what arithmetic intensity evidence supports that?"
details:
  realistic_solution: The layer is memory-bound. To determine the bottleneck, we must calculate the layer's
    Arithmetic Intensity (AI) and compare it to the hardware's ridge point. The AI is the ratio of compute
    operations to bytes moved. If the layer's AI is lower than the hardware's ridge point, it cannot supply
    data fast enough to keep the compute units busy, making it memory-bound.
  common_mistake: Engineers often focus only on the headline TOPS number of a mobile SoC (e.g., 35 TOPS
    for the A17 Pro) and assume any slowdown must be a compute problem. They forget that if a workload
    doesn't perform enough operations for every byte of data it fetches from memory, the powerful compute
    units will spend most of their time idle, waiting for data. This is the 'memory wall' in action.
  napkin_math: Calculate Arithmetic Intensity (AI). If the custom layer performs 7 Giga-Ops per frame
    and reads 50 MB of data, AI = 7 GOps / 50 MB = 140 Ops/Byte. The A17 Pro ridge point is 35 TOPS /
    51.2 GB/s = ~683 Ops/Byte. Since 140 < 683, the custom layer is severely memory-bandwidth bound.
    To hit 16ms, we must fuse this layer with previous ones to keep data in SRAM.
  options:
  - The layer is compute-bound because its 7 GOps are too much for the A17 Pro's 35 TOPS.
  - The layer is memory-bound because its Arithmetic Intensity (140 Ops/Byte) is less than the A17 Pro's
    ridge point (~683 Ops/Byte).
  - The layer is compute-bound because its Arithmetic Intensity (140 Ops/Byte) is a high number, indicating
    a heavy compute load.
  - The layer is neither compute-bound nor memory-bound; the bottleneck is likely power throttling on
    the SoC.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Reasoning_competency and reasoning_mode are not specified, which is unusual for an L3 question.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
