schema_version: '1.0'
id: mobile-1552
track: mobile
level: L5
zone: evaluation
topic: memory-pressure-management
competency_area: memory
bloom_level: analyze
phase: inference
title: Optimizing Large Generative AI Model Deployment on Samsung Exynos 2400 NPU under Memory Constraints
scenario: You are deploying a generative AI model on the Samsung Exynos 2400 NPU. Even quantized, the model requires 15 GB of peak memory during full-batch inference. You must choose between (A) Aggressive Model Partitioning with Host Offloading, or (B) Micro-Batching with Activation Recomputation.
question: How would you evaluate host offloading versus activation recomputation to fit a 15GB peak memory LLM into the Exynos 2400's 12GB RAM?
details:
  realistic_solution: A realistic solution would involve a careful hybrid approach. Start by profiling the model's memory footprint to identify exact peak usage points. For Approach B, optimize micro-batching size to balance recomputation overhead with memory savings, potentially using a 'striping' or 'pipelining' technique to hide recomputation latency. For Approach A, optimize data transfer mechanisms (e.g., using zero-copy buffers where possible, asynchronous transfers).
  common_mistake: |
    **The Pitfall:** Neglecting the dynamic memory requirements of activations and underestimating NPU-host data transfer penalties.
    **The Rationale:** Engineers look at static model weight sizes and assume they fit, ignoring the massive activation tensors generated during inference and the crippling latency of moving them across the PCIe bus.
    **The Consequence:** The model either crashes with an OOM error or runs so slowly due to host offloading that it is completely unusable.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Total RAM: 12 GB.
       - Base Memory (Weights + minimal activations): 8 GB.
       - Peak Memory: 15 GB.
       - Interconnect: 10 GB/s.
    2. **Calculations:**
       - Excess Activations: 15 GB - 8 GB = 7 GB.
       - Approach A (Offloading 3 GB): 3 GB / 10 GB/s = 300 ms per cycle.
       - Approach B (Recomputation): Reduces 7 GB to 3 GB (peak 11 GB). Recomputing 4 GB takes ~50 ms.
    3. **Conclusion & Interpretation:**
       - **Result: 50 ms compute vs 300 ms transfer. Compute-Bound.** Spending 50 ms on compute is vastly superior to spending 300 ms on data transfer.
status: published
validated: true
