schema_version: '1.0'
id: mobile-0885
track: mobile
level: L6+
zone: mastery
topic: compound-ai-systems
competency_area: deployment
bloom_level: create
phase: inference
title: Mastering End-to-End Optimization of a 4-Stage Compound Pipeline on Snapdragon 8 Gen 3
scenario: A Snapdragon 8 Gen 3 compound AI system currently processes queries in 3.8 seconds. The pipeline includes OCR (400ms), embedding+retrieval (250ms), cross-encoder reranking of 20 docs (600ms), and 3B LLM generation of 80 tokens (2550ms). The target is 1.5 seconds P95.
question: How would you design a comprehensive optimization strategy that achieves this target without changing models or hardware?
details:
  realistic_solution: 'Restructure the pipeline: (1) Parallelize OCR + query embedding (run concurrently on CPU and NPU). (2) Reduce reranker candidates via Top-K Sparsification. (3) Apply INT4 quantization to LLM. (4) Reduce generation length context via prompt engineering. Apply pipeline overlap to start LLM prefill during final reranker chunks.'
  common_mistake: |
    **The Pitfall:** Applying standard latency optimizations independently without restructuring the pipeline dependencies.
    **The Rationale:** Candidates fail to realize that the 3B LLM generation is heavily memory-bandwidth-bound, so quantization alone will not bridge the gap to the 1.5s target.
    **The Consequence:** The final pipeline remains far from the 1.5s P95 latency target, rendering the feature unusable.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Target: < 1.5s P95 total latency.
       - Original total: 400ms + 250ms + 600ms + 2550ms = 3800ms.
    2. **Calculations:**
       - Step 1 (Parallel OCR + Embed): max(400ms, 50ms) = 400ms. Retrieval = 200ms.
       - Step 2 (Rerank top-8 instead of 20): 600ms * (8/20) = 240ms.
       - Step 3 (Quantize LLM to INT4): 2550ms / 2 = 1275ms.
       - Step 4 (Reduce generation to 35 tokens): 1275ms * (35/80) = 557ms.
       - Total Restructured: 400ms + 200ms + 240ms + 557ms = 1397ms.
    3. **Conclusion & Interpretation:**
       - **Result: Pipeline-Latency-Bound**. Applying a 5% jitter buffer results in 1467ms, which safely meets the <1.5s P95 target via parallelization and memory-bandwidth reduction.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
