schema_version: '1.0'
id: mobile-0767
track: mobile
level: L4
zone: mastery
topic: neural-architecture-search
competency_area: architecture
bloom_level: evaluate
phase: training
title: The Mobile MoE Memory Illusion
scenario: 'A research team proposes deploying a Mixture of Experts model on mobile: 8 experts of 500M parameters each (4B total) in INT4, with top-2 routing so only 1B parameters are active per token. They claim it fits in 8 GB because only 0.5 GB of active experts need to be in memory.'
question: Why does top-2 routing not reduce the resident expert footprint to 0.5 GB on mobile, and what design alternatives would you consider if the 2 GB resident set is too large?
details:
  realistic_solution: Total expert memory is 4B x 0.5 bytes (INT4) = 2.0 GB. Even though top-2 routing activates only 0.5 GB of experts for a given token, the router can choose different experts on the next token, so the serving system needs the expert pool resident or cached with a latency guarantee. Paging cold experts from mobile flash is far too slow for interactive decoding; fetching 0.5 GB at ~500 MB/s would add about 1 second before compute. If 2 GB is too large, the real architecture choices are fewer or smaller experts, shared experts, more aggressive compression with quality validation, constrained routing with a small hot cache, or abandoning MoE for a dense mobile model.
  common_mistake: |
    **The Pitfall:** Confusing active compute parameters with memory footprint.
    **The Rationale:** Engineers used to server environments with fast NVMe assume they can page experts in and out dynamically.
    **The Consequence:** On mobile, NAND flash is ~1000x slower than DRAM, meaning dynamic paging would cause catastrophic latency spikes.
  napkin_math: |
    **Assumptions & Constraints:**
    - MoE Model: 8 experts, 500M params each (INT4, 0.5 bytes/param).
    - Active parameters per token: Top-2 routing (1B params).
    - NAND flash read speed: ~500 MB/s.

    **Calculations:**
    - Total MoE memory: 8 * 500M * 0.5 bytes = 2.0 GB.
    - Active memory per token: 2 * 500M * 0.5 bytes = 0.5 GB.
    - Flash paging penalty: Fetching 0.5 GB from NAND = 0.5 GB / (500 MB/s) = 1.0 second per token.

    **Conclusion & Interpretation:**
    - **Result: 2.0 GB Resident Requirement**. Paging a cold expert from flash incurs a 1-second delay per token. The entire 2.0 GB must be locked in DRAM to maintain interactive latency.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
