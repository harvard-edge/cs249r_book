schema_version: '1.0'
id: mobile-1438
track: mobile
level: L5
zone: realization
topic: encoder-decoder-tradeoffs
competency_area: architecture
bloom_level: evaluate
phase: inference
title: Sizing On-Device Translation Architectures
scenario: You are designing an on-device real-time translation system targeting a modern NPU. You need to evaluate the deployment tradeoffs between a 1B parameter encoder-decoder model and a 1.5B parameter decoder-only model.
question: How should an on-device translation architecture be sized for NPU memory-bandwidth limits?
details:
  realistic_solution: |
    Size the architecture around measured decode-time bandwidth, because batch-1 generation is dominated by the active decoder working set, KV cache, and attention-state reads. Prefer the 1B encoder-decoder design when quality is comparable: it pays an upfront encoder pass once per source sentence, then decodes with a smaller 500M decoder and bounded cross-attention state. The 1.5B decoder-only model is simpler, but its larger decoder working set is the higher-risk choice for a bandwidth-limited NPU. Validate the choice by profiling resident weights, KV reads, encoder-state cache reads, and token latency at the target sentence length.
  common_mistake: |
    **The Pitfall:** Assuming fewer total parameters guarantees lower latency.
    **The Rationale:** Candidates falsely equate total parameter count directly to inference speed, missing the distinction between compute-bound prefill and bandwidth-bound generation.
    **The Consequence:** Deploying an encoder-decoder architecture that stalls the memory bus during cross-attention generation phases.
  napkin_math: |
    **Assumptions & Constraints:**
    - Hardware: NPU with 35 TOPS peak, 50 GB/s sustained memory bandwidth.
    - Precision: 8-bit weights for sizing.
    - Models: 1B encoder-decoder (500M encoder/500M decoder) vs 1.5B decoder-only.
    - Translation request: 80 source tokens, 80 generated target tokens.
    - Encoder outputs are computed once and reused during decoding; weight traffic estimates below are sizing upper bounds for the active decoder working set if it cannot remain resident near the accelerator.

    **Calculations:**
    - Decoder-only active decoder working-set upper bound: 1.5B * 1 byte = 1.5 GB/token, or 30 ms/token at 50 GB/s before KV traffic if the working set is streamed.
    - Encoder-decoder upfront encoder working-set upper bound: 0.5 GB once for the source sentence.
    - Encoder-decoder active decoder working-set upper bound: 0.5 GB/token, or 10 ms/token at 50 GB/s before decoder KV and cross-attention state reads if the working set is streamed.
    - Cross-attention state is reused from the encoded source; for 80 source tokens it is bounded by source length and layer width, not by the full decoder parameter count.
    - For 80 target tokens, pessimistic decoder-only weight traffic is 80 * 1.5 = 120 GB; encoder-decoder is 0.5 GB encoder + 80 * 0.5 GB decoder = 40.5 GB plus state/KV traffic.

    **Conclusion & Interpretation:**
    - **Result: prefer the encoder-decoder design when quality is comparable.** It trades one upfront encoder pass and bounded cross-attention reads for a much smaller per-token decoder working set; choose decoder-only only if profiling shows its larger decode path still meets the bandwidth and latency target.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
