schema_version: '1.0'
id: mobile-1574
track: mobile
level: L4
zone: optimization
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: inference
title: Optimizing LLM Inference on Snapdragon 8 Gen 3 with INT4 Quantization
scenario: You are tasked with deploying a 7B parameter Large Language Model (LLM) on a mobile device powered by the Snapdragon 8 Gen 3 Hexagon NPU, which boasts 45 TOPS (INT8) and 12-16 GB LPDDR5X memory. The current LLM uses INT8 quantization, but to meet stringent latency and memory footprint requirements for real-time conversational AI, you need to achieve sub-200ms per token generation and reduce memory usage by at least 30%. Your initial profiling shows that the memory bandwidth and compute utilization for INT8 are the primary bottlenecks. The model's weights and activations are currently quantized per-tensor with symmetric zero-point.
question: How would you optimize Snapdragon 8 Gen 3 LLM inference with INT4 quantization to achieve sub-200ms TPOT and reduce memory footprint?
details:
  realistic_solution: 'A realistic solution involves a hybrid approach: start with robust PTQ calibration using a diverse, representative dataset for optimal per-channel quantization of weights and per-tensor dynamic quantization for activations. For critical layers exhibiting significant accuracy drops, employ Quantization-Aware Training (QAT) for a few epochs with a small learning rate to recover precision. Implement zero-point arithmetic carefully, ensuring the NPU''s custom instructions are utilized efficiently. Use fine-grained analysis to identify outlier channels or layers that would benefit more from mixed-precision (e.g., keeping sensitive layers in INT8 or FP16) if pure INT4 doesn''t meet accuracy targets. Leverage the Hexagon NPU''s specific SDK to map quantized operations for optimal performance and memory access patterns.'
  common_mistake: |
    **The Pitfall:** Blindly applying INT4 post-training quantization (PTQ).
    **The Rationale:** Candidates do not consider the impact of activation ranges and specific layer sensitivities, and neglect zero-point complexities.
    **The Consequence:** The model suffers significant accuracy degradation and sub-optimal latency because calibration parameters don't effectively utilize the NPU's capabilities.
  napkin_math: |
    - INT8 Memory: 7B params * 1 byte = 7 GB.
    - INT4 Memory: 7B params * 0.5 bytes = 3.5 GB (50% reduction).
    - Compute Throughput: Effective TOPS double from 45 TOPS (INT8) to ~90 TOPS (INT4).
    - **Latency: If currently 300ms/token in INT8, halving both memory and compute bottlenecks ideally yields 150ms/token, meeting the sub-200ms target.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
