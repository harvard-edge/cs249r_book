schema_version: '1.0'
id: mobile-1582
track: mobile
level: L5
zone: specification
topic: extreme-quantization
competency_area: precision
bloom_level: analyze
phase: inference
title: 'Sub-4-bit LLM Deployment on Google Tensor G3'
scenario: 'Your team must deploy a 7B parameter LLM onto a Google Tensor G3 device (7.5 TOPS INT8 TPU, 12 GB LPDDR5X). The model is currently FP16, resulting in a 14 GB footprint.'
question: 'How can a 14 GB FP16 7B LLM be compressed below 4 bits for Tensor G3 without losing critical task accuracy?'
details:
  realistic_solution: |
    Implement a hybrid quantization strategy using post-training quantization (like AWQ or GPTQ) for sub-4-bit weights, paired with 8-bit activations. Employ quantization-aware fine-tuning (QAT) to recover accuracy. Ensure efficient mapping of the quantized operations to the TPU, potentially leveraging FlashInfer-like custom ops to pack and process sub-4-bit weights within INT8 operations.
  common_mistake: |
    **The Pitfall:** Applying naive quantization without addressing the hardware's INT8 execution constraints.
    **The Rationale:** Candidates overlook that the TPU requires INT8 mapping, meaning sub-4-bit weights must be unpacked efficiently, and ignore the necessity of QAT for accuracy recovery.
    **The Consequence:** The model either loses too much accuracy to be useful or runs slowly due to inefficient hardware utilization.
  napkin_math: |
    **Assumptions & Constraints:**
    - Original FP16 memory footprint: 7B params * 2 bytes/param = 14 GB.

    **Calculations:**
    - 2-bit quantization footprint: 7B params * (2/8) bytes/param = 1.75 GB, easily fitting the 12 GB memory.
    - Compute requirement at 10 tokens/sec: 14 Giga-operations/token * 10 = 140 GOPS.

    **Conclusion & Interpretation:**
    - **Result: 140 GOPS (Compute-Bound)**. The TPU offers 7500 GOPS, providing substantial margin if sub-4-bit operations are efficiently executed as INT8.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
