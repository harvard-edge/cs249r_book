schema_version: '1.0'
id: mobile-2131
track: mobile
level: L3
zone: design
topic: collective-communication
competency_area: networking
bloom_level: analyze
phase: both
title: Automotive Ethernet AllGather
scenario: A prototype vehicle perception cluster with 3 embedded accelerator nodes shares 8MB bounding box tensors via an automotive Gigabit Ethernet switch.
question: What is the ring AllGather latency for an 8MB tensor per node across 3 nodes over Gigabit Ethernet, accounting for a 50us switch processing delay per hop and a 1ms synchronization barrier?
details:
  realistic_solution: For this simplified ring AllGather estimate, model each node as receiving N-1 tensor chunks over the bottleneck Gigabit link. With 3 nodes and an 8MB tensor per node, each node receives 16MB total. At 1000 Mbps, 128Mb takes 128ms; adding two 50us switch hops and a 1ms barrier gives about 129.1ms. A detailed ring model would count two ring steps, but the payload bandwidth still dominates this simplified latency estimate.
  common_mistake: |
    **The Pitfall:** Confusing the AllGather payload scaling factor with AllReduce.
    **The Rationale:** Engineers mistakenly use the AllReduce factor of $2*(N-1)/N$ instead of the AllGather factor of $(N-1)*M$.
    **The Consequence:** This leads to incorrect bandwidth bounds and underestimating the network latency for the aggregation step.
  napkin_math: |
    **Assumptions & Constraints:**
    - Nodes (N): 3.
    - Tensor size (M): 8 MB.
    - Network: Gigabit Ethernet (1000 Mbps).
    - Switch delay: 50us per hop. Sync barrier: 1ms.

    **Calculations:**
    - Ring steps: N - 1 = 2.
    - Data received per node: $(N-1) * M = (3-1) * 8\text{ MB} = 16\text{ MB}$.
    - Convert to Megabits: 16 MB * 8 bits/byte = 128 Mb.
    - Transmission time: 128 Mb / 1000 Mbps = 0.128 s = 128 ms.
    - Switch delay: 2 hops * 50us = 100us = 0.1 ms.
    - Total latency: 128 ms + 0.1 ms + 1.0 ms = 129.1 ms.

    **Conclusion & Interpretation:**
    - **Result: 129.1 ms Latency**. The payload transmission heavily dominates the latency, proving that the switch and barrier delays are negligible.
status: published
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 8
validated: true
validation_status: passed
validation_date: '2026-04-25'
validation_model: pydantic-1.0
math_verified: true
math_status: CORRECT
math_date: '2026-04-25'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
  - gemini-generated
  - target-design-L4
created_at: '2026-04-25'
