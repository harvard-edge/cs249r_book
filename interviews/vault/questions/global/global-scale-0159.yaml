schema_version: '1.0'
id: global-scale-0159
track: global
level: L5
zone: specification
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: evaluate
phase: both
title: Cross-Regime Rollout Spec
scenario: A platform team asks for one rollout policy that covers cloud inference, mobile offline models, and TinyML firmware. Cloud rollback takes 2 minutes, mobile rollback depends on app update adoption, and TinyML rollback requires OTA firmware with battery constraints. Specify the minimum rollout policy fields that make the policy safe across all three regimes.
question: What rollout policy fields are required for cloud, mobile, and TinyML releases with very different rollback latencies?
details:
  realistic_solution: The policy must specify blast-radius unit, canary fraction, minimum sample size, health metrics, rollback trigger, rollback mechanism, maximum rollback latency, and holdout duration per regime. Cloud can use request or pod units, mobile needs device cohorts and staged app rollout, and TinyML needs firmware slots, battery-safe OTA windows, and fallback behavior.
  common_mistake: Writing a cloud-only canary policy and assuming mobile or TinyML rollback behaves like a server deployment.
  napkin_math: If cloud rollback is 2 minutes but mobile rollback takes days, the mobile canary must use smaller blast radius and longer observation before broad rollout.
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 14
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- scaled-gap-pack
