schema_version: '1.0'
id: global-scale-0162
track: global
level: L5
zone: realization
topic: chiplet-architecture
competency_area: compute
bloom_level: evaluate
phase: both
title: Chiplet Bandwidth Budget
scenario: A proposed accelerator uses four compute chiplets. Each chiplet produces 25 GB/s of activation traffic that must cross an interposer during model-parallel inference. The interposer provides 300 GB/s aggregate bisection bandwidth. Size the traffic demand and decide whether bandwidth or compute integration is the first concern.
question: Does a four-chiplet design with 25 GB/s cross-chiplet traffic per chiplet fit within 300 GB/s bisection bandwidth?
details:
  realistic_solution: Total cross-chiplet demand is 4 x 25 = 100 GB/s under one-way accounting of producer traffic. That is below 300 GB/s, leaving 3x bandwidth headroom for this simplified traffic matrix. The design still needs topology and traffic-matrix validation because all-to-all exchange, contention, and latency can consume headroom.
  common_mistake: Comparing one chiplet’s traffic to aggregate bandwidth without summing all producers, or assuming bandwidth headroom eliminates topology latency concerns.
  napkin_math: Demand = 4 x 25 GB/s = 100 GB/s under one-way producer accounting. Headroom = 300 / 100 = 3x. A different all-to-all traffic matrix could change the effective bisection demand.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 14
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- scaled-gap-pack
