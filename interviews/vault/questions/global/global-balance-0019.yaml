schema_version: '1.0'
id: global-balance-0019
track: global
level: L5
zone: optimization
topic: graceful-degradation
competency_area: reliability
bloom_level: evaluate
phase: both
title: Graceful Degradation Threshold
scenario: A service normally runs a 6B model at 120 ms. A 1B fallback runs at 35 ms with 4% lower quality. During overload, queueing adds 200 ms to the 6B path. Decide when to switch.
question: When should a service switch from a 6B model to a 1B fallback if overload adds 200 ms of queueing to the 6B path?
details:
  realistic_solution: Under overload, the 6B path becomes about 320 ms, while fallback is 35 ms. If the product values latency and availability over the 4% quality loss during overload, switch low-priority or latency-sensitive traffic to fallback until queues recover. In production, route by cohort and error budget rather than switching every request at once.
  common_mistake: Keeping the best-quality model for every request even when queueing makes the experience unusable.
  napkin_math: Overloaded 6B latency = 120 + 200 = 320 ms. Fallback saves 285 ms at a 4% quality cost.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 10
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- iteration-gap-pack
