schema_version: '1.0'
id: global-pilot-0006
track: global
level: L5
zone: design
topic: data-pipeline-engineering
competency_area: data
bloom_level: evaluate
phase: both
title: Cross-Track Data Pipeline Placement
scenario: A vision feature needs 30 MB/s of raw camera data. In cloud mode the client uploads compressed frames over a 10 MB/s uplink, in mobile mode preprocessing runs on device at 45 MB/s, and in edge mode a local gateway can preprocess at 120 MB/s but adds a 20 ms hop. Design a pipeline placement that avoids starving the model while preserving user-visible latency.
question: Where should preprocessing run when raw input is 30 MB/s, cloud upload is 10 MB/s, mobile preprocessing is 45 MB/s, and edge preprocessing is 120 MB/s?
details:
  realistic_solution: Cloud upload is infeasible for raw streaming because 30 MB/s demand exceeds a 10 MB/s supply. Mobile preprocessing has capacity about 1.5x demand, or roughly 50% throughput slack, which may be acceptable for short bursts but is fragile under thermal throttling. Edge preprocessing has 4x demand capacity and only a 20 ms hop, so the robust design is local edge preprocessing with mobile fallback for small batches and cloud only for asynchronous analysis.
  common_mistake: Treating preprocessing as a pure compute problem and ignoring the uplink demand-supply ratio.
  napkin_math: Cloud ratio = 30/10 = 3.0, so it starves. Mobile capacity/demand = 45/30 = 1.5x, about 50% slack. Edge capacity/demand = 120/30 = 4x, plus 20 ms latency.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 12
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- pilot-gap-pack
