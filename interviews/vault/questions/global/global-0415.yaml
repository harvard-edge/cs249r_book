schema_version: '1.0'
id: global-0415
track: global
level: L6+
zone: mastery
topic: memory-pressure-management
competency_area: memory
bloom_level: create
phase: both
title: Incomplete Memory Admission Plan
scenario: A serving platform wants to admit multiple models onto shared accelerators. The team provides model weight sizes but omits KV cache, activation peaks, allocator fragmentation, batch size, co-tenant reservations, and model reload behavior.
question: "What information is missing before deciding whether multiple models can share an accelerator safely?"
details:
  realistic_solution: 'You need dynamic memory terms: KV cache, activations, temporary buffers, allocator fragmentation, batch/concurrency, co-tenant reservations, and reload/cold-start peaks. Weight size is a lower bound, not an admission policy. A safe plan uses peak memory plus headroom and admission control.'
  common_mistake: |
    **The Pitfall:** Packing models until weights fill memory and discovering runtime OOM from KV cache or temporary buffers.
    **The Rationale:** Candidates treat model deployment as static storage rather than a dynamic system with varying memory demands during inference.
    **The Consequence:** Unpredictable out-of-memory errors and dropped requests in production.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Accelerator Memory = 80 GB.
       - Model Weights = 60 GB.
    2. **Calculations:**
       - Apparent Headroom = 80 - 60 = 20 GB.
       - Unaccounted Dynamic Demands = 12 GB KV cache + 4 GB temporary buffers + 5 GB fragmentation = 21 GB.
    3. **Conclusion & Interpretation:**
       - **Result: Guaranteed OOM.** Dynamic memory demands exceed the apparent static headroom.
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 18