schema_version: '1.0'
id: global-pilot-0026
track: global
level: L5
zone: design
topic: memory-hierarchy-design
competency_area: memory
bloom_level: evaluate
phase: both
title: Memory Hierarchy Aware Placement
scenario: A retrieval model has a 6 GB embedding table and a 200 MB hot working set. Cloud HBM can hold everything, a phone has 8 GB shared memory with 3 GB already used by the OS and apps, and a gateway has 512 MB DRAM plus SSD. Design a memory hierarchy strategy for each regime.
question: How would you place a 6 GB embedding table with a 200 MB hot set across cloud HBM, mobile shared memory, and a 512 MB edge gateway?
details:
  realistic_solution: Cloud should keep the table in accelerator or host memory depending on bandwidth needs. Mobile cannot safely reserve the full 6 GB because only about 5 GB is free before allocator fragmentation and OS eviction risk, so it should keep a compressed or personalized subset plus a cache. The gateway can keep the 200 MB hot set in DRAM and page cold embeddings from SSD, accepting higher miss latency. The design follows hot/cold separation, not a one-size-fits-all table placement.
  common_mistake: Checking only total device memory and ignoring OS reservations, hot-set size, and storage miss latency.
  napkin_math: Mobile free memory is 8 GB - 3 GB = 5 GB, below the 6 GB table. Gateway DRAM is 512 MB, which fits the 200 MB hot set but not the full table.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 12
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- pilot-gap-pack
