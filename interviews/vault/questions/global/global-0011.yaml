schema_version: '1.0'
id: global-0011
track: global
level: L1
zone: recall
topic: power-budgeting
competency_area: power
bloom_level: remember
phase: both
title: The Cost of Data Movement
scenario: 'Which operation consumes significantly more energy on a modern accelerator: performing an FP16
  multiply-add operation or reading those operands from main memory (HBM/DRAM)?'
question: "Which operation consumes significantly more energy on a modern accelerator: performing an FP16 multiply-add operation or reading those operands from main memory (HBM/DRAM)?"
details:
  realistic_solution: Reading from main memory costs orders of magnitude more energy. Moving data across
    the physical interconnect from DRAM to the compute core is the primary thermal and energetic bottleneck
    in modern systems.
  common_mistake: Assuming compute is the primary energy draw because GPUs are famous for high FLOPs.
  napkin_math: '- FP16 multiply-add: ~0.4 pJ | - DRAM read (64 bits): ~200 pJ | - => Data movement costs
    ~500× more energy than the compute itself'
  options:
  - The FP16 multiply-add consumes about 10x more energy.
  - They consume roughly the same amount of energy.
  - Reading from main memory consumes ~100x to 1000x more energy.
  - Compute consumes more energy only if batch size is exactly 1.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 1
chains:
- id: global-chain-037
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
