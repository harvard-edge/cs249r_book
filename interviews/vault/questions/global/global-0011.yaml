schema_version: '1.0'
id: global-0011
track: global
level: L1
zone: recall
topic: power-budgeting
competency_area: power
bloom_level: remember
phase: both
title: The Cost of Data Movement
scenario: A modern accelerator performs an FP16 multiply-add operation using operands read from main memory (HBM/DRAM).
question: Which operation consumes significantly more energy on a modern accelerator: performing the FP16 multiply-add operation or reading the operands from main memory?
details:
  realistic_solution: Reading from main memory costs orders of magnitude more energy. Moving data across the physical interconnect from DRAM to the compute core is the primary thermal and energetic bottleneck in modern systems.
  common_mistake: |
    **The Pitfall:** Assuming compute is the primary energy draw because GPUs are famous for high FLOPs.
    **The Rationale:** FLOPs are heavily marketed as the primary metric of AI accelerators, obscuring the physical reality that data movement dominates the power budget.
    **The Consequence:** Designing algorithms that minimize math at the expense of memory reads, resulting in worse thermal and energetic efficiency.
  napkin_math: |
    - FP16 multiply-add energy = ~0.4 pJ.
    - DRAM read (64 bits) energy = ~200 pJ.
    - **Data movement costs ~500x more energy than the compute itself.**
  options:
  - The FP16 multiply-add consumes about 10x more energy.
  - They consume roughly the same amount of energy.
  - Reading from main memory consumes ~100x to 1000x more energy.
  - Compute consumes more energy only if batch size is exactly 1.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 1
chains:
- id: global-chain-037
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
