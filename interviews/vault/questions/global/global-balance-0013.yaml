schema_version: '1.0'
id: global-balance-0013
track: global
level: L5
zone: design
topic: encoder-decoder-tradeoffs
competency_area: architecture
bloom_level: evaluate
phase: both
title: Architecture Choice For Long Outputs
scenario: A product serves short commands and long summaries. Encoder-decoder architecture has a 100 ms encoder and 8 ms/token decoder. Decoder-only architecture has no encoder but costs 14 ms/token. Design a routing rule for short and long outputs.
question: How would you route short commands versus long summaries between encoder-decoder and decoder-only architectures?
details:
  realistic_solution: For very short outputs, decoder-only may win because it avoids the fixed 100 ms encoder cost. For long outputs, encoder-decoder can win because its per-token decode is lower. The crossover is when 100 + 8T = 14T, or about 17 tokens. Route short commands to decoder-only and longer summaries to encoder-decoder if quality is comparable.
  common_mistake: Choosing one architecture globally without considering output length distribution.
  napkin_math: 'Crossover: 100 = 6T, so T ≈ 16.7 tokens.'
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 10
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- iteration-gap-pack
