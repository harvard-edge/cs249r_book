schema_version: '1.0'
id: global-0334
track: global
level: L5
zone: design
topic: extreme-quantization
competency_area: precision
bloom_level: evaluate
phase: both
title: Extreme Quantization Rollout Design
scenario: A team wants to ship a 2-bit model to low-memory devices while keeping a higher-quality INT8 model for premium devices.
question: How would you roll out a 2-bit model for low-memory devices without creating quality cliffs versus the INT8 model?
details:
  realistic_solution: Segment devices by memory and performance, run per-segment quality gates, keep server or INT8 fallback where feasible, monitor task-specific regressions, and avoid comparing only aggregate accuracy. The 2-bit model should be a constrained fallback, not the default if quality loss is large.
  common_mistake: |
    **The Pitfall:** Treating 2-bit quantization as a universal compression upgrade simply because it fits on more devices.
    **The Rationale:** Focusing entirely on memory constraints while ignoring non-linear model degradation.
    **The Consequence:** Shipping a heavily degraded model that ruins the user experience for the low-end device segment.
  napkin_math: |
    * Compression ratio: 2-bit weights are 4x smaller than INT8.
    * Memory win: Significant reduction in RAM footprint.
    * **A 4x memory win is unacceptable if it causes a severe accuracy drop on rare but critical classes.**
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 10
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- iteration-gap-pack
