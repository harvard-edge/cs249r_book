schema_version: '1.0'
id: edge-2007
track: edge
level: L3
zone: implement
topic: mlops-lifecycle
competency_area: deployment
bloom_level: analyze
phase: both
title: 'Edge AI Model Deployment: Latency vs. Throughput on Jetson Orin'
scenario: 'Your team is deploying a continuously trained deep learning model for real-time object detection onto a fleet of NVIDIA Jetson Orin devices. Each Jetson Orin has 32 GB LPDDR5 memory and provides up to 275 TOPS (INT8) for AI inference. The current INT8 optimized model requires an average of 15 TOPS of computational effort per inference and consumes 2 GB of memory. A critical requirement is to maximize inference throughput while ensuring an end-to-end latency not exceeding 50ms per inference batch, maintaining training-serving consistency.'
question: 'Can the 15 TOPS INT8 model meet a 50ms latency target on Jetson Orin, and what CI/CD profiling and MLOps steps would you add?'
details:
  realistic_solution: 'Implement a rigorous performance profiling pipeline as part of your CI/CD for ML. This pipeline should measure actual end-to-end latency and throughput on target hardware (Jetson Orin) for various batch sizes. Utilize tools like NVIDIA''s Nsight Systems or `perf` to identify bottlenecks. If the 50ms latency cannot be met, strategies include further model quantization (e.g., to INT4 if supported), model pruning/distillation, or exploring more efficient architectures. Consider dynamic batching, but with caution, as it can introduce latency variability. Ensure model versioning and artifact tracking in a model registry for reproducibility and training-serving consistency, allowing rollbacks to previous, more performant versions if new models fail to meet edge constraints.'
  common_mistake: |
    **The Pitfall:** Overlooking the discrepancy between theoretical maximum throughput and per-inference latency constraints.
    **The Rationale:** Engineers assume that sufficient total compute guarantees meeting individual latency targets.
    **The Consequence:** The deployed system achieves high throughput but consistently misses hard real-time latency deadlines.
  napkin_math: |
    **Assumptions & Constraints:**
    - Jetson Orin max compute: 275 TOPS.
    - Model compute requirement: 15 TOPS per inference.
    - Hard latency target: 50 ms.

    **Calculations:**
    - Theoretical max inferences per second (IPS): 275 TOPS / 15 TOPS/inf = 18.33 IPS.
    - Minimum possible time per inference: 1000 ms / 18.33 IPS = 54.5 ms.

    **Conclusion & Interpretation:**
    - **Result: 54.5 ms Latency (Compute-Bound)**. Since the minimum theoretical latency exceeds 50 ms, it is mathematically impossible to meet the target on this hardware without further model optimization.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
