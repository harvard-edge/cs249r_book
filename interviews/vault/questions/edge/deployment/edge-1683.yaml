schema_version: '1.0'
id: edge-1683
track: edge
level: L4
zone: optimization
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: both
title: Canary Rollout Context Thrashing on AI 100
scenario: 'You are executing a 50/50 canary rollout of an upgraded INT8 vision model on an on-premise server equipped with a single Qualcomm Cloud AI 100. Both the 12GB legacy model and the 12GB canary model fit comfortably within the 32 GB LPDDR4x memory. However, during the rollout phase, overall system throughput drops dramatically despite utilizing only a fraction of the 400 TOPS INT8 capacity, and power draw spikes near the 75W TDP limit.'
question: What is the structural bottleneck of this traffic splitting approach on the Cloud AI 100, and how would you quantify a fix?
details:
  realistic_solution: 'The bottleneck is severe context thrashing of the accelerator''s internal SRAM caches. Round-robin alternating between two 12GB models saturates the LPDDR4x bandwidth. The solution is dynamic request batching at the inference router level: chunk requests (e.g., 50 requests for Model A, then 50 for Model B) to amortize the SRAM weight swap penalty across a batch.'
  common_mistake: |
    **The Pitfall:** Assuming the bottleneck is compute-bound and suggesting further quantization.
    **The Rationale:** Candidates focus on the models rather than the infrastructure handling the traffic split.
    **The Consequence:** They fail to solve the memory bandwidth thrashing, leaving the system starved for data and wasting compute capacity.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model Size: 12 GB.
    - LPDDR4x Bandwidth: ~136 GB/s.

    **Calculations:**
    - Context Switch Penalty: 12 GB / 136 GB/s = 88 ms.
    - Naive Throughput: Round-robin adds 88 ms penalty per inference (max ~11 FPS).
    - Batched Throughput: Batching 50 requests amortizes to 88 ms / 50 = 1.76 ms penalty.

    **Conclusion & Interpretation:**
    - **Result: 1.76 ms Penalty (Memory-Bandwidth Bound)**. Request batching increases throughput 50x by mitigating SRAM cache thrashing.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
