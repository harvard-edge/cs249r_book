schema_version: '1.0'
id: edge-1250
track: edge
level: L3
zone: analyze
topic: systolic-dataflow
competency_area: compute
bloom_level: apply
phase: both
title: Hailo-8 Data Streaming Bottleneck Analysis
scenario: We deployed a vision model on a Hailo-8 dataflow accelerator (26 TOPS INT8,
  2.5W). The device has no local DRAM and streams data directly from host memory via
  a 4 GB/s PCIe link. A specific convolutional layer requires 10 GOPS of compute,
  takes 2 MB of input activations, 1 MB of weights, and produces 2 MB of output activations.
  When running this layer, we observe that the accelerator achieves only a fraction
  of its peak compute. Calculate the achieved TOPS for this layer and explain why
  the architecture is underutilized.
question: What TOPS does the Hailo-8 achieve on this layer, and why is it underutilized
  despite a 26 TOPS peak?
details:
  realistic_solution: The accelerator is entirely I/O bound because it lacks local
    DRAM and must stream all inputs and outputs over the host PCIe link. Even if the
    internal dataflow is perfectly optimized, the 4 GB/s connection cannot feed data
    fast enough to sustain the 26 TOPS engine given this layer's low arithmetic intensity,
    leading to severe compute starvation.
  common_mistake: Candidates often try to calculate internal SRAM bandwidth or analyze
    specific systolic array dataflows (like weight-stationary) instead of recognizing
    the external I/O bottleneck. They may also incorrectly add compute and I/O times
    together rather than assuming they overlap.
  napkin_math: Total I/O per inference = 2 MB (input) + 1 MB (weights) + 2 MB (output)
    = 5 MB. I/O time = 5 MB / 4000 MB/s = 1.25 ms. Minimum compute time at peak =
    10 GOPS / 26,000 GOPS = 0.38 ms. Since I/O time (1.25 ms) is much greater than
    compute time (0.38 ms), the pipeline is bottlenecked by the PCIe stream. Achieved
    compute = 10 GOPS / 1.25 ms = 8 TOPS (~31% utilization of the 26 TOPS peak).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Unit error: The denominator should be 26,000 GOPS/s (a rate), not 26,000 GOPS,
  to yield a time in milliseconds.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
