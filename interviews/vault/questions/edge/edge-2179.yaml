schema_version: '1.0'
id: edge-2179
track: edge
level: L6+
zone: mastery
topic: latency-decomposition
competency_area: latency
bloom_level: create
phase: both
title: Incomplete Edge SLA Diagnosis
scenario: An edge speech assistant misses a 150 ms latency SLO. The team reports model inference takes 70 ms, but does not provide audio windowing delay, feature extraction time, queueing, network hop, wake-word delay, or P99 distribution.
question: What missing latency components are needed before diagnosing a 150 ms edge speech SLA miss?
details:
  realistic_solution: "Solely monitoring the 70ms inference metric guarantees phantom SLA breaches. For edge speech assistants, you must instrument the entire telemetry lifecycle: audio windowing accumulation, DSP feature extraction (MFCCs), OS thread scheduling, IPC queueing, and network routing hops. These 'hidden' components easily consume the remaining 80ms budget. Decomposing P95/P99 across all pipeline nodes is mandatory to diagnose the 150ms SLO failure."
  common_mistake: |
    **The Pitfall:** Optimizing the model immediately without decomposing the remaining latency budget.
    **The Rationale:** Candidates focus solely on the 70ms inference metric because it is the most visible number.
    **The Consequence:** Effort is wasted on model compression while hidden pipeline components push the system past the 150ms SLA.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - SLA threshold: 150 ms.
       - Known inference latency: 70 ms.
    2. **Calculations:**
       - SLA headroom = 150 ms - 70 ms = 80 ms.
       - Example hidden costs = 40 ms (audio window) + 30 ms (queue) + 20 ms (feature extraction) = 90 ms.
       - Total realistic latency = 70 + 90 = 160 ms.
    3. **Conclusion & Interpretation:**
       - **160ms SLA Failure.** Hidden pipeline delays easily consume the 80ms headroom.
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 18
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- pilot-gap-pack
- incomplete-information
