schema_version: '1.0'
id: edge-1484
track: edge
level: L5
zone: design
topic: operator-scheduling
competency_area: optimization
bloom_level: evaluate
phase: inference
title: Multi-Tenant Operator Scheduling on Qualcomm Cloud AI 100
scenario: You are tasked with designing the inference execution engine for a multi-tenant on-premise deployment utilizing the Qualcomm Cloud AI 100 accelerator. The system must schedule concurrent execution of multiple transformer-based models within the power envelope while maximizing the INT8 compute capacity. You must architect a scheduling system that handles operator fusion, memory reuse across tenants, and execution order optimization to minimize memory thrashing and ensure high throughput.
question: How would you schedule multiple transformer models on Cloud AI 100 to maximize 400 INT8 TOPS without LPDDR4x memory thrashing?
details:
  realistic_solution: 'The scheduler should use admission control before execution: admit only model sets whose weights, KV cache, runtime reserve, and peak activation buffers fit in the 32 GB LPDDR4x budget. Compile each tenant with operator fusion and planned layer ordering so its peak intermediate buffer is capped, then run the admitted tenants with per-model queues and short time slices rather than launching all ready work at once. If a model would exceed its activation budget, queue it or lower its batch size instead of allowing LPDDR4x spill and thrash.'
  common_mistake: |
    **The Pitfall:** Designing a static scheduling approach tailored for a single model instead of multi-tenant environments.
    **The Rationale:** Candidates overlook the necessity of dynamic memory pooling and the severe impact of unfused operators on LPDDR4x bandwidth.
    **The Consequence:** Massive memory thrashing causes the 32 GB limit to be exceeded, collapsing system throughput.
  napkin_math: |
    **Assumptions & Constraints:**
    - Concurrent Models: 4.
    - Static Memory per Model: 5 GB INT8 weights + 1 GB KV Cache (6 GB total).
    - LPDDR4x limit: 32 GB.
    - Runtime reserve for OS, DMA queues, and fragmentation: 4 GB.
    - Unfused intermediate block: 3 GB. Fused intermediate block: 1 GB.

    **Calculations:**
    - Static footprint for 4 models: 4 * 6 GB = 24 GB.
    - Memory left after static footprint: 32 GB - 24 GB = 8 GB.
    - Safe dynamic-buffer budget after reserve: 8 GB - 4 GB = 4 GB.
    - Unfused active concurrency: floor(4 GB / 3 GB) = 1 model.
    - Fused active concurrency: floor(4 GB / 1 GB) = 4 models.

    **Conclusion & Interpretation:**
    - **Result: Operator fusion enables 4 active tenants (Memory Footprint-Bound).** TOPS utilization improves only after the scheduler proves the fused working set fits inside the 4 GB dynamic-buffer budget.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
