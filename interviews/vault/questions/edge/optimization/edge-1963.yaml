schema_version: '1.0'
id: edge-1963
track: edge
level: L2
zone: analyze
topic: knowledge-distillation
competency_area: optimization
bloom_level: analyze
phase: training
title: 'Optimizing Large Language Models on Qualcomm AI 100: Knowledge Distillation Strategies'
scenario: A machine learning team is preparing to deploy a complex conversational AI model on a fleet of Qualcomm Cloud AI 100 accelerators. The target platform offers 32 GB LPDDR4x memory, 400 TOPS INT8 performance, and operates within a 75W power envelope. The initial large model exceeds these constraints. You propose Knowledge Distillation to create a smaller, more efficient student model.
question: How would you use logit and feature distillation to build an INT8 student for Cloud AI 100, and when is distillation better than pruning?
details:
  realistic_solution: |
    Implement a multi-stage distillation strategy. First, use logit matching with a quantized (INT8) teacher model to guide a smaller student model, ensuring the student also targets INT8 deployment. Simultaneously, experiment with feature distillation, carefully selecting intermediate layers to distill, and profile memory access patterns to ensure the LPDDR4x bandwidth doesn't become a bottleneck. Prioritize distillation over aggressive pruning when fine-grained accuracy preservation is paramount.
  common_mistake: |
    **The Pitfall:** Ignoring the memory bandwidth implications of LPDDR4x for feature distillation.
    **The Rationale:** Treating edge devices like datacenter GPUs where massive intermediate feature maps can be stored in HBM.
    **The Consequence:** Feature distillation grinds to a halt due to bandwidth saturation, making it far inferior to logit matching for this hardware.
  napkin_math: |
    **Assumptions & Constraints:**
    - Hardware Compute: 400 TOPS = 400,000 GOPS.
    - LPDDR4x Bandwidth: ~50 GB/s (128-bit bus width).
    - Feature Distillation Cost: 10 intermediate layers * 10 MB feature map = 100 MB per forward pass.

    **Calculations:**
    - Bandwidth Requirement: 100 MB / 50 GB/s = 2 ms just for feature map movement.

    **Conclusion & Interpretation:**
    - **Result: 2 ms overhead (Memory-Bound Distillation)**. Feature distillation creates massive memory traffic overhead compared to logit distillation, making it inferior for memory-bound edge hardware.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
