schema_version: '1.0'
id: edge-2216
track: edge
level: L4
zone: diagnosis
topic: autograd-computational-graphs
competency_area: optimization
bloom_level: apply
phase: training
title: Exporting a model with custom autograd to TensorRT for Jetson Orin deployment
scenario: You have trained a 50M parameter object detection model on H100 with a custom non-maximum suppression (NMS) operation implemented as a torch.autograd.Function. You need to deploy it to Jetson AGX Orin. TensorRT export via torch.onnx.export fails because the custom autograd function has no ONNX equivalent.
question: How do you export custom autograd NMS to TensorRT, and what is the theoretical latency for a 4 GOP INT8 model on a 275 TOPS Orin engine?
details:
  realistic_solution: To resolve the export failure, define a custom ONNX symbolic function using `@staticmethod def symbolic(g, ...)` within the `torch.autograd.Function`, or replace NMS with a supported ONNX/TensorRT plugin path. The exporter only needs the forward graph, so mapping the custom NMS to supported forward operators or a plugin lets TensorRT compile the model. If the deployed INT8 convolution/backbone graph requires 4 GOPs and can use a 275 TOPS engine perfectly, the theoretical compute-only lower bound is 0.0145 ms; end-to-end detector latency must add memory movement, kernel launch, decode, and NMS/plugin overhead.
  common_mistake: |
    **The Pitfall:** Trying to export the custom autograd function directly to ONNX without a symbolic function, and mixing up TOPS and GOPS.
    **The Rationale:** Engineers forget that ONNX export only traces the forward pass and requires explicit symbolic (forward-only) registration, and incorrectly use GOPS instead of TOPS for latency calculation.
    **The Consequence:** The model fails to compile for TensorRT, and performance predictions are off by three orders of magnitude.
  napkin_math: |
    **Assumptions & Constraints:**
    - Jetson Orin: 275 TOPS INT8.
    - Quantized TensorRT graph before postprocessing: 4 GOPs INT8.
    - Excludes memory traffic, kernel launch overhead, box decode, and NMS/plugin execution.

    **Calculations:**
    - 4 GOPs = 4 * 10^9 operations.
    - 275 TOPS = 275 * 10^12 operations/second.
    - Ideal compute-only time = (4 * 10^9) / (275 * 10^12) = 1.4545 * 10^-5 seconds = 0.0145 ms.
    - Practical detector latency must be measured after TensorRT build because NMS plugins, memory bandwidth, and launch overhead can dominate this lower bound.

    **Conclusion & Interpretation:**
    - **Result: 0.0145 ms compute-only lower bound.** This is not an end-to-end latency prediction for the exported detector; validate the TensorRT engine including the NMS/plugin path.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
