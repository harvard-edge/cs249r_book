schema_version: '1.0'
id: edge-0028
track: edge
level: L5
zone: specification
topic: data-pipeline-engineering
competency_area: data
bloom_level: evaluate
phase: training
title: The ROS 2 IPC Overhead
scenario: You build an autonomous driving stack using ROS 2. Node A captures a 4K frame and publishes it. Node B subscribes to the frame, runs inference, and publishes bounding boxes. The ML inference takes 20ms. However, the end-to-end latency from Node A to Node B is 55ms.
question: What is ROS 2 doing that costs 35ms, and how do you fix it?
details:
  realistic_solution: Eliminate the Data Distribution Service (DDS) serialization overhead by implementing ROS 2 Intra-Process Communication (IPC) or deploying a zero-copy transport like `ros2_shared_memory`. This paradigm shift passes lightweight memory pointers between composable nodes instead of executing massive, synchronous memory copies across the CPU.
  common_mistake: |
    **The Pitfall:** Blaming the network layer (UDP/DDS) for the latency bottleneck.
    **The Rationale:** Candidates know DDS utilizes UDP, wrongly assuming that loopback packet routing is the culprit rather than memory serialization.
    **The Consequence:** Wasting time proposing arcane network optimizations while the actual CPU-bound memory copy operation continues to stall the pipeline.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 4K Image footprint: 3840 * 2160 * 3 = 24.8 MB
       - Available Bandwidth: 25 GB/s
       - Unexplained latency gap: 35ms
    2. **Calculations:**
       - Standard ROS DDS memory traffic (serialize + deserialize) = 24.8 MB * 2 = 50 MB
       - Theoretical memory copy time = 50 MB / 25 GB/s = ~2 ms
       - CPU serialization logic overhead = 35 ms - 2 ms = 33 ms
    3. **Conclusion & Interpretation:**
       - **Result: CPU-Bound. Deep serialization protocols completely override actual memory bandwidth speeds.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED