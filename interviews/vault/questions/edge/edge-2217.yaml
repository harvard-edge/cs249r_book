schema_version: '1.0'
id: edge-2217
track: edge
level: L5
zone: optimization
topic: autograd-computational-graphs
competency_area: optimization
bloom_level: evaluate
phase: training
title: Reducing activation memory for on-device fine-tuning on Jetson Orin
scenario: You need to perform on-device fine-tuning of a MobileNetV3-Large model on
  Jetson Orin (275 TOPS, 32GB, 60W) using only local sensor data. The full fine-tuning
  run OOMs at batch=8 even though the model is only 5.4M parameters (21MB in FP32).
  You need to fit within 2GB RAM to leave headroom for the OS and other processes.
question: What is the FP32 activation memory for a batch=8, 96-channel, 28×28 feature
  map, and why is 7.2 MB too high?
details:
  realistic_solution: 'Activation size: 8 × 96 × 28² × 4B = 2.4MB (not 7.2MB which
    is 3× too large).'
  common_mistake: 'Assuming that a 21MB model means fine-tuning will use ~63MB (parameters
    + gradients + Adam states). Ignoring activation memory: for MobileNetV3-Large
    at input 224×224×3, batch=8, intermediate feature maps across all layers sum to
    hundreds of MB — far more than the parameters.'
  napkin_math: 'Activation memory: batch=8, channels=96, spatial=28×28, float32. Size
    = 8 × 96 × 28 × 28 × 4B = 8 × 96 × 784 × 4 = 8 × 96 × 3136 = 2,408,448 bytes ≈
    2.4MB (not 7.2MB which would require 3× more channels or float96).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Calculation error: ''8x96x28x28x4 = 7.2MB''. 8 * 96 * 28 * 28 * 4 = 2,408,448 bytes
  (approx 2.4MB). The stated 7.2MB is exactly 3x the actual value.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
