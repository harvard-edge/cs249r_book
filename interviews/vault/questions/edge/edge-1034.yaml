schema_version: '1.0'
id: 'edge-1034'
track: 'edge'
level: 'L4'
zone: 'implement'
topic: 'dataset-curation'
competency_area: 'data'
bloom_level: 'apply'
phase: 'training'
title: 'Dataset Curation: Implement On-Device Data Augmentation Budget for Jetson Orin'
scenario: 'Data augmentation for online learning is being implemented on a Jetson Orin. The model trains on 64 images/batch at 224x224. The augmentation pipeline must complete within 10ms per batch to not bottleneck the 100ms training step.'
question: 'How would you implement augmentation for 64-image batches and verify it stays under the 10 ms budget?'
details:
  realistic_solution: |
    Utilize hardware-accelerated augmentation frameworks like NVIDIA DALI or torchvision with CUDA backends. These bypass the CPU bottleneck by leveraging the Orin's Ampere GPU to perform vectorized transformations in parallel, easily satisfying the 10 ms batch budget.
  common_mistake: |
    **The Pitfall:** Running augmentation on the CPU while the GPU is idle waiting.
    **The Rationale:** Standard tutorials often demonstrate augmentation using CPU libraries (PIL or standard torchvision), which engineers copy-paste without considering edge CPU bottlenecks.
    **The Consequence:** The CPU limits the pipeline well over the 10 ms budget, leaving the powerful GPU starving and stalling overall training.
  napkin_math: |
    **Assumptions & Constraints:**
    - 64 images per batch, 224x224 resolution.
    - 10 ms target budget.

    **Calculations:**
    - CPU Resize: 0.2 ms/image * 64 = 12.8 ms (Fails budget).
    - GPU Transfer: (64 * 224 * 224 * 3 bytes) = 9.63 MB. At 68 GB/s = 0.14 ms.
    - GPU Augmentation Ops: Vectorized transforms ≈ 2 ms.

    **Conclusion & Interpretation:**
    - **Result: ~2.1 ms Total Latency (Compute-Bound).** Using GPU-accelerated pipelines brings the total latency comfortably below the 10 ms deadline.
status: 'published'
provenance: 'imported'
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: 'OK'
validation_date: '2026-04-01'
validation_model: 'claude-sonnet-4-6'
math_verified: true
math_status: 'CORRECT'
math_date: '2026-04-03'
math_model: 'gemini-3.1-pro-preview'
human_reviewed:
  status: 'not-reviewed'
  by: null
  date: null
  notes: null