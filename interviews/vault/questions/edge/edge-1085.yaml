schema_version: '1.0'
id: edge-1085
track: edge
level: L4
zone: design
topic: model-size-estimation
competency_area: memory
bloom_level: create
phase: both
title: 'Model Size Estimation: Design Memory Layout for Multi-Model Edge Vision System'
scenario: 'Design the memory layout for an edge vision system on Jetson Orin (32GB
  LPDDR5, 204 GB/s) running 4 concurrent models: face detection (10M params INT8),
  license plate OCR (8M params INT8), crowd density estimation (15M params FP16),
  and anomaly detection (20M params FP16). All models must be resident in memory simultaneously
  for <5ms switching latency.'
question: How would you lay out memory so all four Jetson Orin vision models stay
  resident and switch in under 5ms?
details:
  realistic_solution: 'Weights: face 10MB + OCR 8MB + crowd 30MB + anomaly 40MB =
    88MB. TensorRT engines (1.3× overhead): 10×1.3+8×1.3+30×1.3+40×1.3=114.4MB. Working
    activation memory (largest activation, e.g., anomaly at 640x640): ~104MB peak.
    Total resident: 114.4+104 = 218.4MB of 32GB LPDDR5 = 0.68%. Trivially fits.
    Switching: engines already in memory, switch = change inference stream. Latency:
    ~0.5ms (CUDA context switch). Well under 5ms.'
  common_mistake: Loading models on-demand from NVMe (reducing memory pressure but
    adding 10-50ms load time), when the problem specifies <5ms switching — models
    must be pre-loaded.
  napkin_math: 'Weights: 10+8+30+40=88MB (INT8 for first two, FP16 for latter). TensorRT:
    ×1.3=114MB. Max activation (anomaly 20M FP16 at 640×640 input): 640×640×128 channels
    × 2B = 104MB peak. But pipelining: only 1 model infers at a time → 1 activation
    buffer. Total: 114+104=218MB of 32GB (0.68%). 31.78GB free for camera buffers,
    OS, other apps.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
