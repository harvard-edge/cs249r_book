schema_version: '1.0'
id: edge-0559
track: edge
level: L5
zone: specification
topic: roofline-analysis
competency_area: compute
bloom_level: evaluate
phase: both
title: The Dataflow vs GPU Roofline
scenario: Your team is choosing between a Hailo-8 (dataflow architecture) and a Jetson Orin NX (GPU architecture) for a drone running YOLOv8-S. The Orin has 4× the TOPS. But in your benchmarks, the Hailo runs the model at 800 FPS while the Orin only hits 1000 FPS.
question: "Why is the 4× TOPS advantage only yielding a 1.25× speedup?"
details:
  realistic_solution: The Hailo-8 is a dataflow architecture — it maps the entire model graph onto a spatial pipeline of physical compute units, flowing activations through on-chip buffers without touching external DRAM. The Orin NX is a GPU that executes layers sequentially, reading weights and activations from DRAM. For YOLOv8-S (arithmetic intensity ~516 Ops/Byte), the Orin is memory-bandwidth bound. The dataflow architecture changes the roofline itself, eliminating the memory wall for this workload.
  common_mistake: |
    **The Pitfall:** Blaming unoptimized drivers or flawed benchmarks for performance discrepancies.
    **The Rationale:** Engineers default to software explanations rather than evaluating the structural differences between Von Neumann (GPU) and Dataflow architectures under memory constraints.
    **The Consequence:** Rejecting the more power-efficient dataflow chip in favor of a theoretical compute advantage that cannot be physically realized.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Hailo-8: 26 TOPS peak, 2.5W.
       - Orin NX: 100 TOPS peak, 15W, 102.4 GB/s LPDDR5.
       - YOLOv8-S Arithmetic Intensity: 516 Ops/Byte.
    2. **Calculations:**
       - Orin effective TOPS limit = 516 Ops/Byte * 102.4 GB/s = 52.8 TOPS maximum.
       - To hit 1000 FPS, Orin utilizes ~28.4 effective TOPS (28.4% of 100 TOPS).
       - Hailo-8 hits 800 FPS utilizing ~22.7 effective TOPS (87% of 26 TOPS).
       - Hailo-8 power efficiency = 800 FPS / 2.5W = 320 FPS/W.
       - Orin NX power efficiency = 1000 FPS / 15W = 66.6 FPS/W.
    3. **Conclusion & Interpretation:**
       - **Result: 4.8× more power-efficient on Hailo** (Orin is Memory-Bandwidth Bound).
  options:
  - The Hailo-8 is a dataflow architecture — it maps the entire model graph onto a spatial pipeline of physical compute units. Activations flow between stages through on-chip buffers without ever touching external DRAM. The Orin NX is a GPU — it executes layers sequentially, reading weights and activations from LPDDR5 between each layer.
  - The Orin's drivers aren't optimized yet, or the Hailo benchmark is wrong.
  - If the inference process is still running (visible in ps/top), the system is healthy and no recovery action is needed.
  - Operator fusion primarily reduces compute time by eliminating redundant arithmetic operations between layers.
  correct_index: 0
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: edge-chain-168
  position: 3
- id: edge-chain-182
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
