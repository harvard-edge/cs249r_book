schema_version: '1.0'
id: edge-1276
track: edge
level: L5
zone: design
topic: operator-scheduling
competency_area: optimization
bloom_level: evaluate
phase: inference
title: Operator Scheduling Architecture on Cloud AI 100
scenario: Design the operator scheduling architecture for deploying a multi-modal perception pipeline on an on-premise Qualcomm Cloud AI 100 inference accelerator (32 GB LPDDR4x, 400 TOPS INT8, 75W). The pipeline consists of several concurrent models that share the same accelerator.
question: How would you statically schedule concurrent models on Cloud AI 100 to saturate 400 TOPS while reusing memory within 32 GB?
details:
  realistic_solution: 'Design an ahead-of-time (AOT) static scheduler using liveness analysis to aggressively reuse memory buffers for intermediate activations. Evaluate the tradeoff between vertical layer fusion (to reduce memory trips) and horizontal parallel execution (to increase utilization), concluding that a predefined memory pool with static offset assignments is essential for deterministic inference.'
  common_mistake: |
    **The Pitfall:** Proposing dynamic scheduling approaches typical of cloud GPUs.
    **The Rationale:** Candidates fail to realize that dynamic scheduling introduces unacceptable runtime overhead on a 75W edge accelerator, and that memory bandwidth will bottleneck before compute.
    **The Consequence:** The system will fail to saturate compute and will exceed the LPDDR4x memory limits.
  napkin_math: |
    - **Memory Allocation:** 3 concurrent models with 4 GB weights each = 12 GB total INT8.
    - **Available Activations:** 32 GB - 12 GB = 20 GB remaining for activations.
    - **Unoptimized Peak:** Unoptimized parallel execution requires 15 GB of peak intermediate activation memory.
    - **Optimization Impact:** Fusing operators reduces intermediate writes by ~40%.
    - **Result:** Peak activation footprint shrinks to **~9 GB**, ensuring the 75W budget is spent on compute rather than DRAM transactions.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
