schema_version: '1.0'
id: edge-0663
track: edge
level: L4
zone: diagnosis
topic: real-time-deadlines
competency_area: memory
bloom_level: analyze
phase: inference
title: The Swap File Latency Cliff
scenario: >-
  You deploy a 4GB model to a Jetson Nano that has exactly 4GB of physical RAM. You enable a 4GB Linux Swap File on the SD card to prevent OOM crashes. The model loads successfully. However, when the inference runs, the latency is completely erratic—sometimes it takes 50ms, sometimes it takes 4,000ms. The CPU and GPU are barely being utilized during the 4,000ms spikes.
question: Why does the Jetson Nano with a 4GB swap file experience erratic 4,000ms latency spikes during inference?
details:
  realistic_solution: 'The erratic latency is caused by Linux swap thrashing. The 4GB model plus the OS, CUDA runtime, and kernel buffers exceed 4GB of physical RAM. Linux silently evicts pages to the SD card swap file. During inference, when a layer''s weights have been swapped out, the GPU stalls while the CPU fetches the page from the sluggish SD card.'
  common_mistake: |
    **The Pitfall:** Blaming slow model load times on the SD card.
    **The Rationale:** Candidates hear "SD card" and assume the model is just slow to initialize into RAM.
    **The Consequence:** They fail to diagnose that the model is already loaded, and the problem is actively thrashing memory *during* the inference forward pass.
  napkin_math: |
    1. **RAM Bandwidth:** GPU Memory Bandwidth is ~25 GB/s.
    2. **SD Card Bandwidth:** Typical SD Card Bandwidth is ~0.025 GB/s (25 MB/s).
    3. **Impact:** Swapping reduces memory throughput by a factor of 1,000x.
    4. **Result:** If a layer takes 4ms to read from RAM, fetching it from the SD card takes `4ms * 1000 = 4,000ms`, perfectly explaining the erratic latency spikes.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
