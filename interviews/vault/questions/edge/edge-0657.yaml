schema_version: '1.0'
id: edge-0657
track: edge
level: L3
zone: fluency
topic: quantization-fundamentals
competency_area: precision
bloom_level: apply
phase: both
title: The Bloated INT8 Model
scenario: You've successfully quantized your FP32 object detection model to INT8 for deployment on an edge device, expecting a 4x reduction in model memory footprint (weights). However, after deployment, you notice the total RAM usage during inference is only reduced by about 2x-2.5x compared to the FP32 version.
question: What explains this discrepancy, and what other components contribute significantly to the overall memory footprint?
details:
  realistic_solution: "While model weights often see a near 4x reduction from FP32 to INT8, the total memory footprint during inference includes much more: intermediate activations (often kept in FP16/FP32), input/output buffers, scratchpad memory for operations like im2col, runtime overhead from the engine itself, and Look-up Tables (LUTs)."
  common_mistake: |
    **The Pitfall:** Assuming "model memory footprint" only refers to weights.
    **The Rationale:** Candidates focus entirely on static weight reduction, assuming that quantization applies uniformly to all data within the inference pipeline.
    **The Consequence:** Misjudging the total system RAM requirements, leading to unexpected Out-Of-Memory (OOM) errors during execution.
  napkin_math: |
    1. **Weights (FP32 vs INT8):** 25 MB / 4 = 6.25 MB.
    2. **Peak Activations:** A 14x14x1024 layer in FP32 takes 14*14*1024*4 = ~0.8 MB.
    3. **Runtime Overhead:** Engine and scratchpad add an irreducible ~10-15 MB.
    4. **Total Comparison:** FP32 (25MB weights + 15MB overhead = 40MB) vs INT8 (6.25MB weights + 15MB overhead = 21.25MB).
    **Result: 40MB / 21.25MB is a ~1.88x reduction, not the expected 4x.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
