schema_version: '1.0'
id: edge-2019
track: edge
level: L4
zone: analyze
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: both
title: Analyzing Performance Degradation in Edge ML Model Rollouts
scenario: >-
  You are deploying a new computer vision model (v2) to a fleet of NVIDIA Jetson Orin devices (each with 32 GB LPDDR5, 275 TOPS INT8, and a 60W power envelope). During a canary deployment, you observe a significant drop in inferencing throughput (60 FPS to 30 FPS) and increased P99 latency (50ms to 150ms) compared to the v1 model. The v2 model has 10% more parameters and uses a more aggressive 4-bit quantization scheme.
question: How would you diagnose the Jetson Orin canary drop from 60 to 30 FPS and P99 jump from 50 to 150 ms, and which metrics would you correlate?
details:
  realistic_solution: 'Implement a phased diagnostic approach: 1. Collect granular device-level metrics (CPU/GPU utilization, memory bandwidth, temperature, power draw) and compare against v1 baselines. 2. Profile inference on-device using NVIDIA Nsight Systems to identify bottlenecks (e.g., 4-bit unpacking overhead, memory access patterns). 3. Conduct targeted micro-benchmarks for the 4-bit inference kernel in isolation.'
  common_mistake: |
    **The Pitfall:** Assuming model complexity or quantization inherently improves performance without profiling the specific hardware implementation.
    **The Rationale:** Candidates assume 4-bit quantization is "smaller and faster" than INT8 without considering if the hardware natively accelerates 4-bit ops.
    **The Consequence:** They overlook critical edge resource contention (like memory bandwidth saturation or CPU unpacking overhead) and fail to isolate the true systemic bottleneck.
  napkin_math: |
    1. **v1 Baseline:** 60 FPS (~16.7ms per frame), 50ms P99 latency.
    2. **v2 Canary:** 30 FPS (~33.3ms per frame), 150ms P99 latency.
    3. **Impact:** Throughput halved, P99 latency tripled.
    4. **Analysis:** The Orin natively accelerates INT8 (275 TOPS) but lacks native 4-bit tensor cores. If 4-bit unpacking requires generic CUDA core processing, it severely limits effective utilization of the TOPS.
    5. **Result:** A 10% parameter increase cannot cause a 50% throughput drop; the bottleneck is the **unaccelerated 4-bit software unpacking overhead** saturating memory bandwidth or ALU cycles.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The napkin math section is incomplete and ends abruptly.
- The reasoning_competency, knowledge_area, and mode fields are all set to 'None',
  which should be specified for this question.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
