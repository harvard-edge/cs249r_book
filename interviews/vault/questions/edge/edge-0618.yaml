schema_version: '1.0'
id: edge-0618
track: edge
level: L6+
zone: mastery
topic: real-time-deadlines
competency_area: compute
bloom_level: create
phase: inference
title: The 4D Radar Fusion Bottleneck
scenario: >-
  Your self-driving car's perception system runs on a Jetson AGX Orin (60W configuration), fusing cameras and 3D LiDAR within a 33ms latency budget. A new 4D Radar model (sparse point cloud network requiring 8 TOPS) is added to the Orin GPU. Power jumps to 58W (safe), but latency hits 55ms, causing missed frames. Profiling shows the new radar model's arithmetic intensity is only 200 Ops/Byte.
question: What heterogeneous compute architecture would meet the 33ms/60W constraints, and which workloads would you place on each unit?
details:
  realistic_solution: 'The 4D Radar model is severely memory-bound on the Orin GPU because its arithmetic intensity is too low to saturate the GPU''s Tensor Cores. Adding a dedicated DSP or sparse-optimized accelerator with high internal SRAM bandwidth (e.g., 500 GB/s scratchpad) specifically for the sparse radar workload removes the memory bottleneck, preserving the GPU for dense camera/LiDAR fusion.'
  common_mistake: |
    **The Pitfall:** Throwing a bigger, more powerful monolithic GPU at the problem.
    **The Rationale:** Candidates see an 8 TOPS model missing deadlines and assume the system simply lacks raw compute power.
    **The Consequence:** This violates strict automotive power/thermal constraints (a 700W server GPU isn't viable) and fails to address the root cause: the workload is memory-bound, so adding more TFLOPS will not improve latency.
  napkin_math: |
    1. **Radar AI:** 200 Ops/Byte.
    2. **Orin Ridge Point:** `275 TOPS / 204.8 GB/s ≈ 1342 Ops/Byte`.
    3. **Bottleneck:** Since `200 << 1342`, the radar model is deeply memory-bound.
    4. **Effective Compute:** `204.8 GB/s * 200 Ops/Byte = 40.9 TOPS` max effective throughput for the entire GPU, which is actively shared with cameras/LiDAR.
    5. **Result:** Offloading to a dedicated DSP with high SRAM bandwidth isolates the sparse workload and resolves the memory bandwidth collision.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
