schema_version: '1.0'
id: edge-1470
track: edge
level: L5
zone: design
topic: extreme-quantization
competency_area: precision
bloom_level: evaluate
phase: both
title: Sub-4-bit Weight Streaming Architecture for Hailo-8
scenario: Your team is deploying a heavy vision transformer to an edge accelerator for real-time drone surveillance. The accelerator utilizes a dataflow architecture with no local DRAM, meaning all weights and activations must stream continuously from the host system's memory. The model's footprint exceeds the host's strict memory and PCIe bandwidth limits if kept in standard INT8.
question: How would you use sub-4-bit host-side weight storage while still feeding INT8 weights efficiently to the accelerator?
details:
  realistic_solution: A robust design stores the model in a 4-bit or 3-bit compressed format on the host to meet capacity and bandwidth constraints. A dedicated host-side pipeline using SIMD/NEON instructions performs asynchronous, batched dequantization to INT8 into a ring buffer. The DMA controller then streams these INT8 weights directly to the accelerator.
  common_mistake: |
    **The Pitfall:** Proposing advanced 3-bit AWQ but failing to account for severe CPU latency during on-the-fly dequantization.
    **The Rationale:** Candidates ignore the strict INT8 dataflow engine limits and assume the accelerator can natively perform mixed-precision MACs.
    **The Consequence:** The host CPU cannot dequantize weights fast enough, starving the compute engine and missing real-time deadlines.
  napkin_math: |
    **Assumptions & Constraints:**
    - 80M parameter model.
    - Target 30 FPS pipeline.
    - PCIe limit: 2 GB/s.
    
    **Calculations:**
    - INT8 requires 80MB. Pipeline bandwidth: 80MB * 30 = 2.4 GB/s (Exceeds PCIe limit).
    - 4-bit quantization reduces footprint to 40MB, dropping PCIe bandwidth to 1.2 GB/s.
    
    **Conclusion & Interpretation:**
    - **Result: 1.2 GB/s (Host-Compute Bound)**. The host CPU must dequantize 40MB to 80MB INT8 per frame at >2.4 GB/s throughput to keep the accelerator fully fed.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
