schema_version: '1.0'
id: edge-1241
track: edge
level: L3
zone: analyze
topic: neural-architecture-search
competency_area: architecture
bloom_level: analyze
phase: training
title: Hailo-8 Dataflow Topology Memory Bottleneck
scenario: A hardware-aware NAS yielded two INT8 object detection models for a Hailo-8 edge device (dataflow architecture, no local DRAM). Model X requires 15 GOPs with heavy branching. Model Y requires 20 GOPs with a strictly sequential topology.
question: Why does the lower-compute Model X run at 150 FPS while the higher-compute Model Y reaches 600 FPS on the Hailo-8?
details:
  realistic_solution: Because the Hailo-8 is a dataflow accelerator lacking local DRAM, it relies on mapping the entire network graph directly onto its distributed SRAM. A sequential model can be pipelined efficiently. Model X's heavy branching exhausts internal SRAM, forcing it to spill intermediate activations over the PCIe bus, causing severe bandwidth bottlenecks.
  common_mistake: |
    **The Pitfall:** Assuming that fewer FLOPs/GOPs mathematically guarantees lower latency on dataflow accelerators.
    **The Rationale:** Candidates rely on standard CPU/GPU intuition, failing to account for the unique memory topology of specialized NPUs that lack local DRAM.
    **The Consequence:** Selecting a theoretically "efficient" model that heavily thrashes the host PCIe bus because intermediate activations spill out of the chip's internal SRAM.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Peak Compute: 26,000 GOPs.
       - PCIe Bus: 4 GB/s.
       - Model X: 15 GOPs, heavy branching (spills 4MB activation).
       - Model Y: 20 GOPs, sequential.
    2. **Calculations:**
       - Compute Bound Model X: 26,000 / 15 = 1,733 FPS.
       - Compute Bound Model Y: 26,000 / 20 = 1,300 FPS.
       - Model X Spilling Latency: 8 MB (R/W) / 4000 MB/s = 2.0ms per inference.
       - Model X Throughput Ceiling: 1000ms / 2.0ms = 500 FPS limit (degrades to 150 FPS due to sync).
    3. **Conclusion & Interpretation:**
       - **Result: Model X achieves 150 FPS due to PCIe spilling (Memory Bandwidth-Bound).** Model Y avoids spilling and hits 600 FPS.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
