schema_version: '1.0'
id: edge-1266
track: edge
level: L5
zone: design
topic: gpu-compute-architecture
competency_area: compute
bloom_level: evaluate
phase: both
title: 'Architecting a Real-Time Vision Pipeline on Jetson Orin'
scenario: 'You are tasked with designing a real-time multi-camera perception system for an autonomous guided vehicle using an NVIDIA Jetson Orin. The system must process four 4K camera streams at 30 FPS, running object detection and semantic segmentation.'
question: 'How would you partition object detection and segmentation across Jetson Orin''s Ampere GPU and DLA while managing 32 GB LPDDR5 bandwidth and the 60W TDP?'
details:
  realistic_solution: 'The optimal design maps the dense, standard convolutions of the object detector to the DLA, freeing up the Ampere GPU''s Tensor Cores and CUDA cores for the complex, non-standard operations of the segmentation model or post-processing. Memory traffic is minimized by utilizing zero-copy memory buffers, preventing unnecessary host-to-device copies, and ensuring memory accesses are coalesced for the Ampere architecture.'
  common_mistake: |
    **The Pitfall:** Defaulting to running all neural network inference on the Ampere GPU.
    **The Rationale:** Candidates are familiar with standard GPUs but ignore the Deep Learning Accelerator (DLA), leading to underutilization of the total 275 TOPS compute pool.
    **The Consequence:** The system bottlenecks on the GPU or exceeds the 60W TDP by running the GPU at maximum clock states, while the DLA sits completely idle.
  napkin_math: |
    **Assumptions & Constraints:**
    - Raw video: 4 cameras * 3840 * 2160 pixels * 3 bytes (RGB) * 30 FPS.
    - Compute needed: 4 streams * 30 FPS * 0.5 TOPS/frame.
    - Hardware limits: 275 TOPS INT8 total, 60W TDP limit.

    **Calculations:**
    - Raw video bandwidth: 4 * 3840 * 2160 * 3 * 30 = ~2.98 GB/s.
    - Compute needed: 4 * 30 * 0.5 = 60 TOPS.
    - Offloading 30 TOPS of dense convolutions (detection) to the DLA allows the Ampere GPU to only handle 30 TOPS (segmentation).

    **Conclusion & Interpretation:**
    - **Result: 30 TOPS GPU / 30 TOPS DLA (Power-Bound)**. Parallel execution allows both chips to run at lower clock states, cleanly preserving the 60W power budget.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null