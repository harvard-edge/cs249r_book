schema_version: '1.0'
id: edge-2431
track: edge
level: L4
zone: analyze
topic: data-pipeline-engineering
competency_area: data
bloom_level: analyze
phase: both
title: Jetson Zero-Copy Pipelines
scenario: A Jetson AGX Orin passes decoded 1080p camera frames from GStreamer CPU buffers into a TensorRT engine, causing high memory usage.
question: Analyze why standard CPU memory buffers are highly inefficient on this platform and name a better primitive.
details:
  realistic_solution: Standard buffers force the unified LPDDR5 memory to copy data redundantly from the hardware decoder to CPU memory, and then back to GPU-accessible memory. Utilizing zero-copy hardware primitives like NvSciBuf or EGLStreams passes memory pointers instead of copying bytes, drastically reducing memory bus utilization.
  common_mistake: |
    **The Pitfall:** Assuming the Jetson requires traditional host-to-device transfers.
    **The Rationale:** Candidates treat the Jetson like a desktop CPU with a discrete PCIe GPU, failing to recognize its unified memory architecture.
    **The Consequence:** Implementing explicit `cudaMemcpy` operations that pointlessly duplicate data in RAM, saturating the memory bus and killing real-time performance.
  napkin_math: |
    * Standard memory buffer: Copies 6.22 MB (1080p RGB) per frame across the bus.
    * Zero-copy buffer (NvSciBuf/EGLStreams): Passes a single 8-byte memory pointer.
    * Bandwidth saved: **~6.22 MB** of redundant copying eliminated per frame.
status: published
provenance: llm-draft
expected_time_minutes: 8
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-recall-L4
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
