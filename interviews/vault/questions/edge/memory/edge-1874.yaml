schema_version: '1.0'
id: edge-1874
track: edge
level: L5
zone: evaluation
topic: dma-data-movement
competency_area: memory
bloom_level: analyze
phase: both
title: 'Edge Inference Data Movement: Coral vs. CPU/GPU'
scenario: A company is developing a real-time object detection system for an industrial edge application. They are evaluating two embedded system architectures for deploying an INT8 quantized model. Architecture A is an ARM Cortex-A based SoC with an integrated GPU. Architecture B is a system utilizing a USB/PCIe attached Edge TPU paired with a host ARM CPU. The application continuously processes 1080p video streams at 30fps.
question: How do the integrated-GPU SoC and attached Edge TPU differ in DMA, zero-copy, and host-device data movement for high-resolution video inference?
details:
  realistic_solution: |
    Architecture A (Integrated SoC) leverages a Unified Memory Architecture (UMA) allowing Zero-Copy access: the host CPU pins the video buffer in RAM, and the integrated GPU accesses it via shared physical pointers, eliminating data movement overhead entirely. Architecture B (Attached TPU) requires explicit host-to-device memory copies over a constrained bus (USB/PCIe). To prevent the discrete accelerator from starving, Architecture B mandates pinned memory allocation on the host and multi-threaded double-buffering to hide the DMA transfer latency.
  common_mistake: |
    **The Pitfall:** Assuming the attached TPU operates autonomously without host CPU intervention.
    **The Rationale:** Focusing solely on the accelerator's compute TOPS while ignoring the data orchestration responsibilities of the host.
    **The Consequence:** Severe latency bottlenecks as the host CPU chokes on continuous memory copies and I/O scheduling, starving the accelerator of data.
  napkin_math: |
    **Assumptions & Constraints:**
    - Video Stream: 1080p at 30 fps (1920 * 1080 * 3 bytes).
    - USB 3.0 Effective Bandwidth: ~400 MB/s.

    **Calculations:**
    - Uncompressed Data Rate: 1920 * 1080 * 3 * 30 = 178 MB/s.
    - Architecture A Bus Overhead: 0 MB/s (Zero-Copy).
    - Architecture B Bus Overhead: 178 MB/s / 400 MB/s = ~45% of total USB bandwidth.

    **Conclusion & Interpretation:**
    - **Result: Attached TPU consumes 45% of bus bandwidth just for ingress (I/O-Bound)**. Host-to-Device Transfer.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
