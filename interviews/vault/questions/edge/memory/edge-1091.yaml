schema_version: '1.0'
id: edge-1091
track: edge
level: L4
zone: optimization
topic: model-size-estimation
competency_area: memory
bloom_level: evaluate
phase: both
title: 'Model Size Estimation: Diagnose Memory Pressure Causing Swapping on Jetson Orin'
scenario: A Jetson Orin deployment running 3 models (total 28GB weights FP16) on 32GB LPDDR5 shows 50% performance degradation after 2 hours. htop shows swap usage at 1GB (to NVMe SSD at 3.5 GB/s). Diagnose and quantify the fix.
question: Why does the Jetson Orin with 28GB FP16 weights on 32GB LPDDR5 start swapping and slow down 50%, and what quantified fix eliminates it?
details:
  realistic_solution: '28GB weights + 3GB OS + 2GB CUDA = 33GB > 32GB → swapping expected. Swap read: 1GB / 3.5GB/s = 0.286s added latency every time swapped pages accessed. At 50% degradation: swap IO is constant. Fix: (1) Quantize largest model (e.g., 14GB) from FP16 to INT8: saves 7GB. New total: 21+3+2=26GB << 32GB. No swap. (2) Alternatively, use model offloading: keep only active model in GPU memory, others on NVMe — but this adds 100ms switch time. Fix (1) with INT8 is best.'
  common_mistake: |
    **The Pitfall:** Attempting to reduce model precision immediately without first isolating the system-level memory bottleneck.
    **The Rationale:** Candidates see OOM issues and blindly apply quantization, ignoring potential OS-level memory fragmentation or swap thrashing.
    **The Consequence:** Missing the root cause of the 50% latency degradation, which is NVMe swap I/O latency.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Total LPDDR5: 32 GB.
       - Current usage: 28 GB weights + 3 GB OS + 2 GB CUDA = 33 GB.
       - Overflow: 1 GB swapped to NVMe (3.5 GB/s read).
    2. **Calculations:**
       - NVMe Swap Latency = 1 GB / 3.5 GB/s = 0.286s (286 ms) per swap event.
       - With 1 swap per 2s, average latency increase = 143 ms.
       - Quantizing the largest model (14 GB) from FP16 to INT8 saves 7 GB.
       - New memory footprint = 21 GB + 5 GB = 26 GB.
    3. **Conclusion & Interpretation:**
       - **Result: 26 GB footprint.** By saving 7 GB, the system avoids swap entirely, eliminating the **I/O-Latency-Bound** performance drop.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
