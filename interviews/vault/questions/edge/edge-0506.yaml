schema_version: '1.0'
id: edge-0506
track: edge
level: L3
zone: diagnosis
topic: neural-architecture-search
competency_area: architecture
bloom_level: apply
phase: training
title: The Self-Attention Latency Trap
scenario: Your perception model, a small Vision Transformer (ViT), is running on a Jetson AGX Orin but is taking 45ms per inference, missing the critical 33ms 'hard real-time' deadline for obstacle detection.
question: Which of the following strategies would you apply to solve this latency crisis while best preserving model quality?
details:
  realistic_solution: 'Replace the ViT with a CNN built using depthwise separable convolutions. ViT''s quadratic self-attention results in random-access memory patterns and low arithmetic intensity, making it severely memory-bandwidth-bound. CNNs have high spatial locality.'
  common_mistake: |
    **The Pitfall:** Believing that FLOPs are the only metric that matters for latency.
    **The Rationale:** Engineers shrink models with inefficient memory patterns instead of switching to architectures with higher arithmetic intensity.
    **The Consequence:** The model remains memory-bandwidth-bound and fails to utilize the specialized tensor cores efficiently.
  napkin_math: |
    **Assumptions & Constraints:**
    - Hardware: Orin (275 TOPS, 204.8 GB/s bandwidth).
    - ViT attention: O(N² * D) complexity, non-local access.

    **Calculations:**
    - Standard 3x3 conv (256 in/out): 589,824 MACs.
    - Depthwise separable 3x3: 67,840 MACs.
    - Ops reduction: ~8.7x.

    **Conclusion & Interpretation:**
    - **Result: Compute-Bound Architecture (Compute-Bound)**. Depthwise convolutions dramatically increase data locality and arithmetic intensity, bringing latency under 33ms.
  options:
  - Apply aggressive INT4 quantization to the existing ViT.
  - Use Neural Architecture Search (NAS) to find a smaller ViT configuration.
  - Replace the ViT with a CNN built using depthwise separable convolutions.
  - Implement a Mixture-of-Experts (MoE) layer.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
