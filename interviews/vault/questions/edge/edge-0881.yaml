schema_version: '1.0'
id: edge-0881
track: edge
level: L6+
zone: diagnosis
topic: vram-budgeting
competency_area: memory
bloom_level: analyze
phase: both
title: Paged Attention Fragmentation Stalls
scenario: You deploy a 4-bit quantized 7B LLM on a device with 8GB of unified memory. PagedAttention is enabled to handle concurrent requests from smart home sensors. Page size is set to 512 tokens. Under moderate load (8 concurrent sensors sending short 50-token queries), the system hits OOM, swaps to eMMC, and generation stalls for over 5 seconds.
question: Why does PagedAttention OOM on 8 short 50-token queries with 512-token pages, and what page size would you choose?
details:
  realistic_solution: Execute a memory allocation analysis to identify severe internal fragmentation within the PagedAttention memory manager. When static block sizes drastically exceed average sequence lengths, KV-cache allocations are wasted. Radically compress the page size configuration (e.g., to 16 or 32 tokens) to align the memory allocation granularity with the actual distribution of user query lengths, immediately resolving the OOM without requiring further model quantization.
  common_mistake: |
    **The Pitfall:** Assuming 8GB of memory is fundamentally insufficient and attempting aggressive re-quantization.
    **The Rationale:** Candidates see an OOM error and immediately jump to shrinking the model weights, ignoring the dynamic memory overhead of the KV cache allocator.
    **The Consequence:** Unnecessary model quality degradation and wasted engineering effort, while the underlying memory fragmentation bug remains active.
  napkin_math: |
    **Assumptions & Constraints:**
    - Device RAM: 8GB (OS+Model leaves ~3GB for KV-cache)
    - 1 Token KV size: ~524 KB
    - Page size: 512 tokens
    - Workload: 8 concurrent requests of 50 tokens each

    **Calculations:**
    - 1 Page Allocation = 512 * 524 KB = ~268 MB
    - Total Allocated for 8 requests = 8 * 268 MB = 2.14 GB
    - Actual Memory Used = 8 * 50 * 524 KB = ~209 MB
    - Wasted Memory = 2.14 GB - 209 MB = 1.93 GB (90% fragmentation waste)

    **Conclusion & Interpretation:**
    - **Result: 1.93 GB Wasted (Memory-Capacity Bound)**. Internal fragmentation from oversized pages consumes 90% of the KV cache budget, triggering OS swapping.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 18
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview