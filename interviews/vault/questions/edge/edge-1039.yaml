schema_version: '1.0'
id: edge-1039
track: edge
level: L4
zone: realization
topic: dataset-curation
competency_area: data
bloom_level: apply
phase: training
title: 'Dataset Curation: Realize Training Data Pipeline for Edge Model Refresh'
scenario: You are designing an end-to-end data pipeline for a monthly model refresh across 1,000 edge devices. Each device collects 1,000 curated images/month. The data must be aggregated, deduplicated, labeled, and used for centralized fine-tuning on 8 cloud GPUs.
question: How would you design the monthly model refresh pipeline from edge collection to cloud fine-tuning?
details:
  realistic_solution: 'Implement a multi-stage pipeline: (1) Edge upload via LTE (extremely fast, ~25s per device). (2) Centralized perceptual hashing and global deduplication to filter the 1M raw images down to a high-signal subset. (3) Active learning scoring to select the top ~50K samples for human labeling. (4) Cloud fine-tuning on 8 H100 GPUs (which takes less than 10 minutes). The primary bottleneck is the human labeling turnaround time, dictating a multi-day SLA.'
  common_mistake: |
    **The Pitfall:** Not accounting for full pipeline latency or global deduplication overhead.
    **The Rationale:** Candidates often assume data simply arrives ready to train and forget about network upload times, or they assume deduplication happens locally on each device.
    **The Consequence:** The pipeline estimate misses major delays like cross-device global index building and labeling turnaround times.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Devices: 1,000. Images/device: 1,000. Total: 1,000,000 images.
       - Image size: 310 KB.
       - Connection: 100 Mbps LTE per device.
    2. **Calculations:**
       - Upload time per device: (1000 * 310 KB) / (100 Mbps / 8) ≈ 24.8 seconds.
       - Fine-tuning dataset (after dedup/filtering): 600K images.
       - Fine-tuning time (8 H100s, batch=512, 85ms/step, 5 epochs): (600,000 * 5) / 512 = 5,859 steps * 0.085s = 498 seconds.
    3. **Conclusion & Interpretation:**
       - **Result: ~7 days end-to-end latency.** The computational workload (upload, dedup, training) is trivial, but human-in-the-loop labeling for active learning dictates the true pipeline turnaround time.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
