schema_version: '1.0'
id: edge-1196
track: edge
level: L5
zone: design
topic: transformer-systems-cost
competency_area: compute
bloom_level: create
phase: both
title: Design Continuous Batching for Edge LLM Serving on Hailo-8
scenario: 'A Hailo-8 edge server handles LLM inference requests for a retail analytics
  application. Requests arrive at 2 req/s (Poisson). Each request has 128-token prompt
  and 64-token response. Hailo-8 running a 3B INT4 model: prefill 128 tokens = 20ms,
  decode per token = 8ms, decode 64 tokens = 512ms. Total per request: 532ms. Design
  a continuous batching strategy to maximize throughput while keeping P99 latency
  < 1000ms for each request.'
question: For the Hailo-8 continuous batching queue, should P99 be estimated from
  waiting time Wq or total sojourn time W?
details:
  realistic_solution: P99 latency ≈ 4.6 × W (mean sojourn time = Wq + 1/μ), not 4.6
    × Wq. Wq excludes the service time component.
  common_mistake: Using static batching where the full batch waits for the longest
    request; continuous batching inserts new requests at decode boundaries, improving
    GPU utilization without increasing individual request latency.
  napkin_math: 'M/M/1 P99 approximation: P99 ≈ -ln(0.01)/μ for sojourn time W, not
    waiting time Wq. W = 1/(μ(1-ρ)) is the mean sojourn time. P99 ≈ 4.6 × W (mean
    sojourn time) where W includes service time. Using Wq (queue wait only) would
    underestimate by excluding service time.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- P99 latency calculation for M/M/1 is conceptually incorrect. It should be ~4.6 *
  average sojourn time (4075ms), not wait + 4.6*service (2036ms).
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
