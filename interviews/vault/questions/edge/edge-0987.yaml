schema_version: '1.0'
id: edge-0987
track: edge
level: L5
zone: realization
topic: graceful-degradation
competency_area: reliability
bloom_level: evaluate
phase: both
title: Edge Fallback Capacity
scenario: An edge gateway normally runs a neural detector at 20 FPS. A fallback rule-based detector can run at 80 FPS but has lower accuracy. During thermal throttling, the neural detector drops to 12 FPS, below the 15 FPS minimum.
question: Can an 80 FPS rule-based fallback restore throughput when the neural detector drops to 12 FPS and the minimum is 15 FPS?
details:
  realistic_solution: Yes. The fallback exceeds the 15 FPS minimum by a large margin, so it can preserve availability while the neural detector cools down. The tradeoff is lower accuracy during degradation, so use it only under thermal or overload conditions.
  common_mistake: |
    **The Pitfall:** Refusing to failover because of accuracy degradation.
    **The Rationale:** Candidates often insist on keeping the primary neural model active, failing to realize that dropping below the minimum throughput SLA constitutes a hard system failure.
    **The Consequence:** Complete loss of real-time availability instead of gracefully degraded functionality.
  napkin_math: |
    1. **Neural Throttle Deficit:** Minimum 15 FPS - Throttled 12 FPS = -3 FPS.
    2. **Fallback Headroom:** Fallback 80 FPS - Minimum 15 FPS = +65 FPS.
    **Result: Fallback easily satisfies the 15 FPS minimum constraint.**
status: draft
provenance: llm-draft
requires_explanation: true
expected_time_minutes: 10
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- iteration-gap-pack
