schema_version: '1.0'
id: edge-1201
track: edge
level: L5
zone: optimization
topic: transformer-systems-cost
competency_area: compute
bloom_level: evaluate
phase: both
title: Optimize KV Cache Quantization for Jetson Orin Memory Budget
scenario: A 7B model on Jetson Orin NX uses 3.5GB for INT4 weights, leaving 4.5GB for KV cache. The team wants to support 5 concurrent requests at 16K context each.
question: Which KV quantization lets 5 concurrent 16K-context requests fit in the 4.5GB KV budget, and what memory does it use?
details:
  realistic_solution: Treat K-INT8/V-INT4 as the best fit under the stated hypothetical quantization result. It uses about 3.93 GB for 5 concurrent 16K-context requests, leaving roughly 0.57 GB of the 4.5 GB KV budget for allocator overhead, scales, padding, and runtime bookkeeping. That headroom is implementation-dependent, so the design should be validated with the actual serving stack rather than described as a guaranteed optimum. The reason to prefer it over uniform INT4 is that keys are usually more accuracy-sensitive than values, while uniform INT8 misses the budget.
  common_mistake: |
    **The Pitfall:** Quantizing both K and V to INT4 uniformly.
    **The Rationale:** Candidates assume uniform compression is the only approach, ignoring the disparate impact of Key vs Value precision on self-attention collapse.
    **The Consequence:** The model fits in memory but exhibits catastrophic perplexity degradation and hallucination at long contexts.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - KV Budget: 4.5 GB.
       - FP16 KV size/token = 131,072 bytes.
       - Workload: 5 requests * 16K tokens = 80K tokens total.
    2. **Calculations:**
       - FP16 Total = 80K * 131,072 = 10.48 GB (OOM).
       - INT8 KV (2x compress) = 10.48 / 2 = 5.24 GB (OOM).
       - K-INT8/V-INT4 (1.5 bytes total per element, 2.66x compress) = 80K * 49,152 = 3.93 GB (Fits).
       - INT4 KV (4x compress) = 10.48 / 4 = 2.62 GB (Fits, poor accuracy).
    3. **Conclusion & Interpretation:**
       - **Result: K-INT8/V-INT4 best fit under the stated assumptions**. The asymmetric 3.93 GB footprint clears the 4.5GB KV budget by about 0.57 GB, but production feasibility depends on allocator overhead, quantization metadata, padding, and the actual accuracy profile.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
