schema_version: '1.0'
id: edge-1845
track: edge
level: L3
zone: analyze
topic: systolic-dataflow
competency_area: compute
bloom_level: analyze
phase: both
title: 'Dataflow and On-chip Memory Utilization'
scenario: 'You are optimizing a real-time object detection pipeline for deployment on an edge dataflow accelerator. A specific, latency-critical 1x1 convolutional layer processing an intermediate feature map is observed to be a bottleneck.'
question: 'How should on-chip SRAM and weight-stationary versus output-stationary dataflows minimize latency for the 1x1 convolution?'
details:
  realistic_solution: 'A realistic solution involves deeply understanding the compiler''s capabilities and physical architecture. For a 1x1 layer, consider whether a weight-stationary dataflow (weights in SRAM, inputs stream) or output-stationary dataflow (outputs in SRAM) is optimal to avoid costly external memory fetches and minimize internal data movement.'
  common_mistake: |
    **The Pitfall:** Optimizing for external DDR memory access as if the hardware were a standard GPU.
    **The Rationale:** Candidates fall back on conventional GPU optimization paradigms, ignoring that dataflow accelerators rely on compiler-orchestrated on-chip SRAM.
    **The Consequence:** Designing inefficient data pipelines that fail to utilize internal SRAM fabric, missing performance potential.
  napkin_math: |
    **Assumptions & Constraints:**
    - Peak performance: 26 TOPS (INT8).
    - Typical power consumption: 2.5W.

    **Calculations:**
    - Peak TOPS/Watt efficiency = 26 TOPS / 2.5W = 10.4 TOPS/Watt.

    **Conclusion & Interpretation:**
    - **Result: 10.4 TOPS/Watt (Compute-Bound)**. The accelerator is highly efficient when data is kept on-chip.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true