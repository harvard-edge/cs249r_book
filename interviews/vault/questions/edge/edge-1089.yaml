schema_version: '1.0'
id: edge-1089
track: edge
level: L5
zone: mastery
topic: model-size-estimation
competency_area: memory
bloom_level: evaluate
phase: both
title: 'Model Size Estimation: Master Full Memory Audit for Edge LLM Deployment on
  Jetson Orin'
scenario: 'Perform a complete memory audit for deploying a 1B-parameter LLM (INT8
  quantized) on Jetson Orin (32GB LPDDR5, 204 GB/s, 60W) for on-device chat inference.
  Include all components: model weights, KV-cache pool, activation buffers, ONNX/TensorRT
  runtime, operating system, camera/sensor processes, and CUDA context overhead. Determine
  maximum sustainable context length and concurrent users.'
question: What is the INT8 KV-cache memory per token for the 1B LLM with 32 layers,
  32 KV heads, and head_dim 64?
details:
  realistic_solution: 'KV cache per token = 2 × num_layers × num_kv_heads × head_dim
    × bytes. For INT8 (1B/element): 2 × 32 × 32 × 64 × 1B = 128KB per token.'
  common_mistake: Forgetting that Jetson Orin runs a full Linux OS (JetPack/Ubuntu)
    consuming 2-4GB RAM at idle, and GPU CUDA context consumes 200-500MB, leaving
    less than the full 32GB for ML.
  napkin_math: 'KV cache per token for edge transformer (32 layers, 32 KV heads, head_dim=64,
    INT8): 32 layers × 2 (K+V) × 32 heads × 64 dim × 1B = 32 × 2 × 32 × 64 × 1 = 131,072
    bytes = 128KB per token (not 256KB which would use 2B per element for INT8).'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 18
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- KV/token calculation mistakenly multiplies by 2 twice for K,V. Correct value for
  1B INT8 is 128KB, not 262KB.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
