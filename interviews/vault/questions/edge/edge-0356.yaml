schema_version: '1.0'
id: edge-0356
track: edge
level: L2
zone: recall
topic: attention-scaling
competency_area: architecture
bloom_level: understand
phase: both
title: The Depthwise Separable Dividend (edge-0356)
scenario: You are optimizing a CNN-based perception model for an autonomous vehicle's edge compute module.
  A key layer is a standard 3x3 convolution with 256 input channels and 512 output channels. To meet the
  latency budget on the Jetson AGX Orin, you are considering replacing this layer with a 3x3 depthwise
  separable convolution. Explain the architectural change and calculate the approximate parameter reduction
  factor for this single layer.
question: "How does a 3x3 depthwise separable convolution change the layer, and what parameter reduction factor do you get for 256 input to 512 output channels?"
details:
  realistic_solution: 'A standard 3x3 convolution performs spatial and cross-channel convolution in one
    step. A depthwise separable convolution splits this into two steps:

    1.  **Depthwise Convolution:** A single 3x3 filter is applied to each of the 256 input channels independently.
    This handles the spatial dimension.

    2.  **Pointwise Convolution:** A 1x1 convolution then combines the outputs of the depthwise step to
    create the 512 output channels. This handles the cross-channel dimension.'
  common_mistake: Engineers often miscalculate the reduction factor by either completely ignoring the
    parameter cost of the 1x1 pointwise convolution, leading to a wildly optimistic estimate, or by incorrectly
    summing the components. Another common error is to simply guess a small constant factor like '2x'
    or '3x' without understanding the relationship between the kernel size and the number of channels.
  napkin_math: "1. **Standard Convolution Parameters:** 3 * 3 * 256 * 512 = 1,179,648. 2. **Depthwise Separable Parameters:** Depthwise (3 * 3 * 256 = 2,304) + Pointwise (1 * 1 * 256 * 512 = 131,072) = 133,376. 3. **Reduction Factor:** 1,179,648 / 133,376 = 8.84x (~9x reduction)."
  options:
  - ~2x reduction. This comes from the ratio of output to input channels (512/256).
  - ~512x reduction. The depthwise step is tiny, so you save by the number of output filters.
  - ~9x reduction. The parameter savings come from factorizing the standard convolution into a spatial
    depthwise step and a cross-channel pointwise step.
  - ~3x reduction. The reduction is proportional to the kernel dimension.
  correct_index: 2
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 2
deletion_reason: Duplicate of edge-depthwise-separable-convolution-the-depthwise-separable-dividend-282
  (same title + track)
chains:
- id: edge-chain-038
  position: 1
validated: false
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The Napkin Math section is incomplete, it lists 'Standard Convolution Parameters:' but does not show
  the actual calculation for the parameters or the parameter reduction factor for depthwise separable
  convolution.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
