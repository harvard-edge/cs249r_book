schema_version: '1.0'
id: edge-0317
track: edge
level: L2
zone: implement
topic: quantization-fundamentals
competency_area: precision
bloom_level: understand
phase: both
title: The Perception Model's Memory Diet (edge-0317)
scenario: You are an engineer on an autonomous vehicle team deploying a LiDAR perception model to a Jetson AGX Orin. A key convolutional layer in your model produces an activation tensor of size 256x256x128. The model currently runs in FP16 precision. To reduce memory pressure, you are considering quantizing the network to INT8.
question: How much memory in MB is saved for the 256x256x128 activation tensor when converting it from FP16 to INT8?
details:
  realistic_solution: |
    The calculation is a straightforward comparison of data type sizes. FP16 (half-precision float) uses 2 bytes per element. INT8 (8-bit integer) uses 1 byte per element. For the same number of elements, INT8 uses exactly half the memory of FP16. The savings are therefore 50% of the original FP16 size.
  common_mistake: |
    **The Pitfall:** Forgetting that quantization applies to activations, not just weights.
    **The Rationale:** Engineers often focus solely on weight reduction and miscalculate the size difference by reporting the final INT8 size instead of the *savings*, or confusing byte-to-bit ratios.
    **The Consequence:** Underestimating actual memory savings, leading to inaccurate memory profiling.
  napkin_math: |
    - Total elements: 256 * 256 * 128 = 8,388,608 elements.
    - FP16 size: 8,388,608 * 2 bytes = 16,777,216 bytes (~16.8 MB).
    - INT8 size: 8,388,608 * 1 byte = 8,388,608 bytes (~8.4 MB).
    - Savings: 16.8 MB - 8.4 MB = **8.4 MB**.
  options:
  - ~16.8 MB. This is the original FP16 size, not the savings.
  - ~4.2 MB. This incorrectly assumes a 4x reduction from FP16.
  - ~8.4 MB. Correct, this is half the original FP16 size.
  - 0 MB. This incorrectly assumes quantization only saves memory on weights, not activations.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
