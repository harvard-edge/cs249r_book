schema_version: '1.0'
id: edge-1683
track: edge
level: L4
zone: optimization
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: both
title: Canary Rollout Context Thrashing on AI 100
scenario: You are executing a 50/50 canary rollout of an upgraded INT8 vision model on an on-premise server equipped with a single Qualcomm Cloud AI 100. Both the 12GB legacy model and the 12GB canary model fit comfortably within the 32 GB LPDDR4x memory. However, during the rollout phase, overall system throughput drops dramatically despite utilizing only a fraction of the 400 TOPS INT8 capacity, and power draw spikes near the 75W TDP limit.
question: "What is the structural bottleneck of this traffic splitting approach on the Cloud AI 100, and how would you quantify a fix?"
details:
  realistic_solution: The bottleneck is memory bandwidth saturation from alternating model executions, which thrashes the accelerator's internal SRAM caches. The solution is to optimize the progressive rollout strategy by implementing request batching at the traffic router level, routing chunks of requests to Model A then Model B, amortizing the cost of loading model weights from LPDDR4x into the active compute tiles.
  common_mistake: |
    **The Pitfall:** Assuming the bottleneck is compute-bound and suggesting further quantization.
    **The Rationale:** Candidates focus on the models rather than the infrastructure handling the traffic split.
    **The Consequence:** They fail to solve the memory bandwidth thrashing, leaving the system starved for data and wasting compute capacity.
  napkin_math: |
    *   **Context Switch Penalty:** Swapping a 12 GB model into execution units takes 12 GB / 136 GB/s = 88 ms.
    *   **Naive Throughput:** A round-robin 50/50 split adds an 88 ms penalty per inference, limiting throughput to **~11 FPS**.
    *   **Amortization:** Batching 50 requests per model amortizes the penalty to 88 ms / 50 = 1.76 ms per inference.
    *   **Result:** This unblocks the INT8 engine, **increasing throughput by nearly 50x** while staying within the power budget.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
