schema_version: '1.0'
id: edge-0479
track: edge
level: L3
zone: fluency
topic: real-time-deadlines
competency_area: compute
bloom_level: apply
phase: inference
title: The Autonomous Driving Perception Bottleneck
scenario: 'You are an ML Systems Engineer at an AV company, trying to optimize a new YOLO-like perception model on a Jetson AGX Orin. Profiling reveals that a specific depthwise separable convolution layer is a major performance bottleneck, preventing the pipeline from meeting its 33ms deadline.'
question: Using roofline analysis, is the depthwise separable convolution layer on Orin compute-bound or memory-bound?
details:
  realistic_solution: 'Calculate the layer''s Arithmetic Intensity (AI) and compare it to the hardware''s capabilities via Nsight Systems profiling. The AI determines how many operations are performed per byte of data moved from memory. This reveals the layer is severely memory-bandwidth bound.'
  common_mistake: |
    **The Pitfall:** Instinctively blaming the raw number of operations.
    **The Rationale:** Engineers focus on pruning or quantizing without checking if the workload is actually compute-bound.
    **The Consequence:** Reducing operations in a memory-bound layer yields exactly zero speedup, wasting engineering time.
  napkin_math: |
    1. **Assumptions & Constraints:** Orin provides 275 TOPS (INT8) and 204.8 GB/s bandwidth. Layer requires 500 MFLOPs (0.5 GOPS) and 50 MB memory traffic.
    2. **Calculations:** Arithmetic Intensity (AI) = 500M / 50M = 10 Ops/Byte. Orin Ridge Point = 275 TOPS / 204.8 GB/s = ~1,342 Ops/Byte. Attainable throughput = 204.8 GB/s * 10 Ops/Byte = 2.048 TOPS.
    3. **Conclusion & Interpretation:** **The compute units are idle ~99% of the time; layer fusion is required, not pruning. Result: Memory-Bandwidth-Bound.**
  options:
  - The layer is compute-bound because 500 Million OPs is too much computation for a single layer on an edge device.
  - The layer is memory-bound because its arithmetic intensity (10 Ops/Byte) is far below the hardware's ridge point (~1,342 Ops/Byte).
  - The layer is thermally limited; the AGX Orin is likely throttling and unable to deliver its peak 275 TOPS.
  - The layer is compute-bound because its low arithmetic intensity means it isn't doing enough work per byte to be efficient.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: ERROR
validation_issues_pro:
- The 'Napkin Math' section is empty, failing to provide the core Arithmetic Intensity calculation required to answer the question.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
