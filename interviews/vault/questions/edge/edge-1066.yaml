schema_version: '1.0'
id: edge-1066
track: edge
level: L3
zone: evaluation
topic: latency-decomposition
competency_area: latency
bloom_level: evaluate
phase: both
title: 'Latency Decomposition: Compare TFLite vs. TensorRT Inference Latency on Jetson Orin'
scenario: 'A MobileNetV3-Large model (5.4M params, 224x224 input) is deployed on Jetson Orin (275 TOPS INT8, 60W). Two runtimes are evaluated for deployment: TFLite with GPU delegate versus TensorRT with FP16.'
question: For MobileNetV3-Large on Jetson Orin, how do TFLite GPU delegate and TensorRT FP16 compare in end-to-end inference latency and overhead?
details:
  realistic_solution: 'TensorRT FP16 uses fused kernels achieving ~2ms latency. TFLite GPU delegate per-op dispatch overhead adds ~3ms setup, bringing the total to ~5ms. TensorRT wins 2.5x on latency.'
  common_mistake: |
    **The Pitfall:** Assuming TFLite GPU delegate achieves full GPU utilization.
    **The Rationale:** Forgetting that TFLite's kernel fragmentation and op-by-op dispatch add significant overhead compared to TensorRT's fused kernels.
    **The Consequence:** Grossly underestimating the end-to-end inference latency in production.
  napkin_math: |
    - MobileNetV3-Large MACs: 219M → 0.44 GOPs.
    - Orin GPU FP16 peak: ~40 TFLOPS.
    - Raw compute time: 0.44 / 40000 ≈ 0.011ms.
    - Realistic TensorRT with memory ops and fused kernels: ~2ms.
    - TFLite overhead: 150+ kernel launches × 0.02ms = ~3ms.
    - TFLite total: 2ms (compute) + 3ms (overhead) = 5ms.
    **TensorRT (2ms) outperforms TFLite (5ms) by 2.5x due to the elimination of dispatch overhead.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
