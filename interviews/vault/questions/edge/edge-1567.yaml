schema_version: '1.0'
id: edge-1567
track: edge
level: L3
zone: fluency
topic: dma-data-movement
competency_area: memory
bloom_level: apply
phase: both
title: Edge TPU USB Bandwidth Bottleneck
scenario: >-
  You are deploying an INT8 quantized image classification model on a Google Coral Edge TPU USB Accelerator. The 4 TOPS INT8 accelerator processes a single inference in 2.5 ms. The model takes a standard 224x224x3 RGB image input, quantized to INT8. Your deployment environment forces you to connect the Coral device to a legacy host via a USB 2.0 port, which provides a maximum effective transfer rate of 35 MB/s.
question: What is the host-to-device transfer time for one 224x224x3 INT8 image over USB 2.0, and is the system compute-bound or I/O-bound?
details:
  realistic_solution: 'Calculate the exact payload size in bytes, divide by the effective interface bandwidth to find the transfer time, and compare this to the 2.5 ms compute time. Because the transfer time is significantly longer than the inference time, the system is strictly I/O-bound.'
  common_mistake: |
    **The Pitfall:** Calculating input size using 32-bit floats.
    **The Rationale:** Candidates are accustomed to FP32 training pipelines and default to 4 bytes per pixel value.
    **The Consequence:** They overestimate the payload size by 4x, fundamentally misunderstanding INT8 quantized pipelines and failing to compare the correct transfer time against the 2.5 ms compute time.
  napkin_math: |
    1. **Input Size:** `224 * 224 * 3 channels * 1 byte (INT8) = 150,528 bytes` (~150.5 KB).
    2. **Transfer Time:** `150.5 KB / 35,000 KB/s ≈ 0.0043 seconds = 4.3 ms`.
    3. **Comparison:** Transfer time (4.3 ms) > Compute time (2.5 ms).
    4. **Result:** The system is heavily **I/O-bound** (bandwidth-bound) by the USB 2.0 connection, meaning the Edge TPU will sit idle.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
