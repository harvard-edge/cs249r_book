schema_version: '1.0'
id: edge-0517
track: edge
level: L3
zone: optimization
topic: attention-scaling
competency_area: architecture
bloom_level: apply
phase: both
title: The Vision Transformer Performance Regression
scenario: 'You are an ML Systems Engineer at an autonomous trucking company. Your team has just deployed
  a new Vision Transformer (ViT) model for the main forward-facing camera, replacing an older CNN architecture.
  The new ViT is more accurate and has 20% fewer G-MACs. However, you observe a severe performance regression:
  the old CNN achieved 40 FPS, while the new ViT only achieves 15 FPS. You profile the hardware (a Jetson
  AGX Orin, 204.8 GB/s memory bandwidth) and get the following data per-inference: The CNN uses 150 G-MACs
  and reads 1.2 GB from DRAM. The ViT uses 120 G-MACs and reads 4.5 GB from DRAM.'
question: "Why is the new ViT slower than the old CNN on Jetson Orin despite having 20% fewer G-MACs?"
details:
  realistic_solution: The ViT is severely memory-bandwidth bound. While it requires fewer calculations
    (120 G-MACs vs. 150), it reads nearly 4x more data from DRAM (4.5 GB vs. 1.2 GB). The time spent waiting
    for data from memory far exceeds the time spent on computation, starving the powerful compute units.
    The model's low Arithmetic Intensity (Ops/Byte) makes it unable to leverage the Orin's high peak TOPS,
    leading to the performance drop despite having fewer G-MACs.
  common_mistake: Focusing only on G-MACs/TOPS as the sole measure of performance. Engineers often assume
    fewer operations automatically means faster inference, ignoring that data movement (memory bandwidth)
    is frequently the real bottleneck, especially with architectures like Transformers that have high
    memory access patterns from the self-attention mechanism.
  napkin_math: 'CNN memory transfer time: 1.2 GB / 204.8 GB/s = 5.8 ms. ViT memory transfer time: 4.5 GB / 204.8 GB/s = 22 ms. The ViT spends nearly 4x more time waiting for memory data compared to the CNN. Because the Jetson Orin is highly parallel, compute time is hidden behind this massive memory access bottleneck.'
  options:
  - The G-MAC count is misleading; Transformer MACs are less efficient and take longer to execute on the
    Orin's Tensor Cores.
  - The ViT is memory-bandwidth bound; its low arithmetic intensity means the compute units are starved
    waiting for data from DRAM.
  - The model has too many parameters, causing it to be capacity-bound by the Orin's 8-32GB of DRAM.
  - The performance drop is due to insufficient quantization; the model needs to be converted to INT4
    to increase throughput.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
chains:
- id: edge-chain-053
  position: 1
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The 'Napkin Math' section is incomplete, starting with a diagnostic instruction but not providing the
  calculation.
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- No mathematical calculations provided to verify.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
