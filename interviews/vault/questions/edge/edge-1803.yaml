schema_version: '1.0'
id: edge-1803
track: edge
level: L4
zone: specification
topic: distribution-drift-detection
competency_area: reliability
bloom_level: analyze
phase: both
title: Hailo-8 Traffic Camera Drift Detection Specification
scenario: 'A smart city traffic camera uses a Hailo-8 accelerator (26 TOPS INT8, 2.5W) to run an object detection model, streaming data directly from the host SoC''s memory since it lacks local DRAM. The city experiences significant seasonal weather changes (snow, heavy rain) that degrade model accuracy.'
question: How would you detect seasonal data drift at the edge on Hailo-8 without adding host memory bandwidth pressure or disrupting inference?
details:
  realistic_solution: 'The optimal specification leverages the Hailo-8''s dataflow architecture by extracting low-dimensional intermediate INT8 feature maps or embeddings from the existing inference pipeline. These tiny representations are sampled periodically (e.g., 1 FPS) and sent to the host CPU, which maintains a reference distribution and computes a lightweight Population Stability Index (PSI) to detect drift with minimal memory and compute overhead.'
  common_mistake: |
    **The Pitfall:** Computing complex statistical metrics on raw high-resolution images.
    **The Rationale:** Candidates default to standard cloud drift detection techniques (like KL divergence on raw pixels) ignoring edge constraints.
    **The Consequence:** The host CPU becomes heavily bottlenecked on memory bandwidth, disrupting the 26 TOPS accelerator and exceeding the 2.5W power budget.
  napkin_math: |
    *   **Raw Ingest Bandwidth:** 30 FPS of raw 1080p RGB (1920x1080x3 = 6.2 MB) consumes 186 MB/s of host-to-accelerator bandwidth.
    *   **Drift Sampling:** Extracting a 512-byte INT8 feature vector at 1 FPS = 512 B/s transfer.
    *   **Overhead Calculation:** 512 B/s / 186 MB/s = 0.00027% bandwidth overhead.
    *   **Result:** **Safely preserves memory bandwidth for inference while enabling continuous edge drift detection.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
