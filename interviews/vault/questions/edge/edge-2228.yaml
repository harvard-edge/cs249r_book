schema_version: '1.0'
id: edge-2228
track: edge
level: L4
zone: diagnosis
topic: autograd-computational-graphs
competency_area: optimization
bloom_level: analyze
phase: training
title: Analyzing when torch.compile helps vs hurts on Jetson Orin
scenario: You apply torch.compile to a 5M parameter model for inference on Jetson Orin. TorchInductor compilation takes 45 seconds on first call. After compilation, inference is actually 2ms slower per step than eager mode (12ms vs 10ms). This is unexpected since torch.compile typically speeds up larger models.
question: For a 5M-parameter inference model, what is the correct FLOP estimate and why is 10 GFLOPs a unit error?
details:
  realistic_solution: '5M params × 2 = 10 MFLOPs (not 10 GFLOPs — GFLOPs would require 5B params). Inference time = 10M / 275T = 36 nanoseconds. Unit error: M (million) ≠ G (billion).'
  common_mistake: |
    **The Pitfall:** Assuming `torch.compile` is a universal speedup.
    **The Rationale:** Engineers assume `torch.compile` always improves performance regardless of model size or hardware, given its success on large LLMs.
    **The Consequence:** On small models with short compute kernels, the overhead of launching compiled Triton kernels exceeds the benefit of fusion, causing a performance regression on hardware like Orin.
  napkin_math: |
    1. **FLOPs Estimate**: 5M parameters × 2 = 10M FLOPs = 10 MFLOPs.
    2. **The Unit Error**: 10 GFLOPs would require 5 Billion parameters, a 1000× error in magnitude.
    3. **Time Calculation**: Inference compute time at 275 TOPS = 10×10^6 / 275×10^12 = 3.63×10^-8 seconds.
    4. **Result**: **~36.3 ns** (extremely fast, proving the model is tiny and entirely bottlenecked by kernel launch overhead, not raw compute).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
