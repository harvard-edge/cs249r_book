schema_version: '1.0'
id: edge-1348
track: edge
level: L3
zone: fluency
topic: gpu-compute-architecture
competency_area: compute
bloom_level: apply
phase: both
title: Jetson Orin INT8 Compute Utilization for Vision Transformers
scenario: You are deploying a quantized vision transformer on an NVIDIA Jetson Orin
  edge device (Ampere GPU + DLA accelerator, 275 TOPS INT8, 32 GB LPDDR5, 60W TDP).
  The model requires 5.5 Tera-Operations (TOPS) of INT8 compute per inference. The
  system processes a continuous 30 FPS video stream from a single high-resolution
  camera. Calculate the total required INT8 compute throughput and determine the theoretical
  minimum INT8 compute utilization of the Jetson Orin.
question: What INT8 compute throughput is required for 30 FPS, and what is the theoretical
  minimum Jetson Orin utilization?
details:
  realistic_solution: The candidate should correctly calculate the theoretical utilization
    but immediately caveat it by noting that sustaining 165 TOPS on a 60W edge device
    is extremely challenging due to memory bandwidth bottlenecks and imperfect warp
    scheduling. A realistic solution would involve extensive operator fusion, optimizing
    thread block sizes for the Ampere SMs, and maximizing LPDDR5 bandwidth efficiency
    to approach this theoretical bound.
  common_mistake: Candidates often assume that because the theoretical compute utilization
    is less than 100%, the hardware will easily meet the 30 FPS target. They frequently
    forget that achieving peak TOPS requires perfect memory coalescing, optimal Tensor
    Core occupancy, and high arithmetic intensity.
  napkin_math: 1) Required Compute Throughput = Compute per inference * Frames per
    second = 5.5 TOPS/inference * 30 inferences/second = 165 TOPS. 2) Theoretical
    Compute Utilization = Required Compute Throughput / Total Available INT8 Peak
    Throughput = 165 TOPS / 275 TOPS = 0.60 (or 60%).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The question is missing `reasoning_competency` and `reasoning_mode` classifications.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
