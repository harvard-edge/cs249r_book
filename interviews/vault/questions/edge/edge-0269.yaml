schema_version: '1.0'
id: edge-0269
track: edge
level: L2
zone: analyze
topic: roofline-analysis
competency_area: compute
bloom_level: analyze
phase: both
title: The Orin's Ridge Point
scenario: Your autonomous vehicle team is profiling a new perception model on a Jetson AGX Orin. You observe
  that a specific layer performs 200 billion INT8 operations (200 GOPS) and reads 250 MB of data (weights
  and activations) from DRAM for a single inference. The Orin has 275 TOPS of peak INT8 compute and 204.8
  GB/s of memory bandwidth. Is this specific layer compute-bound or memory-bound on the AGX Orin? Explain
  your reasoning.
question: "Is this specific layer compute-bound or memory-bound on the AGX Orin?"
details:
  realistic_solution: The layer is memory-bound. To determine this, we must compare the model's Arithmetic
    Intensity (AI) to the hardware's Ridge Point. The Ridge Point is the minimum AI a model needs to have
    to be compute-bound.
  common_mistake: Engineers often guess based on qualitative feelings about the hardware, saying 'the
    Orin has a lot of compute, so it must be memory-bound' without doing the math. Another common error
    is incorrectly calculating the Arithmetic Intensity or the hardware's Ridge Point, leading to the
    wrong conclusion. Forgetting to normalize units (e.g., TOPS vs GOPS, GB/s vs MB) is a frequent source
    of bugs in this calculation.
  napkin_math: >
    1. Calculate the Orin's Ridge Point: 275 TOPS / 204.8 GB/s = 1,342 OPS/Byte.

    2. Calculate the workload's Arithmetic Intensity: 200 GOPS / 250 MB = 200,000 MOPS / 250 MB = 800 OPS/Byte.

    3. Compare AI to Ridge Point: Workload AI (800) < Ridge Point (1,342). Since the workload's AI is below the ridge point, the layer is memory-bound.
  options:
  - Compute-bound, because its Arithmetic Intensity (800 Ops/Byte) is greater than the Ridge Point.
  - Memory-bound, because the model's Arithmetic Intensity (800 Ops/Byte) is less than the Orin's Ridge
    Point (~1342 Ops/Byte).
  - Compute-bound, because 200 GOPS is a very large number of operations.
  - It's impossible to tell without knowing the model architecture.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
chains:
- id: edge-chain-173
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: OK
validation_issues_pro: []
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
