schema_version: '1.0'
id: edge-0364
track: edge
level: L2
zone: implement
topic: transformer-systems-cost
competency_area: architecture
bloom_level: understand
phase: both
title: 'The Edge Efficiency Trade-off: Standard vs. Depthwise Convolution'
scenario: You're optimizing a CNN-based driver monitoring system to run on a new automotive-grade processor
  like a Jetson AGX Orin. A colleague suggests replacing all standard 3x3 convolutions with 3x3 depthwise
  separable convolutions to save on compute and memory. Consider a typical layer in your network that
  takes a feature map with 128 input channels and produces one with 256 output channels. Can you explain
  the efficiency gain by calculating the approximate reduction in the number of parameters for this single
  layer?
question: "Can you explain the efficiency gain by calculating the approximate reduction in the number of parameters for this single layer?"
details:
  realistic_solution: 'A depthwise separable convolution splits a standard convolution into two much cheaper
    operations: a 3x3 depthwise convolution (one filter per input channel) and a 1x1 pointwise convolution
    (to combine the outputs and change channel depth).'
  common_mistake: Engineers often approximate the parameter reduction to be simply the size of the kernel
    (e.g., 9x for a 3x3 kernel). While a good first-order guess, this ignores the non-trivial parameter
    cost of the 1x1 pointwise convolution, which is required to mix channels and change the channel depth.
    The actual savings are always less than the kernel size.
  napkin_math: 'C_in = 128, C_out = 256, K = 3. Standard conv parameters: K^2 * C_in * C_out = 9 * 128 * 256 = 294,912. Depthwise separable parameters: (K^2 * C_in) + (C_in * C_out) = (9 * 128) + (128 * 256) = 1,152 + 32,768 = 33,920. Ratio = 294,912 / 33,920 = 8.694x reduction (~8.7x).'
  options:
  - The parameter count is reduced by ~2x, as the output channels are double the input.
  - The parameter count is reduced by exactly 9x, the size of the 3x3 kernel.
  - The parameter count is reduced by ~8.7x.
  - The parameter count is reduced by ~256x, because the output channel dimension is factored out of the
    spatial convolution.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The Napkin Math provides a compute reduction ratio, but the question asks for the approximate reduction
  in the number of parameters, which is not directly calculated.
- The Napkin Math assumes `C_in = C_out = 128`, but the scenario states a typical layer has 128 input
  channels and 256 output channels, creating an inconsistency.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
