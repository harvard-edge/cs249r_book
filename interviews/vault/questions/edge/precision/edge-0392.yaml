schema_version: '1.0'
id: edge-0392
track: edge
level: L2
zone: implement
topic: quantization-fundamentals
competency_area: precision
bloom_level: understand
phase: both
title: The Edge Memory Budget
scenario: You are on an autonomous vehicle team deploying a 500 million parameter perception model to an edge device. The model is currently in FP16 format, and you need to reduce its memory footprint. Your team decides to quantize only the model's weights to INT8.
question: How much weight memory, in GB, is saved by quantizing a 500 million parameter model from FP16 to INT8?
details:
  realistic_solution: 'The solution requires knowing the byte representation for each data type. FP16 (half-precision float) uses 2 bytes per parameter, while INT8 (8-bit integer) uses 1 byte. The memory savings is the difference between the total memory required for the weights in each format.'
  common_mistake: |
    **The Pitfall:** Confusing FP16 with FP32 or confusing bits with bytes.
    **The Rationale:** Candidates often treat FP16 as 4 bytes, or apply bit-width multipliers (16 vs 8) incorrectly, yielding results that are 8x off.
    **The Consequence:** Incorrectly estimating memory savings, leading to critically flawed hardware provisioning decisions.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model parameters: 500 million.
    - FP16 uses 2 bytes per parameter.
    - INT8 uses 1 byte per parameter.

    **Calculations:**
    - FP16 weight memory: 500,000,000 x 2 bytes = 1,000,000,000 bytes (1 GB).
    - INT8 weight memory: 500,000,000 x 1 byte = 500,000,000 bytes (0.5 GB).
    - Memory savings: 1.0 GB - 0.5 GB = 0.5 GB.

    **Conclusion & Interpretation:**
    - **Result: 0.5 GB Savings (Memory-Bound).** Quantizing weights from FP16 to INT8 halves the memory footprint, saving 0.5 GB.
  options:
  - 1.5 GB
  - 1.0 GB
  - 0.5 GB
  - 4.0 GB
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
