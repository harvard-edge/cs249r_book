schema_version: '1.0'
id: edge-0384
track: edge
level: L2
zone: recall
topic: quantization-fundamentals
competency_area: precision
bloom_level: understand
phase: both
title: 'The Perception Model Memory Diet: Quantization Fundamentals'
scenario: You're working on an autonomous vehicle's perception stack. Your team has a new object detection model with 80 million parameters, currently consuming too much memory in its default FP16 precision. You are tasked with evaluating a switch to INT8 PTQ for the model weights.
question: How much weight memory does the 80 million parameter model use in FP16 versus INT8?
details:
  realistic_solution: The calculation is a straightforward comparison based on the byte size of each data type. FP16 (half-precision float) uses 2 bytes per value, while INT8 (8-bit integer) uses 1 byte.
  common_mistake: |
    **The Pitfall:** Confusing bits and bytes or misjudging FP16 size.
    **The Rationale:** Engineers often assume FP16 takes 4 bytes (like FP32) or make a factor-of-8 error by not converting bits to bytes.
    **The Consequence:** Completely inaccurate baseline memory capacity estimates, leading to poor hardware sizing choices.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model Size: 80 million parameters.
    - Precisions: FP16 (2 bytes) and INT8 (1 byte).

    **Calculations:**
    - FP16 Size: 80M * 2 bytes = 160 MB.
    - INT8 Size: 80M * 1 byte = 80 MB.

    **Conclusion & Interpretation:**
    - **Result: 80 MB (Memory-Bound)**. Switching to INT8 halves the memory requirement from 160 MB to 80 MB, directly doubling the memory-bound framerate limit.
  options:
  - 320 MB for FP16, 80 MB for INT8
  - 160 MB for FP16, 80 MB for INT8
  - 20 MB for FP16, 10 MB for INT8
  - 160 MB for FP16, 160 MB for INT8
  correct_index: 1
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 2
deletion_reason: Duplicate of edge-quantization-memory-footprint-the-perception-model-memory-diet-257 (same title + track)
chains:
- id: edge-chain-158
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: OK
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
