schema_version: '1.0'
id: edge-2165
track: edge
level: L2
zone: fluency
topic: model-format-conversion
competency_area: deployment
bloom_level: understand
phase: inference
title: TFLite INT8 Conversion on Cortex-M4
scenario: We are converting a PyTorch anomaly detection CNN into TensorFlow Lite for
  Microcontrollers to deploy on a Cortex-M4. The M4 has 256 KB of SRAM and no FPU,
  making INT8 conversion mandatory. The original FP32 model weights consume exactly
  600,000 bytes. Assuming the TFLite Micro runtime engine requires 32,000 bytes of
  static overhead, calculate the memory footprint of the INT8 converted weights and
  the exact number of bytes remaining in SRAM for the tensor arena (activations).
question: What are the INT8 weight size and remaining SRAM tensor arena after converting
  the 600,000-byte FP32 model for the Cortex-M4?
details:
  realistic_solution: The correct approach requires understanding the data type mappings
    during model format conversion. FP32 uses 4 bytes per parameter, meaning the model
    has 150,000 parameters. An INT8 conversion maps each parameter to 1 byte. We then
    calculate total device SRAM in bytes and subtract the converted weights and runtime
    overhead to find the available tensor arena space.
  common_mistake: Candidates often forget the exact byte sizes of data types, failing
    to divide the FP32 size by 4 to get the parameter count. Another frequent error
    is using a base-10 conversion (1 KB = 1000 bytes) instead of base-2 (1 KB = 1024
    bytes) for the device's SRAM.
  napkin_math: '1. Parameter Count: 600,000 bytes / 4 bytes per FP32 param = 150,000
    parameters.

    2. INT8 Weights Size: 150,000 parameters * 1 byte per INT8 param = 150,000 bytes.

    3. Total Device SRAM: 256 KB * 1024 bytes/KB = 262,144 bytes.

    4. Remaining SRAM (Tensor Arena): 262,144 bytes - 150,000 bytes (weights) - 32,000
    bytes (runtime) = 80,144 bytes available for activations.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
