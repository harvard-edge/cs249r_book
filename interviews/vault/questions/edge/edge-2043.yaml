schema_version: '1.0'
id: edge-2043
track: edge
level: L6+
zone: realization
topic: graceful-degradation
competency_area: reliability
bloom_level: analyze
phase: both
title: Designing a Graceful Degradation Strategy for Real-time ML on NVIDIA Jetson Orin
scenario: You are tasked with deploying a critical real-time object detection system on an NVIDIA Jetson Orin for autonomous vehicle perception. The system must maintain responsiveness even under unexpected load spikes, sensor failures, or environmental interference, which can reduce available compute or introduce noisy data.
question: How would you design fail-operational graceful degradation on Jetson Orin using model fallbacks, degradation ladders, and QoS shedding under load or sensor faults?
details:
  realistic_solution: 'A realistic solution involves defining multiple operational modes or ''degradation rungs.'' For example, a primary high-accuracy model runs when resources are abundant. If compute drops due to thermal throttling or increased sensor input, the system dynamically switches to a smaller, faster model (e.g., MobileNet SSD), reduces frame rate, or only processes regions of interest. Furthermore, implement explicit fail-safe states for critical components (e.g., revert to a simpler rule-based collision avoidance). QoS shedding involves prioritizing detection of critical objects over background details if the system is under duress.'
  common_mistake: |
    **The Pitfall:** Over-reliance on a single, monolithic high-fidelity model.
    **The Rationale:** Candidates plan for ideal conditions and assume the hardware will never throttle.
    **The Consequence:** The system becomes brittle and catastrophically fails during thermal events or load spikes, severely violating safety SLAs.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Latency SLA: < 100ms critical budget.
       - High-fidelity model base latency: 50ms (Peak 20 FPS).
       - Low-fidelity model base latency: 10ms (Peak 100 FPS).
    2. **Calculations:**
       - If hardware throttles by 50%, High-fidelity latency jumps to 100ms (borderline failure).
       - If hardware throttles by 60%, High-fidelity latency jumps to 125ms (SLA violation).
       - Low-fidelity fallback at 60% throttle: 10ms / 0.40 = 25ms.
    3. **Conclusion & Interpretation:**
       - **Result: Compute-Bound under duress.** Dynamic switching to the 10ms fallback model guarantees the latency SLA is met (25ms < 100ms) even when available TOPS drop significantly due to thermal throttling.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
