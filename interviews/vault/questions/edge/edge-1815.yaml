schema_version: '1.0'
id: edge-1815
track: edge
level: L4
zone: specification
topic: model-format-conversion
competency_area: deployment
bloom_level: analyze
phase: inference
title: Jetson Orin Multi-Model TensorRT Deployment
scenario: You are tasked with deploying a complex vision pipeline consisting of a
  heavy object detector and a lightweight semantic segmentation model on an NVIDIA
  Jetson Orin. The device features an Ampere GPU, a DLA accelerator, 32 GB LPDDR5
  memory, and has a strict 60W TDP constraint. You need to design the model conversion
  specification from PyTorch to a deployment-ready format, detailing your strategy
  for operator coverage gaps and hardware delegation to achieve the target 275 TOPS
  INT8 performance.
question: How would you convert and delegate the detector and segmentation models
  on Jetson Orin to maximize INT8 performance under 60W?
details:
  realistic_solution: The optimal specification converts PyTorch models to ONNX, then
    compiles them into TensorRT engines. To maximize the 275 TOPS and stay under the
    60W TDP, the heavy object detector is compiled for the Ampere GPU in INT8 precision,
    while the segmentation model is explicitly delegated to the DLA. Any operator
    gaps during ONNX export are resolved by writing custom TensorRT plugins or modifying
    the source model architecture to use supported operations rather than allowing
    CPU fallbacks.
  common_mistake: Candidates often propose running all models on the Ampere GPU, completely
    neglecting the DLA accelerator, which leads to suboptimal power usage within the
    60W TDP. They also frequently overlook the cost of CPU fallbacks when ONNX-to-TensorRT
    conversion fails due to unsupported operators.
  napkin_math: If the detector requires 150 GOPS per frame and runs at 60 FPS, it
    needs 9 TOPS. The segmentation model requires 50 GOPS at 60 FPS, needing 3 TOPS.
    Total compute is 12 TOPS. While this is well under the 275 TOPS INT8 theoretical
    limit of the Jetson AGX Orin 64GB (60W TDP), memory bandwidth is the real bottleneck.
    A 2GB FP32 model requires reading 2GB per inference; at 60 FPS, that's 120 GB/s.
    Converting to INT8 reduces the model size to ~0.5GB, requiring only 30 GB/s from
    the 64 GB LPDDR5, drastically reducing power consumption and keeping the system
    well within the 60W TDP.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The 'Napkin Math' section truncates the concluding statement about power consumption.
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Hardware spec mismatch: Jetson AGX Orin 32GB has 200 TOPS and 40W TDP. The 64GB
  version has 275 TOPS and 60W TDP. The scenario mixes these specifications.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
