schema_version: '1.0'
id: edge-2307
track: edge
level: L4
zone: realization
topic: data-pipeline-engineering
competency_area: data
bloom_level: analyze
phase: both
title: Calculate memory bandwidth overhead for sequential image preprocessing
scenario: 'A Jetson AGX Orin (up to 204.8 GB/s LPDDR5) processes 4K video frames (3840x2160, 3 channels, INT8 input) at 60 FPS. The pre-processing pipeline performs three steps: (1) read raw INT8 frame, (2) normalize to FP16 and write back, (3) read the FP16 normalized buffer and write the final NCHW tensor for the NPU.'
question: Calculate the minimum memory bandwidth consumed by this pre-processing pipeline as a percentage of total available bandwidth.
details:
  realistic_solution: 'Calculate the total per-frame DRAM traffic by summing the read/write operations of the intermediate tensors, explicitly accounting for the datatype expansion from INT8 to FP16.'
  common_mistake: |
    **The Pitfall:** Forgetting data type expansion during normalization.
    **The Rationale:** Candidates assume bandwidth cost scales linearly with operation count without tracking the byte-size growth of intermediate tensors.
    **The Consequence:** Severely underestimating memory bandwidth consumption by ignoring that FP16 doubles the byte traffic downstream.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - INT8 frame size: 3840 * 2160 * 3 = 24.88 MB
       - FP16 frame size: 24.88 * 2 = 49.76 MB
       - Framerate: 60 FPS
       - Total Bandwidth: 204.8 GB/s
    2. **Calculations:**
       - Kernel 1 (Normalize): Read INT8 (24.88 MB) + Write FP16 (49.76 MB)
       - Kernel 2 (Format): Read FP16 (49.76 MB) + Write FP16 (49.76 MB)
       - NPU read: Read FP16 (49.76 MB)
       - Total traffic per frame: 24.88 + (4 * 49.76) = 223.92 MB
       - Bandwidth at 60 FPS: 223.92 MB * 60 = 13.44 GB/s
    3. **Conclusion & Interpretation:**
       - **Result: ~6.6% Utilization.** Preprocessing consumes 13.44 GB/s (13.44 / 204.8), nearly double a naive 1-byte-per-channel estimate.
status: draft
provenance: llm-draft
expected_time_minutes: 8
requires_explanation: true
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-realization-L4
created_at: '2026-04-25'
