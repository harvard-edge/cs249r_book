schema_version: '1.0'
id: edge-1079
track: edge
level: L3
zone: evaluation
topic: model-format-conversion
competency_area: deployment
bloom_level: evaluate
phase: inference
title: 'Model Format Conversion: Compare TFLite vs. ONNX Runtime for ARM Cortex on Jetson'
scenario: A MobileNetV2 model (3.4M params, 300M MACs) needs to run on the Cortex-A78AE CPU cores of Jetson Orin. Compare TFLite with XNNPACK delegate vs. ONNX Runtime with ACL (ARM Compute Library) for FP16 inference. Evaluate latency, memory usage, and op coverage.
question: For MobileNetV2 on Jetson Orin Cortex-A78AE CPUs, how do TFLite XNNPACK and ONNX Runtime ACL compare for FP16 latency, memory, and op coverage?
details:
  realistic_solution: 'MobileNetV2 requires 300M MACs (600M FLOPs). The Cortex-A78AE at 2.2GHz with 4 cores and FP16 NEON can theoretically achieve ~70.4 GFLOPS. Latency: 600M FLOPs / 70.4 GFLOPS = 8.5ms. TFLite XNNPACK is highly optimized for ARM NEON, measuring ~10ms in practice (due to packing overhead). ONNX Runtime ACL runs slightly slower around ~15ms because it is less optimized for this specific core. TFLite wins on CPU. Memory: TFLite runtime overhead ~2MB vs. ONNX Runtime ~8MB. Both fit comfortably.'
  common_mistake: 'Ignoring op coverage differences: ONNX Runtime ACL may not support all MobileNetV2 ops natively, causing CPU fallback for some ops, degrading latency unpredictably.'
  napkin_math: 'MobileNetV2: 300M MACs = 600M FLOPs. Cortex-A78AE FP16 NEON: 4 cores × 2.2GHz × 8 FLOPs/cycle = 70.4 GFLOPS. Latency: 600M / 70.4G = 8.5ms. XNNPACK adds packing/unpacking overhead → ~10ms measured. ONNX RT ACL: ~15ms. TFLite XNNPACK wins.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
math_verified: true
math_status: CORRECT
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
