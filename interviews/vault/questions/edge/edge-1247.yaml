schema_version: '1.0'
id: edge-1247
track: edge
level: L3
zone: analyze
topic: responsible-ai
competency_area: cross-cutting
bloom_level: analyze
phase: both
title: Hardware-Constrained Safety Guardrail Degradation
scenario: An automated factory safety compliance system is deployed on a Google Coral
  Edge TPU (4 TOPS INT8, 2W). The pipeline runs a primary anomaly detector (50 GOPS
  per frame) and a secondary fairness-aware guardrail model (30 GOPS per frame) that
  verifies the anomaly detector isn't systematically biased against specific worker
  demographics. The camera streams at 60 frames per second (FPS) and both models are
  fully quantized to INT8 due to the limited operator support. Post-deployment audits
  reveal that during peak factory floor activity, the system silently stops logging
  guardrail verifications, leading to unaccounted bias in the safety reports. Explain
  why this system behavior occurs under these hardware constraints.
question: Why does the Coral Edge TPU silently stop logging the 30-GOPS guardrail
  at 60 FPS during peak activity?
details:
  realistic_solution: The system behavior occurs because the combined compute requirement
    of the primary and guardrail models at 60 FPS exceeds the Coral Edge TPU's maximum
    theoretical capacity. To maintain the primary anomaly detector's real-time performance,
    the system drops the execution of the secondary guardrail model. This highlights
    a critical governance failure where performance was prioritized over fairness
    checks without a proper failsafe mechanism.
  common_mistake: Candidates often assume the bias is purely a result of INT8 quantization
    precision loss without analyzing the actual compute requirements. They might overlook
    that the guardrail model is systematically being starved of compute resources
    or dropped by the scheduler due to the strict 4 TOPS limit.
  napkin_math: 'Primary model compute: 50 GOPS/frame * 60 FPS = 3,000 GOPS (3.0 TOPS).
    Guardrail model compute: 30 GOPS/frame * 60 FPS = 1,800 GOPS (1.8 TOPS). Total
    compute required: 3.0 TOPS + 1.8 TOPS = 4.8 TOPS. Since the Google Coral Edge
    TPU provides a maximum of 4 TOPS INT8, the workload exceeds the hardware capacity
    by 0.8 TOPS. Thus, the system physically cannot run both models at 60 FPS.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
