schema_version: '1.0'
id: edge-1658
track: edge
level: L6+
zone: mastery
topic: load-balancing
competency_area: networking
bloom_level: create
phase: inference
title: Edge TPU Cluster Load Balancing for Real-Time Video Analytics
scenario: You are designing an edge computing node for a smart city intersection that processes video streams from 8 independent 30 FPS cameras. The node is equipped with a host CPU and an array of 4 Google Coral Edge TPUs.
question: How would you route and load-balance 8 camera streams across 4 Coral Edge TPUs while handling bursts and frame drops?
details:
  realistic_solution: |
    A robust architecture uses a least-outstanding-requests routing strategy to account for variations in inference times. Since the Coral Edge TPUs only support specific INT8 operations, the host CPU must act as an intelligent gateway, compiling the graph to run fully on the TPU to avoid expensive context switching back to the host. The router should implement an active-worker pull model with backpressure to drop frames gracefully at the gateway level rather than saturating the PCIe queue.
  common_mistake: |
    **The Pitfall:** Proposing a basic round-robin load balancer that pushes frames directly to the PCIe bus.
    **The Rationale:** Treating the Edge TPUs like standard web servers without understanding host CPU graph compilation and unsupported operator fallbacks.
    **The Consequence:** Saturating the PCIe queue with unprocessable frames during a burst, leading to system-wide lockups instead of graceful frame dropping.
  napkin_math: |
    **Assumptions & Constraints:**
    - Workload: 8 cameras * 30 FPS = 240 FPS total.
    - Hardware: 4 Edge TPUs.

    **Calculations:**
    - TPU Capacity: ~500 FPS per TPU (including PCIe overhead).
    - Total Capacity: 4 * 500 = 2000 FPS.
    - PCIe Bandwidth Required: 240 FPS * 270 KB = ~65 MB/s.

    **Conclusion & Interpretation:**
    - **Result: 65 MB/s is << PCIe capacity (Load-Balancer Logic-Bound)**. Not Hardware-Bound.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
