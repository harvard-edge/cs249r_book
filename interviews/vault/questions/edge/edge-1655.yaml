schema_version: '1.0'
id: edge-1655
track: edge
level: L6+
zone: mastery
topic: gpu-compute-architecture
competency_area: compute
bloom_level: create
phase: both
title: Heterogeneous Pipeline Design for Jetson Orin
scenario: You are architecting the compute pipeline for an autonomous robotics platform
  utilizing the NVIDIA Jetson Orin. The system requires processing multiple sensor
  modalities under a strict 60W TDP. You must design a custom execution model that
  optimally partitions a massive transformer-based perception model across the Ampere
  GPU and the DLA to maximize the 275 TOPS INT8 capacity. Furthermore, detail how
  your thread block configuration and memory access patterns will mitigate bottlenecks
  on the shared 32 GB LPDDR5 memory.
question: How would you partition the transformer perception model across the Ampere
  GPU and DLA and design kernels to avoid LPDDR5 bottlenecks?
details:
  realistic_solution: The optimal architecture statically schedules standard INT8
    dense operations (like standard convolutions or MLPs) onto the DLA to maximize
    power efficiency within the 60W budget. The Ampere GPU's Tensor Cores are reserved
    for irregular or dynamic operations like fused multi-head attention. To prevent
    memory bottlenecks, the implementation must use zero-copy memory (pinned memory)
    to avoid redundant copies, and warp-level primitives with asynchronous copy (cp.async)
    to load data from the 32 GB LPDDR5 directly into shared memory, maximizing memory
    coalescing and Tensor Core occupancy.
  common_mistake: Candidates frequently treat the system as a standard discrete GPU
    setup, scheduling all matrix operations onto the Ampere SMs and entirely neglecting
    the DLA, which wastes power and compute capacity. They also often fail to account
    for the shared unified memory architecture, designing kernels that cause severe
    memory contention between the CPU, GPU, and DLA.
  napkin_math: The Jetson Orin's 256-bit LPDDR5 at 6400 MT/s yields a peak memory
    bandwidth of 204.8 GB/s (32 bytes * 6.4 GT/s). To fully utilize the 275 INT8 TOPS,
    the required arithmetic intensity is (275e12 OPs) / (204.8e9 Bytes/s) = ~1342
    OPs/Byte. Standard attention mechanisms often have an arithmetic intensity below
    400 OPs/Byte, making them severely memory-bound. To bypass the 204.8 GB/s bandwidth
    wall, intermediate tensors (like the NxN attention matrix) must be strictly materialized
    in the Ampere SM's shared memory rather than written back to LPDDR5.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The arithmetic intensity calculation (Ops / Byte) is correct, but then it states
  'If a single token's KV-cache requires 256 KB...' without explicitly defining what
  this refers to (e.g., for a specific model size or hidden dimension), making it
  hard to verify.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
