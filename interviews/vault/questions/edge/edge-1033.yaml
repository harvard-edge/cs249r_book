schema_version: '1.0'
id: edge-1033
track: edge
level: L4
zone: evaluation
topic: dataset-curation
competency_area: data
bloom_level: evaluate
phase: training
title: 'Dataset Curation: Evaluate Active Learning Strategies for Edge-Deployed Models'
scenario: Your object detection model runs on Jetson Orin at 200 inference/sec. You want to implement active learning to select which images to upload for labeling. Compare: (A) entropy-based selection, (B) margin-based selection, (C) random selection.
question: How would you compare entropy, margin, and random image selection for active learning on the 200 inference/sec Jetson Orin detector?
details:
  realistic_solution: Entropy and margin require only the softmax output (already computed during inference). Both add < 0.1ms overhead. Random selection is faster but wastes labels on easy examples. Entropy-based typically selects 3-5× more informative samples per label dollar.
  common_mistake: |
    **The Pitfall:** Implementing complex ensemble-based uncertainty estimation on edge devices.
    **The Rationale:** Candidates borrow state-of-the-art active learning methods from academic research without considering edge compute constraints.
    **The Consequence:** The heavy computational burden of running multiple models (ensembles) destroys the real-time inference framerate, making the system unusable in production.
  napkin_math: |
    1. **Inference Rate**: Jetson Orin runs at 200 inference/sec (5ms per frame).
    2. **Entropy Overhead**: H = -Sum(p log p) for 80 COCO classes requires 80 multiplications and log operations. At 240MHz with SIMD, this takes ~0.01ms.
    3. **Margin Overhead**: max - second_max is trivial (<0.01ms).
    4. **Relative Compute Cost**: 0.01ms / 5ms = 0.2% compute overhead for entropy/margin.
    5. **Label Efficiency**: Entropy-based selection identifies uncertain images. Achieving equivalent performance requires only 20% of the labels compared to 60% with random selection, resulting in a **3x label efficiency gain** and significant cost savings.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
