schema_version: '1.0'
id: edge-1621
track: edge
level: L3
zone: implement
topic: memory-mapped-inference
competency_area: memory
bloom_level: apply
phase: inference
title: Calculate Mmap Cold Start Latency on Jetson Orin
scenario: You are deploying a 15 billion parameter vision-language model quantized
  to INT8 on an NVIDIA Jetson Orin to utilize its 275 TOPS compute capability. The
  device features 32 GB LPDDR5 memory and an Ampere GPU + DLA accelerator. To support
  multiple concurrent worker processes without duplicating RAM usage, you load the
  15 GB weight file using memory mapping (`mmap`). Assuming the underlying NVMe storage
  has a sustained sequential read throughput of 2.5 GB/s, calculate the absolute minimum
  time required for the OS to page the entire model into the Jetson's memory during
  a cold start.
question: What is the absolute minimum cold-start time to page the 15 GB INT8 model
  from 2.5 GB/s NVMe into Jetson memory?
details:
  realistic_solution: The correct calculation isolates the storage bottleneck. Since
    the weights are not in RAM at cold start, the theoretical minimum time to fault
    them into the 32 GB LPDDR5 memory is strictly bounded by the NVMe sequential read
    speed. To mitigate this latency in production, engineers use flags like `MAP_POPULATE`
    or sequentially read the memory region during application startup to force page-ins
    before accepting live requests.
  common_mistake: Candidates often confuse the high bandwidth of the Jetson's LPDDR5
    memory with the much slower NVMe storage bandwidth when calculating cold-start
    load times. They may also fail to realize that `mmap` lazily loads pages by default,
    meaning this 15 GB transfer happens via page faults upon first access unless explicitly
    pre-fetched.
  napkin_math: Model size = 15 billion parameters * 1 byte (INT8) = 15 GB. NVMe read
    throughput = 2.5 GB/s. Minimum cold start latency = 15 GB / 2.5 GB/s = 6.0 seconds.
    The 32 GB LPDDR5 is large enough to hold the memory-mapped weights, but the 6-second
    IO bottleneck must be completed before the Ampere GPU and DLA can begin inference
    at full speed.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
