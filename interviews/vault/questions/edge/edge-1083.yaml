schema_version: '1.0'
id: edge-1083
track: edge
level: L4
zone: specification
topic: model-format-conversion
competency_area: deployment
bloom_level: create
phase: inference
title: 'Model Format Conversion: Specify ONNX Conversion Pipeline with Validation for Edge'
scenario: Specify a production-grade ONNX conversion pipeline for deploying PyTorch models to edge accelerators. The pipeline must validate numerical equivalence (< 0.1% MAE), handle unsupported ops, report conversion failures with actionable messages, and run in CI/CD in under 10 minutes.
question: What CI/CD stages and pass/fail criteria would you specify for this PyTorch-to-ONNX conversion pipeline?
details:
  realistic_solution: 'Stage 1 (2min): ONNX export with torch.onnx.export, opset 17, static shapes. onnx.checker.check_model — fail if any violation. Stage 2 (1min): onnxruntime inference on 100 validation inputs, compute MAE vs. PyTorch. Fail if MAE > 0.001. Stage 3 (3min): Hailo DFC compilation with INT8 calibration (50 images). Stage 4 (2min): Run HEF on Hailo-8 simulator, compare vs. ONNX output on 50 inputs. Fail if accuracy drop > 1%. Stage 5 (2min): Report engine size, estimated throughput. Total: 10min ✓.'
  common_mistake: |
    **The Pitfall:** Validating numerical equivalence using only a single static input shape.
    **The Rationale:** Engineers verify a successful forward pass on a "dummy" tensor but skip rigorous validation against dynamic shapes or edge-case stride patterns.
    **The Consequence:** The model passes CI/CD but fails randomly in production when presented with diverse, real-world inputs that trigger unsupported op fallbacks.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 100 validation inputs. 30M param model. Target CI/CD deadline: 10 minutes.
    2. **Calculations:**
       - ONNX Validation (CPU): 100 * 15 GFLOPs / 10 TFLOPS = 150 ms.
       - Quantization Search/Compile: ~3 minutes.
       - Edge Simulation: 50 inputs at 10 FPS simulator speed = 5 seconds.
    3. **Conclusion & Interpretation:**
       - **Result: Total CI Time < 4 minutes.** The entire pipeline (export + inference + compile + simulate) executes well within the 10-minute SLA constraint.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
