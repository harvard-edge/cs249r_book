schema_version: '1.0'
id: edge-2136
track: edge
level: L4
zone: optimization
topic: attention-scaling
competency_area: memory
bloom_level: apply
phase: both
title: Sliding Window Attention for Long Context on Jetson AGX Orin
scenario: Mistral-7B is deployed on a Jetson AGX Orin (32GB LPDDR5, 204 GB/s) using sliding window attention (SWA) with window_size=4096. The sequence length is 16K tokens. The model has 32 layers, 8 KV heads, and a head_dim of 128.
question: For a 16K-token sequence, how do full attention and 4096-token SWA compare in KV cache memory usage and per-token decode bandwidth?
details:
  realistic_solution: 'Full attention KV cache: 2×8×16384×128×2B×32 = 2.14GB. SWA
    KV cache: only 4096 tokens active per layer = 2×8×4096×128×2B×32 = 535MB — 4×
    reduction in KV memory. Per-token decode bandwidth (full): load 2.14GB KV cache;
    per-token decode (SWA): load 535MB. At 204 GB/s: full attention decode = 10.5ms/token;
    SWA decode = 2.6ms/token — 4× faster decode. Trade-off: tokens beyond window size
    have no attention to earlier context.'
  common_mistake: |
    **The Pitfall:** Assuming SWA saves compute proportionally to the context reduction.
    **The Rationale:** It's common to conflate the O(N^2) compute savings of attention during prefill with the memory-bound nature of autoregressive decode.
    **The Consequence:** Overestimating the compute speedup while failing to accurately quantify the primary benefits: KV cache memory reduction and decode bandwidth savings.
  napkin_math: |
    1. **Full KV memory**: 2 (K&V) * 8 heads * 16,384 tokens * 128 dim * 2 bytes (FP16) * 32 layers = 2.14 GB.
    2. **SWA KV memory**: 2 * 8 * 4,096 * 128 * 2 * 32 = 536 MB.
    3. **Full decode latency**: 2.14 GB / 204 GB/s = 10.5 ms per token.
    4. **SWA decode latency**: 536 MB / 204 GB/s = 2.6 ms per token.
    5. **Conclusion**: SWA provides a **4x reduction in KV memory (2.14GB to 536MB)** and a **4x decode speedup (10.5ms to 2.6ms)**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
