schema_version: '1.0'
id: edge-0107
track: edge
level: L4
zone: optimization
topic: real-time-deadlines
competency_area: data
bloom_level: analyze
phase: inference
title: The Memory Copy Choke
scenario: You are processing 4K video on an ARM-based edge device. You notice that the CPU utilization is at 100%, and the NPU is mostly idle, even though the model is highly optimized.
question: Based on the data path diagram, what is the 'silent' task consuming all your CPU cycles?
details:
  realistic_solution: The bottleneck is the CPU memcpy between memory spaces. A 4K frame is ~24 MB. Standard Linux drivers often force multiple copies between kernel-space, application-space, and NPU-space. At 30 FPS, the CPU is forced to move ~1.4 GB/s across RAM boundaries, saturating the bus. The fix is a Zero-Copy DMA Pipeline (e.g., using `dmabuf`), allowing the NPU to read directly from the camera buffer.
  common_mistake: |
    **The Pitfall:** Assuming the NPU must be struggling with 4K resolution.
    **The Rationale:** Engineers assume large resolution directly overloads the accelerator.
    **The Consequence:** Attempting to prune or quantize the model further, which yields zero improvement because the NPU is already waiting on CPU data transfers.
  napkin_math: |
    1. **Frame Size:** A 4K frame (3840x2160, RGB) is ~24.8 MB.
    2. **Transfer Rate:** 30 FPS * 24 MB/frame = 720 MB/s per copy.
    3. **Copy Cost:** With 2 copies (kernel to user, user to NPU), the bandwidth hits **1.44 GB/s**.
    4. **Impact:** For a low-power ARM CPU, this consumes 100% of available memory bandwidth and compute cycles.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
