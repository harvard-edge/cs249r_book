schema_version: '1.0'
id: edge-1011
track: edge
level: L3
zone: fluency
topic: cnn-efficient-design
competency_area: architecture
bloom_level: apply
phase: both
title: 'Fluency: explain MobileNet width multiplier for resource-constrained edge deployment'
scenario: A hardware engineer on your team is deploying a custom product detector on a Hailo-8 (26 TOPS, 2.5W). They ask what a 'width multiplier of 0.5' means for MobileNetV3 and how to choose the right value given the Hailo-8's compute budget.
question: What does a 0.5 MobileNetV3 width multiplier mean, how does it affect FLOPs and accuracy, and which value should Hailo-8 use?
details:
  realistic_solution: The width multiplier α symmetrically scales all layer channel dimensions. Because channel counts dictate both input and output dimensions of dense convolutions, compute (FLOPs) scales quadratically (α²). Since the hardware possesses massive headroom (26 TOPS) relative to the MobileNet compute footprint, one should specify α=1.0 or higher to maximize accuracy, as compute is not the binding constraint.
  common_mistake: |
    **The Pitfall:** Treating width multiplier as a linear scaler on FLOPs.
    **The Rationale:** Candidates assume halving the width halves the compute, forgetting that both input and output channels are scaled, making the reduction quadratic.
    **The Consequence:** Over-compressing the model and sacrificing accuracy unnecessarily when the hardware has ample headroom.
  napkin_math: |
    **Assumptions & Constraints:**
    - MobileNetV3-Large Baseline Compute: 0.22 GFLOPs.
    - Hardware Capability: 26 TOPS (26,000 GOPS).
    - Target Framerate: 30 FPS.

    **Calculations:**
    - FLOPs scaling with width multiplier α is quadratic (α²).
    - Compute at α=0.5: 0.22 GFLOPs * (0.5)² = 0.055 GFLOPs = 0.11 GOPS.
    - Compute at α=0.75: 0.22 GFLOPs * (0.75)² = 0.12375 GFLOPs = 0.2475 GOPS.
    - Hailo-8 Load at α=0.75 (30 FPS): 0.2475 GOPS * 30 = 7.425 GOPS.

    **Conclusion & Interpretation:**
    - **Result: 7.425 GOPS Requirement (Accuracy-Bound)**. The compute load is vastly below the 26,000 GOPS limit. Higher multipliers should be used to maximize accuracy since compute is abundant.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
