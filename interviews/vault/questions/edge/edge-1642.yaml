schema_version: '1.0'
id: edge-1642
track: edge
level: L6+
zone: mastery
topic: batching-strategies
competency_area: latency
bloom_level: create
phase: inference
title: Dataflow-Aware Dynamic Batching for Multi-Camera Hailo-8 Edge Appliances
scenario: You are designing an edge inference appliance to process 4 concurrent 1080p camera streams at 30 FPS using a single Hailo-8 accelerator (26 TOPS INT8, 2.5W). The Hailo-8 operates as a dataflow accelerator without local DRAM, meaning it must stream input tensors directly from host memory over PCIe. To minimize host-to-device memory transfer overhead and hit strict 33ms latency targets per frame, you need to synthesize a custom batching strategy. Design an architecture that dynamically batches asynchronous frames from these cameras while accounting for the PCIe bandwidth constraints and the accelerator's strict dataflow nature.
question: How would you dynamically batch 4 asynchronous 1080p 30 FPS camera streams on a DRAM-less Hailo-8 while meeting the 33 ms target?
details:
  realistic_solution: The optimal design employs a host-side memory pooling strategy to spatially concatenate incoming frames into a unified 'mega-tensor' (e.g., a 2x2 grid) in pinned host memory before streaming it to the Hailo-8 as a single batch-1 execution. This perfectly leverages the accelerator's spatial dataflow pipeline without incurring the overhead of multiple small PCIe transfers or requiring on-device buffering. The architecture should use a strict time-bound trigger (e.g., 20ms) to pad missing frames with zeros if a camera lags, ensuring the dataflow pipeline never stalls and the 33ms end-to-end latency budget is preserved.
  common_mistake: |
    **The Pitfall:** Proposing standard dynamic batching strategies on device memory.
    **The Rationale:** Candidates assume the Hailo-8 operates like a standard GPU with local DRAM, failing to account for its DRAM-less spatial dataflow architecture.
    **The Consequence:** Attempting to accumulate batches on-device causes massive PCIe thrashing and host memory bottlenecks, destroying real-time performance.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 4 asynchronous 1080p 30 FPS streams (Batch interval: 33.3 ms).
       - Frame size = 1920 * 1080 * 3 bytes ≈ 6.22 MB.
       - Hailo-8 compute: 26 TOPS. Model requires 8 GOPS per frame.
    2. **Calculations:**
       - Spatially batched tensor = 4 * 6.22 MB = 24.88 MB.
       - PCIe stream bandwidth = 24.88 MB * 30 FPS = ~746 MB/s.
       - Batched execution compute = 32 GOPS / 26,000 GOPS = 0.0012 seconds (1.2 ms).
    3. **Conclusion & Interpretation:**
       - **Result: 746 MB/s bandwidth, 1.2 ms compute.** Inference latency is dominated by host-to-device streaming. The spatial batching strategy perfectly resolves this **I/O-Bandwidth-Bound** constraint.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
