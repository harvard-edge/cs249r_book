schema_version: '1.0'
id: edge-1649
track: edge
level: L6+
zone: mastery
topic: dma-data-movement
competency_area: memory
bloom_level: create
phase: both
title: Bypassing PCIe Bottlenecks on Edge TPU
scenario: You are designing an anomaly detection system using a PCIe-based Google Coral Edge TPU (4 TOPS INT8, 2W, PCIe Gen2 x1). The system ingests 4 HD camera streams (1080p @ 30 FPS). The legacy pipeline crops 100 suspect regions (224x224 RGB) per frame on the host CPU, allocates them in standard memory, and queues them to the TPU individually. The TPU compute is severely underutilized, and the system drops frames due to host-device data transfer overhead.
question: What memory and data-movement architecture would make 4 HD 30 FPS camera streams real-time on a PCIe Coral Edge TPU?
details:
  realistic_solution: The optimal architecture eliminates ROI cropping and small DMA transfers entirely by redesigning the model as a Fully Convolutional Network (FCN) taking full 1080p frames. Furthermore, the pipeline should configure the camera ISP to directly DMA grayscale INT8 frames into host pinned memory. A single contiguous DMA transfer per frame drastically reduces PCIe overhead and payload size, bypassing the IO bottleneck while leveraging the TPU's spatial processing efficiency.
  common_mistake: |
    **The Pitfall:** Attempting to optimize the CPU multi-threading or memory pinning for the 12,000 individual ROIs.
    **The Rationale:** Candidates focus on improving the speed of the existing software loop rather than identifying the hardware limit.
    **The Consequence:** While zero-copy host buffers help CPU load, thousands of small transfers completely saturate the PCIe Gen2 x1 setup overhead, starving the TPU.
  napkin_math: |
    1. Legacy ROI approach: 100 ROIs * 4 cams * 30 FPS = 12,000 inferences/s.
    2. 224x224x3 INT8 = 150 KB/ROI. 12,000 * 150 KB = 1.8 GB/s (Exceeds Coral PCIe Gen2 x1 ~400 MB/s practical limit).
    3. FCN Grayscale approach: 4 cams * 30 FPS = 120 frames/s.
    4. 1920x1080x1 INT8 = 2.07 MB/frame. 120 * 2.07 MB = 248.4 MB/s (Safely within 400 MB/s limit).
    5. **Compute Check: A standard INT8 FCN at 1080p requires ~15 GOPS. 120 frames/s * 15 GOPS = 1.8 TOPS, utilizing 45% of the Coral's 4 TOPS capacity.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
