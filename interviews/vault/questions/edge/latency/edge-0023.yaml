schema_version: '1.0'
id: edge-0023
track: edge
level: L4
zone: diagnosis
topic: real-time-deadlines
competency_area: latency
bloom_level: analyze
phase: inference
title: The Camera-to-Inference Latency Budget
scenario: 'Your autonomous vehicle''s perception pipeline runs at 30 FPS on a Jetson Orin. The budget per frame is 33.3ms. Your measured average breakdown: ISP (5ms) -> resize/normalize (3ms) -> YOLO inference (20ms) -> NMS post-processing (2ms) = 30ms total. That leaves 3.3ms of margin. During road testing, the system occasionally drops frames — about 1 in 200. The safety team says zero dropped frames are acceptable.'
question: "What's going wrong, and how do you fix it without buying faster hardware?"
details:
  realistic_solution: The 1-in-200 frame drops are caused by tail latency — the p99.5 inference time exceeds 33.3ms even though the average is 30ms. The fix is to pipeline the execution stages (ISP -> CPU -> GPU -> CPU) and lock the hardware clocks to prevent dynamic voltage and frequency scaling (DVFS) jitter.
  common_mistake: |
    **The Pitfall:** Blindly attempting to "optimize the model to run faster" to add more margin.
    **The Rationale:** Candidates focus on the 3.3ms average margin, believing a slightly smaller model will eliminate the drops.
    **The Consequence:** This misdiagnoses the problem; the issue is latency *variance* (jitter) and lack of pipelining, not the average execution time.
  napkin_math: |
    **Assumptions & Constraints:**
    - Frame budget: 33.3ms (30 FPS).
    - Breakdown: ISP (5ms), resize/normalize (3ms), YOLO inference (20ms), NMS (2ms).
    - Sequential pipeline at p99.9: inference spikes to 32ms.

    **Calculations:**
    - Sequential pipeline at p99.9: 5 + 3 + 32 + 2 = 42ms (frame drop).
    - Pipelined at p99.9: max(5, 3, 32, 2) = 32ms (fits in 33.3ms with 1.3ms margin).
    - With clock locking to eliminate DVFS jitter: max(5, 3, 25, 2) = 25ms (8.3ms margin).

    **Conclusion & Interpretation:**
    - **Result: 25ms (Latency-Bound)**. Frame drop rate drops from 1/200 to <1/100,000. Throughput cost of pipelining adds 1 frame of end-to-end latency (33ms extra), which is acceptable.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
