schema_version: '1.0'
id: edge-0171
track: edge
level: L2
zone: recall
topic: batching-strategies
competency_area: latency
bloom_level: understand
phase: inference
title: The Continuous Batching Sweet Spot
scenario: 'You are designing a video analytics server for a factory floor, processing streams from 10 cameras. Each camera sends a frame every 100ms (10 FPS). Your accelerator''s inference time scales with batch size N according to the formula: T_inference(N) = 40ms + 5ms * (N-1).'
question: What batch size maximizes throughput while keeping worst-case frame latency within 150ms?
details:
  realistic_solution: The optimal batch size is 8. This maximizes throughput while keeping worst-case latency within the 150ms SLA budget. Continuous batching requires accounting for both the wait time to form the batch and the actual sequential inference time.
  common_mistake: |
    **The Pitfall:** Being greedy and assuming the largest possible batch size is always best for throughput, ignoring the latency constraint.
    **The Rationale:** Engineers calculate the inference latency but forget the waiting time component. They solve for T_inference(N) <= 150ms but forget that in continuous batching, a frame must wait for the rest of its batch to arrive.
    **The Consequence:** The system fails to meet the worst-case 150ms frame latency SLA.
  napkin_math: |
    **Assumptions & Constraints:**
    - Arrival rate: 10 cameras * 10 FPS = 100 frames/s (1 frame every 10ms).
    - Inference time: T_inference(N) = 35ms + 5N.
    - Worst-case latency: Wait for (N-1) frames + T_inference(N) <= 150ms.

    **Calculations:**
    - Batch start time = max(T_inference(N), 10*(N-1)).
    - Worst-case latency = max(35 + 5N, 10N - 10) + (35 + 5N).
    - Test N=8: T_inference(8) = 75ms. Fill time = 70ms. Batch starts at max(75, 70) = 75ms. Total latency = 75 + 75 = 150ms.
    - Test N=9: T_inference(9) = 80ms. Fill time = 80ms. Batch starts at max(80, 80) = 80ms. Total latency = 80 + 80 = 160ms.

    **Conclusion & Interpretation:**
    - **Result: N=8 (Throughput-Optimized Latency-Bound)**. N=8 strictly satisfies the <= 150ms worst-case latency constraint while maximizing throughput (106.7 FPS).
  options:
  - '8'
  - '23'
  - '11'
  - As large as memory allows
  correct_index: 0
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
math_verified: true
math_status: CORRECT
math_date: '2026-04-27'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
