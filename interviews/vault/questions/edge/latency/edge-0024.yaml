schema_version: '1.0'
id: edge-0024
track: edge
level: L5
zone: evaluation
topic: real-time-deadlines
competency_area: latency
bloom_level: evaluate
phase: inference
title: The WCET Analysis Wall
scenario: Your team must certify a neural network for an ASIL-B automotive braking assistant running on a real-time edge processor. The safety assessor demands a WCET (Worst-Case Execution Time) bound for every inference. Your ML engineer runs the model 10,000 times and reports 'max observed latency is 4.2ms.' The assessor rejects this.
question: Why is measurement-based WCET insufficient for safety certification, and what does a valid WCET analysis require for a neural network?
details:
  realistic_solution: 'Measurement-based WCET fails because it cannot prove coverage of all execution paths. A neural network has data-dependent execution time even on fixed architectures (sparse acceleration, DMA contention from other bus masters, interrupt preemption). Valid WCET analysis requires static analysis (abstract interpretation of the binary) combined with hardware timing models. For NNs, this means eliminating all data-dependent control flow, using only bounded operations, and explicitly modeling DMA bus contention.'
  common_mistake: |
    **The Pitfall:** Relying purely on empirical measurement for hard real-time safety guarantees.
    **The Rationale:** ML engineers assume that if they run an inference 10,000 times, the maximum observed value securely captures the "worst case."
    **The Consequence:** Measurement-based timing only captures typical worst cases, failing to guarantee the absolute, provably safe upper bound required for automotive (ASIL-B) certification.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Processor: Cortex-R52 (800 MHz, 4 INT8 MACs/cycle)
       - Empirical Baseline: 4.2ms observed latency
       - Safety Constraints: ASIL-B static analysis requires 1.55x bus margin and 1.2x certification margin.
    2. **Calculations:**
       - Peak Capacity: 4 * 800M = 3.2 GOPS
       - Bounded Static WCET: 4.2ms * 1.55 = 6.51ms
       - Certified Safety WCET: 6.51ms * 1.2 = ~7.81ms
    3. **Conclusion & Interpretation:**
       - **Result: Certification-Bound.** Empirical testing is rejected for hard real-time environments. Static abstract interpretation provides a mathematically safe 7.81ms WCET limit, successfully satisfying ASIL-B bounds.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
