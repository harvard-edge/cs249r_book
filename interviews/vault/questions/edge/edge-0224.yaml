schema_version: '1.0'
id: edge-0224
track: edge
level: L1
zone: recall
topic: real-time-deadlines
competency_area: latency
bloom_level: remember
phase: inference
title: The 30 FPS Deadline (edge-0224)
scenario: Your team is designing the perception system for an autonomous vehicle. The main camera feed runs at 30 frames per second (FPS) to ensure smooth tracking of pedestrians and other vehicles. To guarantee safety, this is a hard real-time system.
question: What is the per-frame latency budget that the entire inference pipeline must meet?
details:
  realistic_solution: The latency budget is 33.3 milliseconds. This is a hard real-time deadline, meaning that if the system takes longer than this to process a single frame, it has failed. This could result in a safety-critical event, like failing to detect an obstacle in time.
  common_mistake: |
    **The Pitfall:** Confusing hard real-time edge deadlines with soft cloud or UI latency targets.
    **The Rationale:** Engineers recall generic latency numbers (like 100ms for web services or 16ms for 60FPS UI) instead of deriving the budget from the system's operational frame rate.
    **The Consequence:** Designing a system that either drastically over-engineers the compute hardware (aiming for 16ms) or fatally misses safety deadlines (aiming for 100ms).
  napkin_math: |
    * **Calculation**: 1 second = 1000 milliseconds.
    * **Frame Rate**: 30 frames per second.
    * **Budget**: 1000 ms / 30 frames = **33.3 ms per frame**.
  options:
  - 100 ms (Cloud Latency Confusion)
  - 16 ms (Mobile Jank Confusion)
  - 33 ms
  - 1 ms (TinyML Interrupt Confusion)
  correct_index: 2
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 1
deletion_reason: Duplicate of edge-real-time-frame-budget-the-30-fps-deadline-22 (same title + track)
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
