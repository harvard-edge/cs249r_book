schema_version: '1.0'
id: edge-0458
track: edge
level: L3
zone: fluency
topic: attention-scaling
competency_area: architecture
bloom_level: apply
phase: both
title: 'The Drone''s Dilemma: Compute vs. Bandwidth'
scenario: 'You are the ML Systems Engineer for an autonomous drone company using an NVIDIA Jetson AGX
  Orin (205 GB/s memory bandwidth) for its perception system. You have a strict 33ms (30 FPS) latency
  budget for inference to ensure stable flight control. You are asked to evaluate two new INT8-quantized
  models:'
question: "Why does the INT8 EdgeViT miss the 33ms deadline despite its G-Op count seeming feasible on the Orin?"
details:
  realistic_solution: The most likely cause is that the `EdgeViT` model is **memory-bandwidth bound**.
    Its performance isn't limited by the Jetson's compute power, but by the 205 GB/s speed limit for fetching
    data from DRAM.
  common_mistake: Engineers often fixate on FLOPs (or G-Ops) as the primary measure of performance. They
    might incorrectly assume the G-Op count for the Transformer is wrong, that the hardware isn't 'good
    at Transformers', or that the model is thermal throttling. While possible, these usually aren't the
    fundamental reason for this classic performance inversion.
  napkin_math: Orin Ridge Point = 275 TOPS / 205 GB/s = ~1341 Ops/Byte. If EdgeViT performs 3 TeraOps
    and moves 6 GB of memory per frame, its Arithmetic Intensity (AI) = 3e12 / 6e9 = 500 Ops/Byte. Since
    500 < 1341, EdgeViT is heavily memory-bound by the 205 GB/s limit. The MACs sit idle waiting for self-attention
    matrices to load from DRAM.
  options:
  - The EdgeViT's G-Op count is misleading; its self-attention operations are inherently more complex
    and take more clock cycles per OP on the Jetson's architecture.
  - The EdgeViT is likely causing the Jetson to exceed its thermal design power (TDP), forcing the chip
    to throttle its clock speed and increase latency.
  - The EdgeViT has a lower Arithmetic Intensity (AI) due to its memory access patterns, making it memory-bandwidth
    bound on the Jetson's 205 GB/s DRAM interface.
  - The CUDA kernels for Transformer operations are not as mature as CNN kernels, leading to significant
    software overhead from things like kernel launch latency.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: edge-chain-051
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: WARN
validation_issues_pro:
- The 'Napkin Math' section describes the concept of Arithmetic Intensity but does not contain any actual
  calculations to support the conclusion.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
