schema_version: '1.0'
id: edge-1940
track: edge
level: L6+
zone: realization
topic: model-size-estimation
competency_area: architecture
bloom_level: analyze
phase: both
title: 'Coral Edge TPU Feasibility for MobileNetV3-Small: Memory and Performance Estimation'
scenario: A MobileNetV3-Small variant (2.5M parameters) is being deployed on a Google Coral Edge TPU (4 TOPS INT8, 2W). The system requires 30 FPS inference (33ms latency). The model performs roughly 220 Million INT8 operations per inference.
question: Based on a feasibility estimation of memory footprint and INT8 operations, can the Coral Edge TPU meet the 30 FPS requirement?
details:
  realistic_solution: "1.  **Parameter & Weight Memory Estimation**: Calculate the\
    \ memory required for INT8 weights (e.g., `param_count * 1 byte/param`).\n\n\
    2.  **Activation Memory Estimation**: This is more complex. You'd typically run\
    \ a dummy inference with a representative input size on a quantized model (or a\
    \ simulated quantized model) and profile peak memory usage for activations. Alternatively,\
    \ analytical estimation based on layer output shapes.\n\n3.  **TOPS Requirement Calculation**: Determine the total number of INT8 operations\
    \ (e.g., MACs) the model performs for a single inference. Then multiply by the required\
    \ FPS. For example, if a MobileNetV3-Small performs ~220 M INT8 operations, then\
    \ `220M ops * 30 FPS = 6.6 GOPS = 0.0066 TOPS`.\n\n4.  **Feasibility Check**: Compare calculated memory footprint against Coral's\
    \ available memory (e.g., 8MB SRAM for model, 1GB RAM for system) and required\
    \ operations against Coral's 4 TOPS peak.\n\n5.  **Tools**: Use tools like `pytorch_model_summary`, `onnx_optimizer` to get\
    \ layer-wise operations and memory. TensorFlow Lite Converter can provide detailed\
    \ insights into quantized model size and operations."
  common_mistake: |
    **The Pitfall:** Overlooking activation memory or directly comparing FP32 FLOPs to INT8 TOPS.
    **The Rationale:** Engineers size the hardware purely based on parameter count, ignoring intermediate feature maps.
    **The Consequence:** The model fits in storage but OOMs during inference, or fails to meet latency targets because precision conversion wasn't accounted for.
  napkin_math: |
    1. **Weight Memory**: 2.5M parameters * 1 byte (INT8) = 2.5 MB.
    2. **Activation Memory**: Estimated peak ~12 MB.
    3. **Total Footprint**: 2.5 MB + 12 MB = **14.5 MB** (Fits comfortably in 512MB RAM).
    4. **Required Compute**: 220M ops/frame * 30 FPS = 6.6 GOPS = **0.0066 TOPS**.
    5. **Conclusion**: The requirement is < 0.2% of the Coral's 4 TOPS peak. Feasibility is **guaranteed**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
