schema_version: '1.0'
id: edge-0911
track: edge
level: L6+
zone: evaluation
topic: memory-hierarchy-design
competency_area: memory
bloom_level: analyze
phase: both
title: On-Device LLM KV Cache Spilling
scenario: Deploying a 3B parameter LLM on a premium smartphone (8GB unified LPDDR5 RAM at 51.2 GB/s). Initially, text generation achieves 18 tokens/sec. However, when the context window reaches 2000 tokens, generation speed plummets to 3 tokens/sec, and the OS occasionally kills the background app.
question: Why does generation slow at a 2000-token context, and how would you manage the KV cache to avoid OS kills?
details:
  realistic_solution: The performance cliff is due to the KV cache exceeding the NPU/GPU's localized SRAM tier and competing with the OS for unified DRAM, causing severe page thrashing. To fix this, you must change the architecture to aggressively manage memory locality. First, implement 4-bit KV cache quantization to reduce the footprint by 75%. Second, implement a chunked attention mechanism (like Flash-Decoding) that loads smaller blocks of the KV cache into the fast SRAM compute cores, performing local reductions before a final global reduction, minimizing DRAM round-trips and avoiding OS-level page faults.
  common_mistake: |
    **The Pitfall:** Reducing the vocabulary size or hidden dimension of the model to save memory.
    **The Rationale:** Candidates try to solve the memory issue by statically shrinking the model architecture.
    **The Consequence:** This degrades model quality significantly without solving the dynamic scaling issue of the KV cache.
  napkin_math: |
    1. 3B model (INT8) footprint: 3 GB.
    2. KV cache (FP16, 2000 tokens, d_model=3072, 32 layers): 2 * 2 * 3072 * 32 * 2000 = ~786 MB.
    3. Total footprint: 3 GB + 0.786 GB = 3.786 GB.
    4. Max theoretical throughput (memory-bound): 51.2 GB/s / 3.786 GB = 13.5 tokens/sec.
    5. Dropping to 3 tokens/sec implies severe **OS page thrashing/swapping**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 15
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- '`reasoning_competency` and `reasoning_mode` are set to `None`, which limits proper classification of the question''s learning objectives.'
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
