schema_version: '1.0'
id: edge-0405
track: edge
level: L3
zone: fluency
topic: real-time-deadlines
competency_area: latency
bloom_level: apply
phase: inference
title: The Edge Batch Size Paradox
scenario: 'Your colleague trained a model on an A100 using batch size 256 and got great GPU utilization (85%). Now you''re deploying on a Rockchip RK3588 (6 TOPS NPU) for a robotic arm that needs per-frame decisions in under 20ms. Your colleague suggests batching 8 frames to improve NPU utilization.'
question: "Why is this a terrible idea for your use case, and what does the roofline model tell you about batch=1 on edge?"
details:
  realistic_solution: 'The robotic arm needs to react to what it sees *now*. At 30 FPS, collecting 8 frames spans 267ms before inference even begins. Batching means the arm''s control loop acts on data that is 267ms stale - at a 1 m/s arm speed, that''s 26.7cm of uncompensated motion, enough to miss a grasp or collide with an obstacle. The sub-millisecond hardware latency difference is dwarfed by the massive temporal staleness introduced by waiting to build the batch.'
  common_mistake: |
    **The Pitfall:** Conflating throughput optimization with latency optimization.
    **The Rationale:** Candidates often assume that because larger batches yield better hardware utilization, batching is always the correct approach, ignoring real-time temporal staleness.
    **The Consequence:** Introducing unacceptable control loop delays, causing autonomous systems to act on stale data and fail their physical objectives.
  napkin_math: |
    * RK3588 NPU: 6 TOPS, LPDDR4X at 51.2 GB/s. Ridge point = 6T / 51.2G = ~117 Ops/Byte.
    * MobileNetV3 batch=1: 0.22 GFLOPs, ~4.4 MB memory traffic -> AI = 50 Ops/Byte (memory-bound).
    * Attainable performance: 51.2 GB/s * 50 = 2.56 TOPS (43% utilization).
    * Latency (batch=1): 0.22G / 2.56T = **0.086ms**.
    * Batch=8: AI = 200 Ops/Byte (compute-bound). Attainable ~5.1 TOPS (85% util).
    * Latency (batch=8): 1.76G / 5.1T = **0.345ms**.
    * Throughput: Batch 8 is 23,188 FPS vs Batch 1 at 11,627 FPS, but Batch 8 waits 267ms to accumulate frames.
  options:
  - "Batching is essential because it increases NPU utilization. The roofline model shows batch=8 is compute-bound with higher throughput, so we should always use it."
  - "Batching introduces 267ms of temporal staleness before inference even begins, which violates the real-time control requirements. The roofline model shows batch=1 has 0.086ms latency, which easily meets the 20ms deadline despite low utilization."
  - "Batch=1 is compute-bound, so it's already optimal. Batching will make it memory-bound and increase latency."
  - "Batch=8 reduces inference latency to 0.345ms, so it should be used. The 267ms staleness is acceptable for a 1 m/s robot."
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
