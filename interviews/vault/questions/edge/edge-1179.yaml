schema_version: '1.0'
id: edge-1179
track: edge
level: L4
zone: evaluation
topic: transformer-systems-cost
competency_area: compute
bloom_level: evaluate
phase: both
title: 'Edge Transformer Cost Evaluation: Quantized vs Full Transformer on Jetson'
scenario: You must choose between deploying BERT-base (110M params) at FP16 vs INT8 on a Jetson Orin (32GB LPDDR5, 275 TOPS INT8) for a document triage system.
question: Which is better for a real-time document triage system requiring P99 < 50ms, considering throughput, memory footprint, and latency at batch=1?
details:
  realistic_solution: INT8 is vastly superior. At batch size 1, transformer execution is severely memory-bandwidth bound. FP16 weights (220MB) take ~2.15ms to stream from DRAM, easily beating the 50ms deadline. However, INT8 halves this memory footprint to 110MB (~1.07ms latency), doubles energy efficiency, and unlocks massive batching throughput (9800/s vs 4900/s) at virtually zero accuracy degradation for classification tasks.
  common_mistake: |
    **The Pitfall:** Assuming the bottleneck is compute and picking based on TOPS.
    **The Rationale:** Failing to check the arithmetic intensity of batch=1 execution, relying on compute-bound profiling tools instead of memory bandwidth.
    **The Consequence:** Deploying an FP16 model that draws 2x more energy per inference without utilizing the GPU's compute capability.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - BERT-base: 110M parameters. Target: P99 < 50ms.
       - Orin: 102.4 GB/s effective memory bandwidth, 60W.
    2. **Calculations:**
       - FP16 Size: 220 MB. Transfer Time: 220 MB / 102.4 GB/s = 2.15 ms.
       - INT8 Size: 110 MB. Transfer Time: 110 MB / 102.4 GB/s = 1.07 ms.
       - Energy (FP16): 60W * 0.00215s = 129 mJ.
       - Energy (INT8): 60W * 0.00107s = 64.2 mJ.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bandwidth Bound. Both models crush the 50ms SLO, but INT8 consumes half the energy and memory.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
