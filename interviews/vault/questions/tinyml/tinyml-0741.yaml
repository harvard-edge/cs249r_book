schema_version: '1.0'
id: tinyml-0741
track: tinyml
level: L3
zone: diagnosis
topic: graph-compilation
competency_area: optimization
bloom_level: apply
phase: inference
title: The Volatile Variable Wipe
scenario: You deploy a TFLite Micro model on an STM32. To save memory, you allocate a large `static uint8_t arena[10240]` and point both the ML inference engine and a secondary USB data-transfer task to use it. The ML model runs perfectly. When you plug in the USB cable to download logs, the device hard-faults.
question: Why does sharing a 10,240-byte TFLite Micro arena between STM32 inference and USB logging cause a hard fault?
details:
  realistic_solution: You broke the **TFLite Micro Memory Lifecycles**. TFLite metadata takes ~2 KB. If your USB task writes a 512-byte payload to `arena[0]`, you just destroyed the pointers for the first 10 layers of your neural network.
  common_mistake: |
    **The Pitfall:** Assuming a mutex lock resolves memory sharing issues for a static array.
    **The Rationale:** Candidates approach this as a threading problem, assuming tasks are just colliding concurrently.
    **The Consequence:** They add locks but the hard fault persists, because the issue is persistent state corruption, not just concurrent access.
  napkin_math: |
    * **Arena Layout:** TFLite Micro places ~2KB of persistent metadata (tensor shapes, pointers) at `arena[0]`.
    * **USB Write:** The USB task writes a 512-byte packet starting at `arena[0]`.
    * **Corruption:** This permanently overwrites bytes 0–511, destroying the neural network's graph descriptors.
    * **Result:** The next ML inference dereferences a corrupted pointer, causing a HardFault at 0xDEAD_BEEF.
    * **Fix:** Allocate ML arena (8KB) and USB buffer (2KB) as entirely isolated arrays.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
chains:
- id: tinyml-chain-000
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
