schema_version: '1.0'
id: tinyml-0698
track: tinyml
level: L2
zone: fluency
topic: mcu-compute-constraints
competency_area: memory
bloom_level: apply
phase: inference
title: The Flash-SRAM Boundary
scenario: 'A junior engineer asks: ''Why can''t we just run the model entirely from flash? It''s 1 MB
  — way bigger than SRAM.'' Explain why model weights live in flash but activations must live in SRAM
  on a Cortex-M4.'
question: Why can Cortex-M4 weights stay in 1 MB flash while activations must fit in 256 KB SRAM?
details:
  realistic_solution: 'Weights are read-only during inference — they never change, so they map naturally
    to flash (non-volatile, read-only, large). Activations are the intermediate tensors computed at each
    layer — they are written, overwritten, and discarded every inference, requiring read-write SRAM. On
    an STM32F4: 1MB flash holds weights (200KB model uses 20% of flash), while 256KB SRAM holds activations
    (~150KB peak), firmware (40KB), and stack (8KB). If you tried to copy weights into SRAM, you would
    need 200+150+48 = 398KB, exceeding the 256KB limit. Flash-resident weights are a requirement, not
    an optimization.'
  common_mistake: '**The Pitfall:** Believing flash is too slow for inference.

    **The Rationale:** Candidates assume flash read speed is the bottleneck, ignoring that STM32F4 flash
    can be read at up to 168 MHz with the ART accelerator.

    **The Consequence:** They wrongly conclude weights must be copied to SRAM, leading to out-of-memory
    errors.

    '
  napkin_math: '**Assumptions & Constraints:**

    - STM32F4: 1 MB flash, 256 KB SRAM.

    - 200 KB INT8 model.

    - Activations peak at ~150 KB.

    - Firmware: 40 KB; Stack: 8 KB.


    **Calculations:**

    - SRAM with weights in Flash: 150 KB (activations) + 40 KB (firmware) + 8 KB (stack) = 198 KB (Fits
    in 256 KB).

    - SRAM if weights copied: 200 KB (weights) + 198 KB = 398 KB (Exceeds 256 KB).


    **Conclusion & Interpretation:**

    - **Result: 198 KB vs 398 KB (Memory-Bound)**. Flash-resident weights are not an optimization; they''re
    a requirement, as copying them to SRAM exceeds capacity.

    '
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Classification sanity - `reasoning_competency` and `reasoning_mode` are incorrectly set to `None` for
  a question that clearly tests Memory Management, Microcontroller Architecture, and TFLite Micro Architecture.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
