schema_version: 1
id: tinyml-0471
title: The Real-Time Interrupt Stall
topic: real-time-deadlines
status: published
provenance: imported
scenario: 'You are a TinyML engineer diagnosing a fault in a smart factory sensor. The device uses a Cortex-M4
  MCU (168 MHz) to monitor a conveyor belt. It runs two tasks:'
details:
  realistic_solution: The most likely cause is uncontrolled preemption. The 0.2ms temperature check interrupt
    is firing in the middle of the vibration analysis. While the MCU is fast enough to run the analysis
    in time, the interruption adds just enough overhead and delay to push it over the 1ms deadline. The
    high-priority, real-time task is being sabotaged by a low-priority, non-critical one. The solution
    is to use a Real-Time Operating System (RTOS) to manage priorities correctly, or, for a bare-metal
    system, to disable interrupts during the time-critical section of the FFT calculation to ensure it
    runs to completion without being preempted.
  common_mistake: Engineers often blame the hardware first, suggesting a faster MCU or more memory. They
    might also incorrectly calculate the compute time without considering the cost of preemption (context
    switching and cache misses) or assume that a short interrupt task can't impact a longer one.
  napkin_math: 'Vibration FFT: 50M FLOPs / 336 MFLOPS = 0.149ms per inference, fits 1ms deadline. But
    temp interrupt fires every 5ms for 0.2ms. During 0.149ms FFT window, probability of interrupt = 0.149/5
    = 3%. When hit: FFT takes 0.149+0.2 = 0.349ms. Still under 1ms. But if FFT is 0.9ms: 0.9+0.2 = 1.1ms
    > 1ms. Preemption pushes past deadline. Fix: disable IRQ during critical section or raise FFT priority.'
chain:
  id: tinyml-chain-081
  position: 3
last_modified: '2026-04-01'
tags:
- scope:Microcontroller
