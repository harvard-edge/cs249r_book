schema_version: '1.0'
id: tinyml-1070
track: tinyml
level: L3
zone: analyze
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: inference
title: Per-Channel Requantization Overhead Analysis
scenario: You are profiling a keyword spotting model on an ARM Cortex-M4 (STM32F4)
  microcontroller (168 MHz, 256 KB SRAM, 1 MB flash, no FPU). You notice that switching
  a convolutional layer with 128 output channels from per-tensor to per-channel INT8
  quantization increases the execution time by exactly 4.57 microseconds. The CMSIS-NN
  SIMD core (2x INT8 MACs/cycle) executes the exact same number of core operations
  in both cases. Explain why this system behavior occurs. Given that the latency overhead
  scales linearly with the number of output channels, calculate the number of additional
  clock cycles required per output channel.
question: How many extra cycles per channel explain the 4.57 us per-channel quantization
  overhead on STM32F4?
details:
  realistic_solution: The core matrix multiplication uses the same 2x INT8 MACs/cycle
    SIMD instructions regardless of the quantization scheme. The latency increase
    occurs during the requantization step in the outer loop. Per-channel quantization
    requires fetching a unique scale multiplier and shift value from memory for each
    output channel to downscale the 32-bit accumulator back to INT8, whereas per-tensor
    uses a single global scale that can be kept in registers.
  common_mistake: Candidates often mistakenly believe the latency difference comes
    from the inner loop MAC operations, assuming that per-channel scales break the
    SIMD execution pipeline. They fail to recognize that the core dot product remains
    identical and the overhead is strictly isolated to the outer loop.
  napkin_math: Total time overhead = 4.57 microseconds (0.00000457 seconds). CPU frequency
    = 168 MHz (168,000,000 cycles/sec). Total overhead cycles = 0.00000457 * 168,000,000
    = 767.76 cycles (round to 768). Overhead per output channel = 768 cycles / 128
    channels = 6 extra clock cycles per output channel.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- RC, KA, and Mode are all set to None; these should be explicitly classified for
  a Staff-level question.
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
