schema_version: '1.0'
id: tinyml-0740
track: tinyml
level: L4
zone: design
topic: mcu-compute-constraints
competency_area: precision
bloom_level: create
phase: inference
title: The Integer Arithmetic Engine
scenario: Walk me through exactly how a quantized Conv2D layer executes on a Cortex-M4 with no floating-point unit.
question: How does an integer-only quantized Conv2D execute on a Cortex-M4 from inputs through requantized outputs?
details:
  realistic_solution: 'The full integer-only inference pipeline accumulates an integer dot product into 32-bit registers, then multiplies by a precomputed multiplier M (applied via integer multiply and bit-shift), and finally shifts by the zero point before clamping back to 8-bit.'
  common_mistake: |
    **The Pitfall:** Over-simplifying INT8 convolution.
    **The Rationale:** Candidates think INT8 convolution just means "multiply and truncate."
    **The Consequence:** They ignore accumulator width overflows, zero-point offsets, and the complex integer-shift requantization step necessary for mathematical correctness.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 3x3 depthwise conv on 48x48x64 feature map.
       - CMSIS-NN SMLAD instruction (2 MACs/cycle) at 100 MHz.
    2. **Calculations:**
       - MACs = 3 * 3 * 48 * 48 * 64 = 1,327,104.
       - SMLAD Throughput = 200M MACs/s.
       - Core Time = 1.327M / 200M = 6.63 ms.
       - Requantization Overhead = ~147,456 ops (negligible).
    3. **Conclusion & Interpretation:**
       - **Result: ~6.6 ms.** Execution is strictly **Compute-Bound** on the integer ALU, with CMSIS-NN SIMD effectively halving the latency.
  options:
  - Simply multiply INT8 inputs by INT8 weights, truncate to INT8, and output. The zero-points and scales are only needed during training, not inference.
  - 'Full integer pipeline: accumulate INT8xINT8 products into INT32, subtract cross-terms involving zero-points, add INT32 bias. Then requantize via integer multiply and bit-shift, apply zero-point, and clamp to [-128, 127]. No floats anywhere.'
  - The Cortex-M4 must use software floating-point emulation for the scale factors during requantization, adding ~10 cycles per output element.
  - 'The pipeline uses lookup tables (LUTs) for all multiplications: precompute all 256x256 possible INT8 products in a 64 KB table and replace multiplication with table lookups.'
  correct_index: 1
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
chains:
- id: tinyml-chain-076
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
