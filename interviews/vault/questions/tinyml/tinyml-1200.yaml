schema_version: '1.0'
id: tinyml-1200
track: tinyml
level: L6+
zone: mastery
topic: pruning-sparsity
competency_area: optimization
bloom_level: create
phase: training
title: NPU-Aware Sparsity on Corstone-300
scenario: You are deploying a 15 million MAC, 750 KB acoustic anomaly detection model on an ARM Cortex-M7 + Ethos-U55 (Corstone-300) system. The system has 512 KB of shared SRAM, but your model currently exceeds this physical budget. You must design a pruning methodology from scratch that not only compresses the model to fit into SRAM but also maximizes the specific MAC utilization of the Ethos-U55 NPU.
question: How should a 750 KB acoustic model be pruned to fit Corstone-300's 512 KB SRAM and preserve Ethos-U55 utilization?
details:
  realistic_solution: 'Design a hardware-aware structured pruning pipeline tailored to the Ethos-U55''s native weight compression format and MAC array configuration. By aligning the pruned blocks with the NPU''s 128-512 MAC/cycle burst sizes, the hardware can efficiently decode weights and skip zero-fetches entirely. This reduces the memory footprint to fit within the 512 KB SRAM and drastically reduces the memory bandwidth contention with the 480 MHz M7.'
  common_mistake: |
    **The Pitfall:** Proposing generic unstructured magnitude pruning to reach the size constraint.
    **The Rationale:** Developers often apply standard cloud-based pruning techniques without realizing that unstructured sparse formats (like CSR) require indexing metadata and do not align with hardware acceleration blocks.
    **The Consequence:** The model footprint may actually bloat due to metadata, or the Ethos-U55 NPU falls back to extremely slow execution because it cannot natively accelerate unstructured sparsity.
  napkin_math: |
    **Assumptions & Constraints:**
    - Target Memory: 512 KB SRAM - ~137 KB for activations = max 375 KB for weights.
    - Target NPU: Ethos-U55 natively accelerates zero-skipping if aligned to 128-512 MAC/cycle bursts.

    **Calculations:**
    - Pruning requirement: 750 KB / 375 KB = 50% sparsity required.
    - Compute time: 15M MACs reduced to 7.5M active MACs via 50% structured block pruning.
    - Inference Latency: Ethos-U55 at 480 MHz (256 MAC/cycle) has a peak of 122.8 GMAC/s. Assuming 40% memory-bound utilization: 7.5M / (122.8 * 10^9 * 0.4) ≈ 0.15 ms.

    **Conclusion & Interpretation:**
    - **Result: ~0.15 ms Latency (Compute-Bound).** Hardware-aware structured pruning drastically reduces memory footprint and maximizes NPU MAC utilization.
status: published