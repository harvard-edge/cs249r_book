schema_version: '1.0'
id: tinyml-1469
track: tinyml
level: L5
zone: evaluation
topic: streaming-ingestion
competency_area: data
bloom_level: analyze
phase: inference
title: 'Real-Time Anomaly Detection on TinyML: Cortex-M7 + Ethos-U55 Architecture Evaluation'
scenario: A large industrial facility needs to implement real-time anomaly detection across hundreds of machines. Each machine will be outfitted with an edge device collecting high-frequency sensor data (e.g., accelerometer at 10kHz, microphone at 44.1kHz). The goal is to detect anomalous patterns directly on the device, triggering immediate alerts. The chosen hardware platform is a System-on-Chip featuring a Cortex-M7 processor (480 MHz clock speed, 512 KB SRAM) paired with an Arm Ethos-U55 microNPU (configurable from 128 to 512 MAC/cycle). Two architectural approaches are proposed: Architecture A (Edge-Centric Feature Extraction & NPU Inference) and Architecture B (Raw Data NPU Inference with Minimal CPU Pre-processing).
question: How should Cortex-M7 plus Ethos-U55 streaming architectures compare feature extraction versus raw-data NPU inference?
details:
  realistic_solution: For Architecture A, carefully select very low-complexity feature extractors that fit within the Cortex-M7's processing budget and SRAM, coupled with highly quantized NPU models. For Architecture B, focus on designing NPU-friendly models (e.g., convolutional networks with efficient operations) that can process raw data directly and make optimal use of the Ethos-U55's MAC capabilities, using the Cortex-M7 mainly for data marshaling. Using DMA for sensor data transfer to minimize CPU overhead. Employing dynamic power management strategies, leveraging the NPU for bursts of activity and allowing the CPU to sleep.
  common_mistake: |
    **The Pitfall:** Overlooking SRAM limitations and underutilizing the NPU.
    **The Rationale:** System designers focus on the massive compute capability of the NPU but forget that raw data ingestion and intermediate activations require significant memory.
    **The Consequence:** Architecture A burns CPU cycles unnecessarily, while Architecture B runs out of SRAM, leading to system failure despite having plenty of compute headroom.
  napkin_math: |
    *   **Data Rate:** Accelerometer at 10 kHz, 3-axis float32 = 3 × 4 bytes × 10,000 = 120 KB/s.
    *   **Raw Window (Arch B):** 128-sample window (12.8 ms) = 128 × 3 × 4 = 1,536 bytes.
    *   **NPU Compute Time:** 100,000 MACs / (512 MACs/cycle × 480 MHz) = ~400 nanoseconds.
    *   **Memory Constraint:** A 100 KB model leaves 412 KB of SRAM for buffers and code.
    *   **Conclusion:** The NPU is incredibly fast (~400 ns for inference within a 12.8 ms window), making SRAM capacity the primary architectural bottleneck, not compute.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
