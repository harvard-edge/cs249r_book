schema_version: '1.0'
id: tinyml-0408
track: tinyml
level: L3
zone: diagnosis
topic: mcu-compute-constraints
competency_area: compute
bloom_level: apply
phase: inference
title: The Hardware Divider Stall
scenario: 'You are porting an anomaly detection algorithm to a Cortex-M0+. The code
  has a normalization step inside the tightest inner loop: `float norm = val / max_val;`.
  Your profiling shows this single division operation takes an astonishing 40 clock
  cycles. Why is division so slow on this specific chip?'
question: Why does `val / max_val` cost about 40 cycles inside a Cortex-M0+ normalization
  loop?
details:
  realistic_solution: 'The Cortex-M0+ lacks hardware divide instructions (SDIV/UDIV).
    Division is emulated in software via iterative bit-shifting, taking ~40 cycles
    vs 1-2 cycles for hardware MUL. Fix: precompute the reciprocal once (float inv
    = 1.0f / max_val) outside the loop, then multiply inside (norm = val * inv). This
    converts a 40-cycle division to a 1-2 cycle multiplication — a 20x speedup on
    the inner loop. For INT8 inference, use fixed-point arithmetic: multiply by a
    scaled integer reciprocal and right-shift, avoiding floating-point entirely.'
  common_mistake: '"Floating point math is slow." While true on chips without an FPU,
    division is uniquely terrible on the M0+.'
  napkin_math: 'Software division: ~40 cycles. Hardware multiplication: 1-2 cycles.
    Changing a `/` to a `*` makes the normalization step 20x faster.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
chains:
- id: tinyml-chain-085
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
