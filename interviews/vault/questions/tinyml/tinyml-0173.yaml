schema_version: '1.0'
id: tinyml-0173
track: tinyml
level: L3
zone: recall
topic: real-time-deadlines
competency_area: latency
bloom_level: remember
phase: inference
title: The 1ms Interrupt Budget
scenario: You are designing a keyword spotting system on a Cortex-M4 microcontroller. The system uses DMA to fill an audio buffer, which then triggers an interrupt.
question: To ensure the system feels instantaneous and never misses an audio packet, what is the typical latency budget for the Interrupt Service Routine (ISR) that handles this trigger?
details:
  realistic_solution: The latency budget for a real-time ISR on a microcontroller is extremely tight, typically around 1 millisecond (1ms). The ISR's job is to perform the absolute minimum work required—like acknowledging the interrupt and swapping buffer pointers—and then return control to lower-priority tasks that will run the actual inference. Exceeding this budget can block other critical system interrupts (like timers) and lead to system failure.
  common_mistake: |
    **The Pitfall:** Assuming latencies are in the tens or hundreds of milliseconds.
    **The Rationale:** Engineers accustomed to cloud or mobile development confuse a hard real-time interrupt deadline with softer deadlines like UI 'jank' budgets or web request latencies.
    **The Consequence:** An ISR that takes 16ms will block other critical system interrupts (like timers) and lead to catastrophic system failure and data loss.
  napkin_math: |
    1. **Hard Real-Time Constraint:** ~1 ms.
    2. **Mobile 'Jank' Budget:** 16 ms (16x too slow).
    3. **Real-Time Edge Deadline:** 33 ms (33x too slow).
    4. **Cloud P99 Latency:** 100 ms (100x too slow).
  options:
  - ~100 ms
  - ~33 ms
  - ~16 ms
  - ~1 ms
  correct_index: 3
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
chains:
- id: tinyml-chain-080
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
