schema_version: '1.0'
id: tinyml-1255
track: tinyml
level: L5
zone: realization
topic: quantization-fundamentals
competency_area: precision
bloom_level: evaluate
phase: inference
title: Evaluating Quantization Granularity on nRF5340
scenario: You are deploying a continuous wake-word detection model on the Nordic nRF5340. The application core runs at 128 MHz and must process audio frames within a strict 50ms latency budget to maintain the ~5mA active current draw target for battery life. You have a choice between per-tensor and per-channel symmetric INT8 post-training quantization for the model's depthwise separable convolutional layers. The device has 256 KB SRAM and 1 MB flash, which must also accommodate the BLE 5.3 networking stack.
question: Should an nRF5340 wake-word model use per-tensor or per-channel INT8 quantization for depthwise layers under a 50 ms budget?
details:
  realistic_solution: Recommend per-tensor quantization to meet the strict latency and power constraints, using QAT if accuracy is insufficient. For depthwise layers, per-channel quantization requires applying a different scale factor per output channel, which drastically increases the cycle count in the inner loop on a standard ARM Cortex-M33. Given the 50ms latency constraint, the extra cycles for channel-wise scaling and the memory overhead for storing scale factors make per-channel quantization prohibitive.
  common_mistake: |
    **The Pitfall:** Defaulting to per-channel quantization for better accuracy.
    **The Rationale:** ML practitioners prioritize model accuracy and assume per-channel quantization has negligible runtime cost.
    **The Consequence:** On an ARM Cortex-M33 lacking specialized vector instructions, per-channel rescaling adds severe computational and memory overhead, violating real-time latency and power budgets.
  napkin_math: |
    *   **Cycle Budget:** 128 MHz × 0.05 s = 6.4 million cycles per inference.
    *   **SRAM Budget:** 256 KB total - ~64 KB (BLE stack & OS) = ~192 KB for model.
    *   **Memory Overhead (Per-Channel):** For a layer with 256 channels, scale factors add 256 × 4 bytes = 1 KB. Across 10 layers, this is 10 KB overhead.
    *   **Compute Overhead (Per-Channel):** Rescaling adds ~4 extra cycles per MAC. For 1 million MACs, this is 4 million extra cycles.
    *   **Latency Impact:** 4 million / 6.4 million = 62% of the total cycle budget consumed just by rescaling, severely risking a latency miss.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
