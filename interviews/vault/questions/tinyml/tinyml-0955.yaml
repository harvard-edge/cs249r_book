schema_version: '1.0'
id: tinyml-0955
track: tinyml
level: L3
zone: analyze
topic: dataset-curation
competency_area: data
bloom_level: analyze
phase: training
title: Active Learning SRAM Overflow
scenario: >
  A defect detection system on a Corstone-300 (M7 + Ethos-U55, 512 KB shared SRAM) extracts 64x64 ROIs.
  To capture low-contrast anomalies, the upstream edge-detection threshold is lowered, causing OOM crashes or missed 20ms real-time latency budgets.
question: Why does curating a dataset of low-contrast anomalies cause these specific hardware failures?
details:
  realistic_solution: |
    By actively curating low-contrast anomalies to train the CNN, the team forced a system-level relaxation of the M7's ROI extraction threshold so those faint defects could actually reach the NPU. This lowered threshold generates an exponential increase in false-positive ROIs per frame. Because the system batches these ROIs to maximize Ethos-U55 utilization, the memory footprint of the batched 64x64 crops quickly exceeds the Corstone-300's tight 512 KB shared SRAM limit, causing OOM crashes. When memory doesn't fault, the sheer M7 CPU time spent cropping and moving hundreds of ROIs starves the latency budget.
  common_mistake: |
    **The Pitfall:** Arguing that the Ethos-U55 dynamically takes longer to compute or allocates more memory for 'harder' or 'noisier' low-contrast images.
    **The Rationale:** Candidates fail to realize that NPU execution on static CNN graphs is input-independent and deterministic.
    **The Consequence:** They waste time trying to optimize the CNN instead of fixing the classical preprocessing bottleneck on the M7.
  napkin_math: |
    **Assumptions & Constraints:**
    - Budget: 20 ms. Memory limit: 512 KB shared SRAM.
    - ROI: 64x64 8-bit (4 KB/ROI).

    **Calculations:**
    - Lowered threshold increases ROIs from 10 to 150 per frame.
    - Batched Memory: 150 ROIs * 4 KB = 600 KB. (Exceeds 512 KB limit -> OOM).
    - Processing Time: 150 ROIs * 0.1ms/crop = 15ms CPU overhead.
    - Remaining budget: 20ms - 15ms = 5ms (Starves NPU scheduling).

    **Conclusion & Interpretation:**
    - **Result: Memory/Compute-Bound (Preprocessing Overhead)**. High ROI count blows out both memory and CPU cycle budgets.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
