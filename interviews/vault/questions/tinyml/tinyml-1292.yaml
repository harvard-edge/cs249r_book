schema_version: '1.0'
id: tinyml-1292
track: tinyml
level: L4
zone: specification
topic: extreme-quantization
competency_area: precision
bloom_level: analyze
phase: inference
title: Sub-4-bit Quantization Specification for nRF5340 Audio Model
scenario: You are tasked with deploying a continuous audio event classification model on a Nordic nRF5340 (128 MHz app core, 256 KB SRAM, 1 MB flash). The baseline float32 model has 1.2 million parameters and peak float32 activations of 600 KB. The system must run concurrently with the BLE 5.3 stack operating at ~5mA active.
question: How should you specify sub-4-bit weights and INT8 activations so a 1.2M-parameter nRF5340 audio model fits flash and SRAM?
details:
  realistic_solution: The correct specification applies a mixed-precision or extreme quantization technique like AWQ or GPTQ to the weights, reducing them to an average of 3 bits per parameter, and quantizes activations to INT8. The design must explicitly partition the 1 MB flash to leave room for the BLE stack and application code, and similarly partition the 256 KB SRAM to ensure the INT8 activations do not trigger out-of-memory errors during context switching.
  common_mistake: |
    **The Pitfall:** Suggesting standard uniform 8-bit quantization for weights and forgetting OS memory overhead.
    **The Rationale:** Candidates assume INT8 is always sufficient and treat the entire memory footprint as available strictly for the ML model.
    **The Consequence:** The model weights require 1.2 MB (exceeding the 1 MB flash limit) and activation buffers overflow the remaining SRAM.
  napkin_math: |
    - Baseline weights: 1.2M params * 4 bytes = 4.8 MB.
    - Flash available: 1 MB total - 300 KB (RTOS + BLE) = 700 KB.
    - Max weight bit-width: (700 KB * 1024 * 8 bits) / 1,200,000 params = 4.77 bits/param.
    - Specifying 3-bit average weight quantization: 1.2M * 3 / 8 = 450 KB (fits safely in flash).
    - SRAM available: 256 KB total - 64 KB (OS/BLE) = 192 KB.
    - Quantizing activations to INT8 reduces peak from 600 KB to 150 KB.
    - **150 KB activations < 192 KB available SRAM, satisfying the memory limits.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
