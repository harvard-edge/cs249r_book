schema_version: '1.0'
id: tinyml-0197
track: tinyml
level: L2
zone: recall
topic: tensor-arena-planning
competency_area: memory
bloom_level: understand
phase: inference
title: 'The Tensor Arena Squeeze: Tensor Arena Planning'
scenario: 'You''re deploying a keyword spotting model to a Cortex-M4 microcontroller with 256KB of SRAM.
  The TensorFlow Lite for Microcontrollers interpreter needs a pre-allocated ''tensor arena'' for activations.
  Your model has the following structure and intermediate tensor sizes:'
details:
  realistic_solution: The tensor arena is a single, contiguous block of memory allocated upfront from
    which TensorFlow Lite for Microcontrollers carves out space for all input, output, and intermediate
    activation tensors. It uses a memory manager to reuse space from tensors that are no longer live in
    the computation graph.
  common_mistake: The most common mistake is to sum the sizes of *all* tensors. This ignores the fact
    that the memory interpreter reuses memory. Once a tensor is no longer needed for a subsequent calculation
    (e.g., the input tensor after the first convolution), its memory can be reclaimed for a future tensor.
    The peak usage is what matters, not the total.
  napkin_math: Tensor arena must hold all live tensors at each op. E.g., Conv1 in=2KB out=8KB (live=10KB).
    Conv2 in=8KB out=16KB (live=24KB). Conv3 in=16KB out=4KB (live=20KB). Peak = 24KB at Conv2. Arena
    size = 24KB. Must fit within 256KB along with model weights, stack, and firmware. If model weights
    = 200KB, only 56KB left for arena.
  options:
  - 3534 bytes (Sum of all tensors; forgets memory reuse)
  - 2000 bytes (Size of largest single tensor; forgets concurrent tensors)
  - 3040 bytes
  - 2490 bytes (Peak usage during Conv1, but not the absolute peak)
  correct_index: 2
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 2
deletion_reason: Duplicate of tinyml-tinyml-tensor-arena-the-tensor-arena-squeeze-135 (same title + track)
validated: false
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- 'The ''Scenario'' describes the model structure with ''Layer 1: Input=2KB, Output=8KB'', ''Layer 2:
  Input=8KB, Output=16KB'', ''Layer 3: Input=16KB, Output=4KB'', but the ''Napkin Math'' introduces generic
  ''Conv1'', ''Conv2'', ''Conv3'' with different input/output sizes (Conv1 in=2KB out=8KB; Conv2 in=8KB
  out=16KB; Conv3 in=16KB out=4KB). While the structure is similar, using the exact labels and values
  from the scenario would improve consistency.'
- The 'Napkin Math' provides an example of model weights = 200KB. However, the scenario does not specify
  model weights, and this could be an implicit assumption that might not be true for all interpretations
  of the question, making the calculation less general.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
classification_review: Flash is correct. The question asks about calculating tensor arena sizes — this
  requires quantitative computation of tensor dimensions and peak memory, not just concept recall. Mode
  should be 'napkin-math'. The napkin_math field is also incomplete ('1. Calculate size of each tensor:'
  with no actual calculations).
