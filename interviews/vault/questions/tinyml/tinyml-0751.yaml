schema_version: '1.0'
id: tinyml-0751
track: tinyml
level: L4
zone: diagnosis
topic: graph-compilation
competency_area: optimization
bloom_level: evaluate
phase: inference
title: The TFLite Micro Resolving Pointer
scenario: You profile a TFLite Micro inference loop on a Cortex-M4. You notice that the actual math kernel
  (`arm_convolve_s8`) takes 10ms, but the overall `invoke()` call takes 13ms. You trace the extra 3ms
  to the framework's internal `GetTensor()` function. Why does looking up a tensor address take 3ms?
question: "Why does looking up a tensor address take 3ms?"
details:
  realistic_solution: 'TFLite Micro stores tensors in a FlatBuffer — a serialized format designed for
    mobile, not MCUs. Each GetTensor() call parses the FlatBuffer''s offset tables, performs bounds checking,
    and resolves nested pointers. With poor cache locality on the FlatBuffer''s vtable structure (scattered
    across memory), each lookup incurs multiple cache misses. For 150 GetTensor calls per inference at
    ~1000 cycles each: 150K cycles = 1-3ms overhead. Fix: (1) use TFLite Micro''s static memory planner
    which pre-resolves all tensor pointers at init, or (2) cache resolved pointers in a lookup table after
    the first inference. This eliminates repeated FlatBuffer parsing.'
  common_mistake: '"The tensor is stored in slow Flash." `GetTensor()` just returns a pointer; it doesn''t
    read the whole tensor.'
  napkin_math: A 50-layer model with 3 tensors per layer = 150 `GetTensor` calls per inference. If a dynamic
    lookup and bounds check takes 1,000 cycles (due to poor cache locality on the FlatBuffer), you lose
    150,000 cycles (~1-3ms) purely on framework plumbing.
  options:
  - GetTensor() is slow because it reads the full tensor data from flash memory into SRAM on each call,
    wasting time copying data that's already accessible via XIP.
  - 'TFLite Micro stores tensors in a FlatBuffer format. Each GetTensor() call parses offset tables, performs
    bounds checking, and resolves nested pointers. With poor cache locality on scattered vtable structures,
    each lookup incurs multiple cache misses. For 150 GetTensor calls per inference at ~1000 cycles each:
    150K cycles = 1-3ms overhead. Fix: use TFLite Micro''s static memory planner (pre-resolves all pointers
    at init) or cache resolved pointers in a lookup table after first inference.'
  - The 3ms overhead is caused by TFLite Micro's garbage collector, which runs between every layer to
    reclaim temporary activation buffers.
  - GetTensor() acquires a mutex lock for thread safety on each call. The lock/unlock overhead of the
    RTOS accounts for the 3ms because the RTOS scheduler runs on every mutex release.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
chains:
- id: tinyml-chain-029
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The question is missing 'reasoning_competency' and 'reasoning_mode' classifications.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
