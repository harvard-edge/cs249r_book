schema_version: '1.0'
id: tinyml-0728
track: tinyml
level: L5
zone: evaluation
topic: mcu-compute-constraints
competency_area: memory
bloom_level: evaluate
phase: inference
title: The Flash Read Disturb
scenario: Your always-on vibration monitoring system on a Cortex-M4 (STM32L4, 80 MHz, 256 KB SRAM, 1 MB
  internal flash) runs inference 10 times per second, reading the 400 KB model weights from flash via
  XIP (Execute-in-Place) each time. After 6 months of continuous operation, the model's anomaly detection
  precision drops from 0.94 to 0.82. A firmware update (re-flashing the same model) restores performance.
  What degraded?
question: "Why does a Cortex-M4 vibration model reading 400 KB weights from flash lose precision after 6 months until re-flashed?"
details:
  realistic_solution: The culprit is **NOR flash read disturb** — a well-documented but rarely encountered
    phenomenon in embedded systems where repeatedly reading the same memory cells without erasing them gradually
    injects stray charge into adjacent cells, eventually flipping their bits and corrupting the weights.
  common_mistake: '"Flash doesn''t wear out from reading — only writes cause wear. Must be a firmware
    bug that accumulated over time." Flash does have a read-related degradation mechanism that most embedded
    developers never encounter because their devices don''t read the same addresses billions of times.'
  napkin_math: 'Reads per cell over 6 months: 155.5M × 8 = 1.24B. Disturb threshold: ~500M reads (for
    STM32L4''s 90nm NOR flash). Margin exceeded by: 1.24B / 500M = 2.48×. Expected bit flip rate: ~15%
    of cells in hot pages. Model weight errors: 480K flipped bits across 400 KB. Average weight error:
    ~3 LSBs (weighted by bit position probability). Accuracy impact: empirically, ±3 LSB average weight
    noise on INT8 models causes ~10-15% accuracy drop — matches the observed 0.94 → 0.82. Refresh interval
    to prevent: 500M reads / (10 inf/s × 8 disturbs × 86,400 s/day) = 72.3 days. Refresh monthly for safety
    margin.'
  options:
  - Flash doesn't wear out from reading — only writes cause wear. The accuracy drop must be from a firmware
    bug that accumulated sensor calibration drift over 6 months.
  - 'NOR flash read disturb: the 400 KB model is read via XIP at 10 inferences/second, accumulating ~1.24
    billion reads per cell over 6 months. This exceeds the ~500M read-disturb threshold for 90nm NOR flash,
    causing ~15% of cells in hot pages to flip bits. The ~480K flipped bits create ~3 LSB average weight
    noise on INT8 values, matching the observed 12% accuracy drop. Fix: periodically refresh (re-write)
    the model sectors every ~72 days, or move the model to MRAM/FRAM.'
  - The flash memory cells are slowly losing charge at room temperature (data retention degradation),
    causing random bit flips that accumulate over 6 months.
  - The MCU's voltage regulator has drifted over 6 months, causing the flash read voltage to shift outside
    the optimal sensing window, producing intermittent read errors.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
