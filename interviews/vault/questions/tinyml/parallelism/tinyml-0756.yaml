schema_version: '1.0'
id: tinyml-0756
track: tinyml
level: L6+
zone: design
topic: on-device-learning
competency_area: parallelism
bloom_level: create
phase: training
title: Continuous Learning on MCU
scenario: Your Cortex-M7 (480 MHz, 1 MB SRAM) runs a 50 KB anomaly detection model on remote oil rigs utilizing expensive satellite connectivity (2400 bps, $8/MB). The data distribution has shifted, requiring an updated model.
question: Is it economically and computationally feasible to perform on-device incremental learning on the Cortex-M7 rather than pushing a full model update via satellite?
details:
  realistic_solution: On-device incremental learning is highly practical if restricted to last-layer fine-tuning. By freezing the convolutional backbone and only updating the final classification head, the memory requirements for activation buffers, gradients, and optimizer states drop significantly (to ~92 KB). This comfortably fits inside the 1 MB SRAM, enabling the 480 MHz CPU to converge the network locally for less than a cent of electricity, entirely avoiding the exorbitant satellite data costs.
  common_mistake: |
    **The Pitfall:** Dismissing backpropagation on microcontrollers outright.
    **The Rationale:** Engineers assume standard backpropagation memory footprints mathematically rule out any form of on-device training.
    **The Consequence:** They architect massively expensive cloud retraining loops, burning significant capital transmitting raw data over slow satellite links when the local hardware possesses adequate capacity for targeted fine-tuning.
  napkin_math: |
    **Assumptions & Constraints:**
    - 50 KB model on 480 MHz Cortex-M7 (1 MB SRAM).
    - Satellite Cost: $8/MB at 2400 bps.
    - Training samples: 100 samples at 5000 MACs/sample.

    **Calculations:**
    - SRAM Footprint: Activations (4 KB) + Gradients (4 KB) + Optimizer (4 KB) + Data Buffer (80 KB) = 92 KB.
    - Local Compute Time: (10 epochs * 100 samples * 5000 MACs) / (480,000,000 * 2) = 5.2 ms.
    - Satellite Cost: 50 KB = 0.05 MB * $8/MB = $0.40 per device.

    **Conclusion & Interpretation:**
    - **Result: Cost-Bound Deployment**. Last-layer fine-tuning requires only 92 KB of SRAM and 5ms of compute, reducing the $0.40/device satellite update cost to absolute zero.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
