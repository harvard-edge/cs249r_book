schema_version: '1.0'
id: tinyml-0653
track: tinyml
level: L6+
zone: design
topic: 3d-parallelism
competency_area: parallelism
bloom_level: create
phase: training
title: The Dual-Core Synchronization Deadlock
scenario: 'You are using an RP2040 (Dual-core Cortex-M0+). Core 0 handles camera I/O and Core 1 runs the
  ML model. To share the tensor arena safely, you implement a simple boolean spinlock: `while(lock ==
  true); lock = true;`. Core 0 locks it, writes the image, and unlocks it. Core 1 locks it, runs ML, and
  unlocks it. After an hour, both cores freeze simultaneously in the `while` loop. How did a lock designed
  to prevent collisions cause a permanent deadlock?'
details:
  realistic_solution: 'The boolean spinlock is not atomic on RP2040. The read-modify-write sequence (read
    lock, check if false, write true) takes multiple cycles. If both cores execute the read simultaneously
    and both see ''false'', both set ''true'' and proceed — corrupting shared data. Eventually, a timing
    coincidence causes both cores to enter the while() loop waiting for the other to release. At 133MHz
    with a 15ns race window, this happens roughly once per hour at 30 fps. Fix: use RP2040''s hardware
    spinlocks (SIO block) — atomic test-and-set in a single cycle, guaranteed by silicon, not software.'
  common_mistake: '"One core forgot to release the lock." Assuming the code has no bugs, the lock was
    released, but the hardware didn''t register it correctly.'
  napkin_math: At 133 MHz, the window for a race condition is about 15 nanoseconds. If you pass frames
    30 times a second, a 15ns collision window might take an hour to hit, creating a nightmare "heisenbug"
    that only crashes in production.
  options:
  - One core forgot to release the lock, causing the other core to spin forever. The bug is a missing
    unlock() call in an error handling path.
  - 'The boolean spinlock is not atomic on RP2040. Both cores can simultaneously read ''false'', both
    set ''true'', and both proceed — eventually causing state corruption. The 15ns race window at 133
    MHz means the collision takes ~1 hour to manifest at 30 fps. Fix: use RP2040''s hardware spinlocks
    (SIO block) which provide atomic test-and-set in a single cycle, guaranteed by silicon.'
  - The Cortex-M0+ cores lack a data cache, so the boolean variable is always read from SRAM with 2-cycle
    latency. This slow read creates a timing window where both cores see stale 'false' values.
  - The compiler optimized away the while() loop because it detected that the boolean variable is never
    modified within the loop body (missing 'volatile' keyword), causing both cores to skip the lock entirely.
  correct_index: 1
status: deleted
provenance: imported
requires_explanation: true
expected_time_minutes: 20
deletion_reason: 'Physics violation: 3d-parallelism has no substrate on tinyml'
validated: false
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Classification sanity - `reasoning_competency` and `reasoning_mode` are incorrectly set to `None` for
  a question that clearly tests Concurrency, Synchronization, Low-Level Programming, and Hardware Architecture.
- Napkin Math - The collision probability calculation for a "15ns collision window might take an hour
  to hit" is an estimation without specific details, making it difficult to precisely verify.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
