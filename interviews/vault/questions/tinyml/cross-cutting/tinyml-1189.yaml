schema_version: '1.0'
id: tinyml-1189
track: tinyml
level: L6+
zone: mastery
topic: federated-learning
competency_area: cross-cutting
bloom_level: create
phase: inference
title: Decentralized Anomaly Detection via Federated Averaging on nRF5340
scenario: A predictive maintenance system uses nRF5340 sensors. Each device locally updates an anomaly detection model and participates in Federated Averaging over BLE 5.3 without exceeding the 256 KB SRAM limit or depleting the battery (~5mA active current). Data is highly non-IID, and bandwidth is severely constrained.
question: Architect a cross-device federated learning system that handles non-IID data while strictly adhering to nRF5340 memory and power budgets?
details:
  realistic_solution: |
    Architect the device as an inference-first node with a frozen INT8 base anomaly model and a small trainable LoRA adapter kept in SRAM. Each client trains only the adapter on local windows, clips the update, stores it in a small flash/SRAM staging buffer, and transmits compressed deltas over BLE during scheduled connection windows. The server aggregates adapter updates with FedProx-style client objectives or proximal weighting to reduce non-IID drift, then sends back a new adapter or periodic base-model refresh. Failed BLE syncs keep the last known-good adapter, and local inference continues from the frozen base plus resident adapter without exceeding SRAM.
  common_mistake: |
    **The Pitfall:** Using standard FedAvg with full model gradient transmissions.
    **The Rationale:** Ignoring the steep energy cost of BLE transmissions and SRAM limits, assuming standard FL algorithms scale down perfectly to edge nodes.
    **The Consequence:** Full model updates blow past the 256 KB SRAM limit and the massive data transfers rapidly drain the sensor battery, rendering the system undeployable.
  napkin_math: |
    **Assumptions & Constraints:**
    - SRAM: 256 KB. Model: 100K params (INT8). Active current: 5mA.
    - BLE 5.3 throughput: ~1 Mbps (125 KB/s).

    **Calculations:**
    - LoRA update (rank-4): ~2K parameters (2 KB).
    - BLE Tx time for 2 KB: 2 KB / 125 KB/s = 16 ms.
    - Energy per Tx: 5 mA * 16 ms = 0.08 mAs.
    - Energy per local epoch (500 ms): 5 mA * 500 ms = 2.5 mAs.

    **Conclusion & Interpretation:**
    - **Result: 0.08 mAs per sync using LoRA (Energy-Bound)**. The architecture sends only small adapter deltas during BLE windows, while FedProx augments FedAvg to limit non-IID client drift.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
