schema_version: '1.0'
id: tinyml-0591
track: tinyml
level: L4
zone: diagnosis
topic: mcu-compute-constraints
competency_area: precision
bloom_level: analyze
phase: inference
title: The Factory Floor Failure
scenario: 'You developed a keyword spotting model on a Cortex-M4 with 256KB SRAM. The model is quantized to full INT8 using a clean calibration dataset of 1,000 samples. In the lab, it hits 99% accuracy, but it fails consistently on the factory floor.'
question: 'What quantization failure explains the factory-floor accuracy collapse, and what mixed-precision fix fits the SRAM budget?'
details:
  realistic_solution: 'The root cause is a catastrophic mismatch between the calibration data dynamic range and the factory noisy environment. The static quantization process maps FP32 activations to [-128, 127] based on clean audio. Loud, unpredictable background noise causes the input audio signal amplitude to far exceed the calibration maximum, clipping the signal.'
  common_mistake: |
    **The Pitfall:** Blaming the model architecture or training data without identifying activation overflow.
    **The Rationale:** Engineers assume the model lacks robustness or needs more data, missing the mechanical failure in the quantization scale.
    **The Consequence:** They waste time retraining without addressing the distributional shift causing the INT8 clipping.
  napkin_math: |
    **Assumptions & Constraints:**
    - INT8 quantization scale: S = max_abs / 127.
    - Clean calib max_abs = 5.0.
    - Factory noise true activation = 15.0.
    - INT16 memory footprint: 2 bytes per element.

    **Calculations:**
    - Clean S = 5.0 / 127 = 0.039.
    - Factory noise clipped activation = 127 * 0.039 = 4.96.
    - INT16 memory: 6,400 elements * 2B = 12.8 KB (vs 6.4 KB INT8).
    - SRAM overhead is +6.4 KB (2.5% of 256 KB).

    **Conclusion & Interpretation:**
    - **Result: +6.4 KB overhead ([Memory-Bound])**. INT16 handles the full dynamic range without clipping, safely fitting in SRAM.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
