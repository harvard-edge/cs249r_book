schema_version: '1.0'
id: tinyml-0773
track: tinyml
level: L6+
zone: design
topic: collective-communication
competency_area: networking
bloom_level: create
phase: inference
title: The LoRa Telemetry Capacity
scenario: You are designing a LoRaWAN-based wildlife monitoring network with 500 sensor nodes. Each node
  transmits a 20-byte inference result every 10 minutes. The LoRaWAN gateway supports SF7 at 50 kbps effective
  (after encoding overhead). Regulatory duty cycle is 1% (transmit max 36 seconds per hour). Calculate
  whether one gateway can handle the load, and design a time-slotted schedule.
details:
  realistic_solution: 'Per-node uplink: 20 bytes = 160 bits. At SF7 (50 kbps effective), airtime ≈ 160
    / 50,000 = 3.2ms plus protocol overhead (~50ms preamble + header at SF7) ≈ 53ms per packet. 500 nodes
    × 6 packets/hour = 3,000 packets/hour. Total airtime: 3,000 × 53ms = 159 seconds/hour. Duty cycle:
    159 / 3600 = 4.42% — exceeds the 1% per-channel limit. Solution: spread across 5 frequency channels
    (standard in EU868) → 4.4%/5 = 0.88% per channel. This fits, but with minimal margin. Alternatively,
    increase the reporting interval to 15 minutes (2,000 packets/hour → 2.9% → 0.58% per channel).'
  common_mistake: Ignoring the regulatory duty cycle limit. Even if raw bandwidth is sufficient, the 1%
    transmit duty cycle means the gateway's receive window is limited, and each node's transmit time counts
    against its own duty cycle budget.
  napkin_math: 'Payload: 20 B = 160 bits. SF7 airtime (with overhead): ~53ms/packet. Packets/hour: 500
    × 6 = 3,000. Total airtime: 3,000 × 53ms = 159s/hr. Single channel duty: 159 / 3600 = 4.42% > 1%.
    Need 5 channels: 4.4/5 = 0.88% ✓. Safety margin: (1.0-0.88)/1.0 = 12%.'
  options:
  - One gateway easily handles 500 nodes. At 50 kbps, 3,000 packets/hour of 20 bytes each consumes only
    0.1% of the channel capacity.
  - 'Per-packet airtime at SF7: ~53ms (including preamble/header overhead). 500 nodes x 6 packets/hr =
    3,000 packets/hr. Total airtime: 159 seconds/hr. Single-channel duty cycle: 4.42% — exceeds the 1%
    regulatory limit. Fix: spread across 5 frequency channels (standard EU868) = 0.88%/channel. Or increase
    reporting interval to 15 min (0.58%/channel).'
  - The 1% duty cycle limit applies only to the end nodes, not the gateway. The gateway can receive continuously
    with no duty cycle restriction, so one gateway handles 500 nodes easily.
  - LoRaWAN SF7 at 50 kbps can handle 500 nodes sending every 10 minutes because the packets are only
    20 bytes and the collision probability is negligible with ALOHA-style random access.
  correct_index: 1
status: deleted
provenance: imported
requires_explanation: true
expected_time_minutes: 20
deletion_reason: 'Physics violation: collective-communication has no substrate on tinyml'
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The question's RC, KA, and Mode fields are set to 'None', but the question tests specific competencies
  and modes, like wireless communication and system design.
validation_status_pro: WARN
validation_issues_pro:
- The effective data rate of 50 kbps for LoRaWAN SF7 is factually inaccurate for standard configurations,
  which are typically closer to 5 kbps.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
