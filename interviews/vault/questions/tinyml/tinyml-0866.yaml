schema_version: '1.0'
id: tinyml-0866
track: tinyml
level: L3
zone: evaluation
topic: model-format-conversion
competency_area: deployment
bloom_level: evaluate
phase: inference
title: 'Model Format Conversion: Compare TFLite Micro vs. TensorFlow Lite for MCU Deployment'
scenario: A keyword spotting model (DS-CNN-S, 100K params, INT8, 0.09 MOPs) needs to run on both a Cortex-M4 (168MHz, 256KB SRAM) and a Jetson Orin.
question: How would you compare TFLite Micro (MCU runtime, <20KB RAM overhead) vs. standard TFLite (GPU delegate) for each platform?
details:
  realistic_solution: Cortex-M4 relies on static memory planning via TFLM since bare metal lacks a filesystem and heap for dynamic allocation. Jetson Orin runs standard TFLite or TensorRT for GPU acceleration.
  common_mistake: |
    **The Pitfall:** Suggesting standard TFLite for the Cortex-M4.
    **The Rationale:** Candidates fail to realize that standard TFLite requires dynamic memory allocation and a file system, which are unavailable on bare-metal MCUs.
    **The Consequence:** The standard TFLite binary simply won't compile or run on a Cortex-M4, requiring a complete rewrite using TFLite Micro.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: DS-CNN-S (100K params, 0.09 MOPs).
       - Cortex-M4: 168 MHz, 256KB SRAM (Bare metal).
       - Jetson Orin: 275 TOPS, full Linux OS.
    2. **Calculations:**
       - Cortex-M4 (TFLM): Weights in Flash (100KB), Arena (50KB). Latency = 0.09 MOPs / 168 MHz = 0.54 ms.
       - Jetson Orin: Raw compute = 0.09 MOPs / 275 TOPS = 0.33 µs.
       - GPU Delegate/TensorRT overhead on Jetson ~1-2 ms.
    3. **Conclusion & Interpretation:**
       **Result: M4 uses TFLM (0.54 ms); Orin uses TensorRT (~1-2 ms).** The Orin is **Overhead-Bound** (kernel launch latency dominates), while the M4 requires a static memory planner (TFLM).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 3
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
