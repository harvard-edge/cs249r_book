schema_version: '1.0'
id: tinyml-0561
track: tinyml
level: L4
zone: diagnosis
topic: data-pipeline-engineering
competency_area: data
bloom_level: analyze
phase: training
title: The DMA Pipeline for Sensor Data
scenario: Your Cortex-M4 reads accelerometer data over SPI at 1.6 kHz. Without DMA, your firmware polls the SPI bus in a tight loop, consuming 100% CPU. With DMA, the CPU is free to run inference. But your colleague's DMA setup drops 5% of samples.
question: Why does single-buffer SPI DMA at 1.6 kHz drop samples during 30-50 ms Cortex-M4 inference, and how do you fix it?
details:
  realistic_solution: The problem is a single-buffer race condition. The DMA writes sensor data into a buffer while the CPU reads from the same buffer for feature extraction. When the DMA write pointer overtakes the CPU read pointer, unprocessed samples are overwritten — hence the 5% drop rate (it happens during inference when the CPU is busy for 30-50ms).
  common_mistake: |
    **The Pitfall:** Believing DMA automatically prevents data loss.
    **The Rationale:** Engineers assume pointing DMA at a single buffer completely offloads I/O safely.
    **The Consequence:** The DMA overwrites data the CPU hasn't processed yet, creating a race condition and dropping samples.
  napkin_math: |
    **Assumptions & Constraints:**
    - 1.6 kHz SPI (6 bytes/sample).
    - Cortex-M4 inference is 50 ms.
    - Single buffer is 256 samples.

    **Calculations:**
    - Buffer size: 256 * 6 = 1,536 bytes (1.5 KB).
    - DMA fill time: 256 / 1600 Hz = 160 ms.
    - Race condition overlap: 50 ms / 160 ms = 31.25%.

    **Conclusion & Interpretation:**
    - **Result: 31% overlap (I/O-Race-Condition)**. CPU and DMA compete 31% of the time, and double-buffering eliminates the drop rate.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
