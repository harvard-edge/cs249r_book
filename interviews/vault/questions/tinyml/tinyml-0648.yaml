schema_version: '1.0'
id: tinyml-0648
track: tinyml
level: L6+
zone: mastery
topic: ota-firmware-updates
competency_area: deployment
bloom_level: create
phase: inference
title: The Bootloader Vector Table Relocation
scenario: You implement a custom bootloader for your TinyML device to handle OTA updates. The bootloader
  lives at Flash address `0x08000000`. It verifies the new ML application at `0x08020000` and jumps to
  it. The ML application's first line of code executes perfectly. But the moment a hardware timer interrupt
  fires, the device crashes and reboots. Why?
question: "Why?"
details:
  realistic_solution: 'The interrupt vector table (IVT) was not relocated. At boot, the CPU fetches interrupt
    handlers from the IVT at 0x08000000 (bootloader''s IVT). After jumping to the application at 0x08020000,
    the VTOR register still points to the bootloader''s IVT. When a timer interrupt fires, the CPU fetches
    the handler address from the bootloader''s vector table, which points to bootloader code — executing
    random instructions from the wrong binary, causing an immediate hard fault. Fix: the application''s
    first instruction must set VTOR = 0x08020000 (SCB->VTOR = APP_BASE_ADDRESS) before enabling any interrupts.'
  common_mistake: '"The ML application didn''t initialize the timer correctly." The timer fired, so it
    was initialized. The problem is what the CPU does *after* the timer fires.'
  napkin_math: A jump instruction takes 1-3 cycles. Without VTOR relocation, the CPU jumps to an address
    compiled for a completely different binary, executes random bytes as instructions, and triggers a
    fault in less than a microsecond.
  options:
  - The bootloader's CRC verification of the application image has a bug that passes invalid binaries,
    causing random code execution when the first interrupt fires.
  - 'The interrupt vector table (IVT) was not relocated. After jumping to the application at 0x08020000,
    the VTOR register still points to the bootloader''s IVT at 0x08000000. When a timer interrupt fires,
    the CPU fetches the handler address from the wrong vector table, executing bootloader code as if it
    were an ISR handler — causing an immediate hard fault. Fix: set SCB->VTOR = 0x08020000 as the application''s
    first instruction before enabling interrupts.'
  - The timer peripheral's interrupt priority was not reconfigured after the jump, causing a priority
    inversion that deadlocks the application's interrupt handler.
  - The bootloader disables the timer before jumping, but the application re-enables it before the clock
    tree is fully initialized, causing the timer to fire at the wrong frequency and crash the interrupt
    handler.
  correct_index: 1
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
chains:
- id: tinyml-chain-002
  position: 3
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Classification sanity - `reasoning_competency` and `reasoning_mode` are incorrectly set to `None` for
  a question that clearly tests Systems Design, Low-Level Programming, and Troubleshooting skills related
  to bootloaders and interrupts.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
