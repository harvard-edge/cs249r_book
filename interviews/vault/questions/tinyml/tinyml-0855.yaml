schema_version: '1.0'
id: tinyml-0855
track: tinyml
level: L3
zone: realization
topic: dataset-curation
competency_area: data
bloom_level: apply
phase: training
title: 'Dataset Curation: Realize PSRAM-Constrained Training Dataset for ESP32-S3'
scenario: Realize the constraints for an on-device training dataset on ESP32-S3 (240MHz, 512KB SRAM, 8MB PSRAM). You want to train a gesture recognition model with 5 gestures using 200 samples per class collected from the integrated IMU.
question: How would you specify what fits in PSRAM, the training batch strategy, and training loop design?
details:
  realistic_solution: Store dataset in PSRAM, load mini-batches into SRAM for computation. Use streaming training to avoid holding full dataset in fast memory.
  common_mistake: |
    **The Pitfall:** Attempting to load all training data simultaneously into SRAM.
    **The Rationale:** In typical deep learning pipelines on servers, fitting the entire dataset into RAM is common practice to speed up epoch iterations.
    **The Consequence:** The device immediately crashes via an Out-of-Memory (OOM) error because SRAM is strictly limited to 512 KB, halting all development.
  napkin_math: |
    - **Dataset Size:** 5 classes × 200 samples × (100Hz × 1-sec × 6 axes × 1B int8) = 1,000 samples × 600 Bytes = **600 KB**.
    - 600 KB exceeds SRAM (512 KB) but comfortably fits in **PSRAM (8 MB)**.
    - **SRAM During Training:**
      - Model weights: 1K params × 4B = 4 KB.
      - Mini-batch: 16 samples × 600B = 9.6 KB.
      - Activations: 16 × 128 hidden × 4B = 8 KB.
      - Gradients + Optimizer: 4 KB + 8 KB = 12 KB.
      - Total SRAM required: **~33.6 KB** (fits easily within 512 KB).
    - **PSRAM Access Overhead:**
      - Latency: ~100 cycles vs SRAM ~1 cycle at 240MHz.
      - Loading 600B batch: 600 / (240MHz/100) = **250 microseconds**.
    - **Compute Time:**
      - Forward pass: 16 × 600 × 128 × 2 FLOPs = 2.46M FLOPs.
      - At 240MHz scalar: **10.25 ms**.
      - PSRAM load overhead ratio: 250 µs / 10.25 ms = **2.4%** (negligible).
    - Total On-Device Time (200 epochs): 12,500 steps × 10.25 ms = 128 seconds (**~2 minutes**).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
