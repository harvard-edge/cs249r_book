schema_version: '1.0'
id: tinyml-1334
track: tinyml
level: L3
zone: analyze
topic: tensor-arena-planning
competency_area: memory
bloom_level: analyze
phase: inference
title: Tensor Arena Planning for TinyML on Nordic nRF5340
scenario: You are developing a TinyML application for a Nordic nRF5340 microcontroller
  (128 MHz, 256 KB SRAM, 1 MB flash, BLE). Your task is to deploy a small convolutional
  neural network (CNN) model that processes sensor data. The model has several intermediate
  tensors and requires careful memory management. Describe your strategy for static
  memory planning, focusing on the use of flat tensor arenas, operator scheduling
  to manage peak SRAM usage, and how you would decide on flash versus SRAM placement
  for model parameters and intermediate data. Consider the nRF5340's specific memory
  constraints.
question: How should you plan a flat tensor arena and flash versus SRAM placement
  for a CNN on the Nordic nRF5340?
details:
  realistic_solution: A realistic approach involves creating a memory plan by analyzing
    the model's computational graph to identify tensor lifetimes. Implement a flat
    tensor arena where memory is allocated statically and reused across non-overlapping
    tensors. Optimize operator scheduling to minimize the number of live tensors concurrently,
    thereby reducing peak SRAM demand. Store read-only model weights and constant
    parameters in the 1 MB flash memory, loading them into SRAM only when actively
    needed or streaming them if the model is too large for SRAM. Intermediate mutable
    tensors should primarily reside in SRAM.
  common_mistake: A common mistake is to allocate memory for all tensors upfront or
    without considering their overlapping lifetimes, leading to peak SRAM requirements
    that exceed the device's 256 KB capacity. Another error is storing all model weights
    in SRAM, which can quickly exhaust available memory, especially if the model is
    large.
  napkin_math: If an input image tensor is 64x64x1 (uint8) and requires 4 KB, and
    an intermediate activation tensor is 32x32x16 (float32) and requires 64 KB, what
    is the minimum peak SRAM required for these two tensors if they can reuse the
    same memory region? Assuming an additional 10 KB for other model overhead, what's
    the remaining SRAM budget on the nRF5340?
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
