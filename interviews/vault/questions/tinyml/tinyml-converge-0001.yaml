schema_version: '1.0'
id: tinyml-converge-0001
track: tinyml
level: L5
zone: specification
topic: operator-scheduling
competency_area: optimization
bloom_level: evaluate
phase: both
title: TinyML Cooperative Scheduling Spec
scenario: A Cortex-M MCU must run sensor sampling, feature extraction, inference, and BLE transmission without an RTOS. Specify a cooperative schedule that prevents inference from blocking sampling.
question: What cooperative scheduling rules prevent TinyML inference from blocking sensor sampling and BLE transmission?
details:
  realistic_solution: Use sensor sampling interrupts for the deadline path, double buffers for data handoff, and short run-to-yield inference chunks in the main loop. BLE transmission should run only when buffers have slack or when lower-priority telemetry can be delayed. Long inference work must yield before sensor deadlines are missed.
  common_mistake: Writing one blocking inference loop and assuming sensor peripherals and BLE transmission can wait indefinitely.
  napkin_math: If sampling must run every 10 ms, inference chunks must be shorter than 10 ms minus interrupt and buffer-management overhead.
status: draft
provenance: llm-then-human-edited
requires_explanation: true
expected_time_minutes: 12
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- convergence-gap-pack
