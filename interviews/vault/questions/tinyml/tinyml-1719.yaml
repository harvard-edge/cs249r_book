schema_version: '1.0'
id: tinyml-1719
track: tinyml
level: L4
zone: implement
topic: communication-computation-overlap
competency_area: optimization
bloom_level: analyze
phase: both
title: DMA Overlap to Meet Hard Real-Time Deadlines
scenario: A Cortex-M4 reads an I2C sensor at 100Hz (10ms deadline). Inference takes 8ms. The blocking I2C read takes 3ms.
question: "Can a single-threaded execution model sustain the 100Hz rate, and what is the CPU idle percentage if DMA is used?"
details:
  realistic_solution: A blocking single thread takes 8ms (compute) + 3ms (read) = 11ms. 11ms exceeds the 10ms deadline, so it drops frames. By using DMA, the 3ms I2C transfer for frame N+1 overlaps with the 8ms compute for frame N. The longest path is 8ms. CPU idle time is (10ms - 8ms) / 10ms = 20%.
  common_mistake: |
    **The Pitfall:** Subtracting 3ms from 8ms to find idle time, or adding them linearly.
    **The Rationale:** Candidates fail to visualize the parallel timeline and miss the overarching 10ms frame period constraint.
    **The Consequence:** The timing feasibility and idle state calculations are completely incorrect, leading to missed deadlines.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Sensor rate: 100 Hz (10 ms frame period).
       - Inference compute time: 8 ms.
       - Blocking I2C read time: 3 ms.
    2. **Calculations:**
       - Single thread: 8 ms + 3 ms = 11 ms (Exceeds 10 ms).
       - DMA overlapped: Max(8 ms compute, 3 ms I/O) = 8 ms.
       - CPU idle time = 10 ms frame period - 8 ms active compute = 2 ms.
       - CPU idle percentage = 2 ms / 10 ms = 20%.
    3. **Conclusion & Interpretation:**
       - **Result: 20% CPU Idle Time.**
       - **Compute-Bound (with DMA optimization)**
status: published
provenance: llm-draft
expected_time_minutes: 9
requires_explanation: true
validated: true
math_verified: true
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-implement-L4
created_at: '2026-04-25'
math_status: CORRECT
math_model: gemini-3.1-pro-preview
validation_status: passed
validation_model: pydantic-1.0
math_date: '2026-04-25'
validation_date: '2026-04-25'
