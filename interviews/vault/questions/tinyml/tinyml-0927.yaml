schema_version: '1.0'
id: tinyml-0927
track: tinyml
level: L5
zone: design
topic: transformer-systems-cost
competency_area: compute
bloom_level: create
phase: inference
title: Design Layer-Wise Quantization Schedule for Transformer on MCU
scenario: 'A 6-layer transformer with 512 hidden dimensions targets Cortex-M7+Ethos-U55.
  Full INT8 quantization reduces model from 12MB to 3MB (fits flash) but drops accuracy
  by 8%. Sensitivity analysis shows: embedding layer and final classifier are high-sensitivity
  (INT8 drops 5%), middle layers 2-5 are low-sensitivity (INT8 drops <1% each). SRAM
  budget is 256KB for activations. Design a mixed-precision quantization schedule
  that preserves accuracy while fitting memory constraints.'
question: How would you design a mixed-precision quantization schedule that preserves
  accuracy while fitting memory constraints?
details:
  realistic_solution: 'Mixed precision model: 1M params × FP16 = 2MB weights. Activation
    overhead is small (<<1MB for batch=1). Total ≈ 2MB (not 2.5MB).'
  common_mistake: Applying uniform INT4 to all layers to save more memory; embedding
    quantization to INT4 causes vocabulary collapse for rare tokens.
  napkin_math: 'Model memory in mixed precision: weights FP16 + activations FP32.
    1M params × 2B (FP16) = 2MB weights. Activations at FP32: input 32×32×16×4B =
    65,536B = 64KB. Total: 2MB + 64KB = ~2MB (not 2.5MB — 2.5MB would require 512KB
    activations which needs batch=8 or larger intermediate layers).'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Mixed precision memory calculation is incorrect (2MB vs 2.5MB)
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
