schema_version: '1.0'
id: tinyml-1304
track: tinyml
level: L4
zone: specification
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: inference
title: Designing Quantization for Keyword Spotting on Cortex-M4
scenario: You are tasked with deploying a 500 KB (FP32) keyword spotting CNN onto an ARM Cortex-M4 (STM32F4) microcontroller. The target platform operates at 168 MHz, features 256 KB SRAM, 1 MB flash, and utilizes CMSIS-NN SIMD instructions (2x INT8 MACs/cycle) but lacks a hardware FPU.
question: What INT8 quantization specification lets a 500 KB FP32 Cortex-M4 keyword spotter avoid FPU emulation and fit memory limits?
details:
  realistic_solution: The design must specify INT8 quantization for weights (symmetric) and activations (asymmetric) to leverage the 2x INT8 MACs/cycle CMSIS-NN SIMD instructions. To avoid the FPU-less bottleneck, the specification must require folding batch normalization and representing the FP32 scale factors as a combination of an INT32 multiplier and a bit shift (fixed-point arithmetic). Per-channel quantization should be specified for convolutional weights to maintain accuracy, fitting the quantized ~125 KB model comfortably within the 1 MB flash.
  common_mistake: |
    **The Pitfall:** Proposing generic INT8 quantization without addressing floating-point scaling factors.
    **The Rationale:** Candidates overlook that even if weights and activations are INT8, standard de-quantization steps require floating-point operations. On an FPU-less MCU, these will trigger slow software emulation.
    **The Consequence:** The memory footprint is successfully reduced, but the model latency is drastically worse than expected due to software FP32 emulation bottlenecks.
  napkin_math: |
    1. Model compression: 500 KB FP32 / 4 = 125 KB INT8 (Fits easily in 1 MB flash).
    2. Software FP32 penalty: 1 million MACs via FP32 emulation takes ~100 cycles/MAC = 100M cycles (595 ms at 168 MHz).
    3. Hardware SIMD benefit: CMSIS-NN SIMD handles 2x INT8 MACs/cycle. 1M MACs takes 500,000 cycles (~3 ms).
    4. Result: Proper fixed-point quantization avoids emulation, yielding an **~200× speedup**.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
