schema_version: '1.0'
id: tinyml-1224
track: tinyml
level: L4
zone: optimization
topic: ota-firmware-updates
competency_area: deployment
bloom_level: analyze
phase: inference
title: Delta OTA Optimization for Ethos-U55 NPU Models
scenario: You are designing an OTA update mechanism for a smart sensor running on
  an ARM Cortex-M7 + Ethos-U55 (Corstone-300) platform. The system has 512 KB of shared
  SRAM and 2 MB of internal Flash, with the current firmware consuming 1.7 MB. A recent
  model update for the Ethos-U55 requires replacing the 1.2 MB compiled Vela network,
  but a standard A/B partition scheme fails due to insufficient Flash. How do you
  analyze this storage bottleneck and quantify an optimization strategy to perform
  reliable, in-place OTA updates without external memory?
question: How do you analyze this storage bottleneck and quantify an optimization
  strategy to perform reliable, in-place OTA updates without external memory?
details:
  realistic_solution: Implement a differential (delta) OTA update strategy targeting
    only the changed weights and Vela command streams. By downloading only the delta
    payload, storing it in the small remaining Flash space, and streaming the patching
    process block-by-block using a minimal SRAM buffer, the update succeeds without
    violating memory constraints or requiring a full A/B dual-bank setup.
  common_mistake: Candidates often suggest relying solely on external SPI flash or
    compressing the entire image without considering the severe RAM constraints. They
    fail to recognize that the 512 KB SRAM is shared between the 480 MHz M7 and the
    NPU, meaning in-place decompression of a large model during the update can easily
    cause an SRAM overflow and crash the RTOS.
  napkin_math: 'Total Flash = 2 MB. Active firmware = 1.7 MB. Free Flash = 300 KB.
    Full model size = 1.2 MB (bottleneck: 1.2 MB > 300 KB). Assuming a 15% change
    in network weights, the delta patch size = 1.2 MB * 0.15 = 180 KB. The 180 KB
    patch fits in the 300 KB free Flash. During patching, a 16 KB read/write buffer
    is allocated from the 512 KB SRAM, leaving 496 KB for the M7 RTOS and Ethos-U55
    shared memory during the update process.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
