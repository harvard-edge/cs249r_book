schema_version: '1.0'
id: tinyml-0937
track: tinyml
level: L4
zone: implement
topic: transformer-systems-cost
competency_area: compute
bloom_level: apply
phase: inference
title: Implement Tiled Matrix Multiply for Attention on Cortex-M7
scenario: You are implementing the QK^T attention matrix multiply for a 4-head transformer with 32KB L1 data cache. Each head has 64-dim keys, sequence length 128. The full QK^T matrix per head is 128x128x2B = 32KB (exactly fitting L1). You must implement tiled matrix multiply ensuring tiles fit in cache with Q, K, and output simultaneously.
question: How would you determine optimal tile size and compute expected speedup over naive implementation?
details:
  realistic_solution: Optimal tiling requires fitting Q_tile, K_tile, and output_tile in the 32KB L1 simultaneously to avoid SRAM thrashing. By allocating equal 64x64 chunks for each tensor, the working set consumes 24KB, safely fitting in L1 while leaving headroom for software prefetch buffers and register spilling. Executing memory-bound matmul strictly within L1 delivers roughly a 3x speedup over naive implementations that constantly stall waiting for full SRAM fetches.
  common_mistake: |
    **The Pitfall:** Sizing tiles so Q and K perfectly fill the L1 cache, forgetting the output tensor.
    **The Rationale:** Engineers focus purely on input tensor footprints and neglect the space required for output accumulation.
    **The Consequence:** The output tensor spills to SRAM, causing severe cache thrashing and destroying the intended speedup.
  napkin_math: |
    **Assumptions & Constraints:**
    - L1 Cache: 32 KB = 32,768 bytes.
    - Tensors: Q, K, and Output must coexist.

    **Calculations:**
    - Tile bytes: 3 * T * 64 * 2 bytes = 384T bytes.
    - Budget limit: 384T <= 32,768 -> T = 85. Round down to power-of-2: T=64.
    - Cache utilization: 3 * 64 * 64 * 2 = 24,576 bytes (75% of L1).
    - Speedup: L1 access (4 cycles) vs SRAM (12 cycles) yields ~3x speedup.

    **Conclusion & Interpretation:**
    - **Result: T=64 (Memory-Bandwidth Bound)**. Tiling to 64x64 blocks maximizes L1 utilization and prevents thrashing.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
