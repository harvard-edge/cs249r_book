schema_version: '1.0'
id: tinyml-0925
track: tinyml
level: L5
zone: design
topic: transformer-systems-cost
competency_area: compute
bloom_level: create
phase: inference
title: 'Design Speculative Decoding for Cortex-M4 Transformer'
scenario: 'You are designing a speculative decoding system for a draft model on a Cortex-M4 paired with a target model on an Ethos-U55. The draft model generates 4 candidate tokens per step at 8ms each. The target model verifies all 4 simultaneously at 12ms. Token acceptance rate is α=0.75.'
question: 'How would you design the speculation window size and fallback strategy to maximize tokens/second?'
details:
  realistic_solution: 'Expected tokens per cycle: E[tokens] = (1-0.75^5)/0.25 ≈ 3.05. Time per cycle: 4×8ms + 12ms = 44ms. Throughput: 3.05/44ms ≈ 69.3 tok/s vs baseline 1/12ms = 83.3 tok/s... speculative decoding hurts here. Reduce draft steps to k=2: E=2.31 tokens, time=2×8+12=28ms, throughput=82.5 tok/s. Still worse. Only beneficial if draft model is much faster.'
  common_mistake: |
    **The Pitfall:** Assuming that speculative decoding and a larger speculation window size always lead to higher token throughput.
    **The Rationale:** Candidates apply LLM datacenter scaling intuition without mathematically verifying if the weak/slow CPU draft model actually outpaces the NPU target model.
    **The Consequence:** The speculative architecture actively harms system performance, reducing overall tokens/second compared to just running the target model sequentially.
  napkin_math: |
    **Assumptions & Constraints:**
    - Speculation window k=4.
    - Draft time = 8 ms/token.
    - Target verify time = 12 ms.
    - Acceptance rate α = 0.75.
    - Target-only baseline time = 12 ms/token (83.3 tok/s).

    **Calculations:**
    - Expected tokens per cycle (k=4): E[tokens] = (1 - 0.75^5) / (1 - 0.75) ≈ 3.05 tokens.
    - Cycle Time (k=4) = (4 * 8ms) + 12ms = 44 ms.
    - Throughput (k=4) = 3.05 tokens / 44 ms ≈ 69.3 tok/s.
    - For k=2: E=2.31 tokens, time = (2*8)+12 = 28ms, throughput = 82.5 tok/s.

    **Conclusion & Interpretation:**
    - **Result: 69.3 tok/s vs 83.3 tok/s (Compute-Bound)**. Both k=4 (69.3 tok/s) and k=2 (82.5 tok/s) are slower than the non-speculative baseline (83.3 tok/s). The draft model must be optimized to generate tokens in <4 ms for speculative decoding to yield any latency benefit.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
