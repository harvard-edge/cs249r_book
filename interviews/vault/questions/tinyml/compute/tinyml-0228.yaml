schema_version: '1.0'
id: tinyml-0228
track: tinyml
level: L1
zone: recall
topic: mcu-compute-constraints
competency_area: compute
bloom_level: remember
phase: inference
title: The TinyML Ridge Point
scenario: You are analyzing the performance of a keyword spotting model on a Cortex-M4 microcontroller. You've generated a Roofline plot for the device.
question: What does the 0.28 Ops/Byte ridge point mean on a Cortex-M4 roofline plot?
details:
  realistic_solution: The Ridge Point identifies the minimum Arithmetic Intensity (AI), measured in Operations per Byte (Ops/Byte), that a workload must have to become limited by the processor's peak compute speed rather than by its memory bandwidth. Below this AI, the system is memory-bound; above it, it's compute-bound. For a typical Cortex-M4, the Ridge Point is exceptionally low (e.g., ~0.28 Ops/Byte). Because typical neural network layers have an AI greater than 1, they easily exceed this ridge point, meaning these systems are fundamentally compute-bound for almost all ML workloads.
  common_mistake: |
    **The Pitfall:** Assuming microcontrollers are always memory-bound due to limited SRAM bandwidth.
    **The Rationale:** Engineers fail to calculate the Ridge Point, missing that peak FLOPS are so low that typical ML workloads easily exceed the required Arithmetic Intensity.
    **The Consequence:** Misidentifying the performance bottleneck and optimizing memory instead of compute.
  napkin_math: |
    **Assumptions & Constraints:**
    - Peak Compute (Cortex-M4): ~336 MFLOPS.
    - Memory Bandwidth (SRAM): ~1.2 GB/s.

    **Calculations:**
    - 336 MFLOPS / 1.2 GB/s = 0.28 Ops/Byte.

    **Conclusion & Interpretation:**
    - **Result: 0.28 Ops/Byte (Compute-Bound).** The minimum Arithmetic Intensity needed to be compute-bound.
  options:
  - The peak theoretical performance (MFLOPS) of the microcontroller.
  - The maximum memory bandwidth (GB/s) of the on-chip SRAM.
  - The minimum Arithmetic Intensity (Ops/Byte) needed to be compute-bound.
  - The energy cost (in pJ) of a single memory access.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 1
chains:
- id: tinyml-chain-090
  position: 0
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
