schema_version: '1.0'
id: tinyml-0946
track: tinyml
level: L4
zone: design
topic: transformer-systems-cost
competency_area: compute
bloom_level: apply
phase: inference
title: Realize Quantization-Aware Training Pipeline for MCU Transformer
scenario: You must deploy an INT8 transformer for predictive maintenance on Cortex-M7 within 3 weeks. The FP32 baseline achieves 95% F1 at 48ms inference. PTQ drops accuracy to 88% (unacceptable). You must implement QAT using TensorFlow Lite Micro's QAT API with fake quantization. The training dataset has 50K samples, training time budget is 24 GPU-hours on a single A10G GPU.
question: How should an INT8 QAT pipeline be staged for the Cortex-M7 transformer to recover F1 within the 24 GPU-hour budget?
details:
  realistic_solution: 'Pipeline: (1) Load FP32 checkpoint. (2) Insert fake-quant nodes: weights per-channel (128 bins), activations per-tensor (256 bins), attention logits clip at ±6. (3) Phase 1 (4h): fine-tune activation scales, LR=1e-5, freeze weights. (4) Phase 2 (8h): unfreeze weights, train all with Straight-Through Estimator (STE) gradients. (5) Phase 3 (12h): full QAT convergence. Export: convert fake-quant to INT8 using TFLM converter. Expected result: 93-95% F1. (6) Validate on MCU: compare with aarch32 TFLM runtime, verify 48ms budget met.'
  common_mistake: |
    **The Pitfall:** Applying QAT to all layers simultaneously from the start.
    **The Rationale:** Engineers assume QAT works identical to standard fine-tuning, flipping on all fake quantization nodes immediately.
    **The Consequence:** Gradients become highly unstable due to the Straight-Through Estimator (STE), causing the model to diverge or fail to recover the target F1 score within the limited time budget.
  napkin_math: |
    **Assumptions & Constraints:**
    - Dataset: 50K samples, Batch size = 64.
    - A10G throughput: ~500 steps/min.
    - Time budget: 24h = 1440 mins.

    **Calculations:**
    - Steps per epoch = 50,000 / 64 = 781 steps.
    - Time per epoch = 781 / 500 = 1.56 min/epoch.
    - Max epochs available = 1440 / 1.56 = 923 epochs.
    - Phase 1 (50 epochs) = 78 mins.
    - Phase 2 (200 epochs) = 312 mins.
    - Phase 3 (500 epochs) = 780 mins.

    **Conclusion & Interpretation:**
    - **Result: 1170 mins (Compute-Bound)**. Total time safely meets the 1440 mins budget constraint.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
