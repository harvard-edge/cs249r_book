schema_version: '1.0'
id: tinyml-0229
track: tinyml
level: L2
zone: implement
topic: mcu-compute-constraints
competency_area: compute
bloom_level: understand
phase: inference
title: The MCU Memory Wall
scenario: You're evaluating a Cortex-M4 microcontroller for a keyword spotting application.
  According to the datasheet and the 'Numbers Every ML Systems Engineer Should Know'
  guide, the chip has a peak compute performance of approximately 336 MFLOPS and its
  on-chip SRAM provides about 1.2 GB/s of memory bandwidth.
question: What is the Cortex-M4 ridge point, and what does it imply about whether
  typical ML layers are compute-bound?
details:
  realistic_solution: The ridge point defines the boundary between being memory-bound
    and compute-bound. It's the minimum arithmetic intensity (AI) a model needs to
    have to saturate the compute units. With a ridge point of ~0.28 Ops/Byte, the
    Cortex-M4 has a very low threshold. Most neural network layers, even simple ones,
    have a higher AI than 0.28 Ops/Byte. Therefore, the processor will generally be
    compute-bound, meaning its theoretical peak MFLOPS is the primary bottleneck,
    and it will rarely be starved for data by the SRAM.
  common_mistake: Engineers correctly calculate the ridge point but confuse the implications.
    A low ridge point means the memory is fast relative to the compute, making it
    *easier* to be compute-bound. If AI > Ridge Point, the system is compute-bound.
    Falsely concluding the device is memory-bound leads to wasted effort optimizing
    memory access patterns when the focus should be on reducing FLOPs (e.g., via quantization
    or pruning).
  napkin_math: 1. **Recall the formula:** Ridge Point is the ratio of peak compute
    to peak memory bandwidth.
  options:
  - ~280 Ops/Byte, making it easily compute-bound.
  - ~3.57 Bytes/Op, meaning it's inefficient.
  - ~0.28 Ops/Byte, making it profoundly memory-bound.
  - ~0.28 Ops/Byte, making it typically compute-bound.
  correct_index: 3
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
chains:
- id: tinyml-chain-091
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
