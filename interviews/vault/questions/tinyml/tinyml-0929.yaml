schema_version: '1.0'
id: tinyml-0929
track: tinyml
level: L4
zone: diagnosis
topic: transformer-systems-cost
competency_area: compute
bloom_level: analyze
phase: inference
title: Diagnose Attention Head Collapse in Quantized MCU Transformer
scenario: After INT4 quantization of a 4-head attention transformer deployed on Cortex-M4, word error rate jumps from 8% to 31% on a wake-word detection task. Attention visualization shows 3 of 4 heads produce nearly uniform distributions (entropy > 0.95 of maximum) while 1 head handles all meaningful patterns. The model was quantized using post-training quantization (PTQ) with 100 calibration samples.
question: How would you diagnose why INT4 caused head collapse and propose a targeted fix?
details:
  realistic_solution: 'INT4 provides only 16 discrete levels. Attention logits before softmax typically span [-3, 3] std devs. With 100 calibration samples, scale = max(|logits|)/7 ≈ 3.0/7 = 0.43. Values < 0.43 quantize to 0 (dead zone). Heads 2-4 with lower-magnitude weights get zeroed out. Fix: (1) increase calibration to 1000+ samples, (2) use per-head quantization scales instead of per-tensor, (3) apply attention logit clipping at 2.5σ before quantization.'
  common_mistake: |
    **The Pitfall:** Blaming INT4 precision uniformly across all heads.
    **The Rationale:** Candidates fail to trace the error back to the PTQ calibration sample size, causing outlier-driven scale factors that crush small attention weights.
    **The Consequence:** The model is unnecessarily bumped to INT8, breaking strict memory/compute constraints.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Quantization: INT4 (range -8 to +7).
       - Sequence length: 64.
       - Calibration max logit magnitude: 3.0.
    2. **Calculations:**
       - Scale factor: max / 7 = 3.0 / 7 = 0.43.
       - Dead zone: Weights < 0.43 map to 0.
       - Uniform distribution entropy: log_e(64) = 4.16 nats.
       - Observed entropy: 0.95 * 4.16 = 3.95 nats.
    3. **Conclusion & Interpretation:**
       - **Result: Precision-Bound.** Values < 0.43 are crushed to 0, collapsing attention heads into near-uniform entropy.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
