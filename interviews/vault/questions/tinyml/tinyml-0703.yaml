schema_version: '1.0'
id: tinyml-0703
track: tinyml
level: L3
zone: fluency
topic: mcu-compute-constraints
competency_area: memory
bloom_level: apply
phase: inference
title: Flash Wear from Logging Frequency
scenario: 'Your TinyML sensor node runs on an STM32L4 with 1 MB internal flash and a 2 MB external NOR
  flash (SPI, rated for 100,000 P/E cycles). The firmware logs inference results (32 bytes per result:
  timestamp, class, confidence, sensor readings) to a circular buffer in external flash. The device runs
  10 inferences per second, 24/7. How long until the flash wears out?'
question: "How long will 2 MB of 100K-cycle external NOR flash last when logging 32 bytes at 10 inferences per second?"
details:
  realistic_solution: 'NOR flash has a critical constraint: writes can only flip bits from 1→0. To write
    new data, you must first erase the sector (flip all bits to 1), then write. Erase granularity for
    typical NOR flash: 4 KB sectors.'
  common_mistake: '"2 MB × 100,000 cycles = 200 GB total writes. At 32 bytes × 10/s = 320 bytes/s = 27.6
    MB/day. 200 GB / 27.6 MB = 7,246 days = 19.8 years. No problem." This ignores the NOR flash erase
    granularity — you can''t erase individual bytes; you must erase entire sectors.'
  napkin_math: 'Data writes only: 13.2 cycles/day → 20.8 years. With write-ahead log: 26.4 cycles/day
    → 10.4 years. With naive metadata pointer: 864,000 cycles/day → 2.8 hours (!). The metadata sector
    wears out 65,000× faster than the data sectors. Lesson: on flash-based systems, the hottest byte determines
    the system lifetime, not the average. Alternative: use FRAM (ferroelectric RAM, 10¹⁰ cycles) for the
    write pointer — adds $0.50 to BOM but eliminates the wear-out entirely.'
  options:
  - 2 MB x 100,000 cycles = 200 GB total writes. At 32 bytes x 10/s = 320 bytes/s = 27.6 MB/day. 200 GB
    / 27.6 MB = 7,246 days = 19.8 years. No problem.
  - 'NOR flash erases in 4 KB sectors. Naive metadata pointer updates erase a sector on every write (864,000
    cycles/day), wearing out in 2.8 hours. Even data-only writes with proper circular buffer design (13.2
    cycles/day) last 20.8 years. The flash lifetime is determined by the hottest byte, not the average.
    Fix: use FRAM ($0.50 BOM) for the write pointer, or wear-level the metadata sector across multiple
    pages.'
  - The flash will last indefinitely because modern NOR flash has built-in wear leveling that automatically
    distributes writes across all sectors.
  - At 32 bytes per result and 10 results/second, the 2 MB flash fills up in 17.4 hours. The real problem
    is storage capacity, not endurance — you need a larger flash chip.
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: tinyml-chain-017
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Classification sanity - `reasoning_competency` and `reasoning_mode` are incorrectly set to `None` for
  a question that clearly tests Flash Memory, Wear Leveling, and System Lifetime.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
