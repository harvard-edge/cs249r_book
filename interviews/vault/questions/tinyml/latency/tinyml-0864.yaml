schema_version: '1.0'
id: tinyml-0864
track: tinyml
level: L3
zone: realization
topic: latency-decomposition
competency_area: latency
bloom_level: apply
phase: inference
title: 'Latency Decomposition: Size and Validate TinyML Model for Real-Time ECG'
scenario: 'Size and validate a TinyML model deployment for real-time ECG arrhythmia detection on an MCU with 256KB SRAM. ECG sample rate: 360Hz. The model processes 1-second windows (360 samples) and must infer faster than window arrival (< 1000ms). Model: 1D-CNN with 50K INT8 params, 2.5 MOPs.'
question: Do the ECG model's 50 KB weights, activations, and buffers fit in 256 KB SRAM on a 168 MHz MCU?
details:
  realistic_solution: The model comfortably passes all deployment feasibility checks. The INT8 static weights demand 50KB. The 1D-CNN activations peak at the first layer, requiring roughly 11.5KB for INT8 intermediate tensors. Combining weights, activations, and input buffering yields a total peak footprint well below 75KB, fitting securely in the 256KB SRAM. Furthermore, the 2.5 MOP compute requirement executes in ~15ms on a 168MHz MCU, leaving massive headroom for the 1000ms real-time deadline.
  common_mistake: |
    **The Pitfall:** Checking only the static model weight size without accounting for peak activation memory.
    **The Rationale:** Engineers look at the .tflite file size on disk and ignore the dynamic tensor arena required during inference execution.
    **The Consequence:** CNN models with large initial activation layers unexpectedly crash with OOM errors in production despite seemingly fitting in Flash.
  napkin_math: |
    **Assumptions & Constraints:**
    - SRAM: 256KB. Clock: 168MHz. Budget: 1000ms.
    - Model: 50K INT8 params, 2.5 MOPs.

    **Calculations:**
    - Weights: 50K * 1 byte = 50 KB.
    - Input Buffer: 360 samples * 2 bytes = 720 bytes.
    - Peak Activations (INT8): 360 * 32 channels = 11.5 KB.
    - Total Peak Memory: 50 KB + 11.5 KB + 1 KB = 62.5 KB.
    - Latency: 2.5 MOPs / 168 MHz = 14.9 ms.

    **Conclusion & Interpretation:**
    - **Result: 62.5 KB (Compute and Memory Satisfied)**. The model consumes only ~63 KB of SRAM and 15 ms of latency.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
