schema_version: '1.0'
id: tinyml-0784
track: tinyml
level: L4
zone: diagnosis
topic: latency-decomposition
competency_area: latency
bloom_level: analyze
phase: inference
title: Tinyml New 0006
scenario: An object detection model benchmarked on Jetson Orin at Batch Size 32 utilizes 80% of the 275 TOPS. In production at Batch Size 1, utilization drops to 5% and latency misses the 10ms real-time target.
question: Why does the Jetson Orin model lose utilization and miss latency targets when moving from batch size 32 to batch size 1?
details:
  realistic_solution: 'The model shifted from being compute-bound to memory-bandwidth-bound on the roofline. At BS=1, the arithmetic intensity (FLOPs per byte read) drops drastically because weights are loaded from memory for a single image. At BS=32, weights are reused across 32 images. To fix this, apply weight quantization, prune the model, or optimize L2 cache blocking to reduce memory traffic.'
  common_mistake: |
    **The Pitfall:** Using server-style large batch benchmarking to estimate real-time edge performance.
    **The Rationale:** Engineers assume compute capacity (TOPS) is the only metric that matters and expect performance to scale linearly down to single batches.
    **The Consequence:** The deployment misses strict real-time latency targets because the hardware hits a memory bandwidth bottleneck that was completely hidden during high batch-size testing.
  napkin_math: |
    **Assumptions & Constraints:**
    - Jetson Orin Compute: 275 TOPS.
    - Memory bandwidth: 204 GB/s.
    - Model layer requirement: 40M FLOPs, 20 MB weights.

    **Calculations:**
    - BS=1 Arithmetic Intensity: 40M FLOPs / 20 MB = 2 OPs/byte.
    - Max attainable compute at BS=1: 2 OPs/byte × 204 GB/s = 408 GOPS.

    **Conclusion & Interpretation:**
    - **Result: 408 GOPS (Memory-Bound)**. 408 GOPS is a minuscule fraction of the 275 TOPS theoretical limit, proving that single-batch inference completely starves the GPU of data.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
