schema_version: '1.0'
id: tinyml-1347
track: tinyml
level: L6+
zone: realization
topic: latency-decomposition
competency_area: latency
bloom_level: analyze
phase: inference
title: Real-time Anomaly Detection Latency on Nordic nRF5340
scenario: A health monitoring wearable device performs real-time anomaly detection on streaming accelerometer data sampled at 100 Hz. Upon detecting an anomaly, the device must immediately transmit an alert via BLE. The overall end-to-end latency must not exceed 250 ms to ensure timely intervention. You are observing that the system is occasionally exceeding this latency budget.
question: Why can an nRF5340 wearable exceed a 250 ms anomaly-alert budget even when TinyML inference itself is fast?
details:
  realistic_solution: 'To address the latency budget breach, one would perform a systematic latency decomposition. This involves profiling each stage: 1) Sensor acquisition and data windowing/preprocessing (e.g., FFT or scaling), 2) ML model inference (MAC operations, memory access patterns), 3) Postprocessing (thresholding, alert formatting), and 4) BLE transmission (packetization, stack overhead, connection interval optimization). Using hardware debuggers, logic analyzers, and internal timers on the nRF5340, precise measurements can be taken for each component. Optimizations might include reducing model complexity, optimizing data transfer between peripherals and CPU, streamlining BLE advertising/connection parameters (e.g., shorter connection intervals if power budget allows), or offloading some pre/post-processing to a more powerful edge device if the nRF5340 cannot meet the required performance.'
  common_mistake: |
    **The Pitfall:** Focusing solely on ML inference time.
    **The Rationale:** Engineers assume that since the model is tiny and fast, the system will easily meet latency requirements.
    **The Consequence:** They overlook significant latency contributions from sensor data acquisition (which dominates), preprocessing, and BLE communication stack overheads, leading to budget breaches.
  napkin_math: |
    **Assumptions & Constraints:**
    - Sampling Rate: 100 Hz.
    - Model Size: 200K MACs.
    - CPU Clock: 128 MHz.
    - Latency Budget: 250 ms.
    
    **Calculations:**
    - Data Acquisition: 100 samples at 100 Hz = 1000 ms (violates budget if fully sequential).
    - Preprocessing: ~10 ms.
    - Inference: 200,000 MACs * 5 cycles/MAC / 128 MHz = 7.8 ms.
    - Postprocessing: ~5 ms.
    - BLE Transmission: ~100 ms average.
    - Total Post-Acquisition Latency: 7.8 + 5 + 100 = 112.8 ms.
    
    **Conclusion & Interpretation:**
    - **Result: Latency-Bound Pipeline (Latency-Bound)**. While inference is fast (7.8 ms), data acquisition and BLE connection variability push the pipeline over the 250 ms budget.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The 'Napkin Math' section is incomplete, with the last sentence cut off.
- Reasoning Competency, Knowledge Area, and Mode are missing for an L6+ question.
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- At a 100 Hz sampling rate, acquiring 100 samples takes 1000ms (1 second), not 10ms. 10ms is the acquisition time for a single sample.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
