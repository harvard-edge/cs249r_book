schema_version: '1.0'
id: tinyml-1063
track: tinyml
level: L3
zone: analyze
topic: model-size-estimation
competency_area: architecture
bloom_level: analyze
phase: inference
title: Analyzing SRAM OOM in Early Convolution Layers
scenario: We are deploying an INT8 quantized CNN for audio classification on an ARM Cortex-M4 (STM32F4) microcontroller featuring 256 KB of SRAM and 1 MB of Flash. The compiled model weights and code take 150 KB and are stored in Flash. The first convolutional layer takes a 1x64x64 INT8 spectrogram input, applies 32 filters of 3x3 with stride 1 and SAME padding. When running this layer using CMSIS-NN, the system crashes with an SRAM out-of-memory (OOM) error.
question: Why can the first 64x64x32 INT8 convolution OOM an STM32F4 despite weights staying in flash?
details:
  realistic_solution: 'To determine if the layer can execute, we must calculate the peak dynamic memory footprint in SRAM. While the weights reside in Flash, SRAM must hold the input tensor, the output tensor, and the CMSIS-NN im2col scratch buffer simultaneously during the layer''s execution. When these are combined with the baseline SRAM usage of the RTOS and system stack, it exceeds the 256 KB limit.'
  common_mistake: |
    **The Pitfall:** Only comparing the static model size against Flash or SRAM limits.
    **The Rationale:** Candidates ignore the dynamic memory allocations required during inference, such as the intermediate working buffers needed for convolution operations.
    **The Consequence:** Believing there is plenty of room for the model, leading to unexpected runtime OOM crashes during the first layer's execution.
  napkin_math: |
    **Assumptions & Constraints:**
    - SRAM Limit: 256 KB.
    - Input Tensor: 64 x 64 x 1 byte = 4 KB.
    - Output Tensor: 64 x 64 x 32 channels * 1 byte = 128 KB.

    **Calculations:**
    - im2col Scratch Buffer (Worst-Case): 3 * 3 * 1 (filter) * 64 * 64 (spatial dims) = 36,864 bytes (~36 KB).
    - Layer Peak SRAM: 4 KB + 128 KB + 36 KB = 168 KB.

    **Conclusion & Interpretation:**
    - **Result: 168 KB Peak SRAM (Memory-Bound)**. If the RTOS/system stack uses > 88 KB, the total exceeds 256 KB, causing an immediate OOM crash.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
