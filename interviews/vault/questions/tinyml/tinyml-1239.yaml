schema_version: '1.0'
id: tinyml-1239
track: tinyml
level: L5
zone: realization
topic: dma-data-movement
competency_area: memory
bloom_level: evaluate
phase: inference
title: Zero-Copy DMA Pipeline for Ethos-U55
scenario: You are deploying a 60 FPS visual wake-word model on an ARM Cortex-M7 with an Ethos-U55 NPU. The camera outputs 256x256 INT8 grayscale frames over DCMI. The system has 512 KB of shared SRAM.
question: How should the Corstone-300 architecture utilize DMA to handle 60 FPS visual inference without inducing memory bus contention?
details:
  realistic_solution: The architecture must implement a zero-copy ping-pong DMA topology directly mapped to the Ethos-U55's input tensor buffers. The camera DMA writes asynchronously into two pre-allocated SRAM regions. Upon transfer completion, the M7 updates the NPU's input pointer, triggers execution, and sleeps. Weights reside persistently in XIP Flash and are streamed natively over the NPU's AXI bus, dedicating the bulk of the 512 KB SRAM strictly to high-speed activation buffers.
  common_mistake: |
    **The Pitfall:** Relying on explicit, CPU-driven memory replication.
    **The Rationale:** Candidates map distinct camera capture buffers and NPU ingestion buffers, manually shuffling the payload via `memcpy` functions.
    **The Consequence:** This software-driven approach doubles the memory footprint, unnecessarily burns M7 power cycles, and creates immense bus contention, causing the system to frequently drop 60 FPS frames.
  napkin_math: |
    1. **Assumptions & Constraints**:
       - Frame Size: 256 x 256 INT8 = 64 KB. 60 FPS (16.6ms per frame).
       - Shared SRAM: 512 KB. 64-bit bus at 480 MHz.
    2. **Calculations**:
       - Ping-Pong Buffers: 64 KB * 2 = 128 KB (leaves 384 KB for activations).
       - Required DMA Bandwidth: 64 KB / 16.6ms = 3.85 MB/s.
       - Total Bus Bandwidth: 8 bytes * 480 MHz = 3.84 GB/s.
       - Bus Utilization: 3.85 MB/s / 3840 MB/s ≈ 0.1%.
    3. **Conclusion & Interpretation**:
       - **Result: Memory-Bound Optimization**. A zero-copy DMA ping-pong buffer consumes 128 KB of SRAM and mathematically isolates the transfer to ~0.1% of the bus bandwidth, permanently eliminating contention.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
