schema_version: '1.0'
id: tinyml-0185
track: tinyml
level: L2
zone: recall
topic: tensor-arena-planning
competency_area: memory
bloom_level: understand
phase: inference
title: The SRAM Tensor Arena Puzzle
scenario: 'You''re deploying a keyword spotting model on a Cortex-M4 microcontroller. TensorFlow Lite
  for Microcontrollers requires a single contiguous block of memory called the ''Tensor Arena'' for all
  activations and I/O tensors. Your model has the following execution plan:'
question: "What minimum Tensor Arena size is needed for the keyword-spotting execution plan based on peak concurrent tensors?"
details:
  realistic_solution: 'The Tensor Arena''s size is determined by the *peak concurrent memory usage* at
    any point during inference. We must analyze the memory footprint at each step:'
  common_mistake: A common mistake is to simply sum the sizes of all tensors (12+30+10+2 = 54 KB), failing
    to account for the fact that not all tensors are live simultaneously. Another error is to only allocate
    for the largest single tensor (30 KB), which ignores the concurrent memory needs of inputs and outputs
    during an operation.
  napkin_math: 'Step1: input 12KB. Step2: Conv1 reads 12KB input, writes 20KB output (live: 12+20=32KB).
    Step3: Conv2 reads 20KB, writes 16KB (live: 20+16=36KB). Step4: Dense reads 16KB, writes 4KB (live:
    16+4=20KB). Peak = max(12, 32, 36, 20) = 36KB. Arena must be >= 36KB. If scenario layers differ, apply
    same max-concurrent-tensors logic.'
  options:
  - 54 KB, the sum of all tensor sizes.
  - 30 KB, the size of the largest tensor.
  - 42 KB, the peak concurrent memory usage during the first layer's operation.
  - 40 KB, the peak concurrent memory usage during the second layer's operation.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- The 'Napkin Math' assumes specific layer sizes (Conv1, Conv2, Dense) that are not provided in the scenario.
  The scenario only states 'Your model has the following execution plan' followed by bullet points about
  input, output, and intermediate tensor sizes but doesn't map them to specific 'Conv1', 'Conv2', 'Dense'
  layers. This makes the napkin math not directly derivable from the scenario's information.
- The 'Napkin Math' should directly use the generic input, output, and intermediate tensor sizes provided
  in the scenario rather than introducing new, specific layer types (Conv1, Conv2, Dense) and their associated
  sizes which are not explicitly given.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
