schema_version: '1.0'
id: tinyml-1245
track: tinyml
level: L5
zone: realization
topic: latency-decomposition
competency_area: latency
bloom_level: evaluate
phase: inference
title: ESP32-S3 Speech-to-Intent Latency Breakdown
scenario: You are architecting a local speech-to-intent engine on an ESP32-S3. The pipeline captures audio, extracts MFCC features, runs a 2.5M parameter INT8 autoregressive sequence model to generate a 5-token intent, and transmits via WiFi.
question: Evaluate the end-to-end latency by decomposing it into preprocessing, TTFT, TPOT, and network transmission.
details:
  realistic_solution: The correct approach recognizes that autoregressive generation (batch size of 1) is entirely memory-bandwidth bound. While compute is heavily accelerated by the INT8 vector extensions, fetching weights from PSRAM dominates the time per output token. A comprehensive L5 response decomposes latency into SRAM-based preprocessing, PSRAM-bound TPOT, and fixed WiFi overhead.
  common_mistake: |
    **The Pitfall:** Assuming latency is purely compute-bound by the 240 MHz CPU.
    **The Rationale:** Candidates overlook that a 2.5MB model exceeds the 512KB SRAM and must reside in PSRAM. They fail to account for the PSRAM bandwidth bottleneck (~80 MB/s).
    **The Consequence:** Drastically underestimating Time per Output Token (TPOT) for autoregressive generation.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model Size: 2.5 MB INT8.
    - ESP32-S3 PSRAM Bandwidth: ~80 MB/s.
    - Preprocessing (SRAM): ~20 ms.
    - Network (WiFi): ~150 ms.
    - Token Generation: 5 tokens.

    **Calculations:**
    - TPOT (Time per Output Token): 2.5 MB / 80 MB/s = 31.25 ms. Total ~35 ms (with compute).
    - TTFT (Time to First Token): 20 ms (prep) + 35 ms (first token) = 55 ms.
    - Total Inference Latency: TTFT (55 ms) + 4 * TPOT (140 ms) = 195 ms.
    - End-to-End Latency: 195 ms + 150 ms (WiFi) = 345 ms.

    **Conclusion & Interpretation:**
    - **Result: Memory Bandwidth-Bound.** Autoregressive generation with batch size 1 is limited by PSRAM fetch speeds (~80 MB/s), dominating the Time per Output Token (TPOT).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
