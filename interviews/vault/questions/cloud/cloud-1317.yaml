schema_version: '1.0'
id: cloud-1317
track: cloud
level: L5
zone: specification
topic: collective-communication
competency_area: networking
bloom_level: evaluate
phase: both
title: Gradient Quantization vs Sparsification
scenario: You are training a 10B parameter model using Data Parallelism across 64 GPUs over a 100 Gbps Ethernet network. Gradients are currently in FP16 (20 GB). Dense Ring AllReduce takes ~3.2 seconds, severely bottlenecking the training step. The ML research team proposes Top-1% gradient sparsification (with local error feedback) to reduce the per-node payload by 100x. The infrastructure team proposes INT8 block-quantization, which only reduces the payload by 2x.
question: "For the 10B model on 64 GPUs over 100GbE, should you deploy Top-1% sparsification or INT8 gradient quantization, and why?"
details:
  realistic_solution: Choose INT8 quantization. Top-1% of 10B parameters requires 1GB per GPU (values + INT64 indices). Gathering this across 64 GPUs transfers 63GB per node, which is actually slower than doing absolutely nothing (FP16 dense AllReduce transfers 40GB). INT8 maintains O(1) communication scaling, perfectly utilizes optimized collective primitives, and avoids the 20GB memory overhead required to store the local error feedback buffer.
  common_mistake: |
    **The Pitfall:** Instinctively assuming a '100x reduction' in payload makes sparsification 50x faster than INT8.
    **The Rationale:** Candidates fail to realize that sparse synchronization relies on AllGather (scaling O(N)), whereas dense Ring AllReduce transfers a constant ~2x payload regardless of N.
    **The Consequence:** Deploying sparsification actually slows down communication at scale compared to doing nothing.
  napkin_math: |
    1. FP16 Dense AllReduce: 2 * 20 GB = 40 GB transferred per node (3.2s at 12.5 GB/s bandwidth).
    2. INT8 AllReduce: 2 * 10 GB = 20 GB transferred (1.6s).
    3. Top-1% Sparse: 100M params. Using INT64 indices, each element is 10 bytes (2B FP16 + 8B INT64) = 1 GB payload per node.
    4. AllGather at N=64: 63 * 1 GB = 63 GB transferred per node (5.0s).
    5. **At N=64, sparsification is 50% slower than the FP16 baseline, whereas INT8 is 2x faster.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T22:33:41.457830'
