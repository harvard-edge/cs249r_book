schema_version: '1.0'
id: cloud-0714
track: cloud
level: L5
zone: evaluation
topic: 3d-parallelism
competency_area: parallelism
bloom_level: evaluate
phase: training
title: The Heterogeneous GPU Training
scenario: Your cloud budget gets you 32 A100 80GB GPUs and 32 H100 80GB GPUs. An engineer proposes combining them into a single 64-GPU DDP training job for a 7B model. The H100 does a training step in 400 ms; the A100 takes 650 ms.
question: What happens when you combine A100 and H100 GPUs in a single DDP training job, and how can you use both efficiently?
details:
  realistic_solution: 'With naive DDP, every step takes 650 ms (the A100 speed). Solutions: (1) Separate jobs — run two independent training jobs, one per GPU type, and merge checkpoints periodically. (2) Weighted data parallelism — give H100s larger micro-batches proportional to their speed (H100 gets batch=6, A100 gets batch=4), so both finish at the same time.'
  common_mistake: |
    **The Pitfall:** Believing DDP averages the throughput across heterogeneous GPUs.
    **The Rationale:** Assuming the overall speed will be somewhere between the A100 and H100.
    **The Consequence:** DDP is synchronous, meaning the entire cluster drops to the speed of the slowest GPU (A100), wasting the H100s.
  napkin_math: |
    - 1. **Naive DDP:** Assume batch=4 per GPU. H100s sit idle for 250ms per step. Throughput = 64 batch × 4 / 0.65s = 394 samples/s. (H100 utilization = 61%).
    - 2. **Weighted DDP:** Assign H100 batch=6 (compute ≈ 550ms) and A100 batch=4 (compute = 650ms).
    - 3. **Result: Global batch = (32 × 6) + (32 × 4) = 320. Throughput = 320 / 0.65s = 492 samples/s (25% faster than naive DDP).**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null