schema_version: '1.0'
id: cloud-2887
track: cloud
level: L5
zone: realization
topic: activation-memory
competency_area: memory
bloom_level: apply
phase: training
title: Realizing Activation Checkpointing in a ZeRO-3 + Tensor-Parallel Hybrid Configuration
scenario: You are deploying a 65B model training job on 32 GPUs using DeepSpeed ZeRO-3 with tensor parallelism degree 4. Activation memory profiling shows 22 GB per GPU before checkpointing. ZeRO-3 shards parameters and gradients across all 32 GPUs but does NOT shard activations.
question: "What happens to the 22 GB/GPU activation memory when DeepSpeed activation checkpointing is enabled, and how does it interact with TP=4?"
details:
  realistic_solution: 'With TP=4, activations for each layer are already split: Q/K/V projections are column-parallel, MLP is row-parallel. When `checkpoint_activations=True`, DeepSpeed discards all intermediate activations between checkpoints and recomputes them during backward. The TP communication (AllReduce for row-parallel layers) is re-executed during recompute. This is correct but adds collective communication overhead.'
  common_mistake: |
    **The Pitfall:** Assuming ZeRO-3 activation checkpointing will shard activation tensors across the data-parallel dimension.
    **The Rationale:** Candidates wrongly assume ZeRO-3 shards all memory structures, including activations.
    **The Consequence:** They miscalculate memory requirements, leading to unexpected OOMs when activations remain fully local to each GPU.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 65B model, 80 layers.
       - Pre-checkpointing activation memory = 22 GB/GPU.
       - Checkpointing interval = 20 layers (4 segments).
       - Tensor Parallelism (TP) = 4.
    2. **Calculations:**
       - Active segment memory: 22 GB / 4 = 5.5 GB/GPU.
       - TP AllReduce recompute overhead per backward pass: 2 × 4 × T_allreduce × 20 segments ≈ 40 ms.
       - Assuming 500 ms/step total compute time, overhead = 40 ms / 500 ms = 8%.
    3. **Conclusion & Interpretation:** **Activation memory drops to 5.5 GB/GPU at the cost of an 8% compute overhead due to re-executed TP communication. Result: Memory-Bound (Mitigated).**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Contradictory math and logic: ''22 GB / 32 GPUs; per-GPU activation = 22 GB''.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
