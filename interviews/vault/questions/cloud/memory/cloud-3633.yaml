schema_version: '1.0'
id: cloud-3633
track: cloud
level: L4
zone: optimization
topic: rdma-transport
competency_area: memory
bloom_level: evaluate
phase: both
title: 'GPUDirect Storage and RDMA for Checkpoint Loading on H100'
scenario: 'Checkpointing a 70B parameter model (280GB BF16) to NFS takes 8 minutes. Restoring after failure takes 12 minutes, causing 20-minute recovery windows during 14-day training.'
question: 'Would GPUDirect Storage (GDS) meaningfully reduce 70B checkpoint save and restore times to NFS, and what improvement should you expect?'
details:
  realistic_solution: 'Current path: GPU HBM -> CPU DRAM (PCIe, 64GB/s) -> CPU copy -> NFS write. CPU copy overhead = 280GB / 64GB/s = 4.4s (CPU copy). NFS bandwidth: 280GB / 480s = 0.58GB/s -> NFS is the bottleneck, not the CPU copy! GDS path: GPU HBM -> P2P RDMA to storage NIC -> NFS server. Eliminates CPU copy but NFS throughput is still 0.58GB/s. GDS speedup limited. Real fix: NFS server RDMA bandwidth upgrade (25GbE -> 100GbE), or use distributed checkpointing with multiple NFS servers in parallel.'
  common_mistake: |
    **The Pitfall:** Assuming GPUDirect Storage always provides a massive speedup.
    **The Rationale:** Candidates fail to quantify the bottlenecks; they apply a "fast" technology without calculating that the network-attached storage throughput is orders of magnitude slower than the PCIe bus.
    **The Consequence:** Spending weeks engineering a complex RDMA/GDS implementation for a sub-1% performance improvement.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model size: 280 GB (BF16).
    - NFS save time: 480 s (8 minutes).
    - PCIe 4.0 x16 bandwidth: 64 GB/s.

    **Calculations:**
    - Current NFS throughput: 280 GB / 480 s = 583 MB/s.
    - CPU copy time over PCIe: 280 GB / 64 GB/s = 4.4 s.
    - GDS impact: Eliminates the 4.4s PCIe/CPU copy.
    - Remaining NFS time with GDS: 480 s - 4.4 s = 475.6 s.

    **Conclusion & Interpretation:**
    - **Result: Network-Storage Bound**. GDS provides less than 1% improvement because the bottleneck is the 583 MB/s NFS throughput, not the 64 GB/s PCIe bus.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
