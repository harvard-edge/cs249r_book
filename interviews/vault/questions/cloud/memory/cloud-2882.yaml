schema_version: '1.0'
id: cloud-2882
track: cloud
level: L4
zone: diagnosis
topic: activation-memory
competency_area: memory
bloom_level: evaluate
phase: training
title: Evaluating Activation Memory Profiler Output for Silent Memory Fragmentation
scenario: 'A PyTorch memory snapshot shows peak allocated memory of 68 GB during training a 30B model, but the job crashes with an OOM at step 47 despite peak allocation appearing stable. The memory timeline shows reserved memory is consistently 74 GB.'
question: How would you evaluate whether activation memory fragmentation is the cause and determine a remediation path?
details:
  realistic_solution: 'Inspect the memory snapshot with `torch.cuda.memory_snapshot()` to identify large fragmented blocks. Enable `PYTORCH_NO_CUDA_MEMORY_CACHING=1` temporarily to confirm the OOM disappears (proving fragmentation). Then set `torch.cuda.memory.set_per_process_memory_fraction(0.90)` to trigger earlier GC, or sort gradient checkpointed segments so that the largest activation tensors are allocated and freed in LIFO order.'
  common_mistake: |
    **The Pitfall:** Confusing PyTorch's 'allocated' metric with 'reserved' metric.
    **The Rationale:** Engineers assume that if allocated memory is lower than total VRAM, the job shouldn't OOM.
    **The Consequence:** Failing to diagnose memory fragmentation, leading to wasted time trying to reduce batch size or precision unnecessarily.
  napkin_math: |
    **Assumptions & Constraints:**
    - Reserved memory = 74 GB.
    - Allocated memory = 68 GB.
    - Activation tensor requirement = 512 MB contiguous.
    
    **Calculations:**
    - Fragmented memory: 74 GB - 68 GB = 6 GB.
    - If largest free block < 512 MB, allocation fails.
    
    **Conclusion & Interpretation:**
    - **Result: 6 GB fragmented (Memory-Bound)**. A 512 MB allocation triggers OOM despite 6 GB nominally free due to fragmentation.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
