schema_version: '1.0'
id: cloud-2577
track: cloud
level: L3
zone: fluency
topic: vram-budgeting
competency_area: memory
bloom_level: apply
phase: both
title: ZeRO Stage Memory Budget for a 65B AdamW Model on 32 GPUs
scenario: You are training a 65B model with AdamW in mixed precision on 32 GPUs. You have reserved 15 GB per GPU for activations.
question: What is the per-GPU memory footprint for ZeRO-1, ZeRO-2, and ZeRO-3, and which stage fits?
details:
  realistic_solution: |
    65B params. FP16 params: 130 GB. FP16 gradients: 130 GB. AdamW optimizer states (FP32 copy + m + v): 65B * 12B = 780 GB. Total model-state memory is 1,040 GB. Budget per GPU is 80 - 15 = 65 GB. ZeRO-1 shards only optimizer state: 130 + 130 + 780/32 = 284.4 GB per GPU, so it does not fit. ZeRO-2 shards optimizer state and gradients: 130 + 130/32 + 780/32 = 158.4 GB per GPU, so it also does not fit. ZeRO-3 shards parameters, gradients, and optimizer state: 1040/32 = 32.5 GB per GPU, so ZeRO-3 is the only listed stage that fits.
  common_mistake: |
    **The Pitfall:** Forgetting that ZeRO-2 still replicates full parameters on every GPU.
    **The Rationale:** Assuming ZeRO-2 solves memory issues for models larger than single-GPU capacity.
    **The Consequence:** OOM crashes immediately upon model initialization.
  napkin_math: |
    **Assumptions & Constraints:**
    - 65B Parameters. GPU memory budget: 80 GB - 15 GB = 65 GB.
    - FP16 Params: 130 GB. FP16 Gradients: 130 GB.
    - AdamW FP32 Optimizer States: 65B * 12 bytes = 780 GB.
    - Total Memory: 1040 GB.

    **Calculations:**
    - ZeRO-1 (shard OS): 130 + 130 + (780/32) = 284 GB/GPU.
    - ZeRO-2 (shard OS+G): 130 + (130/32) + (780/32) = 158 GB/GPU.
    - ZeRO-3 (shard all): 1040 / 32 = 32.5 GB/GPU.

    **Conclusion & Interpretation:**
    - **Result: Memory-Capacity Bound.** Only ZeRO-3 fits within the 65 GB VRAM budget (using 32.5 GB/GPU).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
