schema_version: '1.0'
id: cloud-4373
track: cloud
level: L5
zone: realization
topic: vram-budgeting
competency_area: memory
bloom_level: create
phase: both
title: 'MI300X vs A100: VRAM Capacity Advantage for Long-Context Serving'
scenario: 'A legal AI application requires serving a 13B FP16 model with 128K context length. KV cache per token per layer is: 2 x num_heads x head_dim x 2 bytes = 2 x 40 x 128 x 2 = 20.5KB per token per layer. The model has 40 layers.'
question: For the 13B model at 128K context, how large is one request's KV cache, and how many requests fit on 80GB vs 192GB GPUs?
details:
  realistic_solution: 'Per-token KV cache (all 40 layers): 40 layers x 20.5KB = 820KB per token. For 128K context: 820KB x 128,000 tokens = 105GB per request. Weight memory: 13B x 2 bytes = 26GB. Available KV cache memory on 80GB GPU: 80 - 26 = 54GB. Requests: floor(54GB / 105GB) = 0. On 192GB GPU: 192 - 26 = 166GB. floor(166 / 105) = 1 request.'
  common_mistake: |
    **The Pitfall:** Calculating KV cache for a single layer.
    **The Rationale:** Candidates forget that the KV cache must be stored across all Transformer layers, not just one.
    **The Consequence:** The candidate underestimates memory requirements by 40x and promises the client that a standard 80GB GPU can serve the 128K context.
  napkin_math: |
    **Assumptions & Constraints:**
    - 13B model weights size: 26 GB.
    - KV cache per token: 20.5 KB per layer.

    **Calculations:**
    - Total KV per token: 20.5 KB x 40 layers = 820 KB.
    - Total KV per request: 820 KB x 128,000 tokens = ~105 GB.
    - 80GB GPU: 80 GB - 26 GB = 54 GB available (< 105 GB).
    - 192GB GPU: 192 GB - 26 GB = 166 GB available.

    **Conclusion & Interpretation:**
    - **Result: 1 request (Memory-Capacity-Bound)**. 80GB GPUs serve 0 requests. 192GB GPUs serve exactly 1 concurrent request.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
