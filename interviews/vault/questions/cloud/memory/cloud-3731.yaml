schema_version: '1.0'
id: cloud-3731
track: cloud
level: L6+
zone: mastery
topic: memory-hierarchy-design
competency_area: memory
bloom_level: create
phase: both
title: Multi-Level Memory Hierarchy Specification for 405B LLM Inference
scenario: You are designing a tiered memory hierarchy system for serving Llama-3-405B (FP8 = 405GB) on a production cluster with H100s, NVMe SSDs, and DRAM. The model must serve 500 concurrent users with an 8192 token context.
question: What tiered memory strategy or limits are required to serve 500 concurrent 8192-token users for Llama-3-405B FP8 on H100s?
details:
  realistic_solution: 'Model weights require 405GB FP8, so an 8x H100 node provides enough aggregate HBM for the model: 640GB total, leaving about 235GB for KV and runtime overhead. KV cache per token is 2 tensors × 126 layers × 8 KV heads × 128 dim × 1 byte = 258,048 bytes. For 500 users at 8192 tokens, KV alone is 500 × 8192 × 258,048 bytes ≈ 1057GB, so one 8x H100 node cannot serve the stated requirement fully in HBM. Required strategy: either shard across several nodes, offload cold KV pages to host DRAM/NVMe with latency-aware paging, reduce context/concurrency, or cap the hot HBM tier. As an HBM-only example, about 100 users at 8192 tokens need 211GB KV, and 405GB + 211GB = 616GB fits before overhead; this is a feasible cap, not the answer for all 500 users.'
  common_mistake: |
    **The Pitfall:** Trying to fit the full model and all KV caches in GPU HBM without concurrency limits or CPU offload.
    **The Rationale:** Candidates size the cluster purely based on model weights and underestimate the massive KV cache required for 500 users at 8K context.
    **The Consequence:** Out-of-memory errors in production or severe thrashing as the system attempts to page massive KV caches to slow storage.
  napkin_math: |
    **Assumptions & Constraints:**
    - 405GB FP8 model, 126 layers, 8 KV heads, 128 dim.
    - 8x H100 node has 640GB HBM total, leaving about 235GB after model weights before overhead.

    **Calculations:**
    - Per-token KV cache: 2 * 126 * 8 * 128 * 1B = 258,048 bytes.
    - 500 users * 8192 tokens * 258,048 bytes = ~1057GB KV cache.
    - Total for 500 users = 405GB model + 1057GB KV = 1462GB, which exceeds the 640GB HBM in one 8x H100 node.
    - HBM-only concurrency cap before overhead: 235GB available KV / (8192 * 258,048 bytes/user) = about 111 users.
    - Practical example cap: 100 users gives 405GB + 211GB KV = 616GB before overhead.

    **Conclusion & Interpretation:**
    - **Result: 500 Concurrent 8K Users Do Not Fit in One 8x H100 HBM Tier.** Use a lower hot-concurrency cap, shorter contexts, more GPU nodes, or KV offload. The 100-user case is only an example of an HBM-feasible limit.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 20
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-27'
math_model: gemini-cli
math_issues:
  - Mixed binary and decimal prefixes led to an incorrect KV cache size calculation. 252 KB \times 4,096,000 does not equal 1,032 GB. The actual size is roughly 1057 GB, which changes the total memory requirements from 611 GB to 616 GB.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
