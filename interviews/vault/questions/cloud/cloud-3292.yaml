schema_version: '1.0'
id: cloud-3292
track: cloud
level: L4
zone: optimization
topic: memory-pressure-management
competency_area: memory
bloom_level: analyze
phase: both
title: Optimizing 70B LLM Training on NVIDIA A100 Under Memory Pressure
scenario: You are fine-tuning a 70-billion parameter Large Language Model on a single NVIDIA A100 GPU. Despite 8-bit quantization and gradient accumulation, you frequently encounter erratic Out-Of-Memory (OOM) errors. You must diagnose and resolve these memory pressure issues to ensure stable and efficient training.
question: How would you diagnose and resolve these memory pressure issues to ensure stable and efficient training?
details:
  realistic_solution: 1. **Profile Memory:** Utilize Nsight Compute or PyTorch's `profile` with `memory_stats=True` to pinpoint exact memory allocation patterns, identify peak memory usage, and detect fragmentation hotspots. 2. **Activation Management:** Implement gradient checkpointing or offload activations to CPU memory (if bandwidth permits) to reduce the memory footprint of intermediate activations during the backward pass. 3. **Optimizer State Sharding:** Employ advanced techniques like ZeRO-Offload (e.g., DeepSpeed ZeRO-2/3) or Fully Sharded Data Parallelism (FSDP) to shard optimizer states and even model parameters across CPU/GPU or multiple GPUs, significantly reducing per-GPU memory usage. 4. **Memory Allocation Strategy:** Investigate custom memory allocators or pre-allocation strategies within the framework (e.g., PyTorch's caching allocator) to mitigate fragmentation. 5. **System-Level Monitoring:** Monitor host CPU memory and swap usage to differentiate between GPU OOMs and system-wide memory pressure impacting data loading or other processes.
  common_mistake: |
    **The Pitfall:** Relying solely on reducing batch size or applying basic model weight quantization.
    **The Rationale:** Lacking a deeper understanding of the full memory footprint, particularly optimizer states, during backpropagation.
    **The Consequence:** OOM crashes persist because the 420 GB optimizer state dwarfs the savings gained from quantizing the model weights.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 70B parameters. A100 capacity: 80 GB HBM2e.
       - FP16 baseline size: 140 GB. 8-bit quantized size: 70 GB.
       - Adam Optimizer needs FP16 copy (2 bytes) + FP32 momentum/variance (4 bytes).
    2. **Calculations:**
       - Optimizer state size: (70B × 2) + (70B × 4) = 140 GB + 280 GB = 420 GB.
       - Total footprint (8-bit model + FP16 Adam): 70 GB + 420 GB = 490 GB.
    3. **Conclusion & Interpretation:** **Result: Memory-Bound.** The optimizer states alone (420 GB) grossly exceed the A100's 80 GB limit, making DeepSpeed ZeRO/FSDP state sharding absolutely critical.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Reasoning Competency, Knowledge Area, and Mode are missing. Napkin Math calculation is incomplete and does not explicitly address the OOM errors.
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Math error: 70B parameters * 2 bytes/param is 140 GB, not 70 GB. Total should be 420 GB, not 350 GB.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
