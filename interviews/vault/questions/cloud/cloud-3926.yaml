schema_version: '1.0'
id: cloud-3926
track: cloud
level: L5
zone: implement
topic: communication-computation-overlap
competency_area: parallelism
bloom_level: apply
phase: both
title: NCCL Async Operations and CUDA Stream Orchestration for Overlap
scenario: You are implementing a custom training loop (not using PyTorch DDP) for
  a mixture-of-experts model. You need to manually overlap AllReduce with backward
  computation using NCCL async operations on separate CUDA streams. The model has
  expert parameters and shared parameters that need different AllReduce groups.
question: If 4GB of gradients are sent over a 50 GB/s link, what is the serial communication
  time before any overlap?
details:
  realistic_solution: 4GB / 50 GB/s = 80ms serial time (not 40ms which requires either
    2GB data or 100 GB/s bandwidth).
  common_mistake: Issuing NCCL calls on the default CUDA stream, which serializes
    all operations. NCCL operations must be on a dedicated communication stream to
    overlap with compute on the default stream.
  napkin_math: 'Serial transfer time: 4GB at 50 GB/s = 4/50 = 0.08s = 80ms (not 40ms
    — 40ms would require 100 GB/s bandwidth or 2GB data). 4GB / 50GB/s = 0.080s =
    80ms.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Serial time for 4GB (2GB expert + 2GB shared) at 50GB/s is 80ms, not 40ms.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
