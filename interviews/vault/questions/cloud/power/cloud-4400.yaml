schema_version: '1.0'
id: cloud-4400
track: cloud
level: L5
zone: realization
topic: datacenter-efficiency
competency_area: power
bloom_level: apply
phase: both
title: MI300X Unified Memory Architecture and Its Thermal Implications
scenario: The MI300X uses a unified memory architecture where CPU and GPU share the same 192GB HBM3 pool across 8 compute dies on a single package. During a production incident, a model serving process on MI300X unexpectedly causes memory bandwidth saturation at 5.1 TB/s—despite the workload previously running at 3.8 TB/s. The system power jumps from 680W to 740W.
question: "Why did the MI300X serving process jump to 5.1 TB/s bandwidth and 740W, what are the thermal consequences, and what workload change caused it?"
details:
  realistic_solution: 'Power delta: 740W - 680W = 60W additional. Actual delta is 60W, suggesting HBM power model is ~0.6x of theoretical (other power domains decrease as compute is stalled waiting for memory). Workload change: likely a sequence length increase (e.g., from 2K to 16K context) triggering much larger KV cache reads per token.'
  common_mistake: |
    **The Pitfall:** Assuming HBM bandwidth is free in terms of power.
    **The Rationale:** Engineers focus on compute TFLOPS as the primary power draw and ignore that pushing 5+ TB/s across a package requires immense energy.
    **The Consequence:** Near-peak memory bandwidth dramatically increases thermal load, which can throttle compute and cause unpredictable latency spikes.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - MI300X HBM3 bandwidth: 5.3 TB/s max.
       - HBM energy cost: ~10pJ/bit.
       - Base power 680W, jumped to 740W (Delta: 60W).
    2. **Calculations:**
       - HBM power at 5.1 TB/s: 5.1e12 bytes/s × 8 bits/byte × 10e-12 J/bit = 408W.
       - HBM power at 3.8 TB/s: 3.8e12 bytes/s × 8 bits/byte × 10e-12 J/bit = 304W.
       - Power delta from HBM alone: 408W - 304W = 104W (actual system delta is 60W, meaning compute stalled and drew less power).
       - KV cache at 16K vs 2K context: 8x increase in memory reads per token, adding ~1.3 TB/s.
    3. **Conclusion & Interpretation:** **Longer contexts triggered massive KV cache reads, pushing bandwidth to 5.1 TB/s and surging HBM power by 104W. Result: Memory-Bound.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
