schema_version: '1.0'
id: cloud-0747
track: cloud
level: L6+
zone: realization
topic: collective-communication
competency_area: networking
bloom_level: create
phase: both
title: The Ring vs Tree Dilemma
scenario: For our 10B parameter model, Ring AllReduce utilizes our network perfectly. However, when we
  switch to a 100M parameter model, it is terribly slow despite moving far less data. Why does the 'best'
  algorithm fail here?
question: "Why does the 'best' algorithm fail here?"
details:
  realistic_solution: Ring AllReduce is bandwidth-optimal but latency-bound for small payloads. It requires
    $2(N-1)$ steps around the ring. For huge models, the bandwidth saturation hides the latency. For small
    models, the network transfer happens instantly, but the latency of hopping through $N$ nodes dominates.
    You must switch to a Tree reduction ($O(\log N)$ latency) for small messages.
  common_mistake: '"Ring AllReduce is always optimal — something else must be wrong." Ring is bandwidth-optimal
    but not latency-optimal.'
  napkin_math: '64 nodes, 5 μs per hop. Ring: $2 \times 63$ hops × 5 μs = 630 μs of pure latency. Tree:
    $2 \times \log_2(64)$ = 12 hops × 5 μs = 60 μs. For a 100M param model (200 MB), the actual data transfer
    at 50 GB/s takes only 4 ms — but Ring adds 630 μs of latency overhead (16% tax) vs Tree''s 60 μs (1.5%
    tax).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
