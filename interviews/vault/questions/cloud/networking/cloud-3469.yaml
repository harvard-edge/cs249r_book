schema_version: '1.0'
id: cloud-3469
track: cloud
level: L5
zone: design
topic: load-balancing
competency_area: networking
bloom_level: analyze
phase: inference
title: Designing a High-Throughput Inference Load Balancer for NVIDIA A100 Clusters
scenario: You are designing the load balancing system for a multi-datacenter ML inference service running on clusters. Inference requests vary significantly in complexity, leading to fluctuating processing times per model. The system must efficiently distribute traffic, minimize latency, and maximize GPU utilization.
question: "How would you design request routing for this service considering consistent hashing, weighted round-robin, and dynamic traffic management to meet a strict P99 latency target?"
details:
  realistic_solution: Employ a smart routing layer that uses Least Outstanding Requests (LOR) combined with real-time continuous batching metrics. The load balancer must dynamically weight nodes such that requests are routed away from GPUs nearing their SLA queue depth limit.
  common_mistake: |
    **The Pitfall:** Implementing a simple round-robin or least-connection strategy.
    **The Rationale:** These are standard L4/L7 load balancing strategies that candidates default to without considering ML workload heterogeneity.
    **The Consequence:** This leads to hot-spotting, imbalanced GPU utilization, and increased tail latencies, as a slow-processing request blocks subsequent requests on the same GPU.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Target SLA: P99 latency of L ms.
       - Average processing time: T ms.
       - A100 HBM2e bandwidth: 2000 GB/s.
       - Model memory per inference: B GB.
    2. **Calculations:**
       - Memory Bottleneck Throughput: 2000 GB/s / B inferences/second.
       - Queue Latency Constraint: Queue depth D must satisfy D * T << L.
    3. **Conclusion & Interpretation:**
       - **Result: Latency-Bound Queuing.** The load balancer must dynamically weight nodes using Least Outstanding Requests (LOR) such that no GPU receives traffic if its estimated D * T approaches the SLA limit L.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
