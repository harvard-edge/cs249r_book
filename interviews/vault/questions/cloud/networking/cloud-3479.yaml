schema_version: '1.0'
id: cloud-3479
track: cloud
level: L5
zone: evaluation
topic: congestion-control
competency_area: networking
bloom_level: analyze
phase: both
title: Evaluating Congestion Control Strategies in an A100 GPU Cluster
scenario: 'You are managing a large-scale distributed ML training cluster comprised of 512 GPUs utilizing a high-speed Ethernet fabric. During peak training phases, you observe significant incast congestion where many GPUs simultaneously send gradient updates to parameter servers. Your team has identified two primary architectural proposals: (A) DCQCN-centric with specific NIC and core switch configurations, and (B) PFC/ECN/Scheduling utilizing Priority Flow Control.'
question: Which proposal would you choose for the 512-GPU incast problem, and what are the trade-offs of DCQCN versus PFC/ECN with flow scheduling?
details:
  realistic_solution: |
    Choose Proposal B, but only if it is implemented as PFC/ECN plus explicit flow scheduling rather than PFC alone. Proposal A, a DCQCN-centric design, is useful for rate adaptation on RoCE flows and should still be tuned on NICs and switches, but by itself it reacts after queues form and can be too slow for synchronized 512-GPU incast. Proposal B gives a stronger control loop: PFC prevents loss on the no-drop class, ECN marks congestion before buffers overflow, and framework or transport scheduling staggers gradient buckets so the fan-in is shaped before it reaches the aggregation links. The trade-off is operational complexity and PFC risk: bad thresholds can cause head-of-line blocking or pause storms, so lossless classes must be narrow, ECN thresholds tested, and scheduling coordinated with collectives. I would use B with DCQCN/ECN tuning as guardrails, not a pure PFC configuration.
  common_mistake: |
    **The Pitfall:** Applying generic TCP/IP congestion control mechanisms to ML training.
    **The Rationale:** Network engineers default to standard CUBIC or Reno, failing to account for the unique, synchronized, bursty all-reduce patterns of ML workloads.
    **The Consequence:** These protocols react too slowly to microbursts, failing to prevent severe queue build-up and packet drops during massive incast events.
  napkin_math: |
    **Assumptions & Constraints:**
    - 512 GPUs participate in the incast.
    - Each sends a 64 MB gradient shard toward a small set of aggregation targets.
    - The burst arrives over a 1ms scheduling window.

    **Calculations:**
    - Total burst size = 512 * 64 MB = 32,768 MB = 32 GB.
    - Instantaneous offered load over 1ms = 32 GB / 0.001s = 32 TB/s.
    - A single 400 Gbps egress carries about 50 GB/s, so unscheduled fan-in can exceed one output by hundreds of times.

    **Conclusion & Interpretation:**
    - **Result: Incast must be shaped before queues overflow.** The burst can exceed a single aggregation egress by hundreds of times, so Proposal B's PFC/ECN controls need flow scheduling or gradient bucketing; Proposal A's DCQCN-only reaction is not enough for the synchronized fan-in.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
