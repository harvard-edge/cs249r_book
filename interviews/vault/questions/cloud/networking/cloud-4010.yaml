schema_version: '1.0'
id: cloud-4010
track: cloud
level: L4
zone: analyze
topic: congestion-control
competency_area: networking
bloom_level: analyze
phase: both
title: PFC Deadlock in Lossless Ethernet Fabrics
scenario: 'A cluster running RoCE v2 over a Clos network with Priority Flow Control (PFC) enabled experiences a full fabric stall during a large-scale training job. Monitoring shows PFC pause frames propagating backward from ToR switches all the way to the spine. The job hangs for 30+ seconds before partial recovery.'
question: "How does PFC deadlock form in this Clos network, and how would you prevent full-fabric stalls?"
details:
  realistic_solution: 'PFC deadlock occurs when a circular dependency forms: Switch A pauses Switch B, B pauses C, and C pauses A, creating a cycle where no switch can drain. In Clos networks, this happens when uphill and downhill traffic share the same PFC priority class. The fix is to use separate virtual lanes (VLs) for different traffic directions, implement DCBX with proper PFC watchdog timers (typically 100ms), or use DSCP-based ECN marking with DCQCN to reduce reliance on PFC entirely. On MI300X clusters with 400G links, a single PFC storm can freeze 5.3 TB/s of HBM bandwidth per GPU waiting on the network.'
  common_mistake: |
    **The Pitfall:** Assuming PFC simply slows traffic like TCP backpressure.
    **The Rationale:** Misunderstanding that PFC can create circular buffer dependencies when pause frames propagate across tiers.
    **The Consequence:** Full fabric stalls that freeze training jobs, wasting massive compute resources.
  napkin_math: |
    **Assumptions & Constraints:**
    - PFC pause quanta = 65535 at 400Gbps.
    - 1 quantum = 512 bit times = 1.28 ns.
    - ~500ns delay per hop, 3 tiers.

    **Calculations:**
    - Max pause per frame: 65535 * 1.28 ns = 83.88 µs.
    - Switch propagation delay: 3 hops * 500 ns = 1.5 µs.

    **Conclusion & Interpretation:**
    - **Result: Full fabric stall occurs within ~5 µs of circular dependency forming (Network-Congestion-Bound).**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- A PFC pause quanta of 65535 at 400Gbps equals ~83.9µs, not ~1.3µs (1 quantum = 512 bit times = 1.28ns; 65535 * 1.28ns = 83.88µs).
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
