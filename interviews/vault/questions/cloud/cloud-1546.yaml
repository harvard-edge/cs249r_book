schema_version: '1.0'
id: cloud-1546
track: cloud
level: L5
zone: optimization
topic: memory-hierarchy-design
competency_area: memory
bloom_level: evaluate
phase: both
title: LLM Serving Bottleneck Evaluation
scenario: 'You are evaluating optimization strategies for serving a 70B parameter autoregressive model (140GB in BF16 weights) on an 8x A100 GPU cluster (80GB HBM, 2 TB/s bandwidth per GPU). The product requires maximizing throughput for concurrent users while maintaining a strict 50ms time-between-tokens (TBT).'
question: 'For 70B decoding on 8x A100s with a 50ms TBT target, should you prioritize INT8 matmul or continuous batching with PagedAttention?'
details:
  realistic_solution: 'Prioritize continuous batching and memory management (PagedAttention). The decode phase is strictly memory-bandwidth bound. Efficient memory management eliminates KV cache fragmentation, allowing larger batch sizes which directly increase system throughput by amortizing the massive weight load cost.'
  common_mistake: |
    **The Pitfall:** Prioritizing compute optimizations like INT8 matrix multiplication over advanced memory management.
    **The Rationale:** Candidates observe the model's massive FLOPs-to-Bytes ratio and incorrectly assume generation is compute-bound, missing that token-by-token decoding is memory-bandwidth bound.
    **The Consequence:** Engineering time is wasted writing custom INT8 kernels while throughput remains crippled by KV cache fragmentation and tiny batch sizes.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 70B (140GB BF16), sharded over 8 GPUs = 17.5GB per GPU.
       - Hardware: A100 (2 TB/s bandwidth).
       - Target TBT SLA: 50ms.
    2. **Calculations:**
       - Weight Load Time: 17.5 GB / 2 TB/s = 8.75ms per token step.
       - Theoretical Max Batch Size: 50ms / 8.75ms ≈ 5.7 requests.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bandwidth-Bound. Without continuous batching and PagedAttention to eliminate KV fragmentation, the system cannot even support a batch size of 5.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
chains:
- id: cloud-chain-248
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
