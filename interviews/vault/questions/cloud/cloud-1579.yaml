schema_version: '1.0'
id: cloud-1579
track: cloud
level: L5
zone: evaluation
topic: mixture-of-experts
competency_area: architecture
bloom_level: evaluate
phase: both
title: Evaluating MoE Serving Topologies on H100 Nodes
scenario: Your team is deploying an 8x22B Mixture of Experts (MoE) LLM (141B total parameters, 39B active per token) on a node with 8x GPUs connected via NVSwitch. Serving with standard Tensor Parallelism (TP=8) yields poor throughput due to massive communication bottlenecks during expert routing.
question: Which parallelization topology should you use for the 8x22B MoE on 8x GPUs, and why is it better than pure TP or pipeline parallelism?
details:
  realistic_solution: Implement a hybrid strategy using Tensor Parallelism for the dense attention layers and Expert Parallelism (EP=8) for the MoE layers. By placing one expert on each GPU, you isolate the massive parameters while utilizing the NVSwitch efficiently for All-to-All token routing.
  common_mistake: |
    **The Pitfall:** Treating the MoE model exactly like a dense model by defaulting to TP=8 across all layers.
    **The Rationale:** TP=8 is the standard, easy-to-deploy strategy for large dense models.
    **The Consequence:** Ignoring the severe All-to-All communication overhead introduced by token routing, leading to heavily bottlenecked throughput.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 141B params FP16 = 282GB.
       - Node VRAM = 8 * 80GB = 640GB.
       - NVSwitch = 900 GB/s.
    2. **Calculations:**
       - Transfer size (batch 256, seq 2048, hidden 4096): 256 * 2048 * 4096 * 2 bytes ≈ 4.3GB.
       - Transfer time over NVSwitch < 5ms per layer.
    3. **Conclusion & Interpretation:**
       - **Result: The transfer time is trivial compared to the compute gains of isolating experts via EP.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
chains:
- id: cloud-chain-259
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T23:39:32.034390'
