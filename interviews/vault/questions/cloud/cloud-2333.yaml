schema_version: '1.0'
id: cloud-2333
track: cloud
level: L6+
zone: diagnosis
topic: neural-architecture-search
competency_area: architecture
bloom_level: analyze
phase: training
title: Systolic Array Tile Padding Collapse
scenario: 'You are deploying a BERT-style model on a custom cloud NPU featuring a massive 256x256 systolic array. At batch size 256, the NPU achieves 15,000 QPS. When the serving layer dynamically adjusts the batch size to 257 to handle a traffic spike, throughput inexplicably plummets to 3,500 QPS.'
question: 'Why does throughput collapse when the batch size changes from 256 to 257 on the 256x256 NPU, and how would you fix it?'
details:
  realistic_solution: 'The symptom is a catastrophic performance cliff caused by a mismatch between the algorithmic tensor dimensions and the hardware systolic array tile size. At batch size 257, the hardware must allocate a second tile, which processes 1 valid row and 255 padded rows. The fix is to enforce strict sequence/batch padding at the API level.'
  common_mistake: |
    **The Pitfall:** Assuming the throughput drop is caused by exceeding total SRAM capacity and spilling to off-chip DRAM.
    **The Rationale:** Candidates look for memory capacity cliffs, ignoring the rigid geometric constraints of fixed-size hardware systolic arrays.
    **The Consequence:** Engineers might attempt to quantize the model to save memory, which fails to fix the >4x slowdown caused by dimension padding.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Hardware: 256x256 systolic array.
       - Tile size: 256x256 blocks.
    2. **Calculations:**
       - Batch 256: 1 tile needed. Cycles = 1C. 100% utilization.
       - Batch 257: 2 tiles needed. Cycles = 2C. Compute time doubles.
       - Padding Overhead in 2nd tile: 255 padded rows * 256 = 65,280 zero-elements.
       - Effective Utilization = 257 / 512 = 50.1%.
    3. **Conclusion & Interpretation:**
       - **Result: Compute-Bound. Pushing mostly-empty padded tiles through a deep systolic array stalls the pipeline and destroys QPS.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 18
validated: true
math_verified: true
