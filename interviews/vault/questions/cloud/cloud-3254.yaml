schema_version: '1.0'
id: cloud-3254
track: cloud
level: L3
zone: analyze
topic: systolic-dataflow
competency_area: compute
bloom_level: analyze
phase: both
title: Optimizing Dense GEMM on AMD MI300X with Weight-Stationary Dataflow
scenario: You are tasked with optimizing a dense matrix multiplication (GEMM) `C = A * B` on the AMD MI300X (1,307 TFLOPS FP16 sparse peak, 5.3 TB/s HBM3). Matrix `A` is 65536x512 (FP16) and `B` is 512x65536 (FP16). Your team is exploring a weight-stationary dataflow.
question: How would a weight-stationary dataflow work for the 65536x512 by 512x65536 FP16 GEMM on MI300X, and is it compute-bound or memory-bound?
details:
  realistic_solution: For a weight-stationary dataflow, the weight matrix is loaded into on-chip memory (e.g., systolic array buffers) and reused across computations. Input activations stream in, and partial sums accumulate on-chip before being written out. Tiling minimizes costly HBM accesses.
  common_mistake: |
    **The Pitfall:** Overlooking the specific 'sparse' designation for peak TFLOPS.
    **The Rationale:** Candidates directly apply sparse peak TFLOPS to dense operations, drastically overestimating compute capability.
    **The Consequence:** Misidentifying the bottleneck and miscalculating expected execution time.
  napkin_math: |
    - **Total FLOPs:** 2 × 65536 × 512 × 65536 ≈ 4.4 TFLOPS.
    - **Minimum Data Transfer:** A (64 MB) + B (64 MB) + C (8 GB) = 8.125 GB.
    - **Operational Intensity:** 4.4e12 FLOPs / 8.125e9 Bytes ≈ 541 FLOPs/Byte.
    - **Compute Time (Sparse Peak):** 4.4 TFLOPS / 1307 TFLOPS/s ≈ 0.0034s.
    - **Memory Time:** 8.125 GB / 5.3 TB/s ≈ 0.0015s.
    - **Result:** Since memory time (0.0015s) < compute time (0.0034s), the GEMM is strictly **compute-bound**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
