schema_version: '1.0'
id: cloud-2384
track: cloud
level: L6+
zone: evaluation
topic: federated-learning
competency_area: data
bloom_level: analyze
phase: both
title: Privacy-Preserving Health Wearable FL
scenario: 1M smart rings (128KB SRAM, 20mAh battery) collect PPG (50Hz) and Temp (1Hz). Cloud orchestrates
  Federated Learning to train a sleep stage model. Transmitting raw data via BLE (10mA) drains the tiny
  battery. Architect the distributed training system.
question: "How would you architect federated sleep-stage training for 1M smart rings without draining the 20mAh battery?"
details:
  realistic_solution: Use cross-device Federated Learning with edge feature extraction. The ring extracts
    low-dimensional features (e.g., Heart Rate, HRV, avg temp per minute) locally. During the night, while
    charging, the ring performs local SGD on a lightweight model using these features. It then syncs the
    model weight updates via BLE to the phone, which proxies to the cloud FL server.
  common_mistake: Uploading raw PPG data over BLE to a phone gateway for centralized cloud training, destroying
    battery life.
  napkin_math: 'Raw PPG: 50Hz * 2B = 100 B/s = 8.6 MB/day. BLE tx (~60 KB/s practical) takes ~140s. Energy
    = 140s * 10mA = 1400 mAs = 0.388 mAh (~2% of 20mAh battery per day, which is massive for a 7-day wearable).
    Extracted features: 4 bytes/min = 5.7 KB/day. Local training + sending 10KB weight update takes <1s
    of BLE active time = 10 mAs = 0.0027 mAh. Negligible battery impact.'
  options:
  - GPU memory is inherently isolated between CUDA processes; no additional security measures are needed
    because one process cannot access another's GPU allocations by design
  - 'Secure multi-tenant model serving: (1) Network isolation — separate VPCs/namespaces per tenant, encrypted
    gRPC/TLS for all model API traffic. (2) Memory isolation — MIG partitions prevent cross-tenant GPU
    memory access; without MIG, use process-level isolation with CUDA MPS memory limits. (3) Input/output
    sanitization — validate inputs to prevent model extraction attacks (repeated identical queries). (4)
    Audit logging — log all inference requests per tenant for compliance. (5) Rate limiting — per-tenant
    QPS caps to prevent resource starvation.'
  - Model serving security is identical to web application security; standard WAF (Web Application Firewall)
    and HTTPS terminate all security threats without GPU-specific measures
  - Multi-tenant model serving is inherently insecure because all tenants share the same model weights;
    deploy separate model instances per tenant to achieve any meaningful security
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 15
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: WARN
validation_issues_pro:
- The napkin math for BLE energy cost is inconsistent; a 140s transfer at 10mA would consume ~0.39 mAh,
  not 1.4 mAh.
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- healthcare
- federated-learning
- ble
