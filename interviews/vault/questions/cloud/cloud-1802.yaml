schema_version: '1.0'
id: cloud-1802
track: cloud
level: L4
zone: diagnosis
topic: collective-communication
competency_area: networking
bloom_level: analyze
phase: both
title: Diagnosing Flat Ring AllReduce Bottlenecks
scenario: You are profiling a distributed training job for a 10B parameter (40GB gradient) model across
  8 nodes (64 GPUs total, 8x A100 per node). Intra-node bandwidth is 600 GB/s (NVLink), and inter-node
  bandwidth is 50 GB/s (400 Gbps Infiniband). You observe that the gradient synchronization step takes
  ~1.58 seconds, which is 5x longer than your target. Profiling shows a flat Ring AllReduce algorithm
  is being used across all 64 GPUs. Diagnose the root cause of this poor performance.
details:
  realistic_solution: 'The flat ring forces every chunk transfer to operate at the 50 GB/s Infiniband
    limit. The solution is to switch to a Hierarchical AllReduce topology: perform a local Reduce-Scatter
    via NVLink, an inter-node AllReduce via IB on the reduced chunks, and a local All-Gather via NVLink.'
  common_mistake: Assuming the bottleneck is purely total data volume divided by inter-node bandwidth,
    missing that a flat ring topology throttles the fast intra-node NVLink connections to the speed of
    the slowest inter-node link for all 63 ring steps.
  napkin_math: 'Flat Ring: 2 * (64-1)/64 * 40GB / 50GB/s = 1.575s. Hierarchical: Intra-node Reduce-Scatter
    (35GB / 600GB/s = 0.058s) + Inter-node AllReduce (2 * 7/8 * 5GB / 50GB/s = 0.175s) + Intra-node All-Gather
    (35GB / 600GB/s = 0.058s). Total time = 0.291s (a ~5.4x speedup).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: cloud-chain-339
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:46:04.886081'
