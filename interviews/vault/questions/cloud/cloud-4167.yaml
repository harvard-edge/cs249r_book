schema_version: '1.0'
id: cloud-4167
track: cloud
level: L3
zone: analyze
topic: interconnect-topology
competency_area: networking
bloom_level: analyze
phase: both
title: Fat-Tree Bisection Bandwidth for AllReduce
scenario: A 256-GPU H100 cluster uses a 3-tier fat-tree topology with 400 Gbps InfiniBand
  links. Calculate the bisection bandwidth and explain why a 2:1 oversubscribed fat-tree
  degrades AllReduce performance more than it degrades point-to-point communication.
question: What is the ring AllReduce time for a 1 GB gradient on 256 GPUs over 400
  Gbps links?
details:
  realistic_solution: 'Ring AllReduce: 2 × 255/256 × 1GB / 50GB/s = 39.8ms (not 10.2ms
    — 10.2ms would require 4× higher bandwidth or 4× smaller gradient).'
  common_mistake: Assuming oversubscription uniformly degrades all traffic patterns.
    AllReduce is an all-to-all communication pattern that saturates every link in
    the network simultaneously, making it uniquely sensitive to bisection bandwidth
    reductions.
  napkin_math: 'Ring AllReduce on 256 GPUs, 1 GB gradient: time = 2 × (N-1)/N × D
    / BW = 2 × 255/256 × 1GB / 50GB/s = 2 × 0.996 × 0.02s = 2 × 255/256 / 50 = 510/12800
    = 0.03984s = 39.8ms (not 10.2ms — 10.2ms would require BW=195 GB/s or D=255MB).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Arithmetic error: 2 * 1 * (255/256) / 50 equals 0.0398 seconds (39.8ms), not 10.2ms.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
