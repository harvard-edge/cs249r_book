schema_version: '1.0'
id: cloud-3687
track: cloud
level: L4
zone: fluency
topic: scheduling-resource-management
competency_area: parallelism
bloom_level: understand
phase: inference
title: Gang Scheduling for Distributed Training Efficiency
scenario: Your team is running 30 concurrent 8-GPU training jobs on a 200-GPU MI300X cluster. The Kubernetes scheduler is launching jobs as GPUs become available, but jobs start with 4-6 GPUs and block waiting for the remaining GPUs. GPU utilization sits at 32% even though 190 of 200 GPUs are allocated.
question: Why is all-or-nothing gang scheduling necessary for the 30 concurrent 8-GPU jobs, and what scheduler behavior would fix utilization?
details:
  realistic_solution: 'Implement gang scheduling with all-or-nothing allocation: a job requesting 8 GPUs either receives all 8 simultaneously or waits in a queue. Use a backfill scheduler that allows smaller jobs to run during wait periods. Tools: Volcano scheduler for Kubernetes, Slurm with --ntasks-per-node and --exclusive, or YARN capacity scheduler with GPU gang support.'
  common_mistake: |
    **The Pitfall:** Using a default scheduler that treats GPUs as independent resources and allocates them greedily.
    **The Rationale:** Candidates incorrectly assume distributed jobs can start computing as soon as partial resources are granted.
    **The Consequence:** Partial allocations starve jobs while holding resources, creating massive cluster-wide deadlocks and 0% true utilization on allocated nodes.
  napkin_math: |
    1. 30 jobs × 8 GPUs = 240 GPUs requested.
    2. 8 jobs complete gang and run (64 GPUs working, 32% utilization).
    3. 22 jobs are in a partial state holding 126 allocated GPUs (~5.7 avg each).
    4. **126/200 = 63% of the cluster is allocated but completely idle; gang scheduling prevents this by ensuring atomic allocation.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
