schema_version: '1.0'
id: cloud-0255
track: cloud
level: L2
zone: implement
topic: roofline-analysis
competency_area: compute
bloom_level: understand
phase: both
title: The H100's Roofline Ridge Point
scenario: An NVIDIA H100 GPU has a peak FP16 tensor core performance of 989 TFLOPS
  and a memory bandwidth of 3.35 TB/s. Interpret these two numbers to calculate the
  'ridge point' of the H100's roofline model. What does this value represent for a
  kernel running on the GPU?
question: What is the H100 ridge point from 989 TFLOPS and 3.35 TB/s, and what does
  it mean for kernels?
details:
  realistic_solution: 'The ridge point is the value of Arithmetic Intensity (AI) at
    which a workload transitions from being memory-bound to compute-bound. It is the
    intersection of the memory bandwidth roof and the compute performance ceiling
    in a roofline plot. It''s calculated by dividing the peak compute performance
    by the peak memory bandwidth. For the H100: Ridge Point = 989 TFLOPS / 3.35 TB/s
    = 295.2 FLOPs/Byte. This means any kernel with an arithmetic intensity below ~295
    FLOPs/Byte is memory-bound on the H100 — its performance is limited by how fast
    data can be fetched from HBM, not by compute capacity. Kernels above this threshold
    are compute-bound and can theoretically achieve peak FLOPS. In practice, this
    high ridge point means that many common ML operations (element-wise ops, batch
    normalization, small matrix multiplies, autoregressive decoding) are memory-bound
    on the H100, despite its enormous compute capability.'
  common_mistake: A frequent error is unit mismatch. Engineers often divide the numbers
    directly (989 / 3.35) without ensuring the units are consistent (e.g., both in
    Tera- or both in Giga-). Since TFLOPS and TB/s both use the same 10^12 prefix,
    the prefixes cancel out, but this is often a point of confusion.
  napkin_math: 1. **Identify Peak Compute:** | `Peak Compute = 989 TFLOPS = 989 *
    10^12 FLOPs/s`. 2. **Identify Peak Memory Bandwidth:** | `Peak BW = 3.35 TB/s
    = 3.35 * 10^12 Bytes/s`. 3. **Calculate Ridge Point:** | `Ridge Point = Peak Compute
    / Peak BW = 989 * 10^12 / 3.35 * 10^12 = **295.2 FLOPs/Byte**`. 4. **Interpretation:**
    A kernel must perform at least 295 floating-point operations for every byte it
    moves from HBM to be compute-bound. Below this, it is memory-bound. 5. **Examples:**
    Autoregressive LLM decode (AI ≈ 1-2 FLOPs/Byte) → deeply memory-bound. Large GEMM
    in prefill (AI > 1000 FLOPs/Byte) → compute-bound. LayerNorm (AI ≈ 5 FLOPs/Byte)
    → memory-bound.
  options:
  - 0.0034 FLOPs/Byte
  - 295.2 FLOPs/Byte
  - 295,200 FLOPs/Byte
  - 3.32 Ops/Byte
  correct_index: 1
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: ERROR
validation_issues_pro:
- The 'Napkin Math' section is incomplete and does not show the final calculation.
- The reasoning mode 'concept-recall' is less accurate than 'napkin-math' for a question
  requiring a calculation.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
