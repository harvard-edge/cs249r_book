schema_version: '1.0'
id: cloud-3008
track: cloud
level: L5
zone: realization
topic: kv-cache-management
competency_area: memory
bloom_level: apply
phase: inference
title: 'KV-Cache: Realize Cross-Request KV Cache Sharing for RAG Applications'
scenario: Realize a KV cache sharing architecture for a RAG (Retrieval-Augmented Generation)
  application on H100s (80GB HBM3) where 1000 documents (avg 512 tokens each) are
  frequently retrieved and included in prompts. 60% of requests use one of the top-50
  most popular documents. Design the shared document KV cache.
question: What KV-cache memory per token should the shared RAG document cache budget
  for the 32-layer GQA-8 model?
details:
  realistic_solution: 'KV cache per token for GQA-8 model with 32 layers: 2 × 32 ×
    8 × 128 × 2B = 131KB per token.'
  common_mistake: Not accounting that shared KV cache pages must be read-only (copy-on-write)
    to prevent one request modifying another's cache; forgetting that attention is
    autoregressive and document KV depends on position in the prompt.
  napkin_math: 'KV per token: 32 layers × 2 (K and V) × 8 KV heads × 128 head_dim
    × 2B = 131KB (not 32KB).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Math error: 32 × 2 × 8 × 128 × 2B = 131KB, not 32KB.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
