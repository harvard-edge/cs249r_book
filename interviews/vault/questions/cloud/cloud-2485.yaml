schema_version: '1.0'
id: cloud-2485
track: cloud
level: L5
zone: evaluation
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: both
title: Depthwise Quantization Catastrophe
scenario: 'Your cloud pipeline automatically quantizes FP32 object detection models (MobileNetV2 backbone) to INT8 using Post-Training Quantization (PTQ) for edge deployment. The model size shrinks from 20MB to 5MB, but the evaluation pipeline shows mAP dropping severely from 42 to 18.'
question: Why does PTQ drop MobileNetV2 mAP from 42 to 18, and how would you preserve INT8 accuracy on the edge?
details:
  realistic_solution: 'Identify that depthwise separable convolutions (the core of MobileNet) have extreme variance in weight ranges across different channels. Applying standard per-tensor PTQ crushes the dynamic range of outlier channels. The co-design solution requires modifying the compiler/quantizer to use per-channel quantization for depthwise layers, or modifying the training algorithm to use Quantization-Aware Training (QAT) with fake-quantization nodes. Tradeoff: QAT requires more cloud compute and pipeline complexity, but preserves INT8 performance on the edge NPU.'
  common_mistake: |
    **The Pitfall:** Assuming PTQ is fundamentally broken and switching entirely to FP16.
    **The Rationale:** Depthwise separable convolutions have extreme variance in weight ranges across channels, making per-tensor PTQ crush dynamic range.
    **The Consequence:** Switching to FP16 cuts the edge NPU throughput in half and doubles memory bandwidth requirements, failing to realize INT8 benefits.
  napkin_math: |
    1. **Per-channel overhead**: Per-channel quantization adds 1 scale and zero-point (8 bytes total) per channel.
    2. **Layer overhead**: For a depthwise layer with 512 channels, overhead is 512 * 8 = 4KB.
    3. **Total overhead**: Across 20 layers, overhead is 20 * 4KB = 80KB.
    4. **Final Result**: **80KB, which is negligible compared to the 15MB saved by moving from FP32 to INT8, while recovering mAP.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
