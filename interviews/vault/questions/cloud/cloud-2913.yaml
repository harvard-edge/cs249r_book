schema_version: '1.0'
id: cloud-2913
track: cloud
level: L4
zone: design
topic: cnn-efficient-design
competency_area: architecture
bloom_level: evaluate
phase: both
title: Optimize MobileNetV3 width multiplier selection for a fixed A100 latency budget
scenario: You have a 2ms latency budget per batch of 128 images for a product search feature. MobileNetV3-Large meets the budget at 1.4ms but you want to close an accuracy gap to EfficientNet-B0 without exceeding 2ms.
question: What MobileNetV3-Large width multiplier best improves accuracy while keeping batch-128 latency under the 2ms budget?
details:
  realistic_solution: 'Width multiplier α scales FLOPs as α² and params as α². Current: 0.22G FLOPs at α=1.0. Target latency headroom: 2.0 - 1.4 = 0.6ms → can afford 0.6/1.4 = 43% more FLOPs → α² ≤ 1.43 → α ≤ 1.2. MobileNetV3-Large at α=1.2 (sometimes denoted 1.25): ~0.22 × 1.44 = 0.32 GFLOPs, ~76.6% top-1 (published results). Check 1.4ms × 1.44 = 2.016ms — effectively on the budget boundary. For α=1.4: 0.22 × 1.96 = 0.43 GFLOPs, ~1.4×1.96ms = 2.74ms — exceeds budget.'
  common_mistake: |
    **The Pitfall:** Assuming FLOPs scale linearly with the width multiplier (α).
    **The Rationale:** Candidates forget that scaling the width (channels) affects both the input and output dimensions of convolutions, causing FLOPs to scale quadratically (α²).
    **The Consequence:** Choosing a multiplier that drastically overshoots the latency budget.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Current Latency (α=1.0): 1.4ms. Budget: 2.0ms.
       - FLOP scaling: Scales quadratically with α (α²).
    2. **Calculations:**
       - Allowed FLOP Increase: 2.0ms / 1.4ms ≈ 1.428 (can afford ~42.8% more FLOPs).
       - Width Scaling Constraint: α² ≤ 1.428.
       - Multiplier Calculation: α ≤ √1.428 ≈ 1.195.
    3. **Conclusion & Interpretation:** **Use a width multiplier of α=1.1 to strictly meet the budget, though α=1.2 (2.016ms) is practically on the boundary. Result: Compute-Bound.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
