schema_version: '1.0'
id: cloud-2389
track: cloud
level: L5
zone: evaluation
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: analyze
phase: training
title: Mitigating Cold Start Cascading Failures
scenario: A 30B parameter LLM service experiences a sudden traffic spike from 500 to 3,000 QPS. The Kubernetes HPA instantly scales pods from 50 to 300. However, each pod must download a 60GB weight file from S3 on startup. During this time, existing pods are overwhelmed, requests time out, and new pods are failing their readiness probes and crash-looping.
question: "How would you redesign autoscaling so 60GB model weights don't make new pods crash-loop during the 3,000 QPS spike?"
details:
  realistic_solution: 'Design a multi-tier weight caching and pre-fetching architecture. 1) Implement a Kubernetes DaemonSet to cache the 60GB weights on the underlying Node''s local NVMe storage using a hostPath volume. 2) When pods scale up, they mount the hostPath, reducing weight load time to local disk read speeds. 3) Decouple the health checks: Use a startup probe to allow weight loading without failing liveness, and a readiness probe that only passes once the model is in VRAM. 4) Use request queueing (circuit breaking) at the gateway to fast-fail excess traffic rather than overwhelming the baseline pods.'
  common_mistake: |
    **The Pitfall:** Increasing the readiness probe initial delay to 10 minutes to stop the crashloop.
    **The Rationale:** Trying to give the pods enough time to download the 60GB weights over a saturated network.
    **The Consequence:** The crashloop stops, but the new pods are useless for 10 minutes, leaving the existing 50 pods to take down the service under the traffic spike.
  napkin_math: |
    * **S3 Node Bandwidth Limit:** 5 Gbps = 625 MB/s.
    * **Ideal Download Time:** 60 GB / 625 MB/s = 96 seconds.
    * **Network Contention:** 250 pods downloading simultaneously drops throughput to 50 MB/s per pod.
    * **Contended Download Time:** 60 GB / 50 MB/s = 1200 seconds (20 mins).
    * **NVMe Read Speed (Local):** 3,000 MB/s.
    * **Local Load Time:** 60 GB / 3000 MB/s = 20 seconds.
    * **Conclusion:** **Caching weights on local NVMe reduces startup time by 60x and bypasses the network bottleneck.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: UNCHECKED
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
