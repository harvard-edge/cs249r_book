schema_version: '1.0'
id: cloud-3371
track: cloud
level: L4
zone: realization
topic: encoder-decoder-tradeoffs
competency_area: architecture
bloom_level: analyze
phase: both
title: A100 Inference Tradeoffs for Encoder-Only, Decoder-Only, and Encoder-Decoder Models
scenario: Your team is tasked with deploying a large language model for real-time inference on a fleet of NVIDIA A100 GPUs. You need to decide between an encoder-only, decoder-only, or a full encoder-decoder architecture. The primary constraints are minimizing inference latency and maximizing throughput, while also being highly cost-efficient at scale.
question: How would you analyze the system costs and architectural tradeoffs, specifically leveraging the A100's capabilities, to make a recommendation for a low-latency, high-throughput service?
details:
  realistic_solution: A candidate should propose a methodical evaluation. First, define key metrics (e.g., latency percentiles, throughput/GPU, total cost of ownership, cost-per-inference). Then, analyze memory requirements for each architecture type, considering model size (parameters * bytes/param) against the 80 GB HBM2e. Next, estimate FLOPs for inference for each architecture, mapping against the A100's 312 TFLOPS FP16 to predict theoretical compute-bound throughput. Crucially, factor in memory bandwidth (2.0 TB/s) for data transfer-bound operations, especially for token generation in decoder-only models where the KV cache can dominate memory access patterns. Discuss how strategies like quantization (e.g., FP8, INT8) could further exploit A100 capabilities by reducing memory footprint and potentially increasing effective throughput. Finally, present a cost-per-inference projection based on resource utilization, power consumption (400W TDP), and model performance trade-offs, justifying
    the choice of architecture for the given constraints.
  common_mistake: |
    **The Pitfall:** Overlooking the critical impact of memory bandwidth (2.0 TB/s) on effective sequence length.
    **The Rationale:** Candidates focus solely on total parameter count and raw TFLOPS, ignoring how the KV cache dominates memory access patterns in decoder-only models.
    **The Consequence:** Deploying an architecture that suffers severe latency spikes as sequence lengths grow, destroying the cost-efficiency at scale.
  napkin_math: |
    **Assumptions & Constraints:**
    - Target Hardware: A100 (80GB HBM2e, 312 TFLOPS FP16, 2.0 TB/s bandwidth).
    - Candidate decoder-only model: 70B parameters.
    - INT8 serving is considered for memory fit; batch-1 decode is the latency-sensitive case.
    - Example KV shape: 80 layers, 64 KV heads, d_head=128.

    **Calculations:**
    - FP16 weights = 70B * 2 bytes = 140 GB, requiring tensor parallelism across at least 2 A100s.
    - INT8 weights = 70B * 1 byte = 70 GB, leaving about 10 GB for KV cache, activations, and runtime buffers on one 80GB A100.
    - Decode compute per output token is roughly 2 * 70B = 140 GFLOPs; at 30% of 312 TFLOPS sustained, compute time ≈ 140 / 93,600 = 1.5 ms.
    - Batch-1 INT8 weight-streaming lower bound is about 70 GB / 2000 GB/s = 35 ms per token before KV-cache reads and kernel overhead.
    - KV cache per generated token in FP16 = 2 * 80 * 64 * 128 * 2 bytes ≈ 2.5 MB, so long contexts and batching quickly consume the remaining HBM.

    **Conclusion & Interpretation:**
    - **Result: latency is memory-traffic dominated for decoder-only batch-1 serving.** Encoder-only models can batch input work more efficiently, decoder-only models need careful KV and weight-bandwidth budgeting, and encoder-decoder models pay both encoder pass and autoregressive decode costs.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
