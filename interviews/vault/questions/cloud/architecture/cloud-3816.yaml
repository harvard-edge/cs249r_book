schema_version: '1.0'
id: cloud-3816
track: cloud
level: L6+
zone: mastery
topic: mixture-of-experts
competency_area: architecture
bloom_level: evaluate
phase: both
title: 'MoE Gradient Computation: The Router Gradient Challenge'
scenario: 'In MoE with top-k routing, the argmax (top-k selection) is non-differentiable. The standard solution uses the straight-through estimator: forward pass uses hard top-k, backward pass pretends the top-k selection was soft.'
question: "What gradient bias does the straight-through top-k estimator introduce, and what alternative routing-gradient approaches exist?"
details:
  realistic_solution: 'The bias means the gradient for non-selected experts receives full feedback despite having zero forward contribution. Alternatives include Gumbel-Softmax (accurate but numerically unstable), REINFORCE policy gradients (unbiased but high variance), Hash routing (deterministic but no learning), or Expert choice routing.'
  common_mistake: |
    **The Pitfall:** Treating MoE top-k routing as perfectly differentiable.
    **The Rationale:** The straight-through estimator hides the non-differentiable `argmax` operation under the hood.
    **The Consequence:** Ignoring the significant gradient bias introduced for non-selected experts during training.
  napkin_math: |
    **Assumptions & Constraints:**
    - Router linear layer parameters: 8192 x 8 = 65K.
    - Top-2/8 routing.

    **Calculations:**
    - Gradient bias magnitude ≈ (1 - top_k/E) × avg_gradient_norm.
    - Bias ≈ (1 - 2/8) = 75%.

    **Conclusion & Interpretation:**
    - **Result: 75% counterfactual gradient bias (Algorithmic)**. The network receives noisy feedback for experts it never actually evaluated.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
