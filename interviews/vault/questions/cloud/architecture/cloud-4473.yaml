schema_version: '1.0'
id: cloud-4473
track: cloud
level: L4
zone: optimization
topic: recommendation-systems-engineering
competency_area: architecture
bloom_level: apply
phase: inference
title: Embedding Gradient Sparsity and Optimizer Choice
scenario: 'A DLRM training system uses the Adam optimizer for all parameters including embedding tables. Training throughput is 25% slower than a comparable system using SGD.'
question: 'Why is Adam slowing DLRM training on embedding tables, and which optimizer would you use for embeddings versus dense layers?'
details:
  realistic_solution: 'Dense Adam wastes memory bandwidth updating unaccessed embedding rows. Use SparseAdam or Adagrad for sparse embeddings (updating only active rows) and dense Adam for MLP layers. PyTorch torch.optim.SparseAdam handles this efficiently.'
  common_mistake: |
    **The Pitfall:** Using Adam uniformly for all parameters, including sparse embedding tables.
    **The Rationale:** Adam is the default "best" optimizer in deep learning, and frameworks make it easy to apply to the entire model.
    **The Consequence:** Adam maintains two moment estimates (m, v) per parameter. For embedding tables where 99% of rows have zero gradients per batch, Adam stores 3x the memory and performs unnecessary memory-bandwidth-heavy updates on unused rows.
  napkin_math: |
    **Assumptions & Constraints:**
    - Embedding Table: 10M row * 64 dim.
    - Precision: FP32.
    - Sparsity: Batch of 4K samples touches max 40K unique rows.

    **Calculations:**
    - Adam Memory Overhead: 640M params = 2.56GB. Adam moment estimates add 5.12GB.
    - Sparsity Rate: 40K / 10M = 0.4%.
    - Sparse Speedup: Updating only the 0.4% active rows drops optimizer time by ~75x.

    **Conclusion & Interpretation:**
    - **Result: Memory-Bound**. Transitioning to SparseAdam cuts out 99.6% of unnecessary memory-bandwidth updates, restoring throughput.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
