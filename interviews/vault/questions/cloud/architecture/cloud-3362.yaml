schema_version: '1.0'
id: cloud-3362
track: cloud
level: L4
zone: optimization
topic: mixture-of-experts
competency_area: architecture
bloom_level: analyze
phase: both
title: Optimizing MoE Routing on NVIDIA A100 for Extreme Scale
scenario: Your team is deploying a massive Mixture of Experts (MoE) model with 128 experts, each requiring approximately 40GB of resident GPU memory for expert weights and working buffers. The model is hosted on a cluster of NVIDIA A100 GPUs. The routing mechanism directs tokens to two experts per layer. You are observing significantly lower throughput than anticipated (50 tokens/sec vs. 200 tokens/sec).
question: What bottlenecks explain the 50 vs. 200 tokens/sec MoE throughput on A100s, and how would you quantify and mitigate them?
details:
  realistic_solution: |
    Implement dynamic expert load balancing techniques (e.g., auxiliary loss for balancing, Jitter-gate, or expert capacity throttling), optimize the routing algorithm to minimize cross-node communication for selected experts, and employ techniques like expert data parallelism where experts are replicated across devices for higher availability and reduced remote access. Leverage A100's high memory bandwidth for efficient local gather/scatter operations and consider strategies to keep frequently accessed expert data closer to compute. Quantify the impact by profiling expert utilization, communication overhead, memory pressure per expert, and router decision latency.
  common_mistake: |
    **The Pitfall:** Overlooking the devastating tail latency introduced by poor load balancing and assuming uniform token distribution across all 128 experts.
    **The Rationale:** Engineers assume that average-case routing evenly spreads compute across the cluster, missing that MoE models naturally exhibit severe hotspots where popular experts are overwhelmed while others sit idle.
    **The Consequence:** The cluster achieves a fraction of its theoretical throughput, bottlenecked by a single overloaded GPU, while massive amounts of cross-node bandwidth are saturated transferring tokens to that hotspot.
  napkin_math: |
    **Assumptions & Constraints:**
    - 128 experts, each with about 40GB of resident expert weights and working buffers.
    - Hardware: A100 (80GB HBM2e), so at most 2 such experts fit per GPU before KV cache and runtime overhead.
    - Routing: 2 experts per token. Target SLA: 200 tokens/sec.

    **Calculations:**
    - Capacity floor = 128 experts / 2 experts per A100 = 64 A100s before replicas, KV cache, and overhead.
    - Uniform target load would average 200 tok/sec * 2 routes / 128 experts = 3.1 expert-token/sec per expert.
    - If 10% of experts receive 50% of routes, the hot experts see (200 * 2 * 0.5) / 13 ≈ 15.4 expert-token/sec each, about 5x the uniform load.
    - Any remote routing adds activation all-to-all traffic; measure bytes per routed token from profiles rather than assuming a fixed MB/token value.

    **Conclusion & Interpretation:**
    - **Result: Load-imbalance and communication bound.** The observed 50 tok/sec throughput is consistent with hot experts and remote dispatch, not merely insufficient GPU count. Mitigate with capacity-aware routing, auxiliary balancing loss, hot-expert replication, and locality-aware placement.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
