schema_version: '1.0'
id: cloud-4162
track: cloud
level: L3
zone: implement
topic: tail-latency
competency_area: latency
bloom_level: apply
phase: inference
title: Little's Law Under Variable Service Times
scenario: 'Your A100 inference server processes a mix of requests: 70% are short (20ms)
  and 30% are long (200ms). The arrival rate is 10 req/s. Use Little''s Law to calculate
  the average concurrency, and explain why the variance in service time makes the
  actual P99 queue depth much worse than the average suggests.'
question: What average concurrency does Little's Law predict, and why does service-time
  variance make P99 queue depth much worse?
details:
  realistic_solution: 'Mean service time W = 0.7 × 20ms + 0.3 × 200ms = 74ms. Little''s
    Law: L = λ × W = 10 × 0.074 = 0.74 concurrent requests on average. But the Pollaczek-Khinchine
    formula shows that variance matters enormously. Variance = 0.7 × (20-74)² + 0.3
    × (200-74)² = 0.7 × 2916 + 0.3 × 15876 = 2041 + 4763 = 6804. CV² = 6804/74² =
    1.24. For M/G/1 queue, mean waiting time = (ρ × W × (1 + CV²)) / (2 × (1-ρ)).
    With utilization ρ = 0.74: mean wait = (0.74 × 74 × 2.24) / (2 × 0.26) = 236ms.
    P99 wait ≈ 3× mean wait ≈ 708ms. The high variance makes P99 queueing 10× the
    mean service time.'
  common_mistake: Using just the mean service time in Little's Law and assuming the
    system behaves as if all requests were average. High variance in service time
    (coefficient of variation > 1) causes queueing delays that are much worse than
    the mean predicts.
  napkin_math: 'Mean service: 74ms. Little''s Law: L = 10 × 0.074 = 0.74 concurrent.
    Utilization: ρ = 10 × 0.074 = 0.74. CV² = 6804/5476 = 1.24. M/G/1 mean wait:
    236ms. P99 wait: ~708ms. If all requests were 74ms (CV=0): mean wait = 0.74 ×
    74 / (2 × 0.26) = 105ms. Variance penalty: 236/105 = 2.25×.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
