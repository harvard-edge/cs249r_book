schema_version: '1.0'
id: cloud-2248
track: cloud
level: L4
zone: optimization
topic: data-pipeline-engineering
competency_area: data
bloom_level: analyze
phase: training
title: The Distributed Join Strategy Selection
scenario: You need to enrich a 50 TB training dataset with metadata from three lookup tables: a 5 GB language-detection table, a 200 GB domain-quality table, and a 2 TB URL-to-category mapping. Each join is on a different key. You have a 500-node Spark cluster with 64 GB RAM per executor.
question: Which join strategy should you use for the 5 GB, 200 GB, and 2 TB lookup joins, and what is the total network I/O?
details:
  realistic_solution: 'Join 1 (50 TB × 5 GB on language key): Broadcast the 5 GB table. Each of the 500 executors receives a copy: 5 GB × 500 = 2.5 TB network I/O, but the 50 TB table stays put (zero shuffle). Total: 2.5 TB. Join 2 (50 TB × 200 GB on domain key): The 200 GB table does NOT fit in the 64 GB executor memory, so it cannot be broadcast. Use shuffle hash join or sort-merge join: shuffle both sides by domain key. Network I/O: 50 TB + 200 GB ≈ 50.2 TB. Join 3 (50 TB × 2 TB on URL key): Both tables are large. Sort-merge join: shuffle both sides by URL. Network I/O: 50 TB + 2 TB = 52 TB. Total across all 3 joins: 2.5 + 50.2 + 52 = 104.7 TB. Optimization: if you order the joins so that Join 1 (broadcast) happens first, the output is still ~50 TB. Then chain Join 2 and Join 3 — but with careful co-partitioning, you can make Join 2''s output already partitioned by URL for Join 3, saving one shuffle.'
  common_mistake: |
    **The Pitfall:** Using Sort-Merge join for everything.
    **The Rationale:** Candidates assume that because the main table is massive (50 TB), all joins must be heavily shuffled.
    **The Consequence:** The candidate generates 50 TB of completely unnecessary shuffle network I/O for a tiny 5 GB table that could have simply been broadcasted.
  napkin_math: |
    *   **Join 1 (50 TB × 5 GB):** Broadcast the 5 GB table (fits in 64 GB). Network I/O = 5 GB × 500 nodes = 2.5 TB.
    *   **Join 2 (50 TB × 200 GB):** 200 GB > 64 GB (cannot broadcast). Shuffle both. Network I/O = 50 TB + 200 GB = 50.2 TB.
    *   **Join 3 (50 TB × 2 TB):** Shuffle both. Network I/O = 50 TB + 2 TB = 52 TB.
    *   **Result:** Total network I/O = 2.5 + 50.2 + 52 = **104.7 TB**.
  options:
  - "Use Broadcast join for all three tables since Spark can spill broadcast variables to disk. Total network I/O is 2.5 TB."
  - "Use Sort-Merge join for all three tables to avoid memory errors. Total network I/O is 50 TB + 5 GB + 200 GB + 2 TB ≈ 52.2 TB."
  - "Use Broadcast join for the 5 GB table, and Sort-Merge or Shuffle Hash join for the 200 GB and 2 TB tables. Total network I/O is roughly 2.5 TB (broadcast) + 50.2 TB (shuffle 2) + 52 TB (shuffle 3) = 104.7 TB."
  - "Use Shuffle Hash join for the 5 GB and 200 GB tables, and Sort-Merge join for the 2 TB table. Total network I/O is 154 TB."
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
