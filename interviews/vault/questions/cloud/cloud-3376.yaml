schema_version: '1.0'
id: cloud-3376
track: cloud
level: L6+
zone: realization
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: Optimizing Large Language Model Deployment on AMD MI300X with Structured Sparsity
scenario: You are tasked with deploying a large language model for real-time inference on an accelerator. The model exceeds memory capacity and latency targets.
question: How would you approach structured versus unstructured pruning, and what sparsity patterns would best exploit sparse compute hardware?
details:
  realistic_solution: 'Prioritize structured pruning (e.g., 2:4 N:M sparsity) that tightly aligns with the accelerator hardware capability. Applying unstructured pruning reduces theoretical FLOPs but yields minimal speedup because irregular memory accesses defeat tensor cores. Leverage the hardware sparse support to mathematically double throughput while halving the memory footprint, fitting large models strictly within memory limits.'
  common_mistake: |
    **The Pitfall:** Applying aggressive unstructured pruning to achieve a high sparsity percentage.
    **The Rationale:** Engineers chase theoretical parameter reduction without considering how matrix multiplication hardware actually functions.
    **The Consequence:** A highly sparse model that is fundamentally slower to execute than the dense baseline due to disorganized memory access patterns and failure to utilize sparse tensor cores.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Baseline dense model: 200 GB of FP16 weights.
       - Accelerator memory limit: 192 GB.
       - Dense peak: 653 TFLOPS. Sparse peak: 1,307 TFLOPS.
       - Sparsity applied: 2:4 structured sparsity (50% sparse).
    2. **Calculations:**
       - Dense memory footprint: 200 GB (OOM).
       - Sparse memory footprint: 200 GB / 2 = 100 GB.
       - Sparse theoretical throughput: 1,307 TFLOPS.
    3. **Conclusion & Interpretation:**
       - **Result: Compute and Memory Optimized.** Assuming full utilization, theoretical throughput doubles from 653 to 1,307 TFLOPS, effectively halving latency and fitting within the 192 GB limit.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
