schema_version: '1.0'
id: cloud-1047
track: cloud
level: L3
zone: fluency
topic: roofline-analysis
competency_area: compute
bloom_level: apply
phase: both
title: Calculating Credit Assignment Compute Requirements
scenario: Your team is training a 1.5-billion parameter Transformer on an 8x NVIDIA A100 node. A researcher proposes solving the credit assignment problem by avoiding backpropagation and instead using parallel finite differences (perturbing each weight slightly and measuring the loss change) to 'perfectly isolate' each weight's contribution. A single forward pass takes 15ms on this node.
question: "How long would one optimization step take with finite differences versus standard reverse-mode autodiff for the 1.5B-parameter Transformer?"
details:
  realistic_solution: Reject the proposal. Finite differences require N+1 forward passes for N parameters, whereas reverse-mode autodiff solves the credit assignment problem for all parameters simultaneously in a single backward pass requiring roughly 2x the compute of the forward pass.
  common_mistake: |
    **The Pitfall:** Entertaining the finite difference proposal because "it distributes perfectly" across GPUs.
    **The Rationale:** Candidates fail to recognize that O(N) forward passes for billions of parameters is mathematically absurd, regardless of parallelism.
    **The Consequence:** Billions of hours of compute time would be requested for a mathematically doomed experiment.
  napkin_math: |
    **Assumptions & Constraints:**
    - Parameters: 1.5 billion.
    - Base forward pass: 15 ms.
    - Hardware: 8x GPUs (linear scaling assumed).

    **Calculations:**
    - Finite Differences: Requires N forward passes.
    - Forward passes needed: 1,500,000,000.
    - Total time: (1.5e9 passes * 0.015 s) / 8 GPUs = 2,812,500 seconds.
    - 2,812,500 seconds ≈ 32.5 days.
    - Backprop time: ~2x forward time = 30 ms. Total step = 45 ms.

    **Conclusion & Interpretation:**
    - **Result: 32.5 days/step (Compute-Bound)**. Finite difference requires 32.5 days per step, making it ~100 million times slower than backprop.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T21:35:48.101480'
