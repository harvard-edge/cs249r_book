schema_version: '1.0'
id: cloud-3226
track: cloud
level: L5
zone: design
topic: kv-cache-management
competency_area: memory
bloom_level: create
phase: inference
title: KV-Cache Compression via Sliding Window + Sink Tokens
scenario: Your chatbot application uses Mistral 7B (sliding window attention, window=4096)
  but receives multi-turn conversations of 20K+ tokens. Users report quality degradation
  after turn 5. Design a KV-cache management strategy that maintains quality for long
  conversations within the 4K window constraint.
question: How should you manage KV-cache for 20K-token conversations on Mistral 7B
  while respecting its 4096-token sliding window?
details:
  realistic_solution: 'Implement StreamingLLM-style ''attention sink + sliding window''
    strategy: (1) Attention sinks: Always keep the first 4 tokens'' KV-cache (the
    ''sink tokens'' that absorb disproportionate attention mass due to softmax normalization).
    Discarding them causes attention distribution collapse. (2) Window tokens: Keep
    the most recent 4092 tokens (4096 - 4 sink tokens). (3) Conversation memory: For
    multi-turn, implement ''important token pinning'' — use attention scores from
    previous turns to identify high-attention tokens (topic sentences, entity introductions)
    and pin their KV entries even as the window slides. Budget: 256 pinned tokens
    + 4 sink + 3836 window = 4096 total. (4) Implementation: maintain a priority queue
    of KV blocks sorted by cumulative attention received. When the window slides,
    evict the lowest-attention tokens outside the recent window. This keeps conversation-critical
    context while discarding filler. Memory: constant 4096 × 65.5 KB (for 7B model)
    = 268 MB per request regardless of conversation length.'
  common_mistake: Simply discarding KV entries beyond the window. This loses critical
    context from early conversation turns (user name, task description, constraints)
    that inform all subsequent responses.
  napkin_math: 'Standard window: 4096 tokens × 65.5 KB = 268 MB per request. Full
    20K without window: 20K × 65.5 KB = 1.31 GB (4.9× more memory). Sink + pinned
    strategy: 4 + 256 + 3836 = 4096 tokens, constant 268 MB. Quality: maintains ~95%
    of full-context quality vs ~70% with naive sliding window.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
