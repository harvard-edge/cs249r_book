schema_version: '1.0'
id: cloud-3782
track: cloud
level: L4
zone: design
topic: flash-attention
competency_area: optimization
bloom_level: evaluate
phase: inference
title: 'FlashDecoding: Parallelizing Attention During Inference'
scenario: 'During autoregressive decoding, each new token attends to all previous tokens. For a single query token with a 32K context, standard FlashAttention iterates over KV blocks sequentially within one thread block, under-utilizing the GPU SMs.'
question: 'How does FlashDecoding parallelize a single-token decode over KV blocks for a 32K context, and what throughput improvement does it provide?'
details:
  realistic_solution: 'Standard FlashAttention during decoding assigns one thread block per attention head, utilizing only a fraction of available SMs (e.g., 64 heads on 132 SMs). FlashDecoding splits the KV cache into P partitions and assigns each partition to a separate thread block, computing a partial softmax and weighted sum. A final reduction kernel combines results via the logsumexp trick. This boosts parallelism to num_heads * P blocks, fully saturating the SMs and yielding a 4-8x speedup for long contexts.'
  common_mistake: |
    **The Pitfall:** Confusing FlashDecoding with speculative decoding.
    **The Rationale:** Candidates hear "decode optimization" and assume it means predicting future tokens, rather than parallelizing the attention computation across KV cache blocks.
    **The Consequence:** Providing an incorrect architectural explanation that fails to address the SM utilization bottleneck.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 64 attention heads, 132 SMs.
       - Context: 32K.
    2. **Calculations:**
       - Standard decode: 64 thread blocks on 132 SMs = 48% utilization.
       - FlashDecoding (P=16 partitions): 64 * 16 = 1024 blocks -> 100% utilization.
    3. **Conclusion & Interpretation:**
       - **Result: FlashDecoding provides a ~5.7x speedup by fully saturating SMs (Compute-Bound).**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
