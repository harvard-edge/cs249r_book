schema_version: '1.0'
id: cloud-3230
track: cloud
level: L5
zone: optimization
topic: kv-cache-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: KV-Cache and Speculative Decoding Interaction
scenario: You use speculative decoding with a 1B draft model and a 70B verifier for serving. The draft model generates 8 candidate tokens, then the verifier checks them in a single forward pass.
question: How should speculative decoding manage draft and verifier KV-caches when 5 of 8 candidate tokens are accepted?
details:
  realistic_solution: 'KV-cache management for speculative decoding: Draft model maintains its own lightweight KV-cache. Verifier model processes all 8 speculative tokens in one forward pass, generating KV entries for all 8. If 5 are accepted and 3 rejected: the KV-cache must be truncated — remove entries for the rejected tokens via fast pointer updates.'
  common_mistake: |
    **The Pitfall:** Forgetting that rejected speculative tokens create orphaned KV-cache entries.
    **The Rationale:** It's easy to assume the verifier only caches what it approves.
    **The Consequence:** The verifier's KV-cache fills with garbage tokens from rejected drafts, causing corrupted outputs and rapid Out-Of-Memory errors. It must be managed transactionally.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Pre-allocated speculative tokens: 8.
       - Memory cost per token: 320 KB.
       - Verifier accepts 5 tokens, rejects 3.
    2. **Calculations:**
       - Memory overhead: 8 tokens * 320 KB = 2.56 MB per request.
       - Rollback cost: Freeing 3 tokens' KV (960 KB) is an O(1) pointer update in PagedAttention.
       - Throughput gain: 5 accepted tokens per 1 weight load = 5x improvement.
    3. **Conclusion & Interpretation:**
       - **Result: Compute-Bound Tradeoff. A trivial 2.56 MB VRAM overhead and instant pointer rollbacks yield a massive 5x decode throughput gain.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
