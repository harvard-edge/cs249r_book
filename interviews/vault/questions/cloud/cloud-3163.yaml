schema_version: '1.0'
id: cloud-3163
track: cloud
level: L5
zone: implement
topic: speculative-decoding
competency_area: latency
bloom_level: apply
phase: inference
title: Implementing Rejection Sampling for Lossless Speculation
scenario: In speculative decoding, the verification step must produce exactly the same distribution as the target model alone. You are given draft distribution q(x) and target distribution p(x) with a KL divergence of 0.5 nats.
question: How does rejection sampling preserve the target distribution, and what expected acceptance rate and correction cost apply at KL(p||q)=0.5 nats?
details:
  realistic_solution: 'For each speculated token x_i sampled from q(x_i): accept with probability min(1, p(x_i)/q(x_i)). If rejected: sample a correction token from the adjusted distribution: p''(x) = max(0, p(x) - q(x)) / sum(max(0, p(x) - q(x))). This guarantees the output follows exactly p(x).'
  common_mistake: |
    **The Pitfall:** Using a simple argmax comparison to accept or reject tokens instead of proper rejection sampling.
    **The Rationale:** Argmax is the simplest way to check if the draft model guessed the most likely token.
    **The Consequence:** Argmax matching is not distribution-preserving. It alters the sampling distribution, skewing the final generation quality away from the target model's true output.
  napkin_math: |
    1. Bound the Total Variation distance: By Pinsker's inequality, TV <= sqrt(KL/2) = sqrt(0.5/2) = 0.5.
    2. Calculate acceptance lower bound: Alpha >= 1 - TV = 1 - 0.5 = 0.5.
    3. Calculate correction sampling memory overhead: 128K vocab * 4 bytes/float = 512KB per position.
    4. Conclude: The 512KB correction sampling computes in **~0.1ms on modern GPUs**, which is negligible compared to a ~25ms decode step.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
