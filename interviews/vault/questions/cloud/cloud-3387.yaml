schema_version: '1.0'
id: cloud-3387
track: cloud
level: L5
zone: design
topic: kernel-fusion
competency_area: optimization
bloom_level: analyze
phase: inference
title: Optimizing Transformer Inference on Google TPU v5e via Kernel Fusion
scenario: Your team is deploying a large transformer model for real-time inference on Google TPU v5e. Initial profiling reveals that despite the TPU's significant compute capabilities (197 TFLOPS BF16), the inference pipeline is bottlenecked by a sequence of memory-bound element-wise operations (e.g., GELU, LayerNorm, Add) and frequent, small kernel launches.
question: How would you use operator fusion on TPU v5e to cut memory-bound element-wise latency, and how would you identify, implement, and validate it?
details:
  realistic_solution: 'Utilize XLA Custom Calls to explicitly fuse the sequence of memory-bound element-wise operations (LayerNorm -> GELU -> Add). This forces intermediates to reside in on-chip SRAM rather than bouncing through HBM.'
  common_mistake: |
    **The Pitfall:** Focusing solely on optimizing individual operations or relying purely on automatic compiler fusions.
    **The Rationale:** Candidates neglect to analyze the specific data movement patterns and XLA compilation limits for memory-bound workloads.
    **The Consequence:** They fail to implement custom XLA fusions, leaving intermediate results passing through HBM and missing the latency target.
  napkin_math: |
    **Assumptions & Constraints:**
    - TPU v5e HBM bandwidth: 1.6 TB/s. Tensor size: N = 10^7 elements (BF16, 2 bytes = 20 MB).
    - Sequence of 3 memory-bound ops (e.g., LayerNorm -> GELU -> Add).

    **Calculations:**
    - Without fusion: Each op reads N, writes N. Total HBM traffic = 3 ops * 2 * 20 MB = 120 MB.
    - Data movement time (unfused): 120 MB / 1.6 TB/s = ~75us.
    - With custom XLA fusion: Intermediates stay on-chip. 1 read, 1 write. Total HBM traffic = 40 MB.
    - Data movement time (fused): 40 MB / 1.6 TB/s = ~25us.

    **Conclusion & Interpretation:**
    - **Result: 50us saved per sequence or 3x reduction (Memory-Bandwidth-Bound)**. Operator fusion alleviates the element-wise operations.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
