schema_version: '1.0'
id: cloud-3050
track: cloud
level: L5
zone: specification
topic: network-bandwidth-bottlenecks
competency_area: networking
bloom_level: create
phase: both
title: 'Network Bandwidth Bottlenecks: Specify InfiniBand Fabric for 256-GPU LLM Training
  Cluster'
scenario: 'Specify the InfiniBand fabric topology for a 256-GPU H100 training cluster
  (32 DGX nodes × 8 H100s) optimized for training 70B+ models with 3D parallelism.
  Specify: IB switch tier, rail count, bandwidth per GPU, target AllReduce time for
  70B model, and maximum cluster bisection bandwidth.'
question: What InfiniBand topology, rail count, per-GPU bandwidth, AllReduce target,
  and bisection bandwidth would you use for a 256-GPU H100 70B+ training cluster?
details:
  realistic_solution: '256 GPUs across 32 nodes. Each node: 8 H100 × 400Gb/s (ConnectX-7)
    = 3.2Tb/s intra-node NVLink + 8 × 400Gb/s IB per node = 3.2Tb/s IB uplinks per
    node. 32 nodes × 8 IB ports = 256 ports. Non-blocking fat-tree: 2-tier with 512-port
    Quantum-2 switches or HDR 36-port leafs. Recommended: 2-tier fat-tree with 256-port
    leaf switches (1:1 oversubscription). Bandwidth per GPU: 400Gb/s = 50GB/s. AllReduce
    70B across 32 DP ranks: 2×31/32×140B/32 = 8.45GB per GPU. 8.45/50=169ms. Bisection
    BW: 256×400Gb/s/2=51.2Tb/s.'
  common_mistake: Specifying a single-tier fat-tree when 256 ports per switch isn't
    enough; 256 GPUs with 32 NICs per node = 32 uplinks, requiring a 2-tier or 3-tier
    fat-tree for non-blocking.
  napkin_math: 'Per GPU IB: 400Gb/s=50GB/s. 32 DP nodes: AllReduce data per GPU (70B
    FP16, DP-32, TP-8): per GPU gradient = 17.5GB. Ring: 2×31/32×17.5=33.9GB moved.
    IB 50GB/s: 33.9/50=678ms. Still slow! With ZeRO-3 and 50% overlap: ~340ms overhead
    on 500ms step. GPU util: 500/(500+340)=59.5%. Needs 200Gb/s × 4 rails or 800Gb/s
    IB to achieve >85% util.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
