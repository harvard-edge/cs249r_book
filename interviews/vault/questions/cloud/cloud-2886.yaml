schema_version: '1.0'
id: cloud-2886
track: cloud
level: L5
zone: optimization
topic: activation-memory
competency_area: memory
bloom_level: evaluate
phase: training
title: Optimizing Activation Memory for Pipeline-Parallel Training Across A100 Nodes
scenario: You are running 1F1B (one-forward-one-backward) pipeline-parallel training of a 175B model across 8 A100 nodes (8 GPUs each, 64 GPUs total) with pipeline degree 64. With micro-batch size 4 and gradient accumulation steps 128, you are observing peak activation memory of 62 GB per GPU on the first pipeline stage. The last pipeline stage uses 14 GB of total memory.
question: Why does the first 1F1B pipeline stage use 62 GB of activations while the last stage uses so much less memory, and how would you reduce the imbalance?
details:
  realistic_solution: 'In 1F1B, the first stage must keep pipeline_depth (P=64) micro-batches'' worth of activations live simultaneously in the worst case, while stage k keeps (P - k) micro-batches. First stage: 64 \times (activation per micro-batch). Last stage: 1 \times (activation per micro-batch). To reduce first-stage peak: (1) reduce micro-batch size from 4 to 2 (halves per-micro-batch activation), (2) increase Tensor Parallelism degree to reduce pipeline depth P (e.g., TP=2 halves P to 32, which also halves per-GPU activation, yielding a 4x reduction in activation memory), or (3) apply gradient checkpointing only to the first N pipeline stages.'
  common_mistake: |
    **The Pitfall:** Treating pipeline stages as memory-symmetric.
    **The Rationale:** Candidates assume each GPU holds the same amount of activation memory, forgetting that in 1F1B scheduling, the first stage holds in-flight activations for all pipeline micro-batches currently in the pipeline simultaneously.
    **The Consequence:** The first stage runs out of memory (OOM) while the last stage remains severely underutilized, leading to inefficient scaling and crashes.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Stage 0 peak activation memory: 62 GB.
       - Pipeline depth (in-flight microbatches): 64.
       - Stage 63 memory: 14 GB.
    2. **Calculations:**
       - Per micro-batch activation on stage 0: 62 GB / 64 = 0.968 GB/microbatch.
       - Switching to TP=2 reduces P to 32 and halves per-GPU activation.
       - In-flight activations on stage 0: 32 * 0.484 GB = 15.5 GB.
    3. **Conclusion & Interpretation:**
       - **Result: Memory Imbalance Corrected.** Peak activation memory on stage 0 is effectively reduced to 15.5 GB.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
