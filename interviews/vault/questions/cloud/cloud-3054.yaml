schema_version: '1.0'
id: cloud-3054
track: cloud
level: L4
zone: evaluation
topic: pipeline-parallelism
competency_area: parallelism
bloom_level: evaluate
phase: training
title: 'Pipeline Parallelism Bubble Overhead Comparison: GPipe vs PipeDream'
scenario: You are comparing GPipe and PipeDream-Flush scheduling on a 4-stage H100
  pipeline (80GB HBM3, 3.35TB/s) with micro-batch size m=16. GPipe uses a synchronous
  flush after each mini-batch, while PipeDream-Flush uses 1F1B ordering. Calculate
  bubble overhead for each and recommend the approach for a 32B parameter model training
  run.
question: For a 4-stage H100 pipeline with 16 micro-batches, what are the GPipe and
  PipeDream-Flush bubble overheads, and which should train a 32B model?
details:
  realistic_solution: GPipe bubble fraction = (4-1)/(16+4-1) = 3/19 ≈ 15.8%. PipeDream-Flush
    with 1F1B achieves same bubble fraction but with O(p) memory footprint instead
    of O(m) activations stored. For 32B model across 4 H100s (each 80GB), PipeDream-Flush
    is preferred because storing 16 micro-batch activations in GPipe requires ~16x
    per-stage activation memory.
  common_mistake: Students often forget that GPipe bubble fraction is (p-1)/(m+p-1)
    where p=stages, m=micro-batches, and conflate stage count with device count.
  napkin_math: 'Bubble fraction (same for both schedules): (p-1)/(m + p-1) = 3/19 ≈ 15.8%. Per-stage
    parameter memory (32B model, 4 stages, FP16): 32B / 4 = 8B params * 2 bytes = 16 GB weights per
    stage; with FP16 grads + FP32 master/m/v under mixed-precision Adam, total state per stage ≈ 16
    bytes * 8B = 128 GB — already over the 80 GB H100 even before activations, implying ZeRO/FSDP must
    shard the optimizer state across the data-parallel dimension. Assume that has been done so that
    per-stage non-activation state is ~24 GB. Activation budget per stage: 80 - 24 = 56 GB. Per-micro-batch
    activation (hidden 6144, seq 2048, batch per micro-batch 1, BF16 with checkpointing) ≈ 3.5 GB. GPipe
    peak activation memory = m * 3.5 GB = 56 GB at m=16 → just at the budget; small workspace fluctuation
    OOMs. PipeDream-Flush peak = p * 3.5 GB = 14 GB at p=4 — comfortable headroom, no OOM risk.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
