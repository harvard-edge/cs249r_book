schema_version: '1.0'
id: cloud-0349
track: cloud
level: L2
zone: implement
topic: roofline-analysis
competency_area: compute
bloom_level: understand
phase: both
title: The Deceptive Addition
scenario: 'You are profiling a PyTorch model on an NVIDIA H100 and find that a simple element-wise addition of two large FP16 tensors `C = A + B` is surprisingly slow.'
question: "What is the arithmetic intensity of C = A + B for FP16 tensors, and is it memory-bound or compute-bound on an H100?"
details:
  realistic_solution: The kernel is heavily memory-bound. For every element in the tensors, you perform 1 floating-point operation (the addition). To perform this single operation, you must read 2 elements from memory (one from A, one from B) and write 1 element back (to C). In FP16, each element is 2 bytes. Therefore, the ratio of operations to bytes transferred is extremely low, meaning the GPU spends most of its time waiting for data from HBM, not performing calculations.
  common_mistake: |
    **The Pitfall:** Miscalculating the number of bytes transferred or misinterpreting the roofline model.
    **The Rationale:** Engineers often forget to account for the write operation (C) or one of the read operations (A or B), and incorrectly assume any low AI operation is somehow compute-bound.
    **The Consequence:** Wasting time trying to optimize kernel compute instructions (e.g., loop unrolling) when the kernel is strictly memory-bandwidth limited.
  napkin_math: |
    1. **Calculate FLOPs:** For a tensor with N elements, `A + B` performs exactly N floating-point operations.
    2. **Calculate Memory Moved:** Read 2 elements (A, B) and write 1 element (C) = 3 * N elements. In FP16 (2 bytes/element), this is 6 * N bytes.
    3. **Arithmetic Intensity:** N FLOPs / (6 * N) Bytes = 1/6 ≈ **0.167 FLOPs/Byte**.
    4. **Roofline Comparison:** The H100 ridge point is ~295 FLOPs/Byte. Since 0.167 << 295, the operation is extremely **memory-bound**.
  options:
  - AI is ~0.167 FLOPs/Byte, making it compute-bound.
  - AI is ~0.5 FLOPs/Byte, making it memory-bound.
  - AI is ~1.0 FLOPs/Byte, making it compute-bound.
  - AI is ~0.167 FLOPs/Byte, making it memory-bound.
  correct_index: 3
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
chains:
- id: cloud-chain-344
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
