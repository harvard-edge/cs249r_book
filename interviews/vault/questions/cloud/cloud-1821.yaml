schema_version: '1.0'
id: cloud-1821
track: cloud
level: L4
zone: diagnosis
topic: data-parallelism
competency_area: parallelism
bloom_level: analyze
phase: training
title: Diagnosing Elastic Scaling Collapse
scenario: 'You are observing an elastic pre-training job for a 13B parameter LLM. The job uses a strict static global batch size of 2048 sequences. When the cluster scheduler dynamically scales the job from 64 to 128 GPUs, the global throughput only increases by 15%. The network is verified to be fully operational.'
question: 'Why does throughput rise only 15% when scaling from 64 to 128 GPUs with a static global batch size, and what should the scheduler do?'
details:
  realistic_solution: 'With a static global batch size, doubling the GPU count halves the micro-batch size per GPU. This decreases the arithmetic intensity, causing the compute time to shrink so much that the fixed AllReduce communication latency dominates the step time. To fix this, the scheduler should continuously profile the empirical scaling efficiency curve and set a strict upper bound on worker allocation.'
  common_mistake: |
    **The Pitfall:** Assuming an undetected network failure or straggler node.
    **The Rationale:** Candidates often jump to hardware or network anomalies rather than mathematically analyzing the impact of static batching on per-GPU utilization as worker counts increase.
    **The Consequence:** Misdiagnosing the throughput collapse, leading to unnecessary hardware troubleshooting instead of fixing the scheduler policy.
  napkin_math: |
    **Assumptions & Constraints:**
    - Global Batch Size: 2048.
    - 64 GPUs: MBS = 32. 128 GPUs: MBS = 16.
    - AllReduce time ~15ms.

    **Calculations:**
    - 64 GPUs: Compute = 40ms. Step = max(40, 15) = 40ms. Throughput = 2048 / 0.040 = 51,200 seq/s.
    - 128 GPUs: Compute = 20ms. Step = max(20, 15) = 20ms (practical ~35ms). Throughput = 2048 / 0.035 = 58,514 seq/s.
    - Gain: 58,514 / 51,200 = ~1.14x (+14%).

    **Conclusion & Interpretation:**
    - **Result: ~14% throughput gain (Communication Latency-Bound)**. Doubling GPUs yields marginal gains because fixed communication latency dominates the shrunken compute time.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
