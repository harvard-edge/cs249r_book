schema_version: '1.0'
id: cloud-3290
track: cloud
level: L5
zone: specification
topic: memory-pressure-management
competency_area: memory
bloom_level: analyze
phase: both
title: LLM Memory Pressure Management on NVIDIA H100 Cluster
scenario: You are tasked with designing a system to train a 175B parameter LLM on a cluster of NVIDIA H100 GPUs. The model's FP16 weights alone occupy approximately 350 GB. During a typical training step, the combined memory footprint of weights, activations, and optimizer states significantly exceeds the 80 GB HBM3 capacity of a single H100.
question: How would you design a distributed training strategy to prevent OOM errors, minimize fragmentation, and optimize gradient accumulation for a 175B LLM on H100s?
details:
  realistic_solution: |
    A multi-pronged approach: 1) Data Parallelism with Model/Optimizer Sharding (DeepSpeed ZeRO-3) to distribute massive optimizer states and gradients. 2) Activation Checkpointing to drop intermediate activations and trade compute for memory. 3) Offloading Strategies for less frequently accessed tensors. 4) Efficient Gradient Accumulation, ensuring activations are released after each micro-batch. 5) Profiling tools to track memory hotspots. 6) OS-level mitigations like pinned GPU memory and HugePages.
  common_mistake: |
    **The Pitfall:** Underestimating the memory footprint of optimizer states and activations.
    **The Rationale:** Candidates focus only on the model weights and naive data parallelism.
    **The Consequence:** Frequent OOMs or the necessity to use extremely small, inefficient global batch sizes.
  napkin_math: |
    - Model Parameters: 175B.
    - FP16 Weights: 175B * 2 bytes = 350 GB.
    - FP32 Adam States: 175B * 2 * 4 bytes = 1400 GB.
    - FP32 Weights (optimizer): 175B * 4 bytes = 700 GB.
    - Total Memory without sharding/checkpointing: 350 GB + 2100 GB + ~700 GB (activations) = ~3150 GB.
    - With ZeRO-3 on 64 GPUs: Shared memory is (350+2100) / 64 = ~38.3 GB/GPU.
    - **Combined with activation checkpointing, the footprint comfortably fits within the 80 GB limit per GPU.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Reasoning Competency, Knowledge Area, and Mode are missing.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
