schema_version: '1.0'
id: cloud-3113
track: cloud
level: L4
zone: implement
topic: transformer-systems-cost
competency_area: compute
bloom_level: apply
phase: both
title: 'Transformer Cost Implement: Chinchilla Optimal Model Size Calculation'
scenario: 'Implement the Chinchilla optimal model size and token count for a fixed compute budget of $100K on a GPU cluster ($2.50/hr, 40% MFU, 989 TFLOP/s FP16). Chinchilla optimal: N* = (C/6)^0.5 * (D/N)^0.5 where optimal D/N = 20.'
question: For a $100K budget, what Chinchilla-optimal model size and token count should you train, and how does it compare to a 7B run?
details:
  realistic_solution: Derive the total FLOPs C from the budget, then explicitly use the compute constraint C = 6*N*D to calculate N* and D* jointly.
  common_mistake: |
    **The Pitfall:** Using the simplified Chinchilla rule (D=20*N) without accounting for the fixed compute budget.
    **The Rationale:** Engineers fail to realize that the compute budget C = 6*N*D determines both N and D jointly, not independently.
    **The Consequence:** Suboptimal model sizing that fails to maximize performance for a given monetary budget.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Budget: $100K.
       - Hardware: $2.50/hr, 989 TFLOPs FP16, 40% MFU.
       - Scaling: C = 6ND, D = 20N.
    2. **Calculations:**
       - Compute (C): ($100K / $2.50) × 3600s × 989e12 × 0.4 ≈ 5.696e22 FLOPs.
       - Optimal Size (N*): sqrt(C / 120) = sqrt(5.696e22 / 120) ≈ 21.8B parameters.
       - Optimal Tokens (D*): 20 × 21.8B ≈ 436B tokens.
       - 7B Tokens: 5.696e22 / (6 × 7e9) = 1.356T tokens.
    3. **Conclusion & Interpretation:** **A ~22B model trained on ~436B tokens optimally utilizes the budget over a 7B model. Result: Compute-Bound.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
