schema_version: '1.0'
id: cloud-3067
track: cloud
level: L4
zone: analyze
topic: queueing-theory
competency_area: latency
bloom_level: analyze
phase: inference
title: 'Queueing Theory Analyze: Why Tail Latency Explodes at High Utilization'
scenario: An inference cluster serving LLaMA-70B shows P50 latency of 800ms but P99 latency of 12 seconds. Utilization is measured at 89%.
question: "At 89% inference utilization, how does M/M/1 queueing explain P99 latency far above P50, and what happens at 95% utilization?"
details:
  realistic_solution: 'M/M/1 wait time in queue: Wq = ρ/(μ(1-ρ)). At ρ=0.89: Wq scales as 0.89/(1-0.89) = 8.1x the service time. P99 latency ≈ mean + 4.6σ for exponential service times, and variance grows as ρ/(1-ρ)². The system is in a high-variance regime where rare long requests cause queue buildup visible at P99 but not P50.'
  common_mistake: |
    **The Pitfall:** Thinking that latency degrades linearly with utilization.
    **The Rationale:** Candidates lack queueing theory intuition and assume that 90% utilization means things are merely 10% slower, completely missing the hyperbolic ρ / (1-ρ) relationship.
    **The Consequence:** Designing systems that operate "efficiently" at high utilization but fundamentally violate all P99 latency SLAs.
  napkin_math: |
    **Assumptions & Constraints:**
    - M/M/1 queue model.
    - P50 ≈ 800ms (mean service time).

    **Calculations:**
    - Wq = ρ / (μ(1-ρ)).
    - At ρ=0.89: Queue multiplier = 0.89 / 0.11 = 8.09x service time.
    - Wq = 8.09 * 800ms = 6.47s. Total mean W = 6.47s + 0.8s = 7.27s.
    - P99 for M/M/1: W_99 ≈ -ln(0.01) * W = 4.6 * 7.27s ≈ 33s (theoretical).
    - At ρ=0.95: multiplier = 0.95 / 0.05 = 19x. Total mean W ≈ 16s.

    **Conclusion & Interpretation:**
    - **Result: 33s (Latency-Bound)**. The hyperbolic blowup at high utilization causes extreme tail latency.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
