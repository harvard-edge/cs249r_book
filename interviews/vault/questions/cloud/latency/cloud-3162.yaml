schema_version: '1.0'
id: cloud-3162
track: cloud
level: L3
zone: design
topic: speculative-decoding
competency_area: latency
bloom_level: create
phase: inference
title: Designing a Draft Model Selection Strategy
scenario: 'You must choose a draft model for speculative decoding with a 70B target. Options: (A) 1B model (α=0.65, 0.5ms/token), (B) 7B model (α=0.82, 2ms/token), (C) 2-layer early-exit of the target (α=0.70, 1ms/token). Target decode: 25ms/token.'
question: Which draft option is best when acceptance rate and draft latency both affect speculative decoding speedup?
details:
  realistic_solution: Option B is best under the stated simplified latency model. Compare useful accepted tokens per wall-clock round, not acceptance rate alone. With K=6 and one 25ms target verification round, Option A yields about 2.72 tokens in 28ms, or 97 tok/s; Option B yields about 4.17 tokens in 37ms, or 113 tok/s; Option C yields about 3.06 tokens in 31ms, or 99 tok/s. Option B's 2ms/token draft is slower, but its higher acceptance rate still wins here. Option C would become attractive if the 7B draft latency rose, target verification got cheaper, or hardware contention made the draft path more expensive.
  common_mistake: |
    **The Pitfall:** Always picking the highest acceptance rate (α) draft model without factoring in its latency.
    **The Rationale:** A high acceptance rate feels like better "accuracy", and engineers assume any draft is vastly faster than the target.
    **The Consequence:** The draft's own latency cost ruins the speculation speedup. A slow but accurate draft (Option B) can result in worse wall-clock time than a fast but less accurate one (Option A).
  napkin_math: |
    **Assumptions & Constraints:**
    - Use the same speculative window K=6 for a first-pass comparison.
    - Target verification cost is one 25ms round.
    - Score each option by useful tokens per round divided by draft plus verification time.

    **Calculations:**
    - Formula: E[tokens] = (1 - α^(K+1)) / (1 - α).
    - Option A: E = (1 - 0.65^7) / 0.35 ≈ 2.72 tokens; round time = 6 * 0.5 + 25 = 28ms; rate ≈ 97 tok/s.
    - Option B: E = (1 - 0.82^7) / 0.18 ≈ 4.17 tokens; round time = 6 * 2 + 25 = 37ms; rate ≈ 113 tok/s.
    - Option C: E = (1 - 0.70^7) / 0.30 ≈ 3.06 tokens; round time = 6 * 1 + 25 = 31ms; rate ≈ 99 tok/s.

    **Conclusion & Interpretation:**
    - **Result: Option B has the best expected throughput under these assumptions.** Acceptance rate alone is insufficient, but here B's higher yield more than pays for its slower draft latency.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
