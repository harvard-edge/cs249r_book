schema_version: '1.0'
id: cloud-3603
track: cloud
level: L5
zone: optimization
topic: queueing-theory
competency_area: latency
bloom_level: evaluate
phase: inference
title: Work-Conserving Scheduler Analysis for Mixed Priority on MI300X
scenario: 'An MI300X (192GB HBM3) serves two priority classes: premium (SLA p99<100ms, λ=200/s) and standard (no SLA, λ=800/s). Total service rate µ=1200/s.'
question: How would you design a preemptive priority queue for premium and standard traffic, and can premium meet p99<100 ms?
details:
  realistic_solution: 'Use preemptive-resume priority: premium work can interrupt standard work, and standard resumes later. Premium behaves like an M/M/1 with only premium arrivals, so ρ1=200/1200=0.167, mean premium response time is 1/(µ-λ1)=1/1000=1ms, and an exponential-tail approximation gives p99≈ln(100)/1000=4.6ms. Total utilization is (200+800)/1200=0.833, so the system is stable. Standard traffic absorbs the priority penalty; its mean response is approximately (1/µ)/((1-ρ1)(1-ρ_total)) = 0.833ms/(0.833×0.167) ≈ 6ms. Premium easily meets p99<100ms, but it is not zero-latency and it should still be monitored for service-time variance.'
  common_mistake: |
    **The Pitfall:** Treating preemptive priority as either weighted fair sharing or absolute zero-latency service for premium requests.
    **The Rationale:** Preemptive priority removes standard-class interference from premium queueing, but premium requests still queue behind other premium requests and consume finite service time.
    **The Consequence:** The design either under-isolates premium traffic by using fair sharing, or overstates the SLA margin by ignoring premium self-queueing and service-time variance.
  napkin_math: |
    **Assumptions & Constraints:**
    - Premium traffic (λ1): 200 req/s. SLA: p99 < 100ms.
    - Standard traffic (λ2): 800 req/s.
    - Service rate (µ): 1200 req/s.
    - Scheduler: Preemptive M/M/1 priority queue.

    **Calculations:**
    - Premium utilization (ρ1): 200 / 1200 = 0.167.
    - Total utilization (ρ_total): 1000 / 1200 = 0.833.
    - Premium mean response time under preemptive-resume priority: 1 / (µ - λ1) = 1 / (1200 - 200) = 1ms.
    - Premium p99 response time approximation: ln(100) / (1200 - 200) = 4.6ms.
    - Standard mean response time approximation: (1/1200) / ((1 - 0.167) * (1 - 0.833)) = 6ms.

    **Conclusion & Interpretation:**
    - **Result: ~4.6ms premium p99 response time (Queue-Latency Bound)**. Preemptive priority protects premium traffic from standard-class load, and the 100ms SLA is met with large margin under the stated M/M/1 assumptions.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
