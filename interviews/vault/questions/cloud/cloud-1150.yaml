schema_version: '1.0'
id: cloud-1150
track: cloud
level: L5
zone: specification
topic: latency-decomposition
competency_area: latency
bloom_level: evaluate
phase: both
title: Mitigating Dispatch Overhead in Low-Latency Models
scenario: Your team is deploying a real-time autoregressive text-to-speech model on NVIDIA A100s. The model architecture features 2,000 sequential, lightweight operations per token generation step. Profiling reveals the GPU utilization is stuck at 15%, and the token latency is around 10ms.
question: Would you upgrade to H100s, use TorchInductor fusion, or wrap the TTS step in CUDA Graphs to reduce P99 latency, and why?
details:
  realistic_solution: 'Implement CUDA Graphs or kernel fusion. CUDA Graphs captures the execution graph and replays it with a single CPU launch, while fusion merges operations into fewer kernels. Both eliminate the CPU bottleneck, but CUDA Graphs is often optimal for fixed-shape, small-op autoregressive steps.'
  common_mistake: |
    **The Pitfall:** Assuming upgrading to a faster GPU (like an H100) will automatically reduce latency.
    **The Rationale:** Engineers often assume that low GPU utilization combined with high latency means the GPU is simply too slow at executing the operations.
    **The Consequence:** Spending millions on hardware upgrades that yield zero latency improvement because the bottleneck is the CPU's fixed kernel dispatch overhead.
  napkin_math: |
    1. CPU dispatch overhead is roughly $5\mu\text{s}$ per operation.
    2. For 2,000 operations, total CPU dispatch time = $2,000 \times 5\mu\text{s} = 10\text{ms}$.
    3. GPU compute time is ~$1\mu\text{s}$ per operation, totaling $2\text{ms}$.
    4. Since kernel launches are asynchronous, total latency is $\max(\text{CPU}, \text{GPU}) = \max(10\text{ms}, 2\text{ms}) = 10\text{ms}$.
    5. Wrapping the step in CUDA Graphs drops dispatch overhead to a single ~$5\mu\text{s}$ launch.
    6. **Total latency drops from 10ms down to ~2ms (a 5x speedup), maximizing actual GPU execution time.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: cloud-chain-105
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
