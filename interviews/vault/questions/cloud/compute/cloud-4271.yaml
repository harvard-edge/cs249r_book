schema_version: '1.0'
id: cloud-4271
track: cloud
level: L3
zone: fluency
topic: chiplet-architecture
competency_area: compute
bloom_level: understand
phase: both
title: HBM3 Stack Partitioning Across MCDs
scenario: How are the 4 Memory Cache Dies (MCDs) in MI300X physically connected to
  the 8 XCDs, and what does this mean for memory access symmetry?
question: How are the 4 Memory Cache Dies (MCDs) in MI300X physically connected to
  the 8 XCDs, and what does this mean for memory access symmetry?
details:
  realistic_solution: Each MCD manages a 48 GB HBM3 stack partition. XCDs are arranged
    in two rows of 4 on the interposer. XCDs nearest to an MCD have lower fabric hop
    count (~1 hop) while diagonal XCDs have 2+ hops. AMD's NUMA-aware memory allocator
    (ROCm NUMA) attempts to place tensors on HBM closest to the XCD that will process
    them, but this requires explicit hints via hipMemAdvise.
  common_mistake: Assuming each MCD serves exactly two XCDs in a symmetric 2:1 mapping.
    The actual topology has non-uniform proximity due to physical interposer layout.
  napkin_math: '4 MCDs × 48 GB = 192 GB. Aggregate HBM3 BW: 5.3 TB/s → 1.325 TB/s
    per MCD. XCD-to-local-MCD: 1 hop ~50ns. XCD-to-remote-MCD: 2 hops ~100ns. Bandwidth
    penalty for remote access: ~15-20% throughput reduction due to arbitration.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
