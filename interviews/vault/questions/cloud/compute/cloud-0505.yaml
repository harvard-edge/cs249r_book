schema_version: '1.0'
id: cloud-0505
track: cloud
level: L4
zone: optimization
topic: roofline-analysis
competency_area: compute
bloom_level: analyze
phase: both
title: H100 Roofline Diagnosis for Recommendation Inference
scenario: You've deployed a custom recommendation model. The profiling dashboard shows you are achieving 120 TFLOPS out of a possible 989 TFLOPS on an H100 GPU, while the HBM bandwidth counter is already near the 3.35 TB/s hardware limit. Your tech lead suggests buying a faster GPU to fix the latency.
question: Why is your tech lead wrong to suggest buying a faster GPU when achieving 120 TFLOPS out of 989 peak?
details:
  realistic_solution: The tech lead is wrong because the profile says the workload is already saturating HBM bandwidth, not ALUs. The H100 ridge point is 989 TFLOPS / 3.35 TB/s ≈ 295 FLOP/byte. With observed throughput of 120 TFLOPS while consuming about 3.35 TB/s, the measured arithmetic intensity is about 36 FLOP/byte, far below the ridge point, so the roofline cap is memory bandwidth. A faster GPU with more peak TFLOPS but the same memory bandwidth would not materially improve latency. Better fixes are reducing bytes moved per request, increasing batching/reuse, quantizing, or using hardware with more HBM bandwidth.
  common_mistake: |
    **The Pitfall:** Assuming the GPU isn't fully utilized because it's starved for compute.
    **The Rationale:** Candidates see low TFLOPS utilization and blindly assume throwing more ALUs at the problem will help.
    **The Consequence:** Purchasing incredibly expensive compute upgrades that yield exactly zero latency improvements because the workload is memory-bound.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - H100 peak compute: 989 TFLOPS.
       - H100 memory bandwidth: 3.35 TB/s.
       - Observed throughput: 120 TFLOPS.
       - Observed memory bandwidth: approximately 3.35 TB/s, near the hardware limit.
    2. **Calculations:**
       - Ridge Point = 989 TFLOPS / 3.35 TB/s = 295 Ops/Byte.
       - Measured Arithmetic Intensity = 120 TFLOPS / 3.35 TB/s = 36 FLOP/Byte.
    3. **Conclusion & Interpretation:** **Result: Memory-Bound**. The measured 36 FLOP/Byte is far below the 295 FLOP/Byte ridge point, so adding compute without adding memory bandwidth is unlikely to improve latency.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
