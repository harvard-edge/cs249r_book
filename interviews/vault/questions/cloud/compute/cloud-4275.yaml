schema_version: '1.0'
id: cloud-4275
track: cloud
level: L6+
zone: optimization
topic: chiplet-architecture
competency_area: compute
bloom_level: create
phase: both
title: Cross-Die Prefetching for LLM Decode
scenario: LLM decode on MI300X is memory-bandwidth-limited. You want to hide Infinity Fabric latency for KV-cache access during autoregressive generation.
question: How would you prefetch remote-XCD KV-cache blocks to hide Infinity Fabric latency during MI300X autoregressive decode?
details:
  realistic_solution: Use speculative prefetch for the top-K most likely next tokens based on logit distributions. This effectively overlaps the fabric latency with compute.
  common_mistake: |
    **The Pitfall:** Using simple sequential prefetch.
    **The Rationale:** Assuming contiguous memory access patterns apply to top-p autoregressive sampling.
    **The Consequence:** High prefetch miss rates leaving the Infinity Fabric latency completely unhidden.
  napkin_math: |
    **Assumptions & Constraints:**
    - KV fetch = 300ns. Compute = 500us. Top-4 spec cost = 1.2us.

    **Calculations:**
    - 500us compute window / 300ns remote-fetch latency = about 1667 latency intervals.
    - Top-4 speculative prefetch cost = 4 × 300ns = 1.2us if the four candidate KV blocks are fetched serially.
    - 1.2us is only 0.24% of a 500us compute window, so it can be overlapped if prefetch is issued early enough.
    - The benefit depends on hit rate: a correct prediction hides a 300ns miss; incorrect candidates waste bandwidth but not much latency if overlapped.

    **Conclusion & Interpretation:**
    - **Result: Latency-Hiding**. The math supports speculative top-K prefetch because the remote-fetch latency is tiny relative to the decode compute window, not because 1667 blocks must be fetched.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
