schema_version: '1.0'
id: cloud-3260
track: cloud
level: L3
zone: analyze
topic: compute-cost-estimation
competency_area: compute
bloom_level: analyze
phase: both
title: H100 Training Cost for Large Language Model
scenario: You are tasked with estimating a simplified training compute budget for a 100B parameter LLM. Assume the training run requires 300 total FLOPs per parameter.
question: What GPU-hours, instance cost, electricity cost, and total cost do you estimate for the 100B-parameter training run?
details:
  realistic_solution: Calculate the total FLOPs for the simplified run as parameters times FLOPs per parameter, then divide by the effective FLOPs/sec of a single GPU (peak TFLOPS times utilization). This yields GPU-seconds, which can be converted to GPU-hours. Multiply GPU-hours by the instance cost for compute cost, and calculate power consumption (kW times hours times cost/kWh) for electricity cost. Summing these gives the total estimated cost; for real pretraining, the assumed FLOPs per parameter would normally be much larger because it depends on token count.
  common_mistake: |
    **The Pitfall:** Calculating compute time against peak theoretical TFLOPS rather than effective TFLOPS.
    **The Rationale:** Simply pulling the headline TFLOPS number from hardware specs without discounting for real-world memory bandwidth limitations.
    **The Consequence:** Underestimating training time and budgets by ~40%.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 100B parameters, 300 total FLOPs/parameter for this simplified estimate.
       - GPU: 989 TFLOPS peak, 60% utilization, 700W TDP.
       - Instance cost: $3.50/hour, Electricity: $0.15/kWh.
    2. **Calculations:**
       - Total FLOPs: 100B × 300 = 100e9 × 300 = 3.0e13 FLOPs.
       - Effective Compute: 989 TFLOPS × 0.6 = 593.4 TFLOPS (5.934e14 FLOPs/sec).
       - Time: 3.0e13 / 5.934e14 ≈ 0.0506 seconds = 1.40e-5 GPU-hours.
       - Compute Cost: 1.40e-5 × $3.50 ≈ $0.000049.
       - Electricity Cost: 1.40e-5 × 0.7kW × $0.15 ≈ $0.0000015.
       - Total Cost: about $0.00005 under this toy assumption.
    3. **Conclusion & Interpretation:**
       **Result: The stated 300 FLOPs/parameter assumption gives a negligible toy estimate, not a realistic LLM pretraining budget.** If the intended assumption was 300B FLOPs per parameter, the original scale would need to be stated explicitly.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
