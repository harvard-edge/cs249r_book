schema_version: '1.0'
id: cloud-1380
track: cloud
level: L4
zone: diagnosis
topic: roofline-analysis
competency_area: compute
bloom_level: analyze
phase: both
title: Diagnosing Memory and Latency Blowup in FHE Inference
scenario: You are debugging a healthcare inference pipeline running an FHE-encrypted CNN (CKKS scheme). The plaintext model processes an image in 20ms using 150MB of RAM. Under FHE, inference takes 45 minutes and consumes 120GB of RAM per image, causing OOM kills when concurrent requests > 1. Profiling shows 90% of time and memory is consumed by `Bootstrap` and `EvalMult` operations evaluating non-linear activations.
question: Why does the FHE CNN take 45 minutes and 120GB per image, and what model change would make it practical?
details:
  realistic_solution: |
    The root cause is the high multiplicative depth required to approximate non-linear ReLU activations using high-degree polynomials in FHE. A degree-7 approximation can add depth around 3 per activation block, causing noise growth that forces expensive bootstrapping operations (~30-60s and >5GB RAM each). Replacing ReLU with an FHE-friendly low-degree activation such as x^2 can reduce the depth budget and may remove or greatly reduce bootstrapping if the rest of the CNN fits within leveled-FHE parameters.
  common_mistake: |
    **The Pitfall:** Attempting to solve FHE overhead by simply scaling up hardware.
    **The Rationale:** Cloud engineers are accustomed to solving OOM and CPU bottlenecks by throwing larger instances at the problem.
    **The Consequence:** Failing to realize that deeper circuits consume the noise budget and trigger repeated bootstrapping, making hardware scaling ineffective without algorithmic circuit depth reduction.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - FHE Expansion: ~10,000x over plaintext.
       - Bootstrapping Cost: 30-60s and >5GB RAM per occurrence.
       - Profiling attributes 90% of the 45-minute runtime and 120GB memory footprint to Bootstrap and EvalMult operations.
    2. **Calculations:**
       - Polynomial approximation: ReLU degree-7 can require about 3 multiplicative levels per activation block.
       - Low-degree activation: x^2 requires 1 multiplicative level, reducing noise growth per activation.
       - If this avoids most bootstraps, the removable runtime is up to 90% of 45 minutes: about 40.5 minutes.
       - The remaining lower bound is about 4.5 minutes before retuning parameters and kernels; additional gains depend on the full network depth and FHE parameter budget.
    3. **Conclusion & Interpretation:**
       - **Result: Multiplicative Depth-Bound.** Use an FHE-friendly activation and retune the CNN so the whole circuit fits the leveled-FHE depth budget; this can remove most bootstrapping overhead, but x^2 alone is not a universal guarantee.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T22:47:36.942239'
