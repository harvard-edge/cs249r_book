schema_version: '1.0'
id: cloud-4277
track: cloud
level: L3
zone: implement
topic: model-adaptation-systems
competency_area: architecture
bloom_level: apply
phase: both
title: LoRA Rank Selection and Memory Budget
scenario: You need to fine-tune a 7B parameter base model using LoRA on a single H100
  (80 GB). Calculate the memory budget for rank-16 LoRA vs full fine-tuning.
question: What is the H100 memory budget for rank-16 LoRA fine-tuning of a 7B model
  versus full fine-tuning?
details:
  realistic_solution: 'Full fine-tune 7B (FP32): 7B × 4 bytes = 28 GB weights + 56
    GB optimizer (Adam: 2 moments × 28 GB) + activations = ~120 GB — exceeds H100.
    LoRA rank-16: trainable params = 2 × rank × d_model × num_layers = 2 × 16 × 4096
    × 32 = 4.19M params. Adapter memory: 4.19M × 4B = 16.8 MB weights + 33.6 MB optimizer.
    Frozen base (BF16): 7B × 2B = 14 GB. Total: ~14.05 GB — easily fits.'
  common_mistake: Calculating LoRA memory as just the adapter parameters. Engineers
    forget that the frozen base model weights must still reside in GPU memory, and
    optimizer states are needed for the trainable adapter parameters.
  napkin_math: 'LoRA trainable params: 2 × 16 × 4096 × 32 layers = 4,194,304 ≈ 4.19M.
    Adapter memory: 4.19M × 4 bytes (FP32) = 16.8 MB. Base model BF16: 14 GB. Adapter
    Adam states: 33.6 MB. Activations (batch=4, seq=2048): ~2 GB. Total: 14 + 0.017
    + 0.034 + 2 ≈ 16.1 GB. H100 has 80 GB — 5× headroom for larger batches.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 2 * 16 * 4096 * 32 evaluates to 4.19M, not 4.3M
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
