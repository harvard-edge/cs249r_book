schema_version: '1.0'
id: cloud-4138
track: cloud
level: L3
zone: analyze
topic: container-orchestration
competency_area: deployment
bloom_level: analyze
phase: inference
title: K8s Resource Limits vs Requests for GPU Workloads
scenario: In Kubernetes, CPU and memory support both 'requests' (scheduling guarantee)
  and 'limits' (hard cap). GPU resources only support 'limits.' Why this asymmetry,
  and what does it mean for GPU scheduling?
question: Why this asymmetry, and what does it mean for GPU scheduling?
details:
  realistic_solution: 'GPUs are fundamentally different from CPU/memory: (1) Non-compressible:
    CPU can be time-shared (a pod using ''less than requested'' still allows other
    pods to use the slack). GPU contexts are exclusive — a GPU assigned to a pod is
    fully reserved even if utilization is 0%. (2) No overcommit: CPU allows requests
    < limits, enabling oversubscription (schedule more pods than physical cores).
    GPU device plugin allocates whole devices, so requests = limits is enforced by
    design. If you request 2 GPUs, you get exactly 2 physical GPUs (or MIG instances).
    (3) No fractional allocation: CPU supports milliCPU (e.g., 500m = 0.5 cores).
    GPU has no equivalent — you get 0 or 1+ whole GPUs. Implications: (a) GPU bin-packing
    is coarser than CPU — you cannot request 0.5 GPU natively. (b) GPU utilization
    waste is harder to reclaim — an idle GPU in a running pod cannot be lent to another
    pod. (c) Solutions for fractional GPU: use MIG for hardware partitioning, or NVIDIA''s
    MPS with time-slicing device plugin (beta) for software partitioning. The time-slicing
    plugin allows nvidia.com/gpu: 1 to map to a shared GPU slice.'
  common_mistake: Setting nvidia.com/gpu in resources.requests but not limits. For
    GPUs, requests and limits must be equal — there is no oversubscription. Setting
    only requests without limits has no effect.
  napkin_math: '8-GPU node serving pods requesting 1 GPU each: max 8 pods regardless
    of utilization. With MIG 1g.10gb: max 56 pods (7 per GPU). With time-slicing (10
    replicas per GPU): max 80 pods (but shared performance).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
