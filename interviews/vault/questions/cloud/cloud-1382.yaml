schema_version: '1.0'
id: cloud-1382
track: cloud
level: L5
zone: specification
topic: roofline-analysis
competency_area: compute
bloom_level: evaluate
phase: both
title: Evaluating FHE for Cloud-Based CNN Inference
scenario: A medical imaging provider wants to run a cloud-based CNN (ResNet-50, 4 GFLOPs) for tumor detection
  on encrypted X-rays. They are evaluating Fully Homomorphic Encryption (FHE) using the CKKS scheme. The
  SLA requires processing one image within 10 seconds. In plaintext, a T4 GPU processes an image in 10ms.
  Evaluate the feasibility of an FHE-based architecture for this workload, considering the computational
  overhead of ciphertext multiplications and bootstrapping, and justify your architectural decision.
details:
  realistic_solution: Reject pure FHE for this CNN due to the SLA violation. Propose a Trusted Execution
    Environment (TEE) like AWS Nitro Enclaves or AMD SEV-SNP, which offers memory encryption with only
    a ~1.1x to 1.5x latency overhead, easily meeting the 10s SLA.
  common_mistake: Candidates often assume FHE is plug-and-play for deep neural networks, failing to account
    for the massive 10,000x+ computational overhead of bootstrapping and the difficulty of evaluating
    non-polynomial activation functions like ReLU.
  napkin_math: Plaintext latency = 10 ms. FHE compute overhead for deep CNNs is roughly 10^4x to 10^5x.
    Estimated FHE latency = 10 ms * 10,000 = 100 seconds per image. 100s >> 10s SLA limit. Furthermore,
    ciphertext expansion (10x-100x) would bottleneck I/O bandwidth.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: cloud-chain-188
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T22:48:34.538117'
