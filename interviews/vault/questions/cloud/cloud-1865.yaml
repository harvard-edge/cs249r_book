schema_version: '1.0'
id: cloud-1865
track: cloud
level: L5
zone: specification
topic: latency-decomposition
competency_area: latency
bloom_level: evaluate
phase: both
title: LLM Serving Inversion Trade-offs
scenario: You are deploying a 7B parameter LLM for a chatbot on a cloud instance. During training, static batch size of 128 was used. The SLA requires Time-to-First-Token (TTFT) < 200ms and Inter-Token Latency < 50ms.
question: How should batching and scheduling change to meet TTFT <200ms and inter-token latency <50ms for the 7B LLM?
details:
  realistic_solution: Implement continuous batching (iteration-level scheduling) and drastically reduce the maximum batch size. Prioritize metrics like latency-bounded throughput. Cap the batch size at a small number (e.g., 4-16) where the total time for the memory-bound weight read and compute does not exceed the 50ms inter-token SLA.
  common_mistake: |
    **The Pitfall:** Retaining large static batch sizes or standard dynamic batching to maximize hardware utilization.
    **The Rationale:** Candidates rely on training-optimized setups, failing to recognize that large batches in autoregressive generation cause tail latency spikes.
    **The Consequence:** The system vastly exceeds the strict 50ms per-token latency SLA, rendering the chatbot unresponsive to users.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 7B parameters (14GB in FP16).
       - SLA: Inter-token latency < 50ms.
       - Memory bandwidth: 600 GB/s.
    2. **Calculations:**
       - Weight read time = 14 GB / 600 GB/s = 23.3ms.
       - Remaining time for compute & KV reads = 50ms - 23.3ms = 26.7ms.
    3. **Conclusion & Interpretation:**
       - **Result: Memory Bandwidth-Bound.** Batch size must be tightly capped to ensure weight read and compute times fit within the 50ms SLA.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: cloud-chain-367
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T01:01:43.650609'
