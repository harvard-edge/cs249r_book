schema_version: '1.0'
id: cloud-3967
track: cloud
level: L6+
zone: mastery
topic: communication-computation-overlap
competency_area: parallelism
bloom_level: create
phase: both
title: Communication-Aware Model Architecture Design
scenario: You are designing a new transformer architecture for training on 1024 GPUs. The current architecture has 128 attention heads and hidden_size=16384, requiring tensor-parallel AllReduce of 16384-dimensional vectors per layer. Design the architecture dimensions to maximize communication-computation overlap.
question: How would you choose the transformer dimensions and tensor-parallel layout to maximize communication-computation overlap on 1024 GPUs?
details:
  realistic_solution: 'Communication-aware design: (1) Tensor parallelism degree (TP) should divide attention heads evenly. With TP=8: 128/8=16 heads per GPU (good). With TP=16: 128/16=8 heads per GPU (still ok). (2) AllReduce data per layer (2 ARs) = 2 x seq_len x hidden_size x dtype_size. At seq=4096, hidden=16384, BF16: 2 x 4096 x 16384 x 2 = 256MB per layer. At NVLink 900 GB/s: 0.28ms per layer. (3) Compute per layer: ~17.8ms. Comm/compute ratio: 0.28/17.8 = 1.6% — excellent overlap. (4) But with 128 layers at TP=8: 128 layers x 0.28ms = 35.8ms total communication, well within the 2280ms total compute. (5) If hidden_size were 32768 (for more capacity): AllReduce doubles to 512MB = 0.57ms per layer. Still <3% of compute. The bottleneck shifts to memory, not communication. Design principle: keep TP AllReduce size << per-layer compute time.'
  common_mistake: Designing model architecture purely for model quality (number of parameters, attention heads) without considering how the dimensions map to communication patterns in tensor parallelism.
  napkin_math: 'Target: comm/compute ratio < 5% for effective overlap. Per-layer compute at 989 TFLOPS: 2 x seq x hidden^2 x 8 / 989T = 2x4096x16384^2x8/989T = 17.8ms. AllReduce at 900 GB/s: 256MB/900 = 0.28ms. Ratio: 1.6%. Architecture is communication-friendly.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
