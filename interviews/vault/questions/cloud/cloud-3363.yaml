schema_version: '1.0'
id: cloud-3363
track: cloud
level: L2
zone: analyze
topic: model-size-estimation
competency_area: architecture
bloom_level: analyze
phase: both
title: Estimating a Large Language Model's Fit on Google TPU v5e
scenario: You are tasked with deploying a new, unquantized Large Language Model (LLM)
  with 70 billion parameters, where each parameter is stored as a 16-bit floating
  point (BF16), onto a Google TPU v5e. The TPU v5e has 16 GB of HBM, a BF16 throughput
  of 197 TFLOPS, and 1.6 TB/s memory bandwidth. Without considering quantization or
  other optimizations, what is the initial memory challenge when trying to deploy
  this model on a single Google TPU v5e?
question: Without considering quantization or other optimizations, what is the initial
  memory challenge when trying to deploy this model on a single Google TPU v5e?
details:
  realistic_solution: A realistic solution requires calculating the model's memory
    footprint (70 billion parameters * 2 bytes/parameter = 140 GB). Since this greatly
    exceeds the 16 GB HBM of a single Google TPU v5e, the solution would immediately
    point to the necessity of model parallelism (sharding the model across multiple
    TPUs), aggressive quantization (e.g., to INT8 or lower), or offloading parts of
    the model to host memory. The memory bandwidth and TFLOPS are crucial for performance
    but memory fit is the primary bottleneck here.
  common_mistake: Forgetting to account for the memory required by model parameters
    themselves (70 billion * 2 bytes/parameter = 140 GB) and simply comparing the
    model's effective size to the HBM memory capacity of the TPU (16 GB), or overlooking
    the fact that BF16 parameters occupy 2 bytes each.
  napkin_math: 70 billion parameters * 2 bytes/parameter = 140 GB. A single Google
    TPU v5e has 16 GB HBM. Therefore, 140 GB model requires 140 GB / 16 GB = 8.75
    TPUs minimum for just parameter storage, not accounting for activations, optimizer
    states, or other runtime memory overheads.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
