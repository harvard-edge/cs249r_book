schema_version: '1.0'
id: cloud-4174
track: cloud
level: L4
zone: design
topic: interconnect-topology
competency_area: networking
bloom_level: apply
phase: both
title: Network Topology for Pipeline Parallelism
scenario: You are deploying a 175B model with 16-stage pipeline parallelism across
  16 A100 GPUs spread across 2 nodes (8 GPUs per node with NVLink). The pipeline sends
  activations from stage N to stage N+1. How should you map pipeline stages to physical
  GPUs to minimize inter-node activation transfers, and what is the bandwidth requirement
  for the inter-node link?
question: What bandwidth should you use for a single InfiniBand NDR inter-node link
  in the pipeline transfer calculation?
details:
  realistic_solution: IB NDR = 400 Gbps = 50 GB/s per link. IB HDR = 200 Gbps = 25
    GB/s. The calculation used HDR bandwidth (25 GB/s) instead of NDR (50 GB/s).
  common_mistake: Mapping stages linearly (stages 0-7 on node 0, stages 8-15 on node
    1), which creates a single inter-node bottleneck between stages 7 and 8. Every
    micro-batch must cross this link.
  napkin_math: 'InfiniBand NDR bandwidth: single IB NDR link = 400 Gbps unidirectional
    = 50 GB/s (not 25 GB/s which is HDR). IB HDR = 200 Gbps = 25 GB/s. IB NDR = 400
    Gbps = 50 GB/s. AllReduce bandwidth calculation should use 50 GB/s per NDR link.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Hardware spec error: Single IB NDR is 400 Gbps (50 GB/s), not 25 GB/s (which corresponds
  to HDR).'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
