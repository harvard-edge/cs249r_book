schema_version: '1.0'
id: cloud-3061
track: cloud
level: L3
zone: realization
topic: pipeline-parallelism
competency_area: parallelism
bloom_level: apply
phase: training
title: 'Pipeline Parallelism Realization: Activation Memory with Gradient Checkpointing'
scenario: A 40-stage pipeline (one transformer layer per stage) trains a 40B model. For sequence length 4096 and batch size 8, without gradient checkpointing, each stage stores all intermediate activations. With checkpointing, only the layer input is stored.
question: How much activation memory is saved by checkpointing per stage, and does it fit in an 80GB H100 without it?
details:
  realistic_solution: 'Single transformer layer activations without checkpointing: attention (Q,K,V,attn weights,output) + FFN (up,gate,down intermediates) ≈ 12 * hidden * seq * batch * 2 bytes. Hidden = 8192 (40B model), seq=4096, batch=8. Activation size = 12 * 8192 * 4096 * 8 * 2 = 6.44GB per stage. With checkpointing, store only the layer input: 8192 * 4096 * 8 * 2 = 537MB. Savings are about 12x per stage. Including per-stage BF16 weights (2GB), BF16 gradients (2GB), and FP32 Adam m/v states (8GB), the uncheckpointed per-stage footprint is about 18.44GB, so it fits in an 80GB H100 for this one-layer-per-stage estimate.'
  common_mistake: |
    **The Pitfall:** Assuming gradient checkpointing is a "free" memory optimization with no downside.
    **The Rationale:** It's easy to look at the massive memory savings and blindly enable checkpointing, forgetting the strict computational trade-off.
    **The Consequence:** Training compute increases by roughly 33% because of the mandatory extra forward pass, significantly increasing total training time.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 40B total params, 40 layers -> 1B params/layer, Hidden dim ≈ 8192.
       - Sequence length 4096, batch size 8.
    2. **Calculations:**
       - Uncheckpointed Activations: 12 × 8192 × 4096 × 8 × 2 = 6.44 GB.
       - Checkpointed Activations: 1 × 8192 × 4096 × 8 × 2 = 537 MB.
       - Total Uncheckpointed Footprint: Weights (2GB) + Gradients (2GB) + Optimizer m/v (8GB) + Activations (6.44GB) = 18.44 GB.
    3. **Conclusion & Interpretation:**
       - **Result:** Memory savings is a ~12x reduction (6.44 GB vs 537 MB). It easily fits in an 80GB H100 even without checkpointing.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
