schema_version: '1.0'
id: cloud-3673
track: cloud
level: L5
zone: diagnosis
topic: mixture-of-experts
competency_area: parallelism
bloom_level: evaluate
phase: both
title: Expert Load Imbalance Killing Throughput in MoE
scenario: A 16-expert MoE model deployed across 4 GPUs processes a production batch. GPU utilization drops to 40% on 3 GPUs, while the fourth is pegged at 100% because expert
question: What causes one GPU to hit 100% utilization while the others idle, and how would you fix the load imbalance?
details:
  realistic_solution: 'The cause is skewed router decisions: the gate is sending too many tokens to one expert or group of colocated experts, so that GPU becomes a hotspot while other expert GPUs wait. Fix the routing policy first: monitor per-expert token counts, tune the gate and load-balancing loss, add inference capacity limits with token dropping or second-choice dispatch, and place or shard experts so popular experts cannot concentrate on one GPU.'
  common_mistake: |
    **The Pitfall:** Relying solely on the training auxiliary load-balancing loss in production.
    **The Rationale:** Assuming that because load was balanced during training, it will remain balanced under real-world domain shifts.
    **The Consequence:** Severe throughput degradation as all-reduce operations block waiting for the single overloaded GPU to finish.
  napkin_math: |
    **Assumptions & Constraints:**
    - 16 experts across 4 GPUs.
    - Expert #3 receives 68% of the load.

    **Calculations:**
    - Perfectly balanced load: 1/16 (6.25%) of tokens per expert.
    - Load factor on expert #3: 68% / 6.25% = ~10.8x over expected load.

    **Conclusion & Interpretation:**
    - **Result: Load-Balance Bound.** The overloaded GPU processes nearly 11x its fair share, stalling the other 3 GPUs entirely at the All-Reduce sync barriers.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
