schema_version: '1.0'
id: cloud-4092
track: cloud
level: L1
zone: fluency
topic: model-tensor-parallelism
competency_area: parallelism
bloom_level: apply
phase: training
title: TP Memory Savings Calculation
scenario: A 30B parameter model uses BF16 weights (60 GB total). You apply TP=4 across four A100-80GB GPUs. How much weight memory does each GPU hold? What about optimizer states during training with AdamW in mixed precision?
question: With a 30B BF16 model and TP=4 on A100-80GB GPUs, how much weight and AdamW optimizer-state memory does each GPU hold?
details:
  realistic_solution: 'Weight memory per GPU: ~60 GB / 4 = 15 GB (BF16 weights split across TP ranks). Small components like positional embeddings and final LayerNorm are replicated but negligible (<100 MB). AdamW optimizer-state memory per local parameter is FP32 master weight (4B) + FP32 momentum (4B) + FP32 variance (4B) = 12B. With 30B/4 = 7.5B params per GPU, optimizer states alone are 7.5B × 12B = 90 GB per GPU. BF16 gradients add another 15 GB if resident, so weights + AdamW states + gradients are about 120 GB per GPU. The direct answer is 15 GB of weights and 90 GB of AdamW optimizer states; the full training state exceeds A100-80GB, so ZeRO or another sharding strategy is needed.'
  common_mistake: |
    **The Pitfall:** Assuming TP=4 means each GPU holds exactly 1/4 of everything.
    **The Rationale:** Engineers overlook that some parameters like embedding tables and final LayerNorms may be replicated, and optimizer states scale with the local parameter count.
    **The Consequence:** Severe underestimation of memory requirements, leading to unexpected OOMs during mixed-precision training.
  napkin_math: |
    **Assumptions & Constraints:**
    - 30B parameter model in BF16, TP=4 across A100-80GB GPUs.
    **Calculations:**
    - TP=4: 30B/4 = 7.5B params per GPU.
    - Weight only: 7.5B × 2B = 15 GB.
    - AdamW optimizer states only: 7.5B × (4B master weight + 4B momentum + 4B variance) = 90 GB.
    - If BF16 gradients are also resident: 7.5B × 2B = 15 GB, making weights + optimizer states + gradients = 120 GB.
    **Conclusion & Interpretation:**
    - Each GPU holds about 15 GB of weights and 90 GB of AdamW optimizer states. Including gradients, the local training state is about 120 GB and exceeds A100-80GB without additional sharding.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
