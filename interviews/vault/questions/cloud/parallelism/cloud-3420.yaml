schema_version: '1.0'
id: cloud-3420
track: cloud
level: L4
zone: diagnosis
topic: model-tensor-parallelism
competency_area: parallelism
bloom_level: analyze
phase: training
title: Diagnosing Tensor Parallelism Bottlenecks on NVIDIA GPUs for LLMs
scenario: 'You are managing the training of a 175B parameter LLM using 8-way tensor parallelism across a cluster of GPUs. GPU utilization is high, but training throughput is below expectations. Profiler reports show periods of low compute utilization interleaved with bursts of high memory bandwidth usage, and significant inter-GPU communication.'
question: 'How would you diagnose tensor-parallel underperformance characterized by low compute utilization and heavy inter-GPU communication across 8 GPUs?'
details:
  realistic_solution: 'Diagnosis must start with profiling the kernel execution using nsys to identify communication hotspots and analyze computation-to-communication overlap. Micro-benchmark the collective operations (e.g., all-reduce, reduce-scatter) to isolate topology or NVLink bottlenecks.'
  common_mistake: |
    **The Pitfall:** Assuming communication overhead is uniform and failing to isolate specific collective primitives.
    **The Rationale:** Developers often treat "communication" as a black box, ignoring the difference between all-reduce, all-gather, and reduce-scatter, and their interaction with the specific tensor chunk sizes.
    **The Consequence:** Ineffectively tuning the wrong layer dimensions or network topology, failing to resolve the poor computation-to-communication ratio.
  napkin_math: |
    **Assumptions & Constraints:**
    - 8-way tensor parallelism.
    - 1GB tensor size.
    - NVLink bandwidth: 600 GB/s.

    **Calculations:**
    - All-Reduce Data Volume: For a tensor across N GPUs, an all-reduce sends and receives 2 * (N-1)/N * TensorSize.
    - Data Communicated: 2 * (7/8) * 1GB = 1.75GB per GPU.
    - Ideal Time: 1.75 GB / 600 GB/s ≈ 2.9 milliseconds.

    **Conclusion & Interpretation:**
    - **Result: Communication-Bound**. If profiler shows communication phases lasting 10ms-50ms, effective bandwidth is crippled, indicating network saturation.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
