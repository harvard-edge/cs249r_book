schema_version: '1.0'
id: cloud-3032
track: cloud
level: L3
zone: fluency
topic: model-size-estimation
competency_area: memory
bloom_level: apply
phase: both
title: 'Model Size Estimation: Fluency \u2014 Estimate 7B Model Memory From Scratch'
scenario: In a live interview, you're asked without any reference material to estimate the GPU memory needed for a 7B parameter model at FP16 for inference with a batch of 8, generating up to 512 tokens.
question: How much GPU memory does a 7B parameter model need at FP16 for inference with a batch of 8, generating up to 512 tokens?
details:
  realistic_solution: "Weights: 7B \times 2B (FP16) = 14GB. KV-cache: 8 requests \times 512 tokens \times 2 (K+V) \times hidden_dim \times 2B \times n_layers. For 7B LLaMA (hidden=4096, 32 layers): 8\times512\times2\times4096\times2\times32 = 4.3GB. Activations (peak, 1 layer): 8\times512\times4096\times2B = 32MB (negligible). Total: ~18.3GB. Fits in single H100 (80GB) or A100 (40/80GB). Answer in 30 seconds with rounding."
  common_mistake: |
    **The Pitfall:** Computing only the memory for the weights and forgetting the dynamic memory.
    **The Rationale:** Candidates often memorize rule-of-thumb parameter sizing but fail to account for the actual runtime requirements of the sequence.
    **The Consequence:** Drastically underestimating the memory footprint, especially for longer contexts or larger batch sizes, leading to OOM in production.
  napkin_math: |
    **Assumptions & Constraints:**
    - 7B parameters at FP16 (2 Bytes/param).
    - Batch size: 8. Tokens: 512.
    - Standard architecture: 4096 hidden dim, 32 layers.

    **Calculations:**
    - Weights Memory: 7B \times 2 Bytes = 14 GB.
    - KV Cache Formula: Batch(8) \times Tokens(512) \times 2(K+V) \times HiddenDim(4096) \times Precision(2B) \times Layers(32).
    - KV Cache Size: 8 \times 512 \times 2 \times 4096 \times 2 \times 32 = 2.14 GB.
    - Activations: 8 \times 512 \times 4096 \times 2 Bytes = 32 MB (negligible).
    - Total Footprint: 14 GB + 2.14 GB = ~16.1 GB.

    **Conclusion & Interpretation:**
    - **Result: ~16.1 GB (Memory-Capacity-Bound)**. The inference process requires ~16.1 GB, comfortably fitting in standard mid-tier GPUs.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
