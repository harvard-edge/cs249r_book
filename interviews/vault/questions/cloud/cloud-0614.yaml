schema_version: '1.0'
id: cloud-0614
track: cloud
level: L4
zone: optimization
topic: vram-budgeting
competency_area: memory
bloom_level: analyze
phase: both
title: The Prompt Caching Optimization
scenario: Every request to our customer-support 70B LLM starts with the same 2,000-token system prompt. We are serving 1,000 requests/minute on 4× H100 GPUs. An engineer suggests prefix caching the system prompt's KV-cache.
question: Quantify the compute and memory savings of caching the system prompt, and justify if it is worth the engineering effort.
details:
  realistic_solution: 'Caching the system prompt''s KV-cache provides two savings: compute (skip the 2,000-token prefill) and memory (store one copy instead of one per concurrent request). The compute savings overwhelmingly dominate at high QPS, freeing up massive GPU capacity.'
  common_mistake: |
    **The Pitfall:** Dismissing prompt caching because it "only saves a little memory."
    **The Rationale:** Candidates fail to realize that the system prompt prefill is the most compute-heavy phase and its KV-cache is duplicated across all concurrent requests.
    **The Consequence:** Wasting massive amounts of compute and memory, forcing unnecessary GPU scaling.
  napkin_math: |
    - **KV Cache per Token:** 2 (K+V) × 80 (layers) × 8 (kv_heads) × 128 (head_dim) × 2 bytes (FP16) ≈ 320 KB/token.
    - **System Prompt KV Cache:** 2,000 tokens × 320 KB = 640 MB.
    - **Without Caching (1,000 req/min):** ~33 concurrent requests. Memory: 33 × 640 MB = 21.1 GB duplicated. Compute: 16.7 req/s × 280 TFLOPs = 4,676 TFLOPS load.
    - **With Prefix Caching:** Memory savings: 21.1 GB → 640 MB = 20.5 GB freed. Compute savings: 4,676 TFLOPS saved.
    - **Result:** Prompt caching effectively **frees ~4.7 H100s worth of prefill compute**, allowing the cluster to be halved.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
