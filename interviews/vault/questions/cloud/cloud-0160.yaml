schema_version: '1.0'
id: cloud-0160
track: cloud
level: L4
zone: diagnosis
topic: model-serving-infrastructure
competency_area: deployment
bloom_level: analyze
phase: inference
title: The Tyranny of Throughput
scenario: You are the tech lead for a new real-time AI coding assistant served on H100s. The service has a strict P99 Time-To-First-Token (TTFT) SLO of 200ms. Your dashboards show fantastic system health: `nvidia-smi` reports 95% utilization and your Tokens-Per-Second (TPS) throughput metrics are exceeding targets. However, user feedback is overwhelmingly negative, with frequent complaints of "lag" and "unresponsiveness." Your team is using a classic static batching strategy with a batching timeout of 150ms to maximize hardware efficiency.
question: "Why do high GPU utilization and TPS still produce lag under a 150ms static batching timeout with a 200ms TTFT SLO?"
details:
  realistic_solution: The core problem is the conflict between the goals of maximizing hardware utilization and minimizing user latency, a conflict that static batching handles poorly for real-time services. The 150ms batching timeout is an artificial queueing delay imposed on every single request to build a larger, more efficient batch. While this maximizes the H100's throughput (many tokens generated per second *across all users*), it front-loads a massive latency penalty on each user's TTFT.
  common_mistake: |
    **The Pitfall:** Confusing high throughput (Tokens-Per-Second) with low latency (TTFT).
    **The Rationale:** Believing that high GPU utilization automatically translates to a responsive, real-time user experience.
    **The Consequence:** Maximizing hardware efficiency by introducing massive artificial queueing delays, completely destroying the interactive feel of the application.
  napkin_math: |
    * **TTFT Equation:** TTFT = T_wait + T_batch + T_prefill.
    * **Artificial Delay:** The static timeout forces T_batch up to 150ms.
    * **Remaining Budget:** 200ms SLO - 150ms delay = 50ms remaining for actual compute.
    * **Prefill Compute:** Prefilling a 1000-token prompt on an H100 takes ~141ms.
    * **Total TTFT:** 150ms + 141ms = 291ms (Fails 200ms SLO).
    * **Conclusion:** **The 150ms artificial batching delay makes the 200ms SLO physically impossible to meet.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
