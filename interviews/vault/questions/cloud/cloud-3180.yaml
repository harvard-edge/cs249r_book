schema_version: '1.0'
id: cloud-3180
track: cloud
level: L4
zone: diagnosis
topic: speculative-decoding
competency_area: latency
bloom_level: analyze
phase: inference
title: Speculative Decoding Impact on Time-to-First-Token
scenario: After enabling speculative decoding for a 70B model on H100, users report that Time-To-First-Token (TTFT) increased from 200ms to 350ms. Tokens per second after the first token improved 2.5x.
question: Why did speculative decoding increase TTFT from 200 ms to 350 ms, and how would you fix the first-token delay?
details:
  realistic_solution: 'Without speculation, TTFT = target prefill time for prompt. With speculation: draft must also prefill the prompt to generate its first draft tokens. But draft and target run sequentially on same GPU. The first token is emitted after target verification, not after target prefill alone. Fix: skip speculation for the first decode step — emit first token immediately after target prefill, then start speculation from token 2 onwards.'
  common_mistake: |
    **The Pitfall:** Thinking TTFT degradation is solely caused by draft model initialization.
    **The Rationale:** Engineers assume loading a second model causes the delay.
    **The Consequence:** They miss that the draft model also sequentially needs to prefill the prompt, adding its own prefill latency to the critical path.
  napkin_math: |
    - 1. **Target prefill:** 200ms.
    - 2. **Draft prefill:** 29ms.
    - 3. **Draft generate (K tokens):** 10ms.
    - 4. **Target Verify:** 42ms.
    - 5. **Sequential TTFT:** 200ms + 29ms + 10ms + 42ms = 281ms (actual 350ms suggests extra KV cache setup overhead).
    - 6. **Fix:** Skip the first speculation round. TTFT = 200ms + 42ms = **242ms, then speculate from token 2**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null