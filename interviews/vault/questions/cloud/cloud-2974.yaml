schema_version: '1.0'
id: cloud-2974
track: cloud
level: L4
zone: implement
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: apply
phase: training
title: 'Fault Tolerance: Implement Checkpoint Size Calculation for Mixed-Precision Training'
scenario: You are implementing checkpointing for a 13B parameter model trained with ZeRO-2 on 64 H100s (80GB HBM3). The model uses bf16 parameters and fp32 master weights, AdamW optimizer.
question: What are the per-rank and total checkpoint sizes for the 13B ZeRO-2 job, and do they fit within 100GB of NVMe per node?
details:
  realistic_solution: 'ZeRO-2: parameters bf16 = 13B × 2B = 26GB (replicated on all ranks). Optimizer state (sharded): fp32 params + fp32 momentum + fp32 variance = 3 × 13B × 4B / 64 ranks = 2.44GB/rank. Full checkpoint/rank = 26GB + 2.44GB + gradient buffer (bf16, sharded) = 29.06GB.'
  common_mistake: |
    **The Pitfall:** Forgetting that ZeRO-2 shards optimizer state but NOT model parameters.
    **The Rationale:** Candidates assume all state is sharded under ZeRO-2, missing that all ranks still store the full bf16 parameters. They also frequently forget to account for fp32 master weights.
    **The Consequence:** Severely underestimating the per-rank checkpoint size, leading to out-of-memory errors on local NVMe.
  napkin_math: |
    - **Parameters (bf16, full replica):** 13B * 2B = 26 GB per rank.
    - **Optimizer state (fp32, ZeRO-2 sharded across 64 ranks):**
      - fp32 weights: 13B * 4B / 64 = 813 MB.
      - fp32 momentum: 813 MB.
      - fp32 variance: 813 MB.
      - Total optimizer/rank = 2.44 GB.
    - **Gradient buffer (bf16, sharded):** 13B * 2B / 64 = 406 MB.
    - **Total per rank:** 26 GB + 2.44 GB + 0.406 GB + ~0.01 GB (misc) = **28.86 GB (Fits in 100GB NVMe budget).**
    - **Total checkpoint:** 64 ranks * (2.44 GB + 0.406 GB) = 182 GB optimizer/grad + 26 GB params (de-dup) = **208 GB total.**
    - **Write Time:** Lustre write at 200 GB/s → 208 GB / 200 GB/s = **1.04 seconds.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
