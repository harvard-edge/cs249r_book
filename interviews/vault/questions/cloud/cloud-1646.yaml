schema_version: '1.0'
id: cloud-1646
track: cloud
level: L3
zone: fluency
topic: memory-mapped-inference
competency_area: memory
bloom_level: apply
phase: inference
title: Optimizing Dual-Socket Memory Bandwidth
scenario: You are deploying a compute-heavy PyTorch model on a dual-socket Intel Xeon server (2 NUMA nodes). Your current deployment launches 2 worker processes that share a single model instance in memory (defaulting to Node 0) and schedules threads randomly across all cores on both sockets.
question: What effective memory bandwidth does the shared Node-0 model get, and how should you reconfigure the workers for NUMA locality?
details:
  realistic_solution: Duplicate the model weights so a copy exists in each NUMA node's memory. Launch independent worker processes using `numactl --cpunodebind=N --membind=N` to ensure each worker only uses local cores and local memory.
  common_mistake: |
    **The Pitfall:** Only pinning CPU threads while leaving model weights in a single NUMA node.
    **The Rationale:** Candidates forget that accessing remote memory across the UPI link is a massive bottleneck.
    **The Consequence:** The application threads are pinned correctly, but they still saturate the slow cross-socket bus.
  napkin_math: |
    1. **Assumptions & Constraints:** 2 NUMA nodes, 300 GB/s local memory BW per socket, 64 GB/s cross-socket (UPI) limit. Model shared on Node 0.
    2. **Calculations:** Naive Setup: Node 0 gets 300 GB/s, Node 1 gets 64 GB/s (UPI bottleneck). Total Naive BW < 364 GB/s. NUMA Isolated (duplicated weights): Node 0 gets 300 GB/s AND Node 1 gets 300 GB/s. Total Isolated BW = 600 GB/s.
    3. **Conclusion & Interpretation:** **Result: Memory-Bandwidth Bound (Interconnect). Duplicating weights and pinning memory yields a ~1.6x+ improvement in effective bandwidth.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:00:33.347670'
