schema_version: '1.0'
id: cloud-3287
track: cloud
level: L4
zone: analyze
topic: memory-pressure-management
competency_area: memory
bloom_level: analyze
phase: both
title: 'TPU v5e Memory Management: Diagnosing OOMs in Large Model Training'
scenario: You are training a large language model on an accelerator pod with 16 GB of HBM per core. Your model's checkpoint size is 40GB, and activations for a global batch size of 2048 consume 10GB on each core. You offloaded optimizer states to host memory, yet you frequently encounter OOM errors.
question: Why do OOMs still occur on 16 GB HBM despite optimizer offload, and how would you reduce the peak memory footprint?
details:
  realistic_solution: Implement gradient accumulation with a reduced per-step batch size (e.g., 512) to significantly lower the peak activation memory footprint on HBM. Utilize activation checkpointing/re-materialization strategies to trade re-computation for memory savings. Employ TPU-specific memory profiling tools (like `tf.profiler`) to precisely identify memory hotspots, temporary allocations, and potential fragmentation. Investigate model architecture for opportunities to reduce activation size or re-shard model parameters more aggressively.
  common_mistake: |
    **The Pitfall:** Overlooking the combined peak memory requirements of activations and sharded weights.
    **The Rationale:** Engineers assume offloading optimizer states is sufficient to fit within device HBM, underestimating temporary allocations and activation memory.
    **The Consequence:** Unexpected OOM errors during training due to insufficient device memory capacity.
  napkin_math: |
    1. Assumptions & Constraints: 16 GB HBM limit. 40 GB weights sharded across 4 cores. Batch 2048 activations = 10 GB.
    2. Calculations: Weight memory per core = 40 GB / 4 = 10 GB. Total memory without accumulation = 10 GB (weights) + 10 GB (activations) = 20 GB (>16 GB limit). With grad accumulation (batch 512, count 4): Activation memory = 2.5 GB. Total = 10 GB + 2.5 GB = 12.5 GB.
    3. Conclusion & Interpretation: **Result: Gradient accumulation reduces peak memory to 12.5 GB. Bottleneck: Activation Memory Capacity.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- Reasoning Competency, Knowledge Area, and Mode are missing.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
