schema_version: '1.0'
id: cloud-3107
track: cloud
level: L5
zone: specification
topic: tco-cost-modeling
competency_area: deployment
bloom_level: create
phase: both
title: 'TCO Specification: Design Cost-Optimal Inference for SLO'
scenario: 'Specify the minimum-cost H100 inference configuration for LLaMA-65B (FP16: 4 H100s/pod, INT4: 1 H100/pod) with SLO: P99 < 2s, P50 < 500ms, throughput >= 1000 tokens/s. Each FP16 pod delivers 500 tok/s at $10/hr (4 GPUs). Each INT4 pod delivers 800 tok/s at $2.50/hr. Design the fleet mix meeting all SLOs at minimum cost, including fallback for peak load.'
question: What is the minimum-cost fleet mix of FP16 and INT4 pods to meet the throughput and latency SLOs, and what is its cost?
details:
  realistic_solution: 'To achieve 1000 tok/s at minimum cost, one might naively choose 2 INT4 pods (1600 tok/s for $5/hr). However, INT4 dequantization overhead often causes tail latency spikes that violate a strict P99 < 2s SLO during peak load. To guarantee the SLO while minimizing cost, a hybrid fleet mix is required: 1 FP16 pod (500 tok/s) to absorb latency-sensitive and tail requests, and 1 INT4 pod (800 tok/s) to process the bulk throughput. This fleet delivers 1300 tok/s total, satisfying the throughput and latency SLOs at a cost of $12.50/hr.'
  common_mistake: Choosing purely on throughput-per-dollar without verifying that INT4 quantization meets the latency SLO (quantization adds dequantization overhead that can increase P99 latency).
  napkin_math: '2 INT4 pods = $5/hr, 1600 tok/s (fails P99). 1 FP16 + 1 INT4 = $10 + $2.50 = $12.50/hr, 1300 tok/s (meets throughput and P99 SLO). Minimum cost fleet is $12.50/hr.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
