schema_version: '1.0'
id: cloud-3704
track: cloud
level: L5
zone: mastery
topic: attention-scaling
competency_area: memory
bloom_level: create
phase: both
title: Flash Attention Arithmetic Intensity Analysis on H100
scenario: You're implementing FlashAttention-2 for a 70B LLM with 96 heads, head_dim=128,
  seq_len=8192. H100 has 80MB L2 cache and 50MB SRAM across all SMs. Standard attention
  would materialize a 96×8192×8192 float16 attention matrix (12.8GB). Calculate the
  memory savings and bandwidth improvement from FlashAttention tiling.
question: How much HBM traffic does FlashAttention-2 save versus materializing the
  96×8192×8192 attention matrix, and what bandwidth speedup results?
details:
  realistic_solution: 'Standard attention HBM reads: Q+K+V = 3×96×8192×128×2 = 603MB.
    HBM writes: attention matrix = 96×8192×8192×2 = 12.8GB. Total standard = 603MB
    read + 12.8GB write + 12.8GB read (for softmax) + 603MB write (output) ≈ 26.8GB.
    FlashAttention: Q+K+V reads = 603MB, O write = 201MB. Total ≈ 804MB — 33× less
    HBM traffic. At 3.35 TB/s, standard attention memory time = 26.8GB/3.35TB/s =
    8ms; FlashAttention = 804MB/3.35TB/s = 0.24ms.'
  common_mistake: Assuming FlashAttention eliminates all HBM reads/writes; it eliminates
    the attention matrix materialization but still reads Q, K, V once each.
  napkin_math: 'Standard: 12.8GB attn matrix × 2 (write+read) + 1.2GB QKV = ~26.8GB.
    FlashAttention: ~804MB. Speedup from bandwidth reduction alone: 26.8/0.804 = 33×.
    At 3.35 TB/s: 8ms → 0.24ms.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 18
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
