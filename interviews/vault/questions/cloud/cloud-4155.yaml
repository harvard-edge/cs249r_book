schema_version: '1.0'
id: cloud-4155
track: cloud
level: L4
zone: optimization
topic: tail-latency
competency_area: latency
bloom_level: apply
phase: inference
title: Queueing Theory Applied to GPU Batch Scheduling
scenario: 'Your H100 inference server batches requests every 20ms to maximize throughput. At 200 req/s, the average batch size is 4 requests.'
question: "Using M/D/1, what is the expected wait at 20ms batching, and why can a 50ms batch window improve P99 despite more queueing?"
details:
  realistic_solution: 'M/D/1 queue with 20ms batch window: avg wait = 10ms (half the window). Per-request compute at batch=4: decode takes 12ms per token (weights loaded once for 4 requests). Total = 10ms wait + 12ms compute = 22ms per request. With 50ms window: avg wait = 25ms. Per-request compute at batch=10: weights loaded once for 10 requests, decode = 5ms per token (memory bandwidth amortized). Total = 25ms + 5ms = 30ms average but P99 improves because: (1) larger batches have lower variance in compute time, (2) GPU utilization increases from 30% to 75%, reducing queueing from bursty arrivals. Optimal window: ~35ms, balancing wait time against amortization.'
  common_mistake: |
    **The Pitfall:** Assuming larger batches always strictly increase tail latency.
    **The Rationale:** Focusing solely on the increased queueing delay without considering the reduction in per-request compute time.
    **The Consequence:** Keeping batch windows too small, resulting in poor memory bandwidth amortization and ironically worse P99 under burst load.
  napkin_math: |
    1. **Batch=4 (20ms window):** Avg wait = 10ms. Compute = 12ms. Total = **22ms**.
    2. **Batch=10 (50ms window):** Avg wait = 25ms. Compute = 5ms (amortized weight loading). Total = **30ms**.
    3. **P99 Analysis:** At batch=4, bursts overwhelm the GPU (2.4x parallelism needed) causing P99 ~80ms.
    4. At batch=10, the GPU easily handles the load (1x per cycle), keeping the queue predictable with P99 ~55ms.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
