schema_version: '1.0'
id: cloud-3315
track: cloud
level: L5
zone: specification
topic: queueing-theory
competency_area: latency
bloom_level: analyze
phase: inference
title: Optimizing Real-Time ML Inference Latency on Google TPU v5e with Queueing Theory
scenario: You are designing an online inference service for a critical real-time ML model. Your target average inference latency is 50ms, with a 99th percentile latency not exceeding 150ms. Incoming requests arrive at a peak rate of 10,000 queries per second (QPS). Each inference on a single TPU v5e takes approximately 20ms under optimal load. Due to the high arrival rate and the fixed service time per TPU, you observe a significant increase in queueing delays as traffic approaches capacity.
question: How many TPU v5e devices and what queueing strategy would you use to serve 10,000 QPS with 50 ms average latency and 150 ms P99?
details:
  realistic_solution: Apply an M/M/c queueing model (or more advanced models if traffic patterns are non-Poisson) to determine the optimal number of TPU v5e devices ('c') required to maintain the target P99 latency under peak load. Implement dynamic scaling policies based on real-time queue depth, resource utilization, and predicted traffic patterns. Utilize intelligent load balancing strategies (e.g., least-outstanding-requests) that consider individual TPU utilization to prevent hot-spotting and ensure requests are routed to the least busy worker. Implement admission control or flow control mechanisms to gracefully handle extreme traffic spikes.
  common_mistake: |
    **The Pitfall:** Ignoring queueing delays and static provisioning based only on single-device inference time.
    **The Rationale:** Candidates calculate theoretical minimums and assume 100% utilization is safe, ignoring traffic bursts.
    **The Consequence:** Severely underestimating capacity needs and causing SLA breaches during load.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Peak Arrival Rate: 10,000 QPS.
       - Single TPU Service Time: 20ms (50 QPS).
       - Target Utilization (ρ) for SLA: ~0.7.
    2. **Calculations:**
       - Theoretical Minimum: 10,000 / 50 = 200 TPUs.
       - M/M/c Requirement: c = 200 / 0.7 = 286 TPUs.
    3. **Conclusion & Interpretation:** **Result: Queueing-Delay-Bound.** Sizing for 286 TPUs ensures adequate headroom to absorb bursts and meet the P99 SLA.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null