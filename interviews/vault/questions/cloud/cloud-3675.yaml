schema_version: '1.0'
id: cloud-3675
track: cloud
level: L5
zone: implement
topic: mixture-of-experts
competency_area: optimization
bloom_level: apply
phase: both
title: MoE Auxiliary Loss Tuning for Stable Training
scenario: You are training a 32-expert MoE model on 16x H100. Your auxiliary load-balancing loss uses coefficient $\alpha=0.01$. After 5k steps, expert collapse occurs: 3 experts receive >80% of tokens. The training loss is good but perplexity on the validation set deteriorates.
question: Why is expert collapse occurring after 5k steps with $\alpha=0.01$, and how would you fix the MoE load-balancing loss?
details:
  realistic_solution: "Implement a staged α schedule: start at α=0.001 for first 1k steps (allow initial specialization), ramp to α=0.01 from 1k-5k (enforce balance), then settle at α=0.005. Add an entropy regularization term on the router distribution to prevent collapse. Log per-expert token counts every 100 steps as a training health metric."
  common_mistake: |
    **The Pitfall:** Using a fixed $\alpha$ without a warmup schedule.
    **The Rationale:** Treating the load-balancing coefficient as a static hyperparameter rather than a dynamic constraint.
    **The Consequence:** Setting $\alpha$ too high early prevents specialization; setting it too low (like 0.01 here) allows the network to fall into the local optima of expert collapse.
  napkin_math: |
    *   **Target Load:** Each of 32 experts handles ~1/32 = 3.1% of tokens.
    *   **Current Load:** 3 experts handle 80% (26.7%/each), 29 share 20% (~0.69%/each).
    *   **Imbalance Ratio:** 26.7 / 0.69 $\approx$ 38.7×.
    *   **Auxiliary Loss Impact:** $\alpha=0.01$ contributes $0.01 \times \text{load\_balance\_loss} \approx 0.01 \times 0.35 \approx 0.0035$ to the total loss.
    *   **Conclusion:** This is fundamentally too small relative to a Cross-Entropy loss of ~2.5 at step 5k, allowing the model to ignore the balancing constraint entirely.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
