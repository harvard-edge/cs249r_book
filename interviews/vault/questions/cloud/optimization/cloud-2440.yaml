schema_version: '1.0'
id: cloud-2440
track: cloud
level: L4
zone: analyze
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: Continuous Batching Arithmetic Shift
scenario: An API serves a 15B parameter LLM. With a static batch size of 2, decoding latency is 35ms per token. They upgrade to continuous batching, increasing average batch size to 16. A PM is concerned that processing 16 tokens simultaneously will increase the per-token generation latency to roughly 280ms, violating SLA.
question: Will continuous batching from batch size 2 to 16 raise per-token latency to 280ms, and why?
details:
  realistic_solution: No. Continuous batching does not turn 16 parallel decode tokens into 16 serial token steps. It forms a larger matrix operation for the current decode step, so the batch service time is still dominated by roughly the same weight-streaming pass while more tokens share that pass. The PM's 280ms estimate would be a serial-processing estimate or a confused total-work calculation, not the per-token compute latency of a continuous batch. Per-token latency remains near the original ~35ms, while throughput can rise substantially; queueing delay or batch formation delay may increase separately and must be managed by the scheduler.
  common_mistake: |
    **The Pitfall:** Assuming decode latency scales linearly with batch size.
    **The Rationale:** Candidates misunderstand that auto-regressive decoding at low batch sizes is entirely memory-bound, not compute-bound.
    **The Consequence:** Incorrectly rejecting a throughput optimization under the false assumption that it will degrade latency.
  napkin_math: |
    **Assumptions & Constraints:**
    - Model: 15B parameter LLM (FP16, 30GB weights).
    - Hardware: 864 GB/s HBM, 362 TFLOPS compute.
    - Decode step uses one weight pass for the active microbatch.

    **Calculations:**
    - GPU Ridge Point: 362 TFLOPS / 864 GB/s = 418 FLOPs/Byte.
    - Work per token ≈ 2 FLOPs/parameter * 15B parameters = 30 GFLOPs/token.
    - Weight traffic per decode step ≈ 30 GB, shared by the tokens in the microbatch.
    - Arithmetic intensity (BS=2): (2 tokens * 30 GFLOPs/token) / 30 GB = 2 FLOPs/Byte.
    - Arithmetic intensity (BS=16): (16 tokens * 30 GFLOPs/token) / 30 GB = 16 FLOPs/Byte.
    - Memory Bound Check: Both 2 ≪ 418 and 16 ≪ 418.
    - Memory floor for the decode step: 30 GB / 864 GB/s = 34.7 ms.

    **Conclusion & Interpretation:**
    - **Result: Per-token compute latency does not become 280ms.** The larger batch shares the weight-streaming pass, so the decode step remains around the 35ms memory floor while producing up to 8x more tokens than batch size 2. End-to-end latency can still include scheduler queueing delay.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
