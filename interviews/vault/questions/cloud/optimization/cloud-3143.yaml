schema_version: '1.0'
id: cloud-3143
track: cloud
level: L6+
zone: mastery
topic: flash-attention
competency_area: optimization
bloom_level: evaluate
phase: inference
title: Full-Stack FlashAttention Deployment Decision
scenario: 'You are the Staff ML engineer deciding the attention implementation for a new 405B model served on 64x H100 (8 nodes, NVLink + InfiniBand 400 Gb/s). Context window: 32K. Requirements: P99 TTFT < 2s, 1000 req/s throughput.'
question: What is the complete attention stack, KV cache management, sequence parallelism, and prefix caching strategy, and why is 810 TFLOPs per token a unit error?
details:
  realistic_solution: 'Utilize FlashAttention-2 with block-sparse reads for PagedAttention compatibility. 405B model requires 810 GFLOPs/token, not 810 TFLOPs. At 989 TFLOPS on H100, this is ~0.82ms/token.'
  common_mistake: |
    **The Pitfall:** Treating attention kernel, KV cache manager, and caching layer as independent choices.
    **The Rationale:** Engineers often evaluate optimizations in isolation without considering architectural interplay.
    **The Consequence:** Designing an incompatible stack leading to sub-optimal throughput or implementation failure.
  napkin_math: |
    **Assumptions & Constraints:**
    - 405B model parameters.
    - H100 compute at 989 TFLOPS.
    - P99 TTFT < 2s, 1000 req/s.
    
    **Calculations:**
    - FLOPs per token: 2 * 405B = 810 GFLOPs/token (not 810 TFLOPs).
    - Time per token at 100% util: 810G / 989T = 0.819ms per token.
    - TP=16 throughput: requests span GPUs, so 16 / 0.5s = 32 req/s.
    
    **Conclusion & Interpretation:**
    - **Result: 810 GFLOPs/token (Compute-Bound)**. A unit error (TFLOPs vs GFLOPs) drastically skews compute boundary estimations.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 'Unit error: 405B model takes 810 GFLOP/token, not 810 TFLOP.'
- 'Throughput calculation incorrectly assumes 16 concurrent requests per GPU. With Tensor Parallelism, requests span all GPUs, so throughput should be 16 / 0.5s = 32 req/s, not 2048.'
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
