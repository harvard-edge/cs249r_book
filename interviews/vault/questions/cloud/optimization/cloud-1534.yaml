schema_version: '1.0'
id: cloud-1534
track: cloud
level: L5
zone: evaluation
topic: compound-ai-systems
competency_area: optimization
bloom_level: evaluate
phase: both
title: Evaluating MMD Drift Detection Scale
scenario: You operate a RAG system serving 1,200 embedding queries/sec (~10^8 daily queries) using a 1024-dimensional model. To monitor drift, your team proposes computing exact Maximum Mean Discrepancy (MMD) with an RBF kernel between a 100,000-sample reference set and the full daily window of 10^8 queries. The drift job must run nightly on a single A100 (312 TFLOPs FP16) and finish within a 1-hour SLA.
question: Can exact nightly MMD between 100,000 reference embeddings and 10^8 daily queries finish on one A100 in 1 hour, and what should you use instead?
details:
  realistic_solution: Reject the exact quadratic MMD calculation. Instead, project the embeddings using Random Fourier Features (RFF) to approximate the RBF kernel, or use the linear-time unbiased MMD estimator. RFF allows MMD calculation as the distance between linear mean embeddings, reducing time complexity from O(N^2) to O(N).
  common_mistake: |
    **The Pitfall:** Focusing purely on systems engineering (e.g., chunking the matrix to avoid GPU OOM errors).
    **The Rationale:** Engineers often assume that OOM is the only barrier and that the GPU can compute any mathematically simple operation given enough time.
    **The Consequence:** Creating a system that theoretically doesn't OOM but introduces an insurmountable ExaFLOP compute bottleneck that physically cannot finish in the required time.
  napkin_math: |
    **Assumptions & Constraints:**
    - Daily queries M ≈ 10^8. Reference N = 10^5.
    - Embedding dimension d = 1024.
    - Approximate RBF kernel evaluation cost ≈ 2d = 2,048 FLOPs.
    - A100 peak FP16 throughput = 312 TFLOPs.

    **Calculations:**
    - The cross-sample term alone has N * M = 10^5 * 10^8 = 10^13 kernel evaluations.
    - Full exact MMD also includes within-sample terms, adding N^2 ≈ 10^10 and M^2 ≈ 10^16 evaluations, so the live-window term dominates.
    - Exact FLOPs ≈ 10^16 * 2,048 ≈ 2.05e19 FLOPs.
    - Ideal time = 2.05e19 / 312e12 ≈ 65,700 seconds ≈ 18.3 hours, so it misses the 1-hour SLA.
    - With RFF using r=4096 features, feature mapping costs about (N + M) * d * r * 2 ≈ 8.4e14 FLOPs, followed by a cheap mean-vector reduction.
    - Ideal RFF mapping time ≈ 8.4e14 / 312e12 ≈ 2.7 seconds, before practical overheads.

    **Conclusion & Interpretation:**
    - **Result: Exact MMD misses the nightly SLA (Compute Bottleneck)**. Use RFF mean embeddings or a linear-time MMD estimator; the approximation changes the job from an all-pairs kernel computation into a streaming feature aggregation.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T23:25:47.578738'
