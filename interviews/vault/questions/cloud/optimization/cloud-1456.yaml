schema_version: '1.0'
id: cloud-1456
track: cloud
level: L4
zone: diagnosis
topic: kernel-fusion
competency_area: optimization
bloom_level: analyze
phase: inference
title: Root-Causing Memory-Bound Activations
scenario: While profiling a PyTorch recommendation model on an NVIDIA GPU (2 TB/s HBM bandwidth), you find that a custom element-wise activation block `y = x * torch.tanh(a * x + b)` (where 'a' and 'b' are learned scalars) operating on a 250MB activation tensor takes roughly 1.1 milliseconds. Nsight Systems reveals 5% SM utilization but 95% HBM bandwidth utilization.
question: How do you analyze this bottleneck, and what is the structural cause within the framework?
details:
  realistic_solution: Diagnose this as a lack of kernel fusion. Eager-mode PyTorch executes this as four distinct operations sequentially. Analyze the profiler trace to see four separate kernels, each reading and writing intermediate tensors to HBM. Resolve this by using `torch.compile()` or writing a custom Triton kernel to fuse all element-wise operations into a single pass, eliminating intermediate memory materialization.
  common_mistake: |
    **The Pitfall:** Assuming the bottleneck is compute-related.
    **The Rationale:** Failing to account for the implicit intermediate tensor allocations caused by eager-mode execution.
    **The Consequence:** Trying to optimize floating-point math instead of fusing kernels to reduce HBM traffic.
  napkin_math: |
    - **Unfused traffic**: 5 Reads (1.25GB) + 4 Writes (1.0GB) = 2.25GB.
    - **Ideal unfused time**: 2.25GB / 2TB/s = 1.125 ms.
    - **Fused traffic**: 1 Read (x), 1 Write (y) = 0.5GB.
    - **Ideal fused time**: 0.5GB / 2TB/s = 250 us.
    - **Result: Fusing yields a 4.5x theoretical speedup.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
