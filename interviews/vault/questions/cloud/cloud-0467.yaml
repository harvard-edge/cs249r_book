schema_version: '1.0'
id: cloud-0467
track: cloud
level: L3
zone: diagnosis
topic: collective-communication
competency_area: networking
bloom_level: apply
phase: both
title: The Two-Node Scaling Trap
scenario: You are training a 70B parameter model using 8-way tensor parallelism on a single server node
  with 8 H100 GPUs. The GPUs in this node are connected with NVLink 4.0. To accelerate training, you scale
  up to 16 H100s across two identical nodes, connected via InfiniBand NDR. You run the same 8-way tensor
  parallelism, but now with 2-way data parallelism. You observe that your training time-per-step barely
  improves, showing very poor scaling efficiency. Using the hardware specs provided, diagnose the most
  likely bottleneck that explains this poor scaling.
question: "What hardware bottleneck explains the poor scaling when moving from 8 H100s on one node to 16 H100s across two nodes?"
details:
  realistic_solution: The primary bottleneck is the inter-node InfiniBand connection. In the single-node
    setup, all communication for the tensor parallel all-reduce operation happens over the extremely fast
    900 GB/s NVLink. When scaling to two nodes, half of the all-reduce data must cross the much slower
    InfiniBand fabric that connects the nodes. This relatively slow link becomes the dominant factor in
    the step time, negating most of the performance gains from the additional GPUs.
  common_mistake: Engineers often misdiagnose scaling problems. They might incorrectly blame the GPU compute
    (suggesting the model is too small), the PCIe bus (misunderstanding the GPU-to-GPU communication path
    for tensor parallelism), or the NVLink itself (failing to recognize its massive bandwidth). The key
    is identifying the slowest path that the new configuration introduced.
  napkin_math: In 8-way Tensor Parallelism, activations (e.g., 500MB per layer) are all-reduced multiple
    times per step. On 1 node via NVLink (900 GB/s), an all-reduce takes ~0.55ms. Across 2 nodes via InfiniBand
    (50 GB/s), the transfer takes ~10ms. This 9.5ms penalty per layer ruins TP scaling.
  options:
  - The GPU compute is saturated; the model is too small for 16 GPUs.
  - The PCIe Gen5 bus connecting the GPUs to the CPU is saturated by the increased data parallelism.
  - The inter-node InfiniBand connection has become the primary bottleneck for the tensor-parallel all-reduce.
  - The intra-node NVLink bandwidth is insufficient for the 8-way tensor parallel collectives.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
chains:
- id: cloud-chain-427
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_status_pro: WARN
validation_issues_pro:
- The description of the two-node parallelism strategy ('8-way tensor parallelism... with 2-way data parallelism')
  is ambiguous about how the tensor parallel group is mapped to the nodes.
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
