schema_version: '1.0'
id: cloud-2975
track: cloud
level: L6+
zone: mastery
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: create
phase: training
title: Design Fault Tolerance for a 500B Model on 2,048 GPUs
scenario: You are responsible for reliability engineering for a 3-month training run of a 500B parameter model on 2,048 GPUs. The expected GPU failure rate is 1 per 150 days.
question: What fault-tolerance architecture for checkpointing, detection, recovery, and spares will keep the 500B, 2,048-GPU run above 95% uptime?
details:
  realistic_solution: Design a full architecture including async sharded checkpointing, health monitoring, a hot standby pool, automatic replacement, and proactive replacement based on ECC precursors.
  common_mistake: |
    **The Pitfall:** Treating fault tolerance exclusively as a checkpointing problem.
    **The Rationale:** Candidates focus on saving state but fail to design automated recovery mechanisms and spare node provisioning.
    **The Consequence:** Achieving high checkpoint frequency but still failing SLAs due to lengthy manual recovery times and lack of standby hardware.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 2,048 GPUs over 90 days. Failure rate: 1 per 150 days.
       - A GPU failure event removes one worker unless masked by hot spares.
       - Proactive replacement catches 70% of impending failures during maintenance with no training downtime.
       - Unexpected failures trigger automated spare activation and 2 minutes of job disruption.
    2. **Calculations:**
       - Expected GPU-failure occurrences: 2,048 * (90 / 150) = 1,228.8 GPU failures over the run.
       - Unexpected disruptive events after proactive catches 70%: 1,228.8 * 30% = ~369 events.
       - Automated recovery downtime: 369 events * 2 min = 738 min = 12.3 hours = 0.51 days.
       - Uptime from disruptive events: (90 - 0.51) / 90 = 99.4%.
       - Hot-spare pool sizing: keep at least expected concurrent/near-term replacements plus repair lag; for example, 1,229 failures / 90 days = 13.7 replacements/day, so a 2-day repair buffer needs ~28 spare GPUs plus margin.
    3. **Conclusion & Interpretation:**
       **Result: Reliability Bound. With proactive replacement, hot spares, and 2-minute automated recovery for the remaining failures, expected uptime is about 99.4%, exceeding the 95% target.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
