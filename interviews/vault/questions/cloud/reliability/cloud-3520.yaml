schema_version: '1.0'
id: cloud-3520
track: cloud
level: L5
zone: specification
topic: adversarial-robustness
competency_area: reliability
bloom_level: analyze
phase: both
title: 'Autonomous Systems: Adversarial Robustness and Reliability'
scenario: Your team is deploying a critical, real-time image recognition service for autonomous vehicles on a cloud infrastructure utilizing NVIDIA GPUs. The service processes millions of inferences per second. Due to the safety-critical nature, the system must be highly reliable and robust against adversarial attacks that could lead to misclassifications, such as subtle perturbations in input images or prompt injection if the model incorporates large language model (LLM) components for scene understanding.
question: How would you design the image recognition service to resist adversarial inputs and prompt injection while preserving low-latency throughput?
details:
  realistic_solution: 'Implement a multi-layered, hybrid defense strategy: 1) Employ a dedicated, lightweight pre-processing microservice (potentially on CPU or a smaller GPU) for initial input sanitization, noise reduction, and adversarial example detection. 2) Integrate adversarially robust models on the GPUs, trained with carefully selected, diverse adversarial examples. 3) Leverage GPU performance to run ensemble methods or multiple diversified models in parallel, using a voting or confidence-based mechanism to flag suspicious inferences. 4) For LLM components, implement strong input validation, ''red-teaming'' derived prompt engineering, and LLM firewall techniques at the API gateway level before prompts reach the accelerated LLMs. This offloads simpler checks and maximizes the GPUs for complex, robust inference.'
  common_mistake: |
    **The Pitfall:** Adding heavyweight adversarial filters, prompt-injection checks, or ensembles directly to every autonomous-vehicle inference path.
    **The Rationale:** Candidates focus on robustness coverage without budgeting the extra preprocessing, model passes, and LLM guardrail latency against the real-time perception SLO.
    **The Consequence:** The service becomes safer on paper but misses low-latency deadlines, causing dropped frames, stale scene understanding, or unsafe fallback behavior.
  napkin_math: |
    **Assumptions & Constraints:**
    - End-to-end perception latency budget: 50 ms.
    - Base image model p99 latency: 32 ms.
    - Lightweight input sanitization: 4 ms.
    - Optional robust second-pass or ensemble check: 18 ms.
    - LLM prompt-injection guardrail applies only to scene-language requests, not every image frame.

    **Calculations:**
    - Always-on base plus sanitization path = 32 ms + 4 ms = 36 ms, leaving 14 ms of budget.
    - Adding an 18 ms second pass to every frame gives 36 ms + 18 ms = 54 ms, exceeding the 50 ms SLO.
    - If the second pass is triggered only for 5% suspicious frames, average added latency is 0.05 * 18 ms = 0.9 ms, while suspicious frames can be routed to a slower safe path.

    **Conclusion & Interpretation:**
    - **Result: Latency-budget-bound robustness.** Keep cheap defenses inline, gate expensive adversarial or LLM checks by risk, and define safe fallback behavior when robust checks cannot finish inside the real-time deadline.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
