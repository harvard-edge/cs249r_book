schema_version: '1.0'
id: cloud-2136
track: cloud
level: L4
zone: diagnosis
topic: feature-store-management
competency_area: data
bloom_level: analyze
phase: both
title: The Embedding Sharding Strategy
scenario: Your production DLRM has 50 embedding tables totaling 400 GB. Table sizes range from 200 MB to 80 GB. You have 8 GPUs with 80 GB HBM each. A naive row-parallel sharding splits every table equally across all 8 GPUs. The system runs, but GPU utilization is only 25% and All-to-All communication dominates the profile.
question: Why does row-parallel sharding cause massive communication bottlenecks here, and how do you fix it?
details:
  realistic_solution: 'Row-parallel sharding of all 50 tables across 8 GPUs means every forward pass requires an All-to-All collective to redistribute embeddings. For a 200 MB table, the All-to-All overhead far exceeds the compute. The fix is a mixed sharding strategy: Table-wise placement for small tables, Column-wise sharding for medium tables, and Row-wise sharding for large tables.'
  common_mistake: |
    **The Pitfall:** Assuming uniform sharding is universally optimal.
    **The Rationale:** Candidates blindly apply "split everything evenly" rules, forgetting small embeddings incur massive communication penalties.
    **The Consequence:** The network interface saturates with tiny lookup exchanges, crippling GPU utilization.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 50 tables, 8 GPUs.
    2. **Calculations:**
       - Naive Sharding: 50 tables x 8 GPUs = 400 fragments.
       - Small table (200MB) fragment = 25MB, still invokes full All-to-All.
    3. **Conclusion & Interpretation:**
       - **Result: Interconnect-Bound. Mixed strategy cuts communication volume by ~75% and boosts GPU utilization.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: cloud-chain-455
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
