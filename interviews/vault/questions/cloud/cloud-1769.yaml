schema_version: '1.0'
id: cloud-1769
track: cloud
level: L5
zone: optimization
topic: mlops-lifecycle
competency_area: precision
bloom_level: evaluate
phase: both
title: Evaluate QAT for LLM Serving
scenario: You are tasked with serving a 7B parameter transformer model on a single NVIDIA A10G (24GB VRAM). The FP16 model consumes 14GB for weights. Post-Training Quantization (PTQ) to INT8 causes an unacceptable 1.5% accuracy drop.
question: Why would INT8 QAT recover accuracy where PTQ failed for the 7B model, and what serving gains should you expect over FP16?
details:
  realistic_solution: Implement QAT by fine-tuning the pre-trained FP16 checkpoint with FakeQuantize nodes (simulating INT8 truncation) using the Straight-Through Estimator (STE) for gradients. QAT recovers accuracy by co-adapting weights to absorb the simulated rounding noise, whereas PTQ statically clips weights post-hoc. The INT8 model reduces weight footprint to 7GB, freeing 17GB for KV cache (vs 10GB for FP16), enabling significantly higher concurrency.
  common_mistake: |
    **The Pitfall:** Assuming QAT requires a full from-scratch training run.
    **The Rationale:** Candidates confuse Quantization-Aware "Training" with "Pre-training," overestimating the required compute.
    **The Consequence:** Teams abandon QAT due to perceived impossible compute budgets, settling for inferior accuracy or overly expensive FP16 hardware.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Hardware: 24 GB VRAM, 600 GB/s bandwidth.
       - FP16 model: 14 GB.
       - INT8 model: 7 GB.
    2. **Calculations:**
       - FP16 read time: 14 GB / 600 GB/s = 23.3 ms.
       - INT8 read time: 7 GB / 600 GB/s = 11.6 ms (2x speedup).
       - FP16 KV Cache capacity: 24 GB - 14 GB = 10 GB.
       - INT8 KV Cache capacity: 24 GB - 7 GB = 17 GB.
       - KV Cache relative increase: 17 / 10 = 1.7x (+70%).
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Capacity Bound. INT8 QAT prevents the 1.5% accuracy drop of PTQ while freeing 7 GB, boosting max concurrency by 70%.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:37:39.803293'
