schema_version: '1.0'
id: cloud-3718
track: cloud
level: L4
zone: realization
topic: model-serving-infrastructure
competency_area: memory
bloom_level: apply
phase: inference
title: PagedAttention Memory Fragmentation Reduction for H100 LLM Serving
scenario: A vLLM deployment on an 80GB H100 serves a 13B model (26GB), leaving 54GB for the KV cache. With standard contiguous KV allocation, average memory fragmentation is 35%. With PagedAttention, fragmentation drops to 4%. The 13B model has 40 layers, 40 attention heads, and a head dimension of 128.
question: How much throughput improvement should PagedAttention provide by reducing KV-cache fragmentation from 35% to under 4%?
details:
  realistic_solution: 'KV cache per token: 2 (K+V) × 40 layers × 20 heads × 128 head_dim × 2B = 2×40×20×128×2 = 409.6KB wait — recalculate: per token per layer = 2×n_heads×head_dim×2B = 2×40×128×2 = 20.5KB. Per token all layers = 20.5KB × 40 = ... let me use: 13B has 40 layers, 40 heads, head_dim=128. Per token: 2×40×40×128×2B = 819.2KB/40 = 20.48KB × 40 layers total = 819KB/token across all layers. Wait: per token = 2 (KV) × n_layers × n_heads × head_dim × dtype = 2×40×40×128×2 = 819,200 bytes ≈ 819KB. Available for KV: 54GB. Contiguous (35% frag): usable = 54×0.65 = 35.1GB. Sequences: 35.1GB / (819KB × avg_len). At avg 1000 tokens: 35.1GB/819MB = 42 sequences. PagedAttention (4% frag): 54×0.96 = 51.8GB → 63 sequences. Throughput: 63/42 = 1.5× improvement.'
  common_mistake: |
    **The Pitfall:** Calculating KV cache per sequence using only context length.
    **The Rationale:** It's a common oversight to forget that the KV cache must store keys and values across all layers and attention heads, not just a single token embedding.
    **The Consequence:** Available concurrency and maximum batch sizes are vastly overestimated.
  napkin_math: |
    - **KV per token:** $2 \text{ (K+V)} \times 40\text{ layers} \times 40\text{ heads} \times 128\text{ dim} \times 2\text{ bytes} = 819.2\text{ KB}$.
    - **Contiguous Usable Cache:** $54\text{ GB} \times (1 - 0.35) = 35.1\text{ GB}$.
    - **Contiguous Token Capacity:** $35.1\text{ GB} / 819.2\text{ KB} \approx 42,846\text{ tokens}$.
    - **Paged Usable Cache:** $54\text{ GB} \times (1 - 0.04) = 51.84\text{ GB}$.
    - **Paged Token Capacity:** $51.84\text{ GB} / 819.2\text{ KB} \approx 63,281\text{ tokens}$.
    - **Throughput Gain:** $63,281 / 42,846 = 1.476$.
    - **Conclusion:** **Throughput increases by ~1.48x** due to larger effective batch sizes.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null