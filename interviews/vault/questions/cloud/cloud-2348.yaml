schema_version: '1.0'
id: cloud-2348
track: cloud
level: L4
zone: diagnosis
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: NUMA Boundaries in High-Throughput DMA
scenario: 'You are architecting a high-throughput video ingestion pipeline. A dual-socket AMD EPYC server with 8 GPUs processes 4K video frames. The CPU decodes frames and sends them to the GPUs via PCIe Gen4 x16 (32 GB/s theoretical). However, `nvidia-smi dmon` shows PCIe Rx bandwidth maxing out at 11 GB/s per GPU, and CPU usage is at 100% on a single thread. Inference is starved.'
question: 'Why is PCIe Rx capped at 11GB/s per GPU with one CPU thread saturated, and how would you fix the host transfer path?'
details:
  realistic_solution: 'The data transfer is bottlenecked by two architectural flaws. First, using pageable (unpinned) memory forces the CPU to perform a costly memcpy to a hidden pinned bounce buffer before DMA can occur, saturating a CPU core. Second, the system is crossing NUMA boundaries (e.g., a GPU attached to Socket 0 is reading memory allocated by Socket 1). This forces traffic over the slow inter-socket link (xGMI/UPI). The architecture must be updated to use pinned memory (`cudaHostAlloc`) and enforce strict NUMA affinity per GPU worker process.'
  common_mistake: |
    **The Pitfall:** Assuming the PCIe bus is physically degraded or bottlenecked.
    **The Rationale:** Treating memory allocations as uniform and ignoring host-to-device memory transfer mechanics.
    **The Consequence:** Attempting to compress video frames further or debugging hardware, rather than fixing the memory pinning and NUMA boundaries in code.
  napkin_math: |
    * Pageable memory transfer requires a CPU memcpy. Single-core memcpy bandwidth is typically ~10-15 GB/s, matching the 11 GB/s ceiling.
    * By pinning memory (`cudaHostAlloc`), the GPU's DMA engine pulls directly from RAM without CPU involvement.
    * Crossing NUMA boundaries limits bandwidth to the inter-socket link (UPI at ~16 GB/s).
    * **Result: Fixing pinning and NUMA affinity raises per-GPU throughput to >25 GB/s.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
