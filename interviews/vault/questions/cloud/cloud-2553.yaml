schema_version: '1.0'
id: cloud-2553
track: cloud
level: L3
zone: fluency
topic: latency-decomposition
competency_area: latency
bloom_level: apply
phase: both
title: Cloud Zero Copy Serialization L3 0
scenario: Your inference server receives 10,000 requests/second. Each request carries a 512-dim FP32 embedding vector.
question: At 10,000 requests/s with a 512-dim FP32 embedding per request, what CPU and bandwidth costs do JSON and FlatBuffers incur?
details:
  realistic_solution: 'Payload: 512 x 4 bytes = 2 KB per request (binary). JSON: ~8 KB total. JSON parsing at ~200 MB/s throughput consumes ~0.4 CPU cores plus GC pressure. FlatBuffers: 2 KB binary, zero parse cost. Total savings: 0.4 CPU cores + 75% bandwidth reduction.'
  common_mistake: |
    **The Pitfall:** Only comparing serialization CPU speed and ignoring bandwidth.
    **The Rationale:** Engineers often evaluate serialization formats based on benchmark micro-seconds without considering network payloads.
    **The Consequence:** Missing that zero-copy binary formats are 2-4x smaller than JSON, reducing network transfer time and serialization cost simultaneously.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Payload Size: 512 * 4 bytes = 2 KB binary (FlatBuffers).
       - JSON Size: 512 floats as text (~12 bytes each) + field names ≈ 8 KB.
    2. **Calculations:**
       - JSON Parsing: 8 KB * 10,000 req/s = 80 MB/s. Consumes 0.4 CPU cores.
       - FlatBuffers Parsing: 0 parse CPU, just pointer arithmetic.
       - Bandwidth: JSON uses 80 MB/s wire traffic. FlatBuffers uses 20 MB/s.
    3. **Conclusion & Interpretation:**
       - **Result: Bandwidth and CPU Bound.** FlatBuffers yields a 75% bandwidth savings and saves 0.4 CPU cores.
status: published
