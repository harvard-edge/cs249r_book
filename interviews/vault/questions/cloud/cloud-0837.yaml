schema_version: '1.0'
id: cloud-0837
track: cloud
level: L4
zone: diagnosis
topic: ab-rollout-strategies
competency_area: deployment
bloom_level: analyze
phase: both
title: A/B Testing Survivorship Bias via Latency Timeouts
scenario: You are A/B testing a new Transformer ranking model (Model B) against a BM25 baseline (Model A) with a 50/50 traffic split. Model A has a p99 latency of 15ms. Model B has a mean latency of 80ms, but a p99 of 450ms on long queries. The upstream API gateway enforces a hard 300ms timeout. The analytics team reports Model B's conversion rate per successful page load is 10% higher than Model A. However, the business team reports a 12% drop in total revenue for the treatment group.
question: What root cause in the system logs and A/B telemetry explains Model B's higher reported CVR but 12% lower revenue?
details:
  realistic_solution: 'Diagnose a latency-induced survivorship bias. The most complex, longest queries (often from highest-intent buyers with high Average Order Value) hit the 300ms gateway timeout and fail. The dashboard only calculates CVR on successful responses, hiding the dropped revenue. Fix: Implement a fast fallback to Model A when Model B processing time exceeds 250ms, or reduce maximum sequence lengths to bound tail latency strictly under the timeout.'
  common_mistake: |
    **The Pitfall:** Blaming the model's ranking quality or suggesting a model retraining.
    **The Rationale:** It's easy to assume a drop in revenue means the model's predictions are bad, completely missing the system-level timeouts that slice off a cohort of traffic.
    **The Consequence:** Teams waste cycles retraining models instead of fixing the systemic survivorship bias caused by the API gateway timeout dropping high-value complex queries.
  napkin_math: |
    - **Control (A) 1000 reqs:** 950 short @ 4% CVR = 38 conv (AOV $10 $\rightarrow$ \$380). 50 complex @ 4% CVR = 2 conv (AOV $47.50 $\rightarrow$ \$95). Total Rev = \$475.
    - **Treatment (B) 1000 reqs:** 50 complex hit 300ms timeout = 0 conv (\$0 Rev).
    - **Treatment (B) Successes:** 950 short succeed with +10% CVR boost = 4.4% CVR.
    - **Treatment (B) Conversions:** $950 \times 0.044 = 41.8$. Total Rev = $41.8 \times \$10 = \$418$.
    - **Dashboard CVR:** $41.8 / 950\text{ successful reqs} = 4.4\%$ (Looks like a 10% win!).
    - **Conclusion:** **True revenue dropped by (\$475 - \$418) / \$475 = 12%** due to latency timeouts silently dropping high-value queries.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: cloud-chain-000
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T20:49:41.215629'