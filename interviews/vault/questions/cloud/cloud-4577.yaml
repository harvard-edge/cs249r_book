schema_version: '1.0'
id: cloud-4577
track: cloud
level: L5
zone: fluency
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: apply
phase: both
title: Checkpoint Overhead for Megatron on H100
scenario: A 175B parameter model trains in FP16 using Adam optimizer on GPUs. Training state is saved every 4 hours to a shared file system providing exactly 100 GB/s write bandwidth.
question: Determine the time overhead percentage of checkpointing if training runs continuously and requires saving both weights and optimizer states.
details:
  realistic_solution: Adam requires 12 bytes/param (FP32 master weights, momentum, variance) + 2 bytes for FP16 weights = 14 bytes/param. Total state = 175B * 14 = 2.45 TB. Writing takes 2.45 TB / 100 GB/s = 24.5 seconds. Overhead over 4 hours (14400s) is 24.5 / 14400 = 0.17%.
  common_mistake: |
    **The Pitfall:** Only counting the FP16 model weights (2 bytes/param).
    **The Rationale:** Optimizer states, particularly Adam, have a massive memory footprint (master weights, momentum, variance) that must be saved.
    **The Consequence:** Underestimating checkpoint sizes and storage bandwidth requirements.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 175B parameters.
       - Adam optimizer footprint: 12 bytes/param.
       - FP16 weights: 2 bytes/param.
       - Write bandwidth: 100 GB/s.
    2. **Calculations:**
       - Total state per param: 12 + 2 = 14 bytes/param.
       - Total checkpoint size: 175B * 14 = 2.45 TB (2,450 GB).
       - Write time: 2,450 GB / 100 GB/s = 24.5 seconds.
       - Overhead: 24.5s / 14,400s (4 hours) = 0.17%.
    3. **Conclusion & Interpretation:**
       - **Result: I/O-Bound Overhead**. The Adam optimizer inflates the checkpoint size to 2.45 TB, requiring massive write bandwidth to maintain a 0.17% stall overhead.
status: draft
provenance: llm-draft
expected_time_minutes: 10
requires_explanation: true
validated: false
math_verified: false
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- gemini-generated
- target-fluency-L5
created_at: '2026-04-25'
