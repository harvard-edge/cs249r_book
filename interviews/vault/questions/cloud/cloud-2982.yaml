schema_version: '1.0'
id: cloud-2982
track: cloud
level: L3
zone: fluency
topic: feature-store-management
competency_area: data
bloom_level: apply
phase: both
title: 'Feature Store: Fluency — Feature Vector Size and Retrieval Latency Estimation'
scenario: 'Your recommendation system on H100 serves 50K requests/second. Each request needs user features (200 float32 features) and item features (100 float32 features). Features are stored in Redis.'
question: How much Redis memory and network bandwidth are needed for 10M users, 5M catalog items, and 50K requests per second?
details:
  realistic_solution: 'Redis hash storage overhead: raw data plus key, hash table overhead, allocator fragmentation ≈ 2-4× raw byte size for small hashes. Plan for 3× overhead.'
  common_mistake: |
    **The Pitfall:** Forgetting Redis overhead.
    **The Rationale:** Calculating memory using only raw byte sizes, ignoring hash maps, keys, and allocator fragmentation.
    **The Consequence:** Drastically under-provisioning Redis memory, leading to OOM evictions and system failure.
  napkin_math: |
    **Assumptions & Constraints:**
    - 10M users, 200 float32 features/user (800 bytes).
    - 5M items, 100 float32 features/item (400 bytes).
    - Redis hash overhead is 3x.
    - 50K RPS, fetching 1 user and 1 item per request.

    **Calculations:**
    - User features: 10M * 800 bytes * 3 = 24 GB.
    - Item features: 5M * 400 bytes * 3 = 6 GB.
    - Total Redis Memory: 24 GB + 6 GB = 30 GB.
    - Network Bandwidth: 50K * (800 + 400) bytes = 60 MB/s.

    **Conclusion & Interpretation:**
    - **Result: 30 GB Memory and 60 MB/s Bandwidth (Memory-Capacity-Bound).**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
