schema_version: '1.0'
id: cloud-3913
track: cloud
level: L3
zone: fluency
topic: software-portability
competency_area: cross-cutting
bloom_level: understand
phase: both
title: Evaluating torch.compile Backend Portability Across Accelerators
scenario: 'PyTorch 2.x''s torch.compile offers multiple backends: ''inductor'' (default,
  CUDA/CPU), ''openxla'' (TPU), and experimental AMD support. You want to write model
  code that torch.compile can optimize for any backend. What are the portability constraints?'
question: What are the portability constraints?
details:
  realistic_solution: 'For maximum portability: (1) Avoid torch.autograd.Function
    custom ops (cause graph breaks on all backends). (2) Use only standard torch ops
    from the core aten operator set. (3) Avoid data-dependent control flow (breaks
    XLA backend). (4) Avoid in-place ops on views (breaks inductor fusion). (5) Test
    with TORCH_COMPILE_DEBUG=1 on each backend to identify backend-specific graph
    breaks. The inductor backend for CUDA generates Triton kernels; the AMD path generates
    similar Triton but with ROCm-specific tuning; the XLA path generates HLO graphs.'
  common_mistake: Assuming torch.compile makes any PyTorch code automatically portable.
    In practice, each backend has different graph break triggers, and inductor's CUDA
    codegen is far more mature than the AMD or XLA backends.
  napkin_math: 'Typical torch.compile speedup on CUDA inductor: 1.3-2x over eager
    mode. On AMD via inductor: 1.1-1.5x (less tuned). On TPU via openxla: 1.5-3x (XLA
    benefits most from whole-graph compilation). The variance is the portability tax.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
