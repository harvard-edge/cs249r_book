schema_version: '1.0'
id: cloud-1649
track: cloud
level: L3
zone: fluency
topic: streaming-ingestion
competency_area: data
bloom_level: apply
phase: both
title: Direct S3 Streaming Bandwidth for A100 Clusters
scenario: Your distributed training cluster with 64 A100 GPUs needs to process a 10TB dataset of 1MB image TFRecords. The data is currently sitting in S3 Standard. Each A100 processes 2,000 images per second. You need to stream the data directly to the compute nodes without an intermediate parallel filesystem like Lustre.
question: What network bandwidth and how many concurrent HTTP byte-range requests are required per 8-GPU node to stream the 1MB TFRecords?
details:
  realistic_solution: Use a highly concurrent data loader issuing parallel byte-range requests. A node with 8 GPUs needs 16 GB/s (128 Gbps) of bandwidth, requiring at least a 200 Gbps network interface (or dual 100 Gbps) and 320 concurrent HTTP connections.
  common_mistake: |
    **The Pitfall:** Assuming a single HTTP connection or a small pool can saturate training throughput.
    **The Rationale:** Candidates fail to realize that per-node bandwidth requirements for image training far exceed standard 10Gbps/25Gbps interfaces and typical HTTP connection limits.
    **The Consequence:** The expensive GPU cluster is completely starved for data because the network adapter or HTTP client pool is bottlenecked.
  napkin_math: |
    - Node capacity: 8 GPUs/node * 2,000 images/sec = 16,000 images/sec per node.
    - Ingestion rate: 16,000 images/sec * 1MB/image = 16,000 MB/s = 16 GB/s per node.
    - Network interface requirement: 16 GB/s * 8 bits/byte = 128 Gbps (requires at least a 200 Gbps NIC).
    - HTTP concurrency: 16,000 MB/s / 50 MB/s per connection = 320 concurrent HTTP connections.
    - **Conclusion: Each node requires a 200 Gbps NIC and at least 320 concurrent HTTP byte-range requests to keep the GPUs fed.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: cloud-chain-284
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:02:44.376057'
