schema_version: '1.0'
id: cloud-3053
track: cloud
level: L4
zone: realization
topic: model-size-estimation
competency_area: memory
bloom_level: apply
phase: both
title: 'Model Size Estimation: Realize Full Training Memory Budget for 7B Model on 4×H100'
scenario: You need to size the per-GPU memory budget for fine-tuning a 7B model on 4 GPUs (80GB each) with ZeRO-2. You are using mixed precision (BF16 weights and activations, FP32 optimizer states) with a batch size of 8 per GPU, sequence length of 2048, and gradient checkpointing every 4 layers (10 checkpoints for 40 layers).
question: What is the per-GPU memory budget for fine-tuning a 7B model on 4 GPUs with ZeRO-2, and does it fit in 80GB?
details:
  realistic_solution: 'ZeRO-2: weights replicated (not sharded). Gradients + optimizer states sharded. Per GPU: Weights BF16: 7B×2B=14GB (replicated). Gradients BF16: 14GB/4=3.5GB (sharded). Adam FP32 (m+v): 7B×4B×2/4=14GB (sharded). Activations (batch=8, seq=2048, gradient checkpoint every 4 layers, 40 layers → 10 checkpoints): 10×8×2048×4096×2B=1.34GB. Total: 14+3.5+14+1.34=32.84GB < 80GB ✓. Utilization: 32.84/80=41.1%.'
  common_mistake: |
    **The Pitfall:** Incorrectly sharding the base parameters under ZeRO-2.
    **The Rationale:** Candidates often confuse ZeRO-2 with ZeRO-3, assuming everything is equally divided by the number of GPUs.
    **The Consequence:** Severe underestimation of the model memory footprint, potentially selecting instances with insufficient RAM.
  napkin_math: |
    1. Weights (BF16, Replicated): 7B * 2B = 14 GB.
    2. Gradients (BF16, Sharded by 4): 14 GB / 4 = 3.5 GB.
    3. Optimizer (Adam FP32, Sharded by 4): 7B * 4B * 2 (states) / 4 = 14 GB.
    4. Activations (10 Checkpoints): 8 (batch) * 2048 (seq) * 4096 (hidden) * 2B = ~134 MB per checkpoint.
    5. Total Activations: 10 * 134 MB = 1.34 GB.
    6. **Result: Total per-GPU = 14 + 3.5 + 14 + 1.34 = 32.84 GB (Easily fits 80GB limit at 41% utilization).**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
