schema_version: '1.0'
id: cloud-3673
track: cloud
level: L5
zone: diagnosis
topic: mixture-of-experts
competency_area: parallelism
bloom_level: evaluate
phase: both
title: Expert Load Imbalance Killing Throughput in MoE
scenario: A 16-expert MoE model deployed across 4 GPUs processes a production batch. GPU utilization drops to 40% on 3 GPUs, while the fourth is pegged at 100%. Profiling shows expert #3 receives 68% of all tokens.
question: What causes one GPU to hit 100% utilization while the others idle, and how would you fix the load imbalance?
details:
  realistic_solution: 'Add expert capacity limits at inference: cap each expert buffer at capacity_factor × (batch_size / num_experts). Tokens exceeding the cap are dropped or redirected via a secondary ''overflow'' expert. Combine with online load statistics and periodically re-initialize the least-used experts with weights from overloaded ones.'
  common_mistake: |
    **The Pitfall:** Relying solely on the training auxiliary load-balancing loss in production.
    **The Rationale:** Assuming that because load was balanced during training, it will remain balanced under real-world domain shifts.
    **The Consequence:** Severe throughput degradation as all-reduce operations block waiting for the single overloaded GPU to finish.
  napkin_math: |
    - 16 experts $\to$ perfectly balanced load = $1/16$ (6.25%) of tokens per expert.
    - Expert #3 gets 68% of the load.
    - Because MoE requires All-to-All and All-Reduce syncs, the pipeline runs at the speed of the slowest node.
    - **The overloaded GPU is processing nearly 11× its fair share ($68 / 6.25 \approx 10.8\times$), stalling the other 3 GPUs entirely.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
