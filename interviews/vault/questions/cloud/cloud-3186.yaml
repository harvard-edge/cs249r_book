schema_version: '1.0'
id: cloud-3186
track: cloud
level: L4
zone: fluency
topic: mixture-of-experts
competency_area: architecture
bloom_level: apply
phase: both
title: MoE All-to-All Communication Volume
scenario: A Mixtral-style 8x7B MoE model uses top-2 routing with hidden dimension
  4096 in BF16. Each token dispatches to 2 of 8 experts across 8 GPUs (one expert
  per GPU). For a local micro-batch of 2048 tokens per GPU, estimate the all-to-all communication
  volume per layer in each direction.
question: For a local micro-batch of 2048 tokens per GPU, what is the all-to-all communication volume
  per layer in each direction?
details:
  realistic_solution: 'Each token sends its 4096-dim BF16 hidden state (8 KB) to 2
    experts. With uniform routing, each GPU sends 2048 × 2 / 8 = 512 token-states
    to each of the other 7 GPUs. Total outbound per GPU: 512 × 7 × 8 KB = 28 MB per
    direction. The dispatch all-to-all and combine all-to-all together move ~56 MB
    per GPU per layer. For 32 layers, that is ~1.8 GB per GPU per forward pass — a
    significant fraction of NVLink bandwidth at 900 GB/s.'
  common_mistake: Forgetting that all-to-all is bidirectional (dispatch + combine)
    and that each token sends its hidden state to 2 experts, not 1.
  napkin_math: 'Per token payload: 4096 × 2B = 8 KB. Tokens per GPU per expert (uniform):
    2048×2/8 = 512. Outbound per GPU: 512 × 7 × 8 KB ≈ 28 MB. Both directions: 56
    MB/layer. 32 layers: ~1.8 GB.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
