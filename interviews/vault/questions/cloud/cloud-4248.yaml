schema_version: '1.0'
id: cloud-4248
track: cloud
level: L5
zone: implement
topic: autograd-computational-graphs
competency_area: optimization
bloom_level: create
phase: training
title: Implementing gradient checkpointing for a custom attention mechanism
scenario: You have a custom sliding-window attention mechanism for processing 100K-token
  sequences on H100 (80GB). Standard torch.utils.checkpoint.checkpoint() wraps your
  attention block, but you notice the recompute during backward is 3× slower than
  the original forward because it also recomputes the KV cache generation that should
  be shared across windows.
question: How would you checkpoint the 100K-token sliding-window attention so backward
  does not recompute shared KV projections?
details:
  realistic_solution: 'Split the checkpointed region: pre-compute and save KV projections
    outside the checkpointed block (they are memory-cheap), checkpoint only the attention
    score computation (memory-expensive: O(seq_len²)). Use checkpoint with use_reentrant=False
    for better gradient correctness with nested checkpoints. Verify with torch.autograd.graph.saved_tensors_hooks()
    that the right tensors are being offloaded vs retained.'
  common_mistake: Wrapping the entire attention function (including KV projection)
    in checkpoint, causing KV projections to be recomputed for every window during
    backward even though they could be cached. The sliding-window design shares KV
    states across windows but checkpointing destroys this sharing.
  napkin_math: 'Attention scores for 100K tokens: 100K × 100K × 2 bytes = 20GB per
    layer. With 32 layers: 640GB — obviously cannot store all. KV projections: 100K
    × 8192 × 2 × 2 bytes = 3.2GB per layer, 102GB total. Checkpointing only attention
    scores (recompute) saves 640GB; keeping KV (12% overhead) avoids the 3× recompute
    penalty.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
