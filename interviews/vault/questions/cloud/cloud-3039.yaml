schema_version: '1.0'
id: cloud-3039
track: cloud
level: L4
zone: realization
topic: model-size-estimation
competency_area: memory
bloom_level: apply
phase: both
title: 'Model Size Estimation: Realize Memory Budget for Multi-Modal LLM on H100'
scenario: Concretely size the memory requirements for a multi-modal LLM (LLaVA-13B architecture) on an H100 GPU. Components include a CLIP ViT-L/14 vision encoder (307M params FP16), an MLP projection layer (20M params FP16), a LLaMA-2-13B language model (13B params FP16), and a KV-cache for a 4096-token context.
question: "Does LLaVA-13B fit on one 80GB H100 at batch 8 and 4096 context, and what is the maximum batch size?"
details:
  realistic_solution: Vision encoder 307M * 2B = ~0.6GB. MLP projection 20M * 2B = 40MB. LLaMA-2-13B 13B * 2B = 26GB. KV-cache per request is heavily dependent on layers and heads. Model safely fits with a maximum batch size of 15 before encountering OOM.
  common_mistake: |
    **The Pitfall:** Sizing only the language model and ignoring the vision encoder and projection layers.
    **The Rationale:** Candidates often focus solely on the massive LLM component when estimating memory for multi-modal models.
    **The Consequence:** While the vision encoder only adds ~614MB, ignoring it leads to an imprecise sizing estimate that could cause unexpected OOM errors at maximum batch size.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Vision + MLP params: 327M FP16.
       - LLM params: 13B FP16.
       - Context window: 4096 tokens.
       - KV-cache specs: 40 layers, 40 heads, 128 dim, FP16.
       - H100 VRAM: 80 GB.
    2. **Calculations:**
       - Vision + MLP size: 327M * 2 bytes = 0.65 GB.
       - LLM size: 13B * 2 bytes = 26.0 GB.
       - Base weights total: 26.65 GB.
       - KV-cache per token: 2 (K,V) * 40 * 40 * 128 * 2 bytes = 819.2 KB/token.
       - KV-cache per request: 4096 * 819.2 KB = ~3.355 GB.
       - Available for KV-cache: 80 GB - 26.65 GB = 53.35 GB.
       - Max batch size: floor(53.35 / 3.355) = 15.
    3. **Conclusion & Interpretation:**
       - **Result: Memory-Bound Capacity.** The model safely fits with a maximum batch size of 15. Total memory utilized at max batch is 26.65 + (15 * 3.355) = 76.98 GB.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 9
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
