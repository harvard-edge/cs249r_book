schema_version: '1.0'
id: cloud-4300
track: cloud
level: L5
zone: specification
topic: disaggregated-serving
competency_area: deployment
bloom_level: analyze
phase: inference
title: Prefill-Decode Affinity and Context Caching
scenario: Your service adds prompt caching: if a request shares a prefix with a recently processed request, the KV-cache for that prefix is reused.
question: "Where should prefix KV-caches live in a disaggregated architecture, how should they be invalidated, and what TTFT savings come from a 60% hit rate?"
details:
  realistic_solution: Store prefix KV-caches on decode nodes. On a cache hit, the request skips prefill entirely, and decode starts immediately without any KV transfer. Use consistent hashing on the prefix to route requests to the correct decode node. Invalidate via LRU eviction when KV pool utilization exceeds an 85% watermark.
  common_mistake: |
    **The Pitfall:** Storing prefix caches on prefill nodes.
    **The Rationale:** Candidates intuitively associate "prompt caching" with the "prefill phase" since prefill creates the cache.
    **The Consequence:** A cache hit avoids compute but still requires transferring the massive KV-cache over the network to the decode node, paying a severe latency penalty.
  napkin_math: |
    1. **Prefix size:** Assume 2048 tokens.
    2. **KV cache size:** 2 * 8 (KV heads) * 128 (head dim) * 2048 (tokens) * 96 (layers) * 2 bytes = ~805 MB = **768 MiB**.
    3. **Prefill time vs Network time:** Prefill takes ~10.2 ms. Network transfer at 50 GB/s takes ~15.4 ms. Cache hit on decode node saves ~25.6 ms total.
    4. **Expected savings:** At 60% hit rate, E[TTFT savings] = 0.6 * 25.6 ms = **15.4 ms per request**.
    5. **Bandwidth saved:** At 1000 RPS (600 hits), this frees up 600 * 768 MB = **~461 GB/s** of InfiniBand bandwidth.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
