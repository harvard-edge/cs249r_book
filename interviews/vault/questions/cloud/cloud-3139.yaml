schema_version: '1.0'
id: cloud-3139
track: cloud
level: L4
zone: fluency
topic: flash-attention
competency_area: optimization
bloom_level: apply
phase: inference
title: FlashAttention Arithmetic Intensity Calculation
scenario: On an H100 (989 TFLOPS FP16, 3.35 TB/s HBM3), compute the arithmetic intensity
  of the attention operation for seq_len=4096, head_dim=128, batch=1, 32 heads, and
  determine whether FlashAttention shifts it from memory-bound to compute-bound on
  the roofline.
question: What is the BF16 attention score matrix size for seq_len=4096, batch=1,
  32 heads, and why is 4GB wrong?
details:
  realistic_solution: S matrix = 2 × heads × seq² × bytes = 2 × 32 × 4096² × 2B =
    2.15 GB in BF16 (not 4 GB which uses FP32).
  common_mistake: Computing arithmetic intensity of the full fused kernel rather than
    the per-tile intensity, which is what actually determines whether each SM is compute-bound
    or memory-bound.
  napkin_math: 'S matrix (attention scores) size: batch × heads × seq × seq × bytes.
    For 4096 seq, 32 heads, BF16 (2B): 2 × 4096² × 2B × 32 heads = 2 × 16,777,216
    × 2 × 32 = 2,147,483,648 bytes = 2.15 GB (not 4 GB — 4 GB would require FP32 or
    double the heads). Arithmetic intensity: FLOPs / bytes accessed.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Math `2 x 4096^2 x 2B x 32 = 4GB` is incorrect; it is exactly 2 GiB (or ~2.14 GB),
  which makes the subsequent Arithmetic Intensity calculation wrong.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
