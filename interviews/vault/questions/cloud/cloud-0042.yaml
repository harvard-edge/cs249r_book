schema_version: '1.0'
id: cloud-0042
track: cloud
level: L2
zone: implement
topic: vram-budgeting
competency_area: memory
bloom_level: understand
phase: both
title: The KV-Cache VRAM Budget (cloud-0042)
scenario: You are scoping the VRAM requirements for serving a Llama-2 70B model with a 128,000 token context window. The model has 80 layers, a head dimension of 128, and uses Grouped-Query Attention (GQA) with 8 KV-heads. Assume a single user request with a batch size of 1.
question: How much VRAM is needed just for the FP16 KV-cache for one 128,000-token Llama-2 70B GQA request?
details:
  realistic_solution: The calculation accounts for sequence length, the number of layers, the number of KV heads, the dimension of each head, and the data type. The cache stores a Key and a Value vector for each token, requiring a 2x multiplier, and 2 bytes per element for FP16 precision.
  common_mistake: |
    **The Pitfall:** Using the wrong number of heads or forgetting the 2x factor for Keys and Values.
    **The Rationale:** Candidates conflate standard Multi-Head Attention query heads with Grouped-Query Attention KV heads, or miscalculate data type sizes.
    **The Consequence:** Drastically overestimating or underestimating memory requirements, leading to incorrect hardware provisioning.
  napkin_math: |
    - Dimensions: Sequence Length (S) = 128,000, Layers (L) = 80, KV Heads (H_kv) = 8, Head Dimension (D_h) = 128, Bytes (B) = 2 (FP16).
    - Formula: KV Cache Size = 2 * L * H_kv * D_h * S * B
    - Size = 2 * 80 * 8 * 128 * 128,000 * 2 = 41,943,040,000 bytes.
    - **Conclusion: The KV cache requires ~41.9 GB of VRAM.**
  options:
  - ~20 GB
  - ~140 GB
  - ~42 GB
  - ~335 GB
  correct_index: 2
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 2
deletion_reason: Duplicate of cloud-kv-cache-vram-accounting-the-kv-cache-vram-budget-25 (same title +
  track)
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
