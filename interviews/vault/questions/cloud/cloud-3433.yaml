schema_version: '1.0'
id: cloud-3433
track: cloud
level: L5
zone: specification
topic: 3d-parallelism
competency_area: parallelism
bloom_level: analyze
phase: training
title: Optimizing 3D Parallelism for Frontier LLM Training on AMD MI300X
scenario: You are tasked with training a 1 trillion parameter LLM on a cluster of accelerators (192 GB HBM3, 1,307 TFLOPS FP16). The model requires a total memory footprint of approximately 2 PB for activations and weights during training.
question: "How would you combine data, tensor, and pipeline parallelism on the cluster for a 1T-parameter LLM with a 2 PB training footprint?"
details:
  realistic_solution: 'Implement a hybrid approach: ZeRO-3 for data parallelism to offload optimizer states and gradients to host memory/NVMe where possible, significantly reducing memory footprint per MI300X. Tensor parallelism (e.g., Megatron-LM style) would be applied within tightly-coupled MI300X nodes (e.g., within a single server chassis) to exploit high intra-node bandwidth. Pipeline parallelism would then distribute model layers across these tensor-parallel groups, forming a larger distributed system. Quantization (e.g., FP8) should be explored to further reduce memory requirements.'
  common_mistake: |
    **The Pitfall:** Applying naive tensor parallelism across nodes without sufficient inter-node bandwidth.
    **The Rationale:** Candidates focus entirely on memory capacity (192GB per chip) and forget that TP is extremely communication-heavy.
    **The Consequence:** The training process becomes entirely communication-bound over the slower inter-node network, wasting the massive 1,307 TFLOPS compute capacity.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 1 trillion parameters, FP16 (2 bytes).
       - 192 GB HBM3 per accelerator.
    2. **Calculations:**
       - Weights: 1T * 2 bytes = 2 TB.
       - Activations/Optimizer States: 2-4x the weight memory ≈ 6-8 TB minimum (baseline).
       - Hardware capacity: 6000 GB / 192 GB ≈ 32 accelerators just to hold baseline static memory.
    3. **Conclusion & Interpretation:** **Result: Memory-Capacity-Bound**. Advanced parallelism (intra-node TP, inter-node PP, and ZeRO-3) is mandatory to fit the model and prevent inter-node bandwidth bottlenecks.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
