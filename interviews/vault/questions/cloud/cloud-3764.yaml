schema_version: '1.0'
id: cloud-3764
track: cloud
level: L5
zone: specification
topic: pipeline-parallelism
competency_area: parallelism
bloom_level: evaluate
phase: training
title: Pipeline Drain Latency in Inference Serving
scenario: You deploy a 175B model using PP=8 across GPUs for inference serving. Each stage takes 5ms per token. A user request generates 200 tokens.
question: "What is the time-to-first-token (TTFT) latency penalty from the pipeline, and how does continuous batching interact with pipeline parallelism for inference?"
details:
  realistic_solution: TTFT for a single request requires the prefill pass to go through all 8 stages sequentially. Throughput is improved via continuous batching by keeping stages full, but single-request latency is degraded.
  common_mistake: |
    **The Pitfall:** Thinking pipeline parallelism helps inference latency.
    **The Rationale:** Candidates confuse throughput optimizations with latency optimizations.
    **The Consequence:** Designing systems that increase TTFT because the request must traverse all P stages sequentially.
  napkin_math: |
    **Assumptions & Constraints:**
    - PP=8, 5ms/stage, 200 tokens.

    **Calculations:**
    - TTFT = 8 * 5ms = 40ms.
    - Total generation = 40ms TTFT + 200 * 40ms decode = 8.04 seconds.

    **Conclusion & Interpretation:**
    - **Result: PP adds an 8x latency penalty for a single request (Latency-Bound)**. Allows high aggregate throughput via continuous batching.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
