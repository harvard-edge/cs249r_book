schema_version: '1.0'
id: cloud-2983
track: cloud
level: L6+
zone: mastery
topic: feature-store-management
competency_area: data
bloom_level: create
phase: both
title: 'Feature Store: Mastery — Design Feature Store for Multi-Modal Real-Time LLM'
scenario: "You are designing a feature store for a multi-modal LLM serving system on 256 H100s handling 200K requests/second. Features include: user text embeddings (1536-dim float32 = 6KB), real-time session context (50 float32 = 200B), and image embeddings (2048-dim float16 = 4KB). P99 feature retrieval must be < 3ms."
question: What feature store architecture meets < 3ms p99 for 200K requests/s with text embeddings, session context, and image embeddings?
details:
  realistic_solution: 'Layered by staleness and size: text embeddings (updated daily, large, L2 cache) → session context (real-time, small, L1 per-server) → image embeddings (updated weekly, large, L2/L3). Different retrieval paths for different feature types.'
  common_mistake: |
    **The Pitfall:** Storing all feature types in a single, homogeneous feature store.
    **The Rationale:** Candidates optimize for architectural simplicity instead of accounting for the vastly different staleness requirements, sizes, and access frequencies of each feature type.
    **The Consequence:** The system over-provisions expensive low-latency storage for slow-moving data, or fails the P99 SLA due to bandwidth saturation.
  napkin_math: |
    - **Calculate feature bandwidth:** 200K RPS × (6KB + 0.2KB + 4KB) = ~2.04 GB/s total retrieval bandwidth.
    - **Analyze text embeddings:** 100M users × 6KB = 600GB. Store in L2 SSD. Cache hot 1M users in L1 memory (1M × 6KB = 6GB per server).
    - **Analyze image embeddings:** 50M items × 4KB = 200GB. Store in L2 SSD (1ms retrieval).
    - **Analyze session context:** 200B per request. Compute inline (L1 per-server) with ~0ms cost.
    - **Calculate total P99:** max(session 0ms, text 1ms, image 1ms) = **1ms** (meets the < 3ms SLA).
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
