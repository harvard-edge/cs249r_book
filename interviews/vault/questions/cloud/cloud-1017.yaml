schema_version: '1.0'
id: cloud-1017
track: cloud
level: L5
zone: evaluation
topic: pruning-sparsity
competency_area: optimization
bloom_level: evaluate
phase: training
title: Constant Folding Dense Normalization
scenario: You are deploying a DLRM-style recommendation model to Triton Inference Server on an NVIDIA L4 GPU. The ONNX graph contains a dense feature normalization subgraph processing 1 million FP32 features per request out = (x - mean) / sqrt(var + eps). The mean, var, and eps are static dataset statistics.
question: How much memory traffic does constant folding save for the 1M-feature normalization subgraph on an NVIDIA L4?
details:
  realistic_solution: Constant folding pre-computes scale = 1/sqrt(var+eps) and shift = -mean*scale at compile time, transforming the graph into a single Fused Multiply-Add (FMA) operation (x * scale + shift). On memory-bound GPUs, this eliminates multiple HBM roundtrips for intermediate tensors.
  common_mistake: |
    **The Pitfall:** Assuming the primary benefit is saving ALU compute cycles (FLOPs).
    **The Rationale:** Candidates focus on the square root and division operations.
    **The Consequence:** They fail to recognize that the critical bottleneck is HBM memory bandwidth and intermediate tensor materialization.
  napkin_math: |
    - 1. **Size:** 1M FP32 features = 4MB.
    - 2. **Unoptimized:** 4 sequential ops (add, sqrt, div, sub) require 4 HBM roundtrips = 4 × (4MB read + 4MB write) = 32MB of traffic.
    - 3. **Constant folded (FMA):** 1 read (X), 1 write (Out) = 8MB traffic.
    - 4. **Savings:** 32MB - 8MB = **24MB per inference (saves ~80µs of memory-bound latency)**.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
chains:
- id: cloud-chain-059
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null