schema_version: '1.0'
id: cloud-3001
track: cloud
level: L5
zone: evaluation
topic: kv-cache-management
competency_area: memory
bloom_level: evaluate
phase: inference
title: 'KV-Cache: Evaluate KV Cache Quantization Tradeoffs on H100'
scenario: For a 13B LLM on H100 serving 50 concurrent users with max 4096-token sequences, compare three KV cache strategies: (A) FP16, (B) INT8, (C) INT4.
question: How do FP16, INT8, and INT4 KV caches compare for HBM usage, decode throughput, and quality on this 13B workload?
details:
  realistic_solution: |
    KV quantization reduces memory and bandwidth, enabling more concurrent requests. Quality impact: INT8 typically < 0.5% perplexity increase, INT4 < 2%. Scale factors add overhead: 1 scale per group of 128 values.
  common_mistake: |
    **The Pitfall:** Assuming INT4 KV cache has exactly 4x less memory than FP16.
    **The Rationale:** Candidates forget to account for quantization overhead (scale factors and zero points).
    **The Consequence:** Memory budgets are overly optimistic, leading to OOM errors when deploying at max concurrency.
  napkin_math: |
    - **13B Model Specs:** 40 layers, 8 KV heads, head_dim=128.
    - **Tokens:** 50 users * 4096 tokens = 204,800 token slots.
    - **FP16 KV Size:** 204,800 * 163.84KB = 33.55GB. Total with 26GB weights = 59.55GB (Fits 80GB HBM).
    - **INT8 KV Size:** 16.78GB. Total = 42.78GB. Saves ~16.8GB, allowing ~50 more concurrent users.
    - **Decode Bandwidth:** FP16 takes ~5.01ms per step, INT4 takes ~1.25ms (4x faster decode throughput).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
