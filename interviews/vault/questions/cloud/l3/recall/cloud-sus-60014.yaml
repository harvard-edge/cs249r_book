schema_version: 1
id: cloud-sus-60014
title: Handling in-place operations and their effect on the autograd graph
topic: autograd-computational-graphs
status: published
provenance: imported
scenario: 'A team migrates a model from numpy to PyTorch on A100. They use in-place ReLU (x.relu_()) for
  memory efficiency. During training, they get a RuntimeError: ''one of the variables needed for gradient
  computation has been modified by an inplace operation''. This error appears intermittently, only with
  certain batch sizes.'
details:
  realistic_solution: 'Replace x.relu_() with F.relu(x) (out-of-place). The memory savings from in-place
    ops are marginal for ReLU (no new allocation, just overwrite). Reserve in-place ops for cases where
    you are certain the tensor is no longer needed as a backward input: e.g., in-place updates to buffers
    not in the compute graph. Use torch.autograd.set_detect_anomaly(True) to get a stack trace pointing
    to the exact in-place operation.'
  common_mistake: Using in-place operations on tensors that are inputs to other operations still in the
    graph. PyTorch's autograd tracks version counters; an in-place op increments the version. If a saved
    tensor's version at use-time differs from its version at save-time, PyTorch raises this error — but
    only when the backward pass actually reaches that node, explaining intermittent behavior.
  napkin_math: 'In-place ReLU saves 0 bytes of new allocation (overwrites existing tensor) but corrupts
    backward. Out-of-place ReLU requires 0 bytes for a view — in modern PyTorch, F.relu creates a view
    where possible. Net memory cost of switching to out-of-place: 0 bytes extra. The ''optimization''
    was never real.'
last_modified: '2026-04-02'
tags:
- scope:single-machine
