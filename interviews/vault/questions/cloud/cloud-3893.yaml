schema_version: '1.0'
id: cloud-3893
track: cloud
level: L6+
zone: mastery
topic: dataset-curation
competency_area: data
bloom_level: create
phase: training
title: VLM-Based Automated Curation at 100B Scale
scenario: You are tasked with designing an automated data curation pipeline for a
  new multimodal foundation model. You have a raw data lake of 100 billion image-text
  pairs (average 200KB per pair) in object storage. You need to use a 10B parameter
  Vision-Language Model (VLM) running in FP16 to score and filter this dataset down
  to the highest quality 5%. The VLM requires approximately 6 TFLOPs per sample for
  the forward pass. Design the end-to-end distributed curation architecture, identify
  the primary system bottleneck, and calculate the exact number of A100 (80GB) GPUs
  required to complete this job in exactly 14 days.
question: What distributed VLM scoring architecture would you use, what is the bottleneck,
  and how many A100 GPUs are needed to finish in 14 days?
details:
  realistic_solution: 'For a 10B VLM in FP16, the weights require ~20GB. The 80GB
    HBM2e on the A100 permits very large batch sizes, pushing the arithmetic intensity
    well past the A100''s ridge point of 156 FLOPs/Byte, making the workload strictly
    compute-bound. The architecture should utilize a decoupled pattern: a distributed
    queue streams metadata/URLs to stateless GPU worker nodes, which asynchronously
    pull raw bytes from S3, run inference batches, and sink the quality scores to
    a fast key-value store or analytical database.'
  common_mistake: Candidates often misidentify the bottleneck as object storage I/O
    or memory bandwidth without calculating the arithmetic intensity of a large-batch
    inference workload. They also frequently fail to apply a realistic hardware utilization
    factor (MFU), erroneously assuming the theoretical peak of 312 TFLOPS can be achieved.
  napkin_math: '1. Total Compute: 100B samples × 6 TFLOPs/sample = 600 ZFLOPs. 2.
    Effective Throughput: A100 peak is 312 TFLOPS FP16; assuming a realistic 40% MFU,
    we get ~125 TFLOPS per GPU. 3. Processing Rate: 125 TFLOPS / 6 TFLOPs = ~20.8
    samples/sec per GPU. 4. Time Constraint: 14 days = 1,209,600 seconds. 5. Samples
    per GPU: 20.8 × 1,209,600 = ~25.1 million samples. 6. Total GPUs: 100B / 25.1M
    = 3,984 A100 GPUs (provision 4,000 GPUs or 500 8-GPU nodes). 7. Network I/O Check:
    20.8 samples/s × 200KB = ~4.1 MB/s per GPU (~33 MB/s per node), verifying network
    is not the bottleneck.'
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
validation_issues:
- reasoning_competency and reasoning_mode are not specified.
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- 100B samples * 6 TFLOPs equals 600 ZFLOPs, not 600 EFLOPs
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
