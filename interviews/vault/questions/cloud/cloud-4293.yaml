schema_version: '1.0'
id: cloud-4293
track: cloud
level: L4
zone: analyze
topic: disaggregated-serving
competency_area: deployment
bloom_level: analyze
phase: inference
title: TTFT vs TPOT SLO Tension
scenario: 'Your disaggregated serving cluster has separate TTFT and TPOT SLOs: TTFT < 500 ms, TPOT < 50 ms per token. During a load spike the prefill queue grows. An engineer proposes migrating two decode GPUs to the prefill pool to reduce TTFT.'
question: "What does moving two decode GPUs to prefill fix and break, and what metric determines the right pool ratio?"
details:
  realistic_solution: 'Moving GPUs from decode to prefill reduces prefill queue depth and TTFT, but each decode GPU now serves more concurrent in-flight sequences. TPOT degrades because decode is already memory-bandwidth-bound: adding sequences increases HBM reads per step proportionally. The right signal is the ratio of prefill throughput (tokens/s processed) to decode throughput (tokens/s generated). If prefill throughput >> decode throughput, the bottleneck is decode and migrating to prefill worsens TPOT. Track P99 of both SLOs independently and model the system as two coupled M/D/c queues — find the pool split where both P99s are met with maximum total throughput.'
  common_mistake: |
    **The Pitfall:** Treating TTFT and TPOT as independent uncoupled metrics.
    **The Rationale:** Assuming that shifting compute to prefill only affects new requests without impacting in-flight decoding.
    **The Consequence:** Migrating capacity to prefill directly lengthens decode queues, increasing TPOT for all in-flight requests and causing massive SLO violations.
  napkin_math: |
    1. If prefill pool processes 200k tokens/s and decode pool generates 80k tokens/s, decode is the bottleneck.
    2. Moving 1 GPU from decode (losing ~10k tok/s) to prefill (gaining ~25k tok/s) **widens the bottleneck**.
    3. **Result:** The correct direction is moving prefill GPUs to decode until the throughput ratio equalizes at target utilization ($\rho < 0.8$).
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
