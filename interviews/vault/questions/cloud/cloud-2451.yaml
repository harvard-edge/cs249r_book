schema_version: '1.0'
id: cloud-2451
track: cloud
level: L5
zone: evaluation
topic: speculative-decoding
competency_area: optimization
bloom_level: evaluate
phase: inference
title: Speculative Decoding Draft Model Sizing
scenario: 'You are deploying a 70B parameter LLM for a code generation API with a strict Time Per Output Token (TPOT) budget of 20ms. Baseline TPOT is 35ms. You evaluate speculative decoding with two draft models: a 1.5B model (0.6 acceptance rate, 5ms/token) and a 7B model (0.85 acceptance rate, 12ms/token). The 70B target model evaluation step takes 15ms for verifying up to 5 tokens.'
question: Which draft model should you choose for speculative decoding, the 1.5B or 7B, to meet the 20ms TPOT budget?
details:
  realistic_solution: The correct choice is the 1.5B draft model. Speculative decoding performance is a strict tradeoff between the computational overhead of the draft model and the acceptance rate of the target model. The 1.5B model yields ~10.3ms/token, comfortably beating the 20ms budget. The 7B model yields ~14.3ms/token.
  common_mistake: |
    **The Pitfall:** Blindly choosing the draft model with the highest acceptance rate.
    **The Rationale:** Candidates assume a higher acceptance rate always leads to better performance, ignoring the latency overhead of running a larger draft model.
    **The Consequence:** Deploying a speculative decoding setup that is slower than autoregressive decoding or fails to meet strict latency SLAs.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - 4 tokens drafted per step. Target evaluation takes 15ms.
       - 1.5B Draft: 5ms/token, 0.6 acceptance rate.
       - 7B Draft: 12ms/token, 0.85 acceptance rate.
    2. **Calculations:**
       - 1.5B Draft Model:
         - Draft time = 4 * 5ms = 20ms.
         - Total step time = 20ms + 15ms = 35ms.
         - Expected generated tokens = 1 + (4 * 0.6) = 3.4 tokens.
         - Expected TPOT = 35ms / 3.4 = 10.29 ms/token.
       - 7B Draft Model:
         - Draft time = 4 * 12ms = 48ms.
         - Total step time = 48ms + 15ms = 63ms.
         - Expected generated tokens = 1 + (4 * 0.85) = 4.4 tokens.
         - Expected TPOT = 63ms / 4.4 = 14.31 ms/token.
    3. **Conclusion & Interpretation:**
       - **Result: Latency-Bound.** The 1.5B model achieves ~10.3 ms/token, comfortably beating the 20ms budget and outperforming the 7B draft model's overhead.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
math_verified: true