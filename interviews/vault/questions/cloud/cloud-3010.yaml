schema_version: '1.0'
id: cloud-3010
track: cloud
level: L5
zone: specification
topic: kv-cache-management
competency_area: memory
bloom_level: create
phase: inference
title: 'KV-Cache: Specification — KV Cache Budget for Multi-Model Serving'
scenario: 'Write the KV cache specification for a multi-model serving setup on a single H100: Model A (7B, 30 concurrent) and Model B (3B, 70 concurrent). Both use bf16. Define memory partitioning, priority policy, and overflow handling.'
question: What per-token KV-cache size should the single-H100 multi-model setup budget for its GQA-8 models?
details:
  realistic_solution: KV cache per token = 2 * 32 * 8 * 128 * 2B = 131,072 bytes ≈
    128KB (not 40KB).
  common_mistake: |
    **The Pitfall:** Miscalculating the per-token KV cache size or failing to account for multi-model HBM sharing.
    **The Rationale:** Candidates forget the factor of 2 for K and V, or fail to specify eviction policies when one model's requests surge.
    **The Consequence:** Out-of-memory errors (OOMs) in production when concurrent requests exceed the shared HBM boundary.
  napkin_math: |
    1. **Assumptions & Constraints:** GQA-8 model (32 layers, 8 KV heads, head_dim=128, FP16/BF16 2 bytes).
    2. **Calculations:** KV per token = 32 layers * 2 (K,V) * 8 heads * 128 dim * 2 bytes = 131,072 bytes = 128 KiB (~128KB).
    3. **Conclusion & Interpretation:** **Result: Memory-Bound. The accurate 128KB/token baseline is mandatory to define the single HBM partition across multiple concurrent models.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Model B KV per token arithmetic is incorrect (32*2*8*128*2 = 128KB, not 32KB)
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
