schema_version: '1.0'
id: cloud-4191
track: cloud
level: L5
zone: optimization
topic: gpu-compute-architecture
competency_area: compute
bloom_level: evaluate
phase: both
title: Shared Memory Bank Conflicts in Reduction Kernels
scenario: Your LayerNorm kernel on H100 computes a parallel reduction over 4096 elements using shared memory. Each warp reduces 128 elements, but profiling shows 8-way bank conflicts during the reduction phase. The shared memory has 32 banks, each 4 bytes wide. Diagnose the conflict pattern and fix it.
question: What shared-memory bank conflict pattern causes the 8-way slowdown in the LayerNorm reduction, and how would you fix it?
details:
  realistic_solution: 'Shared memory: 32 banks, 4 bytes wide. Address maps to bank: (address / 4) % 32. During tree reduction, thread i accesses element i and element i + stride. At stride = 4: threads 0,4,8,12,16,20,24,28 all read from banks 0,4,8,12,16,20,24,28 (no conflict). But threads 0,4,8,12,16,20,24,28 reading i+stride=4,8,12,16,20,24,28,32: bank pattern 4,8,12,16,20,24,28,0 — still no conflict. The 8-way conflict occurs at stride = 32: thread 0 reads index 0 (bank 0) and index 32 (bank 0). Threads 0-31 all read from bank (i % 32) for the first operand AND bank ((i+32) % 32) = same bank for the second. Fix: (1) Add a 1-element padding per row: declare shared mem as float smem[128+1]. Now bank(i) = (i + i/32) % 32, breaking the conflict pattern. (2) Use warp shuffle (__shfl_down_sync) instead of shared memory for intra-warp reduction — zero bank conflicts, ~2× faster.'
  common_mistake: |
    **The Pitfall:** Using a stride of 32 for reduction in shared memory.
    **The Rationale:** Thinking it avoids conflicts, but a stride of 32 maps to the exact same bank across threads.
    **The Consequence:** Maximum bank conflict leading to massive latency.
  napkin_math: |
    - Reduction of 128 elements requires 7 steps.
    - Step 1 (64 ops) requires 2 cycles per warp; steps 2-7 require 1 cycle each. Total = 8 cycles without conflicts.
    - With an 8-way bank conflict, the first steps take 8x longer.
    - Warp shuffle avoids shared memory entirely.
    - Result: **Warp shuffle is significantly faster by avoiding all bank conflicts.**
status: deleted
provenance: imported
requires_explanation: false
expected_time_minutes: 14
deletion_reason: Math error unfixable
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: ERROR
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- A 32-thread warp takes 2 cycles for the first step (64 operations) of a 128-element
  reduction, so it is not 1 cycle per step. Also, the bank conflict degree decreases
  at each reduction step, so multiplying 8 cycles by 7 steps is incorrect.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
