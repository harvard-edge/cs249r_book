schema_version: '1.0'
id: cloud-1619
track: cloud
level: L5
zone: evaluation
topic: federated-learning
competency_area: cross-cutting
bloom_level: evaluate
phase: both
title: Evaluating LLM Watermark Sequence Requirements
scenario: You implement a token-level watermarking scheme on an LLM API. The vocabulary is partitioned with a green list fraction $\gamma = 0.5$, yielding an empirical 80% green tokens. The legal team requires a statistical confidence $Z$-score $\ge 4$ to prove theft. The average API response length is 20 tokens.
question: Why is the watermark undetectable on 20-token responses, and what minimum sequence length is needed to reach $Z \ge 4$?
details:
  realistic_solution: The implementation fails because 20 tokens do not provide enough statistical significance to distinguish the watermark from random chance at a $Z \ge 4$ confidence level. The architectural solution is to either accumulate tokens across an entire user session before running the detection test, or drastically increase $\gamma$ (which severely degrades perplexity). The math proves 45 tokens are required.
  common_mistake: |
    **The Pitfall:** Focusing solely on the latency or compute overhead of watermarking.
    **The Rationale:** Ignoring the fundamental statistical limitations of the z-test on short generation sequences.
    **The Consequence:** Deploying a complex watermarking system that provides a false sense of security and zero legal protection.
  napkin_math: |
    - Z-score formula: $Z = \frac{|G| - \gamma T}{\sqrt{\gamma(1-\gamma)T}}$
    - We need $Z \ge 4$. With $\gamma = 0.5$ and empirical green token fraction $p = 0.8$, the expected $|G| = 0.8T$.
    - Substitute the values: $4 \le \frac{0.8T - 0.5T}{\sqrt{0.5 \times 0.5 \times T}}$
    - Simplify numerator: $0.3T$
    - Simplify denominator: $0.5\sqrt{T}$
    - Equation: $4 \le \frac{0.3T}{0.5\sqrt{T}} \implies 4 \le 0.6\sqrt{T}$
    - Solve for T: $\sqrt{T} \ge 6.667 \implies T \ge 44.44$
    - **At least 45 contiguous tokens are required, but the API only averages 20.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
chains:
- id: cloud-chain-273
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T23:52:54.674838'
