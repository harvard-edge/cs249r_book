schema_version: '1.0'
id: cloud-4417
track: cloud
level: L4
zone: diagnosis
topic: chiplet-architecture
competency_area: compute
bloom_level: analyze
phase: inference
title: XCD Hot-Spot Thermal Management Under Sustained Load
scenario: In production, MI300X TDP readings are uniform but sustained throughput
  on one job type drops 12% after 30 minutes. rocm-smi shows temperature uniform across
  XCDs but frequency drops on XCDs 0 and 1. Diagnose and fix.
question: Why do XCDs 0 and 1 throttle and reduce throughput by 12% despite uniform
  MI300X TDP and temperatures, and how would you fix it?
details:
  realistic_solution: 'XCDs 0 and 1 are throttling due to localized hotspots even
    though the package-level thermal sensor reads nominal. Root cause: the job''s
    memory access pattern creates non-uniform HBM utilization — XCDs 0-1 serve as
    the ''master'' ranks handling cross-XCD reduction which increases their compute
    and traffic load. Fix: (1) redistribute tensor parallel ranks so reduction work
    rotates across XCDs; (2) enable per-XCD frequency monitoring via rocm-smi --showclkfreq;
    (3) adjust power limit per XCD using ROCm SMI power cap features; (4) verify thermal
    interface paste distribution — chiplet packages with interposers can have uneven
    TIM coverage.'
  common_mistake: Assuming uniform TDP means uniform thermal load. The TDP reading
    is an aggregate; individual XCDs can have hotspots from unequal workload distribution
    (e.g., embedding lookups concentrated on certain XCDs by tensor parallelism rank
    assignment) while the average temperature appears normal.
  napkin_math: 'MI300X TDP: 750W over 8 XCDs = ~94W per XCD average. If XCDs 0-1 carry
    1.4x load: 130W each vs 83W for others. Junction temperature delta: 130W * 0.08°C/W
    thermal resistance = 10.4°C above average. If average is 75°C, XCDs 0-1 hit 85.4°C
    → frequency throttle threshold typically 85°C.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
