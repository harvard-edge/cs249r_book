schema_version: '1.0'
id: cloud-1771
track: cloud
level: L5
zone: specification
topic: interconnect-topology
competency_area: networking
bloom_level: evaluate
phase: both
title: Evaluating Rail-Optimized Topologies for Cross-Node TP
scenario: You are architecting a cluster of 1,024 H100 GPUs (128 nodes, 8 GPUs each) to train a large-scale LLM. The memory requirements force you to use a Tensor Parallelism (TP) degree of 16, which means TP groups must span across two nodes. You are evaluating two backend network designs: a standard leaf-spine where all 8 NICs on a node connect to a single top-of-rack (ToR) switch, and a rail-optimized topology where NIC i of every node connects to a dedicated switch i.
question: "For TP=16 spanning two nodes, how do standard leaf-spine and rail-optimized topologies affect cross-node TP AllReduce performance?"
details:
  realistic_solution: Choose the rail-optimized topology. Because cross-node TP AllReduce patterns are symmetric (GPU i on Node A communicates directly with GPU i on Node B), connecting all NIC 0s to Switch 0 creates 8 independent, parallel network planes. This physical isolation eliminates ECMP collisions and in-switch contention, guaranteeing predictable, minimum-latency 400Gbps line rate for the latency-sensitive TP collective.
  common_mistake: |
    **The Pitfall:** Relying solely on aggregate bisection bandwidth to evaluate network topologies for collective operations.
    **The Rationale:** Assuming that non-blocking standard fat-trees distribute traffic perfectly without considering hash collisions and microbursts.
    **The Consequence:** 30-50% congestion degradation due to ECMP collisions during highly synchronized TP operations, severely bottlenecking the training loop.
  napkin_math: |
    * **Data Movement:** 2GB cross-node AllReduce moves ~4GB per GPU.
    * **Rail-Optimized (Isolated Planes):** Hits 400Gbps (~50GB/s payload) linearly = 80ms latency.
    * **Standard ToR (ECMP Collisions):** Congestion reduces effective bandwidth to ~25GB/s.
    * **Standard ToR Latency:** 160ms latency (2x slower).
    * **Conclusion:** **Rail-optimized topologies isolate flows and eliminate ECMP collisions, halving cross-node TP latency.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: cloud-chain-329
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:37:58.835122'
