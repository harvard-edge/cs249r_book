schema_version: '1.0'
id: cloud-0955
track: cloud
level: L5
zone: evaluation
topic: adversarial-robustness
competency_area: reliability
bloom_level: evaluate
phase: both
title: Scaling Randomized Smoothing Certification
scenario: You are deploying an ML-based transaction fraud detection service on AWS EC2 (p4d.24xlarge, 8x A100 40GB) to process high-value financial transfers. You must implement randomized smoothing to provide certified robustness against L2-norm adversarial perturbations (radius R=0.5). Your base ResNet-50-equivalent model has an unbatched inference latency of 15ms. To achieve the required 99.9% certification confidence bound, the smoothing algorithm demands N=10,000 Gaussian-perturbed inferences per request.
question: How should you serve N=10,000 randomized-smoothing inferences under a 250ms P99 synchronous SLA?
details:
  realistic_solution: Implement aggressive dynamic batching and leverage the massive parallel capacity of the 8x A100s. A single request's 10,000 samples can be split into large batches (e.g., batch size 1024 or 2048) distributed across the GPUs. Alternatively, decouple the base prediction (served immediately) from the robustness certification (computed asynchronously).
  common_mistake: |
    **The Pitfall:** Treating certified robustness as purely a training-time property.
    **The Rationale:** Ignoring the operational requirement that randomized smoothing requires thousands of forward passes *at inference time*.
    **The Consequence:** Proposing sequential processing or single-GPU architectures that catastrophically violate the strict real-time latency SLAs.
  napkin_math: |
    - **Sequential Latency**: 15ms × 10,000 = 150 seconds (massively fails the 250ms SLA).
    - **Dynamic Batching**: With 8x A100s, use a batch size of 1250 per GPU (1250 × 8 = 10,000).
    - **Parallel Execution**: A ResNet-50-equivalent batch of 1250 takes ~150ms on an A100.
    - **Total Latency**: 150ms compute + ~20ms framework/network overhead = **170ms**.
    - **Result**: By leveraging massive parallel batching across 8 GPUs, the 170ms total time safely meets the 250ms SLA.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
chains:
- id: cloud-chain-038
  position: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T21:16:01.902125'
