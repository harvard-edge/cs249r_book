schema_version: '1.0'
id: cloud-1027
track: cloud
level: L5
zone: specification
topic: neural-architecture-search
competency_area: architecture
bloom_level: evaluate
phase: training
title: Evaluating 3D vs Factorized Convolutions for Video
scenario: Your team is deploying a video action recognition model in the cloud. The input is 32 frames of 1080p video encoded in FP16. The research team proposes a 3D ResNet architecture utilizing standard 3x3x3 spatial-temporal convolutions. During initial load testing, the inference service frequently hits OOM errors and exhibits unacceptably high latency.
question: "Should you retain the standard 3D ResNet on 16GB T4s or replace it with factorized (2D spatial + 1D temporal) convolutions, and why?"
details:
  realistic_solution: Replace the standard 3x3x3 convolutions with a (2+1)D factorized architecture (a 2D spatial convolution followed by a 1D temporal convolution). This algebraically reduces computational complexity and shrinks the size of intermediate feature maps that must be read/written to VRAM. This higher arithmetic intensity allows the model to comfortably fit within the 16GB VRAM limit while enabling larger batch sizes to hide memory latency.
  common_mistake: |
    **The Pitfall:** Suggesting generic techniques like quantization or pruning to fit the model.
    **The Rationale:** Engineers default to standard model compression toolkits.
    **The Consequence:** Failing to evaluate how factorizing the convolution fundamentally improves the arithmetic intensity and drastically reduces the activation memory footprint required by 3D processing.
  napkin_math: |
    **Assumptions & Constraints:**
    - 3D Conv (3x3x3) vs Factorized (2+1)D (3x3 spatial + 3x1 temporal).

    **Calculations:**
    - 3D Conv = 3 * 3 * 3 = 27 MACs per channel.
    - Factorized = (3 * 3) + (3 * 1) = 12 MACs. Reduction = 27 / 12 = 2.25x.

    **Conclusion & Interpretation:**
    - **Result: Memory Bandwidth-Bound.** Factorization reduces parameter fetches by 2.25x, mitigating the bandwidth bottleneck.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
chains:
- id: cloud-chain-065
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T21:31:05.347286'
