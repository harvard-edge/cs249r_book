schema_version: '1.0'
id: cloud-3421
track: cloud
level: L5
zone: specification
topic: model-tensor-parallelism
competency_area: parallelism
bloom_level: analyze
phase: training
title: 'Designing Efficient Tensor Parallelism for Large Language Models on Google TPU v5e'
scenario: 'You are tasked with deploying a 100-billion parameter Large Language Model (LLM) for inference on Google Cloud, specifically utilizing a cluster of Google TPU v5e devices. Each Google TPU v5e offers 16 GB HBM and 1.6 TB/s memory bandwidth. The model''s weights alone consume significantly more memory than a single TPU''s HBM.'
question: 'How would you shard a 100B LLM across TPU v5e devices with tensor parallelism to minimize inference latency and communication?'
details:
  realistic_solution: 'A realistic solution would involve a combination of tensor and possibly pipeline parallelism, carefully orchestrated to exploit the TPU v5e architecture:

    1.  **Tensor Partitioning (Intra-layer)**: For large linear layers (e.g., in attention or feed-forward networks), apply column parallelism for the first linear layer in a block (splitting the output dimension) and row parallelism for the subsequent linear layer (splitting the input dimension). This ensures that the output of the first partitioned layer can be immediately used by the second partitioned layer on the same device without an `all-gather` operation between the two linear layers within a block. An `all-reduce` or `reduce-scatter` would be needed for activation synchronization across devices only when moving to the next block or at specific points.

    2.  **Layer Partitioning (Inter-layer)**: For a 100-billion parameter model, memory is a significant constraint. If pure tensor parallelism isn''t sufficient for the model weights, consider partitioning entire transformer blocks or groups of layers across TPUs. This is often combined with pipeline parallelism to keep all stages busy. Given the 16 GB HBM, a 100B model (approx. 200GB BF16 weights) would need at least 13 TPUs just for weights, making layer partitioning inevitable.

    3.  **Communication-Computation Overlap**: Utilize non-blocking collective operations (e.g., `jax.lax.all_reduce_p`, `all_gather`, `reduce_scatter`) provided by the JAX/XLA framework. Communication for the next layer''s inputs or previous layer''s outputs should be initiated while the current layer is performing its computation. For example, during column parallelism, partial computations can proceed while the necessary intermediate results are being transferred across devices.

    4.  **TPU-Specific Optimizations**: Leverage the high-bandwidth inter-chip interconnect (ICI) on TPUs for efficient collective operations. The JAX `pjit` (partitioned JIT) transformation is crucial for defining the sharding strategy and ensuring that XLA generates optimized code that uses the ICI effectively for communication. Careful layout of sharding annotations (`ShardingSpec`) is paramount.'
  common_mistake: |
    **The Pitfall:** Proposing naive layer or tensor partitioning without accounting for asymmetric communication patterns.
    **The Rationale:** Candidates often focus purely on memory capacity (fitting the model) and overlook the strict latency and bandwidth penalties incurred by misaligned All-Reduce operations across the TPU interconnect.
    **The Consequence:** Creating a distributed serving setup crippled by severe communication bottlenecks and vastly underutilized TPU compute cores.
  napkin_math: |
    **Assumptions & Constraints:**
    - 100B parameter model in BF16 (200 GB total weights).
    - 16 GB HBM and 200 GB/s ICI Bandwidth per TPU v5e.
    - Batch_size=1, seq_len=2048, hidden_size=8192 in BF16.

    **Calculations:**
    - Minimum TPUs for weights: 200 GB / 16 GB/TPU = 12.5 (13 TPUs minimum).
    - Intermediate activation tensor size: 1 * 2048 * 8192 * 2 bytes ≈ 33.5 MB.
    - Transfer time: 33.5 MB / 200 GB/s = ~0.16 ms per non-overlapped collective.

    **Conclusion & Interpretation:**
    - **Result: ~0.16 ms transfer time (Memory-Bound)**. Efficient communication-computation overlap over ICI is critical to maintain utilization on TPU constraints.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
