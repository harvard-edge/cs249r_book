schema_version: '1.0'
id: cloud-2340
track: cloud
level: L6+
zone: diagnosis
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: both
title: INT4 Weight-Only Quantization Slowdown on A100
scenario: To save memory, your team applies INT4 weight-only quantization to a 65B parameter model deployed
  on NVIDIA A100s. The model size shrinks from 130GB to 35GB. However, the token generation latency increases
  from 40ms/token (BF16) to 95ms/token (INT4). Profiling shows high ALU utilization but low Tensor Core
  utilization.
question: "Why does INT4 weight-only quantization make the 65B model slower on A100 despite shrinking weights, and what quantization/deployment fix would you choose?"
details:
  realistic_solution: 'The symptom (higher latency, high ALU usage, low Tensor Core usage despite smaller
    weights) is a classic numeric-hardware mapping failure. The NVIDIA A100 architecture does NOT have
    native INT4 Tensor Cores (these were introduced in Hopper/Ada Lovelace). Because the hardware cannot
    natively multiply INT4 matrices, the custom kernel must fetch INT4 weights, unpack them into INT8
    or FP16 inside the CUDA cores (ALUs) using bit-shifts and masks, and then feed the unpacked FP16 weights
    into the Tensor Cores. This unpacking process takes multiple integer instructions per weight. For
    a memory-bound decoding phase, the time saved fetching 35GB instead of 130GB is completely overshadowed
    by the massive instruction overhead of unpacking 65 billion weights per token in the ALUs, shifting
    the bottleneck from HBM bandwidth to ALU instruction throughput. Co-design fix: Upgrade to H100, or
    use INT8 quantization (which A100 supports natively), or pre-pack weights in a format that minimizes
    bitwise operations.'
  common_mistake: Assuming the memory bandwidth is bottlenecked by the smaller INT4 weights, or that CPU-to-GPU
    transfer of inputs is slowing it down.
  napkin_math: 'BF16 fetch time (assuming 2x A100 for 130GB, 4 TB/s BW) = 130GB / 4000 GB/s = 32.5ms (consistent with 40ms latency). INT4 fits on 1x A100. INT4 fetch time (1x A100, 2 TB/s) = 35GB / 2000 GB/s = 17.5ms. Unpacking INT4 requires ~3 instructions (load, shift, mask) per element. 65B elements * 3 = 195 billion instructions. A100 has ~19 TOPS of INT32 ALU performance. 195e9 / 19e12 = 10.2ms. Thread divergence and register pressure from unpacking often cause 5-8x slowdowns in effective ALU throughput, adding ~80ms of compute stall time. 17.5ms (fetch) + 80ms (unpack) = 97.5ms > 40ms.'
  options:
  - The A100 lacks native INT4 Tensor Cores, so ALUs must unpack INT4 to FP16, creating a severe compute bottleneck; fix by upgrading to H100, using INT8 natively, or pre-packing weights.
  - The A100 memory bandwidth is degraded when fetching 4-bit aligned data due to unaligned memory accesses; fix by using FP16 and relying on KV cache quantization.
  - CPU-to-GPU transfer of the INT4 weights is bottlenecked by the PCIe Gen4 bus on A100; fix by upgrading to a system with PCIe Gen5 or NVLink.
  - INT4 quantization causes massive activation scale outliers that force the A100 to fall back to FP32 kernels; fix by using SmoothQuant.
  correct_index: 0
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 18
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
tags:
- quantization
- gpu-architecture
- a100
- int4
