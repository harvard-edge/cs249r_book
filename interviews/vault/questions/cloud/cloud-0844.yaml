schema_version: '1.0'
id: cloud-0844
track: cloud
level: L4
zone: diagnosis
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: Diagnosing Latency Spikes after GELU Replacement
scenario: A production CPU inference fleet serves a 7B parameter LLM using an engine optimized for dynamic
  activation sparsity. By using ReLU, the engine dynamically skips loading weights for downstream neurons
  when the input activation is zero. A recent model update replaces ReLU with GELU. While downstream accuracy
  improves, inference latency spikes from 75ms to 140ms per token, and server power draw increases by
  40%. Diagnose the root cause of the regression.
question: "Why did replacing ReLU with GELU spike token latency from 75ms to 140ms and increase server power by 40%?"
details:
  realistic_solution: Identify that GELU rarely outputs exact zeros, entirely destroying activation sparsity.
    The inference engine can no longer dynamically skip weight memory loads for inactive neurons, forcing
    dense execution. This massive increase in DRAM reads bottlenecks the memory bandwidth and drives up
    energy usage.
  common_mistake: Attributing the latency and power spike to the increased arithmetic complexity (e.g.,
    computing the error function) of GELU compared to the simpler max(0, x) of ReLU.
  napkin_math: 'A 7B model in FP16 has ~14GB of weights. Roughly 2/3 of these (~9.5GB) are in the FFN layers, and 1/3 (~4.5GB) in attention. With ReLU''s ~70% activation sparsity, only 30% of FFN weights (2.85GB) are fetched per token. Total memory read per token is 4.5GB + 2.85GB = 7.35GB. At 100 GB/s bandwidth, this takes ~73.5ms (explaining the ~75ms baseline with some overhead). With GELU (0% sparsity), the full 14GB is fetched, pushing the memory-bound latency to 140ms (14GB / 100 GB/s).'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T20:52:41.243887'
