schema_version: '1.0'
id: cloud-3683
track: cloud
level: L3
zone: recall
topic: congestion-control
competency_area: networking
bloom_level: understand
phase: both
title: Flow Completion Time vs Bandwidth Tradeoff in Gradient Sync
scenario: Two ML engineers debate congestion control strategies for a 64-GPU A100
  (80GB HBM2e, 2 TB/s) training cluster. Engineer A wants to maximize aggregate bandwidth
  for large gradient all-reduce. Engineer B wants to minimize flow completion time
  (FCT) for the parameter server's many small control messages. Explain why these
  goals conflict and how to address both.
question: Why do large all-reduce gradients and small parameter-server control messages
  need different congestion-control treatment, and how would you support both?
details:
  realistic_solution: 'Implement two traffic classes: (1) a high-priority low-latency
    queue for control messages (<32 KB) with strict priority scheduling, and (2) a
    best-effort weighted fair queue for gradient transfers (>1 MB). Use DSCP marking
    at the sender to classify traffic. This ensures FCT for small flows stays under
    100 µs while large flows share bandwidth fairly.'
  common_mistake: Using a single QoS class for all ML traffic. Large gradient transfers
    and small control-plane messages compete for the same buffers and scheduler slots.
    Large flows cause head-of-line blocking of small flows, increasing FCT for critical
    heartbeats and barrier synchronization.
  napkin_math: 'Control message FCT (no queueing): 32 KB / (100 Gbps / 8) = 2.56 µs.
    With head-of-line behind a 1 GB gradient transfer: 1 GB / (100 Gbps / 8) = 80
    ms delay — 31,000× worse. With strict priority for small flows: FCT ≈ 2.56 µs
    + serialization = ~5 µs. Acceptable for heartbeat intervals of 1-10 ms.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 2
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
