schema_version: '1.0'
id: cloud-3086
track: cloud
level: L4
zone: evaluation
topic: systolic-dataflow
competency_area: compute
bloom_level: evaluate
phase: both
title: 'Systolic Array Evaluation: TPU v5e vs H100 for Training Transformers'
scenario: Analyze the hardware utilization of an H100 serving a GPT-2-large model (774M params, 36 layers, hidden=1280), focusing on the arithmetic intensity of the inference phases.
question: What is the H100 FP16 ridge point, and when do decode and prefill become compute- versus memory-bound?
details:
  realistic_solution: The H100 ridge point is 295 FLOPs/byte. Decode is deeply memory-bound because arithmetic intensity is low (approx 1 FLOPs/byte). Prefill becomes compute-bound when the product of batch and sequence length exceeds 295.
  common_mistake: |
    **The Pitfall:** Using the wrong TFLOPS numbers (e.g., FP8 sparse instead of FP16 dense) and ignoring activation memory footprint.
    **The Rationale:** Candidates often pull the highest marketing number from spec sheets without verifying the precision mode used for standard training.
    **The Consequence:** Calculating a severely skewed arithmetic intensity ridge point, leading to completely incorrect conclusions about whether the system is compute or memory bound.
  napkin_math: |
    1. **H100 specs:** 989 TFLOPS FP16, 3.35 TB/s HBM3 bandwidth.
    2. **H100 ridge point:** 989 × 10^12 / 3.35 × 10^12 = **295 FLOPs/byte**.
    3. **Decode (batch=1):** Arithmetic Intensity AI = 2N / (2N bytes) = **1 FLOPs/byte**. Since 1 << 295, it is heavily memory-bound.
    4. **Prefill (batch=B, seq=S):** AI = 2 × B × S × N / (2N) = B × S. It becomes compute-bound when B × S > 295.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null