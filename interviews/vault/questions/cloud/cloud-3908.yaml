schema_version: '1.0'
id: cloud-3908
track: cloud
level: L4
zone: fluency
topic: software-portability
competency_area: cross-cutting
bloom_level: understand
phase: both
title: Portable Quantization Formats Across Accelerator Backends
scenario: You want to deploy a W4A16 quantized LLM (4-bit weights, 16-bit activations) on both H100 and MI300X. A junior engineer suggests looking for native 4-bit Tensor Cores or MFMA instructions to execute the matrix multiplication.
question: How is W4A16 executed on H100 and MI300X, and why don't you need native 4-bit matrix multiplication hardware?
details:
  realistic_solution: 'W4A16 is a weight-only quantization method where activations remain in 16-bit (FP16/BF16). Because the computation involves 4-bit and 16-bit operands, you cannot use a native 4-bit GEMM (which requires both operands to be 4-bit). Instead, the standard approach on both H100 and MI300X is to load the 4-bit weights from HBM to save memory bandwidth, dequantize them to 16-bit in registers or shared memory, and then use standard FP16/BF16 Tensor Cores / MFMA instructions for the compute. Since LLM decoding is heavily memory-bound, the speedup comes purely from reduced memory traffic, making native 4-bit compute hardware unnecessary.'
  common_mistake: |
    **The Pitfall:** Assuming W4A16 requires native 4-bit compute hardware.
    **The Rationale:** If weights are quantized to 4-bit, it feels logical that the arithmetic should happen natively in 4-bit.
    **The Consequence:** Wasting time searching for non-existent 4-bit matrix hardware on H100 or MI300X, rather than realizing compute happens in 16-bit after dequantization.
  napkin_math: |
    * Decoding (batch=1) is deeply memory bound.
    * Reading a 70B model in 16-bit takes 140 GB. At 3 TB/s bandwidth, this takes ~46 ms.
    * Reading a 70B model in 4-bit takes 35 GB. At 3 TB/s bandwidth, this takes ~11 ms.
    * **The 4× speedup in W4A16 comes entirely from HBM bandwidth savings, while the actual computation relies on standard FP16 ALUs after on-the-fly dequantization.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
