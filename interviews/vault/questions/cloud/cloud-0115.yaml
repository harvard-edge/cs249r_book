schema_version: '1.0'
id: cloud-0115
track: cloud
level: L3
zone: diagnosis
topic: batching-strategies
competency_area: latency
bloom_level: apply
phase: inference
title: The 60% Utilization Mystery
scenario: 'Your team has successfully migrated an LLM inference service from static batching to a modern continuous batching framework. Under heavy, sustained load, you see a 2.5x increase in throughput. However, when you check `nvidia-smi`, you''re puzzled to find that GPU utilization never exceeds ~60-70%, even though the request queue is full. An executive asks why you aren''t "fully using" the expensive H100s.'
question: What is the most likely reason for this phenomenon?
details:
  realistic_solution: "The system is bottlenecked by the CPU-bound scheduling logic of the continuous batching engine itself. GPU utilization measures the percentage of time a kernel is active. In a continuous batching loop, there are two phases: 1) The GPU executes a forward pass on the current batch of requests. 2) The CPU-bound scheduler runs to check for finished sequences, free their KV cache blocks, check the queue for new requests, and allocate blocks for them before launching the next GPU kernel. If this scheduling phase on the CPU takes a non-trivial amount of time relative to the GPU execution phase, the GPU will sit idle during that period, leading to an average utilization below 100%."
  common_mistake: |
    **The Pitfall:** Assuming the GPU is bottlenecked by a hardware limitation like memory bandwidth or PCIe transfer speeds.
    **The Rationale:** Engineers fail to realize that the CPU-bound scheduling logic of continuous batching can stall the GPU between kernel launches.
    **The Consequence:** Wasting time tuning kernel hyperparameters when the actual fix is optimizing CPU scheduling overhead.
  napkin_math: |
    - **GPU Execution Time**: Assume the forward pass for a batch takes 15ms.
    - **CPU Scheduling Time**: Updating KV cache pointers and tokenizing new requests takes 10ms.
    - **Total Cycle Time**: `15ms + 10ms = 25ms`.
    - **Final Result**: The GPU is active for 15ms out of 25ms, capping maximum utilization at **60%** (`15/25`).
  options:
  - The GPU is memory-bandwidth bound, so compute cores are idle waiting for HBM.
  - The Python GIL in the server is preventing the scheduler from running in parallel with GPU execution.
  - The request arrival rate is not high enough to fully saturate the server's capacity.
  - The GPU is frequently idle, waiting for the CPU-bound scheduler to manage requests and memory between batches.
  correct_index: 3
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues: []
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
