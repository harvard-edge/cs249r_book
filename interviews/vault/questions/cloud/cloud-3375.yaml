schema_version: '1.0'
id: cloud-3375
track: cloud
level: L3
zone: implement
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: Optimizing LLM Deployment with Structured Sparsity on Google TPU v5e
scenario: Your team is deploying a large language model (LLM) for real-time inference on a Google TPU v5e, which has 16 GB HBM, 197 TFLOPS BF16 peak performance, and 1.6 TB/s memory bandwidth. The original dense model is 25 GB, exceeding the HBM capacity, and its latency is unacceptable. You've decided to apply pruning. After initial experiments, unstructured pruning reduces the model size to 8 GB with minimal accuracy drop, but on-device profiling shows negligible latency reduction, largely due to poor utilization of the TPU's systolic array for sparse operations. Your goal is to achieve a 2x inference speedup while maintaining acceptable accuracy.
question: How would you change from unstructured to structured pruning on TPU v5e to reach 2x speedup, and what utilization gains would you target?
details:
  realistic_solution: The solution involves shifting from unstructured to structured pruning, such as block sparsity (e.g., 2:4 or 4:8 block patterns), filter pruning, or channel pruning, which aligns with the TPU v5e's systolic array architecture. This allows the TPU to execute remaining operations efficiently, potentially by converting sparse matrices to denser, smaller effective matrices. The process would include retraining the model with the chosen structured sparsity pattern, often requiring sparsity-aware training techniques (e.g., Group Lasso for structured pruning) to regain accuracy. The aim is to create predictable, hardware-friendly sparsity patterns that enable efficient data loading and computation on the TPU's specialized units.
  common_mistake: |
    **The Pitfall:** Naively applying unstructured pruning without understanding the hardware's preference for dense matrix multiplication.
    **The Rationale:** It's assumed that fewer parameters automatically equals less compute and faster execution.
    **The Consequence:** Creating a "sparse but slow" model where scattered memory accesses destroy systolic array utilization, yielding no wall-clock speedup.
  napkin_math: |
    * Assume dense model hits 10% hardware utilization: 19.7 TFLOPS. Time_dense = FLOPs / 19.7T.
    * Unstructured 50% pruning halves the FLOPs but random memory access drops utilization to 5% (9.85 TFLOPS).
    * Time_unstructured = (0.5 × FLOPs) / 9.85T = FLOPs / 19.7T (No speedup!).
    * Structured 50% block sparsity (e.g., 2:4) cuts FLOPs in half but remains hardware-friendly.
    * **To achieve a 2× speedup, structured sparsity only needs to maintain the original 10% utilization (19.7 TFLOPS). If it maps well and hits 20% (39.4 TFLOPS), the speedup would be 4×.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
