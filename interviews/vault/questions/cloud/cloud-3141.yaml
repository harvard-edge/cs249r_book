schema_version: '1.0'
id: cloud-3141
track: cloud
level: L5
zone: realization
topic: flash-attention
competency_area: optimization
bloom_level: create
phase: inference
title: Designing a Prefix Caching Strategy for RAG
scenario: You run a RAG system on 4x A100-80GB serving a 13B model. 80% of requests share one of 50 system prompts (avg 2048 tokens each). Without prefix caching, every request recomputes the system prompt's KV cache.
question: How would you design prefix caching for the 50 shared 2048-token prompts, and what memory cost, hit rate, and TTFT gain result?
details:
  realistic_solution: 'Calculate KV cache size per prefix and distribute using tensor parallelism. Prefix caching saves the massive prefill compute, significantly reducing Time To First Token (TTFT).'
  common_mistake: |
    **The Pitfall:** Caching only the token embeddings.
    **The Rationale:** Engineers assume the embedding lookup is the bottleneck, forgetting the forward pass through all layers to generate KV pairs is the true cost.
    **The Consequence:** The caching system saves minimal compute and fails to improve TTFT.
  napkin_math: |
    **Assumptions & Constraints:**
    - KV cache per prefix (40 layers, 40 heads, dim=128, FP16) = 1.6 GB.
    - 4 GPUs (Tensor Parallelism).

    **Calculations:**
    - Total cache size = 50 * 1.6 GB / 4 = 20 GB/GPU.
    - Compute saved = 2048 * 13B * 2 = ~53 TFLOPs.
    - Time saved (at 312 TFLOPS) = 53 / 312 = 170 ms.
    - Avg TTFT gain (80% hit rate) = 170 ms * 0.8 = 136 ms.

    **Conclusion & Interpretation:**
    - **Result: 20 GB/GPU memory cost yields 136 ms average TTFT reduction.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 13
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
