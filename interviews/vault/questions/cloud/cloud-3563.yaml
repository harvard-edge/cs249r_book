schema_version: '1.0'
id: cloud-3563
track: cloud
level: L5
zone: evaluation
topic: data-efficiency-selection
competency_area: data
bloom_level: analyze
phase: training
title: 'Optimizing LLM Training on A100s: Coreset vs. Synthetic Data for the Data Wall Problem'
scenario: "Your team is designing the pre-training pipeline for a 500B parameter multi-modal foundation model using A100 GPUs. The challenge is the 'data wall problem' – real-world data is prohibitively expensive, risking model collapse without massive volume. Two strategies are proposed: (1) **Coreset Selection**, which violently filters the dataset by 30% using intensive offline computation to isolate representative samples; (2) **Synthetic Data Generation**, utilizing a generative model to synthetically augment the dataset by 2x."
question: How would you evaluate coreset selection versus synthetic data generation on A100s, and would you choose one or a hybrid strategy?
details:
  realistic_solution: 'A hybrid approach often proves most robust: start with a rigorously selected coreset of real data to establish a strong foundation, then strategically augment with high-quality, validated synthetic data, continuously monitoring for distribution drift and model collapse metrics. Implement an A/B testing framework in the training pipeline to compare the effectiveness of different data strategies directly on model performance and training efficiency. Use profiling tools to analyze A100 Tensor Core utilization and HBM2e bandwidth to ensure the chosen data pipeline doesn''t become a bottleneck, justifying the computational expense of data selection or generation against the speedup in convergence or improved final model quality.'
  common_mistake: |
    **The Pitfall:** Treating data pruning or synthetic augmentation as a purely "offline" problem that magically improves the model without its own infrastructural compute overhead.
    **The Rationale:** Data Scientists often analyze algorithms by epoch convergence, while ignoring that filtering 10TB of raw multimodal data using neural embeddings actively burns massive amounts of A100-hours upfront.
    **The Consequence:** The team successfully reduces total training time but ultimately wastes double that amount of GPU-hours performing the exorbitant offline selection metrics, leading to a disastrously negative Net Compute ROI.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Baseline Run: 10TB dataset, 1000 A100-hours.
       - Coreset: 30% reduction (10TB -> 7TB). Offline selection costs 100 A100-hours.
       - A100 HBM2e reads at 2.0 TB/s; network I/O feeds at 100 GB/s.
    2. **Calculations:**
       - Coreset Training Time: New convergence takes 800 A100-hours.
       - Net Coreset Cost: 800 + 100 = 900 A100-hours.
       - I/O Ratio: 2.0 TB/s vs 100 GB/s -> 20x gap.
    3. **Conclusion & Interpretation:**
       - **Result: A pure 10% net savings, but mandates heavy overlap padding to prevent Network-I/O-Bound Tensor Core starvation.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
