schema_version: '1.0'
id: cloud-2033
track: cloud
level: L3
zone: fluency
topic: compound-ai-systems
competency_area: deployment
bloom_level: apply
phase: both
title: The Hybrid Search Score Fusion Problem
scenario: Your RAG system uses both BM25 keyword search and dense vector search. BM25 returns scores in the range [0, 25] while cosine similarity returns scores in [0, 1]. When you naively add the scores, BM25 dominates every query.
question: How do you combine these heterogeneous score distributions for meaningful ranking?
details:
  realistic_solution: 'Use Reciprocal Rank Fusion (RRF) instead of score-based fusion. RRF ignores raw scores entirely and uses only rank positions: score(doc) = Σ 1/(k + rank_i) where k=60 is typical. This is distribution-agnostic — it doesn''t matter if BM25 ranges [0,25] or [0,5]. Alternatively, use per-query min-max normalization (normalize within each result set per query, not globally). A third option: learn a lightweight cross-encoder reranker that takes the union of top-K from both retrievers and produces calibrated relevance scores.'
  common_mistake: |
    **The Pitfall:** Simply normalizing both scores to [0,1] by dividing by the maximum global score.
    **The Rationale:** Candidates apply basic min-max normalization, ignoring that max BM25 scores fluctuate wildly depending on whether a search term is common or rare.
    **The Consequence:** Dense and sparse retrieval signals remain severely miscalibrated, destroying the relevance of the final ranking.
  napkin_math: |
    1. Reciprocal Rank Fusion (RRF) ignores raw scores and uses rank positions: score = 1/(k + rank).
    2. With k=60: doc ranked #1 in both = 1/61 + 1/61 = 0.0328.
    3. Doc ranked #1 in BM25, #50 in dense = 1/61 + 1/110 = 0.0255.
    **RRF mathematically grounds the combination, making disparate scores safely comparable.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 3
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
