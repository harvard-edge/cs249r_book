schema_version: '1.0'
id: cloud-0987
track: cloud
level: L3
zone: fluency
topic: graph-compilation
competency_area: optimization
bloom_level: apply
phase: inference
title: Computational Graph Node Fusion
scenario: You are analyzing the execution of a framework's computational graph on an NVIDIA A100 GPU. A specific model sub-layer consists of 5 sequential element-wise operations. The framework incurs a fixed CPU dispatch overhead of 15 µs for every node in the computational graph. The actual GPU computation takes 5 µs per node. An optimization pass fuses these 5 nodes into a single composite node in the graph. Assume the total GPU compute time remains purely additive (25 µs).
question: What are the total execution times before and after fusing the 5 element-wise graph nodes, and what speedup does fusion provide?
details:
  realistic_solution: Calculate the time spent on dispatch vs computation. Before fusion, the 5 separate nodes each incur the 15 µs overhead and 5 µs compute, taking 100 µs total. After fusion, the graph contains 1 node, incurring a single 15 µs overhead for all 25 µs of compute. This reduces total execution time to 40 µs, achieving a 2.5x speedup.
  common_mistake: |
    **The Pitfall:** Assuming computational graphs only optimize memory bandwidth or FLOPs.
    **The Rationale:** Candidates focus entirely on device-side metrics (GPU utilization) and ignore host-side dispatch mechanics.
    **The Consequence:** Missing the significant latency reduction gained by eliminating repeated framework dispatch overheads for small, fast operations.
  napkin_math: |
    - **Unfused Execution Time:** 5 nodes * (15 us dispatch + 5 us compute) = 100 us.
    - **Fused Execution Time:** 1 fused node * 15 us dispatch + (5 * 5 us compute) = 15 us + 25 us = 40 us.
    - **Conclusion:** **Speedup is 100 us / 40 us = 2.5x speedup.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
chains:
- id: cloud-chain-050
  position: 0
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T21:23:14.617957'
