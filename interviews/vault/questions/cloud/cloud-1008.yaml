schema_version: '1.0'
id: cloud-1008
track: cloud
level: L4
zone: diagnosis
topic: fault-tolerance-checkpointing
competency_area: reliability
bloom_level: analyze
phase: training
title: Diagnosing OOD Overconfidence
scenario: You are debugging a cloud-based diagnostic vision system (ResNet-152) deployed on A100 GPUs. While test set accuracy is 95%, the system routes 10,000 requests/hour and recently failed silently by classifying out-of-distribution (OOD) images (e.g., ultrasound instead of X-ray) with >99% softmax confidence. This skips the human-in-the-loop fallback. The system has a strict P99 latency budget of 200ms per request (current inference latency is 40ms).
question: "How do you diagnose the root cause of this overconfidence and what calibration technique resolves it without requiring a full retraining cycle or violating the latency constraint?"
details:
  realistic_solution: Diagnose the issue as neural networks producing uncalibrated, extreme logits for OOD data which the softmax function exponentially amplifies. The solution is Temperature Scaling, an O(1) post-processing layer that divides logits by a learned temperature T (e.g., T=4). This widens the softmax distribution, pushing OOD confidence below a 90% human-fallback threshold with <1us of added latency, while Deep Ensembles would multiply inference cost by 5x and violate the latency budget.
  common_mistake: |
    **The Pitfall:** Proposing Deep Ensembles to fix neural network overconfidence in a high-throughput, latency-bound system.
    **The Rationale:** Blindly applying a mathematically sound uncertainty technique without measuring its drastic hardware and latency costs.
    **The Consequence:** Multiplying inference latency by 5x, immediately violating the 200ms strict SLA and overwhelming the server.
  napkin_math: |
    * **Original Logits:** [10.0, 2.0] yield Softmax [~0.9996, ~0.0003] (>99% confidence).
    * **Temperature Scaling (T=4):** Logits become [2.5, 0.5], yielding Softmax [~0.88, ~0.12].
    * **Calibration Result:** Confidence drops below 90%, properly triggering human fallback.
    * **Latency Impact:** T-scaling adds <1µs. Deep Ensembles (M=5) would increase 40ms compute to 200ms, breaking the SLA.
    * **Conclusion:** **O(1) Temperature Scaling calibrates OOD predictions without breaking the latency budget.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
chains:
- id: cloud-chain-056
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T21:28:05.352683'
