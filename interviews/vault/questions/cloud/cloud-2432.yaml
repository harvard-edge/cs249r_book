schema_version: '1.0'
id: cloud-2432
track: cloud
level: L5
zone: diagnosis
topic: pruning-sparsity
competency_area: optimization
bloom_level: analyze
phase: training
title: The RoPE Recomputation Paradox
scenario: You optimize a Transformer for a 128k context window. By rewriting the RoPE kernel to dynamically recompute sin/cos on the fly instead of reading a pre-computed HBM table, FLOPs increase by 400% but the kernel executes 4x faster.
question: Why does recomputing RoPE sin/cos on the fly make the kernel 4x faster despite 400% more FLOPs?
details:
  realistic_solution: 'The original RoPE was deeply memory-bandwidth bound. The pre-computed sin/cos table for 128K context (65.5 MB) causes TLB thrashing and cache-line waste on scattered lookups, reducing effective HBM bandwidth to ~1 TB/s. By recomputing sin/cos on the fly, you: (1) eliminate the table reads entirely (halving HBM traffic), and (2) convert scattered memory access into sequential Q/K reads at full 2.0 TB/s bandwidth. The extra trig FLOPs (~688 MFLOPs) execute in 2.2 μs while memory takes 32.8 μs — completely hidden. Net result: 4× speedup from 131 μs → 32.8 μs. This is a textbook roofline shift: trading cheap compute for expensive memory access.'
  common_mistake: |
    **The Pitfall:** Assuming that executing complex trigonometric functions on the ALU is always slower than reading pre-computed values from RAM.
    **The Rationale:** Over-applying CPU-era caching intuition without understanding the extreme compute-to-memory-bandwidth ratio of modern GPUs.
    **The Consequence:** Leaving substantial performance on the table by writing heavily memory-bound, cache-thrashing kernels.
  napkin_math: |
    **Assumptions & Constraints:**
    - Pre-computed table: ~65.5 MB (scattered reads, effective ~1.0 TB/s).
    - Recomputed: Sequentially reads Q and K (effective 2.0 TB/s).
    - Hardware: 312 TFLOPS GPU.

    **Calculations:**
    - Pre-computed read time: 131 MB / 1.0 TB/s = ~131 \mu s.
    - Recomputed read time: 65.5 MB / 2.0 TB/s = ~32.8 \mu s.
    - Recomputation compute time: 688 MFLOPs / 312 TFLOPS = ~2.2 \mu s.

    **Conclusion & Interpretation:**
    - **Result: Memory-Bandwidth Bound.** Because the 2.2 \mu s compute overhead is entirely hidden under the 32.8 \mu s memory time, eliminating scattered table reads speeds the kernel up 4x.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
