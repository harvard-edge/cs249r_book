schema_version: '1.0'
id: cloud-0187
track: cloud
level: L6+
zone: mastery
topic: model-serving-infrastructure
competency_area: deployment
bloom_level: create
phase: inference
title: The SLA Collision
scenario: Your team manages a shared GPU cluster that serves two main services: a latency-sensitive interactive chatbot (strict 200ms Time-To-First-Token SLA) and a high-throughput background summarization job (processes 10,000-token documents). Using static monolithic batches, you find that the chatbot frequently violates its SLA when a summarization job is running. A junior engineer suggests adding a priority queue so chatbot requests are always placed at the front.
question: What serving architecture should replace static monolithic batches, and why is a priority queue insufficient?
details:
  realistic_solution: The correct architectural solution involves moving away from static, monolithic batches towards a more dynamic, continuous system. With continuous batching, the GPU evaluates the scheduler at every token generation step rather than waiting for an entire batch to finish. When a high-priority chatbot request arrives, the scheduler can immediately inject it into the active batch for the very next token iteration. This avoids head-of-line blocking without requiring expensive context switching or stateful preemption, allowing both services to safely co-exist while meeting SLAs.
  common_mistake: |
    **The Pitfall:** Proposing a simple priority queue or OS-style preemption.
    **The Rationale:** A priority queue alone doesn't solve head-of-line blocking once a long batch starts. Full stateful preemption (swapping KV cache) is prohibitively expensive.
    **The Consequence:** SLA violations continue, or the system spends all its time context switching instead of generating tokens.
  napkin_math: |
    1. **Compute Required:** 13B model (26 GFLOPs/token), 10,000-token summarization prefill = $10,000 * 26\text{ GFLOPs} = 260\text{ TFLOPs}$.
    2. **Static Batch Wait:** On a 989 TFLOPS GPU, prefill takes $260 / 989 = 263\text{ms}$.
    3. **SLA Violation:** A chatbot request arriving 1ms after the prefill starts waits 262ms, violating the 200ms SLA.
    4. **Conclusion:** **Continuous batching injects the request at the next token boundary (<15ms), meeting the SLA.**
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
