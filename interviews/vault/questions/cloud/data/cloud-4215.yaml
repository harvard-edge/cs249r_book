schema_version: '1.0'
id: cloud-4215
track: cloud
level: L3
zone: analyze
topic: storage-format-selection
competency_area: data
bloom_level: analyze
phase: both
title: Parquet vs TFRecord for Training Throughput
scenario: Your team must choose between Parquet and TFRecord for storing a 5 TB image classification dataset (10M images, 500 KB average). Training runs on 8× A100 GPUs. The data pipeline must sustain 10 GB/s aggregate read throughput to avoid GPU starvation.
question: Which format, Parquet or TFRecord, should you use for the 5 TB image dataset to sustain 10 GB/s on 8× A100s, and why?
details:
  realistic_solution: 'TFRecord wins for this workload. Parquet''s columnar layout means reading a full image record requires seeking across column chunks — for wide binary blobs (images), this creates random I/O. TFRecord stores records sequentially as length-prefixed protobufs, enabling pure sequential reads. The real differentiator is the I/O pattern: TFRecord files are pre-sharded enabling embarrassingly parallel reads across 8 GPUs. Parquet supports predicate pushdown and column pruning — irrelevant for image training where you read every pixel.'
  common_mistake: |
    **The Pitfall:** Choosing Parquet strictly because it is the "industry standard" for big data.
    **The Rationale:** Parquet is columnar and excels at analytical SQL queries, but ML training reads entire records sequentially.
    **The Consequence:** Column reassembly overhead drastically reduces I/O throughput for wide binary blobs (images), starving the GPUs.
  napkin_math: |
    1. **Assumptions & Constraints:** 5 TB dataset, 10M images (500 KB avg). Target throughput = 10 GB/s across 8 GPUs. Hardware includes high-speed NVMe SSDs.
    2. **Calculations:** TFRecord reads length-prefixed protobufs sequentially, easily saturating NVMe bandwidth (e.g., 2 drives × 7 GB/s = 14 GB/s). Parquet full-record reads of binary blobs incur ~30% software penalty for column reassembly, yielding ~5 GB/s.
    3. **Conclusion & Interpretation:** **Result: TFRecord achieves 14 GB/s vs Parquet's 5 GB/s. IOPS-Bound on column assembly.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null