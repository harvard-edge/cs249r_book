schema_version: '1.0'
id: cloud-1610
track: cloud
level: L5
zone: evaluation
topic: federated-learning
competency_area: cross-cutting
bloom_level: evaluate
phase: both
title: Mitigating Black-Box Model Inversion on Health APIs
scenario: You are deploying a 1.2B parameter Transformer model for clinical readmission risk scoring via a public REST API. The API currently returns full FP32 confidence vectors across 500 risk categories. Security audits show that an attacker can execute a black-box model inversion attack by making ~50,000 queries to reconstruct sensitive patient training data with 85% fidelity. You must evaluate and select a defense to protect the data while keeping API latency under 50ms and maintaining an absolute accuracy drop of less than 1.5%.
question: Which defense should you choose to stop the 50,000-query inversion attack while keeping latency under 50ms and accuracy loss under 1.5%?
details:
  realistic_solution: Restrict the API output payload. Instead of returning the full FP32 probability distribution, return only the Top-3 categories and round the confidence scores to one decimal place (e.g., 0.8 instead of 0.812345). This breaks the numerical gradient estimation required for optimization-based inversion attacks while adding zero latency.
  common_mistake: |
    **The Pitfall:** Jumping immediately to retraining the model with Differential Privacy (DP-SGD).
    **The Rationale:** Relying on heavyweight privacy techniques without considering operational or latency constraints.
    **The Consequence:** Severely degrades model utility (>5% accuracy drop) and incurs massive GPU overhead, failing the business requirements.
  napkin_math: |
    **Assumptions & Constraints:**
    - API returns 500-dimensional FP32 vectors.
    - Attack queries required: ~50,000.
    - Rounding output to 1 decimal place (e.g., 0.1 precision).

    **Calculations:**
    - Optimization inversion relies on gradient approximation: [f(x+δ) - f(x)]/δ.
    - With 0.1 rounding, if true confidence shifts by 0.04, the API output change is exactly 0.
    - Required queries scale from O(d) to O(d / ε²).
    - New queries needed: 500 / (0.1)² = 50,000 × 100 = 5,000,000 queries.

    **Conclusion & Interpretation:**
    - **Result: Security-Bound**. Truncating confidence precision explodes the required queries by 100x (to 5 million), breaking gradient estimation without impacting latency or primary accuracy.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-23T23:48:12.375907'
