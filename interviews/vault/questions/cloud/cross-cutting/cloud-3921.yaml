schema_version: '1.0'
id: cloud-3921
track: cloud
level: L3
zone: diagnosis
topic: software-portability
competency_area: cross-cutting
bloom_level: apply
phase: both
title: Driver Version Compatibility Hell in Multi-GPU Cloud Deployments
scenario: Your containerized inference service works on local dev (CUDA 12.1, driver 535) but crashes on production cloud VMs with 'CUDA driver version is insufficient for CUDA runtime version'. The cloud VMs have driver 525 (CUDA 12.0 compatible).
question: Why does the container crash with driver 525 and CUDA runtime 12.1, and how should you fix it?
details:
  realistic_solution: 'The CUDA runtime 12.1 in your container requires driver >= 530. The cloud VM has driver 525 (max runtime 12.0). Three fixes: (1) Pin your container to CUDA runtime 12.0 (match the lowest production driver). (2) Request cloud provider to update to driver 535+. (3) Use NVIDIA''s CUDA compat library (libcuda_compat.so), available since CUDA 11.7, which allows a newer CUDA runtime to work with an older driver on select GPU architectures. Best practice: lock the CUDA runtime version in your Dockerfile and document the minimum driver requirement.'
  common_mistake: |
    **The Pitfall:** Assuming any CUDA 12.x driver supports any CUDA 12.x runtime.
    **The Rationale:** Candidates confuse forward compatibility (newer driver runs older runtime, which works) with backward compatibility (older driver runs newer runtime, which fails).
    **The Consequence:** Unpredictable deployment crashes across different cloud environments due to mismatched driver-runtime pairs.
  napkin_math: |
    **Assumptions & Constraints:**
    - Container CUDA Runtime: 12.1 (Requires driver >= 530).
    - Cloud VM Driver: 525 (Max supported runtime <= 12.0).

    **Calculations:**
    - Driver Gap: 535 (Required) - 525 (Available) = 10-version driver gap.

    **Conclusion & Interpretation:**
    - **Result: Crash (Software Compatibility Failure).** The 10-version driver gap spans two minor CUDA versions, breaking backward compatibility and causing the crash.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
