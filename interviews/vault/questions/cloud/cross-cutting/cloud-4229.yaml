schema_version: '1.0'
id: cloud-4229
track: cloud
level: L5
zone: evaluation
topic: differential-privacy
competency_area: cross-cutting
bloom_level: evaluate
phase: both
title: Privacy-Utility Tradeoff in LLM Fine-Tuning
scenario: "Your team fine-tunes GPT-2 (1.5B params) on private medical records using DP-SGD. At epsilon = 8.0 (moderate privacy), the model achieves 72% accuracy on medical NER. At epsilon = 1.0 (strong privacy), accuracy drops to 41% \u2014 barely above the 38% random baseline. The non-private baseline is 89%."
question: "Is full DP-SGD viable for GPT-2 medical NER at \u03b5=1, and what alternatives improve the privacy-utility tradeoff?"
details:
  realistic_solution: "The noise in DP-SGD scales with the gradient clipping norm divided by batch size, multiplied by a factor dependent on epsilon. With 1.5B parameters, the gradient norm is large, requiring aggressive clipping that distorts learning signals. Alternatives: (1) DP-LoRA: freeze the base model, apply DP-SGD only to LoRA adapters (e.g., rank 16: 1.5B -> ~10M trainable params). Sensitivity decreases by ~150x because gradients are over far fewer parameters. (2) PATE: train an ensemble of teacher models on disjoint private data partitions. (3) Pre-train on public medical text, then DP fine-tune only the classifier head."
  common_mistake: |
    **The Pitfall:** Concluding that differential privacy is fundamentally incompatible with LLMs.
    **The Rationale:** Applying vanilla DP-SGD to all 1.5 billion parameters requires massive noise injection because sensitivity scales with model size.
    **The Consequence:** Abandoning privacy efforts entirely instead of utilizing parameter-efficient methods (like DP-LoRA) that radically reduce the noise requirement.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - DP-SGD noise formula: \u03c3 = C * \u221a(2 ln(1.25/\u03b4)) / (\u03b5 * batch_size).
       - \u03b5=1.0, \u03b4=1e-5, batch_size=256, C=1.0.
    2. **Calculations:**
       - Factor calculation: \u221a(2 * ln(1.25/1e-5)) = \u221a(2 * 11.736) = \u221a23.47 \u2248 4.84.
       - Step Noise: \u03c3 = 1.0 * 4.84 / 256 \u2248 0.0189 per parameter per step.
       - Full Model Impact: 1.5B params * 0.0189 \u2248 28.3M absolute noise magnitude injected.
    3. **Conclusion & Interpretation:**
       **DP-LoRA reduces the noise dimensionality footprint by 150x. Result: Utility-Bound without parameter reduction.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- DP-SGD noise multiplier calculation `\u221a(2*ln(1.25/1e-5))` is approximately 4.84,
  not 4.06.
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
