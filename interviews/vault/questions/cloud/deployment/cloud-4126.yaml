schema_version: '1.0'
id: cloud-4126
track: cloud
level: L6+
zone: mastery
topic: scheduling-resource-management
competency_area: deployment
bloom_level: create
phase: inference
title: Multi-Cluster GPU Federation Scheduling
scenario: 'Your organization has three heterogeneous GPU clusters: Cluster A (Region A, FP8 support), Cluster B (Region B, max memory), and Cluster C (Region C, legacy/cheapest).'
question: How would you design a federated scheduler to route jobs across the clusters based on job characteristics?
details:
  realistic_solution: 'Federated scheduler architecture: (1) Job feature extraction: parse submitted metadata and probe defaults for memory footprint, precision support, training vs inference, latency SLO, data locality, interconnect sensitivity, budget, and deadline. (2) Capability registry: maintain live facts for each cluster, including FP8 support in Cluster A, high-HBM capacity in Cluster B, lower-cost legacy capacity in Cluster C, quotas, maintenance windows, queue depth, and health. (3) Policy and admission control: enforce org quotas, data-residency rules, priority, preemption limits, and maximum cost before placement. (4) Placement scoring: rank feasible clusters by hard constraints first, then score expected wait time, runtime, cost, locality, and fragmentation. Route FP8-capable training to Cluster A when precision policy allows, memory-heavy KV-cache inference to Cluster B, and flexible cost-sensitive jobs to Cluster C. (5) Feedback loop: ingest telemetry from completions, OOMs,
    preemptions, and SLO misses to recalibrate memory and runtime estimates. (6) Fallback handling: for mixed or underspecified jobs, request missing constraints, run a small sizing probe, split phases across clusters only when checkpoint/data movement cost is acceptable, or hold the job rather than guessing.'
  common_mistake: |
    **The Pitfall:** Treating all GPUs as strictly equivalent compute pools.
    **The Rationale:** Candidates focus entirely on aggregate FLOPS and ignore the massive differences in HBM capacity, interconnect topology, and precision support.
    **The Consequence:** Stranding large-memory inference jobs on multi-GPU setups while wasting top-tier compute accelerators on simple fine-tuning runs.
  napkin_math: |
    **Assumptions & Constraints:**
    - Cluster A supports FP8.
    - Cluster B has high-memory GPUs with 192GB HBM per GPU.
    - Cluster C has lower-cost legacy GPUs with 80GB HBM per GPU.

    **Calculations:**
    - Memory-heavy inference with a large KV cache should prefer Cluster B because one 192GB GPU may avoid tensor parallelism.
    - The same job on 80GB GPUs may require multiple GPUs or a smaller batch, increasing coordination overhead.
    - FP8-capable training or fine-tuning should prefer Cluster A when precision requirements allow it.
    - Cost-sensitive jobs without high memory or FP8 needs should prefer Cluster C.

    **Conclusion & Interpretation:**
    - **Result: route by measured constraints, not aggregate GPU count**. Memory-bound jobs go to high-HBM GPUs, FP8-friendly jobs go to FP8 hardware, and flexible jobs use the cheapest acceptable cluster.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
