schema_version: '1.0'
id: cloud-0225
track: cloud
level: L5
zone: optimization
topic: model-serving-infrastructure
competency_area: deployment
bloom_level: evaluate
phase: inference
title: The LLM Canary Trap
scenario: You are deploying a fine-tuned 70B LLM to replace your production 13B model. A 5% canary rollout shows no latency or error spikes. After full rollout, users report the new model hallucinates wildly long, repetitive outputs.
question: Why do standard infra metrics miss LLM quality regressions, and how could you use the GPU's KV-cache memory profile as a hardware-level canary signal?
details:
  realistic_solution: Traditional canary metrics (latency, error rate, throughput) measure infrastructure health, not output quality. A model that suddenly starts outputting repetitive garbage will still return HTTP 200s and keep the GPU busy. However, LLM quality regressions often manifest as changes in generation length. You can use the GPU's KV-cache memory allocation as a proxy metric for output behavior. If the new model is hallucinating longer responses, the average KV-cache blocks allocated per request will spike. Monitoring the physical memory footprint provides a real-time, hardware-level signal of behavioral changes before users complain.
  common_mistake: |
    **The Pitfall:** Relying exclusively on HTTP status codes and API latency for LLM canaries.
    **The Rationale:** Engineers apply traditional microservice monitoring principles to generative AI.
    **The Consequence:** Promoting functionally broken models that generate garbage text while maintaining perfect infrastructure health scores.
  napkin_math: |
    **Assumptions & Constraints:**
    - Baseline model generation: 200 tokens/request.
    - Canary model generation (bugged): 800 tokens/request.
    - Track KV-cache bytes per request from the serving runtime's block allocator or peak reserved bytes per sequence.
    - Let B_kv be the model-specific KV-cache bytes per generated token; the exact value depends on layers, KV heads, precision, and paged-KV block size.
    - Compare canary and baseline under the same prompt mix, max concurrency, and batching policy.

    **Calculations:**
    - Baseline per-request KV allocation: 200 * B_kv.
    - Canary per-request KV allocation: 800 * B_kv.
    - Ratio: (800 * B_kv) / (200 * B_kv) = 4x more KV bytes per completed request.
    - At 32 concurrent sequences, the active decode set moves from about 6,400 generated token-slots to 25,600 token-slots, so peak reserved KV blocks and HBM pressure rise by the same 4x if batching is unchanged.
    - Rollout gate: alert or stop promotion when canary p50/p95 KV bytes per request or output tokens per request exceeds the baseline by a configured threshold, such as >2x for two consecutive windows, even if HTTP errors and latency remain normal.

    **Conclusion & Interpretation:**
    - **Result: 4x KV-cache-per-request spike (Behavioral Canary)**. The hardware signal is operationally useful because it is measured directly by the runtime allocator and catches runaway generation length before traditional infra metrics report a failure.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
