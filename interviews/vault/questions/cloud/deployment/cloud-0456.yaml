schema_version: '1.0'
id: cloud-0456
track: cloud
level: L3
zone: fluency
topic: activation-memory
competency_area: deployment
bloom_level: apply
phase: training
title: The SLA-Driven Batching Strategy
scenario: 'You are designing a unified inference service to serve two workloads: 1) a real-time chatbot with a strict P99 TTFT SLA of <250ms, and 2) a batch job that summarizes millions of documents overnight, prioritizing maximum throughput. The chatbot has short prompts, while the batch job has long documents. A teammate proposes using static batching with a large batch size (e.g., 64) to maximize throughput for the batch job.'
question: Why is large static batching a poor solution for this unified service, and what scheduling approach is better?
details:
  realistic_solution: Using a large static batch is a poor solution because it creates extreme head-of-line blocking for the latency-sensitive chatbot workload. A chatbot request might arrive but have to wait a long time for 63 other requests to fill the batch, catastrophically violating its 250ms SLA. A better approach is to use continuous batching with priority scheduling. Chatbot requests are assigned a higher priority. The scheduler can then fill the GPU with a mix of requests, but ensures high-priority requests are processed first, meeting their SLA. This allows the batch jobs to 'soak up' any spare capacity, maximizing throughput without compromising the latency of the real-time service.
  common_mistake: |
    **The Pitfall:** Optimizing for maximum throughput at the expense of all other metrics.
    **The Rationale:** Engineers assume a single scheduling policy (large static batches) fits all workloads.
    **The Consequence:** Extreme head-of-line blocking that completely destroys real-time latency SLAs.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Static batch size: 64 long-document requests.
       - Batch prompt length: 8K tokens each on a 70B model.
       - Chatbot TTFT SLA: <250ms.
    2. **Calculations:**
       - A chatbot request arriving behind the static batch cannot start until the scheduler admits and services the 64-request batch ahead of it.
       - Even before estimating exact GPU kernel time, the queued work is 64 * 8,000 = 512,000 prompt tokens.
       - The chatbot budget allows only 250ms total TTFT, including network, queueing, prefill, and first decode.
       - Any wait to fill the batch or drain a long-document prefill consumes the entire SLA before the chatbot's own prefill begins.
    3. **Conclusion & Interpretation:**
       **Large static batches make the service queue-latency-bound for chatbot traffic. Continuous batching with priority scheduling is the better design because it admits short, high-priority requests quickly while using batch work to fill spare GPU capacity.**
  options:
    - It's a good solution; the chatbot users will just have to tolerate higher latency.
    - It fails because large static batches can exhaust HBM, causing frequent swapping.
    - It fails because high-latency chatbot requests will be starved by the throughput-focused batch jobs. A better solution is continuous batching with priority scheduling.
    - It's better to build two separate physical clusters, one for each workload, to guarantee isolation.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECTED
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
