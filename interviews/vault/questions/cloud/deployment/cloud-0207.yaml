schema_version: '1.0'
id: cloud-0207
track: cloud
level: L4
zone: evaluation
topic: compound-ai-systems
competency_area: deployment
bloom_level: analyze
phase: both
title: The Triton Inference Server Ensemble
scenario: 'We''re deploying a RAG pipeline: text preprocessing -> retrieval model -> LLM generation -> JSON postprocessing. An engineer wants to chain these as a Triton Inference Server ensemble. Another says ''just put it all in one Python process.'''
question: How do a Triton ensemble and a single Python process compare for latency and throughput in this RAG pipeline?
details:
  realistic_solution: 'A Triton ensemble chains models as a DAG within a single server process, passing tensors via shared memory (zero-copy on GPU). The latency for a single request is slightly higher than monolithic (~1-2ms overhead per stage for scheduling), but throughput is dramatically higher because stages run as a pipeline: while the LLM generates tokens for request N, the preprocessor handles request N+1.'
  common_mistake: |
    **The Pitfall:** Assuming the ensemble adds network hops between stages.
    **The Rationale:** Developers used to microservices assume splitting components implies network serialization overhead.
    **The Consequence:** Incorrectly concluding the monolithic approach is universally faster, missing the massive pipeline parallelism and shared-memory zero-copy benefits of Triton.
  napkin_math: |
    **Assumptions & Constraints:**
    - Preprocessing (CPU): 5ms.
    - Embedding retrieval (GPU): 3ms.
    - LLM generation (GPU): 200ms.
    - Postprocessing (CPU): 2ms.
    - Triton scheduling overhead: 3ms.

    **Calculations:**
    - Monolithic (Serial) Latency: 5 + 3 + 200 + 2 = 210ms.
    - Monolithic Throughput: 1000 / 210 = 4.8 req/s.
    - Triton Ensemble Latency: 210ms + 3ms overhead = 213ms.
    - Pipeline Bottleneck (Triton): LLM generation at 200ms.
    - Triton Throughput: 1000 / 200 = 5.0 req/s.
    - Scaling: With 4 LLM instances, CPU stages overlap, pushing throughput to ~19 req/s.

    **Conclusion & Interpretation:**
    - **Result: 19 req/s (Throughput-Bound)**. While Triton adds a small latency overhead per request, it drastically increases throughput by enabling pipeline parallelism across CPU and GPU stages.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
math_verified: true