schema_version: '1.0'
id: cloud-4114
track: cloud
level: L5
zone: evaluation
topic: scheduling-resource-management
competency_area: deployment
bloom_level: evaluate
phase: inference
title: GPU Preemption for Priority Inference
scenario: Your GPU cluster serves both latency-sensitive inference and batch training workloads. A high-priority inference model needs 8 MI300X GPUs immediately, but all GPUs are currently running training jobs.
question: How would you preempt training jobs to free 8 MI300X GPUs for urgent inference while minimizing wasted compute and meeting the SLA?
details:
  realistic_solution: 'Tiered preemption strategy: (1) Check for checkpointable training jobs: use DMTCP or application-level checkpointing. Signal the 8 lowest-priority training processes to checkpoint. Give 2-minute deadline. Jobs write checkpoint to fast NVMe storage. After checkpoint, terminate and free GPUs. (2) If inference SLA requires <1 minute: use NVIDIA MPS (Multi-Process Service) to temporarily share GPUs. Once dedicated GPUs free up, migrate inference. (3) Prevention: reserve a small pool (8-16 GPUs) as burst capacity for high-priority inference.'
  common_mistake: |
    **The Pitfall:** Killing training jobs immediately via SIGKILL to rush the inference start.
    **The Rationale:** Schedulers assume batch jobs are entirely disposable and optimizing for time-to-start is the only metric.
    **The Consequence:** This wastes all compute performed since the last periodic checkpoint (potentially hours of multi-GPU training), dwarfing the value of the fast inference start.
  napkin_math: |
    **Assumptions & Constraints:**
    - SLA gap with graceful checkpoint: 2 min checkpoint + 1 min restart = 3 min.
    - Time since last checkpoint: 2 hours.
    - GPUs preempted: 8 MI300X.

    **Calculations:**
    - Wasted training compute (checkpointed): 8 GPUs * 3 min = 24 GPU-minutes.
    - Wasted training compute (immediate kill): 8 GPUs * 2 hr = 16 GPU-hours.

    **Conclusion & Interpretation:**
    - **Result: Compute Utilization Bound**. Graceful checkpointing turns 16 hours of wasted compute into just 24 minutes.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 12
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
