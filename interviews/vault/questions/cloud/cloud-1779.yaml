schema_version: '1.0'
id: cloud-1779
track: cloud
level: L4
zone: diagnosis
topic: collective-communication
competency_area: networking
bloom_level: analyze
phase: both
title: Diagnosing RDMA Fallback and Kernel Overhead
scenario: You are benchmarking a distributed training job across 16 nodes, each with 8x A100 GPUs and 4x 200Gbps InfiniBand NICs. Profiling the NCCL AllReduce operations reveals per-message latencies of 45μs and unusually high `ksoftirqd` CPU usage. Benchmarks only achieve 150Gbps out of 800Gbps theoretical.
question: What does 45μs NCCL latency with high ksoftirqd usage indicate, and how would you restore expected InfiniBand AllReduce throughput?
details:
  realistic_solution: The high `ksoftirqd` usage and 45μs latency strongly indicate that RDMA kernel bypass has failed, forcing NCCL to fall back to standard TCP/IP sockets over IPoIB. You must verify that user-space libraries (libibverbs) are properly loaded, the GPU Direct RDMA modules are active, and NCCL is configured to use the IB interfaces rather than falling back to IP routing.
  common_mistake: |
    **The Pitfall:** Assuming the network switch is congested or the PCIe bus is bottlenecked.
    **The Rationale:** Not recognizing that high `ksoftirqd` CPU usage explicitly indicates the host kernel is actively processing network packets.
    **The Consequence:** Wasting time debugging switch topologies or PCIe topologies when the issue is a failed library configuration forcing NCCL to fall back to standard IP routing.
  napkin_math: |
    - **RDMA Latency**: Zero-copy kernel bypass takes **~2μs**.
    - **TCP/IP Stack**: Adds **~40μs** of latency via context switching and kernel buffer copies.
    - **Transmission Time**: For a 1MB message on a 200Gbps (25GB/s) link, pure wire transfer is 40μs.
    - **Result**: Adding 40μs of TCP overhead doubles the total transmission time to 80μs, halving the effective throughput for 1MB chunks.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: cloud-chain-333
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T00:40:56.387191'
