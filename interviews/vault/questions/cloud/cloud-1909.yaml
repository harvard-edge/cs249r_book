schema_version: '1.0'
id: cloud-1909
track: cloud
level: L4
zone: diagnosis
topic: quantization-fundamentals
competency_area: precision
bloom_level: analyze
phase: both
title: Diagnosing Accuracy Collapse in INT8 LLM Deployments
scenario: You are deploying an INT8 post-training quantized Llama-2-70B model on a cluster of A100 GPUs to increase serving throughput. Despite applying per-channel quantization to the weights, you observe severe perplexity degradation and erratic generations. Upon inspecting the linear layers, you notice massive activation outliers (sometimes >100x the median magnitude) in a small percentage of channels.
question: How do you analyze and resolve this bottleneck without incurring the massive latency overhead of dynamic per-channel activation quantization?
details:
  realistic_solution: Implement SmoothQuant to mathematically migrate the outlier difficulty from the activations to the weights offline. By introducing a per-channel scaling factor $s$, you divide the activations by $s$ (smoothing them) and multiply the weights by $s$. This allows standard per-tensor activation quantization to succeed without clipping, while preserving the original $Y = XW$ output.
  common_mistake: |
    **The Pitfall:** Suggesting per-channel quantization for activations during live inference.
    **The Rationale:** Candidates logically extend weight quantization strategies to activations, failing to realize that dynamic accumulation and scaling per channel is exceptionally slow on Tensor Cores.
    **The Consequence:** The model's serving latency spikes catastrophically, negating any throughput benefits gained from INT8 compression.
  napkin_math: |
    - **Equation:** Y = XW.
    - **SmoothQuant Transformation:** Y = (X · diag(s)⁻¹) · (diag(s) · W) = X̂Ŵ.
    - **Scaling Example:** Outlier activation X_max = 100. Corresponding weight W_max = 1.
    - **Scale Factor (s):** s = sqrt(X_max) / sqrt(W_max) = 10 / 1 = 10.
    - **Smoothed Values:** New activation max = 100 / 10 = 10. New weight max = 1 × 10 = 10.
    - **Result:** Both tensors are mathematically scaled to fit comfortably within the strict INT8 dynamic range without clipping.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 10
chains:
- id: cloud-chain-380
  position: 1
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T01:12:16.903564'
