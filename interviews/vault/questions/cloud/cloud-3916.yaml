schema_version: '1.0'
id: cloud-3916
track: cloud
level: L3
zone: recall
topic: software-portability
competency_area: cross-cutting
bloom_level: remember
phase: both
title: 'Memory Layout Portability: NCHW vs NHWC Across Accelerators'
scenario: Your CNN model uses NCHW layout (PyTorch default). When deployed on TPU
  v5e (which prefers NHWC) and MI300X (which also prefers NHWC for convolutions),
  performance drops 20-30% compared to H100. Why?
question: Why do NCHW layouts cause a 20-30% drop on TPU v5e and MI300X, and how do
  you fix it?
details:
  realistic_solution: 'The 20-30% drop comes from implicit layout transpositions inserted
    by the framework. On TPU, XLA auto-converts NCHW to NHWC but each conversion costs
    a full tensor copy. On MI300X, MIOpen prefers NHWC for convolutions and inserts
    transposes at conv boundaries. Fix: use torch.channels_last memory format throughout
    the model (model.to(memory_format=torch.channels_last)). This avoids transposes
    on TPU and AMD, and is also faster on NVIDIA with cuDNN''s NHWC kernels (which
    are now as fast or faster than NCHW on Ampere+).'
  common_mistake: Ignoring memory layout as a performance-critical portability concern.
    NVIDIA GPUs historically optimized for NCHW (cuDNN's default), while TPU, AMD,
    and mobile accelerators prefer NHWC.
  napkin_math: 'For a ResNet-50 feature map at batch 32, 64 channels, 56x56: NCHW->NHWC
    transpose = 32x64x56x56x2 bytes = 12.8MB memcpy. If 50 conv layers each trigger
    a transpose: 640MB of unnecessary data movement per forward pass.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-02'
validation_model: claude-opus-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
