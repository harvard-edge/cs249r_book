schema_version: '1.0'
id: cloud-1898
track: cloud
level: L5
zone: evaluation
topic: vram-budgeting
competency_area: memory
bloom_level: evaluate
phase: both
title: Evaluating Memory Costs of Skip Connections
scenario: Your team is fine-tuning a 152-layer ResNet-style visual encoder on A10G GPUs (24GB VRAM) using a batch size of 128. Standard feed-forward networks of similar parameter count fit, but this model throws OOM errors during the backward pass.
question: Why do dense skip connections cause OOM during the backward pass on 24GB A10Gs, and how would you preserve gradient flow?
details:
  realistic_solution: Implement activation checkpointing (gradient checkpointing) across segments of residual blocks. This discards intermediate activations and recomputes them during the backward pass, trading ~20-30% extra compute for a drastic reduction in peak memory.
  common_mistake: |
    **The Pitfall:** Focusing solely on the computational overhead of the addition operation in skip connections.
    **The Rationale:** Candidates fail to recognize that the identity path forces initial input activations to stay alive in memory.
    **The Consequence:** Missing the primary driver of peak memory usage and failing to recommend gradient checkpointing.
  napkin_math: |
    - **Memory per feature map**: A 64x64x256 FP16 feature map for a batch of 128 requires 128 * 64 * 64 * 256 * 2 bytes = 268 MB.
    - **Standard layer behavior**: Memory is freed sequentially.
    - **Skip connection behavior**: The 268 MB input is pinned in VRAM until the addition at the end of the block.
    - **Conclusion**: **Across 50 blocks, pinned activations alone consume >13 GB of VRAM, causing the OOM.**
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 14
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
created_at: '2026-03-24T01:10:19.516948'
