schema_version: '1.0'
id: cloud-3652
track: cloud
level: L4
zone: implement
topic: flash-attention
competency_area: precision
bloom_level: apply
phase: inference
title: Flash Attention Numerical Stability with Long Sequences
scenario: A model trained with standard attention is being converted to use Flash
  Attention for inference on TPU v5e (16GB HBM, 197 TFLOPS BF16). At seq_len=32768,
  you observe NaN values in attention outputs that don't appear with standard attention.
  The model was trained in BF16.
question: How would you debug and fix NaNs from Flash Attention at seq_len=32768 on
  TPU v5e?
details:
  realistic_solution: Enable Flash Attention's softmax scaling (divide QK by sqrt(d_head)
    before the attention kernel, which Flash Attention should handle internally).
    Check if the model uses ALiBi or RoPE positional encodings — very long sequences
    with ALiBi can produce large negative logits that cause exp(-large) = 0, leading
    to zero-division in softmax. Use FP32 accumulation in the attention kernel even
    with BF16 inputs.
  common_mistake: Assuming Flash Attention is numerically equivalent to standard attention
    in all cases. Flash Attention uses the online softmax algorithm with per-block
    running max for numerical stability, but BF16's limited dynamic range (exp range
    ~1e-38 to 3.4e38) can cause issues when attention logits span a large range.
  napkin_math: 'BF16 range: exponent 8 bits, significant 7 bits. Max representable:
    ~3.4e38. For seq=32768 tokens, attention logit range can reach ±100 after QK^T/sqrt(d).
    exp(100) = 2.7e43 — overflows BF16. Flash Attention''s block-wise running max
    mitigates this but requires fp32 intermediate accumulation. TPU v5e runs BF16
    natively; force fp32 matmul accumulation in JAX with jax.default_matmul_precision=''float32''.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
