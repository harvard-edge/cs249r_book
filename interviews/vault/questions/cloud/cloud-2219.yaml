schema_version: '1.0'
id: cloud-2219
track: cloud
level: L5
zone: specification
topic: data-parallelism
competency_area: parallelism
bloom_level: evaluate
phase: training
title: The Power Density Ceiling
scenario: A datacenter has 10 MW total power capacity and houses 2,000 GPUs (700W TDP each) plus associated infrastructure. During a large training run, the GPUs hit sustained peak power, tripping a PDU breaker and killing 25% of the cluster. Facilities mandates that all 2,000 GPUs cannot run at max TDP simultaneously.
question: How does the scheduler need to account for power as a schedulable resource, and what is the maximum number of GPUs you can realistically schedule at peak?
details:
  realistic_solution: 'Power must become a first-class schedulable resource. The scheduler needs a power budget per rack, per PDU, and per datacenter. Implement DVFS-based power capping to gracefully cap GPUs when the power budget is tight, trading 10-15% throughput for 30% power reduction.'
  common_mistake: |
    **The Pitfall:** Treating datacenter power as infinite or assuming GPUs always draw exactly their TDP.
    **The Rationale:** Software engineers often abstract away physical infrastructure and assume hardware manages its own power limits safely.
    **The Consequence:** Unpredictable datacenter outages, tripped breakers, and catastrophic training job failures when workloads align at peak power.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Total power: 10 MW. PUE = 1.3.
       - PDU limit: 300 kW per PDU.
       - GPU TDP: 700W. 2,000 GPUs total.
    2. **Calculations:**
       - IT power available: 10 MW / 1.3 = 7.7 MW.
       - Assuming CPUs/Network use 1.5 MW, GPU budget = 6.2 MW.
       - 2,000 GPUs * 700W = 1.4 MW (Total budget is fine).
       - However, if 500 GPUs are on one PDU: 500 * 700W = 350 kW (Trips the 300 kW PDU breaker).
       - Max GPUs per PDU without capping = 300 kW / 700W = 428 GPUs.
    3. **Conclusion & Interpretation:**
       - **Result: Power-Bound.** The cluster must use power-aware scheduling or DVFS capping (e.g., 600W per GPU) to prevent localized PDU overloads.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null