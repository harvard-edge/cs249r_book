schema_version: '1.0'
id: cloud-2094
track: cloud
level: L3
zone: fluency
topic: transformer-systems-cost
competency_area: architecture
bloom_level: apply
phase: both
title: Transformer Layer FLOP Decomposition
scenario: For a single transformer layer with hidden dimension $d = 4096$, $h = 32$ heads, head dimension
  128, FFN inner dimension $4d = 16384$, and sequence length $S = 2048$, decompose the total FLOPs into
  attention and FFN components. Which dominates?
question: "How do the attention and FFN FLOPs break down for this layer, and which component dominates?"
details:
  realistic_solution: 'Attention: Q/K/V projections = $3 \times 2Sd^2 = 6Sd^2$. Score computation = $2S^2
    d$. Output projection = $2Sd^2$. Total: $8Sd^2 + 2S^2 d$. FFN: Two linear layers ($d \to 4d$ and $4d
    \to d$): $2 \times 2S \cdot d \cdot 4d = 16Sd^2$. For $S=2048, d=4096$: Attention ≈ $8 \times 2048
    \times 4096^2 + 2 \times 2048^2 \times 4096 \approx 275$B + 34B = 309B FLOPs. FFN ≈ $16 \times 2048
    \times 4096^2 \approx 550$B FLOPs. FFN is ~64% of total.'
  common_mistake: Assuming attention dominates because it's the 'novel' part of the transformer. For typical
    models, the FFN (two large matmuls with 4× expansion) accounts for roughly 2/3 of total FLOPs.
  napkin_math: 'Attention projections: $8Sd^2 = 8 \times 2K \times 16M \approx 275B$. | Attention scores:
    $2S^2d = 2 \times 4M \times 4K \approx 34B$. | FFN: $16Sd^2 \approx 550B$. | Total per layer: ~860B
    FLOPs. | FFN dominates at ~64%.'
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: WARN
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
math_issues:
- Attention score FLOPs calculation only accounts for Q*K^T (2S^2d). Multiplying the attention scores
  by V adds another 2S^2d, making the total sequence-dependent FLOPs 4S^2d (approx 68B instead of 34B).
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
