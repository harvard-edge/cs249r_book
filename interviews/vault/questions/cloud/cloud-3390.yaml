schema_version: '1.0'
id: cloud-3390
track: cloud
level: L6+
zone: realization
topic: kernel-fusion
competency_area: optimization
bloom_level: analyze
phase: inference
title: Optimizing LLM Inference on AMD MI300X via Kernel Fusion
scenario: You are tasked with optimizing the inference performance of a large language model on an AMD MI300X accelerator. The current implementation involves a sequence of element-wise operations (e.g., activations, normalizations) that are memory-bound, leading to frequent global memory accesses and significant kernel launch overheads.
question: How would you identify and fuse memory-bound element-wise operators into custom ROCm kernels on MI300X to maximize HBM3 bandwidth and cut latency?
details:
  realistic_solution: 'Profile the existing workload to identify memory-bound kernels and their memory access patterns. Design a fused kernel that processes multiple logical operations within a single GPU kernel launch, minimizing redundant global memory accesses by leveraging ROCm''s custom kernel development capabilities for the MI300X, employing shared memory tiling and register blocking.'
  common_mistake: |
    **The Pitfall:** Ignoring the memory wall and focusing solely on compute-bound optimizations.
    **The Rationale:** Engineers often attempt to fuse kernels without analyzing memory access patterns and data dependencies, leading to suboptimal performance or correctness issues.
    **The Consequence:** Failure to reduce global memory round trips, leaving the system heavily memory-bound and latency-constrained.
  napkin_math: |
    **Assumptions & Constraints:**
    - MI300X bandwidth: 5.3 TB/s.
    - Operations: Sequence of N element-wise operations.

    **Calculations:**
    - Access time: 16-byte HBM3 access ≈ 16 bytes / 5.3 TB/s ≈ 3 picoseconds.
    - Unfused round trips: 2N memory accesses per element (1 read, 1 write per op).
    - Fused round trips: Fusing N operations reduces accesses to exactly 2 (1 read, 1 write total).

    **Conclusion & Interpretation:**
    - **Result: N-times Bandwidth Reduction (Memory-Bound Mitigated)**. Kernel fusion directly eliminates redundant HBM round trips, vastly accelerating memory-bound element-wise layers.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 16
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
