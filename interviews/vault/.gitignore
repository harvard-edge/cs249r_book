# Build artifacts — release_hash is committed via corpus-equivalence-hash.txt
# (ARCHITECTURE.md §19.3 / M-4: commit the manifest, not the binary).
vault.db
vault.db-journal
vault.db-wal
vault.db-shm

# Release artifacts staged during vault publish
releases/.pending-*/

# Runtime state
.publish.lock
.ship-journal.json
.llm-spend/
