{
  "name": "staffml-interviewer-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that powers StaffML's Ask Interviewer panel. Adapter pattern over Cloudflare Workers AI / Groq / OpenAI / Anthropic / Gemini / OpenRouter, KV-backed rate limiter, server-side Socratic system prompt enforcement.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.118.0"
  }
}
