\documentclass[10pt,twocolumn]{article}

% Line spacing
\renewcommand{\baselinestretch}{1.05}
\widowpenalty=10000
\clubpenalty=10000
\hyphenpenalty=300
\tolerance=1000

% Packages
\usepackage[T1]{fontenc}
\usepackage{mathpazo}
\usepackage[scaled=0.9]{helvet}
\usepackage{courier}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{listings}
\PassOptionsToPackage{hyphens,spaces,obeyspaces}{url}
\usepackage[round,authoryear]{natbib}
\bibliographystyle{plainnat}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{subcaption}

% Caption styling
\usepackage{caption}
\captionsetup{font=small, labelfont=bf, labelsep=period, justification=justified, singlelinecheck=false, skip=8pt}
\captionsetup[table]{position=top}

% Section spacing
\titlespacing*{\section}{0pt}{0.8\baselineskip}{0.5\baselineskip}
\titlespacing*{\subsection}{0pt}{0.6\baselineskip}{0.4\baselineskip}
\titlespacing*{\subsubsection}{0pt}{0.5\baselineskip}{0.3\baselineskip}

% Page geometry
\usepackage[letterpaper, top=0.75in, bottom=1in, left=0.65in, right=0.65in]{geometry}

% Colors
\definecolor{harvardcrimson}{HTML}{A31F34}
\definecolor{codegreen}{HTML}{2D7A2D}
\definecolor{codegray}{HTML}{555555}
\definecolor{lightgray}{HTML}{F5F5F5}

% Hyperref setup
\hypersetup{
  colorlinks=true,
  linkcolor=harvardcrimson,
  citecolor=harvardcrimson,
  urlcolor=harvardcrimson
}

% Code listing style
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{lightgray},
  frame=single,
  rulecolor=\color{codegray},
  breaklines=true,
  showstringspaces=false
}

% Custom commands
\newcommand{\staffml}{\textsc{StaffML}}
\newcommand{\numquestions}{4{,}802}
\newcommand{\numpublished}{4{,}802}
\newcommand{\numarchived}{426}
\newcommand{\numtotal}{5{,}228}
\newcommand{\numtracks}{5}
\newcommand{\numlevels}{6}
\newcommand{\numareas}{35}
\newcommand{\numcompetencies}{13}
\newcommand{\numconcepts}{193}
\newcommand{\numchains}{533}
\newcommand{\numfullchains}{54}
\newcommand{\numedges}{601}

% ============================================================================
\begin{document}

\title{
  \textbf{StaffML: A Physics-Grounded Interview Question Bank\\
  for Machine Learning Systems Engineers}\\[0.3em]
  {\large Design Methodology, Taxonomy, and Quality Assurance}
}

\author{
  Vijay Janapa Reddi\\
  Harvard University\\
  \texttt{vj@eecs.harvard.edu}
}

\date{Working Draft --- \today}

\maketitle

% ============================================================================
\begin{abstract}
We present \staffml{}, a curated bank of \numquestions{} interview questions for ML systems engineers spanning \numtracks{} deployment tracks (cloud, edge, mobile, TinyML, global), \numlevels{} mastery levels mapped to Bloom's revised taxonomy, and \numareas{} knowledge areas organized into a six-axis faceted classification. Unlike existing interview preparation resources that focus on algorithmic coding, \staffml{} tests \emph{mechanical sympathy}---the ability to reason quantitatively about the physical constraints of ML infrastructure. Every question requires napkin math grounded in real hardware specifications: memory bandwidth limits, thermal envelopes, interconnect topologies, and the economics of accelerator fleets.

This paper describes the methodology behind the question bank: how questions are generated (Automatic Item Generation with LLM-based multi-agent review), how the taxonomy is organized (a three-layer architecture of \numcompetencies{} reasoning principles, \numareas{} knowledge areas, and \numconcepts{} concept tags, following USMLE test blueprinting methodology), how quality is assured (multi-stage deduplication, multi-model math verification, schema validation), and how cognitive progression is verified (depth chains that test Bloom's level monotonicity within topics). The methodology is designed to be reproducible and scalable, following established practices from educational measurement, knowledge organization, and psychometric validation.
\end{abstract}

% ============================================================================
\section{Introduction}
\label{sec:intro}

The role of the ML systems engineer has emerged as one of the most sought-after positions in the technology industry. Unlike ML researchers who design model architectures or software engineers who write application code, ML systems engineers must reason about the \emph{physical constraints} of deploying machine learning at scale: memory bandwidth limits that determine whether a workload is compute-bound or memory-bound, thermal envelopes that cap sustained throughput on edge devices, network congestion that dominates training time in distributed clusters, and the economics of accelerator fleets where a single misconfigured batch size can waste thousands of dollars per hour.

Yet interview preparation resources for this role are remarkably thin. LeetCode \citep{leetcode2024} offers 2,800+ algorithmic coding problems but nothing on GPU memory hierarchies or KV-cache sizing. Chip Huyen's \emph{Designing Machine Learning Systems} \citep{huyen2022designing} provides excellent conceptual coverage but no interactive, quantitative practice. Mock interview platforms like Interviewing.io focus on behavioral and coding rounds, not system design with napkin math. The result is a preparation gap: candidates study the wrong material, and interviewers lack a validated question bank calibrated to the specific cognitive demands of systems reasoning.

\staffml{} fills this gap with \numquestions{} questions that test \emph{mechanical sympathy}---the term originally coined by Martin Thompson for software that works \emph{with} the hardware rather than against it. Every question has:

\begin{itemize}[nosep]
  \item A realistic interview \textbf{scenario} drawn from industry practice
  \item A \textbf{common mistake} that candidates typically make
  \item A \textbf{realistic solution} with quantitative reasoning
  \item \textbf{Napkin math} grounded in real hardware specifications
  \item Classification by \textbf{track}, \textbf{mastery level}, \textbf{knowledge area}, and \textbf{reasoning mode}
\end{itemize}

\staffml{} is the companion assessment tool for the \emph{Machine Learning Systems} textbook \citep{reddi2026mlsys}, developed alongside the Harvard CS249r course \citep{cs249r}. The textbook teaches quantitative reasoning about ML infrastructure---memory hierarchies, parallelism strategies, serving architectures---and \staffml{} provides the practice that reinforces that reasoning. Questions reference the same hardware constants, use the same ``constraints drive architecture'' philosophy, and map to the same deployment tracks (cloud, edge, mobile, TinyML) covered in the two-volume curriculum.

This paper is organized as follows. \Cref{sec:motivation} establishes first-principles motivation for why ML systems interview preparation requires a fundamentally different approach. \Cref{sec:related} surveys related work across interview platforms, ML systems research, and educational measurement. \Cref{sec:taxonomy} describes the three-dimensional taxonomy. \Cref{sec:tracks} explains the physics-based rationale for the five deployment tracks. \Cref{sec:generation} details the item generation pipeline. \Cref{sec:examples} presents example questions demonstrating the cognitive progression from recall to synthesis. \Cref{sec:qa} covers the multi-stage quality assurance process. \Cref{sec:coverage} analyzes corpus coverage. \Cref{sec:difficulty} discusses difficulty calibration. \Cref{sec:tooling} describes the engineering infrastructure. \Cref{sec:status} reports current status and future work.

\begin{table}[t]
\centering
\small
\caption{\staffml{} vault summary statistics.}
\label{tab:summary}
\begin{tabular}{@{}lr@{}}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Published questions & \numpublished{} \\
Archived (duplicates) & \numarchived{} \\
Deployment tracks & \numtracks{} \\
Mastery levels (Bloom's) & \numlevels{} \\
Reasoning competencies & \numcompetencies{} \\
Knowledge areas & \numareas{} \\
Concept tags & \numconcepts{} \\
Reasoning modes & 7 \\
Depth chains ($\geq 3$ levels) & \numchains{} \\
Full chains (L1$\to$L6+) & \numfullchains{} \\
Relationship graph edges & \numedges{} \\
Coverage cube cells filled & 100\% \\
Napkin math coverage & 100\% \\
Deep dive link coverage & 100\% \\
Hardware constants engine & \texttt{mlsysim} \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Why ML Systems Interview Preparation}
\label{sec:motivation}

\subsection{A Distinct Engineering Discipline}

The ML systems engineer occupies a unique position at the intersection of three disciplines---yet belongs fully to none of them. An ML \emph{researcher} designs model architectures and loss functions but may never consider whether the model fits in HBM or how attention scales with sequence length on real silicon. A \emph{software engineer} writes reliable, tested code but may never reason about memory bandwidth, arithmetic intensity, or the roofline model. The ML systems engineer must do both, and more: they must reason about the \emph{physical constraints} that govern how models execute on real hardware.

This is what Martin Thompson calls \emph{mechanical sympathy}---the principle that software performs best when developers understand the hardware it runs on. A Formula 1 driver who understands the engine does not merely drive faster; they drive \emph{differently}, making decisions that are physically impossible to arrive at through pure algorithmic thinking. Similarly, an ML systems engineer who understands that HBM3 access is $\sim$300$\times$ slower than an L1 register read does not merely optimize code---they \emph{architect} systems that minimize data movement in the first place.

\subsection{Constraints Drive Architecture}

The intellectual foundation of \staffml{} is the thesis that \textbf{constraints drive architecture}. You do not choose a Transformer because it is trendy; you choose it because its attention mechanism parallelizes across thousands of cores on real silicon, unlike the sequential dependency chains of RNNs. You do not use mixed precision because a tutorial told you to; you use it because FP16 tensor cores deliver $2\times$ the throughput of FP32 and halve the memory footprint, changing whether your model fits on one GPU or requires four.

This philosophy, drawn from the \emph{Machine Learning Systems} textbook \citep{reddi2026mlsys}, insists that AI is not magic---it is infrastructure, and infrastructure has laws. These laws are rooted in physics: the speed of light bounds interconnect latency, thermodynamics caps sustained power draw, and Shannon's theorem limits how much information a wire can carry. A Staff+ engineer who internalizes these constraints can \emph{derive} optimal architectures from first principles rather than memorizing best practices that may be obsolete by next year.

\subsection{Why Existing Preparation Fails}

Existing interview preparation platforms fail ML systems candidates in three specific ways:

\textbf{LeetCode tests algorithms, not systems.} A candidate who can implement Dijkstra's algorithm in $O(E \log V)$ may not know that the same GPU running inference at batch size 1 is $50\times$ less efficient than at batch size 32---because the workload shifts from memory-bound to compute-bound past the roofline ridge point. LeetCode's 2,800+ problems develop important skills, but none of them test whether a candidate can size a KV-cache, estimate serving throughput, or diagnose why GPU utilization dropped from 80\% to 4\%.

\textbf{Conceptual resources lack quantitative practice.} Chip Huyen's textbook \citep{huyen2022designing} teaches what a feature store is, what data drift means, and how to think about ML system design. But it does not ask the candidate to \emph{calculate} how many tokens per second an H100 can generate for a 70B model, or \emph{derive} the memory bandwidth required to sustain that throughput. The gap between ``I understand the concept'' and ``I can do the napkin math'' is precisely where systems interviews separate strong from weak candidates.

\textbf{No platform combines quantitative reasoning with real hardware constants.} Interview preparation for systems engineering requires a different kind of question---one where the answer is not a clever algorithm but a \emph{number} derived from physical specifications. ``How much VRAM does Llama-70B's KV-cache consume at 128K context length?'' has a definite answer ($\sim$42\,GB) that requires knowing the model's layer count, KV-head count, head dimension, precision, and sequence length. This is napkin math, not memorization: the candidate who understands the formula can answer for \emph{any} model and \emph{any} context length.

\subsection{The First-Principles Gap}

Staff+ engineers (L6 and above in industry engineering ladders) are expected to derive answers from first principles, not memorize them. When a production system fails at 3\,AM, the on-call engineer cannot look up the answer in a textbook. They must reason from the hardware specs in the monitoring dashboard: ``The GPU has 80\,GB HBM. The model weights are 140\,GB in FP16. Therefore we need at least 2 GPUs for inference, and the KV-cache at 128K context adds another 42\,GB, so we need 3 GPUs total---but we only have 2 allocated. That's the OOM.''

This kind of reasoning---rapid, quantitative, grounded in physical constants---is what \staffml{} trains. Every question is designed so that a candidate who has internalized the relevant hardware specifications and formulas can derive the answer in real time, without memorizing it in advance.

% ============================================================================
\section{Related Work}
\label{sec:related}

\subsection{Technical Interview Preparation Platforms}

The dominant paradigm for technical interview preparation is algorithmic problem-solving. LeetCode maintains approximately 2,869 validated problems \citep{leetcode2024} with community-driven difficulty calibration. Neetcode curates a subset of 150 problems organized by pattern \citep{neetcode2024}, while Grind 75 provides a dynamically ranked set based on frequency and coverage \citep{grind75}. These platforms excel at testing data structures and algorithms but do not address systems-level reasoning, hardware awareness, or quantitative estimation.

For ML-specific preparation, Chip Huyen's textbook \citep{huyen2022designing} and the ML Systems Design course at Stanford provide conceptual frameworks but lack interactive, quantitative practice. Educative.io offers guided courses on system design but without the hardware-grounded napkin math that distinguishes systems engineering from software engineering.

\subsection{ML Systems Research}

The questions in \staffml{} are grounded in the systems research that defines modern ML infrastructure. Megatron-LM \citep{shoeybi2019megatron} established tensor parallelism as the dominant strategy for training large models across GPUs. GPipe \citep{huang2019gpipe} introduced pipeline parallelism for models exceeding single-device memory. ZeRO \citep{rajbhandari2020zero} eliminated memory redundancy in data-parallel training, enabling trillion-parameter models. On the inference side, FlashAttention \citep{dao2022flashattention} demonstrated IO-aware algorithm design for attention computation, and vLLM's PagedAttention \citep{kwon2023vllm} applied virtual memory paging to KV-cache management. These systems papers provide the quantitative foundations that \staffml{} questions test---candidates must reason about the same bandwidth, memory, and parallelism tradeoffs that motivated these designs.

\subsection{Automatic Item Generation (AIG)}

Automatic Item Generation uses cognitive models and templates to produce assessment items at scale \citep{gierl2013aig}. Recent work has demonstrated that LLM-generated items can achieve psychometric properties comparable to expert-authored items when paired with multi-agent review pipelines \citep{aig_llm_2024}. Evidence-Centered Design \citep{mislevy2003ecd} provides the theoretical framework: every item begins with a claim about what competency it measures, the evidence needed to evaluate that claim, and the task features that elicit that evidence.

\staffml{} follows ECD by requiring every question to specify its target (track, level, knowledge area, reasoning competency) before generation, ensuring coverage is intentional rather than emergent.

\subsection{Knowledge Organization and Taxonomies}

The Simple Knowledge Organization System (SKOS) \citep{skos2009} provides a standard vocabulary for expressing concept hierarchies with broader/narrower/related relationships. For educational content, Bloom's revised taxonomy \citep{anderson2001taxonomy} maps cognitive demand into six levels: Remember, Understand, Apply, Analyze, Evaluate, Create.

Stack Overflow manages 60,000+ tags through synonym rings and hierarchical merging \citep{stackoverflow_tags}. Wikipedia's category system forms a directed acyclic graph (DAG) with 13 top-level categories \citep{wikipedia_categories}. TaxoGen \citep{shang2018taxogen} demonstrates that embedding-based clustering can construct topic taxonomies automatically from text corpora.

\subsection{Psychometric Validation}

Item Response Theory (IRT) \citep{lord1980irt} provides the gold standard for measuring item difficulty and discrimination. The Rasch model estimates a difficulty parameter $b$ (the ability level at which 50\% of test-takers answer correctly) that is invariant across populations---unlike Classical Test Theory's $p$-value, which depends on the specific cohort tested. IRT calibration requires approximately 30+ responses per item to stabilize \citep{hambleton1991irt}.

For initial deployment, \staffml{} uses editorial difficulty assignment mapped to Bloom's levels, with IRT-ready fields in the schema for future empirical calibration.

% ============================================================================
\section{Taxonomy Design}
\label{sec:taxonomy}

The \staffml{} taxonomy follows a \textbf{three-layer, top-down architecture} grounded in Evidence-Centered Design \citep{mislevy2003ecd} and faceted classification \citep{hjorland2013facet}. Rather than extracting topics from a textbook (bottom-up, literary warrant), we define \emph{what reasoning skills practitioners need} (top-down, user warrant) and derive testable knowledge from that competency model \citep{soergel1985organizing}.

The three layers, ordered from most abstract to most concrete:

\begin{enumerate}[nosep]
  \item \textbf{Reasoning Principles} (13): The enduring ``physics'' of ML systems engineering---invariant laws that hold regardless of specific frameworks, hardware, or model architectures. Each principle has a canonical equation.
  \item \textbf{Knowledge Areas} (35): Drillable topic groups organized as a prerequisite DAG across six domains, analogous to NeetCode's algorithm roadmap but for systems engineering.
  \item \textbf{Concept Tags} (\numconcepts{}): Specific technical knowledge that evolves with technology. Multi-label per question.
\end{enumerate}

Every question is classified on \textbf{six independent axes}: deployment track, mastery level, reasoning competency (which principle it tests), knowledge area (what topic), reasoning mode (how it tests you), and concept tags (specific knowledge required). This faceted classification follows the USMLE test blueprinting model \citep{case2002constructing}, where 4+ orthogonal axes per item enable multi-dimensional gap analysis.

\subsection{Reasoning Principles (Layer 1)}

The 13 reasoning principles represent the permanent ``physics'' of ML systems. They are stated as invariants with canonical equations:

\begin{table}[h]
\centering
\small
\caption{The 13 Reasoning Principles of ML Systems Engineering. Each principle is an invariant law accompanied by a canonical equation.}
\label{tab:principles}
\begin{tabular}{@{}llp{3.8cm}@{}}
\toprule
\textbf{ID} & \textbf{Principle} & \textbf{Canonical Equation / Law} \\
\midrule
RC-1 & Resource Quantification & Iron Law of ML Systems \\
RC-2 & Bottleneck Analysis & Roofline Model \\
RC-3 & HW-Compiler-Algorithm Co-Design & Effective Utilization \\
RC-4 & Scaling Reasoning & Amdahl's Law \\
RC-5 & Representational Efficiency & Compression Ratio \\
RC-6 & Latency Decomposition & E2E Latency Sum \\
RC-7 & Fault \& Reliability & Young-Daly Formula \\
RC-8 & System Design & Constraint Propagation \\
RC-9 & Optimization Methodology & Conservation of Complexity \\
RC-10 & Cost-Efficiency & TCO per Query \\
RC-11 & Locality & Effective Bandwidth \\
RC-12 & Observability & MTTD + MTTD$_{iag}$ \\
RC-13 & Concurrency \& Asynchrony & Overlap Efficiency \\
\bottomrule
\end{tabular}
\end{table}

These principles were validated through expert review by David Patterson (locality reasoning), Jeff Dean (observability as 30\% of Staff differentiation), Jensen Huang (HW-SW co-design scope), and Gemini 3.1 Pro (concurrency reasoning). The critical insight from Patterson: ``Prose invariants are slogans; equations are tools.'' Every principle has a quantitative formulation. Expert reviewers scored the principle framework: Patterson 8/10, Emer 7/10, Reddi 7.5/10---informing the disambiguation protocol described in \cref{sec:disambiguation}.

\subsection{Knowledge Areas (Layer 2)}

Thirty-five knowledge areas are organized into six domains with prerequisite edges, forming a directed acyclic graph. Three merges informed by expert review---E1+E2$\to$E1, E3+E4$\to$E2, and F1+F2$\to$F1---consolidated areas where inter-rater agreement was low due to overlapping scope:

\begin{table}[h]
\centering
\small
\caption{Six domains containing 35 knowledge areas.}
\label{tab:domains}
\begin{tabular}{@{}llc@{}}
\toprule
\textbf{Domain} & \textbf{Scope} & \textbf{Areas} \\
\midrule
A: ML Foundations & Workflow, neural computation, data & 7 \\
B: Hardware \& Compute & Precision, accelerators, compilers & 8 \\
C: Systems \& Scale & Memory, networks, distributed, storage & 9 \\
D: Deployment \& Serving & Compression, inference, MLOps & 7 \\
E: Efficiency \& Governance & Power, security, responsible AI & 3 \\
F: Compound AI Systems & Compound AI systems & 1 \\
\midrule
& \textbf{Total} & \textbf{35} \\
\bottomrule
\end{tabular}
\end{table}

Key structural decisions informed by expert review: Data Engineering was expanded from 1 to 3 areas (Huyen, Dean, Stoica); compiler infrastructure was split from graph optimization (Dean, Jensen); Edge and TinyML were separated as distinct physics regimes (Song Han); fault tolerance was decoupled from distributed training as a horizontal concern (Stoica); and compound AI systems were added as a new domain (Zuckerberg, Huyen, Stoica).

\subsection{Reasoning Modes and Six-Axis Classification}

Every question is classified by \emph{how} it tests the candidate, via seven reasoning modes:

\begin{table}[h]
\centering
\small
\caption{Seven reasoning modes with target distribution.}
\label{tab:modes}
\begin{tabular}{@{}llc@{}}
\toprule
\textbf{Mode} & \textbf{Description} & \textbf{Target \%} \\
\midrule
concept-recall & Retrieve a fact or constant & 5\% \\
napkin-math & Derive a number from specs & 25\% \\
symptom-to-cause & Identify cause from observable symptoms & 18\% \\
tradeoff-analysis & Compare alternatives quantitatively & 18\% \\
requirements-to-architecture & Propose architecture from requirements & 20\% \\
optimization-task & Profile, diagnose, fix, verify & 7\% \\
failure-to-root-cause & Root-cause from failure symptoms & 7\% \\
\bottomrule
\end{tabular}
\end{table}

The \texttt{failure-to-root-cause} mode (originally \texttt{production-debugging}) was added based on feedback from Chip Huyen and Jeff Dean, who noted that working backward from unexpected failures through data, model, and infrastructure layers is a distinct cognitive skill from symptom-based diagnosis or optimization. Three modes were subsequently renamed to better reflect the cognitive operation tested: \texttt{bottleneck-diagnosis}$\to$\texttt{symptom-to-cause}, \texttt{system-design}$\to$\texttt{requirements-to-architecture}, and \texttt{production-debugging}$\to$\texttt{failure-to-root-cause}.

\subsection{Mastery Levels and Bloom's Mapping}

Six mastery levels map directly to Bloom's revised taxonomy \citep{anderson2001taxonomy} and industry engineering ladders:

\begin{table}[h]
\centering
\small
\caption{Mastery levels mapped to Bloom's taxonomy and industry ladders.}
\label{tab:levels}
\begin{tabular}{@{}lllll@{}}
\toprule
\textbf{Level} & \textbf{Bloom's} & \textbf{Scope} & \textbf{Industry} \\
\midrule
L1 & Remember & Own a task & Intern \\
L2 & Understand & Own a task & Intern \\
L3 & Apply & Own a task & New Grad \\
L4 & Analyze & Own a component & Mid (L4) \\
L5 & Evaluate & Own a system & Senior (L5) \\
L6+ & Create & Own architecture & Staff+ (L6) \\
\bottomrule
\end{tabular}
\end{table}

Each level tests a qualitatively different cognitive operation. L1 requires memorization of physical constants (``HBM3 latency is $\sim$300\,ns''). L3 requires applying a formula to a novel scenario (``Calculate KV-cache size for Llama-70B at 128K context''). L6+ requires synthesizing novel architectures from first principles (``Design a scheduling policy that prevents prefill starvation under continuous batching'').

\subsection{Disambiguation Protocol}
\label{sec:disambiguation}

When a question tests multiple reasoning principles, consistent classification requires a formal disambiguation protocol. Without it, inter-rater reliability fails---two reviewers may assign the same question to different primary principles with equal justification. The protocol uses four sequential tests:

\begin{enumerate}[nosep]
  \item \textbf{Diagnostic test}: ``If I removed principle X, would this still be a valid question?'' If yes, X is secondary.
  \item \textbf{Counterfactual test}: ``What is the minimum set of principles needed to answer this?''
  \item \textbf{Verb test}: Key verbs map to specific principles (e.g., \emph{calculate} $\to$ RC-1, \emph{diagnose} $\to$ RC-2, \emph{design} $\to$ RC-8).
  \item \textbf{Specificity tie-break}: When two principles tie, choose the more specific one (e.g., RC-6 over RC-2 for serving-specific latency questions).
\end{enumerate}

This protocol was developed in response to expert reviewer feedback (Patterson 8/10, Emer 7/10, Reddi 7.5/10) and targets Cohen's $\kappa \geq 0.7$ for inter-rater reliability on the primary reasoning competency axis. The protocol is applied during both initial classification and periodic re-audit of existing questions.

\subsection{Concept Inclusion Framework}
\label{sec:concept-inclusion}

The \staffml{} taxonomy uses a different inclusion standard than the companion textbook. The textbook applies the Six Patterson Questions, which include a 10-year endurance test and reject tools and frameworks in favor of timeless principles. An interview question bank has different goals: it must test what candidates need to know \emph{now}, not what will still be true in a decade. We therefore apply three filters, all of which a concept must pass:

\begin{enumerate}[nosep]
  \item \textbf{Systems Relevance.} Does the concept have measurable systems implications---compute, memory, latency, throughput, cost, or power? If you cannot write a napkin-math question about it, it does not belong. This filter admits GPTQ (4$\times$ memory reduction, 2$\times$ throughput gain) but rejects ``Attention Is All You Need'' (a research contribution, not a systems concept).
  \item \textbf{Interview Currency.} Would a hiring committee at a top-5 AI lab expect a Staff ML systems engineer to reason about this concept during an interview loop within the next two years? This is a \textbf{2-year horizon}, not 10 years. This filter admits DeepSpeed ZeRO stages (active production use in 2025--2026) but rejects Caffe (historical).
  \item \textbf{Reasoning Depth.} Can questions be asked at 3+ Bloom's levels? A concept that supports only L1 recall (``What is X?'') is too shallow. A good concept supports recall, calculation, trade-off analysis, and design. This filter admits LoRA (define $\to$ calculate rank vs.\ memory $\to$ compare to full fine-tuning $\to$ design adapter strategy) but rejects CUDA toolkit version numbers (only recall).
\end{enumerate}

\noindent The key distinction from the textbook's Patterson Questions is summarized in \cref{tab:inclusion-comparison}.

\begin{table}[h]
\centering
\small
\caption{Concept inclusion criteria: textbook vs.\ interview bank.}
\label{tab:inclusion-comparison}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Dimension} & \textbf{Textbook} & \textbf{StaffML} \\
\midrule
Time horizon & 10 years & 2 years \\
Tools/frameworks & Rejected & Included if systems \\
Specificity & General principles & Measurable trade-offs \\
Goal & Educate students & Test engineers \\
\bottomrule
\end{tabular}
\end{table}

The taxonomy should be reviewed every six months. Concepts that no longer pass Filter~2 (interview currency) are archived, not deleted---they may still have valid questions but should not receive new generation priority.

\subsection{Concept Extraction and Taxonomy Construction}

The taxonomy is constructed by automated extraction from the textbook corpus, not by manual curation. The extraction pipeline processes all 27 chapters of the two-volume \emph{Machine Learning Systems} textbook:

\begin{enumerate}[nosep]
  \item \textbf{Preprocessing}: Strip code blocks, LaTeX equations, and figure markup from QMD source files, yielding 3.4\,MB of clean prose across 32 chapters.
  \item \textbf{Concept extraction}: Gemini 3.1 Pro processes each chapter to extract concepts with names, descriptions, prerequisite relationships, applicable deployment tracks, and source chapter provenance. The initial extraction produced 523 base concepts with 541 prerequisite edges.
  \item \textbf{Cross-review}: Both Gemini 3.1 Pro and Claude Opus 4.6 review the extracted taxonomy for orphan concepts, missing prerequisites, and inconsistent track assignments. Three review iterations eliminated all orphans.
  \item \textbf{Expert augmentation}: 26 additional concepts identified through the Concept Inclusion Framework (\cref{sec:concept-inclusion}) that are interview-critical but not prominent in the textbook (e.g., DeepSpeed ZeRO, GPTQ, QLoRA, RLHF infrastructure).
  \item \textbf{Tag consolidation}: The initial 549 concepts were consolidated into \numconcepts{} concept tags through synonym merging, hierarchical flattening, and removal of concepts that failed the 2-year interview currency filter. The SKOS-inspired hierarchy was replaced by a flat tag vocabulary with multi-label assignment per question, improving inter-rater agreement.
\end{enumerate}

The resulting taxonomy contains \numconcepts{} concept tags with \numedges{} prerequisite edges spanning all 27 textbook chapters. Each tag records its source chapters, applicable tracks, and prerequisite chain, enabling automated gap analysis and generation planning.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-taxonomy-structure.pdf}
\caption{Taxonomy construction pipeline. Textbook chapters are preprocessed into clean prose, concepts are extracted via Gemini 3.1 Pro, cross-reviewed by multiple models, and augmented with interview-critical concepts that pass the three-filter inclusion framework.}
\label{fig:taxonomy-structure}
\end{figure}

% ============================================================================
\section{Track Design Rationale}
\label{sec:tracks}

The five deployment tracks are not merely different job titles---they correspond to fundamentally different \textbf{physics regimes} where different physical constraints dominate. Four tracks (cloud, edge, mobile, TinyML) represent distinct hardware regimes; the fifth (``global'') captures cross-cutting principles that apply regardless of deployment target. This distinction is critical: the same concept (e.g., KV-cache management) requires different reasoning, different formulas, and different napkin math depending on the deployment target.

\begin{table*}[t]
\centering
\small
\caption{Deployment tracks map to distinct physics regimes with different dominant constraints, characteristic scales, and failure modes.}
\label{tab:tracks}
\begin{tabular}{@{}llllll@{}}
\toprule
\textbf{Track} & \textbf{Dominant Constraint} & \textbf{Scale} & \textbf{Unit} & \textbf{Failure Mode} & \textbf{Napkin Math Involves} \\
\midrule
Cloud & Memory BW + network fabric & PFLOPS & TB/s & Throughput collapse & TFLOPS, TB/s, \$/GPU-hr \\
Edge & Thermal envelope + real-time & TOPS & Watts & Deadline miss & TOPS, Watts, milliseconds \\
Mobile & Battery + shared resources & TOPS/W & mAh & Battery drain & TOPS/W, mAh, background limits \\
TinyML & SRAM + hard real-time & MFLOPS & KB & Stack overflow & MFLOPS, KB, microseconds \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Cloud: Memory Bandwidth and Network Fabric}

In the cloud regime, compute is abundant but data movement is expensive. An H100 GPU delivers 989\,TFLOPS of FP16 compute but only 3.35\,TB/s of HBM bandwidth, yielding a ridge point of 295\,FLOPs/Byte. Most LLM inference workloads---particularly the autoregressive decode phase---operate well below this ridge point, making them memory-bandwidth-bound despite the massive compute available. The dominant engineering challenges are KV-cache management, continuous batching to maximize GPU utilization, and inter-node communication for distributed training where InfiniBand fabric at 400\,Gb/s still becomes the bottleneck during AllReduce operations.

Cloud questions test reasoning about throughput, latency, and cost at datacenter scale. Napkin math involves TFLOPS, TB/s, and dollars per GPU-hour.

\subsection{Edge: Thermal Envelopes and Real-Time Deadlines}

Edge deployment---autonomous vehicles, robotics, industrial inspection---operates under a thermal envelope that caps sustained throughput. An NVIDIA Orin delivers 275\,TOPS INT8 within a 60\,W power budget, but sustained operation at maximum throughput may require thermal throttling. The dominant constraint is the intersection of compute budget and real-time deadlines: the perception pipeline must process a camera frame within 33\,ms (30\,fps) regardless of scene complexity, leaving no room for the tail-latency spikes that cloud systems tolerate.

Edge questions test reasoning about worst-case execution time, power budgets, and the tradeoff between model accuracy and inference latency. Napkin math involves TOPS, Watts, and milliseconds.

\subsection{Mobile: Battery Life and Shared Resources}

Mobile ML operates on shared silicon---the neural processing unit (NPU) shares power, thermal headroom, and memory bandwidth with the CPU, GPU, camera ISP, and cellular modem. A smartphone's 4,500\,mAh battery must last all day, meaning any ML workload must justify its energy cost against competing demands. The dominant constraint is TOPS per Watt: a mobile NPU may deliver 15\,TOPS at 5\,W, but running it continuously for on-device inference would drain the battery in under 2 hours.

Mobile questions test reasoning about energy budgets, model compression for on-device deployment, and the scheduling tradeoffs of running ML inference alongside other system tasks.

\subsection{TinyML: SRAM Limits and Hard Real-Time}

TinyML represents the most constrained physics regime. A Cortex-M4 microcontroller operates at $\sim$100\,MFLOPS with 256\,KB of SRAM and no operating system. There is no virtual memory, no swap space, and no dynamic allocation---the model, activations, and runtime must all fit in SRAM simultaneously. The dominant constraint is hard real-time: a keyword spotting model must classify an audio frame within a fixed time budget, because missing a deadline means dropping audio samples with no recovery mechanism.

TinyML questions test reasoning about model footprint in kilobytes, fixed-point arithmetic, and the systems engineering required to run inference on bare metal. Napkin math involves MFLOPS, KB, and microseconds.

\subsection{Why the Same Concept Requires Different Questions}

The track structure ensures that questions test physics-regime-appropriate reasoning. Consider KV-cache management across tracks:

\begin{itemize}[nosep]
  \item \textbf{Cloud}: ``A 70B model's KV-cache at 128K context is $\sim$42\,GB. How many H100s do you need?'' (Memory capacity reasoning at TB scale)
  \item \textbf{Edge}: ``Your ADAS model caches 10 frames of detection history. At 30\,fps on Orin's 32\,GB LPDDR5, what is the memory pressure?'' (Real-time memory budget reasoning)
  \item \textbf{TinyML}: ``Your keyword spotter needs 2\,KB of rolling buffer on a 256\,KB MCU. What fraction of SRAM does this consume, and what is left for the model?'' (SRAM partitioning at KB scale)
\end{itemize}

All three questions test the same underlying concept---caching intermediate computation to avoid recomputation---but the physics, the formulas, and the failure modes are entirely different.

% ============================================================================
\section{Item Generation Pipeline}
\label{sec:generation}

Questions are generated using an Automatic Item Generation (AIG) pipeline \citep{gierl2013aig} powered by large language models with multi-agent review. \Cref{fig:generation-pipeline} illustrates the end-to-end flow from gap analysis through corpus integration.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-generation-pipeline.pdf}
\caption{The Automatic Item Generation pipeline. Gap analysis identifies underfilled cells in the 3D coverage cube. Prompts are constructed with hardware constants from \texttt{mlsysim}, Bloom's descriptors, and track constraints. LLM-generated candidates pass through schema validation, multi-stage deduplication, and multi-model math verification before corpus integration.}
\label{fig:generation-pipeline}
\end{figure}

\subsection{Evidence-Centered Design}

Following \citet{mislevy2003ecd}, every question begins with a specification of:

\begin{itemize}[nosep]
  \item \textbf{Claim}: What competency does this question measure? (track $\times$ level $\times$ area)
  \item \textbf{Evidence}: What would a correct answer demonstrate? (Bloom's cognitive operation)
  \item \textbf{Task features}: What scenario elicits this evidence? (hardware specs, failure mode, scale)
\end{itemize}

The generation prompt includes real hardware constants from the \texttt{mlsysim} physics engine (H100: 80\,GB HBM3, 3.35\,TB/s, 989\,TFLOPS FP16; Orin: 275\,TOPS INT8, 204.8\,GB/s; Cortex-M4: $\sim$100\,MFLOPS, 256\,KB SRAM), Bloom's level descriptors for the target cognitive demand, and track-specific constraints.

\subsection{Generation and Validation Flow}

\begin{enumerate}[nosep]
  \item \textbf{Gap analysis}: Identify underfilled cells in the 3D coverage cube
  \item \textbf{Prompt construction}: Template with target (track, level, area), hardware specs, Bloom's descriptor
  \item \textbf{LLM generation}: Gemini Pro or Claude Opus produces candidate questions in structured JSON
  \item \textbf{Schema validation}: Pydantic model enforces required fields, types, minimum lengths
  \item \textbf{Multi-stage deduplication}: Exact $\to$ fuzzy $\to$ semantic (see \cref{sec:dedup})
  \item \textbf{Math review}: Multi-model cross-verification (see \cref{sec:math-review})
  \item \textbf{Tag normalization}: Map raw topic to canonical concept via NORMALIZE\_MAP
  \item \textbf{Corpus integration}: Append to \texttt{corpus.json}, regenerate markdown
\end{enumerate}

% ============================================================================
\section{Example Questions}
\label{sec:examples}

To illustrate the cognitive progression that \staffml{} tests, we present three questions from the corpus at different mastery levels. Each demonstrates a qualitatively different type of reasoning, progressing from recall of physical constants through quantitative analysis to architectural synthesis.

\subsection{L1 (Remember): The HBM Latency Penalty}

\begin{quote}
\small
\textbf{Track}: Cloud \quad \textbf{Level}: L1 \quad \textbf{Area}: Memory

\textbf{Scenario}: You're debugging a GPU kernel and suspect it's memory-bound due to random memory access patterns. To build intuition, what is the approximate latency for a single memory access to HBM3 on a modern datacenter GPU like an H100?

\textbf{Common mistake}: Engineers often underestimate this latency, confusing it with on-chip cache speeds. They might guess 10--50\,ns, failing to appreciate the penalty for going off-chip to the HBM stacks.

\textbf{Napkin math}: If an L1 cache access ($\sim$1\,ns) took 1 second in human time, an L2 cache access ($\sim$4\,ns) would take 4 seconds. That HBM3 access ($\sim$300\,ns) would take \textbf{5 minutes}. This highlights why data locality is critical for performance.

\textbf{Answer}: $\sim$300\,ns.
\end{quote}

This is a pure recall question: the candidate either knows the constant or does not. But the napkin math reframing (1\,second $\to$ 5\,minutes) transforms a dry number into physical intuition that informs every subsequent design decision. An engineer who \emph{feels} the 300$\times$ penalty will instinctively minimize off-chip accesses.

\subsection{L4 (Analyze): The Sequence Length Trap}

\begin{quote}
\small
\textbf{Track}: Cloud \quad \textbf{Level}: L4 \quad \textbf{Area}: Memory

\textbf{Scenario}: You need to increase your LLM's context window from 4K to 128K tokens. The model weights fit perfectly in your 80\,GB VRAM. What hidden memory cost will cause your node to OOM during generation?

\textbf{Common mistake}: ``Activations will use more memory.'' Activations matter during training, but during inference the dominant hidden cost is something else entirely.

\textbf{Napkin math}: For Llama-70B (80 layers, 8 KV-heads via GQA, head\_dim=128) at 128K tokens in FP16:
\[
\text{KV-cache} = 2 \times 80 \times 8 \times 128 \times 128{,}000 \times 2 \approx \textbf{41.9\,GB}
\]
Combined with 140\,GB of weights, one 128K-context request requires $\sim$182\,GB---at least 3 H100s. Even GQA's $8\times$ KV reduction leaves the cache at nearly a third of the model size.

\textbf{Answer}: The KV-cache. It grows linearly with sequence length and, at 128K context, can exceed the model weights themselves.
\end{quote}

This question requires \emph{analysis}: the candidate must decompose inference memory into its components (weights, KV-cache, activations), identify which one scales with sequence length, and calculate the specific memory requirement. The common mistake reveals whether candidates have internalized the difference between training-time and inference-time memory profiles.

\subsection{L6+ (Create): The Continuous Batching Stall}

\begin{quote}
\small
\textbf{Track}: Cloud \quad \textbf{Level}: L6+ \quad \textbf{Area}: Latency

\textbf{Scenario}: You implement continuous batching for an LLM endpoint. Under low load, TTFT is 100\,ms. Under high load, throughput triples, but TTFT spikes to over 4 seconds even though token generation speed remains fast. What scheduling flaw caused this?

\textbf{Common mistake}: Assuming that because continuous batching optimizes throughput, it automatically guarantees low latency for incoming requests.

\textbf{Napkin math}: A 2,000-token prefill on a 70B model: $2 \times 70\text{B} \times 2{,}000 = 280$ TFLOPs. An A100 at 312\,TFLOPS needs $\sim$1 full second. If 4 users send prompts simultaneously, the 4th waits 3 seconds before prefill even begins.

\textbf{Answer}: Prefill starvation. The scheduler must chunk or defer prefills to prevent blocking the decode phase, but aggressive deferral under high load creates a queue that destroys TTFT.
\end{quote}

This question requires \emph{synthesis}: the candidate must understand the two-phase nature of LLM serving (prefill vs.\ decode), reason about how continuous batching interleaves these phases, identify the scheduling conflict, and propose an architectural solution. There is no single formula to memorize---the answer emerges from composing multiple pieces of systems knowledge into a coherent diagnosis and design.

\bigskip

These three questions demonstrate the cognitive progression that depth chains (\cref{sec:chains}) verify: from memorizing a hardware constant (L1), through applying a formula to a novel scenario and analyzing its implications (L4), to synthesizing a complete architectural diagnosis from first principles (L6+). Each level requires all the knowledge from the levels below it, plus a qualitatively new cognitive operation.

% ============================================================================
\section{Quality Assurance}
\label{sec:qa}

Quality assurance operates at multiple stages, as shown in \cref{fig:quality-pipeline}. Every question must pass schema validation, deduplication, and math verification before entering the corpus.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-quality-pipeline.pdf}
\caption{Multi-stage quality assurance pipeline. Candidate questions pass through schema validation (Pydantic), three-stage deduplication (exact, fuzzy, semantic), and multi-model math verification (Gemini Flash broad review + Claude Opus deep review). Confirmed errors are corrected; duplicates are archived, never deleted.}
\label{fig:quality-pipeline}
\end{figure}

\subsection{Schema Validation}

Every question is validated against a strict Pydantic schema with both field-level and cross-question rules:

\begin{itemize}[nosep]
  \item \textbf{Field constraints}: minimum character lengths (scenario $\geq 50$, napkin\_math $\geq 10$ and must contain a digit), MCQ must have exactly 4 options with correct\_index in $[0,3]$
  \item \textbf{Taxonomy constraints}: knowledge\_area $\in$ \{35 canonical areas\}, reasoning\_competency $\in$ \{13 principles\}, topic must normalize to a known tag
  \item \textbf{Uniqueness constraints}: no duplicate IDs, no duplicate (track, title) pairs among published questions
  \item \textbf{Linguistic checks}: no trailing quotes, no markdown formatting artifacts, no placeholder text
\end{itemize}

\subsection{Multi-Stage Deduplication}
\label{sec:dedup}

Following best practices from web-scale deduplication \citep{broder1997minhash} and educational assessment, we employ a three-stage pipeline:

\begin{enumerate}[nosep]
  \item \textbf{Exact match}: Lowercased, whitespace-stripped scenario comparison. Catches copy-paste duplicates.
  \item \textbf{Fuzzy match}: \texttt{difflib.SequenceMatcher} $> 0.90$ within same (track, topic) groups. Catches minor rewordings.
  \item \textbf{Semantic match}: Sentence-BERT cosine similarity $> 0.85$ via ChromaDB. Catches same-concept-different-words duplicates.
\end{enumerate}

Confirmed duplicates are \emph{archived} (status set to ``archived''), never deleted, preserving the audit trail---a practice adopted from Anki's large shared deck management \citep{anking2024}.

\subsection{Multi-Model Math Verification}
\label{sec:math-review}

Napkin math is the soul of \staffml{}---and the most error-prone component. Hardware specifications change across generations, unit conversions cascade, and order-of-magnitude errors are easy to introduce. We verify math through multi-model cross-review:

\begin{enumerate}[nosep]
  \item \textbf{Round 1} (broad): Gemini Flash reviews all questions in 50-question chunks, checking arithmetic, hardware specs, unit consistency, and logical coherence. 63 parallel review jobs complete in $\sim$12 minutes.
  \item \textbf{Round 2} (deep): Claude Opus reviews the same questions independently, catching subtle logical errors that Flash misses.
  \item \textbf{Discrepancy analysis}: Errors found by one model but not the other are flagged for manual review. False positives are tracked to calibrate reviewer accuracy.
\end{enumerate}

In Round 1, Gemini Flash found 58 candidate errors. Opus agents verified 48 as genuine errors and correctly rejected 10 as false positives (4.3\% raw $\to$ 1.5\% confirmed). In Round 2 (post-correction), four Opus agents independently reviewed all 3,095 published questions with python3 arithmetic verification. They found 7 additional errors (0.22\% rate): 3 hardware spec swaps (H100/A100 TFLOPS), 2 unit conversion cascades, 1 throughput claim, and 1 spec inconsistency. All were fixed. The combined two-round process reduced the error rate from 4.3\% to $< 0.1\%$.

\subsection{Depth Chain Coherence}
\label{sec:chains}

Depth chains verify that questions on the same topic form a coherent cognitive progression through Bloom's levels. A chain is a sequence of 3--5 questions on the same canonical topic with strictly increasing cognitive demand:

\begin{quote}
\small
L1: ``What is a KV-cache?'' (Remember) \\
$\to$ L3: ``Calculate KV-cache size for Llama-70B'' (Apply) \\
$\to$ L5: ``Server OOM'd at 128K context---diagnose'' (Evaluate) \\
$\to$ L6+: ``Design a PagedAttention allocator'' (Create)
\end{quote}

A chain is \textbf{broken} if a higher-level question does not build on knowledge tested at lower levels. A topic has a \textbf{missing foundation} if L5/L6+ questions exist but no L1/L2 on-ramp. A topic has \textbf{missing depth} if L1/L2 exists but no L4+ extension.

The corpus currently contains \numchains{} topic clusters spanning 3+ levels, with 1,327 questions (42\% of the corpus) participating in chainable clusters. \Cref{fig:depth-chain} illustrates a complete depth chain from L1 through L6+ for the KV-cache topic, showing how each level builds on the knowledge established at the level below.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-depth-chain.pdf}
\caption{A complete depth chain for the KV-cache topic, showing cognitive progression from L1 (recall a definition) through L6+ (design a novel allocator). Each level requires the knowledge from all lower levels plus a qualitatively new cognitive operation. Full chains spanning all six levels exist for \numfullchains{} of \numconcepts{} concept tags.}
\label{fig:depth-chain}
\end{figure}

% ============================================================================
\section{Coverage Analysis}
\label{sec:coverage}

\subsection{3D Coverage Cube}

The primary coverage metric is the 3D cube: track ($\numtracks{}$) $\times$ level ($\numlevels{}$) $\times$ knowledge area ($\numareas{}$) = 1{,}050 cells. Following test blueprint methodology \citep{millman1989}, we target a minimum of 3 questions per cell to ensure reliability. The expansion from 312 cells (4 tracks $\times$ 6 levels $\times$ 13 areas in the initial taxonomy) to 1{,}050 cells reflects both the addition of the ``global'' track and the finer-grained 35 knowledge areas.

Current state:
\begin{itemize}[nosep]
  \item \textbf{Empty cells}: 0 (every testable combination has at least one question)
  \item \textbf{Underfilled} ($< 3$): 37 cells (need 56 additional questions)
  \item \textbf{Healthy} ($\geq 3$): 258 cells
\end{itemize}

\Cref{fig:corpus-distribution} shows the distribution of questions across tracks and levels as a heatmap, alongside per-knowledge-area bar charts showing coverage density. The cloud track has the densest coverage (reflecting the current industry emphasis on LLM serving), while TinyML has the sparsest (reflecting the smaller but growing demand for MCU-level ML systems engineering).

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-corpus-distribution.pdf}
\caption{Corpus distribution across the coverage cube. Left: track $\times$ level heatmap showing question density. Right: per-knowledge-area bars showing coverage depth. The 3D cube contains 1{,}050 cells ($5 \times 6 \times 35$); 258 cells meet the $\geq 3$ reliability threshold.}
\label{fig:corpus-distribution}
\end{figure}

\subsection{Level Distribution}

The target distribution follows a Bloom's pyramid---more foundation questions, fewer advanced:

\begin{table}[h]
\centering
\small
\caption{Level distribution across tracks (actual vs.\ target).}
\label{tab:level-dist}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
& \textbf{L1} & \textbf{L2} & \textbf{L3} & \textbf{L4} & \textbf{L5} & \textbf{L6+} \\
\midrule
Cloud & 15\% & 20\% & 21\% & 17\% & 14\% & 13\% \\
Edge & 18\% & 24\% & 20\% & 14\% & 13\% & 10\% \\
Mobile & 14\% & 23\% & 29\% & 15\% & 11\% & 8\% \\
TinyML & 19\% & 23\% & 23\% & 14\% & 12\% & 9\% \\
\midrule
\textit{Target} & \textit{17\%} & \textit{23\%} & \textit{23\%} & \textit{15\%} & \textit{12\%} & \textit{10\%} \\
\bottomrule
\end{tabular}
\end{table}

All tracks are within 3 percentage points of the target across all levels.

\subsection{Question Format Balance}

Questions are classified by cognitive style to ensure variety within each cell:

\begin{itemize}[nosep]
  \item \textbf{Calculation} (36\%): ``Estimate tokens/sec for batch=1 on H100''
  \item \textbf{Design} (28\%): ``Architect a KV-cache paging system''
  \item \textbf{Conceptual} (27\%): ``Explain why this workload is memory-bound''
  \item \textbf{Optimization} (18\%): ``Reduce TTFT from 500ms to 100ms''
  \item \textbf{Diagnosis} (14\%): ``GPU util is 4\%---what went wrong?''
  \item \textbf{Tradeoff} (6\%): ``Compare pipeline parallelism vs.\ tensor parallelism''
\end{itemize}

L1--L2 questions are predominantly calculation and conceptual (testing recall and understanding). L4--L6+ questions shift toward diagnosis, design, and tradeoff analysis (testing higher-order reasoning). \Cref{fig:format-balance} shows the distribution of question formats by mastery level, confirming this cognitive shift.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig-format-balance.pdf}
\caption{Question format distribution by mastery level. Lower levels (L1--L2) are dominated by calculation and conceptual questions that test recall and understanding. Higher levels (L5--L6+) shift toward diagnosis, design, and tradeoff analysis that require synthesis and evaluation. The format mix is intentionally designed to match the cognitive demands of each Bloom's level.}
\label{fig:format-balance}
\end{figure}

% ============================================================================
\section{Difficulty Calibration}
\label{sec:difficulty}

\subsection{Editorial Assignment}

Each question receives an editorial difficulty level (L1--L6+) based on the target Bloom's cognitive operation and the scope of reasoning required. This assignment is made at generation time, informed by the prompt template.

\subsection{IRT-Ready Schema}

The question schema includes fields for future empirical calibration:

\begin{itemize}[nosep]
  \item \texttt{difficulty\_empirical}: IRT $b$-parameter (null until $\geq 30$ user responses)
  \item \texttt{discrimination}: IRT $a$-parameter (how well the item separates high- and low-ability respondents)
  \item \texttt{attempt\_count}: Total user attempts (for IRT sample size tracking)
\end{itemize}

Following \citet{lord1980irt}, we plan to calibrate these parameters once the StaffML platform collects sufficient user interaction data. Until then, editorial levels serve as the primary difficulty indicator.

% ============================================================================
\section{Tooling: \texttt{vault.py}}
\label{sec:tooling}

All vault operations are consolidated into a single CLI tool, \texttt{vault.py}, which enforces the methodology described above:

\begin{table}[h]
\centering
\small
\caption{vault.py commands and their methodological role.}
\label{tab:vault-commands}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Command} & \textbf{Role} & \textbf{Method} \\
\midrule
\texttt{validate} & Schema enforcement & Pydantic \\
\texttt{stats} & Coverage analysis & 3D cube \\
\texttt{gaps} & Gap identification & Test blueprint \\
\texttt{dedup} & Duplicate detection & 3-stage \\
\texttt{search} & Semantic search & ChromaDB \\
\texttt{chains} & Progression check & Bloom's \\
\texttt{add} & Validated insertion & AIG pipeline \\
\texttt{review} & Math verification & Multi-model \\
\texttt{analyze} & Taxonomy gap analysis & Concept coverage \\
\texttt{generate} & LLM question gen & Gemini CLI \\
\texttt{plan-all} & Parallel plan files & Per level$\times$track \\
\texttt{generate-batch} & Batch generation & Parallel workers \\
\texttt{merge} & Batch ingestion & Validate + dedup \\
\texttt{loop} & Auto-saturation & Analyze$\to$gen$\to$add \\
\texttt{export} & App sync & JSON $\to$ app \\
\texttt{release} & Full pipeline & All steps \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================================
\section{Current Status and Future Work}
\label{sec:status}

As of \today, the \staffml{} vault contains \numquestions{} questions with 100\% coverage on all required fields and six-axis faceted classification. Key milestones completed:

\begin{itemize}[nosep]
  \item \textbf{Taxonomy}: Three-layer architecture with \numcompetencies{} reasoning principles, \numareas{} knowledge areas (6 domains), and \numconcepts{} concept tags. Six-axis faceted classification per question (track, level, reasoning competency, knowledge area, reasoning mode, concept tags). Disambiguation protocol targeting Cohen's $\kappa \geq 0.7$.
  \item \textbf{Expert validation}: Principle framework scored by Patterson (8/10), Emer (7/10), Reddi (7.5/10). Three knowledge area merges (44$\to$35) and three reasoning mode renames based on reviewer feedback.
  \item \textbf{Automated generation}: Overnight generation loop produced 1,211 questions in 41 rounds (5 hours) with 100\% schema validation yield and zero duplicates. Concept coverage increased from 15\% to 94\%.
  \item \textbf{Depth chains}: \numchains{} chains built (\numfullchains{} spanning all 6 levels L1$\to$L6+).
  \item \textbf{Schema}: Pydantic validation operational---all \numquestions{} questions pass. Zero validation errors.
  \item \textbf{Dedup}: 3-stage pipeline operational. Zero exact duplicates in current corpus.
  \item \textbf{Math review}: Two rounds complete. Error rate reduced from 4.3\% to $< 0.1\%$.
\end{itemize}

\textbf{Remaining work}:
\begin{enumerate}[nosep]
  \item Generate $\sim$500 additional hard questions (L5--L6+) to reach $\sim$5{,}200 total
  \item Complete chain coverage: add L1/L2/L6+ questions for concept tags that currently have only L3--L5
  \item Cross-track expansion: generate edge/mobile/tinyml variants for cloud-only concepts
  \item Rebalance corpus: cap over-represented concept tags ($>$50 questions) via archival
  \item Validate disambiguation protocol with external raters ($\kappa \geq 0.7$ target)
  \item Expert panel review of L5--L6+ questions for face validity
\end{enumerate}

\textbf{Future work}:
\begin{itemize}[nosep]
  \item IRT calibration from user interaction data ($\geq 30$ responses per item)
  \item Community contribution pipeline with automated validation
  \item Adaptive question selection based on user performance profiles
  \item Taxonomy refresh cycle (6-month cadence per inclusion framework)
  \item QTI export for LMS integration \citep{qti2020}
  \item Longitudinal study of \staffml{} usage in CS249r course assessments
\end{itemize}

% ============================================================================
\bibliography{references}

\end{document}
