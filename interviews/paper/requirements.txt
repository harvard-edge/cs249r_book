# StaffML paper — Python dependencies (canonical list for CI and local runs)
#
# Install (from repo root):
#   pip install -r interviews/paper/requirements.txt
#
# Coverage (all *.py in this directory):
#   analyze_corpus.py                  — PyYAML, stdlib
#   generate_app_matrix_from_yaml.py   — PyYAML, stdlib (applicability_matrix.yaml → app_matrix.tex)
#   generate_figures.py                — matplotlib, numpy, seaborn
#   generate_macros.py                 — stdlib only
#   restore_examples.py                — stdlib only
#
# CI: .github/workflows/staffml-update-paper.yml
#
# Version bounds: reproducible CI, compatible with Python 3.11+ (GitHub Actions).
matplotlib>=3.8.0,<4
numpy>=1.26.0,<3
seaborn>=0.13.0,<0.15
PyYAML>=6.0,<7
