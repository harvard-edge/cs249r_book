schema_version: '1.0'
id: mobile-1564
track: mobile
level: L3
zone: implement
topic: profiling-bottleneck-analysis
competency_area: latency
bloom_level: analyze
phase: inference
title: Optimizing a Transformer Model on Snapdragon Hexagon NPU
scenario: A large language model based on a transformer architecture is deployed on a mobile device equipped with a Hexagon NPU (45 TOPS INT8, 64 GB/s LPDDR5X bandwidth). Despite quantization to INT8, inference latency is consistently higher than the target 100ms per query. You suspect a bottleneck within the NPU's compute, memory, or I/O subsystem using available profiling tools.
question: How would you diagnose and optimize a transformer model bottleneck on a mobile NPU to reach the 100ms latency target?
details:
  realistic_solution: Profile the deployed graph with Android system tracing plus the device vendor's NPU/delegate profiler, then classify the slow path instead of assuming a single cause. Check per-op latency, delegate placement, CPU fallbacks, host-to-NPU copies, DRAM bandwidth, cache misses, and NPU utilization. If it is compute-bound, focus on supported INT8 matmul/attention kernels, static shapes, fusion, and avoiding unsupported ops. If it is memory-bound, reduce KV-cache and activation traffic, use the delegate's preferred tensor layout, fuse dequantization, and avoid repeated weight or cache movement. If it is I/O-bound, batch or pipeline token steps and remove synchronization points. Re-measure after each change against the 100ms target.
  common_mistake: |
    **The Pitfall:** Focusing solely on FLOPs/MACs and overlooking memory bandwidth or NPU-specific architectural quirks.
    **The Rationale:** Engineers are used to server-side optimization where compute is the primary bottleneck, and fail to realize mobile performance is overwhelmingly dictated by data movement.
    **The Consequence:** Optimization efforts are wasted on compute kernels while the system remains heavily memory-bound, failing to reach the 100ms latency target.
  napkin_math: |
    **Assumptions & Constraints:**
    - Hexagon NPU: 45 TOPS INT8 compute. Memory: 64 GB/s LPDDR5X bandwidth.
    - Target latency: 100 ms per query.
    - Example profiled memory traffic for a mobile-sized query: 4 GB total DRAM traffic, plus host/NPU transfers.

    **Calculations:**
    - Compute limit (100ms): 45 TOPS x 0.1s = 4.5 trillion operations.
    - Memory limit (100ms): 64 GB/s x 0.1s = 6.4 GB of data movement.
    - Example memory time: 4 GB / 64 GB/s = 62.5 ms before cache effects and transfer overhead.
    - If profiling shows traffic above 6.4 GB per query, raw bandwidth alone can exceed the 100 ms target.

    **Conclusion & Interpretation:**
    - **Result: memory traffic can consume most of the 100 ms budget.** A profile showing high DRAM traffic suggests a memory-bound or mixed bottleneck, but the final diagnosis must also account for compute utilization, cache behavior, and host/NPU transfer overhead.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: WARN
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
