schema_version: '1.0'
id: mobile-1554
track: mobile
level: L4
zone: analyze
topic: latency-decomposition
competency_area: latency
bloom_level: analyze
phase: inference
title: Analyzing On-Device LLM Latency on Google Tensor G3
scenario: You are tasked with optimizing an on-device 7B parameter quantized LLM on a Pixel device with a Tensor G3. Users report a 2.5 second end-to-end latency for a 50-word summary from a 500-word input, with a noticeable pause before the first word appears.
question: How would you decompose on-device LLM latency on Tensor G3 into TTFT, TPOT, and processing overheads?
details:
  realistic_solution: |
    Decompose the 2.5 second observation with timestamps, not attribution by guesswork. Record request start, tokenization end, prefill/KV-cache ready, first visible token, every decoded token, and final detokenized response. For the 50-word summary, estimate roughly 75 output tokens; the napkin math gives about 2.0 seconds of optimistic decode time, leaving a 0.5 second residual. That residual is not necessarily TTFT: it can include tokenization, prompt prefill over the 500-word input, scheduling, first-token rendering, and postprocessing. Optimize TTFT with faster tokenization and prefill, TPOT with KV-cache and decode-kernel profiling, and only claim a bottleneck after the timestamp breakdown confirms it.
  common_mistake: |
    **The Pitfall:** Failing to differentiate between Time To First Token (TTFT) and Time Per Output Token (TPOT).
    **The Rationale:** Candidates treat LLM inference as a single monolithic block of latency instead of separating the compute-bound prefill phase from the memory-bound decoding phase.
    **The Consequence:** Applying the wrong optimizations (e.g., trying to quantize weights further to fix slow tokenization/TTFT).
  napkin_math: |
    **Assumptions & Constraints:**
    - Model: 7B INT8 (~7 GB weights).
    - TPU peak compute is treated as an optimistic INT8 operations budget, not guaranteed sustained throughput.
    - Operations accounting convention: 1 MAC is counted as 2 operations.
    - Decode cost example: about 200 GOP/token under that convention.
    - Generation target: 50 words, approximated as 75 output tokens, observed end-to-end latency 2.5 s.

    **Calculations:**
    - Optimistic compute-limited decode rate: 7.5 TOPS / 200 GOP/token = 37.5 tokens/s.
    - Optimistic decode time for 75 tokens: 75 / 37.5 = 2.0 s, or 26.7 ms/token.
    - Observed residual: 2.5 s - 2.0 s = 0.5 s, which should be treated as prefill, tokenization, scheduling, postprocessing, and other fixed overhead, not a precise TTFT measurement.
    - True TTFT and TPOT require instrumentation timestamps: request start, first decoded token, each later token, and response finish.

    **Conclusion & Interpretation:**
    - **Result: estimated TPOT lower bound is about 26.7 ms/token, with about 0.5 s of fixed overhead or prefill in the observation.** The data is insufficient by itself to prove a memory-bound decode path.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 8
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
