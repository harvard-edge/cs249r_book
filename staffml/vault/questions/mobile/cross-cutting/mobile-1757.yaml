schema_version: '1.0'
id: mobile-1757
track: mobile
level: L6+
zone: mastery
topic: fairness-evaluation
competency_area: cross-cutting
bloom_level: analyze
phase: inference
title: 'Bias Mitigation on Edge AI: NPU'
scenario: You are leading the ML engineering team responsible for deploying a critical on-device image classification model for a privacy-sensitive application on mobile devices. Initial internal audits indicate that the model exhibits significant performance disparities across certain demographic subgroups.
question: What metrics would you prioritize, and how would you adapt your evaluation approach to measure intersectional fairness given a 12 GB memory and 34.7 TOPS edge constraint?
details:
  realistic_solution: |
    Prioritize error-rate parity and reliability over a single aggregate fairness score: FPR parity, FNR parity, calibration error, subgroup coverage, and confidence intervals for each intersectional slice. Adapt evaluation with stratified or active sampling so underrepresented slices reach minimum sample counts first, and store only counters, histograms, calibration bins, and uncertainty estimates on device. Use the 12 GB / 34.7 TOPS constraint to avoid raw-image buffers and repeated full reprocessing; log compact summaries locally, upload only consented aggregates, and gate any mitigation decision on statistically meaningful slice coverage.
  common_mistake: |
    **The Pitfall:** Designing comprehensive subgroup evaluation pipelines that ignore device computational overhead.
    **The Rationale:** Candidates propose cloud-like evaluation workflows, forgetting that running hundreds of extra inferences on the edge drains battery and ruins user experience.
    **The Consequence:** The evaluation pipeline is either rejected by the platform team for battery abuse or fails to provide timely insights.
  napkin_math: |
    **Assumptions & Constraints:**
    - Evaluation requirement: 1,000 samples per intersectional slice, 10 slices = 10,000 samples.
    - Candidate inference cost: 20 GOP per sample.
    - Edge budget: 34.7 TOPS peak and 12 GB memory.
    - Store compact counters and histogram bins of 256 bytes per sample equivalent, not full tensors or images.

    **Calculations:**
    - Peak throughput estimate = 34.7 TOPS / 20 GOP = 1,735 samples/sec before utilization losses.
    - Full evaluation compute time at peak = 10,000 / 1,735 = 5.8 seconds; if utilization is only 10%, time is about 58 seconds.
    - Summary memory = 10,000 * 256 B = 2.56 MB, far below 12 GB.
    - For each slice, track TP/FP/TN/FN plus calibration bins; this supports FPR, FNR, coverage, calibration, and confidence intervals without retaining images.

    **Conclusion & Interpretation:**
    - **Result: Summary-Based Intersectional Evaluation Fits**. Prioritize FPR/FNR parity, calibration, coverage, and confidence intervals by slice; use stratified sampling to fill sparse groups and keep memory pressure low with counters and histograms.
status: published
provenance: imported
requires_explanation: true
expected_time_minutes: 22
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
