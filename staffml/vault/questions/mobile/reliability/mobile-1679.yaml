schema_version: '1.0'
id: mobile-1679
track: mobile
level: L3
zone: implement
topic: graceful-degradation
competency_area: reliability
bloom_level: analyze
phase: inference
title: 'Adaptive NLU on Tensor G3: Resource-Aware Graceful Degradation'
scenario: An on-device AI assistant uses an NLU model requiring 5 TOPS and 8 GB of RAM to deliver a 100ms latency. During extended use, thermal throttling drops NPU performance to 3 TOPS and caps memory at 6 GB.
question: How would you design a graceful degradation strategy for the NLU system to maintain critical functionality under dynamic resource constraints?
details:
  realistic_solution: |
    Implement a multi-stage degradation ladder driven by thermal, latency, and memory telemetry. In the healthy state, run the full NLU model. When sustained latency or temperature crosses a warning threshold, shed noncritical features such as reranking, long-context slots, or personalization caches and switch to a smaller INT8/distilled model for core intents. Under stronger memory pressure, preserve the active dialogue state in a compact schema, drop optional context, and move to intent-only classification with cached templates for common actions. If the assistant cannot keep the 100 ms path or loses network/offline resources, fall back to local rules for alarms, calls, settings, and cancellation, while clearly marking unsupported intents. Recover gradually after the device cools and memory headroom returns, promoting one tier at a time and replaying only validated state.
  common_mistake: |
    **The Pitfall:** Deploying a single, high-fidelity NLU model without fallbacks.
    **The Rationale:** Candidates often focus purely on peak accuracy and ignore the reality of mobile thermal throttling and dynamic OS memory management.
    **The Consequence:** When resources become constrained, the system crashes, freezes, or experiences unacceptable latency, leading to a frustrating user experience.
  napkin_math: |
    **Assumptions & Constraints:**
    - Peak constraints: 5 TOPS, 8 GB RAM, 100ms latency.
    - Throttled constraints: 3 TOPS, 6 GB RAM.
    - TOPS is a peak throughput rating; it does not by itself determine end-to-end latency.

    **Calculations:**
    - Compute headroom check: the primary model asks for 5 TOPS while only 3 TOPS is available, so the 100ms target is no longer supportable without measuring the throttled runtime.
    - Memory headroom check: 8 GB required vs 6 GB available leaves a 2 GB deficit before allocator overhead, KV/cache buffers, or OS pressure.

    **Conclusion & Interpretation:**
    - **Result: Insufficient Headroom.** The primary model is likely to miss latency targets and may hit OOM or paging under pressure. Use a smaller quantized/distilled model, paging-aware state management, and feature shedding rather than assuming TOPS ratios predict latency.
status: published
provenance: imported
