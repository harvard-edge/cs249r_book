schema_version: '1.0'
id: edge-0487
track: edge
level: L3
zone: fluency
topic: real-time-deadlines
competency_area: compute
bloom_level: apply
phase: inference
title: Jetson Orin Roofline Comparison for 3D Detection
scenario: You are an ML Systems Engineer for an autonomous delivery robot startup. You must deploy a 3D object detection model onto a Jetson AGX Orin, which is operating in a thermally-constrained 15W power mode (yielding ~68 TOPS and ~102.4 GB/s bandwidth). Your team has profiled two candidate models at the target 30 FPS. Model A requires 80 T-Ops but has a high Arithmetic Intensity (1500 OPS/Byte). Model B requires 60 T-Ops but has a low Arithmetic Intensity (100 OPS/Byte).
question: How should you compare the two 3D detection models for performance per watt on the 15W Jetson AGX Orin?
details:
  realistic_solution: Calculate the arithmetic intensity for each model and compare it to the Jetson AGX Orin's 15W hardware ridge point. Model A has higher total operations, but its AI is above the ridge point, so it can keep the compute units busy. Model B has fewer operations, but its AI is below the ridge point, so performance and energy efficiency are limited by LPDDR5 traffic. Under these assumptions, Model A is the better performance-per-watt candidate.
  common_mistake: |
    **The Pitfall:** Assuming the model with fewer total operations (Model B) is automatically more power-efficient.
    **The Rationale:** Engineers often ignore the energy cost of data movement, equating low FLOPs directly with low power.
    **The Consequence:** Deploying a memory-bound model that wastes significant energy idling compute units while waiting for data transfers.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Jetson AGX Orin 15W mode: 68 TOPS, 102.4 GB/s.
       - Model A AI = 1500 OPS/Byte.
       - Model B AI = 100 OPS/Byte.
    2. **Calculations:**
       - Hardware Ridge Point = 68,000,000,000,000 Ops / 102,400,000,000 Bytes = 664 OPS/Byte.
       - Model A AI (1500) > 664.
       - Model B AI (100) < 664.
    3. **Conclusion & Interpretation:**
       - **Result: Model A is the better perf/W candidate.** Model A is compute-bound because 1500 OPS/Byte is above the 664 OPS/Byte ridge point. Model B is memory-bound because 100 OPS/Byte is below the ridge point, so its lower operation count may still waste the restricted 15W budget on LPDDR5 transfers.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 4
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: gemini-2.5-flash
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
