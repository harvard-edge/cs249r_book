schema_version: '1.0'
id: edge-0453
track: edge
level: L3
zone: implement
topic: neural-architecture-search
competency_area: architecture
bloom_level: apply
phase: training
title: The Real-Time Driver Monitoring Dilemma
scenario: You are the Staff ML Systems Engineer for an automotive Tier 1 supplier. Your team is developing a driver monitoring system on a Jetson AGX Orin. The baseline MobileNetV2-based model correctly detects driver drowsiness in 25ms. The product team now wants to add 'cell phone use' detection to the same model. A naive approach of adding a second classification head and retraining increases model size by only 15%, but latency jumps to 40ms. Profiling shows the model is heavily compute-bound.
question: How do you solve this problem and ship both features while staying within a hard 33ms real-time budget?
details:
  realistic_solution: The correct approach is to use a Mixture-of-Experts (MoE) architecture with an explicit latency budget. The model is compute-bound, so adding both task paths to every frame raises executed FLOPs and produced the measured 40ms miss. Instead, keep each expert path at roughly the single-task MobileNetV2 compute budget and add a tiny gating network that routes each frame to either the drowsiness expert or the phone-use/distraction expert. The baseline is 25ms, leaving 8ms of slack under the 33ms deadline; the gate and routing overhead must fit inside that slack. If profiling keeps gate overhead to about 1-2ms, the active path is about 26-27ms, while the dense multi-head design remains 40ms because it evaluates extra compute every frame.
  common_mistake: |
    **The Pitfall:** Assuming parameter counts scale linearly with latency on accelerators.
    **The Rationale:** Engineers assume adding parameters proportionally increases latency, failing to account for arithmetic intensity or exploring MoE routing.
    **The Consequence:** Missing hard real-time deadlines by deploying dense models that push the hardware past a performance cliff.
  napkin_math: |
    **Assumptions & Constraints:**
    - Target deadline: 33ms.
    - Original Model Latency: 25ms.
    - Naive Multi-Head Latency: 40ms.

    **Calculations:**
    - Proposed Architecture: Mixture-of-Experts (MoE).
    - Deadline slack over baseline = 33ms - 25ms = 8ms.
    - Keep each expert path near the original single-task MobileNetV2 compute budget, so active expert execution is about 25ms.
    - Budget the gate and routing overhead at <= 2ms; this must be verified in profiling because the model is compute-bound.
    - Only one expert is evaluated per frame, so Total MoE Latency: 25ms + 1-2ms = about 26-27ms.
    - Naive dense multi-head latency: 40ms, because extra task logic is evaluated every frame.

    **Conclusion & Interpretation:**
    - **Result: Compute-Optimized for a Compute-Bound Model.** MoE keeps per-frame executed FLOPs near the original baseline, so the estimated 26ms latency stays below the 33ms deadline while the dense multi-head design misses it at 40ms.
  options:
    - Replace the CNN with a more expressive Vision Transformer (ViT) architecture to learn both tasks simultaneously.
    - Run Neural Architecture Search (NAS) to automatically find a single, more efficient architecture for both tasks.
    - Implement a Mixture-of-Experts (MoE) architecture with a gating network that routes inputs to a specialized 'drowsiness' or 'distraction' expert.
    - Apply 8-bit quantization. Since the model is compute-bound, reducing the data size will not help latency.
  correct_index: 2
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 5
validated: true
validation_status: OK
math_verified: true
math_status: CORRECT
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
