schema_version: '1.0'
id: cloud-3048
track: cloud
level: L3
zone: realization
topic: network-bandwidth-bottlenecks
competency_area: networking
bloom_level: apply
phase: both
title: 'Network Bandwidth Bottlenecks: Realize AllReduce Data Volume for 70B Model Training'
scenario: You need to size the AllReduce data volume for training a 70B model (FP16 gradients) across 64 H100 GPUs with 8-way Data Parallelism (DP-8) and 8-way Tensor Parallelism (TP-8).
question: With 70B training on 64 H100s using TP-8 and DP-8, how much gradient data does each GPU AllReduce and how long does it take over 200Gb/s InfiniBand?
details:
  realistic_solution: 'With TP-8, each tensor-parallel rank owns one-eighth of the 70B model: 70B/8 = 8.75B parameters. The DP-8 AllReduce is over that TP shard, not over the full 70B model on every GPU. The FP16 gradient tensor per GPU is 8.75B * 2 bytes = 17.5GB. A ring AllReduce over 8 DP ranks moves 2*(8-1)/8*17.5GB = 30.6GB per GPU. Over 200Gb/s InfiniBand (25GB/s), the bandwidth-bound transfer time is 30.6GB / 25GB/s = 1.22s per step. The direct answer is 17.5GB of gradients all-reduced per GPU, 30.6GB moved per GPU by the ring algorithm, and about 1.22s over 200Gb/s.'
  common_mistake: |
    **The Pitfall:** AllReducing the full 70B gradient from every GPU.
    **The Rationale:** Candidates forget that under Tensor Parallelism, each DP rank only holds its specific TP shard of the model (70B / 8).
    **The Consequence:** Massively overestimating the required network bandwidth by a factor of 8.
  napkin_math: |
    1. **Assumptions & Constraints:**
       - Model: 70B params (FP16).
       - Parallelism: DP-8, TP-8.
       - Network: InfiniBand 200Gb/s (25 GB/s).
       - Ring AllReduce volume per rank = 2 * (P-1)/P * tensor size.
    2. **Calculations:**
       - Per GPU gradient size (TP-8): 70B / 8 = 8.75B params.
       - Per GPU gradient memory (FP16): 8.75B * 2 bytes = 17.5 GB.
       - Ring-AllReduce data moved: 2 * ((8-1)/8) * 17.5 GB = ~30.6 GB per GPU.
       - AllReduce time: 30.6 GB / 25 GB/s = 1.224 seconds.
    3. **Conclusion & Interpretation:**
       - **Result: 17.5GB gradient tensor, 30.6GB moved per GPU, ~1.22s transfer time.** The key is that DP all-reduces the TP shard for each rank, not the full 70B gradient on every GPU.
status: published
provenance: imported
requires_explanation: false
expected_time_minutes: 6
validated: true
validation_status: OK
validation_date: '2026-04-01'
validation_model: claude-sonnet-4-6
math_verified: true
math_status: CORRECT
math_date: '2026-04-03'
math_model: gemini-3.1-pro-preview
human_reviewed:
  status: not-reviewed
  by: null
  date: null
  notes: null
