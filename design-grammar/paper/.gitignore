# LaTeX build artifacts — regenerated by `make paper`.
# The paper source (.tex, .bib), Makefile, figures (.svg + derived .pdf/.png),
# and the compiled paper.pdf ARE checked in. Everything below is ephemeral.
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
