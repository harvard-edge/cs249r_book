## Tokens as the Processor Interface {#sec-vol3-processor-tokenization}

A classical CPU interfaces with memory through binary encodings defined by an Instruction Set Architecture (ISA). In contrast, the stochastic processor core possesses no register file, instruction decoder, or hardware syntax awareness; its substrate consists of continuous tensor contractions over vector spaces.

The host agent runtime bridges external symbolic text with continuous neural state via the *tokenizer*, establishing a bidirectional mapping between UTF-8 byte sequences $\Sigma^*$ and discrete integer identifiers from bounded vocabulary $\mathcal{V}$:

$$\mathcal{T}: \Sigma^* \to \mathcal{V}^*, \quad \mathcal{T}^{-1}: \mathcal{V}^* \to \Sigma^*$$

where $|\mathcal{V}|$ denotes vocabulary cardinality. Discrete *token IDs* constitute the raw data currency of the stochastic computer.

### Subword Tokenization and Discrete Model Representation

Word-level dictionaries fail on open vocabularies (variable names, hashes, timestamps), triggering fatal out-of-vocabulary ($OOV$) faults. Character-level tokenization eliminates $OOV$ errors but inflates sequence lengths $3\times$ to $5\times$, quadraticizing self-attention compute.

Serving runtimes standardize on subword tokenization via Byte-Pair Encoding (BPE)[^fn-bpe-history], building a subword inventory offline:

1. Base vocabulary $\mathcal{V}_0$ contains all 256 raw UTF-8 octets ($0x00$–$0xFF$) and control symbols (`<|im_start|>`, `<|endoftext|>`).
2. Pair co-occurrences are tallied across training corpora.
3. The most frequent pair $(u, v)$ merges into atomic token $w = uv$, updating vocabulary: $\mathcal{V}_{k+1} = \mathcal{V}_k \cup \{w\}$, with merge rule $(u, v) \to w$ ranked by creation priority.
4. Merges iterate until vocabulary cardinality reaches ceiling $|\mathcal{V}| \in [32{,}000, 131{,}072]$.

[^fn-bpe-history]: **Byte-Pair Encoding** (historical data compression): Gage [@gage1994bpe] introduced BPE for compression; @sennrich2016subword adapted it to NLP to eliminate out-of-vocabulary exceptions. In stochastic processor cores, BPE forms the discrete data representation boundary, transforming byte streams into bounded integer vocabulary indices. \index{Byte-Pair Encoding}\index{Tokenizer!Byte-Pair Encoding}

At runtime, BPE executes merge rules by priority rank over raw UTF-8 bytes rather than greedy longest-match prefixing. Because $\mathcal{V}_0$ contains all 256 octets, BPE serializes arbitrary byte streams without $OOV$ exceptions. Decoding maps token IDs to byte chunks concatenated into a raw buffer, performing byte-level reassembly of split multi-byte UTF-8 code points before string emission.

Byte-level BPE [@radford2019language] remaps octets to printable Unicode, mapping space (`0x20`) to `'Ġ'` (U+0120); SentencePiece [@kudo2018sentencepiece] replaces whitespace with `'▁'` (U+2581). Merging whitespace with following characters makes tokenization sensitive to leading spaces: $\mathcal{T}(A) + \mathcal{T}(B) \neq \mathcal{T}(A + B)$.[^fn-whitespace-prefix]

[^fn-whitespace-prefix]: **Whitespace Prefix Encodings**: Because BPE vocabularies encode leading whitespace directly into subword identifiers, modifying indentation or trimming trailing whitespace fundamentally alters downstream token IDs, embedding vector lookups, and continuation probabilities. \index{Whitespace Encoding}\index{Tokenizer!Whitespace Prefix}

In silicon, token ID $x_i \in \{0, \dots, |\mathcal{V}|-1\}$ indexes row $x_i$ in embedding table $\mathbf{W}_{\text{embed}} \in \mathbb{R}^{|\mathcal{V}| \times d_{\text{model}}}$, where $d_{\text{model}}$ is hidden dimension. For prompt $\mathbf{x} = (x_1, x_2, \dots, x_M)$, the accelerator gathers the embedding rows:

$$\mathbf{E} = \mathbf{W}_{\text{embed}}[\mathbf{x}, :] = \begin{bmatrix} \mathbf{W}_{\text{embed}}[x_1, :] \\ \mathbf{W}_{\text{embed}}[x_2, :] \\ \vdots \\ \mathbf{W}_{\text{embed}}[x_M, :] \end{bmatrix} \in \mathbb{R}^{M \times d_{\text{model}}}$$

Each row occupies $d_{\text{model}} \cdot b$ bytes in device memory (parameter byte-width $b$, e.g., $b=2$ for 16-bit BF16), staged into SRAM via coalesced transactions.

#### The Architectural Boundary: Who Owns Tokenization?

The boundary between Host Agent Runtime (Tier 1) and Inference Service Daemon (Tier 2) dictates RPC payload contracts:

1. **Managed Text RPC (Service-Side Tokenization):** The host sends raw UTF-8 strings. Context budgeting ($M \le S_{\max} - K_{\max}$) requires client-side token counting; regex or version drift between host and server causes count mismatch, risking admission rejection.
2. **Deterministic Token-ID RPC (Host-Side Tokenization with Pinned Digest):** The host tokenizes locally on CPU, dispatching token IDs $\mathbf{x} \in \mathcal{V}^*$ alongside a cryptographic *tokenizer hash digest* (SHA-256 of vocabulary and merge tables). The daemon validates this digest during admission control, preventing silent vocabulary misalignment.

### The AST Impedance Mismatch and Serialization Overhead

Compilers parse source code into Abstract Syntax Trees (ASTs) isolating keywords (`def`, `class`), identifiers, and operators. BPE tokenizers optimize purely for statistical byte entropy, unaware of grammar productions, creating an *AST impedance mismatch* (@fig-token-ast-mismatch).

::: {#fig-token-ast-mismatch fig-cap="**Impedance Mismatch Between Compiler ASTs and Statistical BPE**: Comparison of compiler lexical analysis against subword tokenization under cl100k_base, highlighting identifier fragmentation and delimiter fusion." fig-alt="Comparison of compiler AST pipeline against subword BPE tokenization showing identifier splintering and delimiter fusion."}
![](images/svg/token_ast_mismatch.svg){width="100%"}
:::

This mismatch produces three primary failure modes:

1. **Syntactic Boundary Blindness:** Indentation whitespace fuses with subsequent characters. In `cl100k_base`, unindented `def` is token 755 (`"def"`), but two leading spaces yield token 220 (`" "`) and token 711 (`" def"`):
   ```text
   def get_user_id():    -> [755: "def"] [636: " get"] [3398: "_user"] [851: "_id"] [4658: "():"]
     def get_user_id():  -> [220: " "] [711: " def"] [636: " get"] [3398: "_user"] [851: "_id"] [4658: "():"]
   ```
   Tokens 755 and 711 have independent embeddings and logits; trailing prompt whitespace shifts continuation probabilities, suppressing valid completions.
2. **Identifier Splintering:** Identifiers fracture into subwords; `execute_transaction_with_retry` splits into four tokens in `cl100k_base` (`[10469: "execute"]`, `[29984: "_transaction"]`, `[6753: "_with"]`, `[63845: "_retry"]`), compounding autoregressive decode latency.
3. **Lexical Asymmetry:** The mapping $\mathcal{T}$ is non-monotonic: snake_case `get_user_id` consumes three tokens (`["get", "_user", "_id"]`), while camelCase `getUserID` consumes two (`["getUser", "ID"]`), imposing unequal compute on equivalent semantics.

#### The Serialization Overhead Tax

When serializing tool arguments into structured formats (JSON/YAML), punctuation marks, quotes (`"`), backslashes (`\`), braces, and escape sequences compress poorly:

```json
{"tool": "filesystem_write", "parameters": {"path": "/var/log/sys.log", "content": "error: connection reset\n"}}
```

In `cl100k_base`, a raw newline is token 198 (`'\n'`), while a literal escaped newline sequence (`\n`) is mapped to single token 1734, and an escaped quote (`\"`) is token 2153. Although modern tokenizers assign dedicated IDs to common two-character escape sequences, serializing source code inside JSON envelopes inflicts severe token inflation by breaking natural multi-character subword merges. Across indentation boundaries, leading whitespace can no longer merge cleanly with adjacent keyword tokens; furthermore, structural quotes, escaped delimiters, and object framing expand the token footprint by 15 percent to over 40 percent (@tbl-token-compression-ratios).

| Domain / Payload Type | UTF-8 Bytes ($B$) | Chars ($C$) | Tokens ($N$) | Bytes/Token ($B/N$) | Chars/Token ($C/N$) | Systems Hazard Profile |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **English Technical Prose** | 261 | 261 | 38 | 6.87 | 6.87 | Stable compression; high subword coverage. |
| **Python Source Code** | 478 | 478 | 99 | 4.83 | 4.83 | Identifier splintering; indentation fractures. |
| **Structured JSON RPC** | 173 | 173 | 39 | 4.44 | 4.44 | Escaped delimiters and structural punctuation. |
| **Escaped Code in JSON** | 247 | 247 | 63 | 3.92 | 3.92 | Escaped newlines (`\\n`) inflate token count by 43%. |
| **Hex / Base64 / UUID** | 138 | 138 | 79 | 1.75 | 1.75 | Low subword coverage; near character-level encoding. |
| **CJK / Multilingual Text** | 212 | 72 | 80 | 2.65 | 0.90 | 3-byte UTF-8 expansion; 1 to 2 tokens per glyph. |
: **Token compression ratios across computational domains**: Empirical character-to-token compression ratios measured using \texttt{cl100k\_base} (\texttt{tiktoken} v0.9.0), demonstrating the 43 percent token inflation incurred by JSON string escaping. {#tbl-token-compression-ratios}

### Context Capacity, Budget Enforcement, and the KV Cache Footprint

The processor operates within context ceiling $S_{\max}$, bounding sequence length:

$$M + K \le S_{\max}$$

where $M$ is prompt length and $K$ is generated output length. Relying on character heuristics ($1\text{ token} \approx 4\text{ characters}$)[^fn-char-heuristic] fails across code, JSON, and hashes where compression drops below two bytes per token.

[^fn-char-heuristic]: **Token Compression Ratio**: The standard heuristic $1\text{ token} \approx 4\text{ characters}$ fails for indented code, JSON, and hashes, where compression collapses below two bytes per token, doubling context memory demand. \index{Token Compression Ratio}\index{Context Window!Compression Ratio}

#### Input Overflow and Budget Enforcement

Context capacity requires explicit lifecycle governance under the normalized seven-outcome status envelope taxonomy ($\texttt{COMPLETED}$, $\texttt{TRUNCATED}$, $\texttt{REFUSED}$, $\texttt{TRANSPORT\_FAILURE}$, $\texttt{REJECTED}$, $\texttt{CANCELLED}$, $\texttt{FAULTED}$):

1. **Input Overflow Policy:** If $M > S_{\max}$ or $M + K_{\max} > S_{\max}$ under default admission policy $\pi_{\text{overflow}} = \texttt{REJECT}$, the runtime rejects admission before dispatching compute, returning primary status $\texttt{REJECTED}$ with reason code $\texttt{CONTEXT\_OVERFLOW\_EXCEEDED}$; silent truncation discards instructions, destroying determinism.
2. **Output Step Ceiling:** For $M < S_{\max}$, the runtime sets generation ceiling $K_{\max} \le S_{\max} - M$. If decoding reaches $K_{\max}$ without a delimiter or matching suffix stop sequence, execution halts with:
   $$\text{Status} = \texttt{TRUNCATED}, \quad \text{Reason} = \texttt{STEP\_BUDGET\_EXHAUSTED}$$

Because subwords lack AST awareness, truncation severs output mid-statement:

```python
# Truncation Hazard: Incomplete Transaction Logic
# Generation severed at step K_max = 32 tokens
def update_system_state(db_session, account_id):
    try:
        db_session.execute("UPDATE accounts SET status = 'ACTIVE' WHERE id =
```

Severing generation at step $K_{\max}$ drops rollback logic and unclosed syntax. Forwarding truncated text risks compiler crashes on unexpected `EOF` and partial mutation hazards if early commands commit before failure. The host runtime enforces the *Quarantining Invariant*: non-completed responses remain in host DRAM escrow and never reach actuation:

$$\forall \mathcal{E}_{\text{resp}}, \quad S \neq \texttt{COMPLETED} \implies \mathbf{y} \notin \text{ActuationPipeline}$$

#### The Single-Token KV Cache Memory Footprint

While token IDs define logical length $S = M + K$, accelerator memory is dominated by intermediate attention state. To avoid recomputing Key and Value activations for prior tokens $\tau \le t$ at step $t$ ($O(t^2)$ operations), the engine caches them in high-bandwidth memory (HBM) as the *Key-Value (KV) cache*.[^fn-kv-footprint]

[^fn-kv-footprint]: **KV Cache Memory Footprint**: In multi-query and grouped-query attention ($n_{\text{kv}} \ll n_{\text{heads}}$), shared KV heads reduce single-token footprint $4\times$ to $8\times$. At scale, ephemeral KV allocations still exceed parameter weights. \index{Key-Value Cache}\index{Inference Engine!KV Cache Footprint}

For $L$ layers, Key-Value heads $n_{\text{kv}}$, and head dimension $d_{\text{head}}$, channel dimension is $H_{\text{kv}} = n_{\text{kv}} \cdot d_{\text{head}}$ ($H_{\text{kv}} = d_{\text{model}}$ in Multi-Head Attention; $n_{\text{kv}} \ll n_{\text{heads}}$ in Grouped-Query Attention). Storing Key vector $\mathbf{k} \in \mathbb{R}^{H_{\text{kv}}}$ and Value vector $\mathbf{v} \in \mathbb{R}^{H_{\text{kv}}}$ per layer yields $2 \cdot L \cdot H_{\text{kv}}$ scalars. At byte-width $b$ ($b=2$ for 16-bit BF16/FP16; $b=1$ for 8-bit FP8), the single-token footprint is:

$$\text{Mem}_{\text{KV,token}} = 2 \cdot L \cdot H_{\text{kv}} \cdot b = 2 \cdot L \cdot (n_{\text{kv}} \cdot d_{\text{head}}) \cdot b \quad \text{bytes/token}$$

Across sequence length $S = M + K$, total sequence KV cache memory scales linearly:

$$\text{Mem}_{\text{KV}}(\text{seq}) = S \cdot \text{Mem}_{\text{KV,token}} = 2 \cdot L \cdot n_{\text{kv}} \cdot d_{\text{head}} \cdot b \cdot (M + K)$$

For a 70-billion parameter model (such as Llama-3-70B) utilizing Grouped-Query Attention ($L = 80$, $n_{\text{kv}} = 8$, $d_{\text{head}} = 128$, $H_{\text{kv}} = 1024$) in 16-bit BF16 precision ($b = 2$ bytes):

$$\text{Mem}_{\text{KV,token}} = 2 \times 80 \times 1024 \times 2 = 327{,}680\text{ bytes} \approx 328\text{ KB/token}$$

At extended context $S = 128{,}000$ tokens, a single invocation's KV cache consumes:

$$\text{Mem}_{\text{KV}}(\text{seq}) = 128{,}000 \times 327{,}680\text{ bytes} \approx 41.94\text{ GB}$$

At 16-bit precision, a single 128k trajectory occupies $41.94\text{ GB}$ in device memory. On a single 80 GB H100 GPU hosting a 70B parameter model in FP8 (70 GB weights), an extended 128k context KV cache (even compressed to FP8 at $\approx 21.0\text{ GB}$) cannot fit simultaneously ($70\text{ GB} + 21\text{ GB} = 91\text{ GB} > 80\text{ GB}$). Accommodating extended 128k contexts at 70B scale requires either multi-accelerator tensor parallelism across multiple 80 GB GPUs or next-generation accelerators with expanded memory capacity (such as the 180 GB NVIDIA B200 SXM); on a standalone 80 GB card, isolated execution is physically bounded to shorter contexts such as $S \le 32{,}768$ tokens (where the FP8 KV cache consumes $\approx 5.37\text{ GB}$, fitting within the 10 GB headroom). This steep memory scaling makes context capacity the primary physical constraint on concurrency, motivating paged attention virtual memory architectures and prefix-sharing techniques (detailed in @sec-vol3-kvcache-radix-tree).

Once staged as an integer token array, how does the processor compute its candidate output?
