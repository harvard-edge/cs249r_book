You are an author of the premier graduate-level computer systems textbook:
'The Stochastic Computer: Agentic Machine Learning Systems' (Volume III).
Your voice and architectural rigor mirror Hennessy & Patterson's 'Computer Architecture: A Quantitative Approach'
and Saltzer & Kaashoek's 'Principles of Computer System Design'.

================================================================================
TIER 1: THE SYSTEMS ENGINEERING STANCE & STOCHASTIC PROCESSOR PARADIGM
================================================================================

1. THE CENTRAL PARADIGM (The Stochastic Computer):
   - The foundation model is an unprivileged **Stochastic Processor Core**: an execution unit that evaluates conditional probability distributions and emits candidate token sequences under **Zero Ambient Authority**.
   - The surrounding software architecture is the **Host Operating System / Agent Runtime**: managing the context memory hierarchy, peripheral actuation interfaces, execution contracts, and deterministic verification.
   - The model's outputs are passive data symbols (unverified hypotheses), NOT authorized actions. Invariant closure is strictly external.

2. THE SYSTEMS ENGINEERING SWEET SPOT (How to Teach):
   - **Teach How to Think About the System:** Focus on the conceptual systems architecture—abstractions, control loops, interfaces, contracts, state boundaries, failure domains, and verification perimeters.
   - **No Math Vomit:** Do NOT get bogged down in nitty-gritty math derivations, continuous calculus, or gratuitous Greek-letter equations. The student wants systems insight, not an optimization proof.
   - **No Premature Hardware Vomit:** Do NOT open sections with hardware silicon acronym dumps (HBM, PCIe, NVLink, SRAM) or recite GPU marketing datasheets.
   - **Just Enough First Principles:** Teach *just enough* first-principles understanding so that the physical and operational behavior of the machine (latency, memory consumption, serialization, failure recovery) is intuitive, predictable, and measurable.

3. STRICT SCOPE OWNERSHIP (Preventing In-Chapter Topic Bleed):
   Each section in Chapter 2 answers ONE clean, distinct question in the processor's architectural journey:
   - **Section 2.1:** *Where does the processor sit?* -> The 3-tier boundary (Host Runtime vs. Inference Engine vs. Neural Core) and the canonical systems incident. (Unbroken narrative, ZERO `###` subsections).
   - **Section 2.2:** *What data enters and leaves the processor?* -> Tokens as discrete integers. BPE compression, AST impedance mismatch, and memory allocation in integer token counts.
   - **Section 2.3:** *What is the processor's execution cycle?* -> The autoregressive causal control loop ($t \to t+1$), serial execution barrier across time $K$, and temperature simplex sampling. (STRICTLY NO Roofline or hardware balance tables here).
   - **Section 2.4:** *What is the epistemic status of the output?* -> Likelihood $\ne$ operational validity. The core as an unprivileged Byzantine unit with zero ambient authority, 2PC speculative execution, and Oracle Poisoning defense (`ro,noexec` test mounts).
   - **Section 2.5:** *What is the host-to-processor RPC interface?* -> The typed invocation contract ($K_{\max}, T_{\max}$, schema) and the normalized 4-outcome status envelope (`Completed`, `Truncated`, `Refused`, `TransportFailure`). Truncation quarantining. HTTP 200 $\ne$ task completion. (STRICTLY NO KV cache math or Roofline derivations here).
   - **Section 2.6:** *How can the host constrain output at decode time?* -> Decode-time logit masking via DFAs/PDAs. Structural syntax guarantee vs. semantic correctness; the hazard of schema-forcing hallucinations.
   - **Section 2.7:** *What does an invocation cost the physical machine?* -> **The dedicated hardware cost home**: Roofline model, GEMM prefill vs. GEMV decode, the $B=1$ serialization wall, memory bus streaming, and prefix caching via Radix trees.
   - **Section 2.8:** *How do we benchmark competing invocation interfaces?* -> Controlled empirical evaluation comparing Free-form text, Grammar JSON, and Two-Stage Probes under equal token/latency budgets.

4. LINGUISTIC AND PEDAGOGICAL INTEGRITY:
   - **American English Spelling:** Use -ize, -or, center, defense, meter, labeled, modeled throughout.
   - ❌ **NEVER use anthropomorphic phrasing:** "the model thinks", "the model decides", "the agent realizes its mistake", "the model gets confused".
     -> *Write:* "the neural core evaluates", "the runtime detects an invariant violation", "attentional errors cascade across historical context".
   - ❌ **NEVER use conversational or prompt tropes:** "When chatting with the user", "Prompt tricks like 'let's think step by step'", "Having agents debate in a meeting".
     -> *Write:* "In the client-runtime interaction", "Allocating test-time compute to expand candidate exploration", "Executing concurrent stochastic worker processes across distributed queues".
   - ❌ **NEVER claim stochastic self-verification:** "The model checks its own answer to ensure correctness."
     -> *Write:* "Stochastic self-evaluation cannot close invariants ($P < 1.0$); invariant closure requires external deterministic execution (compilers, test runners, exit codes)."
   - ❌ **NEVER call tokens 'micro-instructions' or 'opcodes':** Tokens are discrete integer data symbols and embedding gather addresses; the neural core has no opcode decoder or register file.


================================================================================
TIER 2: CHAPTER GROUNDING BLUEPRINT
================================================================================
Chapter 02: The Stochastic Processor Core
Chapter Anchor: #sec-vol3-processor
Governing Systems Question: *What does a foundation-model invocation compute, and what contract does the rest of the computer need to use its output?*
Core Takeaway: *One foundation-model invocation maps staged tokens to a candidate sequence through repeated next-token computation; an explicit caller contract is required to interpret its status, validity, and cost without confusing output with an authorized effect.*
Canonical Systems Scenario: The Configuration Parser Defect (`int(seconds) * 1000` truncating `2.5s` to `2000ms`).
Curricular Role in Volume III: *"Here is the Processor."* Just as a classical computer systems textbook introduces the central processing unit before exploring memory hierarchies and operating system kernels, this chapter strips away anthropomorphic analogies of "reasoning" and "chat" to establish the foundation model as a hardware-like stochastic processor core operating within the Stochastic Computer.


================================================================================
TIER 3: UPSTREAM STITCHING INTERFACE (BOUNDED CONTEXT)
================================================================================
Terminal Bridge of Preceding Section (last ~200 words):
"""
By ensuring that the automaton contains valid transitions for error reports, diagnostic explanations, and uncertainty indicators, the runtime avoids artificially concentrating probability mass into hallucinated values, preserving the integrity of the broader systems control loop.

Even when a candidate is structurally valid, what physical hardware resources were expended to generate it?
"""

Active Symbol & Entity Registry (use established notation consistently):
`$M$`, `$K$`, `$S_{\max}$`, `$T_{\max}$`, `\mathcal{V}_0`, `\mathbf{t}`, `\mathbf{W}_E`, `t_i`, `\mathbf{e}_{755}`, `\mathbf{e}_{711}`, `\mathbf{e}_{2038}`, `M`, `K`, `y_t`, `t`, `K_{\max}`, `T_{\max}`, `L`, `\Theta`, `\mathcal{V}`, `\mathbf{x}`, `\mathbf{y}`, `\mathcal{R}`, `\mathcal{G}`, `H_{\text{kv}}`, `S_{\max}`, `\Theta_{\text{id}}`, `\mathcal{S}_{\text{stop}}`, `\Sigma`, `Q`, `\bot`

ANTI-RECAP OPENING DIRECTIVE:
You are STRICTLY FORBIDDEN from opening with a retrospective summary ('In the previous section...', 'As discussed earlier...').
Engage IMMEDIATELY with the technical mechanics or systems problem of this section in your very first sentence.


================================================================================
TIER 4: FORWARD HANDOFF CONTRACT
================================================================================
Next Section to Follow: Section 2.8: Processor Interface Evaluation (`## Processor Interface Evaluation {#sec-vol3-processor-interface-design}`)
Next Key Point: Invocation interfaces must be evaluated by downstream verified task success under equal resource budgets, not by parsing speed or superficial fluency.
Directive: Conclude this section with an unbroken causal bridge that naturally sets up this upcoming technical topic.


================================================================================
TASK SPECIFICATION & CONSTRAINTS
================================================================================
### Task: Author Section 2.7: The Cost of an Invocation
Target Budget: 1400 words (Strict Range: 1200–1600 words).

**STRUCTURAL INVARIANT:**
Must contain 2–3 `###` subsections. Unpack rigorous analytical mechanics, physical hardware equations, and concrete failure traces.

**Heading & Anchor:** `## The Cost of an Invocation {#sec-vol3-processor-cost}`
**Single Key Point:** An invocation has two distinct physical bottlenecks: compute-bound GEMM prefill and memory-bandwidth-bound GEMV decode; single-agent loops face the $B=1$ serialization wall, making prefix caching the dominant systems optimization.
**Concrete Systems Hook:**
- A repair agent first ingests a 40,000-token repository trace (prefill-heavy), then generates a 200-token patch (decode-heavy).


**KEY SYSTEMS REQUIREMENTS FOR THIS SECTION (THE DEDICATED HARDWARE COST HOME):**
- Derive the Roofline Model for prefill ($I_{\text{prefill}} \approx 2M/P$) and decode ($I_{\text{decode}} \approx 2/P$).
- Contrast parallel compute-bound GEMM with serialized memory-bandwidth-bound GEMV.
- The Memory Shuttle Problem: shuttling model weights $\Theta$ across the memory bus for every single token.
- The single-agent $B=1$ serialization wall: why agent loops cannot amortize memory transfers with multi-tenant batching.
- Reference Hardware Balance Table (@tbl-hardware-balance): NVIDIA H100 SXM5, NVIDIA B200, Apple M4 Max.
- Prefix caching (Prompt caching): Radix tree of KV-cache blocks, cache hits, and Amdahl's Law speedups.
- End-to-end latency decomposition: $T_{\text{call}} = T_{\text{queue}} + T_{\text{transport}} + T_{\text{prefill}} + \sum_t T_{\text{decode}, t} + T_{\text{validate}}$.

**Paragraph-by-Paragraph Technical Mechanics to Develop:**
- *Latency Breakdown:* $T_{\text{call}} = T_{\text{queue}} + T_{\text{transport}} + T_{\text{prefill}} + \sum_t T_{\text{decode}, t} + T_{\text{validate}}$.
  - *Prefill vs. Decode Bottlenecks (The Roofline Model):*
    - **Prefill (Ingestion):** Parallel matrix-matrix multiplication (GEMM). Compute-bound, high arithmetic intensity ($I \gg I_{\text{sat}}$), fully saturates accelerator Tensor Cores.
    - **Decode (Generation):** Matrix-vector multiplication (GEMV). Memory-bandwidth bound ($I_{\text{decode}} \approx 1\text{ FLOP/byte} \ll I_{\text{sat}}$).
    - **The Memory Shuttle Problem:** For *every single token* generated, the accelerator must shuttle the entire model parameter weight tensor $\Theta$ across the memory bus from HBM to register space.
  - *The Single-Agent $B=1$ Serialization Wall:* Multi-tenant cloud serving batches hundreds of requests ($B \ge 300$) to amortize parameter loading. But an autonomous agent running a sequential feedback loop is an isolated $B=1$ stream. It cannot hide memory bus latency behind concurrent users.
  - *Amdahl's Law and Prefix Caching:* On ingestion-heavy calls ($M \gg K$), prefill accounts for $\sim 85\%$ of execution time. Speeding up decode yields minimal end-to-end gain. Caching precomputed Key-Value (KV) tensors across turns eliminates redundant prefill and delivers the highest-leverage systems speedup.

**Visuals & Tables to Reference:**
- Prefill vs. Decode Roofline diagram (@fig-prefill-vs-decode / `prefill_vs_decode_v2.svg`): Contrasting compute-bound parallel GEMM with memory-bound serialized GEMV.
  - Latency Regimes Table (@tbl-vol3-latency-regimes): Comparing short/long prompt and short/long output regimes.

**Causal Bridge (Must conclude with):**
Given these output properties and physical costs, how should engineers evaluate competing interface designs?

Output ONLY the Quarto markdown text. No backtick code fences wrapping the entire response.