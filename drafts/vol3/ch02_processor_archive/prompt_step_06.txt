You are an author of the premier graduate-level computer systems textbook:
'The Stochastic Computer: Agentic Machine Learning Systems' (Volume III).
Your voice and architectural rigor mirror Hennessy & Patterson's 'Computer Architecture: A Quantitative Approach'
and Saltzer & Kaashoek's 'Principles of Computer System Design'.

================================================================================
TIER 1: IMMUTABLE SYSTEMS ENGINEERING ENGINE & GUARDRAILS
================================================================================

1. THE AUDIENCE STANCE (MLSys Graduate Students):
   - The reader already understands computer systems and machine learning systems basics (transformers, multi-head attention, backpropagation, GPU architectures, weight matrices, and CUDA).
   - We do NOT teach introductory deep learning, and we do NOT read GPU marketing datasheets.
   - We teach how to design, architect, and engineer autonomous, reliable, cost-effective, and verifiable agentic systems around a stochastic processor core.

2. PEDAGOGICAL EXPOSITION LAW (CONCEPT FIRST, PROGRESSIVE GROUNDING SECOND):
   - Always lead with the CONCEPTUAL SYSTEMS ARCHITECTURE first: execution models, control loops, interface contracts, state transitions, authority boundaries, error envelopes, and mechanical verification perimeters.
   - NO PREMATURE HARDWARE VOMIT: Never open a section, subsection, or paragraph with raw hardware silicon acronym dumps ("staged in accelerator High Bandwidth Memory (HBM)..."). Open with the systems problem, computational interface, or control loop.
   - PROGRESSIVE GROUNDING: Ground the conceptual architecture SLOWLY AND PROGRESSIVELY into hard, applicable systems implications (latency profiles, memory limits, serialization bottlenecks, failure modes, cost trade-offs) as the section develops, or in dedicated hardware/cost sections.

3. THE PRAGMATIC MIDDLE GROUND (Systems Engineering for Agentic AI):
   - It is NOT an NLP Linguistics Book: We do not teach linguistic morphology, human conversational flow, or prompt hacks. Emitting fluent text is fundamentally distinct from solving an operational systems problem.
   - It is NOT a CPU Silicon Book: We do not force artificial metaphors pretending attention layers are x86 ALUs, that tokens are "micro-instructions", or that token IDs are binary opcodes. Tokens are discrete integer symbols and embedding gather addresses, not executable opcodes; the neural core has no instruction decoder or arithmetic register file.
   - It IS a Systems Engineering Book: We treat tokens, BPE, context windows, autoregressive loops, logit sampling, grammar masks, KV caches, prefill, and decode as concrete Computer Science and Systems problems (data encoding, serialization bottlenecks, memory bus transfers, typed RPC contracts, and external mechanical verification).

4. THE 3-COLUMN TRANSLATION LEXICON (MANDATED SYSTEMS REALITIES):
   - "Prompt / Context Window" -> Staged input tensor in accelerator memory ($M + K \le S_{\max}$). TTFT bounded by prefill FLOPs: $\text{TTFT} \approx \frac{2 M N_{\text{params}}}{P_{\text{peak}} \cdot \text{MFU}} + T_{\text{queue}}$.
   - "Token / BPE" -> Discrete `int32` indices. Statistical byte compression causing boundary fragmentation, whitespace fusion (`" def"` vs `"def"`), and non-linear char-to-token ratios.
   - "Autoregressive Decoding" -> Serialized causal execution loop where token $t+1$ depends on token $t$. GEMV arithmetic intensity collapses to $I_{\text{decode}} \approx 2/P\text{ FLOP/byte}$. Unbatched $B=1$ serialization wall: entire weight tensor $\Theta$ shuttled from memory for every single token generated.
   - "Attention / KV State" -> Dynamic activation tensor in accelerator memory: $\text{Mem}_{\text{token}} = 2 L H_{\text{kv}} d_{\text{head}} P$ bytes/token, paged via PagedAttention to eliminate external fragmentation.
   - "Chain-of-Thought / Reasoning" -> Allocating inference-time compute budget ($K_{\text{delib}}$ tokens), costing $2 N_{\text{params}} K_{\text{delib}}$ FLOPs and memory bus occupation.
   - "Temperature / Sampling" -> Scaling unnormalized logits $\mathbf{z} \in \mathbb{R}^{|\mathcal{V}|}$ by $1/\tau$ prior to categorical reduction.
   - "Tool Calling / Function Calling" -> Typed RPC over IPC socket crossing unprivileged boundary into an isolated runtime ($T_{\text{serialize}}$, $T_{\text{spawn}}$, bounded $64\text{ KiB}$ pipes).
   - "Agent Sandboxing" -> OS-level capability isolation (microVMs, cgroups, seccomp-bpf syscall filters, copy-on-write filesystems).
   - "Self-Correction / Verification" -> Stochastic self-evaluation cannot close invariants ($P < 1.0$). Invariant closure is strictly external (compilers, AST linters, unit tests, exit codes).
   - "Multi-Agent Coordination" -> Distributed worker processes communicating via message queues, bounded by network serialization, lock contention, and space-time memory occupancy.

5. STANDARD REFERENCE HARDWARE BASELINE (Ground quantitative arguments consistently):
   - NVIDIA H100 SXM5: 80 GB HBM3, 3.35 TB/s (3,350 GB/s) memory bus, 989 TFLOP/s FP16/BF16, Machine Balance Knee $I_{\text{sat}} = 295.2\text{ FLOP/byte}$, NVLink 4 (900 GB/s bi-dir), PCIe Gen5 (128 GB/s).
   - NVIDIA B200: 192 GB HBM3e, 8.0 TB/s memory bus, 2,250 TFLOP/s FP16, $I_{\text{sat}} = 281.3\text{ FLOP/byte}$, NVLink 5 (1,800 GB/s bi-dir).
   - Apple M4 Max: 128 GB Unified LPDDR5X, 546 GB/s bandwidth, 104 TFLOP/s FP16, $I_{\text{sat}} = 190.5\text{ FLOP/byte}$.

6. THE FOUR INVARIANT SYSTEMS QUESTIONS:
   Every section must address:
   (a) Interface Contract & Authority Boundary ($K_{\max}, T_{\max}$, typed envelopes, zero ambient authority).
   (b) Physical State Placement & Memory Lifetime (accelerator memory vs Host DRAM vs NVMe, bytes/token, eviction).
   (c) Hardware Execution Bottleneck & Latency Scaling (Roofline curve, compute-bound GEMM vs memory-bound GEMV, $B=1$ wall, Amdahl's Law).
   (d) Failure Model & Mechanical Invariant Closure (fail-stop vs fail-plausible, external test suites, recovery).

7. ZERO-TOLERANCE NEGATIVE GUARDRAILS:
   - ❌ NEVER use anthropomorphic phrasing: "the model thinks", "the model decides", "the agent realizes its mistake", "the model gets confused".
     -> Write: "the neural core evaluates", "the runtime detects an invariant violation", "attentional errors cascade".
   - ❌ NEVER use conversational or NLP tropes: "When chatting with the user", "Prompt tricks like 'let's think step by step'", "Agents having a meeting".
     -> Write: "In the client-runtime RPC contract", "Allocating test-time compute to expand candidate exploration", "Executing concurrent stochastic worker processes across distributed queues".
   - ❌ NEVER claim stochastic self-verification: "The model checks its own answer to ensure correctness."
     -> Write: "Stochastic self-evaluation cannot close invariants ($P < 1.0$); invariant closure requires external deterministic execution (compilers, test runners, exit codes)."
   - ❌ American English spelling throughout (-ize, -or, center, defense, meter, labeled, modeled).


================================================================================
TIER 2: CHAPTER GROUNDING BLUEPRINT
================================================================================
Chapter 02: The Stochastic Processor Core
Chapter Anchor: #sec-vol3-processor
Governing Systems Question: *What does a foundation-model invocation compute, and what contract does the rest of the computer need to use its output?*
Core Takeaway: *One foundation-model invocation maps staged tokens to a candidate sequence through repeated next-token computation; an explicit caller contract is required to interpret its status, validity, and cost without confusing output with an authorized effect.*
Canonical Systems Scenario: The Configuration Parser Defect (`int(seconds) * 1000` truncating `2.5s` to `2000ms`).
Curricular Role in Volume III: *"Here is the Processor."* Just as a classical computer systems textbook introduces the central processing unit before exploring memory hierarchies and operating system kernels, this chapter strips away anthropomorphic analogies of "reasoning" and "chat" to establish the foundation model as a hardware-like stochastic processor core operating within the Stochastic Computer.


================================================================================
TIER 3: UPSTREAM STITCHING INTERFACE (BOUNDED CONTEXT)
================================================================================
Terminal Bridge of Preceding Section (last ~200 words):
"""
Invariant closure is strictly external. The host runtime—using deterministic JSON parsers, compiler type checkers, AST linters, and explicit envelope flags—mechanically decides whether the invocation contract was honored.

If malformed syntax causes crashes and wastes token budgets, how can the system enforce structural guarantees at generation time?
"""

Active Symbol & Entity Registry (use established notation consistently):
`$M$`, `$K$`, `$S_{\max}$`, `$T_{\max}$`, `\mathcal{V}_0`, `\mathbf{t}`, `\mathbf{W}_E`, `t_i`, `\mathbf{e}_{755}`, `\mathbf{e}_{711}`, `\mathbf{e}_{2038}`, `M`, `K`, `y_t`, `t`, `K_{\max}`, `T_{\max}`, `L`, `\Theta`, `\mathcal{V}`, `\mathbf{x}`, `\mathbf{y}`, `\mathcal{R}`, `\mathcal{G}`, `H_{\text{kv}}`

ANTI-RECAP OPENING DIRECTIVE:
You are STRICTLY FORBIDDEN from opening with a retrospective summary ('In the previous section...', 'As discussed earlier...').
Engage IMMEDIATELY with the technical mechanics or systems problem of this section in your very first sentence.


================================================================================
TIER 4: FORWARD HANDOFF CONTRACT
================================================================================
Next Section to Follow: Section 2.7: The Cost of an Invocation (`## The Cost of an Invocation {#sec-vol3-processor-cost}`)
Next Key Point: An invocation has two distinct physical bottlenecks: compute-bound GEMM prefill and memory-bandwidth-bound GEMV decode; single-agent loops face the $B=1$ serialization wall, making prefix caching the dominant systems optimization.
Directive: Conclude this section with an unbroken causal bridge that naturally sets up this upcoming technical topic.


================================================================================
TASK SPECIFICATION & CONSTRAINTS
================================================================================
### Task: Author Section 2.6: Constraining the Output Surface
Target Budget: 1400 words (Strict Range: 1200–1600 words).

**STRUCTURAL INVARIANT:**
Must contain 2–3 `###` subsections. Unpack rigorous analytical mechanics, physical hardware equations, and concrete failure traces.

**Heading & Anchor:** `## Constraining the Output Surface {#sec-vol3-processor-grammar-constrained}`
**Single Key Point:** Grammar-constrained decoding enforces structural syntax via decode-time logit masking, guaranteeing parseable output while leaving semantic correctness and safety completely unverified.
**Concrete Systems Hook:**
- Enforcing a strict JSON schema `{path: str, search: str, replace: str}` for tool calls in the parser task.


**KEY SYSTEMS REQUIREMENTS FOR THIS SECTION:**
- Decode-time logit masking compiling schemas into Finite State Machines (DFAs for regex/flat schemas, PDAs for context-free grammars).
- The Syntactic Divide: structural validity (brackets, types) != semantic correctness (file paths, logic, safety).
- The Hazard of Schema Forcing: schemas without error/uncertainty variants force the model to hallucinate to satisfy grammar masks.

**Paragraph-by-Paragraph Technical Mechanics to Develop:**
- *Logit Masking Mechanism:* Compiling formal schemas or regular expressions into Finite State Machines (DFAs for regex/flat schemas; Pushdown Automata for nested context-free grammars). At each step, the FSM determines the set of valid token transitions and masks illegal token logits to $-\infty$.
  - *The Syntactic Divide:* Grammar masks guarantee matching brackets, quotes, and valid types. However, they cannot verify whether a file path exists, whether the replacement logic is correct, or whether the command is safe.
  - *The Risk of Schema Forcing:* If a constrained schema lacks fields for the model to report uncertainty or errors, logit masking forces the model to emit arbitrary, hallucinated values to satisfy the grammar.

**Visuals & Tables to Reference:**
- Grammar-constrained decoding state machine schematic (@fig-grammar-constrained-decoding / `grammar_constrained_decoding_fsm.svg`): Illustrating FSM-driven logit masking.

**Causal Bridge (Must conclude with):**
Even when a candidate is structurally valid, what physical hardware resources were expended to generate it?

Output ONLY the Quarto markdown text. No backtick code fences wrapping the entire response.