extends: substitution
message: "Use single space after periods: '%s' instead of '%s'."
level: error
ignorecase: false
swap:
  # Remove double spaces after periods
  '\.  ': '. '
  # Remove double spaces in general
  '  ': ' '