# Runtime dependencies for the news CLI.
# Keep in sync with the `dependencies` block in pyproject.toml.
# Install with:
#     pip install -r site/newsletter/requirements.txt
#
# Or install the CLI as an editable package (preferred), which gives you
# a `news` command on PATH and tracks source changes automatically:
#     pip install -e site/newsletter

rich>=13.0.0
requests>=2.31.0
python-frontmatter>=1.1.0
