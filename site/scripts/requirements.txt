# Dependencies for the site stats pre-render step (site/scripts/build_stats.py).
#
# Only the GA4 tier needs a third-party package. The repo-derived counts and the
# GitHub/Buttondown fetches use the standard library, so the script still runs
# and degrades gracefully when this file has not been installed.
google-analytics-data>=0.18
