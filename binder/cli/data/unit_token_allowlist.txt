# Allowlist: number+letter tokens that are NOT a missing unit space.
#
# Used by `binder check numbers --scope unit-spacing`. One token per line,
# matched case-sensitively as a whole word. Lines starting with `#` are
# comments.
#
# Add an entry when a token legitimately runs a digit into letters because it
# is a vendor part number, an accelerator SKU, a model-size shorthand, or
# another case-sensitive proper noun where inserting a space would be wrong.
# Include a comment naming the exception so future editors know why the rule
# is relaxed. Do NOT add a genuine measurement here — "100ms" is a defect, and
# the fix is the space, not an allowlist entry.
#
# Added 2026-09-20 alongside the math/code/table masking fix in
# _run_unit_spacing; before that the check had no exemptions at all and every
# one of its 20 Volume III hits was a false positive.

# --- NVIDIA accelerator SKUs (capacity is part of the part number) ---
A100-SXM4-80GB                       # NVIDIA part number
A100-SXM4-40GB                       # NVIDIA part number
A100-PCIE-40GB                       # NVIDIA part number
A100-PCIE-80GB                       # NVIDIA part number
H100-SXM5-80GB                       # NVIDIA part number
H100-PCIE-80GB                       # NVIDIA part number
H200-SXM5-141GB                      # NVIDIA part number

# --- Model-size shorthand (canonical in the field: "a 70B dense model") ---
1B                                   # parameter-count shorthand
3B                                   # parameter-count shorthand
7B                                   # parameter-count shorthand
8B                                   # parameter-count shorthand
13B                                  # parameter-count shorthand
32B                                  # parameter-count shorthand
34B                                  # parameter-count shorthand
70B                                  # parameter-count shorthand
405B                                 # parameter-count shorthand
