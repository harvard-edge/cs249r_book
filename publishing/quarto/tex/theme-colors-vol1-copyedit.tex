% =============================================================================
% VOLUME I THEME COLORS — COPYEDIT VERSION (black)
% =============================================================================
% For the MIT Press copy-editor PDF we suppress all accent colors (normally
% Harvard Crimson). This file must be loaded BEFORE header-includes.tex so
% that:
%   1. \providecolor{accentcolor}... inside header-includes.tex is a no-op
%      (accentcolor already exists here as black).
%   2. \colorlet{crimson}{accentcolor} in header-includes.tex aliases
%      `crimson` to black — cascading through every \color{crimson} in
%      chapter headings, TOC, page headers, sidenotes, and TikZ overlays.
% =============================================================================

\definecolor{accentcolor}{HTML}{000000}  % Black (copyedit only)
