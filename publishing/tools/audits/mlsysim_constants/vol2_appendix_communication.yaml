chapter: vol2/appendix_communication
source_file: book/quarto/contents/vol2/backmatter/appendix_communication.qmd
status: migrated
python_cells:
- label: appendix-comm-setup
  start_line: 26
  end_line: 191
  lego_class: CommFoundations
  purpose: Computes chapter-wide alpha-beta interconnect values, ring/tree AllReduce
    examples, pipeline bubble values, and compression break-even tables.
  constants:
  - name: interconnect_bandwidths
    value: INFINIBAND_NDR_BW_GBS=50, INFINIBAND_HDR_BW_GBS=25, ROCE_100G_BW_GBS=12.5,
      NVLINK_H100_BW=900 GB/s, PCIE_GEN5_BW=64 GB/s
    kind: hardware_system_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: The interconnect table, NDR crossover example, hierarchical NVLink/NDR
      comparison, and AllReduce worked example already render from MLSysIM bandwidth
      references.
  - name: interconnect_latencies
    value: IB_NDR_LATENCY_US=5, IB_HDR_LATENCY_US=7, ROCE_LATENCY_US=10, TCP_LATENCY_US=50,
      LATENCY_NVLINK=500 ns, LATENCY_PCIE_GEN5=1000 ns
    kind: hardware_system_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: The alpha-beta parameter table and NDR crossover prose stay tied
      to the canonical MLSysIM latency values.
  - name: gpus_per_host
    value: 8 GPUs per host
    kind: hardware_system_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.core.units (physics/units) or mlsysim.physics
    preserves_rendered_value: true
    narrative_check: The hierarchical AllReduce prose remains valid because it explicitly
      describes eight local GPUs per node.
  - name: ring_n_gpus
    value: '256'
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The worked examples explicitly choose a 256-GPU cluster scale;
      this is not a reusable system constant.
  - name: ring_msg_bytes
    value: 1 GB, described as a 500M-parameter FP16 gradient
    kind: derived_quantity
    current_source: local_literal
    should_change: false
    target_source: Derive as 500 * MILLION * BYTES_FP16, or add a named MLSysIM example
      payload helper if reused.
    preserves_rendered_value: true
    narrative_check: The displayed 1 GB payload and the adjacent 500M-parameter FP16
      prose remain unchanged if the byte count is derived from the precision constant.
  - name: allreduce_formulas
    value: Ring and tree AllReduce times
    kind: derived_quantity
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.physics.calc_ring_allreduce_time and calc_tree_allreduce_time
    preserves_rendered_value: true
    narrative_check: The ring-vs-tree notebook already uses MLSysIM formula helpers
      and preserves the rendered timing comparison.
  - name: crossover_formula
    value: n* = alpha * beta
    kind: derived_quantity
    current_source: local_formula
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The expression is the local mathematical definition being taught;
      it is simple enough to remain inline, with alpha and beta already sourced from
      MLSysIM.
  - name: pipeline_scenario
    value: p_stages=8, m_microbatches=16, v_interleave=4
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The interleaved scheduling paragraph explicitly introduces the
      p=8, m=16, V=4 example.
  - name: pipeline_table_points
    value: p in {4, 8}; m in {8, 16, 32, 64}
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: These are chosen table coordinates for illustrating bubble behavior,
      not canonical model or hardware constants.
  - name: pipeline_bubble_formula
    value: calc_pipeline_bubble for standard bubble; local formula for interleaved
      bubble
    kind: derived_quantity
    current_source: mixed
    should_change: false
    target_source: mlsysim.physics.calc_pipeline_bubble(..., v_stages=v_interleave)
    preserves_rendered_value: true
    narrative_check: The interleaved prose remains valid because the MLSysIM helper
      implements the same denominator with virtual stages.
  - name: compression_ratio
    value: 64x
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The Top-k worked example defines 64x compression as the hypothetical
      sparsification result.
  - name: compression_overhead_ms
    value: 2.0 ms encode + decode
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The codec overhead is a local worked-example assumption, not
      a hardware or model constant.
  - name: compression_breakeven_grid
    value: AllReduce times 10, 40, 100 ms; compression ratios 4, 16, 64, 256
    kind: scenario_assumption
    current_source: local_literal
    should_change: false
    target_source: null
    preserves_rendered_value: true
    narrative_check: The grid values are illustrative table axes for the break-even
      calculation.
  - name: compression_breakeven_formula
    value: comm_ms * (1 - 1/compression_ratio)
    kind: derived_quantity
    current_source: local_formula
    should_change: false
    target_source: new mlsysim.physics.calc_compression_breakeven_overhead or
      equivalent helper
    preserves_rendered_value: true
    narrative_check: The table and prose remain valid if the helper preserves the
      same bandwidth-dominated break-even approximation.
  - name: formatting_helpers
    value: fmt, fmt_percent, fmt_math, check
    kind: formatting_constant
    current_source: mlsysim
    should_change: false
    target_source: mlsysim.fmt
    preserves_rendered_value: true
    narrative_check: Formatting and invariant checks do not introduce independent
      system constants.
findings:
- id: appendix-communication-fp16-gradient-payload-local
  severity: low
  lines: 81-83,153,473
  issue: The AllReduce/compression worked examples encode the gradient payload as
    BILLION bytes even though the adjacent prose defines it as a 500M-parameter FP16
    gradient.
  recommended_change: Compute the payload from the scenario parameter count and MLSysIM
    precision width, for example 500 * MILLION * BYTES_FP16, or introduce a named
    reusable example payload helper if this 500M FP16 gradient appears elsewhere.
  mlsysim_target: mlsysim.core.units.BYTES_FP16 and MILLION; optional new example
    payload helper
  render_impact: No rendered change expected; the payload should still display as
    1 GB and the AllReduce timings should remain unchanged apart from insignificant
    floating-point formatting.
  prose_validation: The setup and compression prose remain narratively valid because
    they already describe the payload as a 500M-parameter FP16 gradient.
- id: appendix-communication-interleaved-bubble-local-formula
  severity: low
  lines: 85-89,108-109,155-157,447
  issue: The standard pipeline bubble values use calc_pipeline_bubble, but the interleaved
    value reimplements the same formula locally even though the MLSysIM helper already
    accepts v_stages.
  recommended_change: Replace the local interleaved expression with calc_pipeline_bubble(p_stages,
    m_microbatches, v_stages=v_interleave).
  mlsysim_target: mlsysim.physics.calc_pipeline_bubble
  render_impact: No rendered change expected; the helper implements (p-1)/(v*m+p-1),
    matching the local expression.
  prose_validation: The p=8, m=16, V=4 explanation and the reported bubble reduction
    remain valid.
- id: appendix-communication-compression-breakeven-helper
  severity: low
  lines: 90-93,120-128,176-187,490-499
  issue: The compression break-even equation is a reusable MLSysIM communication model,
    but the chapter defines it as a local static method and manually expands the table
    cells.
  recommended_change: Add or reuse an MLSysIM helper such as calc_compression_breakeven_overhead(comm_time,
    compression_ratio), then use it for the worked example and break-even table.
  mlsysim_target: new mlsysim.physics.calc_compression_breakeven_overhead
  render_impact: No rendered change expected if the helper preserves comm_ms * (1
    - 1/compression_ratio).
  prose_validation: The adjacent prose explicitly states the bandwidth-dominated approximation,
    so a helper with that documented assumption keeps the narrative accurate.
notes:
- No code or prose files were edited for this audit; only this YAML packet was added.
- The chapter has one Python cell, and it is included in python_cells.
- Hardware interconnect bandwidth, interconnect latency, GPUs-per-host, and ring/tree
  AllReduce formulas are already sourced from MLSysIM.
- GPU counts, microbatch counts, virtual-stage count, compression ratios, codec overhead,
  and break-even table axes are intentionally local worked-example assumptions.
