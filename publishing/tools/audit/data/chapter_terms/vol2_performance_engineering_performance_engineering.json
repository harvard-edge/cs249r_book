{
  "volume": "vol2",
  "section": "performance_engineering_performance_engineering",
  "path": "book/quarto/contents/vol2/performance_engineering/performance_engineering.qmd",
  "total_terms": 101,
  "terms": [
    {
      "abbrev": "GIL",
      "canonical_expansion": "traverse the Python Global Interpreter Lock",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 122,
      "context": "The inherited iron law decomposes execution time into three terms. The compute fraction represents the total floating-point operations divided by realized hardware throughput. The data fraction represents total bytes transferred divided by memory bandwidth. The roofline approximation then asks which exposed term dominates at a given operating point: increasing compute throughput for a memory-bound workload, for example, does not materially improve performance until the memory term is reduced. The overhead term captures everything else: kernel launch latency, synchronization, communication, and software stack inefficiency. PyTorch training loops expose a particularly large slice of this term: every kernel launch must traverse the Python Global Interpreter Lock (GIL) and the framework's CPU dispatcher before reaching the GPU command queue, spending tens of microseconds in Python dispatch per operation before any GPU work begins. This is precisely why ML framework developers prioritized `torch.compile(mode=\"reduce-overhead\")` and CUDA Graphs: both trace away the Python dispatch path entirely, converting repeated GPU submissions into a single native replay that bypasses the GIL and dispatcher on every step."
    },
    {
      "abbrev": "MoE",
      "canonical_expansion": "such as speculative decoding and mixture-of-experts",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 134,
      "context": "Other levers change the overhead or compute terms. Graph compilation, including `torch.compile`, XLA, and TensorRT, reduces overhead by eliminating kernel launch gaps, fusing operations, and optimizing memory allocation across the graph. Communication-computation overlap makes distributed communication concurrent with useful work, removing it from the critical path when $T_{\\text{comm}} \\leq T_{\\text{compute}}$ (that is, communication finishes before the concurrent compute phase completes, leaving zero exposed communication time: $T_{\\text{comm}} - T_{\\text{overlap}} \\leq 0$). This condition is the exposed-time test, not a new law: overlap helps only when useful computation is large enough to cover the communication. Algorithmic innovations such as speculative decoding and mixture-of-experts (MoE) change the compute term itself by making the model perform a different computation that preserves the output contract at lower exposed cost."
    },
    {
      "term": "Efficiency Frontier",
      "index_head": "Efficiency Frontier!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 162,
      "context": "That boundary is the **Efficiency Frontier**\\index{Efficiency Frontier!definition}, the Pareto-optimal curve of model quality vs. system throughput. A model on the frontier cannot improve throughput without sacrificing quality, or vice versa. @thompson2021 show why this frontier matters for deep learning: quality improvements have required rapidly increasing compute, making efficiency gains central to continued progress. Performance engineering pushes the frontier outward by making each quality level achievable at higher throughput, or equivalently, by making each throughput level achievable at higher quality. An organization's goal is not merely to reach the frontier but to find the point on it that best matches their latency, throughput, cost, and quality requirements."
    },
    {
      "term": "Throughput",
      "index_head": "Throughput",
      "type": "bold_indexed_concept",
      "first_lineno": 168,
      "context": "| **Throughput**\\index{Throughput} | Tokens/second or requests/second                    | How much work the system completes per unit time       | Larger batches improve throughput but often degrade latency         |"
    },
    {
      "term": "Latency",
      "index_head": "Latency",
      "type": "bold_indexed_concept",
      "first_lineno": 169,
      "context": "| **Latency**\\index{Latency}       | Time-to-first-token and inter-token latency         | How quickly the system responds to individual requests | Lower latency can require smaller batches and higher per-token cost |"
    },
    {
      "abbrev": "LLM",
      "canonical_expansion": "the same 70B large language model",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 178,
      "context": "A real-time chatbot prioritizes latency (time-to-first-token under 200 ms, inter-token latency under 50 ms) and may tolerate higher per-token cost. A batch processing pipeline for document summarization prioritizes throughput and cost, tolerating seconds of latency. A medical diagnostic system prioritizes quality above all else, accepting lower throughput and higher cost. Each application maps to a different optimal point on the efficiency frontier, and the performance-engineering toolbox provides the methods for reaching that point. To make this concrete, consider two deployment configurations for the same 70B large language model (LLM), using an illustrative price of \\$2.70 per H100-hour for both."
    },
    {
      "term": "HBM (High Bandwidth Memory)",
      "index_head": "HBM!memory hierarchy",
      "type": "bold_indexed_concept",
      "first_lineno": 190,
      "context": "[^fn-hbm-memory-performance]: **HBM (High Bandwidth Memory)**\\index{HBM!memory hierarchy}: Achieves its bandwidth by vertically stacking DRAM dies connected through thousands of through-silicon vias (TSVs), a 3D packaging technique first commercialized by SK Hynix in 2013. Despite the \"high bandwidth\" label, HBM's 3.35 TB/s on the H100 is still 200--600$\\times$ slower than on-chip SRAM access, making the memory hierarchy gap the central constraint of performance optimization."
    },
    {
      "abbrev": "TSVs",
      "canonical_expansion": "connected through thousands of through-silicon vias",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 190,
      "context": "[^fn-hbm-memory-performance]: **HBM (High Bandwidth Memory)**\\index{HBM!memory hierarchy}: Achieves its bandwidth by vertically stacking DRAM dies connected through thousands of through-silicon vias (TSVs), a 3D packaging technique first commercialized by SK Hynix in 2013. Despite the \"high bandwidth\" label, HBM's 3.35 TB/s on the H100 is still 200--600$\\times$ slower than on-chip SRAM access, making the memory hierarchy gap the central constraint of performance optimization."
    },
    {
      "term": "Registers",
      "index_head": "Register File",
      "type": "bold_indexed_concept",
      "first_lineno": 276,
      "context": "| **Registers**\\index{Register File}   | `{python} MemoryWallScenario.reg_per_sm_kib_str` per SM across `{python} MemoryWallScenario.sm_count_str` SMs, about `{python} MemoryWallScenario.register_total_mib_str` total | One clock cycle and ~0.01 pJ per access                                                                               | Private to each thread; FP32 accumulators can force register spilling\\index{Register File!register spilling} to L1/shared memory at 20--30 clock cycles per access |"
    },
    {
      "term": "Shared memory (SRAM)",
      "index_head": "SRAM",
      "type": "bold_indexed_concept",
      "first_lineno": 277,
      "context": "| **Shared memory (SRAM)**\\index{SRAM} | Up to `{python} MemoryWallScenario.shared_mem_per_sm_kib_str` configurable shared memory per SM                                                                                 | 20--30 clock cycles (~20 ns) and ~0.5 pJ per access                                                                   | Shared within a thread block; operator fusion is profitable when intermediates fit here instead of returning to HBM                                                |"
    },
    {
      "abbrev": "SRAM",
      "canonical_expansion": "Shared memory",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 277,
      "context": "| **Shared memory (SRAM)**\\index{SRAM} | Up to `{python} MemoryWallScenario.shared_mem_per_sm_kib_str` configurable shared memory per SM                                                                                 | 20--30 clock cycles (~20 ns) and ~0.5 pJ per access                                                                   | Shared within a thread block; operator fusion is profitable when intermediates fit here instead of returning to HBM                                                |"
    },
    {
      "abbrev": "TFLOP/s",
      "canonical_expansion": "Peak FP16",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 382,
      "context": "| **GPU**  | **Year** |             **Peak FP16 (TFLOP/s)**              |              **HBM BW (TB/s)**              |      **Ridge Point (FLOP/byte)**      |"
    },
    {
      "abbrev": "TB/s",
      "canonical_expansion": "HBM BW",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 382,
      "context": "| **GPU**  | **Year** |             **Peak FP16 (TFLOP/s)**              |              **HBM BW (TB/s)**              |      **Ridge Point (FLOP/byte)**      |"
    },
    {
      "abbrev": "FLOP/byte",
      "canonical_expansion": "Ridge Point",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 382,
      "context": "| **GPU**  | **Year** |             **Peak FP16 (TFLOP/s)**              |              **HBM BW (TB/s)**              |      **Ridge Point (FLOP/byte)**      |"
    },
    {
      "term": "Ridge Point",
      "index_head": "Roofline Model!ridge point",
      "type": "bold_indexed_concept",
      "first_lineno": 397,
      "context": "[^fn-roofline-ridge-point]: **Ridge Point**\\index{Roofline Model!ridge point}: The intersection of the memory-bound and compute-bound lines on a Roofline plot. It represents the minimum arithmetic intensity required to reach peak hardware performance $(R_{\\text{peak}})$. For an H100 GPU (FP16), the ridge point is roughly `{python} WideningGap.h100_ridge_str` FLOP/byte; if an operator's intensity is below this \"ridge,\" it will never saturate the Tensor Cores, regardless of how much compute is available."
    },
    {
      "abbrev": "FP16",
      "canonical_expansion": "For an H100 GPU",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 397,
      "context": "[^fn-roofline-ridge-point]: **Ridge Point**\\index{Roofline Model!ridge point}: The intersection of the memory-bound and compute-bound lines on a Roofline plot. It represents the minimum arithmetic intensity required to reach peak hardware performance $(R_{\\text{peak}})$. For an H100 GPU (FP16), the ridge point is roughly `{python} WideningGap.h100_ridge_str` FLOP/byte; if an operator's intensity is below this \"ridge,\" it will never saturate the Tensor Cores, regardless of how much compute is available."
    },
    {
      "term": "Roofline Model",
      "index_head": "Roofline Model!performance engineering",
      "type": "bold_indexed_concept",
      "first_lineno": 399,
      "context": "[^fn-roofline-williams-origin]: **Roofline Model**\\index{Roofline Model!performance engineering}: The original framing targets multicore CPUs, but the same ceiling diagram applies to accelerators: two numbers (peak FLOP/s and peak bandwidth) define the entire performance envelope. This same simplicity informs GPU purchasing decisions for ML inference, where the ridge point determines whether a workload benefits from faster compute or faster memory."
    },
    {
      "term": "Arithmetic intensity",
      "type": "callout_definition",
      "first_lineno": 401,
      "context": "::: {#dfn-performance-engineering-arithmetic-intensity .callout-definition title=\"Arithmetic intensity\"}"
    },
    {
      "abbrev": "GEMM",
      "canonical_expansion": "Large general matrix multiply",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 710,
      "context": "Large general matrix multiply (GEMM) operations are the most compute-intensive operations in ML. A square matrix multiplication of dimension $4096{\\times}4096$ in FP16 performs approximately `{python} WorkloadIntensityCalc.gemm_flops_billion_str` billion FLOPs while loading roughly `{python} WorkloadIntensityCalc.gemm_bytes_mb_str` of data, yielding an arithmetic intensity of approximately `{python} WorkloadIntensityCalc.gemm_intensity_str`. This sits well above the H100's ridge point, making large GEMMs firmly compute bound."
    },
    {
      "abbrev": "GELU",
      "canonical_expansion": "A Gaussian Error Linear Unit",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 712,
      "context": "Element-wise operations tell the opposite story. A Gaussian Error Linear Unit (GELU) activation applied to a $4096{\\times}4096$ tensor performs roughly 5 operations per element but must load and store each element, yielding an arithmetic intensity of approximately `{python} WorkloadIntensityCalc.elem_intensity_str`. The GPU spends almost all its time waiting for data transfers rather than computing, making these operations profoundly memory-bound."
    },
    {
      "term": "PagedAttention",
      "index_head": "PagedAttention",
      "type": "bold_indexed_concept",
      "first_lineno": 792,
      "context": "A critical enabler for large batch sizes is **PagedAttention**\\index{PagedAttention},[^fn-paged-attention-os] vLLM's paged KV cache management technique [@kwon2023]. Traditional KV cache implementations preallocate contiguous memory for each request's maximum possible sequence length."
    },
    {
      "term": "Prefill Phase",
      "index_head": "Prefill Phase",
      "type": "bold_indexed_concept",
      "first_lineno": 804,
      "context": "The **Prefill Phase**\\index{Prefill Phase} processes the entire input prompt in parallel. If the prompt contains $S$ tokens, the prefill phase executes a single forward pass over all $S$ tokens simultaneously. The GEMM operations have shape $[S,d_{\\text{model}}]{\\times}[d_{\\text{model}},d_{\\text{model}}]$, making the batch dimension equal to $S$. For a prompt of 1024 tokens, this is arithmetically intensive: the arithmetic intensity is approximately $2 \\times 1024/2 = 1024$ FLOP/byte for FP16 weights, well into the compute-bound regime. Prefill is therefore limited by Tensor Core throughput, not memory bandwidth."
    },
    {
      "term": "Decode Phase",
      "index_head": "Decode Phase",
      "type": "bold_indexed_concept",
      "first_lineno": 806,
      "context": "The **Decode Phase**\\index{Decode Phase} generates output tokens one at a time, autoregressively. Each step has a batch dimension of 1 (for a single request) or the number of concurrent requests (for batched serving). At batch size 1, decode is deeply memory-bound as analyzed in @sec-performance-engineering-workload-placement."
    },
    {
      "term": "Operator Fusion",
      "index_head": "Operator Fusion",
      "type": "bold_indexed_concept",
      "first_lineno": 915,
      "context": "Consider the simple sequence of operations $\\mathbf{Y} = \\text{LayerNorm}(\\text{GELU}(\\mathbf{X}\\mathbf{W} + \\mathbf{b}))$. In a naive implementation, the GPU writes the output of the matrix multiply back to main memory, reads it back for the GELU, writes it out again, and reads it one final time for the LayerNorm. This redundant data movement shatters performance. **Operator Fusion**\\index{Operator Fusion} eliminates these intermediate round-trips by keeping results in ultra-fast registers, executing the entire sequence in a single trip to memory."
    },
    {
      "abbrev": "HBM",
      "canonical_expansion": "puts them in the fridge",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 919,
      "context": "Imagine a kitchen where one chef chops vegetables, puts them in the fridge (HBM), then another chef takes them out to boil them, puts them back in the fridge, and a third chef takes them out to plate them. This is unfused execution\\index{Unfused Execution}: the bottleneck is not the cooking but the constant walking to the fridge."
    },
    {
      "abbrev": "SRAM/Registers",
      "canonical_expansion": "the ingredients on their cutting board",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 921,
      "context": "Operator fusion assigns the recipe to a single chef who keeps the ingredients on their cutting board (SRAM/Registers) and performs all three steps consecutively without ever returning to the fridge until the final dish is ready."
    },
    {
      "abbrev": "SMs",
      "canonical_expansion": "thread blocks across its streaming multiprocessors",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 927,
      "context": "Each GPU kernel launch involves overhead: the CPU must prepare launch parameters, dispatch to the GPU command queue, and the GPU must schedule thread blocks across its streaming multiprocessors (SMs). For a small element-wise operation on an accelerator such as an H100, this overhead can be 5--20 $\\mu$s, a time during which a memory-bound kernel might have already completed its useful work. When a transformer layer comprises dozens of small operations (add, multiply, normalize, activate), the cumulative launch overhead becomes significant."
    },
    {
      "term": "GEMM Kernel",
      "index_head": "GEMM!kernel",
      "type": "bold_indexed_concept",
      "first_lineno": 931,
      "context": "1. **GEMM Kernel**\\index{GEMM!kernel}: Read $X$ and $W$ from HBM, compute $XW + b$, write result $Z_1$ to HBM."
    },
    {
      "term": "GELU Kernel",
      "index_head": "GELU!kernel",
      "type": "bold_indexed_concept",
      "first_lineno": 932,
      "context": "2. **GELU Kernel**\\index{GELU!kernel}: Read $Z_1$ from HBM, compute $\\text{GELU}(Z_1)$, write $Z_2$ to HBM."
    },
    {
      "term": "LayerNorm Kernel",
      "index_head": "LayerNorm!kernel",
      "type": "bold_indexed_concept",
      "first_lineno": 933,
      "context": "3. **LayerNorm Kernel**\\index{LayerNorm!kernel}: Read $Z_2$ from HBM, compute $\\text{LayerNorm}(Z_2)$, write $Y$ to HBM."
    },
    {
      "abbrev": "FlashAttention",
      "canonical_expansion": "Operator-specific attention fusion",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1001,
      "context": "To appreciate the quantitative impact, consider each category applied to a single transformer layer with hidden dimension 4096 and batch size 2048 in FP16. Element-wise fusion of a bias-GELU-dropout chain in this shape eliminates two intermediate tensors of `{python} FusionTrafficCalc.tensor_mb_str` each, saving `{python} FusionTrafficCalc.traffic_mb_str` of HBM traffic per layer. Across `{python} FusionTrafficCalc.num_layers_str` layers, this reclaims about `{python} FusionTrafficCalc.traffic_gb_str` of HBM traffic per forward pass. Reduction fusion of LayerNorm avoids materializing large prenormalization tensors and intermediate statistics. Operator-specific attention fusion (FlashAttention) provides the largest single gain by removing the quadratic score and probability matrices that dominate long-context attention. The cumulative effect of all three fusion categories can remove a large fraction of HBM traffic for a memory-bound transformer forward pass, but the end-to-end speedup must still be verified with profiling because the bottleneck may shift."
    },
    {
      "term": "CUDA Graphs",
      "index_head": "CUDA!graphs, overhead reduction",
      "type": "bold_indexed_concept",
      "first_lineno": 1005,
      "context": "An orthogonal technique for reducing the overhead term in the iron law is **CUDA Graphs**\\index{CUDA!graphs, overhead reduction}.[^fn-cuda-graphs-constraint] While operator fusion combines multiple operations into fewer kernels, CUDA Graphs eliminate the CPU overhead of launching those kernels."
    },
    {
      "term": "Online Softmax",
      "index_head": "Online Softmax!FlashAttention",
      "type": "bold_indexed_concept",
      "first_lineno": 1096,
      "context": "The key insight behind FlashAttention is the **Online Softmax**\\index{Online Softmax!FlashAttention}[^fn-online-softmax-algo] trick, which makes tiling possible for an operation that appears to require global information. Standard softmax computes $\\text{softmax}(s_i) = e^{s_i} / \\sum_j e^{s_j}$, but for numerical stability it first subtracts the global maximum: $\\text{softmax}(s_i) = e^{s_i - m} / \\sum_j e^{s_j - m}$ where $m = \\max_j s_j$. Finding this global maximum seems to require seeing all scores first, which would force materializing the full $S{\\times}S$ matrix."
    },
    {
      "abbrev": "TMA",
      "canonical_expansion": "Cores and the Tensor Memory Accelerator",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1158,
      "context": "Newer FlashAttention-family kernels target Hopper-era hardware features such as FP8 Tensor Cores and the Tensor Memory Accelerator (TMA), a hardware path for asynchronous bulk tensor movement between HBM and shared memory. The systems principle is the same as the original algorithm: as the hardware exposes faster movement and lower-precision execution paths, the attention schedule must be rewritten to use those paths without materializing the quadratic workspace."
    },
    {
      "abbrev": "FLOPs",
      "canonical_expansion": "the total number of floating-point operations",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1180,
      "context": "- [ ] **FLOPs unchanged**: evaluate the claim that FlashAttention reduces the total number of floating-point operations (FLOPs) required for attention."
    },
    {
      "abbrev": "FP8",
      "canonical_expansion": "While   treats 8-bit floating point",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1288,
      "context": "Fusion reduces the number of trips through HBM; precision engineering reduces the payload of each trip. Moving FP16 weights through HBM consumes twice as many bytes as FP8. For bandwidth-bound kernels, shrinking weights from 2 bytes to 1 byte can roughly halve weight-read traffic and increase effective memory bandwidth. The engineering decision is where numerical noise can be tolerated to reduce bandwidth pressure and where quality demands higher precision. While @sec-distributed-training-fp8 treats 8-bit floating point (FP8) as a training-time primitive, the quantization techniques in this section enable efficient inference at scale."
    },
    {
      "term": "Outlier Features",
      "index_head": "Outlier Feature!quantization challenge",
      "type": "bold_indexed_concept",
      "first_lineno": 1292,
      "context": "The first inference-side precision decision is how to shrink weights while protecting the channels that carry rare but essential signal. Post-training quantization to INT8 or INT4 delivers even greater bandwidth savings for inference, but LLMs present a unique challenge: **Outlier Features**\\index{Outlier Feature!quantization challenge}.[^fn-outlier-feature-suppression] @dettmers2022llmint8 discovered that large language models develop a small number of hidden dimensions (about 0.1 percent of features) with activation magnitudes roughly 3--20$\\times$ larger than the rest. Applying uniform per-tensor INT8 quantization clips these outliers, destroying the information they carry, or expands the quantization range to accommodate them, wasting precision on the majority of near-zero values."
    },
    {
      "abbrev": "PTQ",
      "canonical_expansion": "models is the reason post-training quantization",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1294,
      "context": "[^fn-outlier-feature-suppression]: **Outlier Features**\\index{Outlier Feature!definition}: Large-scale transformers develop emergent \"outlier\" dimensions with activation magnitudes up to about 20$\\times$ larger than typical values [@dettmers2022llmint8]. While these outliers constitute about 0.1 percent of all features, clipping them during INT8 quantization destroys the model's reasoning capabilities. This physical property of large models is the reason post-training quantization (PTQ) requires \"outlier-aware\" strategies like LLM.int8() or Activation-Aware Weight Quantization (AWQ)."
    },
    {
      "abbrev": "AWQ",
      "canonical_expansion": "or Activation-Aware Weight Quantization",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1294,
      "context": "[^fn-outlier-feature-suppression]: **Outlier Features**\\index{Outlier Feature!definition}: Large-scale transformers develop emergent \"outlier\" dimensions with activation magnitudes up to about 20$\\times$ larger than typical values [@dettmers2022llmint8]. While these outliers constitute about 0.1 percent of all features, clipping them during INT8 quantization destroys the model's reasoning capabilities. This physical property of large models is the reason post-training quantization (PTQ) requires \"outlier-aware\" strategies like LLM.int8() or Activation-Aware Weight Quantization (AWQ)."
    },
    {
      "term": "Block-wise quantization",
      "type": "callout_definition",
      "first_lineno": 1296,
      "context": "::: {#dfn-performance-engineering-block-wise-quantization .callout-definition title=\"Block-wise quantization\"}"
    },
    {
      "abbrev": "GPTQ",
      "canonical_expansion": "Generative Pre-trained Transformer Quantization",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1310,
      "context": "Generative Pre-trained Transformer Quantization (GPTQ) [@frantar2023gptq]\\index{GPTQ} moves the cost into calibration through weight-only quantization using second-order information. Instead of quantizing each weight independently, GPTQ performs a layer-wise reconstruction pass that uses an approximate Hessian inverse from calibration activations to estimate which quantization errors matter most, then compensates for those errors in the remaining unquantized weights. This produces INT4 weight representations with low accuracy loss for many transformer models. The key insight is that quantization error in one weight can sometimes be offset through correlated weights in the same layer."
    },
    {
      "abbrev": "BNNs",
      "canonical_expansion": "Binary neural networks",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1324,
      "context": "For Archetype C (Federated MobileNet), quantization is a prerequisite for survival, not an optimization. On a microcontroller with only 512 KB of SRAM, an FP16 model is physically impossible to load. Binary neural networks (BNNs)\\index{Binary Neural Network!edge quantization} and 1-bit quantization\\index{1-bit Quantization!edge quantization} push this to the extreme, representing weights as single bits (+1/-1). While this trades significant accuracy, it reduces the weight memory footprint by 16$\\times$ relative to FP16 (32$\\times$ relative to FP32) and the energy per operation by up to 100$\\times$, enabling intelligence on devices that operate on harvested energy (microwatts)."
    },
    {
      "term": "Post-Training Quantization (PTQ)",
      "index_head": "Post-Training Quantization",
      "type": "bold_indexed_concept",
      "first_lineno": 1330,
      "context": "When a post-training recipe misses the quality budget, the precision decision shifts from calibration to training cost. The trade-off between **Post-Training Quantization (PTQ)**\\index{Post-Training Quantization} and **Quantization-Aware Training (QAT)**\\index{Quantization-Aware Training} centers on the balance between engineering agility and model fidelity. For a model like Llama-2-70B, PTQ is a common first choice for immediate deployment. Techniques like GPTQ or AWQ process the model layer-by-layer using a small calibration dataset (typically 128--1024 samples) to minimize reconstruction error. This process is computationally cheap, often requiring hours rather than a distributed training run for a 70B model. While PTQ is usually robust at INT8, aggressive quantization to INT4 or INT3 can incur a visible penalty: perplexity may degrade, and reasoning benchmarks such as Massive Multitask Language Understanding (MMLU) can drop several percentage points if the quantization recipe does not protect sensitive layers and outlier channels."
    },
    {
      "term": "Quantization-Aware Training (QAT)",
      "index_head": "Quantization-Aware Training",
      "type": "bold_indexed_concept",
      "first_lineno": 1330,
      "context": "When a post-training recipe misses the quality budget, the precision decision shifts from calibration to training cost. The trade-off between **Post-Training Quantization (PTQ)**\\index{Post-Training Quantization} and **Quantization-Aware Training (QAT)**\\index{Quantization-Aware Training} centers on the balance between engineering agility and model fidelity. For a model like Llama-2-70B, PTQ is a common first choice for immediate deployment. Techniques like GPTQ or AWQ process the model layer-by-layer using a small calibration dataset (typically 128--1024 samples) to minimize reconstruction error. This process is computationally cheap, often requiring hours rather than a distributed training run for a 70B model. While PTQ is usually robust at INT8, aggressive quantization to INT4 or INT3 can incur a visible penalty: perplexity may degrade, and reasoning benchmarks such as Massive Multitask Language Understanding (MMLU) can drop several percentage points if the quantization recipe does not protect sensitive layers and outlier channels."
    },
    {
      "abbrev": "QAT",
      "canonical_expansion": "Quantization-Aware Training",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1330,
      "context": "When a post-training recipe misses the quality budget, the precision decision shifts from calibration to training cost. The trade-off between **Post-Training Quantization (PTQ)**\\index{Post-Training Quantization} and **Quantization-Aware Training (QAT)**\\index{Quantization-Aware Training} centers on the balance between engineering agility and model fidelity. For a model like Llama-2-70B, PTQ is a common first choice for immediate deployment. Techniques like GPTQ or AWQ process the model layer-by-layer using a small calibration dataset (typically 128--1024 samples) to minimize reconstruction error. This process is computationally cheap, often requiring hours rather than a distributed training run for a 70B model. While PTQ is usually robust at INT8, aggressive quantization to INT4 or INT3 can incur a visible penalty: perplexity may degrade, and reasoning benchmarks such as Massive Multitask Language Understanding (MMLU) can drop several percentage points if the quantization recipe does not protect sensitive layers and outlier channels."
    },
    {
      "abbrev": "MMLU",
      "canonical_expansion": "such as Massive Multitask Language Understanding",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1330,
      "context": "When a post-training recipe misses the quality budget, the precision decision shifts from calibration to training cost. The trade-off between **Post-Training Quantization (PTQ)**\\index{Post-Training Quantization} and **Quantization-Aware Training (QAT)**\\index{Quantization-Aware Training} centers on the balance between engineering agility and model fidelity. For a model like Llama-2-70B, PTQ is a common first choice for immediate deployment. Techniques like GPTQ or AWQ process the model layer-by-layer using a small calibration dataset (typically 128--1024 samples) to minimize reconstruction error. This process is computationally cheap, often requiring hours rather than a distributed training run for a 70B model. While PTQ is usually robust at INT8, aggressive quantization to INT4 or INT3 can incur a visible penalty: perplexity may degrade, and reasoning benchmarks such as Massive Multitask Language Understanding (MMLU) can drop several percentage points if the quantization recipe does not protect sensitive layers and outlier channels."
    },
    {
      "abbrev": "STE",
      "canonical_expansion": "backward pass via the straight-through estimator",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1332,
      "context": "When PTQ fails to meet quality thresholds, QAT provides the remedy by integrating quantization noise directly into the training loop. Simulating low-precision rounding during the forward pass and approximating gradients during the backward pass via the straight-through estimator (STE)[^fn-ste-straight-through] allows the network to adjust its weights to be robust to quantization."
    },
    {
      "term": "Straight-Through Estimator (STE)",
      "index_head": "Straight-Through Estimator!quantization",
      "type": "bold_indexed_concept",
      "first_lineno": 1334,
      "context": "[^fn-ste-straight-through]: **Straight-Through Estimator (STE)**\\index{Straight-Through Estimator!quantization}: Discussed by @bengio2013estimating for hard stochastic neurons, the STE handles a fundamental calculus problem that also appears in quantization: the gradient of a rounding function is zero almost everywhere, making backpropagation through quantized layers impossible by standard rules. The STE passes the upstream gradient through the rounding step as a surrogate gradient, pretending the rounding did not happen. This approximation is useful in practice for training networks with discrete or quantized operations, but it is a heuristic rather than a general convergence guarantee for every QAT setup."
    },
    {
      "abbrev": "KV",
      "canonical_expansion": "precision problem extends to the key-value",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1348,
      "context": "The same precision problem extends to the key-value (KV) cache, which determines whether weight savings become larger batches. In decode, weights may be compressed aggressively while per-request cache state still grows with sequence length, so a precision change that leaves the cache untouched may fail to move the serving bottleneck. Numerical compression reduces the bytes stored per cached key or value, while grouped query attention (GQA) reduces how many key-value heads must be cached for each layer. Both mechanisms matter because the scheduler admits requests against the combined memory footprint of weights plus active cache state. The KV cache capacity calculation isolates the local capacity effect before @sec-inference-scale returns to full serving policy."
    },
    {
      "abbrev": "GQA",
      "canonical_expansion": "while grouped query attention",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1348,
      "context": "The same precision problem extends to the key-value (KV) cache, which determines whether weight savings become larger batches. In decode, weights may be compressed aggressively while per-request cache state still grows with sequence length, so a precision change that leaves the cache untouched may fail to move the serving bottleneck. Numerical compression reduces the bytes stored per cached key or value, while grouped query attention (GQA) reduces how many key-value heads must be cached for each layer. Both mechanisms matter because the scheduler admits requests against the combined memory footprint of weights plus active cache state. The KV cache capacity calculation isolates the local capacity effect before @sec-inference-scale returns to full serving policy."
    },
    {
      "abbrev": "INT4",
      "canonical_expansion": "GPU",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1471,
      "context": "- Weights: `{python} KVCacheAnalysis.weight_fp16_shard_gb_str` $\\times$ (`{python} KVCacheAnalysis.int4_scale_num_str`/`{python} KVCacheAnalysis.int4_scale_den_str`) = `{python} KVCacheAnalysis.weight_int4_gb_str`/GPU (INT4)"
    },
    {
      "abbrev": "INT8",
      "canonical_expansion": "KV cache per request",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1473,
      "context": "- KV cache per request (INT8): approximately `{python} KVCacheAnalysis.kv_int8_total_gb_str` total, or `{python} KVCacheAnalysis.kv_int8_per_gpu_gb_str`/GPU"
    },
    {
      "term": "Graph Capture",
      "index_head": "Graph Capture",
      "type": "bold_indexed_concept",
      "first_lineno": 1496,
      "context": "In the **Graph Capture**\\index{Graph Capture} stage, the compiler traces the model's execution to construct a computational graph, a directed acyclic graph where nodes represent operations and edges represent tensor dependencies. For the FFN block, this results in a graph with four primary nodes plus their associated parameter tensors. Dynamic Python control flow (loops, conditionals) must be handled by either tracing through a representative execution path or by using compiler-specific annotations to mark dynamic dimensions."
    },
    {
      "term": "Graph-Level Optimization",
      "index_head": "Graph Compilation!graph-level optimization",
      "type": "bold_indexed_concept",
      "first_lineno": 1498,
      "context": "During **Graph-Level Optimization**\\index{Graph Compilation!graph-level optimization}, the compiler applies algebraic simplifications and operation rewriting. It identifies that the bias addition in the first `Linear` layer can be folded into the matrix multiplication kernel. It also recognizes that the `GELU` activation is an element-wise operation that depends only on the output of the first `Linear`. These standard compiler optimizations can reduce graph size by 10--30 percent before any hardware-specific work begins."
    },
    {
      "term": "TorchDynamo",
      "index_head": "TorchDynamo!graph capture",
      "type": "bold_indexed_concept",
      "first_lineno": 1508,
      "context": "For workloads already written in PyTorch, `torch.compile` is the least disruptive compiler intervention: it tries to capture enough static graph to fuse memory-bound regions while preserving PyTorch's dynamic Python execution model. It operates through three components: **TorchDynamo**\\index{TorchDynamo!graph capture} for graph capture, **TorchInductor**\\index{TorchInductor} for code generation, and **AOTAutograd**\\index{AOTAutograd} for ahead-of-time backward graph construction when training workloads need compiled backward passes."
    },
    {
      "term": "TorchInductor",
      "index_head": "TorchInductor",
      "type": "bold_indexed_concept",
      "first_lineno": 1508,
      "context": "For workloads already written in PyTorch, `torch.compile` is the least disruptive compiler intervention: it tries to capture enough static graph to fuse memory-bound regions while preserving PyTorch's dynamic Python execution model. It operates through three components: **TorchDynamo**\\index{TorchDynamo!graph capture} for graph capture, **TorchInductor**\\index{TorchInductor} for code generation, and **AOTAutograd**\\index{AOTAutograd} for ahead-of-time backward graph construction when training workloads need compiled backward passes."
    },
    {
      "term": "AOTAutograd",
      "index_head": "AOTAutograd",
      "type": "bold_indexed_concept",
      "first_lineno": 1508,
      "context": "For workloads already written in PyTorch, `torch.compile` is the least disruptive compiler intervention: it tries to capture enough static graph to fuse memory-bound regions while preserving PyTorch's dynamic Python execution model. It operates through three components: **TorchDynamo**\\index{TorchDynamo!graph capture} for graph capture, **TorchInductor**\\index{TorchInductor} for code generation, and **AOTAutograd**\\index{AOTAutograd} for ahead-of-time backward graph construction when training workloads need compiled backward passes."
    },
    {
      "term": "Graph Break",
      "index_head": "Graph Break!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1510,
      "context": "TorchDynamo operates at the Python bytecode level,[^fn-torchdynamo-python-bytecode] a design choice that distinguishes it from earlier tracing approaches. Previous tracing methods (torch.jit.trace, torch.fx) operated at the Python source or abstract syntax tree (AST) level, requiring users to avoid unsupported Python constructs. TorchDynamo intercepts the bytecode interpreter itself, capturing a computational graph without requiring the user to modify their model code. When TorchDynamo encounters Python constructs it cannot trace (data-dependent control flow, unsupported operations), it inserts a **Graph Break**\\index{Graph Break!definition} that splits the trace into multiple subgraphs, each compiled independently. The goal is to capture as large a subgraph as possible while gracefully handling dynamic Python behavior."
    },
    {
      "abbrev": "AST",
      "canonical_expansion": "Python source or abstract syntax tree",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1510,
      "context": "TorchDynamo operates at the Python bytecode level,[^fn-torchdynamo-python-bytecode] a design choice that distinguishes it from earlier tracing approaches. Previous tracing methods (torch.jit.trace, torch.fx) operated at the Python source or abstract syntax tree (AST) level, requiring users to avoid unsupported Python constructs. TorchDynamo intercepts the bytecode interpreter itself, capturing a computational graph without requiring the user to modify their model code. When TorchDynamo encounters Python constructs it cannot trace (data-dependent control flow, unsupported operations), it inserts a **Graph Break**\\index{Graph Break!definition} that splits the trace into multiple subgraphs, each compiled independently. The goal is to capture as large a subgraph as possible while gracefully handling dynamic Python behavior."
    },
    {
      "term": "TorchDynamo Bytecode Interception",
      "index_head": "TorchDynamo!bytecode tracing",
      "type": "bold_indexed_concept",
      "first_lineno": 1512,
      "context": "[^fn-torchdynamo-python-bytecode]: **TorchDynamo Bytecode Interception**\\index{TorchDynamo!bytecode tracing}: By hooking CPython's frame evaluation function (`PEP 523`, added in Python 3.6), TorchDynamo captures the computation graph at the lowest level of the Python interpreter, below any source-level abstractions. This is why it can trace through decorators, closures, and third-party libraries that defeated earlier tracing approaches. The trade-off is tight coupling to CPython internals: TorchDynamo must be updated for each new Python version, and it cannot run on alternative interpreters like PyPy."
    },
    {
      "term": "Whole-Program Compilation",
      "index_head": "Whole-Program Compilation",
      "type": "bold_indexed_concept",
      "first_lineno": 1546,
      "context": "XLA\\index{XLA!Accelerated Linear Algebra} pays for performance with static structure. Used as the backend for JAX and TensorFlow, it generates the High Level Optimizer (HLO) intermediate representation that targets multiple backends, including Google Tensor Processing Units (TPUs), NVIDIA GPUs, and CPUs. Unlike TorchInductor, which generates Triton code targeting NVIDIA GPUs while preserving more Python flexibility, XLA enforces **Whole-Program Compilation**\\index{Whole-Program Compilation}, tracing the entire computation as a single static graph and enabling global optimizations that span across layers and even across the forward and backward passes."
    },
    {
      "abbrev": "HLO",
      "canonical_expansion": "it generates the High Level Optimizer",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1546,
      "context": "XLA\\index{XLA!Accelerated Linear Algebra} pays for performance with static structure. Used as the backend for JAX and TensorFlow, it generates the High Level Optimizer (HLO) intermediate representation that targets multiple backends, including Google Tensor Processing Units (TPUs), NVIDIA GPUs, and CPUs. Unlike TorchInductor, which generates Triton code targeting NVIDIA GPUs while preserving more Python flexibility, XLA enforces **Whole-Program Compilation**\\index{Whole-Program Compilation}, tracing the entire computation as a single static graph and enabling global optimizations that span across layers and even across the forward and backward passes."
    },
    {
      "abbrev": "TPUs",
      "canonical_expansion": "including Google Tensor Processing Units",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1546,
      "context": "XLA\\index{XLA!Accelerated Linear Algebra} pays for performance with static structure. Used as the backend for JAX and TensorFlow, it generates the High Level Optimizer (HLO) intermediate representation that targets multiple backends, including Google Tensor Processing Units (TPUs), NVIDIA GPUs, and CPUs. Unlike TorchInductor, which generates Triton code targeting NVIDIA GPUs while preserving more Python flexibility, XLA enforces **Whole-Program Compilation**\\index{Whole-Program Compilation}, tracing the entire computation as a single static graph and enabling global optimizations that span across layers and even across the forward and backward passes."
    },
    {
      "term": "General and Scalable Parallelization for ML Computation Graphs (GSPMD)",
      "index_head": "GSPMD",
      "type": "bold_indexed_concept",
      "first_lineno": 1548,
      "context": "The global view enables XLA's most distinctive capability: **General and Scalable Parallelization for ML Computation Graphs (GSPMD)**\\index{GSPMD}, where SPMD denotes the single program, multiple data execution model in which every device runs the same program over a different shard of the data. In distributed training, GSPMD automatically partitions the computation graph across thousands of TPU cores based on a few high-level user annotations. While a PyTorch user must manually wrap models with `DistributedDataParallel` or `FullyShardedDataParallel`, an XLA user defines the computation for a single device and allows the compiler to infer the necessary communication primitives (AllReduce, AllGather) and insert them into the graph. This allows for complex hybrid sharding strategies that are difficult to implement manually."
    },
    {
      "abbrev": "GSPMD",
      "canonical_expansion": "Scalable Parallelization for ML Computation Graphs",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1548,
      "context": "The global view enables XLA's most distinctive capability: **General and Scalable Parallelization for ML Computation Graphs (GSPMD)**\\index{GSPMD}, where SPMD denotes the single program, multiple data execution model in which every device runs the same program over a different shard of the data. In distributed training, GSPMD automatically partitions the computation graph across thousands of TPU cores based on a few high-level user annotations. While a PyTorch user must manually wrap models with `DistributedDataParallel` or `FullyShardedDataParallel`, an XLA user defines the computation for a single device and allows the compiler to infer the necessary communication primitives (AllReduce, AllGather) and insert them into the graph. This allows for complex hybrid sharding strategies that are difficult to implement manually."
    },
    {
      "term": "Mixed-Precision Quantization",
      "index_head": "Mixed-Precision Quantization",
      "type": "bold_indexed_concept",
      "first_lineno": 1558,
      "context": "This calibration enables **Mixed-Precision Quantization**\\index{Mixed-Precision Quantization} at a granular level. Blindly quantizing all layers of a 70B parameter LLM to INT8 often degrades perplexity. TensorRT can calibrate INT8 ranges from representative data and can build mixed-precision inference engines when precisions are enabled or constrained; for LLMs, layer-wise fallback policies usually require explicit quantization analysis or user-specified precision constraints. TensorRT also eliminates training-only operations (dropout, batch normalization running statistics updates), optimizes for static shapes by generating kernels tuned for exact dimensions, and plans memory precisely since no gradient tensors are needed."
    },
    {
      "term": "Kernel Autotuning",
      "index_head": "Kernel Autotuning",
      "type": "bold_indexed_concept",
      "first_lineno": 1560,
      "context": "TensorRT performs **Kernel Autotuning**\\index{Kernel Autotuning} far beyond simple heuristics. For every operation in the graph, it benchmarks dozens of candidate kernels, varying tile sizes, thread block configurations, and unrolling factors, on the actual target hardware. It selects the fastest implementation for that specific GPU and input shape. The performance gap between TensorRT and general-purpose compilers can be substantial, especially for stable inference workloads where the engine can specialize aggressively to a fixed shape range and GPU architecture."
    },
    {
      "abbrev": "ONNX",
      "canonical_expansion": "Open Neural Network Exchange",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1582,
      "context": "Backend choice follows the same constraint: the more stable and deployment-specific the workload, the more specialized the runtime can be. TorchInductor (the default) generates Triton kernels for GPU and C++ for CPU. For deployment-specific optimization, the model can be exported through `torch.export` to an intermediate representation that can be consumed by TensorRT, Open Neural Network Exchange (ONNX) Runtime, or other inference-specialized runtimes. Each backend applies its own optimization passes on top of the common graph-level transformations."
    },
    {
      "term": "Triton",
      "index_head": "Triton!kernel programming",
      "type": "bold_indexed_concept",
      "first_lineno": 1586,
      "context": "Between hand-written CUDA and fully automated graph compilers sits **Triton**\\index{Triton!kernel programming},[^fn-triton-tiled-compiler] a Python-based language for writing GPU kernels. Triton occupies a middle ground: the programmer specifies the algorithm (tiling strategy, fusion pattern) while Triton handles low-level concerns (thread block scheduling, memory coalescing, shared memory management)."
    },
    {
      "abbrev": "PTX",
      "canonical_expansion": "compiles this to Parallel Thread Execution",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 1623,
      "context": "The programmer thinks in terms of tiles (`BLOCK_SIZE` elements), not individual threads. Triton compiles this to Parallel Thread Execution (PTX) and streaming assembly (SASS) instructions, handling thread-to-data mapping, memory coalescing, and register allocation automatically. This makes it feasible for ML engineers (rather than GPU specialists) to write custom fused kernels when the automatic compiler misses a fusion opportunity."
    },
    {
      "abbrev": "SASS",
      "canonical_expansion": "and streaming assembly",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1623,
      "context": "The programmer thinks in terms of tiles (`BLOCK_SIZE` elements), not individual threads. Triton compiles this to Parallel Thread Execution (PTX) and streaming assembly (SASS) instructions, handling thread-to-data mapping, memory coalescing, and register allocation automatically. This makes it feasible for ML engineers (rather than GPU specialists) to write custom fused kernels when the automatic compiler misses a fusion opportunity."
    },
    {
      "term": "Speculative Decoding",
      "index_head": "Speculative Decoding!definition",
      "type": "bold_indexed_concept",
      "first_lineno": 1740,
      "context": "[^fn-speculative-decoding-pe]: **Speculative Decoding**\\index{Speculative Decoding!definition}: The draft model may produce tokens that the target model rejects, so the technique trades extra draft-model compute for reduced target-model memory bandwidth pressure. The target model's weights are loaded from HBM once to process $K$ candidate tokens, effectively increasing the arithmetic intensity of the decode phase by $K\\times$ when enough tokens are accepted."
    },
    {
      "term": "Router",
      "index_head": "Router!Mixture of Experts",
      "type": "bold_indexed_concept",
      "first_lineno": 1837,
      "context": "A standard MoE transformer layer replaces the feed-forward network (FFN) with multiple parallel \"expert\" FFNs and a lightweight **Router**\\index{Router!Mixture of Experts} (also called a gating network) that selects which experts process each token. That architecture shifts the performance problem from dense matrix throughput to routing, memory residency, and load balance. When experts are distributed across GPUs, expert parallelism places different experts on different devices, and an AllToAll exchange moves each token's hidden state to its assigned expert before returning the result. The active-parameter savings are valuable only if that routing traffic and any expert imbalance remain smaller than the dense compute they replace."
    },
    {
      "abbrev": "FFN",
      "canonical_expansion": "transformer layer replaces the feed-forward network",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 1837,
      "context": "A standard MoE transformer layer replaces the feed-forward network (FFN) with multiple parallel \"expert\" FFNs and a lightweight **Router**\\index{Router!Mixture of Experts} (also called a gating network) that selects which experts process each token. That architecture shifts the performance problem from dense matrix throughput to routing, memory residency, and load balance. When experts are distributed across GPUs, expert parallelism places different experts on different devices, and an AllToAll exchange moves each token's hidden state to its assigned expert before returning the result. The active-parameter savings are valuable only if that routing traffic and any expert imbalance remain smaller than the dense compute they replace."
    },
    {
      "term": "CUDA Streams",
      "index_head": "CUDA!streams",
      "type": "bold_indexed_concept",
      "first_lineno": 2110,
      "context": "The mechanism enabling overlap on NVIDIA GPUs is **CUDA Streams**\\index{CUDA!streams}. A CUDA stream is an ordered sequence of GPU operations (kernel launches, memory copies, NCCL collectives) that execute sequentially within the stream but can execute concurrently with operations in other streams. The application maintains two distinct streams: a compute stream for matrix multiplications and element-wise kernels, and a communication stream for NCCL operations. The workflow proceeds by launching a compute kernel on the first stream and immediately triggering an asynchronous communication call on the second:"
    },
    {
      "term": "SM Partitioning",
      "index_head": "SM Partitioning",
      "type": "bold_indexed_concept",
      "first_lineno": 2125,
      "context": "The GPU hardware scheduler interleaves execution units from both streams, running the GEMM on the SMs while the NVLink engine handles the AllReduce data transfer. However, while streams provide logical concurrency, they contend for physical resources. The SMs must manage the data movement instructions for the communication kernel. On an H100 with 132 SMs, a heavy NCCL operation might occupy 4--8 SMs solely for protocol processing and memory copying, leading to **SM Partitioning**\\index{SM Partitioning}: the available compute throughput is reduced by 3--6 percent during communication. If the compute kernel is dense enough to saturate 100 percent of the SMs, enabling overlap can paradoxically slow down execution due to this resource contention, a phenomenon known as interference. In practice, the 3--6 percent compute throughput reduction is far smaller than the communication time that would otherwise be exposed, making the trade-off overwhelmingly favorable."
    },
    {
      "abbrev": "MFU",
      "canonical_expansion": "Model FLOPs Utilization",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2161,
      "context": "Metrics are not interchangeable; each answers a different bottleneck question. Model FLOPs Utilization (MFU) and Model Bandwidth Utilization (MBU) diagnose useful hardware work, Time-to-First-Token (TTFT) and Inter-Token Latency (ITL) diagnose serving latency, and throughput exposes capacity under batching. Understanding their relationships and trade-offs is essential for performance engineering."
    },
    {
      "abbrev": "MBU",
      "canonical_expansion": "and Model Bandwidth Utilization",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2161,
      "context": "Metrics are not interchangeable; each answers a different bottleneck question. Model FLOPs Utilization (MFU) and Model Bandwidth Utilization (MBU) diagnose useful hardware work, Time-to-First-Token (TTFT) and Inter-Token Latency (ITL) diagnose serving latency, and throughput exposes capacity under batching. Understanding their relationships and trade-offs is essential for performance engineering."
    },
    {
      "abbrev": "TTFT",
      "canonical_expansion": "Time-to-First-Token",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2161,
      "context": "Metrics are not interchangeable; each answers a different bottleneck question. Model FLOPs Utilization (MFU) and Model Bandwidth Utilization (MBU) diagnose useful hardware work, Time-to-First-Token (TTFT) and Inter-Token Latency (ITL) diagnose serving latency, and throughput exposes capacity under batching. Understanding their relationships and trade-offs is essential for performance engineering."
    },
    {
      "abbrev": "ITL",
      "canonical_expansion": "and Inter-Token Latency",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2161,
      "context": "Metrics are not interchangeable; each answers a different bottleneck question. Model FLOPs Utilization (MFU) and Model Bandwidth Utilization (MBU) diagnose useful hardware work, Time-to-First-Token (TTFT) and Inter-Token Latency (ITL) diagnose serving latency, and throughput exposes capacity under batching. Understanding their relationships and trade-offs is essential for performance engineering."
    },
    {
      "abbrev": "HFU",
      "canonical_expansion": "Hardware FLOPs Utilization",
      "category": "proper_noun",
      "type": "acronym_expansion",
      "first_lineno": 2168,
      "context": "| **Hardware FLOPs Utilization (HFU)**                                     | Total arithmetic operations executed, including recomputation, divided by peak FLOP/s | Separating useful model work from overhead such as activation recomputation                          | Usually exceeds MFU, and the HFU\u2013MFU gap estimates wasted compute                                                                                                 |"
    },
    {
      "term": "Time-to-first-token (TTFT)",
      "index_head": "Time to First Token",
      "type": "bold_indexed_concept",
      "first_lineno": 2169,
      "context": "| **Time-to-first-token (TTFT)**\\index{Time to First Token}                | Latency from request arrival to the first generated token                             | Interactive serving responsiveness, including queueing, prefill, and initialization                  | Below 500 ms is generally acceptable; below 200 ms feels responsive                                                                                               |"
    },
    {
      "term": "Inter-token latency (ITL)",
      "index_head": "Inter-Token Latency",
      "type": "bold_indexed_concept",
      "first_lineno": 2170,
      "context": "| **Inter-token latency (ITL)**\\index{Inter-Token Latency}                 | Time between consecutive generated tokens during decode                               | Perceived generation speed after the first token                                                     | Below 50 ms supports comfortable reading; real-time speech may require below 25 ms                                                                                |"
    },
    {
      "term": "Model Bandwidth Utilization (MBU)",
      "index_head": "Model Bandwidth Utilization",
      "type": "bold_indexed_concept",
      "first_lineno": 2172,
      "context": "| **Model Bandwidth Utilization (MBU)**\\index{Model Bandwidth Utilization} | Achieved memory bandwidth divided by hardware peak bandwidth                          | Memory-bound inference, especially decode workloads                                                  | An H100 decode step can show 2% MFU but 85% MBU, which indicates a memory-saturated and well-optimized path                                                       |"
    },
    {
      "abbrev": "JIT",
      "canonical_expansion": "allowing CUDA caches and just-in-time",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2264,
      "context": "The `schedule` parameter defines a warmup period where the profiler runs but does not record, allowing CUDA caches and just-in-time (JIT) compilation to stabilize before measurement begins. Without warmup, the first few iterations include one-time costs (kernel compilation, memory allocation, CUDA context initialization) that inflate the measured times and misrepresent steady-state performance."
    },
    {
      "term": "Kernel timeline",
      "index_head": "Kernel!timeline",
      "type": "bold_indexed_concept",
      "first_lineno": 2270,
      "context": "| **Kernel timeline**\\index{Kernel!timeline}                   | CUDA kernels, their duration, GPU idle gaps, and unexpectedly long kernels   | Fuse operators, use CUDA Graphs, or investigate long kernels with Nsight Compute |"
    },
    {
      "term": "Memory timeline",
      "index_head": "Memory!timeline",
      "type": "bold_indexed_concept",
      "first_lineno": 2271,
      "context": "| **Memory timeline**\\index{Memory!timeline}                   | Allocation and deallocation spikes or gradual memory growth                  | Reuse buffers, plan allocations, or investigate leaks                            |"
    },
    {
      "term": "CPU-GPU synchronization",
      "index_head": "CPU-GPU Synchronization",
      "type": "bold_indexed_concept",
      "first_lineno": 2272,
      "context": "| **CPU-GPU synchronization**\\index{CPU-GPU Synchronization}   | Points where the CPU waits for the GPU, or the GPU waits for CPU launch work | Remove blocking synchronization and reduce launch overhead                       |"
    },
    {
      "term": "Communication events",
      "index_head": "Communication!event timeline",
      "type": "bold_indexed_concept",
      "first_lineno": 2273,
      "context": "| **Communication events**\\index{Communication!event timeline} | NCCL collective duration relative to compute kernels                         | Tune overlap, bucket sizing, or parallelization strategy                         |"
    },
    {
      "abbrev": "GUI",
      "canonical_expansion": "the Nsight Systems graphical user interface",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2292,
      "context": "The resulting `.nsys-rep` file is opened in the Nsight Systems graphical user interface (GUI), which presents a multi-row timeline. Four rows carry the most diagnostic weight."
    },
    {
      "abbrev": "WAF",
      "canonical_expansion": "s Web Application Firewall",
      "category": "generic_concept",
      "type": "acronym_expansion",
      "first_lineno": 2383,
      "context": "**Context**: Cloudflare's Web Application Firewall (WAF) inspects incoming HTTP/HTTPS traffic at the global edge under strict latency constraints ($L_{\\text{lat}} \\le 10\\text{ ms}$) [@cloudflare2019outage]."
    },
    {
      "term": "Hierarchical Profiling",
      "index_head": "Hierarchical Profiling",
      "type": "bold_indexed_concept",
      "first_lineno": 2401,
      "context": "Production systems address this through **Hierarchical Profiling**\\index{Hierarchical Profiling}. At the top level, application-level metrics (MFU, throughput, step time) are collected continuously from every GPU with negligible overhead. These aggregate metrics detect when performance degrades. When a degradation is detected, **Targeted Profiling**\\index{Targeted Profiling} is triggered on a representative subset of GPUs (typically one GPU per pipeline stage, per data-parallel group) for a short window (a few training steps). The resulting traces are analyzed to identify the specific bottleneck."
    },
    {
      "term": "Targeted Profiling",
      "index_head": "Targeted Profiling",
      "type": "bold_indexed_concept",
      "first_lineno": 2401,
      "context": "Production systems address this through **Hierarchical Profiling**\\index{Hierarchical Profiling}. At the top level, application-level metrics (MFU, throughput, step time) are collected continuously from every GPU with negligible overhead. These aggregate metrics detect when performance degrades. When a degradation is detected, **Targeted Profiling**\\index{Targeted Profiling} is triggered on a representative subset of GPUs (typically one GPU per pipeline stage, per data-parallel group) for a short window (a few training steps). The resulting traces are analyzed to identify the specific bottleneck."
    },
    {
      "term": "Statistical Profiling",
      "index_head": "Statistical Profiling",
      "type": "bold_indexed_concept",
      "first_lineno": 2403,
      "context": "Another approach is **Statistical Profiling**\\index{Statistical Profiling}, where each GPU randomly samples a small fraction of its kernels for detailed timing. Over many training steps, the aggregated samples provide a statistically accurate picture of the kernel time distribution without the overhead of full tracing. The approach is analogous to the sampling profilers (like Linux `perf`) used in traditional systems engineering, adapted for the GPU context."
    },
    {
      "term": "MFU",
      "type": "callout_definition",
      "first_lineno": 2417,
      "context": "::: {#dfn-performance-engineering-model-flops-utilization-mfu .callout-definition title=\"MFU\"}"
    },
    {
      "term": "Local node baseline",
      "index_head": "Model FLOPs Utilization!local node baseline",
      "type": "bold_indexed_concept",
      "first_lineno": 2498,
      "context": "*   **Local node baseline**\\index{Model FLOPs Utilization!local node baseline}: A single 8-GPU node achieves `{python} FleetEfficiencyCalc.fleet_local_mfu_str` MFU."
    },
    {
      "term": "Fleet performance",
      "index_head": "Model FLOPs Utilization!fleet performance",
      "type": "bold_indexed_concept",
      "first_lineno": 2499,
      "context": "*   **Fleet performance**\\index{Model FLOPs Utilization!fleet performance}: At `{python} FleetEfficiencyCalc.fleet_nodes_str`, the step time increases to `{python} FleetEfficiencyCalc.fleet_t_step_ms_str`, dropping MFU to `{python} FleetEfficiencyCalc.fleet_global_mfu_str`."
    },
    {
      "term": "Small-scale canaries",
      "index_head": "Small-Scale Canaries",
      "type": "bold_indexed_concept",
      "first_lineno": 2517,
      "context": "| **Small-scale canaries**\\index{Small-Scale Canaries}           | Model behavior on 8 and 64 GPUs to establish a scaling efficiency curve               | Code changes that look harmless on one GPU but bend the curve before full-fleet launch            |"
    },
    {
      "term": "Fleet baseline comparison",
      "index_head": "Fleet Baseline Comparison",
      "type": "bold_indexed_concept",
      "first_lineno": 2518,
      "context": "| **Fleet baseline comparison**\\index{Fleet Baseline Comparison} | Every production run's MFU against the reference baseline for that model architecture | Architecture, compiler, or configuration changes that reduce useful fleet work                    |"
    },
    {
      "term": "Gray failure detection",
      "index_head": "Gray Failure Detection",
      "type": "bold_indexed_concept",
      "first_lineno": 2519,
      "context": "| **Gray failure detection**\\index{Gray Failure Detection}       | Distribution of step times across the fleet                                           | Straggler nodes, such as one 10% slower node that can reduce synchronous data-parallel MFU by 10% |"
    },
    {
      "term": "Maximum Batch Size",
      "index_head": "Batch Size!maximum",
      "type": "bold_indexed_concept",
      "first_lineno": 2601,
      "context": "- **Maximum Batch Size**\\index{Batch Size!maximum}: `{python} KVCacheAnalysis.baseline_policy_batch_size_str` (limited by KV cache memory)"
    }
  ]
}