---
title: "TinyTorch"
subtitle: "The xv6 of Machine Learning Systems"
author: "Prof. Vijay Janapa Reddi"
date: today
---

# Overview {.unnumbered}

**Harvard's TinyTorch** is to Machine Learning Systems what MIT's xv6 was to Operating Systems: the first transparent, builder-first reference implementation where every layer of the modern ML stack is built from scratch and demystified.

Building systems creates irreversible understanding. In this volume, we move beyond calling high-level library functions (`import torch`) to construct the entire ML systems runtime from first principles—from linear memory buffers, dynamic automatic differentiation, and multi-head attention to kernel vectorization, INT8 quantization, and KV-cache inference memoization.

```
┌────────────────────────────────────────────────────────────────────────┐
│  TinyTorch: Core Architecture                                          │
│                                                                        │
│  Part I   • The Core Engine (Tensors, Autograd, Optimizers)            │
│  Part II  • Deep Architectures (Convolutions, Transformers, GPT)       │
│  Part III • Systems & Optimization (Profiling, Quantization, KV Cache) │
│  Part IV  • Verification & Capstone (Benchmarking, Torch Olympics)     │
└────────────────────────────────────────────────────────────────────────┘
```

Everything in this book is built in readable, dependency-free Python using only NumPy for raw buffer storage. Every module is accompanied by formal unit tests, benchmarks, and production sidebars bridging our minimal implementation to production C++/CUDA runtimes.
