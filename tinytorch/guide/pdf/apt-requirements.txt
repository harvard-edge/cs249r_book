# System packages (Ubuntu) required to build the TinyTorch Lab Guide PDF.
# One package per line. Read by `make install-deps` and CI workflows.
# librsvg2-bin provides rsvg-convert, used by the Makefile's `figures`
# target to pre-convert every SVG diagram to a PDF that XeLaTeX can
# embed directly.
librsvg2-bin
