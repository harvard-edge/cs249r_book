/.quarto/
**/*.quarto_ipynb
# Quarto book output and XeLaTeX intermediates — all generated by
# `quarto render pdf/`. Quarto's post-render cleanup refuses to remove
# these because chapters live at ../ (outside this book's project
# root), so they persist between renders. Not sources.
_build/
index.tex
index.log
index.aux
index.toc
index.out
index.synctex.gz
*.pdf
