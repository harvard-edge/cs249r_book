---
title: "reinforcement-learning"
subtitle: "reinforcement suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + selection ledger + datasets.yaml + asset dossiers + draft reference results + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-experimental">experimental</span>

> **Readiness status.** The max CLI preserves the full historical MiniGo quality loop and professional-game evaluator, and the PyTorch adapter executes it locally against the pinned upstream reference. Quality conformance remains open because the laptop budget runs far fewer self-play games per generation than the reference contract specifies, not because the runtime is unavailable. No draft reference result or public baseline is claimed for this workload.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | reinforcement |
| **Model** | MiniGo joint policy and value network |
| **Parameters** | configuration-dependent |
| **Dataset** | minigo-self-play |
| **Dataset source** | Self-play games and fixed playoff evaluation from MLPerf Training v0.5 MiniGo |
| **Scenario** | training |
| **Maturity** | research |
| **Candidate result status** | experimental |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

## Readiness Spiral Status

| **Field** | **Value** |
|:---|:---|
| **Stage** | quality-conformance |
| **Functional ready** | True |
| **Quality conformant** | False |
| **Repeatability verified** | False |
| **Promotion ready** | False |
| **Next gate** | Execute the pinned MiniGo quality loop at or near the reference self-play budget. The adapter runs locally under a reduced budget, so what remains is compute, not runtime availability. |

::: {.callout-caution}
**Quality-conformance boundary.** The `max` runner implements the authoritative quality contract. A result remains experimental until a complete run meets the unchanged target and its provenance verifies. The `min` path remains a functional probe.
:::

## Why This Benchmark Is Included

Canonical historical MLPerf reinforcement-learning workload.

| **Field** | **Value** |
|:---|:---|
| **Classroom value** | Demonstrates self-play, search, dynamic data generation, and evaluation. |
| **Systems behavior** | Repeated inference in tree search coupled to iterative training. |
| **Benchmark lineage** | Direct MLPerf Training lineage. |
| **Model choice** | MiniGo is the MLPerf-selected RL reference. |
| **Dataset choice** | Self-play is intrinsic to the canonical task. |
| **Quality metric** | The original workload fixes professional-move prediction and a 100-game promotion playoff. |
| **Published baseline** | Original MLPerf Training v0.5 MiniGo reference workload |
| **Alternative rejected** | CartPole and Pendulum are substitutions without MLPerf lineage. |

## Authoritative Sources and Adaptation

| **Field** | **Value** |
|:---|:---|
| **Authority** | Historical MLPerf Training MiniGo |
| **Repository** | [open source](https://github.com/mlcommons/training) |
| **Commit** | `0badcd1786fcb007725ed05f1c44e9d80bbeac52` |
| **Path** | `reinforcement/minigo` |

**Adaptation boundary:** The min profile retains a bounded policy-value probe. The max profile invokes the pinned MLPerf Training v0.5 phase entrypoints with resumable orchestration and fails closed until an immutable compatible GPU runtime is supplied.

## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload reinforcement-learning --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd.json provenance)
uv run mlperf run --workload reinforcement-learning --profile max

# quick smoke pass
uv run mlperf run --workload reinforcement-learning --profile min

# research envelope
uv run mlperf run --workload reinforcement-learning --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | professional_move_prediction |
| **Target** | 0.4 |
| **Direction** | higher |
| **Target kind** | inherited acceptance gate |
| **Target basis** | literature |
| **Tolerance** | 0.0 |
| **Reference runs** | 0 |

**Reviewer notes:**

- The target is the original 9-by-9 MLPerf Training MiniGo professional-move prediction gate.
- The functional spiral performs a bounded policy-value self-play and training probe without making a MiniGo quality claim.
- The recorded result is a reduced-configuration probe, not an attempt at the reference contract. It runs 16 self-play games per generation for 4 generations against a reference of 2000 games per generation and up to 10000 generations, so the shortfall is a property of the laptop budget rather than of the adapter. This is a different kind of miss from a workload that executes its full reference procedure and falls short, and it should not be read as evidence that the inherited gate is unreachable.

## Systems Experiment Execution Contract

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | self_play_and_training_seconds |
| **Scenario** | training |
| **Timing scope** | Canonical MiniGo self-play generation, policy-value training, and playoff evaluation |
| **Outer reference runs** | 1 |
| **Outer preconditioning runs** | 0 |
| **Outer inter execution cooldown seconds** | 300 |
| **Repeatability metric** | self_play_and_training_seconds |
| **Repeatability limit** | 0.05 |

## Results

Run the benchmark and read your own report:

```bash
uv run mlperf run --workload reinforcement-learning --profile max
```

Every run writes an HTML dashboard for reading, JSON for machine use, CSV for spreadsheets, and a `.provd.json` provenance manifest. Those artifacts carry the quality decision, timing distribution, hardware and software fingerprint, asset digests, and model lineage for the run you performed.

See [Reading Results](../../guide/results.qmd) for how to interpret them.

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.functional_setup:run_reinforcement_learning_min` |
| max | `mlperf.runners.minigo:run_reinforcement_learning_max` |

Registry source: [`registry/suites/reinforcement/reinforcement-learning.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/reinforcement/reinforcement-learning.yaml)
