---
title: "micro-dlrm-dram-train"
subtitle: "recommender suite"
---

<!-- GENERATED FILE — do not edit by hand.
     Source of truth: registry/ + datasets.yaml + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

<span class="badge status-systems-only">systems-only</span>

> Useful for DRAM-bound recommender architecture studies, but the accuracy baseline is not yet verified enough for public score-bearing status.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | recommender |
| **Model** | micro-dlrm-dram-1m |
| **Parameters** | 512M |
| **Dataset** | movielens-100k |
| **Dataset source** | Harper & Konstan (2015) — same dataset as micro-dlrm-train |
| **Scenario** | server |
| **Maturity** | base |
| **Public status** | systems-only |
| **Provenance** | Naumov et al. 2019 (DLRM) + Weinberger et al. 2009 (hashing trick); production-realistic memory access pattern at laptop scale |

## How to Run

```bash
# one-time asset preparation
mlperf fetch --workload micro-dlrm-dram-train --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
mlperf run --workload micro-dlrm-dram-train --profile max --open-report

# quick smoke pass
mlperf run --workload micro-dlrm-dram-train --profile min

# research envelope
mlperf run --workload micro-dlrm-dram-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | accuracy |
| **Target** | 0.65 |

## Verified Baseline


> Awaiting first verification run on iter-2 harness.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dram_bound |
| **Peak bytes per step** | 67108864 |
| **Note** | 2M-row x 256-dim virtual table = 2 GB total; per-step working set ~64 MB at B=8192 with random hashing |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Flops per byte** | 14.919 |
| **Classification rule** | intensity 14.92 vs [low 13.71, high 54.84]; util 0.019 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/micro-dlrm-dram-train_2026-04-16T21-04-03Z_a229b977a809.json |
| **Evidence sha256 short** | 0f986b0977e6 |
| **Measured at** | 2026-04-16T21-04-03Z |
| **Platform machine class** | apple-silicon |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dispatch_bound |
| **Utilization** | 0.0188 |
| **Achieved bw gbps** | 9.933 |
| **Observation source** | iter-2 smoke_dlrm_dram.py: m_spa=256 explicitly chosen to clear PyTorch's ~50us dispatch floor |
| **Classification rule** | intensity 14.92 vs [low 13.71, high 54.84]; util 0.019 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/micro-dlrm-dram-train_2026-04-16T21-04-03Z_a229b977a809.json |
| **Evidence sha256 short** | 0f986b0977e6 |
| **Measured at** | 2026-04-16T21-04-03Z |
| **Platform machine class** | apple-silicon |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.dlrm:run_dram_min` |
| max | `mlperf.runners.dlrm:run_dram_max` |

Registry source: [`registry/suites/recommender/micro-dlrm-dram-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/recommender/micro-dlrm-dram-train.yaml)
