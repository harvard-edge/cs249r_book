---
title: "micro-dlrm-dram-train"
subtitle: "recommender suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-systems-only">systems-only</span>

> Exercises a 64 MiB hashed embedding table with a broad deterministic user-item cross for recommender architecture studies. The exact allocation is reported, but a DRAM-bound regime is not claimed without hardware-local profiling.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | recommender |
| **Model** | micro-dlrm-dram-17m |
| **Parameters** | 16802833 |
| **Dataset** | movielens-100k |
| **Dataset source** | Harper & Konstan (2015) — same dataset as micro-dlrm-train |
| **Scenario** | training |
| **Maturity** | base |
| **Candidate result status** | systems-only |
| **Provenance** | Naumov et al. 2019 (DLRM) and Weinberger et al. 2009 (hashing trick); uses hashed embedding lookups at laptop scale |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

::: {.callout-caution}
**Current `max` execution boundary.** The max runner uses MovieLens-100K, a deterministic user-item cross, and a 65,536 by 256 fp32 embedding table while enforcing its local accuracy target. It remains systems-only because allocation size alone does not prove a DRAM-bound regime.

- Reported data mode: `real`
- Candidate quality target enforced: **yes**
- Fetched assets used by this runner: **yes**
- Declared dataset used by this runner: **yes**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload micro-dlrm-dram-train --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload micro-dlrm-dram-train --profile max --open-report

# quick smoke pass
uv run mlperf run --workload micro-dlrm-dram-train --profile min

# research envelope
uv run mlperf run --workload micro-dlrm-dram-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | accuracy |
| **Target** | 0.65 |

## Local Calibration Observation

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | bounded-local-methodology-check |
| **Seed** | 42 |
| **Final accuracy** | 0.694609375 |
| **Final roc auc** | 0.7537700041541383 |
| **N params** | 16802833 |
| **Working set bytes** | 67108864 |
| **Baseline note** | A bounded local default-path check passed the 0.65 accuracy gate and confirmed exact model/table accounting. It is not promoted evidence and makes no DRAM-bandwidth or cache-regime claim. |

> This observation is local development context. It is not a review-eligible reference result.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.dlrm:run_dram_min` |
| max | `mlperf.runners.dlrm:run_dram_max` |

Registry source: [`registry/suites/recommender/micro-dlrm-dram-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/recommender/micro-dlrm-dram-train.yaml)
