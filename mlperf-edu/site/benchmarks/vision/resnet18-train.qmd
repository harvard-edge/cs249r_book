---
title: "resnet18-train"
subtitle: "vision suite"
---

<!-- GENERATED FILE — do not edit by hand.
     Source of truth: registry/ + datasets.yaml + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

<span class="badge status-score-bearing">score-bearing</span>

> Uses Fashion-MNIST with a local ResNet-18 implementation and explicit top-1 target, suitable for public edge-training performance reports.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | vision |
| **Model** | resnet18 |
| **Parameters** | 11.2M |
| **Dataset** | fashion-mnist |
| **Dataset source** | Xiao, Rasul & Vollgraf (2017), Fashion-MNIST via torchvision; MIT license; cached locally after first download |
| **Scenario** | single_stream |
| **Maturity** | base |
| **Public status** | score-bearing |
| **Provenance** | He et al. 2016 (ResNet); FULLY LOCAL implementation — no torchvision.models dependency |

## How to Run

```bash
# one-time asset preparation
mlperf fetch --workload resnet18-train --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
mlperf run --workload resnet18-train --profile max --open-report

# quick smoke pass
mlperf run --workload resnet18-train --profile min

# research envelope
mlperf run --workload resnet18-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | top1_accuracy |
| **Target** | 0.75 |
| **Direction** | higher |
| **Target basis** | reference_runs |
| **Tolerance** | 0.0 |
| **Reference runs** | 5 |

**Variance summary:**

| **Field** | **Value** |
|:---|:---|
| **Runs** | 5 |
| **Statistic** | median |
| **Acceptance rule** | median top-1 accuracy must be >= target |
| **Spread note** | Fashion-MNIST split and seed are fixed; tuned runs can exceed this out-of-box target. |

**Reference protocol:**

| **Field** | **Value** |
|:---|:---|
| **Profile** | max |
| **Backend** | pytorch-cpu reference path unless the report declares a different backend |
| **Machine class** | laptop-class CPU or laptop-class accelerator with full hardware fingerprint |
| **Dataset mode** | fetched Fashion-MNIST with fixed torchvision transforms, split, and seed; no synthetic fallback |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Aggregation** | median top-1 accuracy across five independent reference runs |
| **Artifact policy** | preserve JSON, HTML, CSV, .provd.json, run fingerprint, dataset asset metadata, and raw metric values for each run |
| **Rerun policy** | rerun all five references when model code, dataset preprocessing, optimizer schedule, PyTorch major version, or target hardware class changes |

**Reviewer notes:**

- Fashion-MNIST ResNet-18 baseline satisfies the target; target is intentionally reachable for laptop-class classroom runs while preserving a canonical vision training shape.

## Verified Baseline

| **Field** | **Value** |
|:---|:---|
| **Train loss** | 0.638 |
| **Val loss** | 0.584 |
| **Accuracy** | 0.773 |
| **Epochs** | 3 |
| **Time seconds** | 7.4 |
| **Note** | Single-seed Apple MPS default run with 20 train batches and 20 validation batches per epoch; target remains provisional until the five-seed reference sweep. |

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Peak bytes per step** | 48234496 |
| **Note** | 11.2M params; activations + weights ~46 MB at B=64. Just under 4*LLC threshold; classification depends on whether activations stream or stay resident — measure. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | bandwidth_bound |
| **Flops per byte** | 3.931 |
| **Classification rule** | intensity 3.93 vs [low 13.71, high 54.84]; util 0.059 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/resnet18-train_2026-04-16T21-05-40Z_01868a7fce6e.json |
| **Evidence sha256 short** | d600a1c86702 |
| **Measured at** | 2026-04-16T21-05-40Z |
| **Platform machine class** | apple-silicon |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dispatch_bound |
| **Utilization** | 0.0589 |
| **Achieved bw gbps** | 31.054 |
| **Classification rule** | intensity 3.93 vs [low 13.71, high 54.84]; util 0.059 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/resnet18-train_2026-04-16T21-05-40Z_01868a7fce6e.json |
| **Evidence sha256 short** | d600a1c86702 |
| **Measured at** | 2026-04-16T21-05-40Z |
| **Platform machine class** | apple-silicon |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.vision:run_resnet18_min` |
| max | `mlperf.runners.vision:run_resnet18_max` |

Registry source: [`registry/suites/vision/resnet18-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/vision/resnet18-train.yaml)
