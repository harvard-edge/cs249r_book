---
title: "mobilenet-cifar100-composed-fp16"
subtitle: "vision suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-systems-only">systems-only</span>

> Demonstrates compression composition and backend gaps, but the composed path is a research scaffold rather than a standalone accuracy benchmark.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | vision |
| **Model** | MobileNetV2-cifar100 + 2:4 sparsity + fake-INT8 + fp16 |
| **Parameters** | 2.4M |
| **Scenario** | inference |
| **Maturity** | base |
| **Candidate result status** | systems-only |
| **Provenance** | Composition of structured sparsity, fake INT8 accounting, and fp16 execution on MobileNetV2. The runner reports its computed compression ratio, while runtime speedup remains unverified and is not claimed. |

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner measures composed precision and compression accounting on deterministic synthetic inputs. It has no trained checkpoint or task-accuracy claim.

- Reported data mode: `synthetic-deterministic`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **no**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
## How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload mobilenet-cifar100-composed-fp16 --profile max --open-report

# quick smoke pass
uv run mlperf run --workload mobilenet-cifar100-composed-fp16 --profile min

# research envelope
uv run mlperf run --workload mobilenet-cifar100-composed-fp16 --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | effective_compression_ratio |
| **Condition** | compressed inference emits logits and records compression ratio greater than 1 |
| **Reference runs** | 5 |

**Reviewer notes:**

- This workload demonstrates composed optimization accounting; score-bearing accuracy requires a trained checkpoint.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.vision:run_mobilenet_composed_min` |
| max | `mlperf.runners.vision:run_mobilenet_composed_max` |

Registry source: [`registry/suites/vision/mobilenet-cifar100-composed-fp16.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/vision/mobilenet-cifar100-composed-fp16.yaml)
