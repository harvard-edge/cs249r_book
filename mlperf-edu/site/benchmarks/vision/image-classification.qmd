---
title: "image-classification"
subtitle: "vision suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + selection ledger + datasets.yaml + asset dossiers + draft reference results + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-experimental">experimental</span>

> **Source-locked promotion rationale.** Direct MLPerf Tiny inheritance of the official pretrained float ResNet8, 200-sample CIFAR-10 accuracy set, and 85% gate; public score and performance claims remain withheld until repeated timing evidence is committed. Current draft evidence is listed below.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | vision |
| **Model** | mlperf-tiny-resnet8 |
| **Parameters** | 77.7K |
| **Dataset** | cifar10 |
| **Dataset source** | Krizhevsky CIFAR-10 from the pinned University of Toronto Hugging Face mirror, selected by the official MLPerf Tiny 200-sample accuracy index |
| **Scenario** | offline |
| **Maturity** | base |
| **Candidate result status** | experimental |

## Why This Benchmark Is Included

Canonical supervised vision task with established benchmark lineage.

| **Field** | **Value** |
|:---|:---|
| **Classroom value** | Connects a canonical convolutional model, fixed accuracy set, and inherited quality gate to reproducible inference measurement. |
| **Systems behavior** | Dense convolution, input layout, batching, and accelerator utilization. |
| **Benchmark lineage** | Direct MLPerf Tiny workload inheritance. |
| **Model choice** | The model is fixed by the upstream MLPerf Tiny contract. |
| **Dataset choice** | CIFAR-10 is fixed by the upstream MLPerf Tiny contract. |
| **Quality metric** | Top-1 accuracy and the threshold are fixed upstream. |
| **Published baseline** | MLPerf Tiny reference quality contract |
| **Alternative rejected** | ResNet-18 on Fashion-MNIST does not inherit this contract. |

## Authoritative Sources and Adaptation

| **Field** | **Value** |
|:---|:---|
| **Authority** | MLCommons MLPerf Tiny |
| **Repository** | [open source](https://github.com/mlcommons/tiny) |
| **Commit** | `1afd2c9820f795965a6134facd0b4dfae41ef23f` |
| **Path** | `benchmark/training/image_classification` |

**Adaptation boundary:** The official float TFLite weights, fused ResNet8 graph, raw 0..255 input convention, 200-sample accuracy index, and top-1 gate are preserved. MLPerf EDU maps the fused weights into an equivalent PyTorch graph, including TensorFlow SAME stride-two padding, and adds measurement, provenance, and reporting.

## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload image-classification --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd.json provenance)
uv run mlperf run --workload image-classification --profile max

# quick smoke pass
uv run mlperf run --workload image-classification --profile min

# research envelope
uv run mlperf run --workload image-classification --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | top1_accuracy |
| **Target** | 0.85 |
| **Direction** | higher |
| **Target kind** | inherited acceptance gate |
| **Target basis** | mlcommons_derived |
| **Tolerance** | 0.0 |
| **Reference runs** | 0 |

**Reviewer notes:**

- The 85% gate is inherited from MLPerf Tiny; it is not inferred from a project-created calibration.
- The exact official float checkpoint reproduces 87.0% on the 200-sample accuracy set through the PyTorch adapter; promotion still requires committed repeated timing.
- The measured region repeats the unchanged accuracy-set pass 50 times so laptop timing is long enough for the 5% outer-run repeatability gate; quality is computed from the first pass only.

## Systems Experiment Execution Contract

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | inference_and_evaluation_seconds |
| **Scenario** | offline |
| **Timing scope** | 50 complete passes over the official 200-sample MLPerf Tiny accuracy set after model and data construction |
| **Included phases** | host-to-device transfer of raw float CIFAR-10 inputs for every pass, exact PyTorch execution of the fused official float ResNet8 graph for every pass, top-1 prediction for every pass and accuracy accumulation for the first pass |
| **Excluded phases** | dataset fetching, Parquet decoding and accuracy-subset construction, TFLite parsing and PyTorch weight loading, one untimed warmup for each observed batch shape, report and provenance serialization |
| **Device synchronization** | synchronize immediately before and after the measured inference-and-evaluation region |
| **Outer reference runs** | 1 |
| **Outer preconditioning runs** | 0 |
| **Outer inter execution cooldown seconds** | 5 |
| **Repeatability metric** | inference_and_evaluation_seconds |
| **Repeatability limit** | 0.05 |

## Results

To make the metric concrete, one run of this benchmark on Apple M5 Max observed `top1_accuracy` of 0.8700, taking 7.8262 `inference_and_evaluation_seconds`.

That is an illustration, not a target and not a score. It comes from a single machine and your hardware will produce different timing. The number that matters is the one your own run reports.

Run the benchmark and read your own report:

```bash
uv run mlperf run --workload image-classification --profile max
```

Every run writes an HTML dashboard for reading, JSON for machine use, CSV for spreadsheets, and a `.provd.json` provenance manifest. Those artifacts carry the quality decision, timing distribution, hardware and software fingerprint, asset digests, and model lineage for the run you performed.

See [Reading Results](../../guide/results.qmd) for how to interpret them.

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.vision:run_image_classification_min` |
| max | `mlperf.runners.vision:run_image_classification_max` |

Registry source: [`registry/suites/vision/image-classification.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/vision/image-classification.yaml)
