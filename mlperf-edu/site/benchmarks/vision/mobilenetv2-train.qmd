---
title: "mobilenetv2-train"
subtitle: "vision suite"
---

<!-- GENERATED FILE — do not edit by hand.
     Source of truth: registry/ + datasets.yaml + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

<span class="badge status-score-bearing">score-bearing</span>

> Uses Fashion-MNIST with a local MobileNetV2 implementation and explicit top-1 target for edge and model-efficiency studies.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | vision |
| **Model** | mobilenetv2 |
| **Parameters** | 2.4M |
| **Dataset** | fashion-mnist |
| **Dataset source** | Xiao, Rasul & Vollgraf (2017), Fashion-MNIST via torchvision; MIT license; cached locally after first download |
| **Scenario** | single_stream |
| **Maturity** | base |
| **Public status** | score-bearing |
| **Provenance** | Sandler et al. 2018 (MobileNetV2); FULLY LOCAL — no torchvision.models dependency |

## How to Run

```bash
# one-time asset preparation
mlperf fetch --workload mobilenetv2-train --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
mlperf run --workload mobilenetv2-train --profile max --open-report

# quick smoke pass
mlperf run --workload mobilenetv2-train --profile min

# research envelope
mlperf run --workload mobilenetv2-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | top1_accuracy |
| **Target** | 0.7 |
| **Direction** | higher |
| **Target basis** | reference_runs |
| **Tolerance** | 0.0 |
| **Reference runs** | 5 |

**Variance summary:**

| **Field** | **Value** |
|:---|:---|
| **Runs** | 5 |
| **Statistic** | median |
| **Acceptance rule** | median top-1 accuracy must be >= target |
| **Spread note** | Fashion-MNIST split and seed are fixed; target represents a fast out-of-box correctness bar. |

**Reference protocol:**

| **Field** | **Value** |
|:---|:---|
| **Profile** | max |
| **Backend** | pytorch-cpu reference path unless the report declares a different backend |
| **Machine class** | laptop-class CPU or laptop-class accelerator with full hardware fingerprint |
| **Dataset mode** | fetched Fashion-MNIST with fixed torchvision transforms, split, and seed; no synthetic fallback |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Aggregation** | median top-1 accuracy across five independent reference runs |
| **Artifact policy** | preserve JSON, HTML, CSV, .provd.json, run fingerprint, dataset asset metadata, and raw metric values for each run |
| **Rerun policy** | rerun all five references when model code, dataset preprocessing, optimizer schedule, PyTorch major version, or target hardware class changes |

**Reviewer notes:**

- MobileNetV2 baseline satisfies the public target; note remains that higher tuned accuracy is a stretch target.

## Verified Baseline

| **Field** | **Value** |
|:---|:---|
| **Train loss** | 0.805 |
| **Val loss** | 0.738 |
| **Accuracy** | 0.727 |
| **Epochs** | 5 |
| **Time seconds** | 24.2 |
| **Note** | Single-seed Apple MPS calibration with 50 train batches and 50 validation batches per epoch at learning rate 1e-4; CPU fallback is currently much slower and needs runner optimization before endorsement. |

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Peak bytes per step** | 10485760 |
| **Note** | 2.4M params, depthwise-separable convs; 10 MB per-step in grey band — measure. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | bandwidth_bound |
| **Flops per byte** | 3.691 |
| **Classification rule** | intensity 3.69 vs [low 13.71, high 54.84]; util 0.005 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/mobilenetv2-train_2026-04-16T21-09-06Z_6a95ef082eee.json |
| **Evidence sha256 short** | 71606a91b999 |
| **Measured at** | 2026-04-16T21-09-06Z |
| **Platform machine class** | apple-silicon |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dispatch_bound |
| **Note** | Despite 'edge' label, MobileNetV2 is mostly memory-bound, not compute-bound |
| **Utilization** | 0.005 |
| **Achieved bw gbps** | 2.645 |
| **Classification rule** | intensity 3.69 vs [low 13.71, high 54.84]; util 0.005 vs [dispatch 0.25, sat 0.5] |
| **Evidence sidecar** | roofline/mobilenetv2-train_2026-04-16T21-09-06Z_6a95ef082eee.json |
| **Evidence sha256 short** | 71606a91b999 |
| **Measured at** | 2026-04-16T21-09-06Z |
| **Platform machine class** | apple-silicon |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.vision:run_mobilenetv2_min` |
| max | `mlperf.runners.vision:run_mobilenetv2_max` |

Registry source: [`registry/suites/vision/mobilenetv2-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/vision/mobilenetv2-train.yaml)
