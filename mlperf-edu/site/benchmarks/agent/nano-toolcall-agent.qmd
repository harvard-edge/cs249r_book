---
title: "nano-toolcall-agent"
subtitle: "agent suite"
---

<!-- GENERATED FILE — do not edit by hand.
     Source of truth: registry/ + datasets.yaml + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

<span class="badge status-systems-only">systems-only</span>

> Exercises structured-output and tool-call dispatch behavior, but lacks a public quality target and remains a systems-only agent workload.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | agent |
| **Model** | nano-toolcall-agent |
| **Parameters** | 13.7M |
| **Dataset** | react_traces |
| **Dataset source** | Structured multi-step reasoning traces; ReAct format |
| **Scenario** | server |
| **Maturity** | base |
| **Public status** | systems-only |
| **Provenance** | Schick et al. 2024 (Toolformer); measures structured output generation |

## How to Run

```bash
# one-time asset preparation
mlperf fetch --workload nano-toolcall-agent --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
mlperf run --workload nano-toolcall-agent --profile max --open-report

# quick smoke pass
mlperf run --workload nano-toolcall-agent --profile min

# research envelope
mlperf run --workload nano-toolcall-agent --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dram_bound |
| **Peak bytes per step** | 60817408 |
| **Note** | Bonus workload (not in core 16); structured output generation |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | bandwidth_bound |
| **Flops per byte** | 0.7 |
| **Classification rule** | tool-call decode |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | dispatch_bound |
| **Utilization** | 0.1 |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.agent:run_toolcall_min` |
| max | `mlperf.runners.agent:run_toolcall_max` |

Registry source: [`registry/suites/agent/nano-toolcall-agent.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/agent/nano-toolcall-agent.yaml)
