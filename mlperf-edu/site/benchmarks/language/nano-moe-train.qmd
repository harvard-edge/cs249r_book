---
title: "nano-moe-train"
subtitle: "language suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-systems-only">systems-only</span>

> Exercises multi-step MoE routing and conditional-compute training on deterministic synthetic tokens. TinyShakespeare and its candidate loss target are not used by the current max path.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | NanoMoEWhiteBox(vocab=256, width=64, heads=4, layers=2, experts=8, top_k=2) |
| **Parameters** | 608384 |
| **Dataset** | tinyshakespeare |
| **Dataset source** | Project Gutenberg eBook 100, generated MLPerf EDU tiny Shakespeare excerpt, character-level, 1.1MB |
| **Scenario** | training |
| **Maturity** | research |
| **Candidate result status** | systems-only |
| **Provenance** | Shazeer et al. 2017 (MoE); 8 experts, top-2 routing |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner performs three optimizer steps with a 608384-parameter sparse MoE on deterministic tokens. It does not consume TinyShakespeare or enforce the candidate loss target.

- Reported data mode: `synthetic-micro-shard`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **no**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
## How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload nano-moe-train --profile max --open-report

# quick smoke pass
uv run mlperf run --workload nano-moe-train --profile min

# research envelope
uv run mlperf run --workload nano-moe-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | cross_entropy_loss |
| **Target** | 0.05 |

> **Not enforced by the current `max` runner.** This target is research context for future promotion; the current path remains systems-only.

## Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | parameter_delta_l2 |
| **Condition** | three deterministic MoE optimizer steps complete with finite losses, nonzero parameter updates, and positive token throughput |

**Reviewer notes:**

- This systems check uses synthetic tokens and does not evaluate TinyShakespeare loss.

## Local Calibration Observation

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | historical-unverified-no-committed-artifact |
| **Protocol compatibility** | superseded |
| **Replacement required** | True |
| **Train loss** | 0.042 |
| **Val loss** | 0.042 |
| **Epochs** | 25 |
| **Time seconds** | 158 |
| **Note** | This former real-data observation used a different model and training protocol; it is not evidence for the current systems-only max path. |

> This observation is local development context. It is not a review-eligible reference result.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.extended:run_nano_moe_min` |
| max | `mlperf.runners.extended:run_nano_moe_max` |

Registry source: [`registry/suites/language/nano-moe-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nano-moe-train.yaml)
