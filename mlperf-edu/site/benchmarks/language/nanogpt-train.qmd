---
title: "nanogpt-train"
subtitle: "language suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-score-bearing">score-bearing</span>

> Uses a deterministic TinyShakespeare excerpt generated from Project Gutenberg with an explicit cross-entropy target, making it suitable for classroom training correctness and public performance reporting.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-12m |
| **Parameters** | 11.5M |
| **Dataset** | tinyshakespeare |
| **Dataset source** | Project Gutenberg eBook 100, generated MLPerf EDU tiny Shakespeare excerpt, character-level, 1.1MB |
| **Scenario** | training |
| **Maturity** | base |
| **Candidate result status** | score-bearing |
| **Provenance** | Vaswani et al. 2017 (Transformer); maps to MLPerf Training GPT-3/LLaMA |

> **Parameter count note:** Char-level vocab=128 (TinyShakespeare uses 65 unique chars). Earlier README/paper figures of 85.9M and 124.4M reflected a vocab=50,257 BPE config that was never reachable from the char-level data; iteration 1 reconciled the model to its actual effective size. As built (vocab=128, n_embd=384, n_head=6, n_layer=6, max_seq_len=2048, tied wte/lm_head) the base model instantiates 11,483,136 parameters; re-derive this figure if the base max_seq_len changes.

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload nanogpt-train --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload nanogpt-train --profile max --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-train --profile min

# research envelope
uv run mlperf run --workload nanogpt-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | cross_entropy_loss |
| **Target** | 2.3 |
| **Direction** | lower |
| **Target basis** | reference_runs |
| **Tolerance** | 0.0 |
| **Reference runs** | 5 |

**Variance summary:**

| **Field** | **Value** |
|:---|:---|
| **Runs** | 5 |
| **Statistic** | median |
| **Acceptance rule** | median validation loss must be <= target |
| **Metric** | cross_entropy_loss |
| **Median** | 2.087835317850113 |
| **Min** | 1.9816058337688447 |
| **Max** | 2.1344807863235475 |
| **Spread note** | Fresh development calibration across seeds 0-4 produced validation cross-entropy from 1.9816058337688447 to 2.1344807863235475. The clean public-candidate sweep and committed reference summary remain pending. |

**Reference protocol:**

| **Field** | **Value** |
|:---|:---|
| **Profile** | max |
| **Backend** | pytorch-cpu reference path unless the report declares a different backend |
| **Machine class** | laptop-class CPU or laptop-class accelerator with full hardware fingerprint |
| **Dataset mode** | fetched Project Gutenberg Shakespeare source with deterministic TinyShakespeare excerpt recipe, fixed tokenizer, split, and seed; no synthetic fallback |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Aggregation** | median validation loss across five independent reference runs |
| **Artifact policy** | preserve JSON, HTML, CSV, .provd.json, run fingerprint, dataset asset metadata, and raw metric values for each run |
| **Rerun policy** | rerun all five references when model code, dataset preprocessing, optimizer schedule, PyTorch major version, or target hardware class changes |

**Reviewer notes:**

- Every development seed satisfied the 2.3 validation-loss target. Public promotion still requires the same five-seed protocol from a clean worktree with a committed evidence summary.

## Development Calibration (Not Review Eligible)

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | pending-clean-public-candidate-reference-summary |
| **Review eligible** | False |
| **Calibration tier** | development |
| **Development summary id** | nanogpt-train_max_20260711T045626.172816Z |
| **Development summary sha256** | ca74f0561f563f5b478038d555ad2e367ff155a9965e0e478d7a0f7b8be9ddd5 |
| **Development summary availability** | local-handoff |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Cross entropy loss by seed** | 2.1344807863235475, 2.056773912906647, 2.1026881873607635, 1.9816058337688447, 2.087835317850113 |
| **Cross entropy loss** | 2.087835317850113 |
| **Val loss** | 2.087835317850113 |
| **Five seed cross entropy median** | 2.087835317850113 |
| **Five seed cross entropy min** | 1.9816058337688447 |
| **Five seed cross entropy max** | 2.1344807863235475 |
| **Five seed cross entropy stdev** | 0.05808833675616628 |
| **Epochs** | 25 |
| **Duration seconds median** | 127.42291408299934 |
| **Duration seconds min** | 121.23956066707615 |
| **Duration seconds max** | 130.2600357090123 |

> Fresh real-data Apple MPS development calibration across seeds 0-4. The values are not review eligible until a clean public-candidate sweep commits a complete artifact index and retains its content-addressed raw package for review.

> This is a project development calibration, not an MLCommons-verified result or a review-eligible reference package.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.nanogpt:run_min` |
| max | `mlperf.runners.nanogpt:run_max` |

Registry source: [`registry/suites/language/nanogpt-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-train.yaml)
