---
title: "nanogpt-inference"
subtitle: "language suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

This workload family exposes **5 measured variants** of `nanogpt-inference`. Each variant is independently runnable and reported. The table records model and dataset contracts per variant because those contracts can differ within a family.

| **Variant** | **Workload ID** | **Model** | **Params** | **Dataset** | **Scenario** | **Candidate result status** |
|:---|:---|:---|:---|:---|:---|:---|
| [fp32-b16](#variant-fp32-b16) | `nanogpt-decode-fp32-b16` | nanogpt-small-86m | 88.3M | prompt-suite-local | server | systems-only |
| [fp16-b16](#variant-fp16-b16) | `nanogpt-decode-fp16-b16` | nanogpt-small-86m | 88.3M | prompt-suite-local | server | systems-only |
| [speculative](#variant-speculative) | `nanogpt-decode-spec` | nanogpt-small-86m | 88.3M | prompt-suite-local | server | systems-only |
| [prefill](#variant-prefill) | `nanogpt-prefill` | nanogpt-12m | 11.5M | prompt-suite-local | offline | performance-bearing |
| [decode](#variant-decode) | `nanogpt-decode` | nanogpt-12m | 11.5M | prompt-suite-local | server | performance-bearing |

## Variant: fp32-b16 {#variant-fp32-b16}

<span class="badge status-systems-only">systems-only</span>

> Provides an fp32, batch-16 decode scaffold for architecture experiments. Random weights make it ineligible for quality or public performance claims.

### At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-small-86m |
| **Parameters** | 88.3M |
| **Dataset** | prompt-suite-local |
| **Dataset source** | Deterministic local token prompts embedded in the runner; no external evaluation dataset required. |
| **Scenario** | server |
| **Maturity** | research |
| **Candidate result status** | systems-only |
| **Provenance** | GPT-2-Small geometry decoded at fp32 with random weights. This row tests execution and instrumentation only; no measured taxonomy classification is claimed. |

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner measures FP32 KV-cache mechanics on a tiny random-init NanoGPT model and deterministic local token prompts. It does not make a trained-model quality claim.

- Reported data mode: `synthetic-micro-shard`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **yes**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
### How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload nanogpt-inference --variant fp32-b16 --profile max --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-inference --variant fp32-b16 --profile min

# research envelope
uv run mlperf run --workload nanogpt-inference --variant fp32-b16 --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

### Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | output_tokens_per_sec |
| **Condition** | fp32 decode completes the configured steps and records positive output throughput |
| **Reference runs** | 5 |

**Reviewer notes:**

- This variant is for dtype, memory-bandwidth, and dispatch studies; task quality is inherited from the canonical inference workload.

### Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

### Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.extended:run_nanogpt_decode_fp32_min` |
| max | `mlperf.runners.extended:run_nanogpt_decode_fp32_max` |

Registry source: [`registry/suites/language/nanogpt-inference/variants/fp32-b16.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-inference/variants/fp32-b16.yaml)


## Variant: fp16-b16 {#variant-fp16-b16}

<span class="badge status-systems-only">systems-only</span>

> Characterizes fp16 LLM serving behavior and memory footprint with a controlled scaffold; reports systems evidence rather than model quality.

### At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-small-86m |
| **Parameters** | 88.3M |
| **Dataset** | prompt-suite-local |
| **Dataset source** | Deterministic local token prompts embedded in the runner; no external evaluation dataset required. |
| **Scenario** | server |
| **Maturity** | research |
| **Candidate result status** | systems-only |
| **Provenance** | GPT-2-Small geometry decoded at fp16 with random weights. This row supports controlled dtype experiments but makes no unverified speedup or taxonomy claim. |

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner measures FP16 KV-cache mechanics on a tiny random-init NanoGPT model and deterministic local token prompts. It does not make a trained-model quality claim.

- Reported data mode: `synthetic-micro-shard`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **yes**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
### How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload nanogpt-inference --variant fp16-b16 --profile max --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-inference --variant fp16-b16 --profile min

# research envelope
uv run mlperf run --workload nanogpt-inference --variant fp16-b16 --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

### Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | output_tokens_per_sec |
| **Condition** | fp16 decode completes the configured steps and records positive output throughput |
| **Reference runs** | 5 |

**Reviewer notes:**

- This variant is for dtype, memory-footprint, and backend studies; task quality is inherited from the canonical inference workload.

### Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

### Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.extended:run_nanogpt_decode_fp16_min` |
| max | `mlperf.runners.extended:run_nanogpt_decode_fp16_max` |

Registry source: [`registry/suites/language/nanogpt-inference/variants/fp16-b16.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-inference/variants/fp16-b16.yaml)


## Variant: speculative {#variant-speculative}

<span class="badge status-systems-only">systems-only</span>

> Teaches speculative decoding mechanics and acceptance-rate tradeoffs, but uses a scaffolded target/draft pair rather than a canonical trained model.

### At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-small-86m |
| **Parameters** | 88.3M |
| **Dataset** | prompt-suite-local |
| **Dataset source** | Deterministic local token prompts embedded in the runner; no external evaluation dataset required. |
| **Scenario** | server |
| **Maturity** | research |
| **Candidate result status** | systems-only |
| **Provenance** | Speculative decoding scaffold with an 11M-parameter draft, an 88M-parameter target, gamma 4, and lossless argmax verification. Random initialization makes this an execution lesson rather than a performance baseline. |

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner measures speculative-decode control flow with tiny random-init target and draft models on deterministic local prompts. It does not claim trained-model acceptance quality.

- Reported data mode: `synthetic-micro-shard`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **yes**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
### How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload nanogpt-inference --variant speculative --profile max --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-inference --variant speculative --profile min

# research envelope
uv run mlperf run --workload nanogpt-inference --variant speculative --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

### Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | acceptance_rate |
| **Condition** | speculative decode emits the configured tokens and records acceptance rate, cycles, and output throughput |
| **Reference runs** | 5 |

**Reviewer notes:**

- This is a test-time-compute workload for draft-target verification behavior; task quality requires trained draft and target checkpoints.

### Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

### Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.extended:run_nanogpt_decode_spec_min` |
| max | `mlperf.runners.extended:run_nanogpt_decode_spec_max` |

Registry source: [`registry/suites/language/nanogpt-inference/variants/speculative.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-inference/variants/speculative.yaml)


## Variant: prefill {#variant-prefill}

<span class="badge status-performance-bearing">performance-bearing</span>

> Uses the standardized NanoGPT checkpoint path to measure prefill throughput and repeated latency statistics with a functional quality dependency on nanogpt-train.

### At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-12m |
| **Parameters** | 11.5M |
| **Dataset** | prompt-suite-local |
| **Dataset source** | Deterministic local token prompts embedded in the runner; no external evaluation dataset required. |
| **Scenario** | offline |
| **Maturity** | base |
| **Candidate result status** | performance-bearing |
| **Provenance** | GPT-2 prefill regime; corresponds to MLPerf Inference 'prompt processing' phase |

### How to Run

```bash
# prepare and quality-check the shared training checkpoint
OUTPUT_DIR="submissions/nanogpt-inference-max"
uv run mlperf fetch --workload nanogpt-train --profile max
uv run mlperf run --workload nanogpt-train --profile max --output-dir "$OUTPUT_DIR"

# checkpoint-backed benchmark run (reuses the same output directory)
uv run mlperf run --workload nanogpt-inference --variant prefill --profile max --output-dir "$OUTPUT_DIR" --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-inference --variant prefill --profile min

# research envelope
uv run mlperf run --workload nanogpt-inference --variant prefill --profile pro --output-dir "$OUTPUT_DIR"
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

### Candidate Performance Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | prefill_tokens_per_sec |
| **Condition** | a checkpoint-backed prefill run completes with a quality-approved NanoGPT training report, matching checkpoint SHA-256 provenance, and positive measured throughput |

**Reviewer notes:**

- Quality is inherited from the nanogpt-train checkpoint; this workload reports serving prefill performance.
- Review artifacts must retain the training report, training provenance manifest, checkpoint, checkpoint digest, per-run latency samples, and inference provenance manifest together.

**Five-seed reference protocol:**

| **Field** | **Value** |
|:---|:---|
| **Profile** | max |
| **Reference runs** | 5 |
| **Backend** | PyTorch CPU or accelerator path recorded with the complete execution and hardware fingerprint |
| **Machine class** | laptop-class CPU or laptop-class accelerator |
| **Dataset mode** | checkpoint-backed fixed-shape prompt batch using a quality-approved NanoGPT training checkpoint |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Aggregation** | median prefill_tokens_per_sec across five independent seeded runs |
| **Repeatability metric** | sample coefficient of variation of prefill_tokens_per_sec across the five reference runs |
| **Repeatability limit** | 0.05 |
| **Repeatability action** | withhold the performance reference and rerun the complete protocol when the coefficient of variation exceeds 5% |
| **Functional acceptance** | every run must pass the checkpoint, provenance, and positive-throughput functional gate |
| **Artifact policy** | create a new immutable attempt directory and SHA-256 index every report, provenance manifest, checkpoint, export, and runner-declared artifact |
| **Rerun policy** | if any run fails or times out, create a new attempt and rerun all five seeds; never replace one seed in an existing attempt |

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | prefill_tokens_per_sec |
| **Warmup runs** | 3 |
| **Measured runs** | 10 |
| **Latency statistics** | median, p90, p99 |
| **Raw sample metric** | prefill_latency_samples_s |
| **Timing scope** | synchronized checkpoint-backed forward passes over one fixed-shape prompt batch |

**Checkpoint contract:**

| **Field** | **Value** |
|:---|:---|
| **Source workload** | nanogpt-train |
| **Digest** | sha256 |
| **Required artifacts** | training report, training provenance manifest, checkpoint, inference report, inference provenance manifest |

### Recorded Project Reference Baseline

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | committed-reference-summary |
| **Review eligible** | True |
| **Evidence tier** | public-candidate |
| **Evidence id** | nanogpt-prefill_max_20260711T084140.159367Z |
| **Evidence file** | reference_results/nanogpt-prefill/nanogpt-prefill_max_20260711T084140.159367Z.json |
| **Evidence sha256** | bc3e8f01c279d1d2bbf0f8a24b15e85270584a5d35e16883a9751b4b5a04b68b |
| **Reference package availability** | local-handoff |
| **External publication status** | pending |
| **Source git sha** | 0ec4d3e1c415944227d0754d170edb0addc1d925 |
| **Profile** | max |
| **Device requested** | mps |
| **Data mode** | checkpoint-backed |
| **Execution backend** | pytorch-mps |
| **Hardware chip** | Apple M5 Max |
| **Source training evidence id** | nanogpt-train_max_20260711T083347.154092Z |
| **Source training evidence sha256** | 6f58270368d1e75445a7c7bcc8c20ca710bb9994090aa4705440525ef8cc0638 |
| **Source training seed** | 4 |
| **Source training checkpoint sha256** | a0d2f31a747355d47d11c6aa77eb09faf2232f84cb519accb286a78159fb2d8a |
| **Source training package sha256** | 0b0173d78e2c3315c4687b6319beb8a2826c98bce7f52710542f4b496edadd20 |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Primary metric** | prefill_tokens_per_sec |
| **Metric values by seed** | 115665.7588935296, 125183.0113402264, 114052.80426627811, 124578.1801076891, 125062.33989775984 |
| **Prefill tokens per sec** | 124578.1801076891 |
| **Median** | 124578.1801076891 |
| **Min** | 114052.80426627811 |
| **Max** | 125183.0113402264 |
| **Mean** | 120908.41890109661 |
| **Sample stdev** | 5556.061632502751 |
| **Wall seconds median** | 1.6132285830099136 |
| **Wall seconds min** | 1.5671893749386072 |
| **Wall seconds max** | 1.6838348750025034 |
| **Wall seconds mean** | 1.6172479081898927 |
| **Wall seconds sample stdev** | 0.04975564510833179 |
| **Accepted runs** | 5 |
| **Functional passes** | 5 |
| **Coefficient of variation** | 0.04595264484475332 |

> Clean five-run project reference from exact source commit 0ec4d3e1. Evidence semantics were recomputed from the raw reports and manifests during promotion. Every run passed its functional gate. The primary performance metric has 4.60% sample coefficient of variation across the five runs, within the 5% promotion limit. The speed is a machine observation, not a portable target. Content-addressed portable run packages are retained for local review, but no public package URL is recorded. This is not an MLCommons-verified result.

> This is a project reference baseline, not an MLCommons-verified result.

### Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

### Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.nanogpt:run_prefill_min` |
| max | `mlperf.runners.nanogpt:run_prefill_max` |

Registry source: [`registry/suites/language/nanogpt-inference/variants/prefill.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-inference/variants/prefill.yaml)


## Variant: decode {#variant-decode}

<span class="badge status-performance-bearing">performance-bearing</span>

> Uses the standardized NanoGPT checkpoint path to measure autoregressive decode throughput, repeated latency statistics, and KV-cache behavior with a functional check.

### At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | nanogpt-12m |
| **Parameters** | 11.5M |
| **Dataset** | prompt-suite-local |
| **Dataset source** | Deterministic local token prompts embedded in the runner; no external evaluation dataset required. |
| **Scenario** | server |
| **Maturity** | base |
| **Candidate result status** | performance-bearing |
| **Provenance** | GPT-2 autoregressive decode with a KV cache, representing the decode phase implemented by serving systems such as vLLM, TensorRT-LLM, and TGI |

### How to Run

```bash
# prepare and quality-check the shared training checkpoint
OUTPUT_DIR="submissions/nanogpt-inference-max"
uv run mlperf fetch --workload nanogpt-train --profile max
uv run mlperf run --workload nanogpt-train --profile max --output-dir "$OUTPUT_DIR"

# checkpoint-backed benchmark run (reuses the same output directory)
uv run mlperf run --workload nanogpt-inference --variant decode --profile max --output-dir "$OUTPUT_DIR" --open-report

# quick smoke pass
uv run mlperf run --workload nanogpt-inference --variant decode --profile min

# research envelope
uv run mlperf run --workload nanogpt-inference --variant decode --profile pro --output-dir "$OUTPUT_DIR"
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

### Candidate Performance Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | decode_steps |
| **Condition** | a checkpoint-backed decode run completes 64 configured steps with a quality-approved NanoGPT training report, matching checkpoint SHA-256 provenance, and positive measured output throughput |

**Reviewer notes:**

- Quality is inherited from the nanogpt-train checkpoint; this workload reports decode latency, KV-cache behavior, and throughput.
- Review artifacts must retain request-level TTFT and per-token ITL samples together with the training and inference provenance chain.

**Five-seed reference protocol:**

| **Field** | **Value** |
|:---|:---|
| **Profile** | max |
| **Reference runs** | 5 |
| **Backend** | PyTorch CPU or accelerator path recorded with the complete execution and hardware fingerprint |
| **Machine class** | laptop-class CPU or laptop-class accelerator |
| **Dataset mode** | checkpoint-backed requests using a quality-approved NanoGPT training checkpoint |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Aggregation** | median output_tokens_per_sec across five independent seeded runs |
| **Repeatability metric** | sample coefficient of variation of output_tokens_per_sec across the five reference runs |
| **Repeatability limit** | 0.05 |
| **Repeatability action** | withhold the performance reference and rerun the complete protocol when the coefficient of variation exceeds 5% |
| **Functional acceptance** | every run must complete all configured decode steps and pass checkpoint, provenance, and positive-throughput gates |
| **Artifact policy** | create a new immutable attempt directory and SHA-256 index every report, provenance manifest, checkpoint, export, and runner-declared artifact |
| **Rerun policy** | if any run fails or times out, create a new attempt and rerun all five seeds; never replace one seed in an existing attempt |

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | output_tokens_per_sec |
| **Warmup runs** | 3 |
| **Measured runs** | 20 |
| **Decode steps per request** | 64 |
| **Latency statistics** | median, p90, p99 |
| **Raw sample metrics** | request-level TTFT, per-token ITL |
| **Timing scope** | synchronized checkpoint-backed requests with a separate untimed warmup request |

**Checkpoint contract:**

| **Field** | **Value** |
|:---|:---|
| **Source workload** | nanogpt-train |
| **Digest** | sha256 |
| **Required artifacts** | training report, training provenance manifest, checkpoint, inference report, inference provenance manifest |

### Recorded Project Reference Baseline

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | committed-reference-summary |
| **Review eligible** | True |
| **Evidence tier** | public-candidate |
| **Evidence id** | nanogpt-decode_max_20260711T084155.577249Z |
| **Evidence file** | reference_results/nanogpt-decode/nanogpt-decode_max_20260711T084155.577249Z.json |
| **Evidence sha256** | bae464def8db558afcd377d506f9a25098b58d3ad934e03b474fa8b534beddf7 |
| **Reference package availability** | local-handoff |
| **External publication status** | pending |
| **Source git sha** | 0ec4d3e1c415944227d0754d170edb0addc1d925 |
| **Profile** | max |
| **Device requested** | mps |
| **Data mode** | checkpoint-backed |
| **Execution backend** | pytorch-mps |
| **Hardware chip** | Apple M5 Max |
| **Source training evidence id** | nanogpt-train_max_20260711T083347.154092Z |
| **Source training evidence sha256** | 6f58270368d1e75445a7c7bcc8c20ca710bb9994090aa4705440525ef8cc0638 |
| **Source training seed** | 4 |
| **Source training checkpoint sha256** | a0d2f31a747355d47d11c6aa77eb09faf2232f84cb519accb286a78159fb2d8a |
| **Source training package sha256** | 0b0173d78e2c3315c4687b6319beb8a2826c98bce7f52710542f4b496edadd20 |
| **Seeds** | 0, 1, 2, 3, 4 |
| **Primary metric** | output_tokens_per_sec |
| **Metric values by seed** | 127.1280762592526, 133.2852094860287, 131.83444650516844, 133.72933280518927, 129.7121922302817 |
| **Output tokens per sec** | 131.83444650516844 |
| **Median** | 131.83444650516844 |
| **Min** | 127.1280762592526 |
| **Max** | 133.72933280518927 |
| **Mean** | 131.13785145718413 |
| **Sample stdev** | 2.735094518987277 |
| **Wall seconds median** | 14.503981499932706 |
| **Wall seconds min** | 14.158116249949671 |
| **Wall seconds max** | 14.96386087499559 |
| **Wall seconds mean** | 14.52762437479105 |
| **Wall seconds sample stdev** | 0.2882922350491197 |
| **Accepted runs** | 5 |
| **Functional passes** | 5 |
| **Coefficient of variation** | 0.020856636650633795 |

> Clean five-run project reference from exact source commit 0ec4d3e1. Evidence semantics were recomputed from the raw reports and manifests during promotion. Every run passed its functional gate. The primary performance metric has 2.09% sample coefficient of variation across the five runs, within the 5% promotion limit. The speed is a machine observation, not a portable target. Content-addressed portable run packages are retained for local review, but no public package URL is recorded. This is not an MLCommons-verified result.

> This is a project reference baseline, not an MLCommons-verified result.

### Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

### Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.nanogpt:run_decode_min` |
| max | `mlperf.runners.nanogpt:run_decode_max` |

Registry source: [`registry/suites/language/nanogpt-inference/variants/decode.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/nanogpt-inference/variants/decode.yaml)
