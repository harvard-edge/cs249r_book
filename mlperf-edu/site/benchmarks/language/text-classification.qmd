---
title: "text-classification"
subtitle: "language suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-experimental">experimental</span>

> Official pinned DistilBERT checkpoint evaluated on its named GLUE SST-2 validation split; claims remain withheld until exact reproduction and committed five-run timing evidence are complete.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | language |
| **Model** | distilbert-base-uncased-finetuned-sst-2-english |
| **Parameters** | 67M |
| **Dataset** | sst2 |
| **Dataset source** | Official GLUE SST-2 archive, labeled 872-example development split |
| **Scenario** | offline |
| **Maturity** | research |
| **Candidate result status** | experimental |

## Authoritative Sources and Adaptation

| **Field** | **Value** |
|:---|:---|
| **Authority** | Hugging Face DistilBERT model card and GLUE |
| **Model** | [open source](https://huggingface.co/distilbert/distilbert-base-uncased-finetuned-sst-2-english) |
| **Model revision** | `714eb0fa89d2f80546fda750413ed43d93601a13` |
| **Dataset** | [open source](https://dl.fbaipublicfiles.com/glue/data/SST-2.zip) |

**Adaptation boundary:** The official checkpoint and tokenizer run through Transformers in PyTorch. MLPerf EDU adds fixed batching, timing, quality verification, provenance, and reporting.

## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload text-classification --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload text-classification --profile max --open-report

# quick smoke pass
uv run mlperf run --workload text-classification --profile min

# research envelope
uv run mlperf run --workload text-classification --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | accuracy |
| **Target** | 0.9105504587155964 |
| **Direction** | higher |
| **Target basis** | literature |
| **Tolerance** | 0.0 |
| **Reference runs** | 0 |

**Reviewer notes:**

- The target is the model card's verified accuracy for the GLUE SST-2 validation split, not a project-created threshold.
- Promotion requires exact local reproduction and a committed multi-run performance sweep.

## Systems Experiment Execution Contract

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | inference_seconds |
| **Scenario** | offline |
| **Timing scope** | five repetitions of the full 872-example SST-2 validation split after tokenized inputs and model weights are resident on the selected device |
| **Included phases** | batched PyTorch model execution |
| **Excluded phases** | model and dataset fetching, model construction, tokenization and host-to-device transfer, one untimed warmup for each observed batch shape, accuracy computation, report and provenance serialization |
| **Device synchronization** | synchronize immediately before and after the measured inference region |
| **Outer reference runs** | 5 |
| **Outer preconditioning runs** | 0 |
| **Outer inter execution cooldown seconds** | 5 |
| **Repeatability metric** | inference_seconds |
| **Repeatability limit** | 0.05 |

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | Classification is withheld until a reference-platform measurement exists. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | Classification is withheld until a reference-platform measurement exists. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | Classification is withheld until a synchronized utilization trace exists. |

## Model Source

| **Field** | **Value** |
|:---|:---|
| **Type** | huggingface-pinned |
| **Model** | distilbert/distilbert-base-uncased-finetuned-sst-2-english |
| **Pinned revision** | 714eb0fa89d2f80546fda750413ed43d93601a13 |
| **License** | apache-2.0 |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.text:run_text_classification_min` |
| max | `mlperf.runners.text:run_text_classification_max` |

Registry source: [`registry/suites/language/text-classification.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/language/text-classification.yaml)
