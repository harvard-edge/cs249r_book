---
title: "micro-gnn-train"
subtitle: "graph suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-systems-only">systems-only</span>

> Useful for sparse graph-kernel and dispatch studies; kept out of score-bearing status until the download/fallback path and quality target are hardened.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | graph |
| **Model** | MicroGCN(nodes=64, features=32, hidden=32, classes=4) |
| **Parameters** | 1652 |
| **Dataset** | cora |
| **Dataset source** | Sen et al. (2008), Cora citation network (2708 nodes, 7 classes, 5429 edges); auto-download; synthetic fallback available for offline use |
| **Scenario** | training |
| **Maturity** | base |
| **Candidate result status** | systems-only |
| **Provenance** | Kipf & Welling 2017 (GCN); maps to MLPerf Training GNN |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner performs four optimizer steps with a 1652-parameter graph network on a deterministic 64-node ring graph. It does not consume Cora or enforce the candidate accuracy target.

- Reported data mode: `synthetic-micro-shard`
- Candidate quality target enforced: **no**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **no**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
## How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload micro-gnn-train --profile max --open-report

# quick smoke pass
uv run mlperf run --workload micro-gnn-train --profile min

# research envelope
uv run mlperf run --workload micro-gnn-train --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | test_accuracy |
| **Target** | 0.78 |

> **Not enforced by the current `max` runner.** This target is research context for future promotion; the current path remains systems-only.

## Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | parameter_delta_l2 |
| **Condition** | four deterministic graph optimizer steps complete with finite losses, nonzero parameter updates, and logits for all 64 nodes |

**Reviewer notes:**

- This systems check uses a ring graph and does not evaluate Cora accuracy.

## Local Calibration Observation

| **Field** | **Value** |
|:---|:---|
| **Evidence status** | historical-unverified-no-committed-artifact |
| **Train loss** | 0.35 |
| **Val accuracy** | 0.816 |
| **Test accuracy** | 0.816 |
| **Epochs** | 50 |
| **Time seconds** | 2 |
| **Note** | 81.6% matches Kipf & Welling (2017) published result on Cora |

> This observation is local development context. It is not a review-eligible reference result.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.extended:run_micro_gnn_min` |
| max | `mlperf.runners.extended:run_micro_gnn_max` |

Registry source: [`registry/suites/graph/micro-gnn-train.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/graph/micro-gnn-train.yaml)
