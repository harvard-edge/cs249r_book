---
title: "graph-node-classification"
subtitle: "graph suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + selection ledger + datasets.yaml + asset dossiers + draft reference results + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-experimental">experimental</span>

> **Source-locked promotion rationale.** Direct official OGB GCN, ogbn-arxiv time split, evaluator, and published baseline; claims remain withheld until the laptop run and repeated timing evidence complete. Current draft evidence is listed below.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | graph |
| **Model** | ogb-gcn |
| **Parameters** | 110K |
| **Dataset** | ogbn-arxiv |
| **Dataset source** | Official OGB ogbn-arxiv v1 archive and time-based split |
| **Scenario** | training |
| **Maturity** | base |
| **Candidate result status** | experimental |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

## Why This Benchmark Is Included

Recognized reproducible graph-learning benchmark.

| **Field** | **Value** |
|:---|:---|
| **Classroom value** | Introduces message passing and graph-specific data representation. |
| **Systems behavior** | Sparse gather, scatter, and irregular memory access. |
| **Benchmark lineage** | OGB supplies data, split, evaluator, baseline, and leaderboard. |
| **Model choice** | GCN is the official interpretable baseline. |
| **Dataset choice** | ogbn-arxiv is more system-relevant than tiny citation graphs. |
| **Quality metric** | The OGB evaluator fixes the accuracy computation. |
| **Published baseline** | Official OGB baseline and leaderboard |
| **Alternative rejected** | Cora is useful pedagogically but too small for stable systems measurement. |

## Authoritative Sources and Adaptation

| **Field** | **Value** |
|:---|:---|
| **Authority** | Open Graph Benchmark |
| **Repository** | [open source](https://github.com/snap-stanford/ogb) |
| **Commit** | `61e9784ca76edeaa6e259ba0f836099608ff0586` |
| **Quality reference** | [open source](https://ogb.stanford.edu/docs/leader_nodeprop/) |

**Adaptation boundary:** The official three-layer full-batch GCN, symmetric graph, optimizer, 500-epoch schedule, time split, and OGB evaluator are preserved. MLPerf EDU adds measurement, provenance, and reporting.

## How to Run

```bash
# one-time asset preparation
uv run mlperf fetch --workload graph-node-classification --profile max

# benchmark run (writes JSON/HTML/CSV reports + .provd.json provenance)
uv run mlperf run --workload graph-node-classification --profile max

# quick smoke pass
uv run mlperf run --workload graph-node-classification --profile min

# research envelope
uv run mlperf run --workload graph-node-classification --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | test_accuracy |
| **Target** | 0.7174 |
| **Direction** | higher |
| **Target kind** | published mean with tolerance |
| **Target basis** | literature |
| **Tolerance** | 0.0029 |
| **Reference runs** | 0 |

**Reviewer notes:**

- The target and tolerance are the official OGB ogbn-arxiv leaderboard GCN mean and standard deviation for the 110,120-parameter implementation.
- The previously recorded 72.51 +/- 0.35 percent result belongs to a different leaderboard section and is not the ogbn-arxiv GCN reference.
- MLPerf EDU reports test accuracy at the epoch with the highest official validation accuracy, matching the OGB logger.
- {"Seed-sensitive verdict, retained deliberately. Measured across seeds 42, 1, and 7 the test accuracy spans 0.7115 to 0.7210, wider than the 0.0065 margin from the recorded result to the 0.7145 tolerance floor, so seed 1 misses while 42 and 7 pass. A local miss is within the expected spread and is not a setup error. This workload is the suite's clearest demonstration that one run is not a claim": 'rerun it with MLPERF_EDU_SEED changed and watch the verdict move.'}

## Systems Experiment Execution Contract

**Within-run measurement protocol:**

| **Field** | **Value** |
|:---|:---|
| **Primary metric** | train_and_eval_seconds |
| **Scenario** | training |
| **Timing scope** | 500 full-batch training epochs with official train, validation, and test evaluation after every epoch |
| **Included phases** | full-graph forward and backward passes, optimizer steps, official split evaluation |
| **Excluded phases** | dataset fetching and PyG processing, graph symmetrization and device transfer, model construction, checkpoint serialization, report and provenance serialization |
| **Device synchronization** | synchronize immediately before and after the measured training-and-evaluation region |
| **Outer reference runs** | 1 |
| **Outer preconditioning runs** | 0 |
| **Outer inter execution cooldown seconds** | 30 |
| **Repeatability metric** | train_and_eval_seconds |
| **Repeatability limit** | 0.05 |

## Results

To make the metric concrete, one run of this benchmark on Apple M5 Max observed `test_accuracy` of 0.7210, taking 719.819 `train_and_eval_seconds`.

That is an illustration, not a target and not a score. It comes from a single machine and your hardware will produce different timing. The number that matters is the one your own run reports.

Run the benchmark and read your own report:

```bash
uv run mlperf run --workload graph-node-classification --profile max
```

Every run writes an HTML dashboard for reading, JSON for machine use, CSV for spreadsheets, and a `.provd.json` provenance manifest. Those artifacts carry the quality decision, timing distribution, hardware and software fingerprint, asset digests, and model lineage for the run you performed.

See [Reading Results](../../guide/results.qmd) for how to interpret them.

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.graph:run_graph_node_classification_min` |
| max | `mlperf.runners.graph:run_graph_node_classification_max` |

Registry source: [`registry/suites/graph/graph-node-classification.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/graph/graph-node-classification.yaml)
