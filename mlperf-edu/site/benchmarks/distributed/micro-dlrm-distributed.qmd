---
title: "micro-dlrm-distributed"
subtitle: "distributed suite"
---

<!-- GENERATED FILE - do not edit by hand.
     Sources: registry/ + datasets.yaml + asset dossiers + the mlperf CLI.
     Regenerate with: python3 tools/generate_docs.py -->

::: {.callout-warning}
**Independent preview.** MLPerf EDU is not an official MLCommons benchmark and is not endorsed by MLCommons. Registry result labels are candidate classifications for review, not accepted MLPerf result categories.
:::

<span class="badge status-systems-only">systems-only</span>

> Standardizes a localhost DDP systems exercise for communication overhead studies, not a public recommendation-model score.

## At a Glance

| **Field** | **Value** |
|:---|:---|
| **Suite** | distributed |
| **Model** | micro-dlrm-1m via torch.distributed (Gloo, 2 ranks, localhost) |
| **Parameters** | 1.0M |
| **Scenario** | training |
| **Maturity** | base |
| **Candidate result status** | systems-only |
| **Provenance** | Iter-10 (Dean): two-process DDP smoke. Demonstrates DDP-vs- gradient-accumulation loss equivalence (delta 0.0064 < 0.02 check). AllReduce overhead 0.6 ms/step on M5 Max loopback Gloo. |

> **Scenario note:** `training` is a proposed MLPerf EDU label for train-then-quality workloads. It is not an official MLPerf Inference scenario.

::: {.callout-caution}
**Current `max` execution boundary.** The current max runner uses deterministic synthetic batches to compare localhost DDP with gradient accumulation and enforces their loss-equivalence check. It is not a recommender-accuracy benchmark.

- Reported data mode: `synthetic-deterministic`
- Candidate quality target enforced: **yes**
- Fetched assets used by this runner: **no**
- Declared dataset used by this runner: **no**

This path is systems-only. Its measurements are not a public score or performance baseline unless the workload is promoted through a reviewed result contract.
:::
## How to Run

```bash
# no asset fetch is required; current max uses its declared systems-only micro-shard

# benchmark run (writes JSON/HTML/CSV reports + .provd provenance)
uv run mlperf run --workload micro-dlrm-distributed --profile max --open-report

# quick smoke pass
uv run mlperf run --workload micro-dlrm-distributed --profile min

# research envelope
uv run mlperf run --workload micro-dlrm-distributed --profile pro
```

See the [running guide](../../guide/running.qmd) for profile semantics, report handling, and power measurement flags.

## Candidate Quality Target

| **Field** | **Value** |
|:---|:---|
| **Metric** | relative_loss_delta |
| **Target** | 0.05 |
| **Direction** | lower |

## Systems Experiment Execution Contract

**Functional acceptance:**

| **Field** | **Value** |
|:---|:---|
| **Metric** | relative_loss_delta |
| **Condition** | localhost DDP final loss remains within 5 percent of gradient-accumulation baseline |
| **Reference runs** | 5 |

**Reviewer notes:**

- This validates distributed training equivalence and communication overhead, not recommender quality.

## Measured Systems Regime

**Working set:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed working-set measurement and reference-platform capacity record exists; classification is withheld. |

**Arithmetic intensity:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld. |

**Dispatch:**

| **Field** | **Value** |
|:---|:---|
| **Value** | unmeasured |
| **Note** | No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld. |

## Implementation

| **Profile** | **Runner entry point** |
|:---|:---|
| min | `mlperf.runners.dlrm:run_distributed_min` |
| max | `mlperf.runners.dlrm:run_distributed_max` |

Registry source: [`registry/suites/distributed/micro-dlrm-distributed.yaml`](https://github.com/harvard-edge/cs249r_book/blob/main/mlperf-edu/registry/suites/distributed/micro-dlrm-distributed.yaml)
