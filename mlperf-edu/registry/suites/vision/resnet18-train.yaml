id: resnet18-train
model: resnet18
params: 11.2M
dataset: fashion-mnist
dataset_source: Xiao, Rasul & Vollgraf (2017), Fashion-MNIST via torchvision; MIT
  license; cached locally after first download
quality_target:
  metric: top1_accuracy
  value: 0.85
  direction: higher
  target_basis: reference_runs
  tolerance: 0.0
  reference_runs: 5
  variance_summary:
    runs: 5
    statistic: median
    acceptance_rule: median top-1 accuracy must be >= target
    metric: top1_accuracy
    median: 0.8750
    min: 0.8630
    max: 0.8781
    spread_note: Fresh development calibration across seeds 0-4 with five epochs,
      100 training batches per epoch, and the complete test split produced 86.30%
      to 87.81% top-1 accuracy. The clean public-candidate sweep and committed
      reference summary remain pending.
  reference_protocol:
    profile: max
    backend: pytorch-cpu reference path unless the report declares a different backend
    machine_class: laptop-class CPU or laptop-class accelerator with full hardware
      fingerprint
    dataset_mode: fetched Fashion-MNIST with fixed torchvision transforms, five
      epochs, 100 training batches per epoch, and the complete 10,000-example test
      split; no synthetic fallback
    seeds:
    - 0
    - 1
    - 2
    - 3
    - 4
    aggregation: median top-1 accuracy across five independent reference runs
    artifact_policy: preserve JSON, HTML, CSV, .provd.json, run fingerprint, dataset
      asset metadata, and raw metric values for each run
    rerun_policy: rerun all five references when model code, dataset preprocessing,
      optimizer schedule, PyTorch major version, or target hardware class changes
  reviewer_notes:
  - The 85% target leaves more than one percentage point below the weakest five-seed
    calibration result while remaining strong enough to reject broken training runs.
verified_baseline:
  evidence_status: pending-clean-public-candidate-reference-summary
  review_eligible: false
  calibration_tier: development
  development_summary_id: resnet18-train_max_20260711T044245.159697Z
  development_summary_sha256: c49fdf409f4181d0003e6256bdb9cac830d7ccd8d8425583de9818ec42baa15c
  development_summary_availability: local-handoff
  seeds:
  - 0
  - 1
  - 2
  - 3
  - 4
  top1_accuracy_by_seed:
  - 0.8781
  - 0.8755
  - 0.8673
  - 0.8630
  - 0.8750
  top1_accuracy: 0.8750
  accuracy: 0.8750
  five_seed_accuracy_median: 0.8750
  five_seed_accuracy_min: 0.8630
  five_seed_accuracy_max: 0.8781
  five_seed_accuracy_stdev: 0.006350354320823367
  epochs: 5
  duration_seconds_median: 51.4034499169793
  duration_seconds_min: 50.63850987504702
  duration_seconds_max: 62.62304970808327
  note: Fresh real-data Apple MPS development calibration across seeds 0-4. The
    values are not review eligible until a clean public-candidate sweep commits a
    complete artifact index and retains its content-addressed raw package for review.
scenario: training
public:
  status: score-bearing
  rationale: Uses Fashion-MNIST with a local ResNet-18 implementation and explicit
    top-1 target, suitable for public edge-training performance reports.
runner:
  min: mlperf.runners.vision:run_resnet18_min
  max: mlperf.runners.vision:run_resnet18_max
provenance: He et al. 2016 (ResNet); local ResNet-18 implementation (no torchvision.models
  dependency); torchvision datasets/transforms are used only to fetch and preprocess
  Fashion-MNIST
regime:
  working_set:
    value: unmeasured
    note: No committed working-set measurement and reference-platform capacity record exists; classification is withheld.
  arithmetic_intensity:
    value: unmeasured
    note: No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld.
  dispatch:
    value: unmeasured
    note: No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld.
