id: micro-dlrm-dram-train
model: micro-dlrm-dram-17m
params: 16802833
trainable_params: 16802833
virtual_table_bytes: 67108864
llc_capacity_factor: unmeasured
dataset: movielens-100k
dataset_source: Harper & Konstan (2015) — same dataset as micro-dlrm-train
quality_target:
  metric: accuracy
  value: 0.65
calibration_observation:
  evidence_status: bounded-local-methodology-check
  seed: 42
  final_accuracy: 0.694609375
  final_roc_auc: 0.7537700041541383
  n_params: 16802833
  working_set_bytes: 67108864
  baseline_note: A bounded local default-path check passed the 0.65 accuracy gate and
    confirmed exact model/table accounting. It is not promoted evidence and makes no
    DRAM-bandwidth or cache-regime claim.
scenario: training
public:
  status: systems-only
  rationale: Exercises a 64 MiB hashed embedding table with a broad deterministic
    user-item cross for recommender architecture studies. The exact allocation is
    reported, but a DRAM-bound regime is not claimed without hardware-local profiling.
runner:
  min: mlperf.runners.dlrm:run_dram_min
  max: mlperf.runners.dlrm:run_dram_max
max_execution:
  data_mode: real
  quality_target_enforced: true
  fetched_assets_used: true
  declared_dataset_used: true
  scope: systems-only
  note: The max runner uses MovieLens-100K, a deterministic user-item cross, and a
    65,536 by 256 fp32 embedding table while enforcing its local accuracy target. It
    remains systems-only because allocation size alone does not prove a DRAM-bound regime.
provenance: Naumov et al. 2019 (DLRM) and Weinberger et al. 2009 (hashing trick);
  uses hashed embedding lookups at laptop scale
regime:
  working_set:
    value: unmeasured
    note: No committed working-set measurement and reference-platform capacity record exists; classification is withheld.
  arithmetic_intensity:
    value: unmeasured
    note: No committed FLOP/byte measurement and reference-platform ridge-point record exists; classification is withheld.
  dispatch:
    value: unmeasured
    note: No committed synchronized utilization trace and reference-platform peak record exists; classification is withheld.
