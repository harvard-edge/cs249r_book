<!-- generated by tools/listings.py from src/01_tensor/01_tensor.py :: _as_tensor -->
```python
def _as_tensor(x):
    """Wrap a scalar or array as a Tensor; pass a Tensor through unchanged."""
    return x if isinstance(x, Tensor) else Tensor(x)
```
