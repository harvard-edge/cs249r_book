<!-- generated by tools/listings.py from src/01_tensor/01_tensor.py :: Copy -->
```python
class Copy(Function):
    """A contiguous copy of the array (used by contiguous() and 1D transpose)."""

    def forward(self, a):
        """
        Copy the array into contiguous memory.
        """
        return np.ascontiguousarray(a)
```
