# Running the Machine {.unnumbered}

\lettrine{B}{uilding} a physical AI machine is fundamentally an exercise in **reconciling disparate timescales, representations, and failure domains**. No single algorithm, neural model, or silicon core can span the vast continuum between high-level human reasoning and low-level physical dynamics. At the deliberative tier, human intent and task semantics are linguistic and symbolic, evolving over seconds ($0.1\text{--}1\text{ Hz}$). In the middle tier, physical space is continuous and metric, requiring 3D spatial representations and smooth kinematic trajectories generated at tens of hertz ($20\text{--}50\text{ Hz}$). At the execution tier, motor torque control is electromagnetic and immediate, demanding cycle-deterministic execution at kilohertz frequencies ($1000\text{ Hz}$) on bare-metal silicon to prevent mechanical resonance and kinetic destruction.

::: {#pri-vol4-multirate-authority .callout-principle title="The multi-rate authority hierarchy"}

**Invariant**: In an embodied architecture spanning $0.1\text{ Hz}$ to $1000\text{ Hz}$, safety and actuation authority scale inversely with semantic complexity and cycle latency. Fast, deterministic monitors ($1000\text{ Hz}$, microsecond response) always hold preemptive veto power over slow, high-complexity learned planners ($1\text{--}10\text{ Hz}$, multi-millisecond latency).

**Implication**: Real-time execution must run as an asymmetric pipeline. High-level Foundation Models issue advisory intentions, middle-tier planners generate kinematically feasible trajectories, and dedicated real-time microcontrollers project planned actions through Control Barrier Functions (CBFs) to guarantee safety invariants on every millisecond cycle.

:::

Part III details **The Living Organism: The Millisecond Pipeline** that transforms raw physical sensor photons into coordinated motor currents across six computational stages, implementing the complete S·P·A intersection landscape (@sec-boundary-spa-triad, @sec-appendix-spa):

* **From Photons to Spatial Claims (@sec-perception, Sense):** How raw, uncompressed sensor streams from SerDes lanes are ingested via DMA ring buffers, corrected for rolling-shutter distortion, calibrated against IMUs, and lifted into calibrated metric $SE(3)$ spatial claims under strict information-age budgets.
* **Preserving Belief Under Occlusion (@sec-memory, $S \cap P$):** How spatial representations (transform trees, TSDF voxel grids, 3D Gaussian Splats) persist when objects pass behind obstacles or cameras pan away, enforcing explicit temporal decay rates to prevent "frozen transform" collisions.
* **From Semantic Goals to Reachable Affordances (@sec-intent, $S \cap P$):** How open-vocabulary foundation models decompose human language into spatial affordances, issuing cryptographically bounded, expiring **Intent Leases** with hard time-to-live (TTL) envelopes that fail-safe on silence.
* **Synthesizing Continuous Motion (@sec-planning, $P \cap A$):** How continuous generative models predict $H$-step action chunks, smooth kinematic transitions across chunk boundaries using $C^2$ jerk splines, and guarantee safe stopping suffixes when upstream inference experiences latency spikes.
* **The Deterministic Gatekeeper (@sec-enforcement, $A \cap S$ & Causal Boundary):** The bare-metal 1 kHz safety shield. We implement Control Barrier Functions (CBFs) and quadratic programming (QP) projection on dedicated microcontrollers, enforcing the Four-Tier Fallback Ladder and the Simplex Architecture Pattern to retain exclusive veto authority over power stages.
* **The Silicon Substrate (@sec-placement, $S \cap P \cap A$):** How high-throughput neural deliberation and hard-real-time safety co-exist on shared heterogeneous System-on-Chip (SoC) platforms. We quantify on-chip memory crossbar QoS, DRAM bank contention, power distribution network ($L_{\text{bus}} \frac{dI_{\text{bus}}}{dt}$) voltage droop, and hysteretic thermal clock throttling.

::: {#pri-vol4-epistemic-decay .callout-principle title="The epistemic decay law"}

**Invariant**: Every spatial and sensory belief about the physical world degrades over time as unobserved entities move and sensor timestamps recede:
$$ \sigma^2(t) = \sigma_0^2 + Q \cdot (t - t_{\text{meas}}) $$
An unrefreshed spatial belief is an obstacle hazard.

**Implication**: Dynamic spatial representations (TSDF voxel grids, transform trees) must enforce exponential confidence decay on unobserved regions. High-level commands must be issued as bounded **Intent Leases** with strict time-to-live (TTL) deadlines. If a lease expires before renewal, the runtime immediately executes a safe-stop fallback.

:::

::: {#pri-vol4-substrate-isolation .callout-principle title="Heterogeneous substrate isolation"}

**Invariant**: Placing high-throughput neural inference (bursty memory traffic, massive DRAM bandwidth saturation, large $dI/dt$ transients) and real-time safety control on shared System-on-Chip (SoC) silicon creates microarchitectural interference: DRAM bank collisions, crossbar arbitration stalls, and core throttling under supply voltage droop.

**Implication**: Physical AI compute platforms must enforce hard hardware partitioning: memory bus QoS priority tagging, cache reservation for real-time threads, isolated power rails, and dedicated real-time microcontroller cores physically decoupled from GPU/NPU burst pipelines.

:::

Part III teaches students how to architect, synchronize, and isolate the six computational stages that keep high-level machine intelligence grounded in physical reality.
