# Running the Machine {.unnumbered}

\lettrine{A}{s} a mobile robot approaches an obstacle, a camera frame locates a person near its path. The planner proposes a turn, but the permission path must decide whether that observation is still fresh and whether the remaining clearance permits the turn or a stop. The decision has a deadline: another polished trajectory is useless if the robot has already crossed the braking margin. Running a physical AI machine means reconciling the timescales of sensing, planning, and actuation with that physical deadline. Human goals may change over seconds, trajectories over tens of milliseconds, and a particular motor controller may update every millisecond. The required rates and response times come from the plant and its measured delays, not from the names of the software tiers.

::: {#pri-vol4-proposal-permission .callout-principle title="Proposal is not permission"}

**Invariant**: A learned model may propose motion; only an independent permission path may admit it. The path decides each action on state whose age is known, within a deadline set by observation age, computation, transport, actuator response, and the clearance the delay leaves, and it owns a fallback that is feasible from the current state without the proposer.

**Implication**: Every runtime stage serves one decision, whether this proposal can cross the causal boundary before the margin runs out. Stages ahead of the permission path deliver evidence with its age and uncertainty attached, the permission path runs on resources that learned workloads cannot delay, and any guarantee the path offers, such as a barrier filter's, holds only while its model, state estimate, actuator authority, and deadline hold.

:::

Part III follows one proposal through six stages, four that build it, one that decides whether it may act, and one that keeps that decision on time.

* **Sensor Perception (@sec-perception):** Decides whether a measurement is current and calibrated enough to use, and produces the observation contract, which dates each measurement in a clock the permission path can convert to its own.
* **Spatial Memory (@sec-memory):** Decides how long an estimate may stand without fresh evidence, and produces the spatial belief record, which keeps the age of the evidence separate from the age of the estimate.
* **Grounded Intent (@sec-intent):** Decides how a human goal becomes a bounded, expiring request that grants no actuator authority, and produces the intent lease, with a process-plant contrast.
* **Trajectory Planning (@sec-planning):** Decides when a trajectory may commit, and produces the trajectory record, which carries its deadlines and a matched stop suffix.
* **Safety Enforcement (@sec-enforcement):** Decides on every tick whether a proposal is admitted, modified, or refused, and produces the enforcement record, where the two strands of records meet.
* **Silicon Placement (@sec-placement):** Decides where the permission path runs so that learned workloads cannot delay it, and produces the placement record with the tests that support it.

Part III hands forward the runtime evidence that Part IV must judge. Observation contracts date every measurement, belief records expire, intent leases and trajectory records name their parents and their deadlines, the enforcement record states the stopping envelope and the premises of each guarantee, and the placement record shows by test that learned workloads cannot delay the permission path. Each is a claim with conditions, and none is yet a license to operate.
