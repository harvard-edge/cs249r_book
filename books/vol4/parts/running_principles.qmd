# Running the Machine {.unnumbered}

\lettrine{A}{s} a mobile robot approaches an obstacle, a camera frame locates a person near its path. The planner proposes a turn, but the permission path must decide whether that observation is still fresh and whether the remaining clearance permits the turn or a stop. The decision has a deadline: another polished trajectory is useless if the robot has already crossed the braking margin. Running a physical AI machine means reconciling the timescales of sensing, planning, and actuation with that physical deadline. Human goals may change over seconds, trajectories over tens of milliseconds, and a particular motor controller may update every millisecond. The required rates and response times come from the plant and its measured delays, not from the names of the software tiers.

::: {#pri-vol4-multirate-authority .callout-principle title="The multi-rate authority hierarchy"}

**Invariant**: A learned policy or planner proposes motion; an independent permission path controls whether that proposal reaches the actuators. Its deadline must account for observation age, computation, communication, actuator response, and available braking or clearance margin. A 1 kHz microcontroller loop is one implementation with a 1 ms period, not a microsecond response guarantee.

**Implication**: Deliberation can run more slowly than local feedback, provided the permission path can reject stale or infeasible proposals in time. A control-barrier-function (CBF) check implemented with quadratic programming (QP) can support this design only within a validated dynamics model and state estimate, with a feasible intervention, sufficient actuator authority, and a demonstrated deadline.

:::

Part III follows the observation, proposal, permission decision, and physical response through six stages. Their interfaces implement the Sense–Plan–Act (S·P·A) decisions introduced in @sec-boundary-spa-triad and developed in @sec-appendix-spa. The center, $S \cap P \cap A$, is the permission decision that brings sensing age, proposed motion, and physical response into one budget; compute placement supports that decision across all three tasks.

* **Perception (@sec-perception, Sense):** Timestamp and calibrate camera and motion measurements so the robot can decide whether an object's location is current enough to use.
* **Memory (@sec-memory, $S \cap P$):** Track an object through occlusion, carry its uncertainty forward, and require a new observation when the motion margin no longer supports the estimate.
* **Intent (@sec-intent, $S \cap P$):** Turn a human goal into a spatial, time-limited proposal without granting the language model actuator authority.
* **Planning (@sec-planning, $P \cap A$):** Generate continuous motion and check any smooth trajectory or stopping suffix against available braking and clearance before it is admitted.
* **Enforcement (@sec-enforcement, $A \cap S$):** Use fresh state and a separately validated permission path to admit, modify, or reject proposed motion before the actuator command crosses the causal boundary.
* **Placement (@sec-placement):** Allocate compute, memory, power, and thermal resources so inference traffic cannot make the permission path miss its measured deadline.

::: {#pri-vol4-epistemic-decay .callout-principle title="Age-limited physical belief"}

**Invariant**: An unobserved dynamic object's position estimate must carry an age and uncertainty bound; uncertain motion can widen that bound. Under an illustrative constant process-noise model,
$$ \sigma^2(t) = \sigma_0^2 + Q \cdot (t - t_{\text{meas}}) $$
where $\sigma_0^2$ is position variance at measurement time $t_{\text{meas}}$, $t-t_{\text{meas}}$ is the nonnegative observation age ($t\geq t_{\text{meas}}$), and $Q \geq 0$ is the assumed position-variance growth per unit time. This model does not prescribe exponential confidence decay or apply unchanged to every object and sensor.

**Implication**: Revalidate or expire a belief when its position-uncertainty bound exceeds the task's clearance or contact margin. Time-limited intent proposals similarly require renewal before use. On expiry, the permission path rejects the stale proposal and selects a validated fallback; a safe stop is available only if the current speed, clearance, braking capability, and response delay still permit one.

:::

::: {#pri-vol4-substrate-isolation .callout-principle title="Heterogeneous substrate isolation"}

**Invariant**: Shared compute, memory, power, or thermal resources must not delay a safety-critical permission decision beyond its validated deadline. Bursty neural inference can interfere with a real-time path on a shared system-on-chip, so the interference must be measured under the intended workload.

**Implication**: Choose scheduling, memory quality-of-service controls, cache reservation, separate rails, bare-metal execution, or a separate controller according to measured worst-case timing and power budgets. Verify that the chosen isolation preserves the permission deadline during inference bursts and faults.

:::

The six stages keep learned proposals connected to measurements and constrained by the machine's available physical margin.
