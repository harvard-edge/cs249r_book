concept_map:
  source: nn_computation.qmd
  generated_date: 2026-05-21
  primary_concepts:
    - Logic-to-arithmetic paradigm shift
    - Hierarchical representation learning
    - Artificial neuron and layer computation
    - Matrix-dominant forward propagation
    - Loss-driven backpropagation
    - Training vs. inference resource asymmetry
    - End-to-end neural inference pipelines
  secondary_concepts:
    - Rule-based systems vs. handcrafted features vs. learned representations
    - Depth vs. width trade-offs
    - Dense, sparse, and skip connectivity patterns
    - Activation-function hardware implications
    - Parameter count and memory footprint
    - Gradient instability and activation saturation
    - Batch size as a hardware and statistical lever
    - Confidence-threshold routing in production systems
    - Tensor memory layout and data movement overhead
    - Numerical stability in softmax and loss computation
    - Data-Algorithm-Machine alignment
  technical_terms:
    - Multiply-accumulate (MAC)
    - Weight matrix
    - Bias vector
    - Activation function
    - Sigmoid
    - Tanh
    - Rectified Linear Unit (ReLU)
    - Logit
    - Softmax
    - Cross-entropy loss
    - Gradient descent
    - Learning rate
    - Autograd computation graph
    - Tensor memory layout
    - Log-sum-exp trick
    - Arithmetic intensity
    - Memory wall
    - Optimizer state
    - Quantization
  methodologies:
    - MNIST workload sizing
    - Mini-batch stochastic gradient descent
    - Backpropagation with stored activations
    - Napkin-math GPU feasibility checks
    - Training/inference memory budgeting
    - Full-pipeline latency profiling
    - Numerically stable softmax and cross-entropy computation
    - Confidence-based human fallback
    - USPS-style preprocess-infer-postprocess deployment
  formulas:
    - 'Neuron weighted sum: z = sum_i(x_i * w_ij) + b'
    - 'Layer linear transform: Z^(l) = A^(l-1) W^(l) + b^(l)'
    - 'Layer activation: A^(l) = f(Z^(l))'
    - 'Softmax: softmax(z_i) = exp(z_i) / sum_j exp(z_j)'
    - 'Cross-entropy: L(y_hat, y) = -sum_j y_j log(y_hat_j)'
    - 'Batch loss: L_batch = (1 / B) * sum_i L(y_hat_i, y_i)'
    - 'Weight gradient: dL / dW^(l) = A^(l-1)^T * dL / dZ^(l)'
    - 'Chain rule gradient: dL / dA^(l) = dL / dA^(l+1) * dA^(l+1) / dA^(l)'
    - 'Gradient descent: theta_new = theta_old - eta * grad_theta L'
    - 'Batch gradient: grad_theta L_batch = (1 / B) * sum_i grad_theta L_i'
    - 'Stable softmax: softmax(z_i) = exp(z_i - max(z)) / sum_j exp(z_j - max(z))'
    - 'Training memory: weights + gradients + optimizer states + activations'
    - 'Fully connected layer FLOPs: 2 * input_dim * output_dim * batch_size'
    - z = \sum (x_i \cdot w_{ij})
    - z = \sum (x_i \cdot w_{ij}) + b
    - \sigma(x) = \frac{1}{1 + e^{-x}}
    - \tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}
    - \text{ReLU}(x) = \max(0, x)
  applications:
    - MNIST handwritten digit classification
    - USPS mail sorting recognition pipeline
    - Batch inference throughput analysis
    - Training and inference memory budgeting
    - Neural computation mapping to matrix operations
    - Production inference latency budgeting
    - Confidence-thresholded human fallback in mail sorting
keywords:
  - Neural Computation
  - Logic-to-arithmetic paradigm shift
  - Hierarchical representation learning
  - Artificial neuron and layer computation
  - Matrix-dominant forward propagation
  - Loss-driven backpropagation
  - Training vs. inference resource asymmetry
  - End-to-end neural inference pipelines
  - Depth vs. width trade-offs
  - Dense, sparse, and skip connectivity patterns
  - Activation-function hardware implications
  - Parameter count and memory footprint
  - Gradient instability and activation saturation
  - Batch size as a hardware and statistical lever
  - Confidence-threshold routing in production systems
  - Tensor memory layout and data movement overhead
  - Numerical stability in softmax and loss computation
  - Data-Algorithm-Machine alignment
  - Multiply-accumulate (MAC)
  - MAC
  - Weight matrix
  - Bias vector
  - Activation function
  - Sigmoid
  - Tanh
  - Rectified Linear Unit (ReLU)
  - ReLU
  - Logit
  - Softmax
  - Cross-entropy loss
  - Gradient descent
  - Learning rate
  - Autograd computation graph
  - Tensor memory layout
  - Log-sum-exp trick
  - Arithmetic intensity
  - Memory wall
  - Optimizer state
  - Quantization
topics_covered:
  - topic: From Logic to Arithmetic
    subtopics:
      - Overview and motivation
  - topic: Computing with Patterns
    subtopics:
      - From explicit logic to learned patterns
      - The feature engineering bottleneck
      - Automatic pattern discovery
      - Computational infrastructure requirements
      - The artificial neuron as a computing primitive
      - Hardware and software requirements
      - Evolution of neural network computing
  - topic: Neural Network Fundamentals
    subtopics:
      - 'Why depth matters: The power of hierarchical representations'
      - Network architecture fundamentals
      - 'The transistor tax: Logic unit cost'
      - Parameters and connections
      - Architecture design
  - topic: Learning Process
    subtopics:
      - Supervised learning from labeled examples
      - Forward pass computation
      - Loss functions
      - Gradient computation and backpropagation
      - Weight update and optimization
  - topic: Inference Pipeline
    subtopics:
      - Production deployment and prediction pipeline
      - Output interpretation and decision making
  - topic: USPS Digit Recognition
    subtopics:
      - The mail sorting challenge
      - Engineering process and design decisions
      - Production system architecture
      - Performance outcomes and operational impact
      - Key engineering lessons and design principles
  - topic: D·A·M Taxonomy
    subtopics:
      - Overview and motivation
  - topic: Fallacies and Pitfalls
    subtopics:
      - Overview and motivation
  - topic: Summary
    subtopics:
      - Overview and motivation
